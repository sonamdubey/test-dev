/******/ (function(modules) { // webpackBootstrap
/******/ 	// The module cache
/******/ 	var installedModules = {};
/******/
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/
/******/ 		// Check if module is in cache
/******/ 		if(installedModules[moduleId]) {
/******/ 			return installedModules[moduleId].exports;
/******/ 		}
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = installedModules[moduleId] = {
/******/ 			i: moduleId,
/******/ 			l: false,
/******/ 			exports: {}
/******/ 		};
/******/
/******/ 		// Execute the module function
/******/ 		modules[moduleId].call(module.exports, module, module.exports, __webpack_require__);
/******/
/******/ 		// Flag the module as loaded
/******/ 		module.l = true;
/******/
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/
/******/
/******/ 	// expose the modules object (__webpack_modules__)
/******/ 	__webpack_require__.m = modules;
/******/
/******/ 	// expose the module cache
/******/ 	__webpack_require__.c = installedModules;
/******/
/******/ 	// define getter function for harmony exports
/******/ 	__webpack_require__.d = function(exports, name, getter) {
/******/ 		if(!__webpack_require__.o(exports, name)) {
/******/ 			Object.defineProperty(exports, name, {
/******/ 				configurable: false,
/******/ 				enumerable: true,
/******/ 				get: getter
/******/ 			});
/******/ 		}
/******/ 	};
/******/
/******/ 	// getDefaultExport function for compatibility with non-harmony modules
/******/ 	__webpack_require__.n = function(module) {
/******/ 		var getter = module && module.__esModule ?
/******/ 			function getDefault() { return module['default']; } :
/******/ 			function getModuleExports() { return module; };
/******/ 		__webpack_require__.d(getter, 'a', getter);
/******/ 		return getter;
/******/ 	};
/******/
/******/ 	// Object.prototype.hasOwnProperty.call
/******/ 	__webpack_require__.o = function(object, property) { return Object.prototype.hasOwnProperty.call(object, property); };
/******/
/******/ 	// __webpack_public_path__
/******/ 	__webpack_require__.p = "";
/******/
/******/ 	// Load entry module and return exports
/******/ 	return __webpack_require__(__webpack_require__.s = 88);
/******/ })
/************************************************************************/
/******/ ([
/* 0 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
Object.defineProperty(__webpack_exports__, "__esModule", { value: true });
/* WEBPACK VAR INJECTION */(function(process) {/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "version", function() { return version; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "DOM", function() { return DOM; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Children", function() { return Children; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "render", function() { return render$1; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "createClass", function() { return createClass; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "createFactory", function() { return createFactory; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "createElement", function() { return createElement; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "cloneElement", function() { return cloneElement$1; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "isValidElement", function() { return isValidElement; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "findDOMNode", function() { return findDOMNode; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "unmountComponentAtNode", function() { return unmountComponentAtNode; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Component", function() { return Component$1; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "PureComponent", function() { return PureComponent; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "unstable_renderSubtreeIntoContainer", function() { return renderSubtreeIntoContainer; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "__spread", function() { return extend; });
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_prop_types__ = __webpack_require__(1);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_prop_types___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_0_prop_types__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_preact__ = __webpack_require__(94);
/* harmony reexport (default from non-hamory) */ __webpack_require__.d(__webpack_exports__, "PropTypes", function() { return __WEBPACK_IMPORTED_MODULE_0_prop_types___default.a; });



var version = '15.1.0'; // trick libraries to think we are react

var ELEMENTS = 'a abbr address area article aside audio b base bdi bdo big blockquote body br button canvas caption cite code col colgroup data datalist dd del details dfn dialog div dl dt em embed fieldset figcaption figure footer form h1 h2 h3 h4 h5 h6 head header hgroup hr html i iframe img input ins kbd keygen label legend li link main map mark menu menuitem meta meter nav noscript object ol optgroup option output p param picture pre progress q rp rt ruby s samp script section select small source span strong style sub summary sup table tbody td textarea tfoot th thead time title tr track u ul var video wbr circle clipPath defs ellipse g image line linearGradient mask path pattern polygon polyline radialGradient rect stop svg text tspan'.split(' ');

var REACT_ELEMENT_TYPE = (typeof Symbol!=='undefined' && Symbol.for && Symbol.for('react.element')) || 0xeac7;

var COMPONENT_WRAPPER_KEY = (typeof Symbol!=='undefined' && Symbol.for) ? Symbol.for('__preactCompatWrapper') : '__preactCompatWrapper';

// don't autobind these methods since they already have guaranteed context.
var AUTOBIND_BLACKLIST = {
	constructor: 1,
	render: 1,
	shouldComponentUpdate: 1,
	componentWillReceiveProps: 1,
	componentWillUpdate: 1,
	componentDidUpdate: 1,
	componentWillMount: 1,
	componentDidMount: 1,
	componentWillUnmount: 1,
	componentDidUnmount: 1
};


var CAMEL_PROPS = /^(?:accent|alignment|arabic|baseline|cap|clip|color|fill|flood|font|glyph|horiz|marker|overline|paint|stop|strikethrough|stroke|text|underline|unicode|units|v|vector|vert|word|writing|x)[A-Z]/;


var BYPASS_HOOK = {};

/*global process*/
var DEV = typeof process==='undefined' || !Object({"NODE_ENV":"development","SERVER":true}) || "development"!=='production';

// a component that renders nothing. Used to replace components for unmountComponentAtNode.
function EmptyComponent() { return null; }



// make react think we're react.
var VNode = Object(__WEBPACK_IMPORTED_MODULE_1_preact__["c" /* h */])('a', null).constructor;
VNode.prototype.$$typeof = REACT_ELEMENT_TYPE;
VNode.prototype.preactCompatUpgraded = false;
VNode.prototype.preactCompatNormalized = false;

Object.defineProperty(VNode.prototype, 'type', {
	get: function() { return this.nodeName; },
	set: function(v) { this.nodeName = v; },
	configurable:true
});

Object.defineProperty(VNode.prototype, 'props', {
	get: function() { return this.attributes; },
	set: function(v) { this.attributes = v; },
	configurable:true
});



var oldEventHook = __WEBPACK_IMPORTED_MODULE_1_preact__["d" /* options */].event;
__WEBPACK_IMPORTED_MODULE_1_preact__["d" /* options */].event = function (e) {
	if (oldEventHook) { e = oldEventHook(e); }
	e.persist = Object;
	e.nativeEvent = e;
	return e;
};


var oldVnodeHook = __WEBPACK_IMPORTED_MODULE_1_preact__["d" /* options */].vnode;
__WEBPACK_IMPORTED_MODULE_1_preact__["d" /* options */].vnode = function (vnode) {
	if (!vnode.preactCompatUpgraded) {
		vnode.preactCompatUpgraded = true;

		var tag = vnode.nodeName,
			attrs = vnode.attributes = extend({}, vnode.attributes);

		if (typeof tag==='function') {
			if (tag[COMPONENT_WRAPPER_KEY]===true || (tag.prototype && 'isReactComponent' in tag.prototype)) {
				if (vnode.children && String(vnode.children)==='') { vnode.children = undefined; }
				if (vnode.children) { attrs.children = vnode.children; }

				if (!vnode.preactCompatNormalized) {
					normalizeVNode(vnode);
				}
				handleComponentVNode(vnode);
			}
		}
		else {
			if (vnode.children && String(vnode.children)==='') { vnode.children = undefined; }
			if (vnode.children) { attrs.children = vnode.children; }

			if (attrs.defaultValue) {
				if (!attrs.value && attrs.value!==0) {
					attrs.value = attrs.defaultValue;
				}
				delete attrs.defaultValue;
			}

			handleElementVNode(vnode, attrs);
		}
	}

	if (oldVnodeHook) { oldVnodeHook(vnode); }
};

function handleComponentVNode(vnode) {
	var tag = vnode.nodeName,
		a = vnode.attributes;

	vnode.attributes = {};
	if (tag.defaultProps) { extend(vnode.attributes, tag.defaultProps); }
	if (a) { extend(vnode.attributes, a); }
}

function handleElementVNode(vnode, a) {
	var shouldSanitize, attrs, i;
	if (a) {
		for (i in a) { if ((shouldSanitize = CAMEL_PROPS.test(i))) { break; } }
		if (shouldSanitize) {
			attrs = vnode.attributes = {};
			for (i in a) {
				if (a.hasOwnProperty(i)) {
					attrs[ CAMEL_PROPS.test(i) ? i.replace(/([A-Z0-9])/, '-$1').toLowerCase() : i ] = a[i];
				}
			}
		}
	}
}



// proxy render() since React returns a Component reference.
function render$1(vnode, parent, callback) {
	var prev = parent && parent._preactCompatRendered && parent._preactCompatRendered.base;

	// ignore impossible previous renders
	if (prev && prev.parentNode!==parent) { prev = null; }

	// default to first Element child
	if (!prev && parent) { prev = parent.firstElementChild; }

	// remove unaffected siblings
	for (var i=parent.childNodes.length; i--; ) {
		if (parent.childNodes[i]!==prev) {
			parent.removeChild(parent.childNodes[i]);
		}
	}

	var out = Object(__WEBPACK_IMPORTED_MODULE_1_preact__["e" /* render */])(vnode, parent, prev);
	if (parent) { parent._preactCompatRendered = out && (out._component || { base: out }); }
	if (typeof callback==='function') { callback(); }
	return out && out._component || out;
}


var ContextProvider = function () {};

ContextProvider.prototype.getChildContext = function () {
	return this.props.context;
};
ContextProvider.prototype.render = function (props) {
	return props.children[0];
};

function renderSubtreeIntoContainer(parentComponent, vnode, container, callback) {
	var wrap = Object(__WEBPACK_IMPORTED_MODULE_1_preact__["c" /* h */])(ContextProvider, { context: parentComponent.context }, vnode);
	var renderContainer = render$1(wrap, container);
	var component = renderContainer._component || renderContainer.base;
	if (callback) { callback.call(component, renderContainer); }
	return component;
}


function unmountComponentAtNode(container) {
	var existing = container._preactCompatRendered && container._preactCompatRendered.base;
	if (existing && existing.parentNode===container) {
		Object(__WEBPACK_IMPORTED_MODULE_1_preact__["e" /* render */])(Object(__WEBPACK_IMPORTED_MODULE_1_preact__["c" /* h */])(EmptyComponent), container, existing);
		return true;
	}
	return false;
}



var ARR = [];

// This API is completely unnecessary for Preact, so it's basically passthrough.
var Children = {
	map: function(children, fn, ctx) {
		if (children == null) { return null; }
		children = Children.toArray(children);
		if (ctx && ctx!==children) { fn = fn.bind(ctx); }
		return children.map(fn);
	},
	forEach: function(children, fn, ctx) {
		if (children == null) { return null; }
		children = Children.toArray(children);
		if (ctx && ctx!==children) { fn = fn.bind(ctx); }
		children.forEach(fn);
	},
	count: function(children) {
		return children && children.length || 0;
	},
	only: function(children) {
		children = Children.toArray(children);
		if (children.length!==1) { throw new Error('Children.only() expects only one child.'); }
		return children[0];
	},
	toArray: function(children) {
		if (children == null) { return []; }
		return ARR.concat(children);
	}
};


/** Track current render() component for ref assignment */
var currentComponent;


function createFactory(type) {
	return createElement.bind(null, type);
}


var DOM = {};
for (var i=ELEMENTS.length; i--; ) {
	DOM[ELEMENTS[i]] = createFactory(ELEMENTS[i]);
}

function upgradeToVNodes(arr, offset) {
	for (var i=offset || 0; i<arr.length; i++) {
		var obj = arr[i];
		if (Array.isArray(obj)) {
			upgradeToVNodes(obj);
		}
		else if (obj && typeof obj==='object' && !isValidElement(obj) && ((obj.props && obj.type) || (obj.attributes && obj.nodeName) || obj.children)) {
			arr[i] = createElement(obj.type || obj.nodeName, obj.props || obj.attributes, obj.children);
		}
	}
}

function isStatelessComponent(c) {
	return typeof c==='function' && !(c.prototype && c.prototype.render);
}


// wraps stateless functional components in a PropTypes validator
function wrapStatelessComponent(WrappedComponent) {
	return createClass({
		displayName: WrappedComponent.displayName || WrappedComponent.name,
		render: function() {
			return WrappedComponent(this.props, this.context);
		}
	});
}


function statelessComponentHook(Ctor) {
	var Wrapped = Ctor[COMPONENT_WRAPPER_KEY];
	if (Wrapped) { return Wrapped===true ? Ctor : Wrapped; }

	Wrapped = wrapStatelessComponent(Ctor);

	Object.defineProperty(Wrapped, COMPONENT_WRAPPER_KEY, { configurable:true, value:true });
	Wrapped.displayName = Ctor.displayName;
	Wrapped.propTypes = Ctor.propTypes;
	Wrapped.defaultProps = Ctor.defaultProps;

	Object.defineProperty(Ctor, COMPONENT_WRAPPER_KEY, { configurable:true, value:Wrapped });

	return Wrapped;
}


function createElement() {
	var args = [], len = arguments.length;
	while ( len-- ) args[ len ] = arguments[ len ];

	upgradeToVNodes(args, 2);
	return normalizeVNode(__WEBPACK_IMPORTED_MODULE_1_preact__["c" /* h */].apply(void 0, args));
}


function normalizeVNode(vnode) {
	vnode.preactCompatNormalized = true;

	applyClassName(vnode);

	if (isStatelessComponent(vnode.nodeName)) {
		vnode.nodeName = statelessComponentHook(vnode.nodeName);
	}

	var ref = vnode.attributes.ref,
		type = ref && typeof ref;
	if (currentComponent && (type==='string' || type==='number')) {
		vnode.attributes.ref = createStringRefProxy(ref, currentComponent);
	}

	applyEventNormalization(vnode);

	return vnode;
}


function cloneElement$1(element, props) {
	var children = [], len = arguments.length - 2;
	while ( len-- > 0 ) children[ len ] = arguments[ len + 2 ];

	if (!isValidElement(element)) { return element; }
	var elementProps = element.attributes || element.props;
	var node = Object(__WEBPACK_IMPORTED_MODULE_1_preact__["c" /* h */])(
		element.nodeName || element.type,
		extend({}, elementProps),
		element.children || elementProps && elementProps.children
	);
	// Only provide the 3rd argument if needed.
	// Arguments 3+ overwrite element.children in preactCloneElement
	var cloneArgs = [node, props];
	if (children && children.length) {
		cloneArgs.push(children);
	}
	else if (props && props.children) {
		cloneArgs.push(props.children);
	}
	return normalizeVNode(__WEBPACK_IMPORTED_MODULE_1_preact__["b" /* cloneElement */].apply(void 0, cloneArgs));
}


function isValidElement(element) {
	return element && ((element instanceof VNode) || element.$$typeof===REACT_ELEMENT_TYPE);
}


function createStringRefProxy(name, component) {
	return component._refProxies[name] || (component._refProxies[name] = function (resolved) {
		if (component && component.refs) {
			component.refs[name] = resolved;
			if (resolved===null) {
				delete component._refProxies[name];
				component = null;
			}
		}
	});
}


function applyEventNormalization(ref) {
	var nodeName = ref.nodeName;
	var attributes = ref.attributes;

	if (!attributes || typeof nodeName!=='string') { return; }
	var props = {};
	for (var i in attributes) {
		props[i.toLowerCase()] = i;
	}
	if (props.ondoubleclick) {
		attributes.ondblclick = attributes[props.ondoubleclick];
		delete attributes[props.ondoubleclick];
	}
	// for *textual inputs* (incl textarea), normalize `onChange` -> `onInput`:
	if (props.onchange && (nodeName==='textarea' || (nodeName.toLowerCase()==='input' && !/^fil|che|rad/i.test(attributes.type)))) {
		var normalized = props.oninput || 'oninput';
		if (!attributes[normalized]) {
			attributes[normalized] = multihook([attributes[normalized], attributes[props.onchange]]);
			delete attributes[props.onchange];
		}
	}
}


function applyClassName(vnode) {
	var a = vnode.attributes || (vnode.attributes = {});
	classNameDescriptor.enumerable = 'className' in a;
	if (a.className) { a.class = a.className; }
	Object.defineProperty(a, 'className', classNameDescriptor);
}


var classNameDescriptor = {
	configurable: true,
	get: function() { return this.class; },
	set: function(v) { this.class = v; }
};

function extend(base, props) {
	var arguments$1 = arguments;

	for (var i=1, obj = (void 0); i<arguments.length; i++) {
		if ((obj = arguments$1[i])) {
			for (var key in obj) {
				if (obj.hasOwnProperty(key)) {
					base[key] = obj[key];
				}
			}
		}
	}
	return base;
}


function shallowDiffers(a, b) {
	for (var i in a) { if (!(i in b)) { return true; } }
	for (var i$1 in b) { if (a[i$1]!==b[i$1]) { return true; } }
	return false;
}


function findDOMNode(component) {
	return component && component.base || component;
}


function F(){}

function createClass(obj) {
	function cl(props, context) {
		bindAll(this);
		Component$1.call(this, props, context, BYPASS_HOOK);
		newComponentHook.call(this, props, context);
	}

	obj = extend({ constructor: cl }, obj);

	// We need to apply mixins here so that getDefaultProps is correctly mixed
	if (obj.mixins) {
		applyMixins(obj, collateMixins(obj.mixins));
	}
	if (obj.statics) {
		extend(cl, obj.statics);
	}
	if (obj.propTypes) {
		cl.propTypes = obj.propTypes;
	}
	if (obj.defaultProps) {
		cl.defaultProps = obj.defaultProps;
	}
	if (obj.getDefaultProps) {
		cl.defaultProps = obj.getDefaultProps.call(cl);
	}

	F.prototype = Component$1.prototype;
	cl.prototype = extend(new F(), obj);

	cl.displayName = obj.displayName || 'Component';

	return cl;
}


// Flatten an Array of mixins to a map of method name to mixin implementations
function collateMixins(mixins) {
	var keyed = {};
	for (var i=0; i<mixins.length; i++) {
		var mixin = mixins[i];
		for (var key in mixin) {
			if (mixin.hasOwnProperty(key) && typeof mixin[key]==='function') {
				(keyed[key] || (keyed[key]=[])).push(mixin[key]);
			}
		}
	}
	return keyed;
}


// apply a mapping of Arrays of mixin methods to a component prototype
function applyMixins(proto, mixins) {
	for (var key in mixins) { if (mixins.hasOwnProperty(key)) {
		proto[key] = multihook(
			mixins[key].concat(proto[key] || ARR),
			key==='getDefaultProps' || key==='getInitialState' || key==='getChildContext'
		);
	} }
}


function bindAll(ctx) {
	for (var i in ctx) {
		var v = ctx[i];
		if (typeof v==='function' && !v.__bound && !AUTOBIND_BLACKLIST.hasOwnProperty(i)) {
			(ctx[i] = v.bind(ctx)).__bound = true;
		}
	}
}


function callMethod(ctx, m, args) {
	if (typeof m==='string') {
		m = ctx.constructor.prototype[m];
	}
	if (typeof m==='function') {
		return m.apply(ctx, args);
	}
}

function multihook(hooks, skipDuplicates) {
	return function() {
		var arguments$1 = arguments;
		var this$1 = this;

		var ret;
		for (var i=0; i<hooks.length; i++) {
			var r = callMethod(this$1, hooks[i], arguments$1);

			if (skipDuplicates && r!=null) {
				if (!ret) { ret = {}; }
				for (var key in r) { if (r.hasOwnProperty(key)) {
					ret[key] = r[key];
				} }
			}
			else if (typeof r!=='undefined') { ret = r; }
		}
		return ret;
	};
}


function newComponentHook(props, context) {
	propsHook.call(this, props, context);
	this.componentWillReceiveProps = multihook([propsHook, this.componentWillReceiveProps || 'componentWillReceiveProps']);
	this.render = multihook([propsHook, beforeRender, this.render || 'render', afterRender]);
}


function propsHook(props, context) {
	if (!props) { return; }

	// React annoyingly special-cases single children, and some react components are ridiculously strict about this.
	var c = props.children;
	if (c && Array.isArray(c) && c.length===1 && (typeof c[0]==='string' || typeof c[0]==='function' || c[0] instanceof VNode)) {
		props.children = c[0];

		// but its totally still going to be an Array.
		if (props.children && typeof props.children==='object') {
			props.children.length = 1;
			props.children[0] = props.children;
		}
	}

	// add proptype checking
	if (DEV) {
		var ctor = typeof this==='function' ? this : this.constructor,
			propTypes = this.propTypes || ctor.propTypes;
		var displayName = this.displayName || ctor.name;

		if (propTypes) {
			__WEBPACK_IMPORTED_MODULE_0_prop_types___default.a.checkPropTypes(propTypes, props, 'prop', displayName);
		}
	}
}


function beforeRender(props) {
	currentComponent = this;
}

function afterRender() {
	if (currentComponent===this) {
		currentComponent = null;
	}
}



function Component$1(props, context, opts) {
	__WEBPACK_IMPORTED_MODULE_1_preact__["a" /* Component */].call(this, props, context);
	this.state = this.getInitialState ? this.getInitialState() : {};
	this.refs = {};
	this._refProxies = {};
	if (opts!==BYPASS_HOOK) {
		newComponentHook.call(this, props, context);
	}
}
extend(Component$1.prototype = new __WEBPACK_IMPORTED_MODULE_1_preact__["a" /* Component */](), {
	constructor: Component$1,

	isReactComponent: {},

	replaceState: function(state, callback) {
		var this$1 = this;

		this.setState(state, callback);
		for (var i in this$1.state) {
			if (!(i in state)) {
				delete this$1.state[i];
			}
		}
	},

	getDOMNode: function() {
		return this.base;
	},

	isMounted: function() {
		return !!this.base;
	}
});



function PureComponent(props, context) {
	Component$1.call(this, props, context);
}
F.prototype = Component$1.prototype;
PureComponent.prototype = new F();
PureComponent.prototype.isPureReactComponent = true;
PureComponent.prototype.shouldComponentUpdate = function(props, state) {
	return shallowDiffers(this.props, props) || shallowDiffers(this.state, state);
};

var index = {
	version: version,
	DOM: DOM,
	PropTypes: __WEBPACK_IMPORTED_MODULE_0_prop_types___default.a,
	Children: Children,
	render: render$1,
	createClass: createClass,
	createFactory: createFactory,
	createElement: createElement,
	cloneElement: cloneElement$1,
	isValidElement: isValidElement,
	findDOMNode: findDOMNode,
	unmountComponentAtNode: unmountComponentAtNode,
	Component: Component$1,
	PureComponent: PureComponent,
	unstable_renderSubtreeIntoContainer: renderSubtreeIntoContainer,
	__spread: extend
};

/* harmony default export */ __webpack_exports__["default"] = (index);
//# sourceMappingURL=preact-compat.es.js.map

/* WEBPACK VAR INJECTION */}.call(__webpack_exports__, __webpack_require__(91)))

/***/ }),
/* 1 */
/***/ (function(module, exports, __webpack_require__) {

/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */

if (true) {
  var REACT_ELEMENT_TYPE = (typeof Symbol === 'function' &&
    Symbol.for &&
    Symbol.for('react.element')) ||
    0xeac7;

  var isValidElement = function(object) {
    return typeof object === 'object' &&
      object !== null &&
      object.$$typeof === REACT_ELEMENT_TYPE;
  };

  // By explicitly using `prop-types` you are opting into new development behavior.
  // http://fb.me/prop-types-in-prod
  var throwOnDirectAccess = true;
  module.exports = __webpack_require__(92)(isValidElement, throwOnDirectAccess);
} else {
  // By explicitly using `prop-types` you are opting into new production behavior.
  // http://fb.me/prop-types-in-prod
  module.exports = require('./factoryWithThrowingShims')();
}


/***/ }),
/* 2 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
Object.defineProperty(__webpack_exports__, "__esModule", { value: true });
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0__BrowserRouter__ = __webpack_require__(95);
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "BrowserRouter", function() { return __WEBPACK_IMPORTED_MODULE_0__BrowserRouter__["a"]; });
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1__HashRouter__ = __webpack_require__(101);
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "HashRouter", function() { return __WEBPACK_IMPORTED_MODULE_1__HashRouter__["a"]; });
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2__Link__ = __webpack_require__(50);
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "Link", function() { return __WEBPACK_IMPORTED_MODULE_2__Link__["a"]; });
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_3__MemoryRouter__ = __webpack_require__(102);
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "MemoryRouter", function() { return __WEBPACK_IMPORTED_MODULE_3__MemoryRouter__["a"]; });
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_4__NavLink__ = __webpack_require__(104);
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "NavLink", function() { return __WEBPACK_IMPORTED_MODULE_4__NavLink__["a"]; });
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_5__Prompt__ = __webpack_require__(106);
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "Prompt", function() { return __WEBPACK_IMPORTED_MODULE_5__Prompt__["a"]; });
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_6__Redirect__ = __webpack_require__(108);
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "Redirect", function() { return __WEBPACK_IMPORTED_MODULE_6__Redirect__["a"]; });
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_7__Route__ = __webpack_require__(51);
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "Route", function() { return __WEBPACK_IMPORTED_MODULE_7__Route__["a"]; });
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_8__Router__ = __webpack_require__(33);
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "Router", function() { return __WEBPACK_IMPORTED_MODULE_8__Router__["a"]; });
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_9__StaticRouter__ = __webpack_require__(110);
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "StaticRouter", function() { return __WEBPACK_IMPORTED_MODULE_9__StaticRouter__["a"]; });
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_10__Switch__ = __webpack_require__(112);
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "Switch", function() { return __WEBPACK_IMPORTED_MODULE_10__Switch__["a"]; });
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_11__generatePath__ = __webpack_require__(114);
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "generatePath", function() { return __WEBPACK_IMPORTED_MODULE_11__generatePath__["a"]; });
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_12__matchPath__ = __webpack_require__(115);
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "matchPath", function() { return __WEBPACK_IMPORTED_MODULE_12__matchPath__["a"]; });
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_13__withRouter__ = __webpack_require__(116);
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "withRouter", function() { return __WEBPACK_IMPORTED_MODULE_13__withRouter__["a"]; });





























/***/ }),
/* 3 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var _immutable = __webpack_require__(17);

function isServer() {
  return !(typeof window !== 'undefined' && window.document); //TODO 
}

function detectBrowser() {
  window.isBrowserUC = navigator.userAgent.toLowerCase().indexOf("ucbrowser") !== -1;
  window.isBrowserSafari = navigator.userAgent.toLowerCase().indexOf("safari") !== -1;
  window.isBrowserChrome = navigator.userAgent.toLowerCase().indexOf("chrome") !== -1;
}

function isInt(value) {

  if (isNaN(value)) {
    return false;
  }
  var x = parseFloat(value);
  return (x | 0) === x;
}
var CMSUserReviewSlugSearchKey = 'showeditcmsreviewslug';
var CMSUserReviewSlugPosition = 5;
function isCMSUserReviewSlugClosed() {
  var value = bwcache.get(CMSUserReviewSlugSearchKey, true);
  if (value) {
    return true;
  } else {
    return false;
  }
}

var updateData = function updateData(state, updateDict) {
  try {
    return state.withMutations(function (state) {
      for (var key in updateDict) {
        state = state.update(key, function (prevVal) {
          return (0, _immutable.fromJS)(updateDict[key]);
        });
      }
      return state;
    });
    // return state.withMutations(state => state.update(statusVarName , prevVal => fromJS(status))
    // .update(dataVarName , prevVal => fromJS(data))); 
  } catch (err) {
    console.log(err);
    return state;
  }
};

function RemoveSpecialCharacters(inpString) {
  var regex = /[\)'",=!+#\[*\]~;^<\(>]+/g;
  try {
    if (inpString != undefined) {
      return inpString.replace(regex, '');
    }
  } catch (err) {
    console.log(err);
  }
  return inpString;
}

module.exports = {
  isServer: isServer,
  isInt: isInt,
  isCMSUserReviewSlugClosed: isCMSUserReviewSlugClosed,
  CMSUserReviewSlugPosition: CMSUserReviewSlugPosition,
  updateData: updateData,
  RemoveSpecialCharacters: RemoveSpecialCharacters,
  detectBrowser: detectBrowser
};

/***/ }),
/* 4 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


module.exports = {
		NewsArticlesPerPage: 10,
		MOBILE_URL_PREFIX: '/m',
		NEWS_URL_PREFIX: '/news',
		VIDEOS_URL_PREFIX: '/bike-videos',
		HOST_URL: 'https://imgd.aeplcdn.com/',
		Status: {
				'Reset': 1,
				'IsFetching': 2,
				'Fetched': 3,
				'Error': 4
		},

		AD_PATH_NEWS_MOBILE_TOP_320_50: '/1017752/Bikewale_Mobile_EditorialList_Top_320x50',
		AD_PATH_NEWS_MOBILE_BOTTOM_320_50: '/1017752/Bikewale_Mobile_EditorialList_Bottom_320x50',
		AD_PATH_NEWS_MOBILE_MIDDLE_300_250: '/1017752/Bikewale_Mobile_EditorialList_BTF_300x250',
		AD_DIV_REVIEWS_TOP_320_50: 'div-gpt-ad-1395986297721-0',
		AD_DIV_REVIEWS_BOTTOM_320_50: 'div-gpt-ad-1395986297721-1',
		AD_DIV_REVIEWS_MIDDLE_300_250: 'div-gpt-ad-1395986297721-5',
		AD_DIMENSION_320_50: [320, 50],
		GA_PAGE_MAPPING: {
				"ListPage": {
						"id": "37",
						"name": "Editorial_List_Page"
				},
				"DetailsPage": {
						"id": "38",
						"name": "Editorial_Details_Page"
				},
				"VideosPage": {
						"id": "33",
						"name": "Videos_Landing_Page"
				},
				"VideoDetailsPage": {
						"id": "36",
						"name": "Videos_Details_Page"
				},
				"VideoCategoryPage": {
						"id": "45",
						"name": "Videos_Category_Page"
				},
				"FinancePage": {
						"id": "46",
						"name": "Finance_Page"
				}
		},
		AD_DIMENSION_300_250: [300, 250],
		PQ_SOURCE_ID: {
				"Desktop_HP_PQ_Widget": 1,
				"Desktop_New_MostPopular": 2,
				"Desktop_New_NewLaunches": 3,
				"Desktop_New_PQ_Widget": 4,
				"Desktop_MakePage": 5,
				"Desktop_ModelPage": 6,
				"Desktop_ModelPage_Alternative": 7,
				"Desktop_CompareBike": 8,
				"Desktop_PQ_Landing": 9,
				"Desktop_PQ_Alternative": 10,
				"Desktop_DPQ_Alternative": 11,
				"Desktop_LocateDealer_NewLaunches": 12,
				"Desktop_Upcoming_NewLaunches": 13,
				"Desktop_NewLaunchLanding": 14,
				"Desktop_UserReview_ModelPage": 15,
				"Desktop_NewBikeSearch": 16,
				"Mobile_HP_PQ_Widget": 17,
				"Mobile_New_MostPopular": 18,
				"Mobile_New_NewLaunches": 19,
				"Mobile_New_PQ_Widget": 20,
				"Mobile_MakePage": 21,
				"Mobile_ModelPage": 22,
				"Mobile_ModelPage_Alternative": 23,
				"Mobile_CompareBike": 24,
				"Mobile_PQ_Landing": 25,
				"Mobile_PQ_Alternative": 26,
				"Mobile_DPQ_Alternative": 27,
				"Mobile_LocateDealer_NewLaunches": 28,
				"Mobile_Upcoming_NewLaunches": 29,
				"Mobile_NewLaunchLanding": 30,
				"Mobile_UserReview_ModelPage": 31,
				"Mobile_NewBikeSearch": 32,
				"Desktop_PQ_Quotation": 33,
				"Mobile_PQ_Quotation": 34,
				"Desktop_DPQ_Quotation": 35,
				"Mobile_DPQ_Quotation": 36,
				"DeskTop_AutoSuggest": 37,
				"Mobile_AutoSuggest": 38,
				"Desktop_BookingListing": 39,
				"Mobile_BookingListing": 40,
				"Mobile_DealerLocator_Detail": 41,
				"Mobile_DealerLocator_Listing": 42,
				"Desktop_DealerLocator_SubmitButton": 43,
				"Desktop_DealerLocator_GetOfferButton": 44,
				"Desktop_ModelPage_FloatingButton": 45,
				"Desktop_PriceInCity_SelectAreas": 46,
				"Desktop_PriceInCity_Alternative": 47,
				"Desktop_PriceInCity_DealersCard_GetOfferButton": 48,
				"Desktop_SpecsAndFeaturePage_CheckOnRoadPrice": 49,
				"Desktop_SpecsAndFeaturePage_GetOffersFromDealer": 50,
				"Mobile_PriceInCity_DealersCard_GetOfferButton": 51,
				"Mobile_PriceInCity_AlternateBikes": 52,
				"Mobile_PriceInCity_SelectArea": 53,
				"Mobile_SpecsAndFeaturePage_CheckOnRoadPrice": 54,
				"Mobile_SpecsAndFeaturePage_GetOffersFromDealer": 55,
				"Desktop_SpecsAndFeaturePage_OnLoad": 56,
				"Mobile_SpecsAndFeaturePage_OnLoad": 57,
				"Desktop_MakePage_GetOffersFromDealer": 58,
				"Mobile_MakePage_GetOffersFromDealer": 59,
				"Desktop_dealer_details_Get_offers": 60,
				"Mobile_dealer_details_Get_offers": 61,
				"Desktop_HP_MostPopular": 62,
				"Desktop_HP_NewLaunches": 63,
				"Mobile_HP_MostPopular": 64,
				"Mobile_HP_NewLaunches": 65,
				"Mobile_ServiceCenter_Listing_CityPage": 66,
				"Mobile_ServiceCenter_DetailsPage": 67,
				"Desktop_ServiceCenter_DefaultPage": 68,
				"Mobile_ServiceCenter_DefaultPage": 69,
				"Desktop_DPQ_SecondaryDealers": 70,
				"Mobile_DPQ_SecondaryDealers": 71,
				"Desktop_Generic_Bikes": 72,
				"Desktop_Generic_Scooters": 73,
				"Desktop_Generic_MileageBikes": 74,
				"Desktop_Generic_SportsBikes": 75,
				"Desktop_Generic_CruiserBikes": 76,
				"Mobile_GenricBikeInfo_Widget": 77,
				"Mobile_Dealerpricequote_DealersCard_GetOfferButton": 78,
				"Desktop_Dealerpricequote_DealersCard_GetOfferButton": 79,
				"Desktop_PriceInCity_Dealer_Detail_Click": 80,
				"Mobile_PriceInCity_Dealer_Detail_Click": 81,
				"Desktop_DealerLocator_Detail_AvailableModels": 82,
				"Mobile_DealerLocator_Detail_AvailableModels": 83,
				"Mobile_News_Listing_page": 84,
				"Desktop_Photos_page": 85,
				"Desktop_Scooters_Landing_Check_on_road_price": 86,
				"Mobile_Scooters_Landing_Check_on_road_price": 87,
				"Desktop_DealerLocator_Landing_Check_on_road_price": 88,
				"Mobile_DealerLocator_Landing_Check_on_road_price": 89,
				"Desktop_UpcomiingBikes_NewLaunchesWidget": 90,
				"Mobile_UpcomiingBikes_NewLaunchesWidget": 91,
				"Desktop_PriceInCity_Manufacturer_LeadCapture": 92,
				"Mobile_PriceInCity_Manufacturer_LeadCapture": 93,
				"Desktop_HomePage_BestBikes": 94,
				"Desktop_NewPage_BestBikes": 95,
				"Mobile_HomePage_BestBikes": 96,
				"Mobile_NewPage_BestBikes": 97,
				"Desktop_Scooter_MakePage_PopularBikes": 98,
				"Mobile_Scooter_MakePage_PopularBikes": 99,
				"Mobile_Popup_LeadCapture_ForAMP": 100,
				"Mobile_Photos_Page": 101,
				"Desktop_Series_Page": 102,
				"Mobile_Series_Page": 103,
				"Desktop_ElectricBike": 104,
				"Mobile_ElectricBikes": 105,
				"Desktop_Videos_Page_PopularSeries": 106,
				"Mobile_Videos_Page_PopularSeries": 107,
				"Desktop_NewsDetailsPage": 108,
				"Mobile_ExpertReviews_Listing_Page": 109,
				"Mobile_BikeCare_Listing_Page": 110,
				"Mobile_BikeCare_Details_Page": 111,
				"Mobile_Features_Listing_Page": 112,
				"Mobile_Features_Details_Page": 113,
				"Mobile_ComparisionTest_Listing_Page": 114,
				"Mobile_News_Details_Page": 115,
				"Mobile_ExpertReviews_Details_Page": 116
		}
};

/***/ }),
/* 5 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var _typeof = typeof Symbol === "function" && typeof Symbol.iterator === "symbol" ? function (obj) { return typeof obj; } : function (obj) { return obj && typeof Symbol === "function" && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj; };

var _constants = __webpack_require__(4);

var _commonUtils = __webpack_require__(3);

function triggerPageView(url, title) {
    if (typeof dataLayer == "undefined") {
        return;
    }
    if (window.FirstRenderWithAppshell == undefined || window.FirstRenderWithAppshell == null || window.FirstRenderWithAppshell == false) {
        dataLayer.push({
            'event': 'VirtualPageview',
            'virtualPageURL': url,
            'virtualPageTitle': title
        });
    }
}

if (!(0, _commonUtils.isServer)()) {
    var cwTracking = {
        hostPath: location.host,
        isMobileSite: function isMobileSite() {
            var url = document.location.href;
            if (url.indexOf('/m/') > 0) return true;else return false;
        },
        type: {
            impression: 'imp',
            click: 'click'
        },

        getSource: function getSource() {
            var url = document.referrer;
            if (url.length == 0) return 'src=direct';else if (url.indexOf(cwTracking.hostPath) < 0) return 'src=' + url;

            return '';
        },

        getPageUrl: function getPageUrl() {
            var url = document.location.href;
            if (url.indexOf('?') > 0) return 'pi=' + url.split('?')[0];else if (url.indexOf('#') > 0) return 'pi=' + url.split('#')[0];else return 'pi=' + url;
        },

        getQSFromUrl: function getQSFromUrl() {
            var url = document.location.href;
            var qs = '';
            if (url.indexOf('?') > 0) {
                qs = url.split('?')[1];
            } else if (url.indexOf('#') > 0) {
                qs = url.split('#')[1];
            }
            if (qs.length > 0) {
                qs = qs.replace(/&+/g, '|');
                return 'qs=' + qs;
            } else return '';
        },

        getReferrer: function getReferrer() {
            var url = document.referrer;
            var host = location.host;
            if (url.length > 0 && url.indexOf(host) >= 0) {
                var relativePath = url.split(cwTracking.hostPath)[1];
                return 'ref=' + (relativePath == undefined || relativePath == null ? '' : relativePath.replace(/&+/g, '|'));
            }
            return '';
        },

        addToQS: function addToQS(qs, value) {
            if (qs.length > 0) qs += '&' + value;else qs = value;
            return qs;
        },

        getAttributeValue: function getAttributeValue(type) {
            switch (type) {
                case 'imp':
                    return 'cwti';break;
                case 'click':
                    return 'cwtc';break;
                default:
                    return 'cwcti';
            }
        },

        getCompleteQS: function getCompleteQS(node, type) {
            var cat,
                lbl,
                act,
                qs = '',
                attrValue = cwTracking.getAttributeValue(type),
                sendQS = false;
            if (node.is('[qs]')) sendQS = true;
            cat = node.data(attrValue + 'cat'), lbl = node.data(attrValue + 'lbl'), act = node.data(attrValue + 'act');
            qs = cwTracking.getFinalQS(cat, act, lbl, sendQS);
            return qs;
        },

        getFinalQS: function getFinalQS(cat, act, lbl, sendQS) {
            var src,
                pageUrl,
                urlQs,
                ref,
                qs = '';
            src = cwTracking.getSource(), pageUrl = cwTracking.getPageUrl(), ref = cwTracking.getReferrer();
            if (cat != undefined && cat.length > 0) qs = cwTracking.addToQS(qs, 'cat=' + cat);
            if (act != undefined && act.length > 0) qs = cwTracking.addToQS(qs, 'act=' + act);
            if (lbl != undefined && lbl.length > 0) qs = cwTracking.addToQS(qs, 'lbl=' + lbl);
            if (src != undefined && src.length > 0) qs = cwTracking.addToQS(qs, src);
            if (pageUrl != undefined && pageUrl.length > 0) qs = cwTracking.addToQS(qs, pageUrl);

            if (ref != undefined && ref.length > 0) qs = cwTracking.addToQS(qs, ref);

            if (sendQS) {
                urlQs = cwTracking.getQSFromUrl();
                if (urlQs != undefined && urlQs.length > 0) qs = cwTracking.addToQS(qs, urlQs);
            }

            return qs;
        },

        trackCustomData: function trackCustomData(cat, act, lbl, sendQS) {
            var qs = cwTracking.getFinalQS(cat, act, lbl, sendQS);
            cwTracking.sendRequest(qs);
        },

        trackDataFromNode: function trackDataFromNode(node, type) {
            var qs = cwTracking.getCompleteQS(node, type);
            cwTracking.sendRequest(qs);
        },

        sendRequest: function sendRequest(qs) {
            if (qs.length > 0) qs = '&' + qs;
            var img = new Image();
            img.src = cwTracking.getHandlerUrl() + Date.now() + qs;
        },

        getHandlerUrl: function getHandlerUrl() {
            return "/bhrigu/pixel.gif?t=";
        },

        trackPerformace: function trackPerformace(action) {
            var len = 0,
                performanceQS = '',
                performanceTimings;
            if (window.performance != undefined && window.performance.timing != undefined) {
                performanceTimings = JSON.stringify(window.performance.timing);
                performanceTimings = JSON.parse(performanceTimings);
                for (var prop in performanceTimings) {
                    performanceQS += prop + "=" + performanceTimings[prop] + "|";
                }
                len = performanceQS.length;
                if (len > 0) performanceQS = performanceQS.substr(0, len - 1);
                cwTracking.trackCustomData('BWPerformance', action, performanceQS, false);
            }
        },

        trackInviewGA: function trackInviewGA(node) {
            var cat, lbl, act, role;
            cat = node.data('cat'), lbl = node.data('label'), act = node.data('action'), role = node.data('role');
            if (role.indexOf('click') >= 0 && act.indexOf('shown') === -1) {
                act = act + '_shown';
            }
            Common.utils.trackAction('CWNonInteractive', cat, act, lbl);
        },

        prepareLabel: function prepareLabel(trackingparam) {
            var label = '';
            if (trackingparam && (typeof trackingparam === 'undefined' ? 'undefined' : _typeof(trackingparam)) == 'object') {
                for (var property in trackingparam) {
                    label = label + property + '=' + trackingparam[property] + '|';
                }
            }
            return label.substring(0, label.length - 1);
        },

        callImpressionTracking: function callImpressionTracking(node) {
            var role = node.data('role');
            if (role.indexOf('inview-imp') >= 0) {
                node.unbind('inview');
                node.data('role', role.replace('inview-imp', ''));
                cwTracking.trackInviewGA(node);
                return true;
            }
            return false;
        },
        trackUserReview: function trackUserReview(eventName, label) {
            cwTracking.trackCustomData("BWUserReviews", eventName, label, !1);
        },
        trackImagesInteraction: function trackImagesInteraction(category, action, label) {
            cwTracking.trackCustomData(category, action, label, !1);
        }
    };

    window.onload = function () {
        setTimeout(function () {
            if (window.state !== undefined && window.state !== null) {
                cwTracking.trackPerformace("Msite");
            } else {
                cwTracking.trackPerformace("MsiteAppShell");
            }
        }, 100);
    };
}

function triggerGA(cat, act, lab) {
    try {
        dataLayer.push({ 'event': 'Bikewale_all', 'cat': cat, 'act': act, 'lab': lab });
    } catch (e) {// log error   
    }
}

function setPQSourceId(pqPageSource) {
    return _constants.PQ_SOURCE_ID[pqPageSource];
}

function pushNavMenuAnalytics(menuItem) {
    try {
        var categ = GetCatForNav();
        if (categ != null) {
            triggerGA(categ, 'Hamburger_Menu_Item_Click', menuItem);
        }
    } catch (err) {
        console.log(err);
    }
}

function GetCatForNav() {
    var ret_category = "other";
    if (ga_pg_id != null && ga_pg_id != "0") {
        switch (ga_pg_id) {
            case "1":
                ret_category = "HP";
                break;
            case "2":
                ret_category = "Model_Page";
                break;
            case "3":
                ret_category = "Make_Page";
                break;
            case "4":
                ret_category = "New_Bikes_Page";
                break;
            case "5":
                ret_category = "Search_Page";
                break;
            case "6":
                ret_category = "BikeWale_PQ";
                break;
            case "7":
                ret_category = "Dealer_PQ";
                break;
            case "8":
                ret_category = "Booking_Config_Page";
                break;
            case "9":
                ret_category = "Booking_Page";
                break;
            case "10":
                ret_category = "News_Page";
                break;
            case "11":
                ret_category = "News_Detail";
                break;
            case "12":
                ret_category = "Expert_Reviews_Page";
                break;
            case "13":
                ret_category = "Expert_Reviews_Detail";
                break;
            case "14":
                ret_category = "BookingSummary_New";
                break;
            case "15":
                ret_category = "SpecsAndFeature";
                break;
            case "16":
                ret_category = "Price_in_City_Page";
                break;
            case "39":
                ret_category = "BookingListing";
                break;
        }
    }
    return ret_category;
}

function triggerDataToBhrigu(cat, act, lbl) {
    try {
        cwTracking.trackCustomData(cat, act, lbl);
    } catch (err) {}
}
if (!(0, _commonUtils.isServer)()) {
    window.triggerDataToBhrigu = triggerDataToBhrigu;
}

function triggerNonInteractiveGA(cat, act, lab) {
    try {

        dataLayer.push({ 'event': 'Bikewale_noninteraction', 'cat': cat, 'act': act, 'lab': lab });
    } catch (e) {// log error   
    }
}

module.exports = {
    triggerPageView: triggerPageView,
    cwTracking: cwTracking,
    triggerGA: triggerGA,
    GetCatForNav: GetCatForNav,
    triggerDataToBhrigu: triggerDataToBhrigu,
    triggerNonInteractiveGA: triggerNonInteractiveGA,
    pushNavMenuAnalytics: pushNavMenuAnalytics,
    setPQSourceId: setPQSourceId

};

/***/ }),
/* 6 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

var _propTypes = __webpack_require__(1);

var _propTypes2 = _interopRequireDefault(_propTypes);

var _reactDom = __webpack_require__(0);

var _eventlistener = __webpack_require__(123);

var _lodash = __webpack_require__(124);

var _lodash2 = _interopRequireDefault(_lodash);

var _lodash3 = __webpack_require__(125);

var _lodash4 = _interopRequireDefault(_lodash3);

var _parentScroll = __webpack_require__(126);

var _parentScroll2 = _interopRequireDefault(_parentScroll);

var _inViewport = __webpack_require__(127);

var _inViewport2 = _interopRequireDefault(_inViewport);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var LazyLoad = function (_Component) {
  _inherits(LazyLoad, _Component);

  function LazyLoad(props) {
    _classCallCheck(this, LazyLoad);

    var _this = _possibleConstructorReturn(this, (LazyLoad.__proto__ || Object.getPrototypeOf(LazyLoad)).call(this, props));

    _this.lazyLoadHandler = _this.lazyLoadHandler.bind(_this);

    if (props.throttle > 0) {
      if (props.debounce) {
        _this.lazyLoadHandler = (0, _lodash2.default)(_this.lazyLoadHandler, props.throttle);
      } else {
        _this.lazyLoadHandler = (0, _lodash4.default)(_this.lazyLoadHandler, props.throttle);
      }
    }

    _this.state = { visible: false };
    return _this;
  }

  _createClass(LazyLoad, [{
    key: 'componentDidMount',
    value: function componentDidMount() {
      this._mounted = true;
      var eventNode = this.getEventNode();

      this.lazyLoadHandler();

      if (this.lazyLoadHandler.flush) {
        this.lazyLoadHandler.flush();
      }

      (0, _eventlistener.add)(window, 'resize', this.lazyLoadHandler);
      (0, _eventlistener.add)(eventNode, 'scroll', this.lazyLoadHandler);
    }
  }, {
    key: 'componentWillReceiveProps',
    value: function componentWillReceiveProps() {
      if (!this.state.visible) {
        this.lazyLoadHandler();
      }
    }
  }, {
    key: 'shouldComponentUpdate',
    value: function shouldComponentUpdate(_nextProps, nextState) {
      return nextState.visible;
    }
  }, {
    key: 'componentWillUnmount',
    value: function componentWillUnmount() {
      this._mounted = false;
      if (this.lazyLoadHandler.cancel) {
        this.lazyLoadHandler.cancel();
      }

      this.detachListeners();
    }
  }, {
    key: 'getEventNode',
    value: function getEventNode() {
      return (0, _parentScroll2.default)((0, _reactDom.findDOMNode)(this));
    }
  }, {
    key: 'getOffset',
    value: function getOffset() {
      var _props = this.props,
          offset = _props.offset,
          offsetVertical = _props.offsetVertical,
          offsetHorizontal = _props.offsetHorizontal,
          offsetTop = _props.offsetTop,
          offsetBottom = _props.offsetBottom,
          offsetLeft = _props.offsetLeft,
          offsetRight = _props.offsetRight,
          threshold = _props.threshold;


      var _offsetAll = threshold || offset;
      var _offsetVertical = offsetVertical || _offsetAll;
      var _offsetHorizontal = offsetHorizontal || _offsetAll;

      return {
        top: offsetTop || _offsetVertical,
        bottom: offsetBottom || _offsetVertical,
        left: offsetLeft || _offsetHorizontal,
        right: offsetRight || _offsetHorizontal
      };
    }
  }, {
    key: 'lazyLoadHandler',
    value: function lazyLoadHandler() {
      if (!this._mounted) {
        return;
      }
      var offset = this.getOffset();
      var node = (0, _reactDom.findDOMNode)(this);
      var eventNode = this.getEventNode();

      if ((0, _inViewport2.default)(node, eventNode, offset)) {
        var onContentVisible = this.props.onContentVisible;


        this.setState({ visible: true }, function () {
          if (onContentVisible) {
            onContentVisible();
          }
        });
        this.detachListeners();
      }
    }
  }, {
    key: 'detachListeners',
    value: function detachListeners() {
      var eventNode = this.getEventNode();

      (0, _eventlistener.remove)(window, 'resize', this.lazyLoadHandler);
      (0, _eventlistener.remove)(eventNode, 'scroll', this.lazyLoadHandler);
    }
  }, {
    key: 'render',
    value: function render() {
      var _props2 = this.props,
          children = _props2.children,
          className = _props2.className,
          height = _props2.height,
          width = _props2.width;
      var visible = this.state.visible;


      var elStyles = { height: height, width: width };
      var elClasses = 'LazyLoad' + (visible ? ' is-visible' : '') + (className ? ' ' + className : '');

      return _react2.default.createElement(this.props.elementType, {
        className: elClasses,
        style: elStyles
      }, visible && _react.Children.only(children));
    }
  }]);

  return LazyLoad;
}(_react.Component);

exports.default = LazyLoad;


LazyLoad.propTypes = {
  children: _propTypes2.default.node.isRequired,
  className: _propTypes2.default.string,
  debounce: _propTypes2.default.bool,
  elementType: _propTypes2.default.string,
  height: _propTypes2.default.oneOfType([_propTypes2.default.string, _propTypes2.default.number]),
  offset: _propTypes2.default.number,
  offsetBottom: _propTypes2.default.number,
  offsetHorizontal: _propTypes2.default.number,
  offsetLeft: _propTypes2.default.number,
  offsetRight: _propTypes2.default.number,
  offsetTop: _propTypes2.default.number,
  offsetVertical: _propTypes2.default.number,
  threshold: _propTypes2.default.number,
  throttle: _propTypes2.default.number,
  width: _propTypes2.default.oneOfType([_propTypes2.default.string, _propTypes2.default.number]),
  onContentVisible: _propTypes2.default.func
};

LazyLoad.defaultProps = {
  elementType: 'div',
  debounce: true,
  offset: 0,
  offsetBottom: 0,
  offsetHorizontal: 0,
  offsetLeft: 0,
  offsetRight: 0,
  offsetTop: 0,
  offsetVertical: 0,
  throttle: 250
};

/***/ }),
/* 7 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */



/**
 * Use invariant() to assert state which your program assumes to be true.
 *
 * Provide sprintf-style format (only %s is supported) and arguments
 * to provide information about what broke and what you were
 * expecting.
 *
 * The invariant message will be stripped in production, but the invariant
 * will remain to ensure logic does not differ in production.
 */

var invariant = function(condition, format, a, b, c, d, e, f) {
  if (true) {
    if (format === undefined) {
      throw new Error('invariant requires an error message argument');
    }
  }

  if (!condition) {
    var error;
    if (format === undefined) {
      error = new Error(
        'Minified exception occurred; use the non-minified dev environment ' +
        'for the full error message and additional helpful warnings.'
      );
    } else {
      var args = [a, b, c, d, e, f];
      var argIndex = 0;
      error = new Error(
        format.replace(/%s/g, function() { return args[argIndex++]; })
      );
      error.name = 'Invariant Violation';
    }

    error.framesToPop = 1; // we don't care about invariant's own frame
    throw error;
  }
};

module.exports = invariant;


/***/ }),
/* 8 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
    value: true
});

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var SpinnerRelative = function SpinnerRelative() {
    return _react2.default.createElement(
        "div",
        { className: "loader-content loader-relative" },
        _react2.default.createElement("span", { className: "loader" })
    );
};

exports.default = SpinnerRelative;

/***/ }),
/* 9 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
    value: true
});
exports.toJS = undefined;

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

var _immutable = __webpack_require__(17);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var toJS = exports.toJS = function toJS(WrappedComponent) {
    return function (wrappedComponentProps) {

        var propsJS = Object.keys(wrappedComponentProps).reduce(function (newProps, key) {

            newProps[key] = _immutable.Iterable.isIterable(wrappedComponentProps[key]) ? wrappedComponentProps[key].toJS() : wrappedComponentProps[key];
            return newProps;
        }, {});

        return _react2.default.createElement(WrappedComponent, propsJS);
    };
};

/***/ }),
/* 10 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
Object.defineProperty(__webpack_exports__, "__esModule", { value: true });
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0__createStore__ = __webpack_require__(69);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1__combineReducers__ = __webpack_require__(188);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2__bindActionCreators__ = __webpack_require__(189);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_3__applyMiddleware__ = __webpack_require__(190);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_4__compose__ = __webpack_require__(72);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_5__utils_warning__ = __webpack_require__(71);
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "createStore", function() { return __WEBPACK_IMPORTED_MODULE_0__createStore__["b"]; });
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "combineReducers", function() { return __WEBPACK_IMPORTED_MODULE_1__combineReducers__["a"]; });
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "bindActionCreators", function() { return __WEBPACK_IMPORTED_MODULE_2__bindActionCreators__["a"]; });
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "applyMiddleware", function() { return __WEBPACK_IMPORTED_MODULE_3__applyMiddleware__["a"]; });
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "compose", function() { return __WEBPACK_IMPORTED_MODULE_4__compose__["a"]; });







/*
* This is a dummy function to check if the function name has been altered by minification.
* If the function has been minified and NODE_ENV !== 'production', warn the user.
*/
function isCrushed() {}

if ("development" !== 'production' && typeof isCrushed.name === 'string' && isCrushed.name !== 'isCrushed') {
  Object(__WEBPACK_IMPORTED_MODULE_5__utils_warning__["a" /* default */])('You are currently using minified code outside of NODE_ENV === \'production\'. ' + 'This means that you are running a slower development build of Redux. ' + 'You can use loose-envify (https://github.com/zertosh/loose-envify) for browserify ' + 'or DefinePlugin for webpack (http://stackoverflow.com/questions/30030031) ' + 'to ensure you have the correct code for your production build.');
}



/***/ }),
/* 11 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var _NewsCommon = __webpack_require__(22);

var _commonUtils = __webpack_require__(3);

var _scrollLock = __webpack_require__(37);

var _analyticsUtils = __webpack_require__(5);

var topCount = "5";
var popUpHistory = [];
function closeGlobalSearchPopUp() {
    hideElement(document.getElementById('global-search-popup'));
    unlockPopup();
}

function closeGlobalCityPopUp() {
    hideElement(document.getElementById('globalcity-popup'));
    unlockPopup();
}
function closeOnRoadPricePopUp() {
    resetOnRoadPricePopup();
    hideElement(document.getElementById('popupWrapper'));

    closeCityAreaSelectionPopup();
    unlockPopup();
}

function closeCityAreaSelectionPopup() {
    var popupContent = document.getElementById('popupContent');

    popupContent.style.left = '100%';
    popupContent.classList.remove('fix-input-field');
}

function openCityAreaSelectionPopup() {
    var popupContent = document.getElementById('popupContent');

    popupContent.style.left = '0';
    setTimeout(function () {
        popupContent.classList.add('fix-input-field');
    }, 500);
}

function closeGlobalCityPopUpByButtonClick() {
    hideElement(document.getElementById('globalcity-popup'));
    unlockPopup();
    window.history.back();
}

function lockPopup() {
    document.body.classList.add('lock-browser-scroll');
    showElement(document.getElementsByClassName('blackOut-window')[0]);
}

function unlockPopup() {
    document.body.classList.remove('lock-browser-scroll');
    hideElement(document.getElementsByClassName('blackOut-window')[0]);
}

function hideElement(element) {
    try {
        element.style.display = 'none';
    } catch (e) {}
}

function showElement(element) {
    try {
        element.style.display = 'block';
    } catch (e) {}
}

var appendHash = function appendHash(state) {
    window.location.hash = state;
};

function getGlobalCity() {
    if ((0, _commonUtils.isServer)()) {
        return null;
    }
    var cookieName = "location";
    var locationCookie = getCookie(cookieName);
    if (locationCookie) {
        locationCookie = locationCookie.replace('-', ' ').split("_");
        var cityName = locationCookie[1];
        var globalCityId = parseInt(locationCookie[0]);
        return {
            name: cityName,
            id: globalCityId
        };
    }
    return null;
}

function setGlobalCity(cityId, cityName, globalCityId) {
    if (cityId != globalCityId) {
        SetCookieInDays("location", cityId + "_" + cityName, 365);
        bwcache.set("userchangedlocation", window.location.href, true);
    }
}

function IsGlobalCityPresent(cityList, globalCityId) {
    if (cityList != null && cityList.some(function (item) {
        return item.cityId === globalCityId;
    })) {
        return true;
    }
    return false;
}

function getCookie(key) {
    var keyValue = document.cookie.match('(^|;) ?' + key + '=([^;]*)(;|$)');
    return keyValue ? keyValue[2] : null;
}

function isCookieExists(cookiename) {
    var coockieVal = document.cookie(cookiename);
    if (coockieVal == undefined || coockieVal == null || coockieVal == "-1" || coockieVal == "") return false;
    return true;
}

function getHost() {
    var host = document.domain;
    if (host.match("bikewale.com$")) host = ".bikewale.com";
    return host;
}

function SetCookieInDays(cookieName, cookieValue, nDays) {
    var today = new Date();
    var expire = new Date();
    expire.setTime(today.getTime() + 3600000 * 24 * nDays);
    cookieValue = cookieValue.replace(/\s+/g, '-');
    if (/MSIE (\d+\.\d+);/.test(navigator.userAgent) || /Trident\//.test(navigator.userAgent)) document.cookie = cookieName + "=" + cookieValue + ";expires=" + expire.toGMTString() + '; path =/';else document.cookie = cookieName + "=" + cookieValue + ";expires=" + expire.toGMTString() + ';domain=' + getHost() + '; path =/';

    bwcache.remove("userchangedlocation", true);
}

function showHideMatchError(element, error) {
    if (error) {
        document.getElementById('error-icon').classList.remove('hide');
        document.getElementById('bw-blackbg-tooltip').classList.remove('hide');
        element.classList.add('border-red');
    } else {
        document.getElementById('error-icon').classList.add('hide');
        document.getElementById('bw-blackbg-tooltip').classList.add('hide');
        element.classList.remove('border-red');
    }
}

if (!(0, _commonUtils.isServer)()) {
    try {
        docReady(function () {
            var ele = document.getElementsByClassName('blackOut-window');
            if (ele.length > 0) {
                ele[0].addEventListener('mouseup', function (e) {
                    var globalSearchPopup = document.getElementById('global-search-popup');

                    if (e.target.id !== globalSearchPopup.getAttribute('id')) // TODO && !globalSearchPopup.has(e.target).length)
                        {
                            hideElement(globalSearchPopup);
                            unlockPopup();
                        }
                });
            }

            window.addEventListener('hashchange', function (e) {
                var oldUrl, oldHash;
                oldUrl = e.oldURL;
                if (oldUrl && oldUrl.indexOf('#') > 0) {
                    oldHash = oldUrl.split('#')[1];
                    closePopUp(oldHash);
                }
            });
        });
    } catch (err) {}
}

var recentSearches = {
    searchKey: "recentsearches",
    trendingKey: "trendingbikes",
    options: {
        bikeSearchEle: (0, _commonUtils.isServer)() ? null : document.getElementById('globalSearch'),
        recentSearchesEle: (0, _commonUtils.isServer)() ? null : document.getElementById("new-global-recent-searches") && document.getElementById("new-global-recent-searches").length ? document.getElementById("new-global-recent-searches") : document.getElementById("global-recent-searches"),
        recentSearchesLoaded: false
    },
    saveRecentSearches: function saveRecentSearches(opt) {
        if (opt && opt.payload && opt.payload.makeId > 0) {
            var objSearches = bwcache.get(this.searchKey) || {};
            opt.payload["name"] = opt.label;
            objSearches.searches = objSearches.searches || [];
            var eleIndex = this.objectIndexOf(objSearches.searches, opt.payload);
            if (objSearches.searches != null && eleIndex > -1) objSearches.searches.splice(eleIndex, 1);
            objSearches.searches.unshift(opt.payload);

            objSearches["lastModified"] = new Date().getTime();
            if (objSearches.searches.length > 3) objSearches.searches.pop();
            objSearches["noOfSearches"] = objSearches.searches.length;
            bwcache.set(this.searchKey, objSearches);
        }
    },
    showRecentSearches: function showRecentSearches(showRecentSearchList) {
        var objSearches = bwcache.get(this.searchKey);
        if (objSearches && objSearches.searches) {
            showRecentSearchList(objSearches.searches);
        }
    },
    getRecentSearches: function getRecentSearches(showRecentSearchList) {
        var objSearches = bwcache.get(this.searchKey);
        if (objSearches && objSearches.searches) {
            return objSearches.searches;
        } else return null;
    },
    showTrendingSearches: function showTrendingSearches(showTrendingSearchList) {
        var objSearches = bwcache.get(this.trendingKey);
        if (objSearches) {
            showTrendingSearchList(objSearches);
        }
    },
    getTrendingSearches: function getTrendingSearches(showTrendingSearchList) {
        var trendingSearches = bwcache.get(this.trendingKey);
        if (!trendingSearches) {
            var xhr = new XMLHttpRequest();
            xhr.open('GET', "/api/popularbikes/?topCount=" + topCount);
            xhr.setRequestHeader("Content-Type", "application/json; charset=utf-8");
            xhr.onreadystatechange = function () {
                if (xhr.readyState == 4) {
                    if (xhr.status == 200) {
                        var response = JSON.parse(xhr.responseText);
                        if (response != null) {
                            trendingSearches = response;
                            localStorage.setItem("bwc_trendingbikes", JSON.stringify(trendingSearches));
                            showTrendingSearchList();
                        }
                    }
                }
            };
            xhr.send();
        }
        return trendingSearches;
    },
    objectIndexOf: function objectIndexOf(arr, opt) {
        for (var i = 0, len = arr.length; i < len; i++) {
            if (arr[i]["makeId"] === opt.makeId && arr[i]["modelId"] === opt.modelId) return i;
        }return -1;
    }
};

function closePopUp(state) {
    switch (state) {
        case "globalCity":
            closeGlobalCityPopUp();
            break;
        case "onRoadPrice":
            closeOnRoadPricePopUp();
            break;
        default:
            if (window.popupCallback != undefined && window.popupCallback[state] != undefined) {
                window.popupCallback[state]();
                window.popupCallback[state] = undefined;
                (0, _scrollLock.unlockScroll)();
            }
            return true;
    }
}

function getStrippedTerm(term) {
    try {
        return term.replace(/^\s\s*/, '').replace(/\s\s*$/, '').replace(/-/g, ' ').replace(/[^A-Za-z0-9 ]/g, '').toLowerCase().trim();
    } catch (e) {
        return '';
    }
}

function autocomplete(options, term) {
    var orgTerm = term;
    var reqTerm = getStrippedTerm(orgTerm);

    var year = options.year;
    if (year != null && year != undefined && year != '') year = year.val();else year = '';

    var cacheProp = reqTerm + '_' + year;
    if (!(cacheProp in options.cache) && reqTerm.length > 0) {
        showElement(document.getElementById("loaderGlobalCity"));
        var indexToHit = options.source;
        var count = options.recordCount;
        var path = "/api/AutoSuggest/?source=" + indexToHit + "&inputText=" + encodeURIComponent(reqTerm) + "&noofrecords=" + count;

        options.cache[cacheProp] = new Array();

        var xhr = new XMLHttpRequest();
        xhr.open('GET', path);
        xhr.setRequestHeader("Content-Type", "application/json; charset=utf-8");
        if (options.loaderStatus != null && typeof options.loaderStatus == "function") {
            options.loaderStatus(false);
        }

        xhr.onreadystatechange = function () {

            if (xhr.readyState == 4) {
                if (xhr.status == 200) {
                    hideElement(document.getElementById("loaderGlobalCity"));
                    var response = JSON.parse(xhr.responseText); // TODO remve comment
                    var jsonData = response.suggestionList;
                    options.cache[reqTerm + '_' + year] = jsonData.map(function (item) {
                        return { label: item.text, payload: item.payload };
                    });
                    var result = options.cache[cacheProp];

                    if (options.afterfetch != null && typeof options.afterfetch == "function") options.afterfetch(result, reqTerm);
                } else {
                    var result = undefined;
                    options.afterfetch(result, reqTerm);
                }
                if (options.loaderStatus != null && typeof options.loaderStatus == "function") options.loaderStatus(true);
            }
        };
        xhr.send();
    } else {
        var result = options.cache[cacheProp];
        if (options.afterfetch != null && typeof options.afterfetch == "function") options.afterfetch(result, reqTerm);
    }
}

function highlightText(text, matchingText) {
    var matcher = new RegExp("(" + matchingText.replace(/[\-\[\]{}()*+?.,\\\^$|#\s]/g, "\\$&") + ")", "ig");
    return text.replace(matcher, "<strong>$1</strong>");
}

function setPriceQuoteFlag() {
    hideElement(document.getElementById('global-search-popup'));
    unlockPopup();
    IsPriceQuoteLinkClicked = true;
}

function checkCookies() {
    var c = document.cookie.split('; ');
    for (var i = c.length - 1; i >= 0; i--) {
        var C = c[i].split('=');
        if (C[0] == "location") {
            var cData = String(C[1]).split('_');
            onCookieObj.PQCitySelectedId = parseInt(cData[0]) || 0;
            onCookieObj.PQCitySelectedName = cData[1] ? cData[1].replace(/-/g, ' ') : "";
            onCookieObj.PQAreaSelectedId = parseInt(cData[2]) || 0;
            onCookieObj.PQAreaSelectedName = cData[3] ? cData[3].replace(/-/g, ' ') : "";
        }
    }
}

function setDataforPopularBikesWidget(event, item) {
    checkCookies();

    var pageCategory = (0, _NewsCommon.extractPageCategoryFromURL)();
    var pageName = gaObj != null ? gaObj.name : "";
    var isDetail = pageName.indexOf("List") == -1;
    var pqSourcePage = pageCategory == "news" ? isDetail ? "Mobile_News_Details_Page" : "Mobile_News_Listing_page" : isDetail ? "Mobile_ExpertReviews_Details_Page" : "Mobile_ExpertReviews_Listing_Page";
    onRoadPricePopupDataObject.SelectedModelId = item.ModelId != null && item.ModelId != undefined ? item.ModelId : 0;
    onRoadPricePopupDataObject.SelectedCity = onCookieObj.PQCitySelectedId > 0 ? { 'id': onCookieObj.PQCitySelectedId, 'name': onCookieObj.PQCitySelectedName } : null;
    onRoadPricePopupDataObject.SelectedArea = onCookieObj.PQAreaSelectedId > 0 ? { 'id': onCookieObj.PQAreaSelectedId, 'name': onCookieObj.PQAreaSelectedName } : null;
    onRoadPricePopupDataObject.SelectedCityId = onCookieObj.PQCitySelectedId || 0;
    onRoadPricePopupDataObject.SelectedAreaId = onCookieObj.PQAreaSelectedId || 0;
    onRoadPricePopupDataObject.BookingCities = [];
    onRoadPricePopupDataObject.BookingAreas = [];
    onRoadPricePopupDataObject.ModelName = item.ModelName != null ? item.ModelName : "";
    onRoadPricePopupDataObject.MakeName = item.MakeName != null ? item.MakeName : "";
    onRoadPricePopupDataObject.PageCatId = gaObj != null ? gaObj.id : 0;
    onRoadPricePopupDataObject.IsPersistence = false;
    onRoadPricePopupDataObject.IsReload = false;
    onRoadPricePopupDataObject.PageSourceId = (0, _analyticsUtils.setPQSourceId)(pqSourcePage);
    if (onRoadPricePopupDataObject.SelectedCityId == 0) onRoadPricePopupDataObject.LoadingText = 'Fetching Cities...';
    if (onRoadPricePopupDataObject.IsPersistence) onRoadPricePopupDataObject.LoadingText = 'Loading locations...';

    showElement(document.getElementById('popupWrapper'));
    showElement(document.getElementById('popupContent'));
    document.getElementById('popupWrapper').classList.add('loader-active');

    if (window.location.hash != '') {
        window.location.hash = '';
    }
    appendHash("onRoadPrice");
}

function setDataForPriceQuotePopup(event, bikeObj) {
    checkCookies();
    var item = bikeObj;
    if (bikeObj != null && (bikeObj.payload != null || bikeObj.payload != undefined)) {
        item = bikeObj.payload;
    }
    onRoadPricePopupDataObject.SelectedModelId = item.modelId != null && item.modelId != undefined ? item.modelId : 0;
    onRoadPricePopupDataObject.SelectedCity = onCookieObj.PQCitySelectedId > 0 ? { 'id': onCookieObj.PQCitySelectedId, 'name': onCookieObj.PQCitySelectedName } : null;
    onRoadPricePopupDataObject.SelectedArea = onCookieObj.PQAreaSelectedId > 0 ? { 'id': onCookieObj.PQAreaSelectedId, 'name': onCookieObj.PQAreaSelectedName } : null;
    onRoadPricePopupDataObject.SelectedCityId = (item.preselcity != null && item.preselcity != undefined ? item.preselcity : 0) || onCookieObj.PQCitySelectedId || 0;
    onRoadPricePopupDataObject.SelectedAreaId = onCookieObj.PQAreaSelectedId || 0;
    onRoadPricePopupDataObject.BookingCities = [];
    onRoadPricePopupDataObject.BookingAreas = [];
    onRoadPricePopupDataObject.ModelName = item.modelName != null ? item.modelName : "";
    onRoadPricePopupDataObject.MakeName = item.makeName != null ? item.makeName : "";
    onRoadPricePopupDataObject.PageCatId = item.pagecatId != null ? item.makeName : "";
    onRoadPricePopupDataObject.IsPersistence = item.persistent != undefined && item.persistent != null ? item.persistent : false;
    onRoadPricePopupDataObject.IsReload = item.reload != undefined && item.reload != null ? item.reload : false;
    if (onRoadPricePopupDataObject.SelectedCityId == 0) onRoadPricePopupDataObject.LoadingText = 'Fetching Cities...';
    if (onRoadPricePopupDataObject.IsPersistence) onRoadPricePopupDataObject.LoadingText = 'Loading locations...';

    showElement(document.getElementById('popupWrapper'));
    showElement(document.getElementById('popupContent'));
    document.getElementById('popupWrapper').classList.add('loader-active');

    if (window.location.hash != '') {
        window.location.hash = '';
    }
    appendHash("onRoadPrice");
}

var popupState = {
    popupClosed: "popup-closed",
    cityPopupOpen: "city-popup-open",
    areaPopupOpen: "area-popup-open"
};
function resetOnRoadPricePopup() {

    onRoadPricePopupDataObject.SelectedModelId = 0;
    onRoadPricePopupDataObject.SelectedCity = null;
    onRoadPricePopupDataObject.SelectedArea = null;
    onRoadPricePopupDataObject.HasAreas = false;
    onRoadPricePopupDataObject.SelectedCityId = 0;
    onRoadPricePopupDataObject.SelectedAreaId = 0;
    onRoadPricePopupDataObject.BookingCities = [];
    onRoadPricePopupDataObject.BookingAreas = [];
    onRoadPricePopupDataObject.MakeName = "";
    onRoadPricePopupDataObject.ModelName = "";
    onRoadPricePopupDataObject.PageCatId = "";
    onRoadPricePopupDataObject.DealerId = "";
    onRoadPricePopupDataObject.VersionId = "";
    onRoadPricePopupDataObject.IsPersistence = false;
    onRoadPricePopupDataObject.IsReload = false;
    onRoadPricePopupDataObject.LoadingText = "";
    onRoadPricePopupDataObject.state = popupState.cityPopupOpe;
}

var onRoadPricePopupDataObject = {
    SelectedModelId: 0,
    SelectedCity: null,
    SelectedArea: null,
    HasAreas: false,
    SelectedCityId: 0,
    SelectedAreaId: 0,
    BookingCities: [],
    BookingAreas: [],
    MakeName: "",
    ModelName: "",
    PageCatId: "",
    DealerId: "",
    VersionId: "",
    IsPersistence: false,
    IsReload: false,
    LoadingText: "",
    state: popupState.cityPopupOpen
};

function GetGlobalCityArea() {
    var cookieName = "location";
    var cityArea = getCookie(cookieName);
    if (cityArea != null) {
        cityArea = cityArea.replace(/[0-9](_)*/g, '').replace(/-+/g, ' ');
    } else {
        cityArea = '';
    }
    return cityArea;
}

function gtmCodeAppender(pageId, action, label) {
    var category = '';
    if (pageId != null) {
        switch (pageId) {
            case "1":
                category = 'Make_Page';
                break;
            case "2":
                category = "CheckPQ_Series";
                action = "CheckPQ_Series_" + action;
                break;
            case "3":
                category = "Model_Page";
                action = "CheckPQ_Model_" + action;
                break;
            case '4':
                category = 'New_Bikes_Page';
                break;
            case '5':
                category = 'HP';
                break;
            case '6':
                category = 'Search_Page';
                break;
        }
        if (label) {
            dataLayer.push({ 'event': 'Bikewale_all', 'cat': category, 'act': action, 'lab': label });
        } else {
            dataLayer.push({ 'event': 'Bikewale_all', 'cat': category, 'act': action });
        }
    }
}

function MakeModelRedirection(item) {
    if (!IsPriceQuoteLinkClicked) {
        if (item.payload != null) {
            var make = new Object();
            make.maskingName = item.payload.makeMaskingName;
            make.id = item.payload.makeId;
            var model = null;
            if (item.payload.modelId > 0) {
                model = new Object();
                model.maskingName = item.payload.modelMaskingName;
            }
            recentSearches.saveRecentSearches(item);
            closeGlobalSearchPopUp();
            if (model) {
                window.location.href = "/m/" + make.maskingName + "-bikes/" + model.maskingName + "/";
                return true;
            } else {
                window.location.href = "/m/" + make.maskingName + "-bikes/";
                return true;
            }
        }
    }
    if (IsPriceQuoteLinkClicked) {
        IsPriceQuoteLinkClicked = false;
    }
}

function openPopupWithHash(openingFunction, closingFunction, hash) {
    try {
        if (typeof openingFunction === "function" && typeof closingFunction === "function") {
            openingFunction();
            if (window.popupCallback == undefined) {
                window.popupCallback = {};
            }
            window.popupCallback[hash] = closingFunction;
            window.history.pushState(undefined, undefined, window.location.pathname + '#' + hash);
            (0, _scrollLock.lockScroll)();
        }
    } catch (e) {
        console.log(e);
    }
}

function closePopupWithHash(closingFunction) {
    try {
        if (typeof closingFunction === "function") {
            (0, _scrollLock.unlockScroll)();
            closingFunction();
            if (window.popupCallback != undefined) {
                var hash = (window.location.hash || "#").slice(1);
                if (hash.length > 0) {
                    window.history.replaceState(undefined, undefined, window.location.pathname);
                    window.popupCallback[hash] = undefined;
                }
            }
        }
    } catch (e) {
        console.log(e);
    }
}

var globalCityCache = new Object(); // variable for global city autocomplete
var globalSearchCache = new Object(); // variable for global search autocomplete
var globalSearchStatus = {
    RESET: 0,
    ERROR: 1,
    RECENTSEARCH: 2,
    AUTOCOMPLETE: 3
};

var IsPriceQuoteLinkClicked = false;
var onCookieObj = new Object();
module.exports = {
    closeGlobalSearchPopUp: closeGlobalSearchPopUp,
    closeGlobalCityPopUp: closeGlobalCityPopUp,
    closeGlobalCityPopUpByButtonClick: closeGlobalCityPopUpByButtonClick,
    lockPopup: lockPopup,
    unlockPopup: unlockPopup,
    hideElement: hideElement,
    showElement: showElement,
    recentSearches: recentSearches,
    SetCookieInDays: SetCookieInDays,
    autocomplete: autocomplete,
    showHideMatchError: showHideMatchError,
    globalCityCache: globalCityCache,
    globalSearchCache: globalSearchCache,
    getGlobalCity: getGlobalCity,
    highlightText: highlightText,
    getStrippedTerm: getStrippedTerm,
    setPriceQuoteFlag: setPriceQuoteFlag,
    MakeModelRedirection: MakeModelRedirection,
    globalSearchStatus: globalSearchStatus,
    onCookieObj: onCookieObj,
    setDataForPriceQuotePopup: setDataForPriceQuotePopup,
    onRoadPricePopupDataObject: onRoadPricePopupDataObject,
    getCookie: getCookie,
    GetGlobalCityArea: GetGlobalCityArea,
    gtmCodeAppender: gtmCodeAppender,
    popupState: popupState,
    resetOnRoadPricePopup: resetOnRoadPricePopup,
    closeCityAreaSelectionPopup: closeCityAreaSelectionPopup,
    openCityAreaSelectionPopup: openCityAreaSelectionPopup,
    setDataforPopularBikesWidget: setDataforPopularBikesWidget,
    setGlobalCity: setGlobalCity,
    IsGlobalCityPresent: IsGlobalCityPresent,
    openPopupWithHash: openPopupWithHash,
    closePopupWithHash: closePopupWithHash
};

/***/ }),
/* 12 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
Object.defineProperty(__webpack_exports__, "__esModule", { value: true });
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0__components_Provider__ = __webpack_require__(172);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1__components_connectAdvanced__ = __webpack_require__(68);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2__connect_connect__ = __webpack_require__(174);
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "Provider", function() { return __WEBPACK_IMPORTED_MODULE_0__components_Provider__["b"]; });
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "createProvider", function() { return __WEBPACK_IMPORTED_MODULE_0__components_Provider__["a"]; });
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "connectAdvanced", function() { return __WEBPACK_IMPORTED_MODULE_1__components_connectAdvanced__["a"]; });
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "connect", function() { return __WEBPACK_IMPORTED_MODULE_2__connect_connect__["a"]; });






/***/ }),
/* 13 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/**
 * Copyright (c) 2014-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 *
 * @providesModule warning
 */



/**
 * Similar to invariant but only logs a warning if the condition is not met.
 * This can be used to log issues in development environments in critical
 * paths. Removing the logging code for production environments will keep the
 * same logic and follow the same code paths.
 */

var __DEV__ = "development" !== 'production';

var warning = function() {};

if (__DEV__) {
  var printWarning = function printWarning(format, args) {
    var len = arguments.length;
    args = new Array(len > 2 ? len - 2 : 0);
    for (var key = 2; key < len; key++) {
      args[key - 2] = arguments[key];
    }
    var argIndex = 0;
    var message = 'Warning: ' +
      format.replace(/%s/g, function() {
        return args[argIndex++];
      });
    if (typeof console !== 'undefined') {
      console.error(message);
    }
    try {
      // --- Welcome to debugging React ---
      // This error was thrown as a convenience so that you can use this stack
      // to find the callsite that caused this warning to fire.
      throw new Error(message);
    } catch (x) {}
  }

  warning = function(condition, format, args) {
    var len = arguments.length;
    args = new Array(len > 2 ? len - 2 : 0);
    for (var key = 2; key < len; key++) {
      args[key - 2] = arguments[key];
    }
    if (format === undefined) {
      throw new Error(
          '`warning(condition, format, ...args)` requires a warning ' +
          'message argument'
      );
    }
    if (!condition) {
      printWarning.apply(null, [format].concat(args));
    }
  };
}

module.exports = warning;


/***/ }),
/* 14 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var formVideoUrl = function formVideoUrl(urlSuffix, basicId) {
	return '/m/bike-videos/' + urlSuffix + '-' + basicId + '/';
};
var formVideoImageUrl = function formVideoImageUrl(videoId, imageSuffix) {
	return 'https://img.youtube.com/vi/' + videoId + '/' + imageSuffix;
};
var formMoreVideosUrl = function formMoreVideosUrl(urlSuffix) {
	return "/m/bike-videos/category" + urlSuffix;
};
var pushVideoDetailUrl = function pushVideoDetailUrl(context, video) {
	var videoUrl = formVideoUrl(video.VideoTitleUrl, video.BasicId);
	context.props.history.push(videoUrl);
	var videoInitialData = mapVideoDataToInitialData(video);
	context.props.fetchVideoDetail(videoInitialData); // TODO call other apis
	context.props.fetchModelSlug(videoInitialData.BasicId);
};
var getCategoryIdFromUrl = function getCategoryIdFromUrl(url) {
	var regex = /\/m\/bike-videos\/category\/.*-(\d+)\//;
	var matches = regex.exec(url);
	if (matches) {
		return matches[1];
	}
	return -1;
};

var pushVideosByCategoryUrl = function pushVideosByCategoryUrl(context, sectionTitle, moreVideosUrl) {
	var url = formMoreVideosUrl(moreVideosUrl);
	context.props.history.push(url);
	var categoryId = context.props.match.params.categoryId ? context.props.match.params.categoryId : getCategoryIdFromUrl(url);
	context.props.fetchVideosByCategory(categoryId, sectionTitle);
};
var mapVideoDataToInitialData = function mapVideoDataToInitialData(video) {
	if (!video) {
		return null;
	}
	return {
		VideoTitle: video.VideoTitle,
		VideoUrl: video.VideoUrl,
		BasicId: video.BasicId,
		VideoTitleUrl: video.VideoTitleUrl,
		DisplayDate: video.DisplayDate

	};
};

module.exports = {
	formVideoUrl: formVideoUrl,
	formVideoImageUrl: formVideoImageUrl,
	formMoreVideosUrl: formMoreVideosUrl,
	pushVideoDetailUrl: pushVideoDetailUrl,
	pushVideosByCategoryUrl: pushVideosByCategoryUrl,
	mapVideoDataToInitialData: mapVideoDataToInitialData
};

/***/ }),
/* 15 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


function formatToCurrency(value) {
	var amountValue = void 0;

	if (value > 9999999) {
		amountValue = parseFloat((value / 10000000).toFixed(2)) + '+ Cr';
	} else if (value <= 100000) {
		amountValue = parseFloat((value / 1000).toFixed(2)) + 'K';
	} else {
		amountValue = parseFloat((value / 100000).toFixed(2)) + ' lakh';
	}

	return amountValue;
}

function formatToINR(amount) {
	var isSymbol = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : true;

	var inputPrice = amount.toString();
	var formattedPrice = '';
	var breakPoint = 3;
	var currencySymbol = isSymbol ? '\u20B9 ' : '';

	for (var i = inputPrice.length - 1; i >= 0; i--) {
		formattedPrice = inputPrice.charAt(i) + formattedPrice;

		if (inputPrice.length - i === breakPoint && inputPrice.length > breakPoint) {
			formattedPrice = ',' + formattedPrice;
			breakPoint += 2;
		}
	}

	return currencySymbol + formattedPrice;
}

function formatValueWithComma(inputField) {
	var fieldValue = inputField.value;

	fieldValue = fieldValue.replace(/[^\d]/g, "").replace(/^0+/, "");
	inputField.setAttribute('data-value', fieldValue);

	return Number(fieldValue);
}

function formatToRound(value) {
	return Math.round(value);
}

module.exports = {
	formatToCurrency: formatToCurrency,
	formatToINR: formatToINR,
	formatValueWithComma: formatValueWithComma,
	formatToRound: formatToRound
};

/***/ }),
/* 16 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0__createBrowserHistory__ = __webpack_require__(96);
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "a", function() { return __WEBPACK_IMPORTED_MODULE_0__createBrowserHistory__["a"]; });
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1__createHashHistory__ = __webpack_require__(99);
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "b", function() { return __WEBPACK_IMPORTED_MODULE_1__createHashHistory__["a"]; });
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2__createMemoryHistory__ = __webpack_require__(100);
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "d", function() { return __WEBPACK_IMPORTED_MODULE_2__createMemoryHistory__["a"]; });
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_3__LocationUtils__ = __webpack_require__(27);
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "c", function() { return __WEBPACK_IMPORTED_MODULE_3__LocationUtils__["a"]; });
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "f", function() { return __WEBPACK_IMPORTED_MODULE_3__LocationUtils__["b"]; });
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_4__PathUtils__ = __webpack_require__(20);
/* unused harmony reexport parsePath */
/* harmony reexport (binding) */ __webpack_require__.d(__webpack_exports__, "e", function() { return __WEBPACK_IMPORTED_MODULE_4__PathUtils__["b"]; });










/***/ }),
/* 17 */
/***/ (function(module, exports, __webpack_require__) {

/**
 * Copyright (c) 2014-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */

(function (global, factory) {
   true ? module.exports = factory() :
  typeof define === 'function' && define.amd ? define(factory) :
  (global.Immutable = factory());
}(this, function () { 'use strict';var SLICE$0 = Array.prototype.slice;

  function createClass(ctor, superClass) {
    if (superClass) {
      ctor.prototype = Object.create(superClass.prototype);
    }
    ctor.prototype.constructor = ctor;
  }

  function Iterable(value) {
      return isIterable(value) ? value : Seq(value);
    }


  createClass(KeyedIterable, Iterable);
    function KeyedIterable(value) {
      return isKeyed(value) ? value : KeyedSeq(value);
    }


  createClass(IndexedIterable, Iterable);
    function IndexedIterable(value) {
      return isIndexed(value) ? value : IndexedSeq(value);
    }


  createClass(SetIterable, Iterable);
    function SetIterable(value) {
      return isIterable(value) && !isAssociative(value) ? value : SetSeq(value);
    }



  function isIterable(maybeIterable) {
    return !!(maybeIterable && maybeIterable[IS_ITERABLE_SENTINEL]);
  }

  function isKeyed(maybeKeyed) {
    return !!(maybeKeyed && maybeKeyed[IS_KEYED_SENTINEL]);
  }

  function isIndexed(maybeIndexed) {
    return !!(maybeIndexed && maybeIndexed[IS_INDEXED_SENTINEL]);
  }

  function isAssociative(maybeAssociative) {
    return isKeyed(maybeAssociative) || isIndexed(maybeAssociative);
  }

  function isOrdered(maybeOrdered) {
    return !!(maybeOrdered && maybeOrdered[IS_ORDERED_SENTINEL]);
  }

  Iterable.isIterable = isIterable;
  Iterable.isKeyed = isKeyed;
  Iterable.isIndexed = isIndexed;
  Iterable.isAssociative = isAssociative;
  Iterable.isOrdered = isOrdered;

  Iterable.Keyed = KeyedIterable;
  Iterable.Indexed = IndexedIterable;
  Iterable.Set = SetIterable;


  var IS_ITERABLE_SENTINEL = '@@__IMMUTABLE_ITERABLE__@@';
  var IS_KEYED_SENTINEL = '@@__IMMUTABLE_KEYED__@@';
  var IS_INDEXED_SENTINEL = '@@__IMMUTABLE_INDEXED__@@';
  var IS_ORDERED_SENTINEL = '@@__IMMUTABLE_ORDERED__@@';

  // Used for setting prototype methods that IE8 chokes on.
  var DELETE = 'delete';

  // Constants describing the size of trie nodes.
  var SHIFT = 5; // Resulted in best performance after ______?
  var SIZE = 1 << SHIFT;
  var MASK = SIZE - 1;

  // A consistent shared value representing "not set" which equals nothing other
  // than itself, and nothing that could be provided externally.
  var NOT_SET = {};

  // Boolean references, Rough equivalent of `bool &`.
  var CHANGE_LENGTH = { value: false };
  var DID_ALTER = { value: false };

  function MakeRef(ref) {
    ref.value = false;
    return ref;
  }

  function SetRef(ref) {
    ref && (ref.value = true);
  }

  // A function which returns a value representing an "owner" for transient writes
  // to tries. The return value will only ever equal itself, and will not equal
  // the return of any subsequent call of this function.
  function OwnerID() {}

  // http://jsperf.com/copy-array-inline
  function arrCopy(arr, offset) {
    offset = offset || 0;
    var len = Math.max(0, arr.length - offset);
    var newArr = new Array(len);
    for (var ii = 0; ii < len; ii++) {
      newArr[ii] = arr[ii + offset];
    }
    return newArr;
  }

  function ensureSize(iter) {
    if (iter.size === undefined) {
      iter.size = iter.__iterate(returnTrue);
    }
    return iter.size;
  }

  function wrapIndex(iter, index) {
    // This implements "is array index" which the ECMAString spec defines as:
    //
    //     A String property name P is an array index if and only if
    //     ToString(ToUint32(P)) is equal to P and ToUint32(P) is not equal
    //     to 2^32−1.
    //
    // http://www.ecma-international.org/ecma-262/6.0/#sec-array-exotic-objects
    if (typeof index !== 'number') {
      var uint32Index = index >>> 0; // N >>> 0 is shorthand for ToUint32
      if ('' + uint32Index !== index || uint32Index === 4294967295) {
        return NaN;
      }
      index = uint32Index;
    }
    return index < 0 ? ensureSize(iter) + index : index;
  }

  function returnTrue() {
    return true;
  }

  function wholeSlice(begin, end, size) {
    return (begin === 0 || (size !== undefined && begin <= -size)) &&
      (end === undefined || (size !== undefined && end >= size));
  }

  function resolveBegin(begin, size) {
    return resolveIndex(begin, size, 0);
  }

  function resolveEnd(end, size) {
    return resolveIndex(end, size, size);
  }

  function resolveIndex(index, size, defaultIndex) {
    return index === undefined ?
      defaultIndex :
      index < 0 ?
        Math.max(0, size + index) :
        size === undefined ?
          index :
          Math.min(size, index);
  }

  /* global Symbol */

  var ITERATE_KEYS = 0;
  var ITERATE_VALUES = 1;
  var ITERATE_ENTRIES = 2;

  var REAL_ITERATOR_SYMBOL = typeof Symbol === 'function' && Symbol.iterator;
  var FAUX_ITERATOR_SYMBOL = '@@iterator';

  var ITERATOR_SYMBOL = REAL_ITERATOR_SYMBOL || FAUX_ITERATOR_SYMBOL;


  function Iterator(next) {
      this.next = next;
    }

    Iterator.prototype.toString = function() {
      return '[Iterator]';
    };


  Iterator.KEYS = ITERATE_KEYS;
  Iterator.VALUES = ITERATE_VALUES;
  Iterator.ENTRIES = ITERATE_ENTRIES;

  Iterator.prototype.inspect =
  Iterator.prototype.toSource = function () { return this.toString(); }
  Iterator.prototype[ITERATOR_SYMBOL] = function () {
    return this;
  };


  function iteratorValue(type, k, v, iteratorResult) {
    var value = type === 0 ? k : type === 1 ? v : [k, v];
    iteratorResult ? (iteratorResult.value = value) : (iteratorResult = {
      value: value, done: false
    });
    return iteratorResult;
  }

  function iteratorDone() {
    return { value: undefined, done: true };
  }

  function hasIterator(maybeIterable) {
    return !!getIteratorFn(maybeIterable);
  }

  function isIterator(maybeIterator) {
    return maybeIterator && typeof maybeIterator.next === 'function';
  }

  function getIterator(iterable) {
    var iteratorFn = getIteratorFn(iterable);
    return iteratorFn && iteratorFn.call(iterable);
  }

  function getIteratorFn(iterable) {
    var iteratorFn = iterable && (
      (REAL_ITERATOR_SYMBOL && iterable[REAL_ITERATOR_SYMBOL]) ||
      iterable[FAUX_ITERATOR_SYMBOL]
    );
    if (typeof iteratorFn === 'function') {
      return iteratorFn;
    }
  }

  function isArrayLike(value) {
    return value && typeof value.length === 'number';
  }

  createClass(Seq, Iterable);
    function Seq(value) {
      return value === null || value === undefined ? emptySequence() :
        isIterable(value) ? value.toSeq() : seqFromValue(value);
    }

    Seq.of = function(/*...values*/) {
      return Seq(arguments);
    };

    Seq.prototype.toSeq = function() {
      return this;
    };

    Seq.prototype.toString = function() {
      return this.__toString('Seq {', '}');
    };

    Seq.prototype.cacheResult = function() {
      if (!this._cache && this.__iterateUncached) {
        this._cache = this.entrySeq().toArray();
        this.size = this._cache.length;
      }
      return this;
    };

    // abstract __iterateUncached(fn, reverse)

    Seq.prototype.__iterate = function(fn, reverse) {
      return seqIterate(this, fn, reverse, true);
    };

    // abstract __iteratorUncached(type, reverse)

    Seq.prototype.__iterator = function(type, reverse) {
      return seqIterator(this, type, reverse, true);
    };



  createClass(KeyedSeq, Seq);
    function KeyedSeq(value) {
      return value === null || value === undefined ?
        emptySequence().toKeyedSeq() :
        isIterable(value) ?
          (isKeyed(value) ? value.toSeq() : value.fromEntrySeq()) :
          keyedSeqFromValue(value);
    }

    KeyedSeq.prototype.toKeyedSeq = function() {
      return this;
    };



  createClass(IndexedSeq, Seq);
    function IndexedSeq(value) {
      return value === null || value === undefined ? emptySequence() :
        !isIterable(value) ? indexedSeqFromValue(value) :
        isKeyed(value) ? value.entrySeq() : value.toIndexedSeq();
    }

    IndexedSeq.of = function(/*...values*/) {
      return IndexedSeq(arguments);
    };

    IndexedSeq.prototype.toIndexedSeq = function() {
      return this;
    };

    IndexedSeq.prototype.toString = function() {
      return this.__toString('Seq [', ']');
    };

    IndexedSeq.prototype.__iterate = function(fn, reverse) {
      return seqIterate(this, fn, reverse, false);
    };

    IndexedSeq.prototype.__iterator = function(type, reverse) {
      return seqIterator(this, type, reverse, false);
    };



  createClass(SetSeq, Seq);
    function SetSeq(value) {
      return (
        value === null || value === undefined ? emptySequence() :
        !isIterable(value) ? indexedSeqFromValue(value) :
        isKeyed(value) ? value.entrySeq() : value
      ).toSetSeq();
    }

    SetSeq.of = function(/*...values*/) {
      return SetSeq(arguments);
    };

    SetSeq.prototype.toSetSeq = function() {
      return this;
    };



  Seq.isSeq = isSeq;
  Seq.Keyed = KeyedSeq;
  Seq.Set = SetSeq;
  Seq.Indexed = IndexedSeq;

  var IS_SEQ_SENTINEL = '@@__IMMUTABLE_SEQ__@@';

  Seq.prototype[IS_SEQ_SENTINEL] = true;



  createClass(ArraySeq, IndexedSeq);
    function ArraySeq(array) {
      this._array = array;
      this.size = array.length;
    }

    ArraySeq.prototype.get = function(index, notSetValue) {
      return this.has(index) ? this._array[wrapIndex(this, index)] : notSetValue;
    };

    ArraySeq.prototype.__iterate = function(fn, reverse) {
      var array = this._array;
      var maxIndex = array.length - 1;
      for (var ii = 0; ii <= maxIndex; ii++) {
        if (fn(array[reverse ? maxIndex - ii : ii], ii, this) === false) {
          return ii + 1;
        }
      }
      return ii;
    };

    ArraySeq.prototype.__iterator = function(type, reverse) {
      var array = this._array;
      var maxIndex = array.length - 1;
      var ii = 0;
      return new Iterator(function() 
        {return ii > maxIndex ?
          iteratorDone() :
          iteratorValue(type, ii, array[reverse ? maxIndex - ii++ : ii++])}
      );
    };



  createClass(ObjectSeq, KeyedSeq);
    function ObjectSeq(object) {
      var keys = Object.keys(object);
      this._object = object;
      this._keys = keys;
      this.size = keys.length;
    }

    ObjectSeq.prototype.get = function(key, notSetValue) {
      if (notSetValue !== undefined && !this.has(key)) {
        return notSetValue;
      }
      return this._object[key];
    };

    ObjectSeq.prototype.has = function(key) {
      return this._object.hasOwnProperty(key);
    };

    ObjectSeq.prototype.__iterate = function(fn, reverse) {
      var object = this._object;
      var keys = this._keys;
      var maxIndex = keys.length - 1;
      for (var ii = 0; ii <= maxIndex; ii++) {
        var key = keys[reverse ? maxIndex - ii : ii];
        if (fn(object[key], key, this) === false) {
          return ii + 1;
        }
      }
      return ii;
    };

    ObjectSeq.prototype.__iterator = function(type, reverse) {
      var object = this._object;
      var keys = this._keys;
      var maxIndex = keys.length - 1;
      var ii = 0;
      return new Iterator(function()  {
        var key = keys[reverse ? maxIndex - ii : ii];
        return ii++ > maxIndex ?
          iteratorDone() :
          iteratorValue(type, key, object[key]);
      });
    };

  ObjectSeq.prototype[IS_ORDERED_SENTINEL] = true;


  createClass(IterableSeq, IndexedSeq);
    function IterableSeq(iterable) {
      this._iterable = iterable;
      this.size = iterable.length || iterable.size;
    }

    IterableSeq.prototype.__iterateUncached = function(fn, reverse) {
      if (reverse) {
        return this.cacheResult().__iterate(fn, reverse);
      }
      var iterable = this._iterable;
      var iterator = getIterator(iterable);
      var iterations = 0;
      if (isIterator(iterator)) {
        var step;
        while (!(step = iterator.next()).done) {
          if (fn(step.value, iterations++, this) === false) {
            break;
          }
        }
      }
      return iterations;
    };

    IterableSeq.prototype.__iteratorUncached = function(type, reverse) {
      if (reverse) {
        return this.cacheResult().__iterator(type, reverse);
      }
      var iterable = this._iterable;
      var iterator = getIterator(iterable);
      if (!isIterator(iterator)) {
        return new Iterator(iteratorDone);
      }
      var iterations = 0;
      return new Iterator(function()  {
        var step = iterator.next();
        return step.done ? step : iteratorValue(type, iterations++, step.value);
      });
    };



  createClass(IteratorSeq, IndexedSeq);
    function IteratorSeq(iterator) {
      this._iterator = iterator;
      this._iteratorCache = [];
    }

    IteratorSeq.prototype.__iterateUncached = function(fn, reverse) {
      if (reverse) {
        return this.cacheResult().__iterate(fn, reverse);
      }
      var iterator = this._iterator;
      var cache = this._iteratorCache;
      var iterations = 0;
      while (iterations < cache.length) {
        if (fn(cache[iterations], iterations++, this) === false) {
          return iterations;
        }
      }
      var step;
      while (!(step = iterator.next()).done) {
        var val = step.value;
        cache[iterations] = val;
        if (fn(val, iterations++, this) === false) {
          break;
        }
      }
      return iterations;
    };

    IteratorSeq.prototype.__iteratorUncached = function(type, reverse) {
      if (reverse) {
        return this.cacheResult().__iterator(type, reverse);
      }
      var iterator = this._iterator;
      var cache = this._iteratorCache;
      var iterations = 0;
      return new Iterator(function()  {
        if (iterations >= cache.length) {
          var step = iterator.next();
          if (step.done) {
            return step;
          }
          cache[iterations] = step.value;
        }
        return iteratorValue(type, iterations, cache[iterations++]);
      });
    };




  // # pragma Helper functions

  function isSeq(maybeSeq) {
    return !!(maybeSeq && maybeSeq[IS_SEQ_SENTINEL]);
  }

  var EMPTY_SEQ;

  function emptySequence() {
    return EMPTY_SEQ || (EMPTY_SEQ = new ArraySeq([]));
  }

  function keyedSeqFromValue(value) {
    var seq =
      Array.isArray(value) ? new ArraySeq(value).fromEntrySeq() :
      isIterator(value) ? new IteratorSeq(value).fromEntrySeq() :
      hasIterator(value) ? new IterableSeq(value).fromEntrySeq() :
      typeof value === 'object' ? new ObjectSeq(value) :
      undefined;
    if (!seq) {
      throw new TypeError(
        'Expected Array or iterable object of [k, v] entries, '+
        'or keyed object: ' + value
      );
    }
    return seq;
  }

  function indexedSeqFromValue(value) {
    var seq = maybeIndexedSeqFromValue(value);
    if (!seq) {
      throw new TypeError(
        'Expected Array or iterable object of values: ' + value
      );
    }
    return seq;
  }

  function seqFromValue(value) {
    var seq = maybeIndexedSeqFromValue(value) ||
      (typeof value === 'object' && new ObjectSeq(value));
    if (!seq) {
      throw new TypeError(
        'Expected Array or iterable object of values, or keyed object: ' + value
      );
    }
    return seq;
  }

  function maybeIndexedSeqFromValue(value) {
    return (
      isArrayLike(value) ? new ArraySeq(value) :
      isIterator(value) ? new IteratorSeq(value) :
      hasIterator(value) ? new IterableSeq(value) :
      undefined
    );
  }

  function seqIterate(seq, fn, reverse, useKeys) {
    var cache = seq._cache;
    if (cache) {
      var maxIndex = cache.length - 1;
      for (var ii = 0; ii <= maxIndex; ii++) {
        var entry = cache[reverse ? maxIndex - ii : ii];
        if (fn(entry[1], useKeys ? entry[0] : ii, seq) === false) {
          return ii + 1;
        }
      }
      return ii;
    }
    return seq.__iterateUncached(fn, reverse);
  }

  function seqIterator(seq, type, reverse, useKeys) {
    var cache = seq._cache;
    if (cache) {
      var maxIndex = cache.length - 1;
      var ii = 0;
      return new Iterator(function()  {
        var entry = cache[reverse ? maxIndex - ii : ii];
        return ii++ > maxIndex ?
          iteratorDone() :
          iteratorValue(type, useKeys ? entry[0] : ii - 1, entry[1]);
      });
    }
    return seq.__iteratorUncached(type, reverse);
  }

  function fromJS(json, converter) {
    return converter ?
      fromJSWith(converter, json, '', {'': json}) :
      fromJSDefault(json);
  }

  function fromJSWith(converter, json, key, parentJSON) {
    if (Array.isArray(json)) {
      return converter.call(parentJSON, key, IndexedSeq(json).map(function(v, k)  {return fromJSWith(converter, v, k, json)}));
    }
    if (isPlainObj(json)) {
      return converter.call(parentJSON, key, KeyedSeq(json).map(function(v, k)  {return fromJSWith(converter, v, k, json)}));
    }
    return json;
  }

  function fromJSDefault(json) {
    if (Array.isArray(json)) {
      return IndexedSeq(json).map(fromJSDefault).toList();
    }
    if (isPlainObj(json)) {
      return KeyedSeq(json).map(fromJSDefault).toMap();
    }
    return json;
  }

  function isPlainObj(value) {
    return value && (value.constructor === Object || value.constructor === undefined);
  }

  /**
   * An extension of the "same-value" algorithm as [described for use by ES6 Map
   * and Set](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Map#Key_equality)
   *
   * NaN is considered the same as NaN, however -0 and 0 are considered the same
   * value, which is different from the algorithm described by
   * [`Object.is`](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Object/is).
   *
   * This is extended further to allow Objects to describe the values they
   * represent, by way of `valueOf` or `equals` (and `hashCode`).
   *
   * Note: because of this extension, the key equality of Immutable.Map and the
   * value equality of Immutable.Set will differ from ES6 Map and Set.
   *
   * ### Defining custom values
   *
   * The easiest way to describe the value an object represents is by implementing
   * `valueOf`. For example, `Date` represents a value by returning a unix
   * timestamp for `valueOf`:
   *
   *     var date1 = new Date(1234567890000); // Fri Feb 13 2009 ...
   *     var date2 = new Date(1234567890000);
   *     date1.valueOf(); // 1234567890000
   *     assert( date1 !== date2 );
   *     assert( Immutable.is( date1, date2 ) );
   *
   * Note: overriding `valueOf` may have other implications if you use this object
   * where JavaScript expects a primitive, such as implicit string coercion.
   *
   * For more complex types, especially collections, implementing `valueOf` may
   * not be performant. An alternative is to implement `equals` and `hashCode`.
   *
   * `equals` takes another object, presumably of similar type, and returns true
   * if the it is equal. Equality is symmetrical, so the same result should be
   * returned if this and the argument are flipped.
   *
   *     assert( a.equals(b) === b.equals(a) );
   *
   * `hashCode` returns a 32bit integer number representing the object which will
   * be used to determine how to store the value object in a Map or Set. You must
   * provide both or neither methods, one must not exist without the other.
   *
   * Also, an important relationship between these methods must be upheld: if two
   * values are equal, they *must* return the same hashCode. If the values are not
   * equal, they might have the same hashCode; this is called a hash collision,
   * and while undesirable for performance reasons, it is acceptable.
   *
   *     if (a.equals(b)) {
   *       assert( a.hashCode() === b.hashCode() );
   *     }
   *
   * All Immutable collections implement `equals` and `hashCode`.
   *
   */
  function is(valueA, valueB) {
    if (valueA === valueB || (valueA !== valueA && valueB !== valueB)) {
      return true;
    }
    if (!valueA || !valueB) {
      return false;
    }
    if (typeof valueA.valueOf === 'function' &&
        typeof valueB.valueOf === 'function') {
      valueA = valueA.valueOf();
      valueB = valueB.valueOf();
      if (valueA === valueB || (valueA !== valueA && valueB !== valueB)) {
        return true;
      }
      if (!valueA || !valueB) {
        return false;
      }
    }
    if (typeof valueA.equals === 'function' &&
        typeof valueB.equals === 'function' &&
        valueA.equals(valueB)) {
      return true;
    }
    return false;
  }

  function deepEqual(a, b) {
    if (a === b) {
      return true;
    }

    if (
      !isIterable(b) ||
      a.size !== undefined && b.size !== undefined && a.size !== b.size ||
      a.__hash !== undefined && b.__hash !== undefined && a.__hash !== b.__hash ||
      isKeyed(a) !== isKeyed(b) ||
      isIndexed(a) !== isIndexed(b) ||
      isOrdered(a) !== isOrdered(b)
    ) {
      return false;
    }

    if (a.size === 0 && b.size === 0) {
      return true;
    }

    var notAssociative = !isAssociative(a);

    if (isOrdered(a)) {
      var entries = a.entries();
      return b.every(function(v, k)  {
        var entry = entries.next().value;
        return entry && is(entry[1], v) && (notAssociative || is(entry[0], k));
      }) && entries.next().done;
    }

    var flipped = false;

    if (a.size === undefined) {
      if (b.size === undefined) {
        if (typeof a.cacheResult === 'function') {
          a.cacheResult();
        }
      } else {
        flipped = true;
        var _ = a;
        a = b;
        b = _;
      }
    }

    var allEqual = true;
    var bSize = b.__iterate(function(v, k)  {
      if (notAssociative ? !a.has(v) :
          flipped ? !is(v, a.get(k, NOT_SET)) : !is(a.get(k, NOT_SET), v)) {
        allEqual = false;
        return false;
      }
    });

    return allEqual && a.size === bSize;
  }

  createClass(Repeat, IndexedSeq);

    function Repeat(value, times) {
      if (!(this instanceof Repeat)) {
        return new Repeat(value, times);
      }
      this._value = value;
      this.size = times === undefined ? Infinity : Math.max(0, times);
      if (this.size === 0) {
        if (EMPTY_REPEAT) {
          return EMPTY_REPEAT;
        }
        EMPTY_REPEAT = this;
      }
    }

    Repeat.prototype.toString = function() {
      if (this.size === 0) {
        return 'Repeat []';
      }
      return 'Repeat [ ' + this._value + ' ' + this.size + ' times ]';
    };

    Repeat.prototype.get = function(index, notSetValue) {
      return this.has(index) ? this._value : notSetValue;
    };

    Repeat.prototype.includes = function(searchValue) {
      return is(this._value, searchValue);
    };

    Repeat.prototype.slice = function(begin, end) {
      var size = this.size;
      return wholeSlice(begin, end, size) ? this :
        new Repeat(this._value, resolveEnd(end, size) - resolveBegin(begin, size));
    };

    Repeat.prototype.reverse = function() {
      return this;
    };

    Repeat.prototype.indexOf = function(searchValue) {
      if (is(this._value, searchValue)) {
        return 0;
      }
      return -1;
    };

    Repeat.prototype.lastIndexOf = function(searchValue) {
      if (is(this._value, searchValue)) {
        return this.size;
      }
      return -1;
    };

    Repeat.prototype.__iterate = function(fn, reverse) {
      for (var ii = 0; ii < this.size; ii++) {
        if (fn(this._value, ii, this) === false) {
          return ii + 1;
        }
      }
      return ii;
    };

    Repeat.prototype.__iterator = function(type, reverse) {var this$0 = this;
      var ii = 0;
      return new Iterator(function() 
        {return ii < this$0.size ? iteratorValue(type, ii++, this$0._value) : iteratorDone()}
      );
    };

    Repeat.prototype.equals = function(other) {
      return other instanceof Repeat ?
        is(this._value, other._value) :
        deepEqual(other);
    };


  var EMPTY_REPEAT;

  function invariant(condition, error) {
    if (!condition) throw new Error(error);
  }

  createClass(Range, IndexedSeq);

    function Range(start, end, step) {
      if (!(this instanceof Range)) {
        return new Range(start, end, step);
      }
      invariant(step !== 0, 'Cannot step a Range by 0');
      start = start || 0;
      if (end === undefined) {
        end = Infinity;
      }
      step = step === undefined ? 1 : Math.abs(step);
      if (end < start) {
        step = -step;
      }
      this._start = start;
      this._end = end;
      this._step = step;
      this.size = Math.max(0, Math.ceil((end - start) / step - 1) + 1);
      if (this.size === 0) {
        if (EMPTY_RANGE) {
          return EMPTY_RANGE;
        }
        EMPTY_RANGE = this;
      }
    }

    Range.prototype.toString = function() {
      if (this.size === 0) {
        return 'Range []';
      }
      return 'Range [ ' +
        this._start + '...' + this._end +
        (this._step !== 1 ? ' by ' + this._step : '') +
      ' ]';
    };

    Range.prototype.get = function(index, notSetValue) {
      return this.has(index) ?
        this._start + wrapIndex(this, index) * this._step :
        notSetValue;
    };

    Range.prototype.includes = function(searchValue) {
      var possibleIndex = (searchValue - this._start) / this._step;
      return possibleIndex >= 0 &&
        possibleIndex < this.size &&
        possibleIndex === Math.floor(possibleIndex);
    };

    Range.prototype.slice = function(begin, end) {
      if (wholeSlice(begin, end, this.size)) {
        return this;
      }
      begin = resolveBegin(begin, this.size);
      end = resolveEnd(end, this.size);
      if (end <= begin) {
        return new Range(0, 0);
      }
      return new Range(this.get(begin, this._end), this.get(end, this._end), this._step);
    };

    Range.prototype.indexOf = function(searchValue) {
      var offsetValue = searchValue - this._start;
      if (offsetValue % this._step === 0) {
        var index = offsetValue / this._step;
        if (index >= 0 && index < this.size) {
          return index
        }
      }
      return -1;
    };

    Range.prototype.lastIndexOf = function(searchValue) {
      return this.indexOf(searchValue);
    };

    Range.prototype.__iterate = function(fn, reverse) {
      var maxIndex = this.size - 1;
      var step = this._step;
      var value = reverse ? this._start + maxIndex * step : this._start;
      for (var ii = 0; ii <= maxIndex; ii++) {
        if (fn(value, ii, this) === false) {
          return ii + 1;
        }
        value += reverse ? -step : step;
      }
      return ii;
    };

    Range.prototype.__iterator = function(type, reverse) {
      var maxIndex = this.size - 1;
      var step = this._step;
      var value = reverse ? this._start + maxIndex * step : this._start;
      var ii = 0;
      return new Iterator(function()  {
        var v = value;
        value += reverse ? -step : step;
        return ii > maxIndex ? iteratorDone() : iteratorValue(type, ii++, v);
      });
    };

    Range.prototype.equals = function(other) {
      return other instanceof Range ?
        this._start === other._start &&
        this._end === other._end &&
        this._step === other._step :
        deepEqual(this, other);
    };


  var EMPTY_RANGE;

  createClass(Collection, Iterable);
    function Collection() {
      throw TypeError('Abstract');
    }


  createClass(KeyedCollection, Collection);function KeyedCollection() {}

  createClass(IndexedCollection, Collection);function IndexedCollection() {}

  createClass(SetCollection, Collection);function SetCollection() {}


  Collection.Keyed = KeyedCollection;
  Collection.Indexed = IndexedCollection;
  Collection.Set = SetCollection;

  var imul =
    typeof Math.imul === 'function' && Math.imul(0xffffffff, 2) === -2 ?
    Math.imul :
    function imul(a, b) {
      a = a | 0; // int
      b = b | 0; // int
      var c = a & 0xffff;
      var d = b & 0xffff;
      // Shift by 0 fixes the sign on the high part.
      return (c * d) + ((((a >>> 16) * d + c * (b >>> 16)) << 16) >>> 0) | 0; // int
    };

  // v8 has an optimization for storing 31-bit signed numbers.
  // Values which have either 00 or 11 as the high order bits qualify.
  // This function drops the highest order bit in a signed number, maintaining
  // the sign bit.
  function smi(i32) {
    return ((i32 >>> 1) & 0x40000000) | (i32 & 0xBFFFFFFF);
  }

  function hash(o) {
    if (o === false || o === null || o === undefined) {
      return 0;
    }
    if (typeof o.valueOf === 'function') {
      o = o.valueOf();
      if (o === false || o === null || o === undefined) {
        return 0;
      }
    }
    if (o === true) {
      return 1;
    }
    var type = typeof o;
    if (type === 'number') {
      if (o !== o || o === Infinity) {
        return 0;
      }
      var h = o | 0;
      if (h !== o) {
        h ^= o * 0xFFFFFFFF;
      }
      while (o > 0xFFFFFFFF) {
        o /= 0xFFFFFFFF;
        h ^= o;
      }
      return smi(h);
    }
    if (type === 'string') {
      return o.length > STRING_HASH_CACHE_MIN_STRLEN ? cachedHashString(o) : hashString(o);
    }
    if (typeof o.hashCode === 'function') {
      return o.hashCode();
    }
    if (type === 'object') {
      return hashJSObj(o);
    }
    if (typeof o.toString === 'function') {
      return hashString(o.toString());
    }
    throw new Error('Value type ' + type + ' cannot be hashed.');
  }

  function cachedHashString(string) {
    var hash = stringHashCache[string];
    if (hash === undefined) {
      hash = hashString(string);
      if (STRING_HASH_CACHE_SIZE === STRING_HASH_CACHE_MAX_SIZE) {
        STRING_HASH_CACHE_SIZE = 0;
        stringHashCache = {};
      }
      STRING_HASH_CACHE_SIZE++;
      stringHashCache[string] = hash;
    }
    return hash;
  }

  // http://jsperf.com/hashing-strings
  function hashString(string) {
    // This is the hash from JVM
    // The hash code for a string is computed as
    // s[0] * 31 ^ (n - 1) + s[1] * 31 ^ (n - 2) + ... + s[n - 1],
    // where s[i] is the ith character of the string and n is the length of
    // the string. We "mod" the result to make it between 0 (inclusive) and 2^31
    // (exclusive) by dropping high bits.
    var hash = 0;
    for (var ii = 0; ii < string.length; ii++) {
      hash = 31 * hash + string.charCodeAt(ii) | 0;
    }
    return smi(hash);
  }

  function hashJSObj(obj) {
    var hash;
    if (usingWeakMap) {
      hash = weakMap.get(obj);
      if (hash !== undefined) {
        return hash;
      }
    }

    hash = obj[UID_HASH_KEY];
    if (hash !== undefined) {
      return hash;
    }

    if (!canDefineProperty) {
      hash = obj.propertyIsEnumerable && obj.propertyIsEnumerable[UID_HASH_KEY];
      if (hash !== undefined) {
        return hash;
      }

      hash = getIENodeHash(obj);
      if (hash !== undefined) {
        return hash;
      }
    }

    hash = ++objHashUID;
    if (objHashUID & 0x40000000) {
      objHashUID = 0;
    }

    if (usingWeakMap) {
      weakMap.set(obj, hash);
    } else if (isExtensible !== undefined && isExtensible(obj) === false) {
      throw new Error('Non-extensible objects are not allowed as keys.');
    } else if (canDefineProperty) {
      Object.defineProperty(obj, UID_HASH_KEY, {
        'enumerable': false,
        'configurable': false,
        'writable': false,
        'value': hash
      });
    } else if (obj.propertyIsEnumerable !== undefined &&
               obj.propertyIsEnumerable === obj.constructor.prototype.propertyIsEnumerable) {
      // Since we can't define a non-enumerable property on the object
      // we'll hijack one of the less-used non-enumerable properties to
      // save our hash on it. Since this is a function it will not show up in
      // `JSON.stringify` which is what we want.
      obj.propertyIsEnumerable = function() {
        return this.constructor.prototype.propertyIsEnumerable.apply(this, arguments);
      };
      obj.propertyIsEnumerable[UID_HASH_KEY] = hash;
    } else if (obj.nodeType !== undefined) {
      // At this point we couldn't get the IE `uniqueID` to use as a hash
      // and we couldn't use a non-enumerable property to exploit the
      // dontEnum bug so we simply add the `UID_HASH_KEY` on the node
      // itself.
      obj[UID_HASH_KEY] = hash;
    } else {
      throw new Error('Unable to set a non-enumerable property on object.');
    }

    return hash;
  }

  // Get references to ES5 object methods.
  var isExtensible = Object.isExtensible;

  // True if Object.defineProperty works as expected. IE8 fails this test.
  var canDefineProperty = (function() {
    try {
      Object.defineProperty({}, '@', {});
      return true;
    } catch (e) {
      return false;
    }
  }());

  // IE has a `uniqueID` property on DOM nodes. We can construct the hash from it
  // and avoid memory leaks from the IE cloneNode bug.
  function getIENodeHash(node) {
    if (node && node.nodeType > 0) {
      switch (node.nodeType) {
        case 1: // Element
          return node.uniqueID;
        case 9: // Document
          return node.documentElement && node.documentElement.uniqueID;
      }
    }
  }

  // If possible, use a WeakMap.
  var usingWeakMap = typeof WeakMap === 'function';
  var weakMap;
  if (usingWeakMap) {
    weakMap = new WeakMap();
  }

  var objHashUID = 0;

  var UID_HASH_KEY = '__immutablehash__';
  if (typeof Symbol === 'function') {
    UID_HASH_KEY = Symbol(UID_HASH_KEY);
  }

  var STRING_HASH_CACHE_MIN_STRLEN = 16;
  var STRING_HASH_CACHE_MAX_SIZE = 255;
  var STRING_HASH_CACHE_SIZE = 0;
  var stringHashCache = {};

  function assertNotInfinite(size) {
    invariant(
      size !== Infinity,
      'Cannot perform this action with an infinite size.'
    );
  }

  createClass(Map, KeyedCollection);

    // @pragma Construction

    function Map(value) {
      return value === null || value === undefined ? emptyMap() :
        isMap(value) && !isOrdered(value) ? value :
        emptyMap().withMutations(function(map ) {
          var iter = KeyedIterable(value);
          assertNotInfinite(iter.size);
          iter.forEach(function(v, k)  {return map.set(k, v)});
        });
    }

    Map.of = function() {var keyValues = SLICE$0.call(arguments, 0);
      return emptyMap().withMutations(function(map ) {
        for (var i = 0; i < keyValues.length; i += 2) {
          if (i + 1 >= keyValues.length) {
            throw new Error('Missing value for key: ' + keyValues[i]);
          }
          map.set(keyValues[i], keyValues[i + 1]);
        }
      });
    };

    Map.prototype.toString = function() {
      return this.__toString('Map {', '}');
    };

    // @pragma Access

    Map.prototype.get = function(k, notSetValue) {
      return this._root ?
        this._root.get(0, undefined, k, notSetValue) :
        notSetValue;
    };

    // @pragma Modification

    Map.prototype.set = function(k, v) {
      return updateMap(this, k, v);
    };

    Map.prototype.setIn = function(keyPath, v) {
      return this.updateIn(keyPath, NOT_SET, function()  {return v});
    };

    Map.prototype.remove = function(k) {
      return updateMap(this, k, NOT_SET);
    };

    Map.prototype.deleteIn = function(keyPath) {
      return this.updateIn(keyPath, function()  {return NOT_SET});
    };

    Map.prototype.update = function(k, notSetValue, updater) {
      return arguments.length === 1 ?
        k(this) :
        this.updateIn([k], notSetValue, updater);
    };

    Map.prototype.updateIn = function(keyPath, notSetValue, updater) {
      if (!updater) {
        updater = notSetValue;
        notSetValue = undefined;
      }
      var updatedValue = updateInDeepMap(
        this,
        forceIterator(keyPath),
        notSetValue,
        updater
      );
      return updatedValue === NOT_SET ? undefined : updatedValue;
    };

    Map.prototype.clear = function() {
      if (this.size === 0) {
        return this;
      }
      if (this.__ownerID) {
        this.size = 0;
        this._root = null;
        this.__hash = undefined;
        this.__altered = true;
        return this;
      }
      return emptyMap();
    };

    // @pragma Composition

    Map.prototype.merge = function(/*...iters*/) {
      return mergeIntoMapWith(this, undefined, arguments);
    };

    Map.prototype.mergeWith = function(merger) {var iters = SLICE$0.call(arguments, 1);
      return mergeIntoMapWith(this, merger, iters);
    };

    Map.prototype.mergeIn = function(keyPath) {var iters = SLICE$0.call(arguments, 1);
      return this.updateIn(
        keyPath,
        emptyMap(),
        function(m ) {return typeof m.merge === 'function' ?
          m.merge.apply(m, iters) :
          iters[iters.length - 1]}
      );
    };

    Map.prototype.mergeDeep = function(/*...iters*/) {
      return mergeIntoMapWith(this, deepMerger, arguments);
    };

    Map.prototype.mergeDeepWith = function(merger) {var iters = SLICE$0.call(arguments, 1);
      return mergeIntoMapWith(this, deepMergerWith(merger), iters);
    };

    Map.prototype.mergeDeepIn = function(keyPath) {var iters = SLICE$0.call(arguments, 1);
      return this.updateIn(
        keyPath,
        emptyMap(),
        function(m ) {return typeof m.mergeDeep === 'function' ?
          m.mergeDeep.apply(m, iters) :
          iters[iters.length - 1]}
      );
    };

    Map.prototype.sort = function(comparator) {
      // Late binding
      return OrderedMap(sortFactory(this, comparator));
    };

    Map.prototype.sortBy = function(mapper, comparator) {
      // Late binding
      return OrderedMap(sortFactory(this, comparator, mapper));
    };

    // @pragma Mutability

    Map.prototype.withMutations = function(fn) {
      var mutable = this.asMutable();
      fn(mutable);
      return mutable.wasAltered() ? mutable.__ensureOwner(this.__ownerID) : this;
    };

    Map.prototype.asMutable = function() {
      return this.__ownerID ? this : this.__ensureOwner(new OwnerID());
    };

    Map.prototype.asImmutable = function() {
      return this.__ensureOwner();
    };

    Map.prototype.wasAltered = function() {
      return this.__altered;
    };

    Map.prototype.__iterator = function(type, reverse) {
      return new MapIterator(this, type, reverse);
    };

    Map.prototype.__iterate = function(fn, reverse) {var this$0 = this;
      var iterations = 0;
      this._root && this._root.iterate(function(entry ) {
        iterations++;
        return fn(entry[1], entry[0], this$0);
      }, reverse);
      return iterations;
    };

    Map.prototype.__ensureOwner = function(ownerID) {
      if (ownerID === this.__ownerID) {
        return this;
      }
      if (!ownerID) {
        this.__ownerID = ownerID;
        this.__altered = false;
        return this;
      }
      return makeMap(this.size, this._root, ownerID, this.__hash);
    };


  function isMap(maybeMap) {
    return !!(maybeMap && maybeMap[IS_MAP_SENTINEL]);
  }

  Map.isMap = isMap;

  var IS_MAP_SENTINEL = '@@__IMMUTABLE_MAP__@@';

  var MapPrototype = Map.prototype;
  MapPrototype[IS_MAP_SENTINEL] = true;
  MapPrototype[DELETE] = MapPrototype.remove;
  MapPrototype.removeIn = MapPrototype.deleteIn;


  // #pragma Trie Nodes



    function ArrayMapNode(ownerID, entries) {
      this.ownerID = ownerID;
      this.entries = entries;
    }

    ArrayMapNode.prototype.get = function(shift, keyHash, key, notSetValue) {
      var entries = this.entries;
      for (var ii = 0, len = entries.length; ii < len; ii++) {
        if (is(key, entries[ii][0])) {
          return entries[ii][1];
        }
      }
      return notSetValue;
    };

    ArrayMapNode.prototype.update = function(ownerID, shift, keyHash, key, value, didChangeSize, didAlter) {
      var removed = value === NOT_SET;

      var entries = this.entries;
      var idx = 0;
      for (var len = entries.length; idx < len; idx++) {
        if (is(key, entries[idx][0])) {
          break;
        }
      }
      var exists = idx < len;

      if (exists ? entries[idx][1] === value : removed) {
        return this;
      }

      SetRef(didAlter);
      (removed || !exists) && SetRef(didChangeSize);

      if (removed && entries.length === 1) {
        return; // undefined
      }

      if (!exists && !removed && entries.length >= MAX_ARRAY_MAP_SIZE) {
        return createNodes(ownerID, entries, key, value);
      }

      var isEditable = ownerID && ownerID === this.ownerID;
      var newEntries = isEditable ? entries : arrCopy(entries);

      if (exists) {
        if (removed) {
          idx === len - 1 ? newEntries.pop() : (newEntries[idx] = newEntries.pop());
        } else {
          newEntries[idx] = [key, value];
        }
      } else {
        newEntries.push([key, value]);
      }

      if (isEditable) {
        this.entries = newEntries;
        return this;
      }

      return new ArrayMapNode(ownerID, newEntries);
    };




    function BitmapIndexedNode(ownerID, bitmap, nodes) {
      this.ownerID = ownerID;
      this.bitmap = bitmap;
      this.nodes = nodes;
    }

    BitmapIndexedNode.prototype.get = function(shift, keyHash, key, notSetValue) {
      if (keyHash === undefined) {
        keyHash = hash(key);
      }
      var bit = (1 << ((shift === 0 ? keyHash : keyHash >>> shift) & MASK));
      var bitmap = this.bitmap;
      return (bitmap & bit) === 0 ? notSetValue :
        this.nodes[popCount(bitmap & (bit - 1))].get(shift + SHIFT, keyHash, key, notSetValue);
    };

    BitmapIndexedNode.prototype.update = function(ownerID, shift, keyHash, key, value, didChangeSize, didAlter) {
      if (keyHash === undefined) {
        keyHash = hash(key);
      }
      var keyHashFrag = (shift === 0 ? keyHash : keyHash >>> shift) & MASK;
      var bit = 1 << keyHashFrag;
      var bitmap = this.bitmap;
      var exists = (bitmap & bit) !== 0;

      if (!exists && value === NOT_SET) {
        return this;
      }

      var idx = popCount(bitmap & (bit - 1));
      var nodes = this.nodes;
      var node = exists ? nodes[idx] : undefined;
      var newNode = updateNode(node, ownerID, shift + SHIFT, keyHash, key, value, didChangeSize, didAlter);

      if (newNode === node) {
        return this;
      }

      if (!exists && newNode && nodes.length >= MAX_BITMAP_INDEXED_SIZE) {
        return expandNodes(ownerID, nodes, bitmap, keyHashFrag, newNode);
      }

      if (exists && !newNode && nodes.length === 2 && isLeafNode(nodes[idx ^ 1])) {
        return nodes[idx ^ 1];
      }

      if (exists && newNode && nodes.length === 1 && isLeafNode(newNode)) {
        return newNode;
      }

      var isEditable = ownerID && ownerID === this.ownerID;
      var newBitmap = exists ? newNode ? bitmap : bitmap ^ bit : bitmap | bit;
      var newNodes = exists ? newNode ?
        setIn(nodes, idx, newNode, isEditable) :
        spliceOut(nodes, idx, isEditable) :
        spliceIn(nodes, idx, newNode, isEditable);

      if (isEditable) {
        this.bitmap = newBitmap;
        this.nodes = newNodes;
        return this;
      }

      return new BitmapIndexedNode(ownerID, newBitmap, newNodes);
    };




    function HashArrayMapNode(ownerID, count, nodes) {
      this.ownerID = ownerID;
      this.count = count;
      this.nodes = nodes;
    }

    HashArrayMapNode.prototype.get = function(shift, keyHash, key, notSetValue) {
      if (keyHash === undefined) {
        keyHash = hash(key);
      }
      var idx = (shift === 0 ? keyHash : keyHash >>> shift) & MASK;
      var node = this.nodes[idx];
      return node ? node.get(shift + SHIFT, keyHash, key, notSetValue) : notSetValue;
    };

    HashArrayMapNode.prototype.update = function(ownerID, shift, keyHash, key, value, didChangeSize, didAlter) {
      if (keyHash === undefined) {
        keyHash = hash(key);
      }
      var idx = (shift === 0 ? keyHash : keyHash >>> shift) & MASK;
      var removed = value === NOT_SET;
      var nodes = this.nodes;
      var node = nodes[idx];

      if (removed && !node) {
        return this;
      }

      var newNode = updateNode(node, ownerID, shift + SHIFT, keyHash, key, value, didChangeSize, didAlter);
      if (newNode === node) {
        return this;
      }

      var newCount = this.count;
      if (!node) {
        newCount++;
      } else if (!newNode) {
        newCount--;
        if (newCount < MIN_HASH_ARRAY_MAP_SIZE) {
          return packNodes(ownerID, nodes, newCount, idx);
        }
      }

      var isEditable = ownerID && ownerID === this.ownerID;
      var newNodes = setIn(nodes, idx, newNode, isEditable);

      if (isEditable) {
        this.count = newCount;
        this.nodes = newNodes;
        return this;
      }

      return new HashArrayMapNode(ownerID, newCount, newNodes);
    };




    function HashCollisionNode(ownerID, keyHash, entries) {
      this.ownerID = ownerID;
      this.keyHash = keyHash;
      this.entries = entries;
    }

    HashCollisionNode.prototype.get = function(shift, keyHash, key, notSetValue) {
      var entries = this.entries;
      for (var ii = 0, len = entries.length; ii < len; ii++) {
        if (is(key, entries[ii][0])) {
          return entries[ii][1];
        }
      }
      return notSetValue;
    };

    HashCollisionNode.prototype.update = function(ownerID, shift, keyHash, key, value, didChangeSize, didAlter) {
      if (keyHash === undefined) {
        keyHash = hash(key);
      }

      var removed = value === NOT_SET;

      if (keyHash !== this.keyHash) {
        if (removed) {
          return this;
        }
        SetRef(didAlter);
        SetRef(didChangeSize);
        return mergeIntoNode(this, ownerID, shift, keyHash, [key, value]);
      }

      var entries = this.entries;
      var idx = 0;
      for (var len = entries.length; idx < len; idx++) {
        if (is(key, entries[idx][0])) {
          break;
        }
      }
      var exists = idx < len;

      if (exists ? entries[idx][1] === value : removed) {
        return this;
      }

      SetRef(didAlter);
      (removed || !exists) && SetRef(didChangeSize);

      if (removed && len === 2) {
        return new ValueNode(ownerID, this.keyHash, entries[idx ^ 1]);
      }

      var isEditable = ownerID && ownerID === this.ownerID;
      var newEntries = isEditable ? entries : arrCopy(entries);

      if (exists) {
        if (removed) {
          idx === len - 1 ? newEntries.pop() : (newEntries[idx] = newEntries.pop());
        } else {
          newEntries[idx] = [key, value];
        }
      } else {
        newEntries.push([key, value]);
      }

      if (isEditable) {
        this.entries = newEntries;
        return this;
      }

      return new HashCollisionNode(ownerID, this.keyHash, newEntries);
    };




    function ValueNode(ownerID, keyHash, entry) {
      this.ownerID = ownerID;
      this.keyHash = keyHash;
      this.entry = entry;
    }

    ValueNode.prototype.get = function(shift, keyHash, key, notSetValue) {
      return is(key, this.entry[0]) ? this.entry[1] : notSetValue;
    };

    ValueNode.prototype.update = function(ownerID, shift, keyHash, key, value, didChangeSize, didAlter) {
      var removed = value === NOT_SET;
      var keyMatch = is(key, this.entry[0]);
      if (keyMatch ? value === this.entry[1] : removed) {
        return this;
      }

      SetRef(didAlter);

      if (removed) {
        SetRef(didChangeSize);
        return; // undefined
      }

      if (keyMatch) {
        if (ownerID && ownerID === this.ownerID) {
          this.entry[1] = value;
          return this;
        }
        return new ValueNode(ownerID, this.keyHash, [key, value]);
      }

      SetRef(didChangeSize);
      return mergeIntoNode(this, ownerID, shift, hash(key), [key, value]);
    };



  // #pragma Iterators

  ArrayMapNode.prototype.iterate =
  HashCollisionNode.prototype.iterate = function (fn, reverse) {
    var entries = this.entries;
    for (var ii = 0, maxIndex = entries.length - 1; ii <= maxIndex; ii++) {
      if (fn(entries[reverse ? maxIndex - ii : ii]) === false) {
        return false;
      }
    }
  }

  BitmapIndexedNode.prototype.iterate =
  HashArrayMapNode.prototype.iterate = function (fn, reverse) {
    var nodes = this.nodes;
    for (var ii = 0, maxIndex = nodes.length - 1; ii <= maxIndex; ii++) {
      var node = nodes[reverse ? maxIndex - ii : ii];
      if (node && node.iterate(fn, reverse) === false) {
        return false;
      }
    }
  }

  ValueNode.prototype.iterate = function (fn, reverse) {
    return fn(this.entry);
  }

  createClass(MapIterator, Iterator);

    function MapIterator(map, type, reverse) {
      this._type = type;
      this._reverse = reverse;
      this._stack = map._root && mapIteratorFrame(map._root);
    }

    MapIterator.prototype.next = function() {
      var type = this._type;
      var stack = this._stack;
      while (stack) {
        var node = stack.node;
        var index = stack.index++;
        var maxIndex;
        if (node.entry) {
          if (index === 0) {
            return mapIteratorValue(type, node.entry);
          }
        } else if (node.entries) {
          maxIndex = node.entries.length - 1;
          if (index <= maxIndex) {
            return mapIteratorValue(type, node.entries[this._reverse ? maxIndex - index : index]);
          }
        } else {
          maxIndex = node.nodes.length - 1;
          if (index <= maxIndex) {
            var subNode = node.nodes[this._reverse ? maxIndex - index : index];
            if (subNode) {
              if (subNode.entry) {
                return mapIteratorValue(type, subNode.entry);
              }
              stack = this._stack = mapIteratorFrame(subNode, stack);
            }
            continue;
          }
        }
        stack = this._stack = this._stack.__prev;
      }
      return iteratorDone();
    };


  function mapIteratorValue(type, entry) {
    return iteratorValue(type, entry[0], entry[1]);
  }

  function mapIteratorFrame(node, prev) {
    return {
      node: node,
      index: 0,
      __prev: prev
    };
  }

  function makeMap(size, root, ownerID, hash) {
    var map = Object.create(MapPrototype);
    map.size = size;
    map._root = root;
    map.__ownerID = ownerID;
    map.__hash = hash;
    map.__altered = false;
    return map;
  }

  var EMPTY_MAP;
  function emptyMap() {
    return EMPTY_MAP || (EMPTY_MAP = makeMap(0));
  }

  function updateMap(map, k, v) {
    var newRoot;
    var newSize;
    if (!map._root) {
      if (v === NOT_SET) {
        return map;
      }
      newSize = 1;
      newRoot = new ArrayMapNode(map.__ownerID, [[k, v]]);
    } else {
      var didChangeSize = MakeRef(CHANGE_LENGTH);
      var didAlter = MakeRef(DID_ALTER);
      newRoot = updateNode(map._root, map.__ownerID, 0, undefined, k, v, didChangeSize, didAlter);
      if (!didAlter.value) {
        return map;
      }
      newSize = map.size + (didChangeSize.value ? v === NOT_SET ? -1 : 1 : 0);
    }
    if (map.__ownerID) {
      map.size = newSize;
      map._root = newRoot;
      map.__hash = undefined;
      map.__altered = true;
      return map;
    }
    return newRoot ? makeMap(newSize, newRoot) : emptyMap();
  }

  function updateNode(node, ownerID, shift, keyHash, key, value, didChangeSize, didAlter) {
    if (!node) {
      if (value === NOT_SET) {
        return node;
      }
      SetRef(didAlter);
      SetRef(didChangeSize);
      return new ValueNode(ownerID, keyHash, [key, value]);
    }
    return node.update(ownerID, shift, keyHash, key, value, didChangeSize, didAlter);
  }

  function isLeafNode(node) {
    return node.constructor === ValueNode || node.constructor === HashCollisionNode;
  }

  function mergeIntoNode(node, ownerID, shift, keyHash, entry) {
    if (node.keyHash === keyHash) {
      return new HashCollisionNode(ownerID, keyHash, [node.entry, entry]);
    }

    var idx1 = (shift === 0 ? node.keyHash : node.keyHash >>> shift) & MASK;
    var idx2 = (shift === 0 ? keyHash : keyHash >>> shift) & MASK;

    var newNode;
    var nodes = idx1 === idx2 ?
      [mergeIntoNode(node, ownerID, shift + SHIFT, keyHash, entry)] :
      ((newNode = new ValueNode(ownerID, keyHash, entry)), idx1 < idx2 ? [node, newNode] : [newNode, node]);

    return new BitmapIndexedNode(ownerID, (1 << idx1) | (1 << idx2), nodes);
  }

  function createNodes(ownerID, entries, key, value) {
    if (!ownerID) {
      ownerID = new OwnerID();
    }
    var node = new ValueNode(ownerID, hash(key), [key, value]);
    for (var ii = 0; ii < entries.length; ii++) {
      var entry = entries[ii];
      node = node.update(ownerID, 0, undefined, entry[0], entry[1]);
    }
    return node;
  }

  function packNodes(ownerID, nodes, count, excluding) {
    var bitmap = 0;
    var packedII = 0;
    var packedNodes = new Array(count);
    for (var ii = 0, bit = 1, len = nodes.length; ii < len; ii++, bit <<= 1) {
      var node = nodes[ii];
      if (node !== undefined && ii !== excluding) {
        bitmap |= bit;
        packedNodes[packedII++] = node;
      }
    }
    return new BitmapIndexedNode(ownerID, bitmap, packedNodes);
  }

  function expandNodes(ownerID, nodes, bitmap, including, node) {
    var count = 0;
    var expandedNodes = new Array(SIZE);
    for (var ii = 0; bitmap !== 0; ii++, bitmap >>>= 1) {
      expandedNodes[ii] = bitmap & 1 ? nodes[count++] : undefined;
    }
    expandedNodes[including] = node;
    return new HashArrayMapNode(ownerID, count + 1, expandedNodes);
  }

  function mergeIntoMapWith(map, merger, iterables) {
    var iters = [];
    for (var ii = 0; ii < iterables.length; ii++) {
      var value = iterables[ii];
      var iter = KeyedIterable(value);
      if (!isIterable(value)) {
        iter = iter.map(function(v ) {return fromJS(v)});
      }
      iters.push(iter);
    }
    return mergeIntoCollectionWith(map, merger, iters);
  }

  function deepMerger(existing, value, key) {
    return existing && existing.mergeDeep && isIterable(value) ?
      existing.mergeDeep(value) :
      is(existing, value) ? existing : value;
  }

  function deepMergerWith(merger) {
    return function(existing, value, key)  {
      if (existing && existing.mergeDeepWith && isIterable(value)) {
        return existing.mergeDeepWith(merger, value);
      }
      var nextValue = merger(existing, value, key);
      return is(existing, nextValue) ? existing : nextValue;
    };
  }

  function mergeIntoCollectionWith(collection, merger, iters) {
    iters = iters.filter(function(x ) {return x.size !== 0});
    if (iters.length === 0) {
      return collection;
    }
    if (collection.size === 0 && !collection.__ownerID && iters.length === 1) {
      return collection.constructor(iters[0]);
    }
    return collection.withMutations(function(collection ) {
      var mergeIntoMap = merger ?
        function(value, key)  {
          collection.update(key, NOT_SET, function(existing )
            {return existing === NOT_SET ? value : merger(existing, value, key)}
          );
        } :
        function(value, key)  {
          collection.set(key, value);
        }
      for (var ii = 0; ii < iters.length; ii++) {
        iters[ii].forEach(mergeIntoMap);
      }
    });
  }

  function updateInDeepMap(existing, keyPathIter, notSetValue, updater) {
    var isNotSet = existing === NOT_SET;
    var step = keyPathIter.next();
    if (step.done) {
      var existingValue = isNotSet ? notSetValue : existing;
      var newValue = updater(existingValue);
      return newValue === existingValue ? existing : newValue;
    }
    invariant(
      isNotSet || (existing && existing.set),
      'invalid keyPath'
    );
    var key = step.value;
    var nextExisting = isNotSet ? NOT_SET : existing.get(key, NOT_SET);
    var nextUpdated = updateInDeepMap(
      nextExisting,
      keyPathIter,
      notSetValue,
      updater
    );
    return nextUpdated === nextExisting ? existing :
      nextUpdated === NOT_SET ? existing.remove(key) :
      (isNotSet ? emptyMap() : existing).set(key, nextUpdated);
  }

  function popCount(x) {
    x = x - ((x >> 1) & 0x55555555);
    x = (x & 0x33333333) + ((x >> 2) & 0x33333333);
    x = (x + (x >> 4)) & 0x0f0f0f0f;
    x = x + (x >> 8);
    x = x + (x >> 16);
    return x & 0x7f;
  }

  function setIn(array, idx, val, canEdit) {
    var newArray = canEdit ? array : arrCopy(array);
    newArray[idx] = val;
    return newArray;
  }

  function spliceIn(array, idx, val, canEdit) {
    var newLen = array.length + 1;
    if (canEdit && idx + 1 === newLen) {
      array[idx] = val;
      return array;
    }
    var newArray = new Array(newLen);
    var after = 0;
    for (var ii = 0; ii < newLen; ii++) {
      if (ii === idx) {
        newArray[ii] = val;
        after = -1;
      } else {
        newArray[ii] = array[ii + after];
      }
    }
    return newArray;
  }

  function spliceOut(array, idx, canEdit) {
    var newLen = array.length - 1;
    if (canEdit && idx === newLen) {
      array.pop();
      return array;
    }
    var newArray = new Array(newLen);
    var after = 0;
    for (var ii = 0; ii < newLen; ii++) {
      if (ii === idx) {
        after = 1;
      }
      newArray[ii] = array[ii + after];
    }
    return newArray;
  }

  var MAX_ARRAY_MAP_SIZE = SIZE / 4;
  var MAX_BITMAP_INDEXED_SIZE = SIZE / 2;
  var MIN_HASH_ARRAY_MAP_SIZE = SIZE / 4;

  createClass(List, IndexedCollection);

    // @pragma Construction

    function List(value) {
      var empty = emptyList();
      if (value === null || value === undefined) {
        return empty;
      }
      if (isList(value)) {
        return value;
      }
      var iter = IndexedIterable(value);
      var size = iter.size;
      if (size === 0) {
        return empty;
      }
      assertNotInfinite(size);
      if (size > 0 && size < SIZE) {
        return makeList(0, size, SHIFT, null, new VNode(iter.toArray()));
      }
      return empty.withMutations(function(list ) {
        list.setSize(size);
        iter.forEach(function(v, i)  {return list.set(i, v)});
      });
    }

    List.of = function(/*...values*/) {
      return this(arguments);
    };

    List.prototype.toString = function() {
      return this.__toString('List [', ']');
    };

    // @pragma Access

    List.prototype.get = function(index, notSetValue) {
      index = wrapIndex(this, index);
      if (index >= 0 && index < this.size) {
        index += this._origin;
        var node = listNodeFor(this, index);
        return node && node.array[index & MASK];
      }
      return notSetValue;
    };

    // @pragma Modification

    List.prototype.set = function(index, value) {
      return updateList(this, index, value);
    };

    List.prototype.remove = function(index) {
      return !this.has(index) ? this :
        index === 0 ? this.shift() :
        index === this.size - 1 ? this.pop() :
        this.splice(index, 1);
    };

    List.prototype.insert = function(index, value) {
      return this.splice(index, 0, value);
    };

    List.prototype.clear = function() {
      if (this.size === 0) {
        return this;
      }
      if (this.__ownerID) {
        this.size = this._origin = this._capacity = 0;
        this._level = SHIFT;
        this._root = this._tail = null;
        this.__hash = undefined;
        this.__altered = true;
        return this;
      }
      return emptyList();
    };

    List.prototype.push = function(/*...values*/) {
      var values = arguments;
      var oldSize = this.size;
      return this.withMutations(function(list ) {
        setListBounds(list, 0, oldSize + values.length);
        for (var ii = 0; ii < values.length; ii++) {
          list.set(oldSize + ii, values[ii]);
        }
      });
    };

    List.prototype.pop = function() {
      return setListBounds(this, 0, -1);
    };

    List.prototype.unshift = function(/*...values*/) {
      var values = arguments;
      return this.withMutations(function(list ) {
        setListBounds(list, -values.length);
        for (var ii = 0; ii < values.length; ii++) {
          list.set(ii, values[ii]);
        }
      });
    };

    List.prototype.shift = function() {
      return setListBounds(this, 1);
    };

    // @pragma Composition

    List.prototype.merge = function(/*...iters*/) {
      return mergeIntoListWith(this, undefined, arguments);
    };

    List.prototype.mergeWith = function(merger) {var iters = SLICE$0.call(arguments, 1);
      return mergeIntoListWith(this, merger, iters);
    };

    List.prototype.mergeDeep = function(/*...iters*/) {
      return mergeIntoListWith(this, deepMerger, arguments);
    };

    List.prototype.mergeDeepWith = function(merger) {var iters = SLICE$0.call(arguments, 1);
      return mergeIntoListWith(this, deepMergerWith(merger), iters);
    };

    List.prototype.setSize = function(size) {
      return setListBounds(this, 0, size);
    };

    // @pragma Iteration

    List.prototype.slice = function(begin, end) {
      var size = this.size;
      if (wholeSlice(begin, end, size)) {
        return this;
      }
      return setListBounds(
        this,
        resolveBegin(begin, size),
        resolveEnd(end, size)
      );
    };

    List.prototype.__iterator = function(type, reverse) {
      var index = 0;
      var values = iterateList(this, reverse);
      return new Iterator(function()  {
        var value = values();
        return value === DONE ?
          iteratorDone() :
          iteratorValue(type, index++, value);
      });
    };

    List.prototype.__iterate = function(fn, reverse) {
      var index = 0;
      var values = iterateList(this, reverse);
      var value;
      while ((value = values()) !== DONE) {
        if (fn(value, index++, this) === false) {
          break;
        }
      }
      return index;
    };

    List.prototype.__ensureOwner = function(ownerID) {
      if (ownerID === this.__ownerID) {
        return this;
      }
      if (!ownerID) {
        this.__ownerID = ownerID;
        return this;
      }
      return makeList(this._origin, this._capacity, this._level, this._root, this._tail, ownerID, this.__hash);
    };


  function isList(maybeList) {
    return !!(maybeList && maybeList[IS_LIST_SENTINEL]);
  }

  List.isList = isList;

  var IS_LIST_SENTINEL = '@@__IMMUTABLE_LIST__@@';

  var ListPrototype = List.prototype;
  ListPrototype[IS_LIST_SENTINEL] = true;
  ListPrototype[DELETE] = ListPrototype.remove;
  ListPrototype.setIn = MapPrototype.setIn;
  ListPrototype.deleteIn =
  ListPrototype.removeIn = MapPrototype.removeIn;
  ListPrototype.update = MapPrototype.update;
  ListPrototype.updateIn = MapPrototype.updateIn;
  ListPrototype.mergeIn = MapPrototype.mergeIn;
  ListPrototype.mergeDeepIn = MapPrototype.mergeDeepIn;
  ListPrototype.withMutations = MapPrototype.withMutations;
  ListPrototype.asMutable = MapPrototype.asMutable;
  ListPrototype.asImmutable = MapPrototype.asImmutable;
  ListPrototype.wasAltered = MapPrototype.wasAltered;



    function VNode(array, ownerID) {
      this.array = array;
      this.ownerID = ownerID;
    }

    // TODO: seems like these methods are very similar

    VNode.prototype.removeBefore = function(ownerID, level, index) {
      if (index === level ? 1 << level : 0 || this.array.length === 0) {
        return this;
      }
      var originIndex = (index >>> level) & MASK;
      if (originIndex >= this.array.length) {
        return new VNode([], ownerID);
      }
      var removingFirst = originIndex === 0;
      var newChild;
      if (level > 0) {
        var oldChild = this.array[originIndex];
        newChild = oldChild && oldChild.removeBefore(ownerID, level - SHIFT, index);
        if (newChild === oldChild && removingFirst) {
          return this;
        }
      }
      if (removingFirst && !newChild) {
        return this;
      }
      var editable = editableVNode(this, ownerID);
      if (!removingFirst) {
        for (var ii = 0; ii < originIndex; ii++) {
          editable.array[ii] = undefined;
        }
      }
      if (newChild) {
        editable.array[originIndex] = newChild;
      }
      return editable;
    };

    VNode.prototype.removeAfter = function(ownerID, level, index) {
      if (index === (level ? 1 << level : 0) || this.array.length === 0) {
        return this;
      }
      var sizeIndex = ((index - 1) >>> level) & MASK;
      if (sizeIndex >= this.array.length) {
        return this;
      }

      var newChild;
      if (level > 0) {
        var oldChild = this.array[sizeIndex];
        newChild = oldChild && oldChild.removeAfter(ownerID, level - SHIFT, index);
        if (newChild === oldChild && sizeIndex === this.array.length - 1) {
          return this;
        }
      }

      var editable = editableVNode(this, ownerID);
      editable.array.splice(sizeIndex + 1);
      if (newChild) {
        editable.array[sizeIndex] = newChild;
      }
      return editable;
    };



  var DONE = {};

  function iterateList(list, reverse) {
    var left = list._origin;
    var right = list._capacity;
    var tailPos = getTailOffset(right);
    var tail = list._tail;

    return iterateNodeOrLeaf(list._root, list._level, 0);

    function iterateNodeOrLeaf(node, level, offset) {
      return level === 0 ?
        iterateLeaf(node, offset) :
        iterateNode(node, level, offset);
    }

    function iterateLeaf(node, offset) {
      var array = offset === tailPos ? tail && tail.array : node && node.array;
      var from = offset > left ? 0 : left - offset;
      var to = right - offset;
      if (to > SIZE) {
        to = SIZE;
      }
      return function()  {
        if (from === to) {
          return DONE;
        }
        var idx = reverse ? --to : from++;
        return array && array[idx];
      };
    }

    function iterateNode(node, level, offset) {
      var values;
      var array = node && node.array;
      var from = offset > left ? 0 : (left - offset) >> level;
      var to = ((right - offset) >> level) + 1;
      if (to > SIZE) {
        to = SIZE;
      }
      return function()  {
        do {
          if (values) {
            var value = values();
            if (value !== DONE) {
              return value;
            }
            values = null;
          }
          if (from === to) {
            return DONE;
          }
          var idx = reverse ? --to : from++;
          values = iterateNodeOrLeaf(
            array && array[idx], level - SHIFT, offset + (idx << level)
          );
        } while (true);
      };
    }
  }

  function makeList(origin, capacity, level, root, tail, ownerID, hash) {
    var list = Object.create(ListPrototype);
    list.size = capacity - origin;
    list._origin = origin;
    list._capacity = capacity;
    list._level = level;
    list._root = root;
    list._tail = tail;
    list.__ownerID = ownerID;
    list.__hash = hash;
    list.__altered = false;
    return list;
  }

  var EMPTY_LIST;
  function emptyList() {
    return EMPTY_LIST || (EMPTY_LIST = makeList(0, 0, SHIFT));
  }

  function updateList(list, index, value) {
    index = wrapIndex(list, index);

    if (index !== index) {
      return list;
    }

    if (index >= list.size || index < 0) {
      return list.withMutations(function(list ) {
        index < 0 ?
          setListBounds(list, index).set(0, value) :
          setListBounds(list, 0, index + 1).set(index, value)
      });
    }

    index += list._origin;

    var newTail = list._tail;
    var newRoot = list._root;
    var didAlter = MakeRef(DID_ALTER);
    if (index >= getTailOffset(list._capacity)) {
      newTail = updateVNode(newTail, list.__ownerID, 0, index, value, didAlter);
    } else {
      newRoot = updateVNode(newRoot, list.__ownerID, list._level, index, value, didAlter);
    }

    if (!didAlter.value) {
      return list;
    }

    if (list.__ownerID) {
      list._root = newRoot;
      list._tail = newTail;
      list.__hash = undefined;
      list.__altered = true;
      return list;
    }
    return makeList(list._origin, list._capacity, list._level, newRoot, newTail);
  }

  function updateVNode(node, ownerID, level, index, value, didAlter) {
    var idx = (index >>> level) & MASK;
    var nodeHas = node && idx < node.array.length;
    if (!nodeHas && value === undefined) {
      return node;
    }

    var newNode;

    if (level > 0) {
      var lowerNode = node && node.array[idx];
      var newLowerNode = updateVNode(lowerNode, ownerID, level - SHIFT, index, value, didAlter);
      if (newLowerNode === lowerNode) {
        return node;
      }
      newNode = editableVNode(node, ownerID);
      newNode.array[idx] = newLowerNode;
      return newNode;
    }

    if (nodeHas && node.array[idx] === value) {
      return node;
    }

    SetRef(didAlter);

    newNode = editableVNode(node, ownerID);
    if (value === undefined && idx === newNode.array.length - 1) {
      newNode.array.pop();
    } else {
      newNode.array[idx] = value;
    }
    return newNode;
  }

  function editableVNode(node, ownerID) {
    if (ownerID && node && ownerID === node.ownerID) {
      return node;
    }
    return new VNode(node ? node.array.slice() : [], ownerID);
  }

  function listNodeFor(list, rawIndex) {
    if (rawIndex >= getTailOffset(list._capacity)) {
      return list._tail;
    }
    if (rawIndex < 1 << (list._level + SHIFT)) {
      var node = list._root;
      var level = list._level;
      while (node && level > 0) {
        node = node.array[(rawIndex >>> level) & MASK];
        level -= SHIFT;
      }
      return node;
    }
  }

  function setListBounds(list, begin, end) {
    // Sanitize begin & end using this shorthand for ToInt32(argument)
    // http://www.ecma-international.org/ecma-262/6.0/#sec-toint32
    if (begin !== undefined) {
      begin = begin | 0;
    }
    if (end !== undefined) {
      end = end | 0;
    }
    var owner = list.__ownerID || new OwnerID();
    var oldOrigin = list._origin;
    var oldCapacity = list._capacity;
    var newOrigin = oldOrigin + begin;
    var newCapacity = end === undefined ? oldCapacity : end < 0 ? oldCapacity + end : oldOrigin + end;
    if (newOrigin === oldOrigin && newCapacity === oldCapacity) {
      return list;
    }

    // If it's going to end after it starts, it's empty.
    if (newOrigin >= newCapacity) {
      return list.clear();
    }

    var newLevel = list._level;
    var newRoot = list._root;

    // New origin might need creating a higher root.
    var offsetShift = 0;
    while (newOrigin + offsetShift < 0) {
      newRoot = new VNode(newRoot && newRoot.array.length ? [undefined, newRoot] : [], owner);
      newLevel += SHIFT;
      offsetShift += 1 << newLevel;
    }
    if (offsetShift) {
      newOrigin += offsetShift;
      oldOrigin += offsetShift;
      newCapacity += offsetShift;
      oldCapacity += offsetShift;
    }

    var oldTailOffset = getTailOffset(oldCapacity);
    var newTailOffset = getTailOffset(newCapacity);

    // New size might need creating a higher root.
    while (newTailOffset >= 1 << (newLevel + SHIFT)) {
      newRoot = new VNode(newRoot && newRoot.array.length ? [newRoot] : [], owner);
      newLevel += SHIFT;
    }

    // Locate or create the new tail.
    var oldTail = list._tail;
    var newTail = newTailOffset < oldTailOffset ?
      listNodeFor(list, newCapacity - 1) :
      newTailOffset > oldTailOffset ? new VNode([], owner) : oldTail;

    // Merge Tail into tree.
    if (oldTail && newTailOffset > oldTailOffset && newOrigin < oldCapacity && oldTail.array.length) {
      newRoot = editableVNode(newRoot, owner);
      var node = newRoot;
      for (var level = newLevel; level > SHIFT; level -= SHIFT) {
        var idx = (oldTailOffset >>> level) & MASK;
        node = node.array[idx] = editableVNode(node.array[idx], owner);
      }
      node.array[(oldTailOffset >>> SHIFT) & MASK] = oldTail;
    }

    // If the size has been reduced, there's a chance the tail needs to be trimmed.
    if (newCapacity < oldCapacity) {
      newTail = newTail && newTail.removeAfter(owner, 0, newCapacity);
    }

    // If the new origin is within the tail, then we do not need a root.
    if (newOrigin >= newTailOffset) {
      newOrigin -= newTailOffset;
      newCapacity -= newTailOffset;
      newLevel = SHIFT;
      newRoot = null;
      newTail = newTail && newTail.removeBefore(owner, 0, newOrigin);

    // Otherwise, if the root has been trimmed, garbage collect.
    } else if (newOrigin > oldOrigin || newTailOffset < oldTailOffset) {
      offsetShift = 0;

      // Identify the new top root node of the subtree of the old root.
      while (newRoot) {
        var beginIndex = (newOrigin >>> newLevel) & MASK;
        if (beginIndex !== (newTailOffset >>> newLevel) & MASK) {
          break;
        }
        if (beginIndex) {
          offsetShift += (1 << newLevel) * beginIndex;
        }
        newLevel -= SHIFT;
        newRoot = newRoot.array[beginIndex];
      }

      // Trim the new sides of the new root.
      if (newRoot && newOrigin > oldOrigin) {
        newRoot = newRoot.removeBefore(owner, newLevel, newOrigin - offsetShift);
      }
      if (newRoot && newTailOffset < oldTailOffset) {
        newRoot = newRoot.removeAfter(owner, newLevel, newTailOffset - offsetShift);
      }
      if (offsetShift) {
        newOrigin -= offsetShift;
        newCapacity -= offsetShift;
      }
    }

    if (list.__ownerID) {
      list.size = newCapacity - newOrigin;
      list._origin = newOrigin;
      list._capacity = newCapacity;
      list._level = newLevel;
      list._root = newRoot;
      list._tail = newTail;
      list.__hash = undefined;
      list.__altered = true;
      return list;
    }
    return makeList(newOrigin, newCapacity, newLevel, newRoot, newTail);
  }

  function mergeIntoListWith(list, merger, iterables) {
    var iters = [];
    var maxSize = 0;
    for (var ii = 0; ii < iterables.length; ii++) {
      var value = iterables[ii];
      var iter = IndexedIterable(value);
      if (iter.size > maxSize) {
        maxSize = iter.size;
      }
      if (!isIterable(value)) {
        iter = iter.map(function(v ) {return fromJS(v)});
      }
      iters.push(iter);
    }
    if (maxSize > list.size) {
      list = list.setSize(maxSize);
    }
    return mergeIntoCollectionWith(list, merger, iters);
  }

  function getTailOffset(size) {
    return size < SIZE ? 0 : (((size - 1) >>> SHIFT) << SHIFT);
  }

  createClass(OrderedMap, Map);

    // @pragma Construction

    function OrderedMap(value) {
      return value === null || value === undefined ? emptyOrderedMap() :
        isOrderedMap(value) ? value :
        emptyOrderedMap().withMutations(function(map ) {
          var iter = KeyedIterable(value);
          assertNotInfinite(iter.size);
          iter.forEach(function(v, k)  {return map.set(k, v)});
        });
    }

    OrderedMap.of = function(/*...values*/) {
      return this(arguments);
    };

    OrderedMap.prototype.toString = function() {
      return this.__toString('OrderedMap {', '}');
    };

    // @pragma Access

    OrderedMap.prototype.get = function(k, notSetValue) {
      var index = this._map.get(k);
      return index !== undefined ? this._list.get(index)[1] : notSetValue;
    };

    // @pragma Modification

    OrderedMap.prototype.clear = function() {
      if (this.size === 0) {
        return this;
      }
      if (this.__ownerID) {
        this.size = 0;
        this._map.clear();
        this._list.clear();
        return this;
      }
      return emptyOrderedMap();
    };

    OrderedMap.prototype.set = function(k, v) {
      return updateOrderedMap(this, k, v);
    };

    OrderedMap.prototype.remove = function(k) {
      return updateOrderedMap(this, k, NOT_SET);
    };

    OrderedMap.prototype.wasAltered = function() {
      return this._map.wasAltered() || this._list.wasAltered();
    };

    OrderedMap.prototype.__iterate = function(fn, reverse) {var this$0 = this;
      return this._list.__iterate(
        function(entry ) {return entry && fn(entry[1], entry[0], this$0)},
        reverse
      );
    };

    OrderedMap.prototype.__iterator = function(type, reverse) {
      return this._list.fromEntrySeq().__iterator(type, reverse);
    };

    OrderedMap.prototype.__ensureOwner = function(ownerID) {
      if (ownerID === this.__ownerID) {
        return this;
      }
      var newMap = this._map.__ensureOwner(ownerID);
      var newList = this._list.__ensureOwner(ownerID);
      if (!ownerID) {
        this.__ownerID = ownerID;
        this._map = newMap;
        this._list = newList;
        return this;
      }
      return makeOrderedMap(newMap, newList, ownerID, this.__hash);
    };


  function isOrderedMap(maybeOrderedMap) {
    return isMap(maybeOrderedMap) && isOrdered(maybeOrderedMap);
  }

  OrderedMap.isOrderedMap = isOrderedMap;

  OrderedMap.prototype[IS_ORDERED_SENTINEL] = true;
  OrderedMap.prototype[DELETE] = OrderedMap.prototype.remove;



  function makeOrderedMap(map, list, ownerID, hash) {
    var omap = Object.create(OrderedMap.prototype);
    omap.size = map ? map.size : 0;
    omap._map = map;
    omap._list = list;
    omap.__ownerID = ownerID;
    omap.__hash = hash;
    return omap;
  }

  var EMPTY_ORDERED_MAP;
  function emptyOrderedMap() {
    return EMPTY_ORDERED_MAP || (EMPTY_ORDERED_MAP = makeOrderedMap(emptyMap(), emptyList()));
  }

  function updateOrderedMap(omap, k, v) {
    var map = omap._map;
    var list = omap._list;
    var i = map.get(k);
    var has = i !== undefined;
    var newMap;
    var newList;
    if (v === NOT_SET) { // removed
      if (!has) {
        return omap;
      }
      if (list.size >= SIZE && list.size >= map.size * 2) {
        newList = list.filter(function(entry, idx)  {return entry !== undefined && i !== idx});
        newMap = newList.toKeyedSeq().map(function(entry ) {return entry[0]}).flip().toMap();
        if (omap.__ownerID) {
          newMap.__ownerID = newList.__ownerID = omap.__ownerID;
        }
      } else {
        newMap = map.remove(k);
        newList = i === list.size - 1 ? list.pop() : list.set(i, undefined);
      }
    } else {
      if (has) {
        if (v === list.get(i)[1]) {
          return omap;
        }
        newMap = map;
        newList = list.set(i, [k, v]);
      } else {
        newMap = map.set(k, list.size);
        newList = list.set(list.size, [k, v]);
      }
    }
    if (omap.__ownerID) {
      omap.size = newMap.size;
      omap._map = newMap;
      omap._list = newList;
      omap.__hash = undefined;
      return omap;
    }
    return makeOrderedMap(newMap, newList);
  }

  createClass(ToKeyedSequence, KeyedSeq);
    function ToKeyedSequence(indexed, useKeys) {
      this._iter = indexed;
      this._useKeys = useKeys;
      this.size = indexed.size;
    }

    ToKeyedSequence.prototype.get = function(key, notSetValue) {
      return this._iter.get(key, notSetValue);
    };

    ToKeyedSequence.prototype.has = function(key) {
      return this._iter.has(key);
    };

    ToKeyedSequence.prototype.valueSeq = function() {
      return this._iter.valueSeq();
    };

    ToKeyedSequence.prototype.reverse = function() {var this$0 = this;
      var reversedSequence = reverseFactory(this, true);
      if (!this._useKeys) {
        reversedSequence.valueSeq = function()  {return this$0._iter.toSeq().reverse()};
      }
      return reversedSequence;
    };

    ToKeyedSequence.prototype.map = function(mapper, context) {var this$0 = this;
      var mappedSequence = mapFactory(this, mapper, context);
      if (!this._useKeys) {
        mappedSequence.valueSeq = function()  {return this$0._iter.toSeq().map(mapper, context)};
      }
      return mappedSequence;
    };

    ToKeyedSequence.prototype.__iterate = function(fn, reverse) {var this$0 = this;
      var ii;
      return this._iter.__iterate(
        this._useKeys ?
          function(v, k)  {return fn(v, k, this$0)} :
          ((ii = reverse ? resolveSize(this) : 0),
            function(v ) {return fn(v, reverse ? --ii : ii++, this$0)}),
        reverse
      );
    };

    ToKeyedSequence.prototype.__iterator = function(type, reverse) {
      if (this._useKeys) {
        return this._iter.__iterator(type, reverse);
      }
      var iterator = this._iter.__iterator(ITERATE_VALUES, reverse);
      var ii = reverse ? resolveSize(this) : 0;
      return new Iterator(function()  {
        var step = iterator.next();
        return step.done ? step :
          iteratorValue(type, reverse ? --ii : ii++, step.value, step);
      });
    };

  ToKeyedSequence.prototype[IS_ORDERED_SENTINEL] = true;


  createClass(ToIndexedSequence, IndexedSeq);
    function ToIndexedSequence(iter) {
      this._iter = iter;
      this.size = iter.size;
    }

    ToIndexedSequence.prototype.includes = function(value) {
      return this._iter.includes(value);
    };

    ToIndexedSequence.prototype.__iterate = function(fn, reverse) {var this$0 = this;
      var iterations = 0;
      return this._iter.__iterate(function(v ) {return fn(v, iterations++, this$0)}, reverse);
    };

    ToIndexedSequence.prototype.__iterator = function(type, reverse) {
      var iterator = this._iter.__iterator(ITERATE_VALUES, reverse);
      var iterations = 0;
      return new Iterator(function()  {
        var step = iterator.next();
        return step.done ? step :
          iteratorValue(type, iterations++, step.value, step)
      });
    };



  createClass(ToSetSequence, SetSeq);
    function ToSetSequence(iter) {
      this._iter = iter;
      this.size = iter.size;
    }

    ToSetSequence.prototype.has = function(key) {
      return this._iter.includes(key);
    };

    ToSetSequence.prototype.__iterate = function(fn, reverse) {var this$0 = this;
      return this._iter.__iterate(function(v ) {return fn(v, v, this$0)}, reverse);
    };

    ToSetSequence.prototype.__iterator = function(type, reverse) {
      var iterator = this._iter.__iterator(ITERATE_VALUES, reverse);
      return new Iterator(function()  {
        var step = iterator.next();
        return step.done ? step :
          iteratorValue(type, step.value, step.value, step);
      });
    };



  createClass(FromEntriesSequence, KeyedSeq);
    function FromEntriesSequence(entries) {
      this._iter = entries;
      this.size = entries.size;
    }

    FromEntriesSequence.prototype.entrySeq = function() {
      return this._iter.toSeq();
    };

    FromEntriesSequence.prototype.__iterate = function(fn, reverse) {var this$0 = this;
      return this._iter.__iterate(function(entry ) {
        // Check if entry exists first so array access doesn't throw for holes
        // in the parent iteration.
        if (entry) {
          validateEntry(entry);
          var indexedIterable = isIterable(entry);
          return fn(
            indexedIterable ? entry.get(1) : entry[1],
            indexedIterable ? entry.get(0) : entry[0],
            this$0
          );
        }
      }, reverse);
    };

    FromEntriesSequence.prototype.__iterator = function(type, reverse) {
      var iterator = this._iter.__iterator(ITERATE_VALUES, reverse);
      return new Iterator(function()  {
        while (true) {
          var step = iterator.next();
          if (step.done) {
            return step;
          }
          var entry = step.value;
          // Check if entry exists first so array access doesn't throw for holes
          // in the parent iteration.
          if (entry) {
            validateEntry(entry);
            var indexedIterable = isIterable(entry);
            return iteratorValue(
              type,
              indexedIterable ? entry.get(0) : entry[0],
              indexedIterable ? entry.get(1) : entry[1],
              step
            );
          }
        }
      });
    };


  ToIndexedSequence.prototype.cacheResult =
  ToKeyedSequence.prototype.cacheResult =
  ToSetSequence.prototype.cacheResult =
  FromEntriesSequence.prototype.cacheResult =
    cacheResultThrough;


  function flipFactory(iterable) {
    var flipSequence = makeSequence(iterable);
    flipSequence._iter = iterable;
    flipSequence.size = iterable.size;
    flipSequence.flip = function()  {return iterable};
    flipSequence.reverse = function () {
      var reversedSequence = iterable.reverse.apply(this); // super.reverse()
      reversedSequence.flip = function()  {return iterable.reverse()};
      return reversedSequence;
    };
    flipSequence.has = function(key ) {return iterable.includes(key)};
    flipSequence.includes = function(key ) {return iterable.has(key)};
    flipSequence.cacheResult = cacheResultThrough;
    flipSequence.__iterateUncached = function (fn, reverse) {var this$0 = this;
      return iterable.__iterate(function(v, k)  {return fn(k, v, this$0) !== false}, reverse);
    }
    flipSequence.__iteratorUncached = function(type, reverse) {
      if (type === ITERATE_ENTRIES) {
        var iterator = iterable.__iterator(type, reverse);
        return new Iterator(function()  {
          var step = iterator.next();
          if (!step.done) {
            var k = step.value[0];
            step.value[0] = step.value[1];
            step.value[1] = k;
          }
          return step;
        });
      }
      return iterable.__iterator(
        type === ITERATE_VALUES ? ITERATE_KEYS : ITERATE_VALUES,
        reverse
      );
    }
    return flipSequence;
  }


  function mapFactory(iterable, mapper, context) {
    var mappedSequence = makeSequence(iterable);
    mappedSequence.size = iterable.size;
    mappedSequence.has = function(key ) {return iterable.has(key)};
    mappedSequence.get = function(key, notSetValue)  {
      var v = iterable.get(key, NOT_SET);
      return v === NOT_SET ?
        notSetValue :
        mapper.call(context, v, key, iterable);
    };
    mappedSequence.__iterateUncached = function (fn, reverse) {var this$0 = this;
      return iterable.__iterate(
        function(v, k, c)  {return fn(mapper.call(context, v, k, c), k, this$0) !== false},
        reverse
      );
    }
    mappedSequence.__iteratorUncached = function (type, reverse) {
      var iterator = iterable.__iterator(ITERATE_ENTRIES, reverse);
      return new Iterator(function()  {
        var step = iterator.next();
        if (step.done) {
          return step;
        }
        var entry = step.value;
        var key = entry[0];
        return iteratorValue(
          type,
          key,
          mapper.call(context, entry[1], key, iterable),
          step
        );
      });
    }
    return mappedSequence;
  }


  function reverseFactory(iterable, useKeys) {
    var reversedSequence = makeSequence(iterable);
    reversedSequence._iter = iterable;
    reversedSequence.size = iterable.size;
    reversedSequence.reverse = function()  {return iterable};
    if (iterable.flip) {
      reversedSequence.flip = function () {
        var flipSequence = flipFactory(iterable);
        flipSequence.reverse = function()  {return iterable.flip()};
        return flipSequence;
      };
    }
    reversedSequence.get = function(key, notSetValue) 
      {return iterable.get(useKeys ? key : -1 - key, notSetValue)};
    reversedSequence.has = function(key )
      {return iterable.has(useKeys ? key : -1 - key)};
    reversedSequence.includes = function(value ) {return iterable.includes(value)};
    reversedSequence.cacheResult = cacheResultThrough;
    reversedSequence.__iterate = function (fn, reverse) {var this$0 = this;
      return iterable.__iterate(function(v, k)  {return fn(v, k, this$0)}, !reverse);
    };
    reversedSequence.__iterator =
      function(type, reverse)  {return iterable.__iterator(type, !reverse)};
    return reversedSequence;
  }


  function filterFactory(iterable, predicate, context, useKeys) {
    var filterSequence = makeSequence(iterable);
    if (useKeys) {
      filterSequence.has = function(key ) {
        var v = iterable.get(key, NOT_SET);
        return v !== NOT_SET && !!predicate.call(context, v, key, iterable);
      };
      filterSequence.get = function(key, notSetValue)  {
        var v = iterable.get(key, NOT_SET);
        return v !== NOT_SET && predicate.call(context, v, key, iterable) ?
          v : notSetValue;
      };
    }
    filterSequence.__iterateUncached = function (fn, reverse) {var this$0 = this;
      var iterations = 0;
      iterable.__iterate(function(v, k, c)  {
        if (predicate.call(context, v, k, c)) {
          iterations++;
          return fn(v, useKeys ? k : iterations - 1, this$0);
        }
      }, reverse);
      return iterations;
    };
    filterSequence.__iteratorUncached = function (type, reverse) {
      var iterator = iterable.__iterator(ITERATE_ENTRIES, reverse);
      var iterations = 0;
      return new Iterator(function()  {
        while (true) {
          var step = iterator.next();
          if (step.done) {
            return step;
          }
          var entry = step.value;
          var key = entry[0];
          var value = entry[1];
          if (predicate.call(context, value, key, iterable)) {
            return iteratorValue(type, useKeys ? key : iterations++, value, step);
          }
        }
      });
    }
    return filterSequence;
  }


  function countByFactory(iterable, grouper, context) {
    var groups = Map().asMutable();
    iterable.__iterate(function(v, k)  {
      groups.update(
        grouper.call(context, v, k, iterable),
        0,
        function(a ) {return a + 1}
      );
    });
    return groups.asImmutable();
  }


  function groupByFactory(iterable, grouper, context) {
    var isKeyedIter = isKeyed(iterable);
    var groups = (isOrdered(iterable) ? OrderedMap() : Map()).asMutable();
    iterable.__iterate(function(v, k)  {
      groups.update(
        grouper.call(context, v, k, iterable),
        function(a ) {return (a = a || [], a.push(isKeyedIter ? [k, v] : v), a)}
      );
    });
    var coerce = iterableClass(iterable);
    return groups.map(function(arr ) {return reify(iterable, coerce(arr))});
  }


  function sliceFactory(iterable, begin, end, useKeys) {
    var originalSize = iterable.size;

    // Sanitize begin & end using this shorthand for ToInt32(argument)
    // http://www.ecma-international.org/ecma-262/6.0/#sec-toint32
    if (begin !== undefined) {
      begin = begin | 0;
    }
    if (end !== undefined) {
      if (end === Infinity) {
        end = originalSize;
      } else {
        end = end | 0;
      }
    }

    if (wholeSlice(begin, end, originalSize)) {
      return iterable;
    }

    var resolvedBegin = resolveBegin(begin, originalSize);
    var resolvedEnd = resolveEnd(end, originalSize);

    // begin or end will be NaN if they were provided as negative numbers and
    // this iterable's size is unknown. In that case, cache first so there is
    // a known size and these do not resolve to NaN.
    if (resolvedBegin !== resolvedBegin || resolvedEnd !== resolvedEnd) {
      return sliceFactory(iterable.toSeq().cacheResult(), begin, end, useKeys);
    }

    // Note: resolvedEnd is undefined when the original sequence's length is
    // unknown and this slice did not supply an end and should contain all
    // elements after resolvedBegin.
    // In that case, resolvedSize will be NaN and sliceSize will remain undefined.
    var resolvedSize = resolvedEnd - resolvedBegin;
    var sliceSize;
    if (resolvedSize === resolvedSize) {
      sliceSize = resolvedSize < 0 ? 0 : resolvedSize;
    }

    var sliceSeq = makeSequence(iterable);

    // If iterable.size is undefined, the size of the realized sliceSeq is
    // unknown at this point unless the number of items to slice is 0
    sliceSeq.size = sliceSize === 0 ? sliceSize : iterable.size && sliceSize || undefined;

    if (!useKeys && isSeq(iterable) && sliceSize >= 0) {
      sliceSeq.get = function (index, notSetValue) {
        index = wrapIndex(this, index);
        return index >= 0 && index < sliceSize ?
          iterable.get(index + resolvedBegin, notSetValue) :
          notSetValue;
      }
    }

    sliceSeq.__iterateUncached = function(fn, reverse) {var this$0 = this;
      if (sliceSize === 0) {
        return 0;
      }
      if (reverse) {
        return this.cacheResult().__iterate(fn, reverse);
      }
      var skipped = 0;
      var isSkipping = true;
      var iterations = 0;
      iterable.__iterate(function(v, k)  {
        if (!(isSkipping && (isSkipping = skipped++ < resolvedBegin))) {
          iterations++;
          return fn(v, useKeys ? k : iterations - 1, this$0) !== false &&
                 iterations !== sliceSize;
        }
      });
      return iterations;
    };

    sliceSeq.__iteratorUncached = function(type, reverse) {
      if (sliceSize !== 0 && reverse) {
        return this.cacheResult().__iterator(type, reverse);
      }
      // Don't bother instantiating parent iterator if taking 0.
      var iterator = sliceSize !== 0 && iterable.__iterator(type, reverse);
      var skipped = 0;
      var iterations = 0;
      return new Iterator(function()  {
        while (skipped++ < resolvedBegin) {
          iterator.next();
        }
        if (++iterations > sliceSize) {
          return iteratorDone();
        }
        var step = iterator.next();
        if (useKeys || type === ITERATE_VALUES) {
          return step;
        } else if (type === ITERATE_KEYS) {
          return iteratorValue(type, iterations - 1, undefined, step);
        } else {
          return iteratorValue(type, iterations - 1, step.value[1], step);
        }
      });
    }

    return sliceSeq;
  }


  function takeWhileFactory(iterable, predicate, context) {
    var takeSequence = makeSequence(iterable);
    takeSequence.__iterateUncached = function(fn, reverse) {var this$0 = this;
      if (reverse) {
        return this.cacheResult().__iterate(fn, reverse);
      }
      var iterations = 0;
      iterable.__iterate(function(v, k, c) 
        {return predicate.call(context, v, k, c) && ++iterations && fn(v, k, this$0)}
      );
      return iterations;
    };
    takeSequence.__iteratorUncached = function(type, reverse) {var this$0 = this;
      if (reverse) {
        return this.cacheResult().__iterator(type, reverse);
      }
      var iterator = iterable.__iterator(ITERATE_ENTRIES, reverse);
      var iterating = true;
      return new Iterator(function()  {
        if (!iterating) {
          return iteratorDone();
        }
        var step = iterator.next();
        if (step.done) {
          return step;
        }
        var entry = step.value;
        var k = entry[0];
        var v = entry[1];
        if (!predicate.call(context, v, k, this$0)) {
          iterating = false;
          return iteratorDone();
        }
        return type === ITERATE_ENTRIES ? step :
          iteratorValue(type, k, v, step);
      });
    };
    return takeSequence;
  }


  function skipWhileFactory(iterable, predicate, context, useKeys) {
    var skipSequence = makeSequence(iterable);
    skipSequence.__iterateUncached = function (fn, reverse) {var this$0 = this;
      if (reverse) {
        return this.cacheResult().__iterate(fn, reverse);
      }
      var isSkipping = true;
      var iterations = 0;
      iterable.__iterate(function(v, k, c)  {
        if (!(isSkipping && (isSkipping = predicate.call(context, v, k, c)))) {
          iterations++;
          return fn(v, useKeys ? k : iterations - 1, this$0);
        }
      });
      return iterations;
    };
    skipSequence.__iteratorUncached = function(type, reverse) {var this$0 = this;
      if (reverse) {
        return this.cacheResult().__iterator(type, reverse);
      }
      var iterator = iterable.__iterator(ITERATE_ENTRIES, reverse);
      var skipping = true;
      var iterations = 0;
      return new Iterator(function()  {
        var step, k, v;
        do {
          step = iterator.next();
          if (step.done) {
            if (useKeys || type === ITERATE_VALUES) {
              return step;
            } else if (type === ITERATE_KEYS) {
              return iteratorValue(type, iterations++, undefined, step);
            } else {
              return iteratorValue(type, iterations++, step.value[1], step);
            }
          }
          var entry = step.value;
          k = entry[0];
          v = entry[1];
          skipping && (skipping = predicate.call(context, v, k, this$0));
        } while (skipping);
        return type === ITERATE_ENTRIES ? step :
          iteratorValue(type, k, v, step);
      });
    };
    return skipSequence;
  }


  function concatFactory(iterable, values) {
    var isKeyedIterable = isKeyed(iterable);
    var iters = [iterable].concat(values).map(function(v ) {
      if (!isIterable(v)) {
        v = isKeyedIterable ?
          keyedSeqFromValue(v) :
          indexedSeqFromValue(Array.isArray(v) ? v : [v]);
      } else if (isKeyedIterable) {
        v = KeyedIterable(v);
      }
      return v;
    }).filter(function(v ) {return v.size !== 0});

    if (iters.length === 0) {
      return iterable;
    }

    if (iters.length === 1) {
      var singleton = iters[0];
      if (singleton === iterable ||
          isKeyedIterable && isKeyed(singleton) ||
          isIndexed(iterable) && isIndexed(singleton)) {
        return singleton;
      }
    }

    var concatSeq = new ArraySeq(iters);
    if (isKeyedIterable) {
      concatSeq = concatSeq.toKeyedSeq();
    } else if (!isIndexed(iterable)) {
      concatSeq = concatSeq.toSetSeq();
    }
    concatSeq = concatSeq.flatten(true);
    concatSeq.size = iters.reduce(
      function(sum, seq)  {
        if (sum !== undefined) {
          var size = seq.size;
          if (size !== undefined) {
            return sum + size;
          }
        }
      },
      0
    );
    return concatSeq;
  }


  function flattenFactory(iterable, depth, useKeys) {
    var flatSequence = makeSequence(iterable);
    flatSequence.__iterateUncached = function(fn, reverse) {
      var iterations = 0;
      var stopped = false;
      function flatDeep(iter, currentDepth) {var this$0 = this;
        iter.__iterate(function(v, k)  {
          if ((!depth || currentDepth < depth) && isIterable(v)) {
            flatDeep(v, currentDepth + 1);
          } else if (fn(v, useKeys ? k : iterations++, this$0) === false) {
            stopped = true;
          }
          return !stopped;
        }, reverse);
      }
      flatDeep(iterable, 0);
      return iterations;
    }
    flatSequence.__iteratorUncached = function(type, reverse) {
      var iterator = iterable.__iterator(type, reverse);
      var stack = [];
      var iterations = 0;
      return new Iterator(function()  {
        while (iterator) {
          var step = iterator.next();
          if (step.done !== false) {
            iterator = stack.pop();
            continue;
          }
          var v = step.value;
          if (type === ITERATE_ENTRIES) {
            v = v[1];
          }
          if ((!depth || stack.length < depth) && isIterable(v)) {
            stack.push(iterator);
            iterator = v.__iterator(type, reverse);
          } else {
            return useKeys ? step : iteratorValue(type, iterations++, v, step);
          }
        }
        return iteratorDone();
      });
    }
    return flatSequence;
  }


  function flatMapFactory(iterable, mapper, context) {
    var coerce = iterableClass(iterable);
    return iterable.toSeq().map(
      function(v, k)  {return coerce(mapper.call(context, v, k, iterable))}
    ).flatten(true);
  }


  function interposeFactory(iterable, separator) {
    var interposedSequence = makeSequence(iterable);
    interposedSequence.size = iterable.size && iterable.size * 2 -1;
    interposedSequence.__iterateUncached = function(fn, reverse) {var this$0 = this;
      var iterations = 0;
      iterable.__iterate(function(v, k) 
        {return (!iterations || fn(separator, iterations++, this$0) !== false) &&
        fn(v, iterations++, this$0) !== false},
        reverse
      );
      return iterations;
    };
    interposedSequence.__iteratorUncached = function(type, reverse) {
      var iterator = iterable.__iterator(ITERATE_VALUES, reverse);
      var iterations = 0;
      var step;
      return new Iterator(function()  {
        if (!step || iterations % 2) {
          step = iterator.next();
          if (step.done) {
            return step;
          }
        }
        return iterations % 2 ?
          iteratorValue(type, iterations++, separator) :
          iteratorValue(type, iterations++, step.value, step);
      });
    };
    return interposedSequence;
  }


  function sortFactory(iterable, comparator, mapper) {
    if (!comparator) {
      comparator = defaultComparator;
    }
    var isKeyedIterable = isKeyed(iterable);
    var index = 0;
    var entries = iterable.toSeq().map(
      function(v, k)  {return [k, v, index++, mapper ? mapper(v, k, iterable) : v]}
    ).toArray();
    entries.sort(function(a, b)  {return comparator(a[3], b[3]) || a[2] - b[2]}).forEach(
      isKeyedIterable ?
      function(v, i)  { entries[i].length = 2; } :
      function(v, i)  { entries[i] = v[1]; }
    );
    return isKeyedIterable ? KeyedSeq(entries) :
      isIndexed(iterable) ? IndexedSeq(entries) :
      SetSeq(entries);
  }


  function maxFactory(iterable, comparator, mapper) {
    if (!comparator) {
      comparator = defaultComparator;
    }
    if (mapper) {
      var entry = iterable.toSeq()
        .map(function(v, k)  {return [v, mapper(v, k, iterable)]})
        .reduce(function(a, b)  {return maxCompare(comparator, a[1], b[1]) ? b : a});
      return entry && entry[0];
    } else {
      return iterable.reduce(function(a, b)  {return maxCompare(comparator, a, b) ? b : a});
    }
  }

  function maxCompare(comparator, a, b) {
    var comp = comparator(b, a);
    // b is considered the new max if the comparator declares them equal, but
    // they are not equal and b is in fact a nullish value.
    return (comp === 0 && b !== a && (b === undefined || b === null || b !== b)) || comp > 0;
  }


  function zipWithFactory(keyIter, zipper, iters) {
    var zipSequence = makeSequence(keyIter);
    zipSequence.size = new ArraySeq(iters).map(function(i ) {return i.size}).min();
    // Note: this a generic base implementation of __iterate in terms of
    // __iterator which may be more generically useful in the future.
    zipSequence.__iterate = function(fn, reverse) {
      /* generic:
      var iterator = this.__iterator(ITERATE_ENTRIES, reverse);
      var step;
      var iterations = 0;
      while (!(step = iterator.next()).done) {
        iterations++;
        if (fn(step.value[1], step.value[0], this) === false) {
          break;
        }
      }
      return iterations;
      */
      // indexed:
      var iterator = this.__iterator(ITERATE_VALUES, reverse);
      var step;
      var iterations = 0;
      while (!(step = iterator.next()).done) {
        if (fn(step.value, iterations++, this) === false) {
          break;
        }
      }
      return iterations;
    };
    zipSequence.__iteratorUncached = function(type, reverse) {
      var iterators = iters.map(function(i )
        {return (i = Iterable(i), getIterator(reverse ? i.reverse() : i))}
      );
      var iterations = 0;
      var isDone = false;
      return new Iterator(function()  {
        var steps;
        if (!isDone) {
          steps = iterators.map(function(i ) {return i.next()});
          isDone = steps.some(function(s ) {return s.done});
        }
        if (isDone) {
          return iteratorDone();
        }
        return iteratorValue(
          type,
          iterations++,
          zipper.apply(null, steps.map(function(s ) {return s.value}))
        );
      });
    };
    return zipSequence
  }


  // #pragma Helper Functions

  function reify(iter, seq) {
    return isSeq(iter) ? seq : iter.constructor(seq);
  }

  function validateEntry(entry) {
    if (entry !== Object(entry)) {
      throw new TypeError('Expected [K, V] tuple: ' + entry);
    }
  }

  function resolveSize(iter) {
    assertNotInfinite(iter.size);
    return ensureSize(iter);
  }

  function iterableClass(iterable) {
    return isKeyed(iterable) ? KeyedIterable :
      isIndexed(iterable) ? IndexedIterable :
      SetIterable;
  }

  function makeSequence(iterable) {
    return Object.create(
      (
        isKeyed(iterable) ? KeyedSeq :
        isIndexed(iterable) ? IndexedSeq :
        SetSeq
      ).prototype
    );
  }

  function cacheResultThrough() {
    if (this._iter.cacheResult) {
      this._iter.cacheResult();
      this.size = this._iter.size;
      return this;
    } else {
      return Seq.prototype.cacheResult.call(this);
    }
  }

  function defaultComparator(a, b) {
    return a > b ? 1 : a < b ? -1 : 0;
  }

  function forceIterator(keyPath) {
    var iter = getIterator(keyPath);
    if (!iter) {
      // Array might not be iterable in this environment, so we need a fallback
      // to our wrapped type.
      if (!isArrayLike(keyPath)) {
        throw new TypeError('Expected iterable or array-like: ' + keyPath);
      }
      iter = getIterator(Iterable(keyPath));
    }
    return iter;
  }

  createClass(Record, KeyedCollection);

    function Record(defaultValues, name) {
      var hasInitialized;

      var RecordType = function Record(values) {
        if (values instanceof RecordType) {
          return values;
        }
        if (!(this instanceof RecordType)) {
          return new RecordType(values);
        }
        if (!hasInitialized) {
          hasInitialized = true;
          var keys = Object.keys(defaultValues);
          setProps(RecordTypePrototype, keys);
          RecordTypePrototype.size = keys.length;
          RecordTypePrototype._name = name;
          RecordTypePrototype._keys = keys;
          RecordTypePrototype._defaultValues = defaultValues;
        }
        this._map = Map(values);
      };

      var RecordTypePrototype = RecordType.prototype = Object.create(RecordPrototype);
      RecordTypePrototype.constructor = RecordType;

      return RecordType;
    }

    Record.prototype.toString = function() {
      return this.__toString(recordName(this) + ' {', '}');
    };

    // @pragma Access

    Record.prototype.has = function(k) {
      return this._defaultValues.hasOwnProperty(k);
    };

    Record.prototype.get = function(k, notSetValue) {
      if (!this.has(k)) {
        return notSetValue;
      }
      var defaultVal = this._defaultValues[k];
      return this._map ? this._map.get(k, defaultVal) : defaultVal;
    };

    // @pragma Modification

    Record.prototype.clear = function() {
      if (this.__ownerID) {
        this._map && this._map.clear();
        return this;
      }
      var RecordType = this.constructor;
      return RecordType._empty || (RecordType._empty = makeRecord(this, emptyMap()));
    };

    Record.prototype.set = function(k, v) {
      if (!this.has(k)) {
        throw new Error('Cannot set unknown key "' + k + '" on ' + recordName(this));
      }
      if (this._map && !this._map.has(k)) {
        var defaultVal = this._defaultValues[k];
        if (v === defaultVal) {
          return this;
        }
      }
      var newMap = this._map && this._map.set(k, v);
      if (this.__ownerID || newMap === this._map) {
        return this;
      }
      return makeRecord(this, newMap);
    };

    Record.prototype.remove = function(k) {
      if (!this.has(k)) {
        return this;
      }
      var newMap = this._map && this._map.remove(k);
      if (this.__ownerID || newMap === this._map) {
        return this;
      }
      return makeRecord(this, newMap);
    };

    Record.prototype.wasAltered = function() {
      return this._map.wasAltered();
    };

    Record.prototype.__iterator = function(type, reverse) {var this$0 = this;
      return KeyedIterable(this._defaultValues).map(function(_, k)  {return this$0.get(k)}).__iterator(type, reverse);
    };

    Record.prototype.__iterate = function(fn, reverse) {var this$0 = this;
      return KeyedIterable(this._defaultValues).map(function(_, k)  {return this$0.get(k)}).__iterate(fn, reverse);
    };

    Record.prototype.__ensureOwner = function(ownerID) {
      if (ownerID === this.__ownerID) {
        return this;
      }
      var newMap = this._map && this._map.__ensureOwner(ownerID);
      if (!ownerID) {
        this.__ownerID = ownerID;
        this._map = newMap;
        return this;
      }
      return makeRecord(this, newMap, ownerID);
    };


  var RecordPrototype = Record.prototype;
  RecordPrototype[DELETE] = RecordPrototype.remove;
  RecordPrototype.deleteIn =
  RecordPrototype.removeIn = MapPrototype.removeIn;
  RecordPrototype.merge = MapPrototype.merge;
  RecordPrototype.mergeWith = MapPrototype.mergeWith;
  RecordPrototype.mergeIn = MapPrototype.mergeIn;
  RecordPrototype.mergeDeep = MapPrototype.mergeDeep;
  RecordPrototype.mergeDeepWith = MapPrototype.mergeDeepWith;
  RecordPrototype.mergeDeepIn = MapPrototype.mergeDeepIn;
  RecordPrototype.setIn = MapPrototype.setIn;
  RecordPrototype.update = MapPrototype.update;
  RecordPrototype.updateIn = MapPrototype.updateIn;
  RecordPrototype.withMutations = MapPrototype.withMutations;
  RecordPrototype.asMutable = MapPrototype.asMutable;
  RecordPrototype.asImmutable = MapPrototype.asImmutable;


  function makeRecord(likeRecord, map, ownerID) {
    var record = Object.create(Object.getPrototypeOf(likeRecord));
    record._map = map;
    record.__ownerID = ownerID;
    return record;
  }

  function recordName(record) {
    return record._name || record.constructor.name || 'Record';
  }

  function setProps(prototype, names) {
    try {
      names.forEach(setProp.bind(undefined, prototype));
    } catch (error) {
      // Object.defineProperty failed. Probably IE8.
    }
  }

  function setProp(prototype, name) {
    Object.defineProperty(prototype, name, {
      get: function() {
        return this.get(name);
      },
      set: function(value) {
        invariant(this.__ownerID, 'Cannot set on an immutable record.');
        this.set(name, value);
      }
    });
  }

  createClass(Set, SetCollection);

    // @pragma Construction

    function Set(value) {
      return value === null || value === undefined ? emptySet() :
        isSet(value) && !isOrdered(value) ? value :
        emptySet().withMutations(function(set ) {
          var iter = SetIterable(value);
          assertNotInfinite(iter.size);
          iter.forEach(function(v ) {return set.add(v)});
        });
    }

    Set.of = function(/*...values*/) {
      return this(arguments);
    };

    Set.fromKeys = function(value) {
      return this(KeyedIterable(value).keySeq());
    };

    Set.prototype.toString = function() {
      return this.__toString('Set {', '}');
    };

    // @pragma Access

    Set.prototype.has = function(value) {
      return this._map.has(value);
    };

    // @pragma Modification

    Set.prototype.add = function(value) {
      return updateSet(this, this._map.set(value, true));
    };

    Set.prototype.remove = function(value) {
      return updateSet(this, this._map.remove(value));
    };

    Set.prototype.clear = function() {
      return updateSet(this, this._map.clear());
    };

    // @pragma Composition

    Set.prototype.union = function() {var iters = SLICE$0.call(arguments, 0);
      iters = iters.filter(function(x ) {return x.size !== 0});
      if (iters.length === 0) {
        return this;
      }
      if (this.size === 0 && !this.__ownerID && iters.length === 1) {
        return this.constructor(iters[0]);
      }
      return this.withMutations(function(set ) {
        for (var ii = 0; ii < iters.length; ii++) {
          SetIterable(iters[ii]).forEach(function(value ) {return set.add(value)});
        }
      });
    };

    Set.prototype.intersect = function() {var iters = SLICE$0.call(arguments, 0);
      if (iters.length === 0) {
        return this;
      }
      iters = iters.map(function(iter ) {return SetIterable(iter)});
      var originalSet = this;
      return this.withMutations(function(set ) {
        originalSet.forEach(function(value ) {
          if (!iters.every(function(iter ) {return iter.includes(value)})) {
            set.remove(value);
          }
        });
      });
    };

    Set.prototype.subtract = function() {var iters = SLICE$0.call(arguments, 0);
      if (iters.length === 0) {
        return this;
      }
      iters = iters.map(function(iter ) {return SetIterable(iter)});
      var originalSet = this;
      return this.withMutations(function(set ) {
        originalSet.forEach(function(value ) {
          if (iters.some(function(iter ) {return iter.includes(value)})) {
            set.remove(value);
          }
        });
      });
    };

    Set.prototype.merge = function() {
      return this.union.apply(this, arguments);
    };

    Set.prototype.mergeWith = function(merger) {var iters = SLICE$0.call(arguments, 1);
      return this.union.apply(this, iters);
    };

    Set.prototype.sort = function(comparator) {
      // Late binding
      return OrderedSet(sortFactory(this, comparator));
    };

    Set.prototype.sortBy = function(mapper, comparator) {
      // Late binding
      return OrderedSet(sortFactory(this, comparator, mapper));
    };

    Set.prototype.wasAltered = function() {
      return this._map.wasAltered();
    };

    Set.prototype.__iterate = function(fn, reverse) {var this$0 = this;
      return this._map.__iterate(function(_, k)  {return fn(k, k, this$0)}, reverse);
    };

    Set.prototype.__iterator = function(type, reverse) {
      return this._map.map(function(_, k)  {return k}).__iterator(type, reverse);
    };

    Set.prototype.__ensureOwner = function(ownerID) {
      if (ownerID === this.__ownerID) {
        return this;
      }
      var newMap = this._map.__ensureOwner(ownerID);
      if (!ownerID) {
        this.__ownerID = ownerID;
        this._map = newMap;
        return this;
      }
      return this.__make(newMap, ownerID);
    };


  function isSet(maybeSet) {
    return !!(maybeSet && maybeSet[IS_SET_SENTINEL]);
  }

  Set.isSet = isSet;

  var IS_SET_SENTINEL = '@@__IMMUTABLE_SET__@@';

  var SetPrototype = Set.prototype;
  SetPrototype[IS_SET_SENTINEL] = true;
  SetPrototype[DELETE] = SetPrototype.remove;
  SetPrototype.mergeDeep = SetPrototype.merge;
  SetPrototype.mergeDeepWith = SetPrototype.mergeWith;
  SetPrototype.withMutations = MapPrototype.withMutations;
  SetPrototype.asMutable = MapPrototype.asMutable;
  SetPrototype.asImmutable = MapPrototype.asImmutable;

  SetPrototype.__empty = emptySet;
  SetPrototype.__make = makeSet;

  function updateSet(set, newMap) {
    if (set.__ownerID) {
      set.size = newMap.size;
      set._map = newMap;
      return set;
    }
    return newMap === set._map ? set :
      newMap.size === 0 ? set.__empty() :
      set.__make(newMap);
  }

  function makeSet(map, ownerID) {
    var set = Object.create(SetPrototype);
    set.size = map ? map.size : 0;
    set._map = map;
    set.__ownerID = ownerID;
    return set;
  }

  var EMPTY_SET;
  function emptySet() {
    return EMPTY_SET || (EMPTY_SET = makeSet(emptyMap()));
  }

  createClass(OrderedSet, Set);

    // @pragma Construction

    function OrderedSet(value) {
      return value === null || value === undefined ? emptyOrderedSet() :
        isOrderedSet(value) ? value :
        emptyOrderedSet().withMutations(function(set ) {
          var iter = SetIterable(value);
          assertNotInfinite(iter.size);
          iter.forEach(function(v ) {return set.add(v)});
        });
    }

    OrderedSet.of = function(/*...values*/) {
      return this(arguments);
    };

    OrderedSet.fromKeys = function(value) {
      return this(KeyedIterable(value).keySeq());
    };

    OrderedSet.prototype.toString = function() {
      return this.__toString('OrderedSet {', '}');
    };


  function isOrderedSet(maybeOrderedSet) {
    return isSet(maybeOrderedSet) && isOrdered(maybeOrderedSet);
  }

  OrderedSet.isOrderedSet = isOrderedSet;

  var OrderedSetPrototype = OrderedSet.prototype;
  OrderedSetPrototype[IS_ORDERED_SENTINEL] = true;

  OrderedSetPrototype.__empty = emptyOrderedSet;
  OrderedSetPrototype.__make = makeOrderedSet;

  function makeOrderedSet(map, ownerID) {
    var set = Object.create(OrderedSetPrototype);
    set.size = map ? map.size : 0;
    set._map = map;
    set.__ownerID = ownerID;
    return set;
  }

  var EMPTY_ORDERED_SET;
  function emptyOrderedSet() {
    return EMPTY_ORDERED_SET || (EMPTY_ORDERED_SET = makeOrderedSet(emptyOrderedMap()));
  }

  createClass(Stack, IndexedCollection);

    // @pragma Construction

    function Stack(value) {
      return value === null || value === undefined ? emptyStack() :
        isStack(value) ? value :
        emptyStack().unshiftAll(value);
    }

    Stack.of = function(/*...values*/) {
      return this(arguments);
    };

    Stack.prototype.toString = function() {
      return this.__toString('Stack [', ']');
    };

    // @pragma Access

    Stack.prototype.get = function(index, notSetValue) {
      var head = this._head;
      index = wrapIndex(this, index);
      while (head && index--) {
        head = head.next;
      }
      return head ? head.value : notSetValue;
    };

    Stack.prototype.peek = function() {
      return this._head && this._head.value;
    };

    // @pragma Modification

    Stack.prototype.push = function(/*...values*/) {
      if (arguments.length === 0) {
        return this;
      }
      var newSize = this.size + arguments.length;
      var head = this._head;
      for (var ii = arguments.length - 1; ii >= 0; ii--) {
        head = {
          value: arguments[ii],
          next: head
        };
      }
      if (this.__ownerID) {
        this.size = newSize;
        this._head = head;
        this.__hash = undefined;
        this.__altered = true;
        return this;
      }
      return makeStack(newSize, head);
    };

    Stack.prototype.pushAll = function(iter) {
      iter = IndexedIterable(iter);
      if (iter.size === 0) {
        return this;
      }
      assertNotInfinite(iter.size);
      var newSize = this.size;
      var head = this._head;
      iter.reverse().forEach(function(value ) {
        newSize++;
        head = {
          value: value,
          next: head
        };
      });
      if (this.__ownerID) {
        this.size = newSize;
        this._head = head;
        this.__hash = undefined;
        this.__altered = true;
        return this;
      }
      return makeStack(newSize, head);
    };

    Stack.prototype.pop = function() {
      return this.slice(1);
    };

    Stack.prototype.unshift = function(/*...values*/) {
      return this.push.apply(this, arguments);
    };

    Stack.prototype.unshiftAll = function(iter) {
      return this.pushAll(iter);
    };

    Stack.prototype.shift = function() {
      return this.pop.apply(this, arguments);
    };

    Stack.prototype.clear = function() {
      if (this.size === 0) {
        return this;
      }
      if (this.__ownerID) {
        this.size = 0;
        this._head = undefined;
        this.__hash = undefined;
        this.__altered = true;
        return this;
      }
      return emptyStack();
    };

    Stack.prototype.slice = function(begin, end) {
      if (wholeSlice(begin, end, this.size)) {
        return this;
      }
      var resolvedBegin = resolveBegin(begin, this.size);
      var resolvedEnd = resolveEnd(end, this.size);
      if (resolvedEnd !== this.size) {
        // super.slice(begin, end);
        return IndexedCollection.prototype.slice.call(this, begin, end);
      }
      var newSize = this.size - resolvedBegin;
      var head = this._head;
      while (resolvedBegin--) {
        head = head.next;
      }
      if (this.__ownerID) {
        this.size = newSize;
        this._head = head;
        this.__hash = undefined;
        this.__altered = true;
        return this;
      }
      return makeStack(newSize, head);
    };

    // @pragma Mutability

    Stack.prototype.__ensureOwner = function(ownerID) {
      if (ownerID === this.__ownerID) {
        return this;
      }
      if (!ownerID) {
        this.__ownerID = ownerID;
        this.__altered = false;
        return this;
      }
      return makeStack(this.size, this._head, ownerID, this.__hash);
    };

    // @pragma Iteration

    Stack.prototype.__iterate = function(fn, reverse) {
      if (reverse) {
        return this.reverse().__iterate(fn);
      }
      var iterations = 0;
      var node = this._head;
      while (node) {
        if (fn(node.value, iterations++, this) === false) {
          break;
        }
        node = node.next;
      }
      return iterations;
    };

    Stack.prototype.__iterator = function(type, reverse) {
      if (reverse) {
        return this.reverse().__iterator(type);
      }
      var iterations = 0;
      var node = this._head;
      return new Iterator(function()  {
        if (node) {
          var value = node.value;
          node = node.next;
          return iteratorValue(type, iterations++, value);
        }
        return iteratorDone();
      });
    };


  function isStack(maybeStack) {
    return !!(maybeStack && maybeStack[IS_STACK_SENTINEL]);
  }

  Stack.isStack = isStack;

  var IS_STACK_SENTINEL = '@@__IMMUTABLE_STACK__@@';

  var StackPrototype = Stack.prototype;
  StackPrototype[IS_STACK_SENTINEL] = true;
  StackPrototype.withMutations = MapPrototype.withMutations;
  StackPrototype.asMutable = MapPrototype.asMutable;
  StackPrototype.asImmutable = MapPrototype.asImmutable;
  StackPrototype.wasAltered = MapPrototype.wasAltered;


  function makeStack(size, head, ownerID, hash) {
    var map = Object.create(StackPrototype);
    map.size = size;
    map._head = head;
    map.__ownerID = ownerID;
    map.__hash = hash;
    map.__altered = false;
    return map;
  }

  var EMPTY_STACK;
  function emptyStack() {
    return EMPTY_STACK || (EMPTY_STACK = makeStack(0));
  }

  /**
   * Contributes additional methods to a constructor
   */
  function mixin(ctor, methods) {
    var keyCopier = function(key ) { ctor.prototype[key] = methods[key]; };
    Object.keys(methods).forEach(keyCopier);
    Object.getOwnPropertySymbols &&
      Object.getOwnPropertySymbols(methods).forEach(keyCopier);
    return ctor;
  }

  Iterable.Iterator = Iterator;

  mixin(Iterable, {

    // ### Conversion to other types

    toArray: function() {
      assertNotInfinite(this.size);
      var array = new Array(this.size || 0);
      this.valueSeq().__iterate(function(v, i)  { array[i] = v; });
      return array;
    },

    toIndexedSeq: function() {
      return new ToIndexedSequence(this);
    },

    toJS: function() {
      return this.toSeq().map(
        function(value ) {return value && typeof value.toJS === 'function' ? value.toJS() : value}
      ).__toJS();
    },

    toJSON: function() {
      return this.toSeq().map(
        function(value ) {return value && typeof value.toJSON === 'function' ? value.toJSON() : value}
      ).__toJS();
    },

    toKeyedSeq: function() {
      return new ToKeyedSequence(this, true);
    },

    toMap: function() {
      // Use Late Binding here to solve the circular dependency.
      return Map(this.toKeyedSeq());
    },

    toObject: function() {
      assertNotInfinite(this.size);
      var object = {};
      this.__iterate(function(v, k)  { object[k] = v; });
      return object;
    },

    toOrderedMap: function() {
      // Use Late Binding here to solve the circular dependency.
      return OrderedMap(this.toKeyedSeq());
    },

    toOrderedSet: function() {
      // Use Late Binding here to solve the circular dependency.
      return OrderedSet(isKeyed(this) ? this.valueSeq() : this);
    },

    toSet: function() {
      // Use Late Binding here to solve the circular dependency.
      return Set(isKeyed(this) ? this.valueSeq() : this);
    },

    toSetSeq: function() {
      return new ToSetSequence(this);
    },

    toSeq: function() {
      return isIndexed(this) ? this.toIndexedSeq() :
        isKeyed(this) ? this.toKeyedSeq() :
        this.toSetSeq();
    },

    toStack: function() {
      // Use Late Binding here to solve the circular dependency.
      return Stack(isKeyed(this) ? this.valueSeq() : this);
    },

    toList: function() {
      // Use Late Binding here to solve the circular dependency.
      return List(isKeyed(this) ? this.valueSeq() : this);
    },


    // ### Common JavaScript methods and properties

    toString: function() {
      return '[Iterable]';
    },

    __toString: function(head, tail) {
      if (this.size === 0) {
        return head + tail;
      }
      return head + ' ' + this.toSeq().map(this.__toStringMapper).join(', ') + ' ' + tail;
    },


    // ### ES6 Collection methods (ES6 Array and Map)

    concat: function() {var values = SLICE$0.call(arguments, 0);
      return reify(this, concatFactory(this, values));
    },

    includes: function(searchValue) {
      return this.some(function(value ) {return is(value, searchValue)});
    },

    entries: function() {
      return this.__iterator(ITERATE_ENTRIES);
    },

    every: function(predicate, context) {
      assertNotInfinite(this.size);
      var returnValue = true;
      this.__iterate(function(v, k, c)  {
        if (!predicate.call(context, v, k, c)) {
          returnValue = false;
          return false;
        }
      });
      return returnValue;
    },

    filter: function(predicate, context) {
      return reify(this, filterFactory(this, predicate, context, true));
    },

    find: function(predicate, context, notSetValue) {
      var entry = this.findEntry(predicate, context);
      return entry ? entry[1] : notSetValue;
    },

    forEach: function(sideEffect, context) {
      assertNotInfinite(this.size);
      return this.__iterate(context ? sideEffect.bind(context) : sideEffect);
    },

    join: function(separator) {
      assertNotInfinite(this.size);
      separator = separator !== undefined ? '' + separator : ',';
      var joined = '';
      var isFirst = true;
      this.__iterate(function(v ) {
        isFirst ? (isFirst = false) : (joined += separator);
        joined += v !== null && v !== undefined ? v.toString() : '';
      });
      return joined;
    },

    keys: function() {
      return this.__iterator(ITERATE_KEYS);
    },

    map: function(mapper, context) {
      return reify(this, mapFactory(this, mapper, context));
    },

    reduce: function(reducer, initialReduction, context) {
      assertNotInfinite(this.size);
      var reduction;
      var useFirst;
      if (arguments.length < 2) {
        useFirst = true;
      } else {
        reduction = initialReduction;
      }
      this.__iterate(function(v, k, c)  {
        if (useFirst) {
          useFirst = false;
          reduction = v;
        } else {
          reduction = reducer.call(context, reduction, v, k, c);
        }
      });
      return reduction;
    },

    reduceRight: function(reducer, initialReduction, context) {
      var reversed = this.toKeyedSeq().reverse();
      return reversed.reduce.apply(reversed, arguments);
    },

    reverse: function() {
      return reify(this, reverseFactory(this, true));
    },

    slice: function(begin, end) {
      return reify(this, sliceFactory(this, begin, end, true));
    },

    some: function(predicate, context) {
      return !this.every(not(predicate), context);
    },

    sort: function(comparator) {
      return reify(this, sortFactory(this, comparator));
    },

    values: function() {
      return this.__iterator(ITERATE_VALUES);
    },


    // ### More sequential methods

    butLast: function() {
      return this.slice(0, -1);
    },

    isEmpty: function() {
      return this.size !== undefined ? this.size === 0 : !this.some(function()  {return true});
    },

    count: function(predicate, context) {
      return ensureSize(
        predicate ? this.toSeq().filter(predicate, context) : this
      );
    },

    countBy: function(grouper, context) {
      return countByFactory(this, grouper, context);
    },

    equals: function(other) {
      return deepEqual(this, other);
    },

    entrySeq: function() {
      var iterable = this;
      if (iterable._cache) {
        // We cache as an entries array, so we can just return the cache!
        return new ArraySeq(iterable._cache);
      }
      var entriesSequence = iterable.toSeq().map(entryMapper).toIndexedSeq();
      entriesSequence.fromEntrySeq = function()  {return iterable.toSeq()};
      return entriesSequence;
    },

    filterNot: function(predicate, context) {
      return this.filter(not(predicate), context);
    },

    findEntry: function(predicate, context, notSetValue) {
      var found = notSetValue;
      this.__iterate(function(v, k, c)  {
        if (predicate.call(context, v, k, c)) {
          found = [k, v];
          return false;
        }
      });
      return found;
    },

    findKey: function(predicate, context) {
      var entry = this.findEntry(predicate, context);
      return entry && entry[0];
    },

    findLast: function(predicate, context, notSetValue) {
      return this.toKeyedSeq().reverse().find(predicate, context, notSetValue);
    },

    findLastEntry: function(predicate, context, notSetValue) {
      return this.toKeyedSeq().reverse().findEntry(predicate, context, notSetValue);
    },

    findLastKey: function(predicate, context) {
      return this.toKeyedSeq().reverse().findKey(predicate, context);
    },

    first: function() {
      return this.find(returnTrue);
    },

    flatMap: function(mapper, context) {
      return reify(this, flatMapFactory(this, mapper, context));
    },

    flatten: function(depth) {
      return reify(this, flattenFactory(this, depth, true));
    },

    fromEntrySeq: function() {
      return new FromEntriesSequence(this);
    },

    get: function(searchKey, notSetValue) {
      return this.find(function(_, key)  {return is(key, searchKey)}, undefined, notSetValue);
    },

    getIn: function(searchKeyPath, notSetValue) {
      var nested = this;
      // Note: in an ES6 environment, we would prefer:
      // for (var key of searchKeyPath) {
      var iter = forceIterator(searchKeyPath);
      var step;
      while (!(step = iter.next()).done) {
        var key = step.value;
        nested = nested && nested.get ? nested.get(key, NOT_SET) : NOT_SET;
        if (nested === NOT_SET) {
          return notSetValue;
        }
      }
      return nested;
    },

    groupBy: function(grouper, context) {
      return groupByFactory(this, grouper, context);
    },

    has: function(searchKey) {
      return this.get(searchKey, NOT_SET) !== NOT_SET;
    },

    hasIn: function(searchKeyPath) {
      return this.getIn(searchKeyPath, NOT_SET) !== NOT_SET;
    },

    isSubset: function(iter) {
      iter = typeof iter.includes === 'function' ? iter : Iterable(iter);
      return this.every(function(value ) {return iter.includes(value)});
    },

    isSuperset: function(iter) {
      iter = typeof iter.isSubset === 'function' ? iter : Iterable(iter);
      return iter.isSubset(this);
    },

    keyOf: function(searchValue) {
      return this.findKey(function(value ) {return is(value, searchValue)});
    },

    keySeq: function() {
      return this.toSeq().map(keyMapper).toIndexedSeq();
    },

    last: function() {
      return this.toSeq().reverse().first();
    },

    lastKeyOf: function(searchValue) {
      return this.toKeyedSeq().reverse().keyOf(searchValue);
    },

    max: function(comparator) {
      return maxFactory(this, comparator);
    },

    maxBy: function(mapper, comparator) {
      return maxFactory(this, comparator, mapper);
    },

    min: function(comparator) {
      return maxFactory(this, comparator ? neg(comparator) : defaultNegComparator);
    },

    minBy: function(mapper, comparator) {
      return maxFactory(this, comparator ? neg(comparator) : defaultNegComparator, mapper);
    },

    rest: function() {
      return this.slice(1);
    },

    skip: function(amount) {
      return this.slice(Math.max(0, amount));
    },

    skipLast: function(amount) {
      return reify(this, this.toSeq().reverse().skip(amount).reverse());
    },

    skipWhile: function(predicate, context) {
      return reify(this, skipWhileFactory(this, predicate, context, true));
    },

    skipUntil: function(predicate, context) {
      return this.skipWhile(not(predicate), context);
    },

    sortBy: function(mapper, comparator) {
      return reify(this, sortFactory(this, comparator, mapper));
    },

    take: function(amount) {
      return this.slice(0, Math.max(0, amount));
    },

    takeLast: function(amount) {
      return reify(this, this.toSeq().reverse().take(amount).reverse());
    },

    takeWhile: function(predicate, context) {
      return reify(this, takeWhileFactory(this, predicate, context));
    },

    takeUntil: function(predicate, context) {
      return this.takeWhile(not(predicate), context);
    },

    valueSeq: function() {
      return this.toIndexedSeq();
    },


    // ### Hashable Object

    hashCode: function() {
      return this.__hash || (this.__hash = hashIterable(this));
    }


    // ### Internal

    // abstract __iterate(fn, reverse)

    // abstract __iterator(type, reverse)
  });

  // var IS_ITERABLE_SENTINEL = '@@__IMMUTABLE_ITERABLE__@@';
  // var IS_KEYED_SENTINEL = '@@__IMMUTABLE_KEYED__@@';
  // var IS_INDEXED_SENTINEL = '@@__IMMUTABLE_INDEXED__@@';
  // var IS_ORDERED_SENTINEL = '@@__IMMUTABLE_ORDERED__@@';

  var IterablePrototype = Iterable.prototype;
  IterablePrototype[IS_ITERABLE_SENTINEL] = true;
  IterablePrototype[ITERATOR_SYMBOL] = IterablePrototype.values;
  IterablePrototype.__toJS = IterablePrototype.toArray;
  IterablePrototype.__toStringMapper = quoteString;
  IterablePrototype.inspect =
  IterablePrototype.toSource = function() { return this.toString(); };
  IterablePrototype.chain = IterablePrototype.flatMap;
  IterablePrototype.contains = IterablePrototype.includes;

  mixin(KeyedIterable, {

    // ### More sequential methods

    flip: function() {
      return reify(this, flipFactory(this));
    },

    mapEntries: function(mapper, context) {var this$0 = this;
      var iterations = 0;
      return reify(this,
        this.toSeq().map(
          function(v, k)  {return mapper.call(context, [k, v], iterations++, this$0)}
        ).fromEntrySeq()
      );
    },

    mapKeys: function(mapper, context) {var this$0 = this;
      return reify(this,
        this.toSeq().flip().map(
          function(k, v)  {return mapper.call(context, k, v, this$0)}
        ).flip()
      );
    }

  });

  var KeyedIterablePrototype = KeyedIterable.prototype;
  KeyedIterablePrototype[IS_KEYED_SENTINEL] = true;
  KeyedIterablePrototype[ITERATOR_SYMBOL] = IterablePrototype.entries;
  KeyedIterablePrototype.__toJS = IterablePrototype.toObject;
  KeyedIterablePrototype.__toStringMapper = function(v, k)  {return JSON.stringify(k) + ': ' + quoteString(v)};



  mixin(IndexedIterable, {

    // ### Conversion to other types

    toKeyedSeq: function() {
      return new ToKeyedSequence(this, false);
    },


    // ### ES6 Collection methods (ES6 Array and Map)

    filter: function(predicate, context) {
      return reify(this, filterFactory(this, predicate, context, false));
    },

    findIndex: function(predicate, context) {
      var entry = this.findEntry(predicate, context);
      return entry ? entry[0] : -1;
    },

    indexOf: function(searchValue) {
      var key = this.keyOf(searchValue);
      return key === undefined ? -1 : key;
    },

    lastIndexOf: function(searchValue) {
      var key = this.lastKeyOf(searchValue);
      return key === undefined ? -1 : key;
    },

    reverse: function() {
      return reify(this, reverseFactory(this, false));
    },

    slice: function(begin, end) {
      return reify(this, sliceFactory(this, begin, end, false));
    },

    splice: function(index, removeNum /*, ...values*/) {
      var numArgs = arguments.length;
      removeNum = Math.max(removeNum | 0, 0);
      if (numArgs === 0 || (numArgs === 2 && !removeNum)) {
        return this;
      }
      // If index is negative, it should resolve relative to the size of the
      // collection. However size may be expensive to compute if not cached, so
      // only call count() if the number is in fact negative.
      index = resolveBegin(index, index < 0 ? this.count() : this.size);
      var spliced = this.slice(0, index);
      return reify(
        this,
        numArgs === 1 ?
          spliced :
          spliced.concat(arrCopy(arguments, 2), this.slice(index + removeNum))
      );
    },


    // ### More collection methods

    findLastIndex: function(predicate, context) {
      var entry = this.findLastEntry(predicate, context);
      return entry ? entry[0] : -1;
    },

    first: function() {
      return this.get(0);
    },

    flatten: function(depth) {
      return reify(this, flattenFactory(this, depth, false));
    },

    get: function(index, notSetValue) {
      index = wrapIndex(this, index);
      return (index < 0 || (this.size === Infinity ||
          (this.size !== undefined && index > this.size))) ?
        notSetValue :
        this.find(function(_, key)  {return key === index}, undefined, notSetValue);
    },

    has: function(index) {
      index = wrapIndex(this, index);
      return index >= 0 && (this.size !== undefined ?
        this.size === Infinity || index < this.size :
        this.indexOf(index) !== -1
      );
    },

    interpose: function(separator) {
      return reify(this, interposeFactory(this, separator));
    },

    interleave: function(/*...iterables*/) {
      var iterables = [this].concat(arrCopy(arguments));
      var zipped = zipWithFactory(this.toSeq(), IndexedSeq.of, iterables);
      var interleaved = zipped.flatten(true);
      if (zipped.size) {
        interleaved.size = zipped.size * iterables.length;
      }
      return reify(this, interleaved);
    },

    keySeq: function() {
      return Range(0, this.size);
    },

    last: function() {
      return this.get(-1);
    },

    skipWhile: function(predicate, context) {
      return reify(this, skipWhileFactory(this, predicate, context, false));
    },

    zip: function(/*, ...iterables */) {
      var iterables = [this].concat(arrCopy(arguments));
      return reify(this, zipWithFactory(this, defaultZipper, iterables));
    },

    zipWith: function(zipper/*, ...iterables */) {
      var iterables = arrCopy(arguments);
      iterables[0] = this;
      return reify(this, zipWithFactory(this, zipper, iterables));
    }

  });

  IndexedIterable.prototype[IS_INDEXED_SENTINEL] = true;
  IndexedIterable.prototype[IS_ORDERED_SENTINEL] = true;



  mixin(SetIterable, {

    // ### ES6 Collection methods (ES6 Array and Map)

    get: function(value, notSetValue) {
      return this.has(value) ? value : notSetValue;
    },

    includes: function(value) {
      return this.has(value);
    },


    // ### More sequential methods

    keySeq: function() {
      return this.valueSeq();
    }

  });

  SetIterable.prototype.has = IterablePrototype.includes;
  SetIterable.prototype.contains = SetIterable.prototype.includes;


  // Mixin subclasses

  mixin(KeyedSeq, KeyedIterable.prototype);
  mixin(IndexedSeq, IndexedIterable.prototype);
  mixin(SetSeq, SetIterable.prototype);

  mixin(KeyedCollection, KeyedIterable.prototype);
  mixin(IndexedCollection, IndexedIterable.prototype);
  mixin(SetCollection, SetIterable.prototype);


  // #pragma Helper functions

  function keyMapper(v, k) {
    return k;
  }

  function entryMapper(v, k) {
    return [k, v];
  }

  function not(predicate) {
    return function() {
      return !predicate.apply(this, arguments);
    }
  }

  function neg(predicate) {
    return function() {
      return -predicate.apply(this, arguments);
    }
  }

  function quoteString(value) {
    return typeof value === 'string' ? JSON.stringify(value) : String(value);
  }

  function defaultZipper() {
    return arrCopy(arguments);
  }

  function defaultNegComparator(a, b) {
    return a < b ? 1 : a > b ? -1 : 0;
  }

  function hashIterable(iterable) {
    if (iterable.size === Infinity) {
      return 0;
    }
    var ordered = isOrdered(iterable);
    var keyed = isKeyed(iterable);
    var h = ordered ? 1 : 0;
    var size = iterable.__iterate(
      keyed ?
        ordered ?
          function(v, k)  { h = 31 * h + hashMerge(hash(v), hash(k)) | 0; } :
          function(v, k)  { h = h + hashMerge(hash(v), hash(k)) | 0; } :
        ordered ?
          function(v ) { h = 31 * h + hash(v) | 0; } :
          function(v ) { h = h + hash(v) | 0; }
    );
    return murmurHashOfSize(size, h);
  }

  function murmurHashOfSize(size, h) {
    h = imul(h, 0xCC9E2D51);
    h = imul(h << 15 | h >>> -15, 0x1B873593);
    h = imul(h << 13 | h >>> -13, 5);
    h = (h + 0xE6546B64 | 0) ^ size;
    h = imul(h ^ h >>> 16, 0x85EBCA6B);
    h = imul(h ^ h >>> 13, 0xC2B2AE35);
    h = smi(h ^ h >>> 16);
    return h;
  }

  function hashMerge(a, b) {
    return a ^ b + 0x9E3779B9 + (a << 6) + (a >> 2) | 0; // int
  }

  var Immutable = {

    Iterable: Iterable,

    Seq: Seq,
    Collection: Collection,
    Map: Map,
    OrderedMap: OrderedMap,
    List: List,
    Stack: Stack,
    Set: Set,
    OrderedSet: OrderedSet,

    Record: Record,
    Range: Range,
    Repeat: Repeat,

    is: is,
    fromJS: fromJS

  };

  return Immutable;

}));

/***/ }),
/* 18 */
/***/ (function(module, exports) {

var g;

// This works in non-strict mode
g = (function() {
	return this;
})();

try {
	// This works if eval is allowed (see CSP)
	g = g || Function("return this")() || (1,eval)("this");
} catch(e) {
	// This works if the window reference is available
	if(typeof window === "object")
		g = window;
}

// g can still be undefined, but nothing to do about it...
// We return undefined, instead of nothing here, so it's
// easier to handle this case. if(!global) { ...}

module.exports = g;


/***/ }),
/* 19 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/*
object-assign
(c) Sindre Sorhus
@license MIT
*/


/* eslint-disable no-unused-vars */
var getOwnPropertySymbols = Object.getOwnPropertySymbols;
var hasOwnProperty = Object.prototype.hasOwnProperty;
var propIsEnumerable = Object.prototype.propertyIsEnumerable;

function toObject(val) {
	if (val === null || val === undefined) {
		throw new TypeError('Object.assign cannot be called with null or undefined');
	}

	return Object(val);
}

function shouldUseNative() {
	try {
		if (!Object.assign) {
			return false;
		}

		// Detect buggy property enumeration order in older V8 versions.

		// https://bugs.chromium.org/p/v8/issues/detail?id=4118
		var test1 = new String('abc');  // eslint-disable-line no-new-wrappers
		test1[5] = 'de';
		if (Object.getOwnPropertyNames(test1)[0] === '5') {
			return false;
		}

		// https://bugs.chromium.org/p/v8/issues/detail?id=3056
		var test2 = {};
		for (var i = 0; i < 10; i++) {
			test2['_' + String.fromCharCode(i)] = i;
		}
		var order2 = Object.getOwnPropertyNames(test2).map(function (n) {
			return test2[n];
		});
		if (order2.join('') !== '0123456789') {
			return false;
		}

		// https://bugs.chromium.org/p/v8/issues/detail?id=3056
		var test3 = {};
		'abcdefghijklmnopqrst'.split('').forEach(function (letter) {
			test3[letter] = letter;
		});
		if (Object.keys(Object.assign({}, test3)).join('') !==
				'abcdefghijklmnopqrst') {
			return false;
		}

		return true;
	} catch (err) {
		// We don't expect any of the above to throw, but better to be safe.
		return false;
	}
}

module.exports = shouldUseNative() ? Object.assign : function (target, source) {
	var from;
	var to = toObject(target);
	var symbols;

	for (var s = 1; s < arguments.length; s++) {
		from = Object(arguments[s]);

		for (var key in from) {
			if (hasOwnProperty.call(from, key)) {
				to[key] = from[key];
			}
		}

		if (getOwnPropertySymbols) {
			symbols = getOwnPropertySymbols(from);
			for (var i = 0; i < symbols.length; i++) {
				if (propIsEnumerable.call(from, symbols[i])) {
					to[symbols[i]] = from[symbols[i]];
				}
			}
		}
	}

	return to;
};


/***/ }),
/* 20 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "a", function() { return addLeadingSlash; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "f", function() { return stripLeadingSlash; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "c", function() { return hasBasename; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "e", function() { return stripBasename; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "g", function() { return stripTrailingSlash; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "d", function() { return parsePath; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "b", function() { return createPath; });
var addLeadingSlash = function addLeadingSlash(path) {
  return path.charAt(0) === '/' ? path : '/' + path;
};

var stripLeadingSlash = function stripLeadingSlash(path) {
  return path.charAt(0) === '/' ? path.substr(1) : path;
};

var hasBasename = function hasBasename(path, prefix) {
  return new RegExp('^' + prefix + '(\\/|\\?|#|$)', 'i').test(path);
};

var stripBasename = function stripBasename(path, prefix) {
  return hasBasename(path, prefix) ? path.substr(prefix.length) : path;
};

var stripTrailingSlash = function stripTrailingSlash(path) {
  return path.charAt(path.length - 1) === '/' ? path.slice(0, -1) : path;
};

var parsePath = function parsePath(path) {
  var pathname = path || '/';
  var search = '';
  var hash = '';

  var hashIndex = pathname.indexOf('#');
  if (hashIndex !== -1) {
    hash = pathname.substr(hashIndex);
    pathname = pathname.substr(0, hashIndex);
  }

  var searchIndex = pathname.indexOf('?');
  if (searchIndex !== -1) {
    search = pathname.substr(searchIndex);
    pathname = pathname.substr(0, searchIndex);
  }

  return {
    pathname: pathname,
    search: search === '?' ? '' : search,
    hash: hash === '#' ? '' : hash
  };
};

var createPath = function createPath(location) {
  var pathname = location.pathname,
      search = location.search,
      hash = location.hash;


  var path = pathname || '/';

  if (search && search !== '?') path += search.charAt(0) === '?' ? search : '?' + search;

  if (hash && hash !== '#') path += hash.charAt(0) === '#' ? hash : '#' + hash;

  return path;
};

/***/ }),
/* 21 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

var _commonUtils = __webpack_require__(3);

var _googleAdUtils = __webpack_require__(36);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var AdUnit = function (_React$Component) {
	_inherits(AdUnit, _React$Component);

	function AdUnit(props) {
		_classCallCheck(this, AdUnit);

		var _this = _possibleConstructorReturn(this, (AdUnit.__proto__ || Object.getPrototypeOf(AdUnit)).call(this, props));

		_this.displayAd = _this.displayAd.bind(_this);
		_this.refreshAd = _this.refreshAd.bind(_this);
		_this.addAdSlot = _this.addAdSlot.bind(_this);
		_this.clearAdDiv = _this.clearAdDiv.bind(_this);
		return _this;
	}

	_createClass(AdUnit, [{
		key: 'clearAdDiv',
		value: function clearAdDiv() {
			var element = document.getElementById(this.props.adContainerId);
			if (element) {
				element.setAttribute('data-google-query-id', '');
				element.innerHTML = '';
			}
		}
	}, {
		key: 'addAdSlot',
		value: function addAdSlot() {
			(0, _googleAdUtils.addAdSlot)(this.props.adSlot, this.props.adDimension ? this.props.adDimension : [320, 50], this.props.adContainerId, this.props.tags);
		}
	}, {
		key: 'refreshAd',
		value: function refreshAd() {
			if (!this.props) return;
			(0, _googleAdUtils.removeAdSlot)(this.props.adSlot);
			this.clearAdDiv();
			this.addAdSlot();
			this.displayAd();
		}
	}, {
		key: 'displayAd',
		value: function displayAd() {
			if (googletag) {
				var adContainerId = this.props.adContainerId;
				googletag.cmd.push(function () {
					googletag.display(adContainerId);
				});
			}
		}
	}, {
		key: 'componentDidMount',
		value: function componentDidMount() {
			this.refreshAd();
		}
	}, {
		key: 'componentWillUpdate',
		value: function componentWillUpdate(nextProps, nextState) {
			this.refreshAd();
		}
	}, {
		key: 'componentWillUnmount',
		value: function componentWillUnmount() {

			(0, _googleAdUtils.removeAdSlot)(this.props.adSlot);
			this.clearAdDiv();
		}
	}, {
		key: 'shouldComponentUpdate',
		value: function shouldComponentUpdate(nextProps, nextState) {
			if (this.props.uniqueKey === nextProps.uniqueKey) {
				return false;
			}
			return true;
		}
	}, {
		key: 'render',
		value: function render() {
			return _react2.default.createElement('div', { id: this.props.adContainerId, className: 'margin-top15 margin-top10 margin-bottom10 text-center' });
		}
	}]);

	return AdUnit;
}(_react2.default.Component);

module.exports = AdUnit;

/***/ }),
/* 22 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


function extractPageNoFromURL() {
	var url = window.location.pathname;
	//var matches =  url.match(/page\/(\d+)\/$/);
	var regex = /\d+(?=\D*$)/;
	var matches = regex.exec(url);

	if (matches) {
		return matches[0];
	} else {
		return 1; // no /page/no in url means first page
	}
}

function mapNewsArticleDataToInitialData(article) {
	if (!article) return null;
	var initialData = {
		ArticleUrl: article.ArticleUrl ? article.ArticleUrl : "",
		BasicId: article.BasicId ? article.BasicId : "",
		Title: article.Title ? article.Title : "",
		AuthorName: article.AuthorName ? article.AuthorName : "",
		DisplayDateTime: article.DisplayDateTime ? article.DisplayDateTime : "",
		ArticleApi: article.ArticleApi ? article.ArticleApi : "",
		LargePicUrl: article.LargePicUrl ? article.LargePicUrl : "",
		HostUrl: article.HostUrl ? article.HostUrl : "",
		AuthorMaskingName: article.AuthorMaskingName || ''
	};

	return initialData;
}

function extractPageCategoryFromURL() {
	var url = typeof window !== 'undefined' ? window.location.pathname : "";
	var regex = /\/m\/([A-Za-z0-9-_.~]+)\/{0,1}/;
	var res = url.match(regex);
	if (res != null && res.length > 1 && res[1] == "news") return "news";else if (res != null && res.length > 1 && res[1] == "expert-reviews") return "expert-reviews";else return "";
}

function isReactCategory(categoryName) {
	var allowedCategoriesInReact = ["NEWS", "EXPERT REVIEWS", "AutoExpo 2018"];
	if (allowedCategoriesInReact.indexOf(categoryName) !== -1) {
		return true;
	}
	return false;
}

module.exports = {
	extractPageNoFromURL: extractPageNoFromURL,
	mapNewsArticleDataToInitialData: mapNewsArticleDataToInitialData,
	extractPageCategoryFromURL: extractPageCategoryFromURL,
	isReactCategory: isReactCategory
};

/***/ }),
/* 23 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
    value: true
});

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

var _reactRouterDom = __webpack_require__(2);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var Breadcrumb = function (_React$Component) {
    _inherits(Breadcrumb, _React$Component);

    function Breadcrumb() {
        _classCallCheck(this, Breadcrumb);

        return _possibleConstructorReturn(this, (Breadcrumb.__proto__ || Object.getPrototypeOf(Breadcrumb)).apply(this, arguments));
    }

    _createClass(Breadcrumb, [{
        key: 'renderLastItem',
        value: function renderLastItem(item, index) {
            if (!item) return null;
            return _react2.default.createElement(
                'li',
                { key: index },
                _react2.default.createElement(
                    'span',
                    { className: 'breadcrumb-link__label' },
                    item.Title
                )
            );
        }
    }, {
        key: 'renderLinkableItem',
        value: function renderLinkableItem(item, index) {
            if (!item) {
                return null;
            }

            if (item.isReactLink) {
                return _react2.default.createElement(
                    'li',
                    { key: index },
                    _react2.default.createElement(
                        _reactRouterDom.Link,
                        { to: item.Href, title: item.Title, className: 'breadcrumb-link' },
                        _react2.default.createElement(
                            'span',
                            { className: 'breadcrumb-link__label' },
                            item.Title
                        )
                    )
                );
            }
            return _react2.default.createElement(
                'li',
                { key: index },
                _react2.default.createElement(
                    'a',
                    { className: 'breadcrumb-link', href: item.Href, title: item.Title },
                    _react2.default.createElement(
                        'span',
                        { className: 'breadcrumb-link__label' },
                        item.Title
                    )
                )
            );
        }
    }, {
        key: 'render',
        value: function render() {
            if (!this.props.breadcrumb || this.props.breadcrumb.length == 0) return null;
            var length = this.props.breadcrumb.length;
            return _react2.default.createElement(
                'section',
                null,
                _react2.default.createElement(
                    'div',
                    { className: 'breadcrumb' },
                    _react2.default.createElement(
                        'span',
                        { className: 'breadcrumb-title' },
                        'You are here:'
                    ),
                    _react2.default.createElement(
                        'ul',
                        null,
                        this.props.breadcrumb.map(function (item, index) {
                            if (index == length - 1) {
                                return this.renderLastItem(item, index);
                            } else {
                                return this.renderLinkableItem(item, index);
                            }
                        }.bind(this))
                    ),
                    _react2.default.createElement('div', { className: 'clear' })
                ),
                _react2.default.createElement('div', { className: 'clear' })
            );
        }
    }]);

    return Breadcrumb;
}(_react2.default.Component);

exports.default = Breadcrumb;

/***/ }),
/* 24 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
    value: true
});

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var Footer = function (_React$Component) {
    _inherits(Footer, _React$Component);

    function Footer() {
        _classCallCheck(this, Footer);

        return _possibleConstructorReturn(this, (Footer.__proto__ || Object.getPrototypeOf(Footer)).apply(this, arguments));
    }

    _createClass(Footer, [{
        key: "render",
        value: function render() {

            return _react2.default.createElement(
                "footer",
                { className: "bwm-footer" },
                _react2.default.createElement(
                    "div",
                    { className: "text-center padding-bottom15 border-solid-bottom" },
                    _react2.default.createElement(
                        "div",
                        { className: "grid-4" },
                        _react2.default.createElement("a", { href: "/m/", className: "bwmsprite bw-footer-icon", title: "Bikewale" })
                    ),
                    _react2.default.createElement(
                        "div",
                        { className: "grid-4" },
                        _react2.default.createElement("a", { href: "https://www.carwale.com/m/", target: "_blank", rel: "noopener", className: "bwmsprite cw-footer-icon", title: "CarWale" }),
                        _react2.default.createElement(
                            "p",
                            { className: "cw-logo-label" },
                            "ask the experts"
                        )
                    ),
                    _react2.default.createElement(
                        "div",
                        { className: "grid-4" },
                        _react2.default.createElement("a", { href: "https://m.cartrade.com/", target: "_blank", rel: "noopener", className: "bwmsprite ct-footer-icon", title: "CarTrade" })
                    ),
                    _react2.default.createElement("div", { className: "clear" })
                ),
                _react2.default.createElement(
                    "div",
                    { className: "text-center padding-top10 padding-bottom20 border-solid-bottom" },
                    _react2.default.createElement(
                        "ul",
                        { className: "footer-link-list" },
                        _react2.default.createElement(
                            "li",
                            null,
                            _react2.default.createElement(
                                "a",
                                { href: "/m/contactus.aspx", rel: "nofollow" },
                                "Contact Us"
                            )
                        ),
                        _react2.default.createElement(
                            "li",
                            null,
                            _react2.default.createElement(
                                "a",
                                { href: "/m/advertisewithus.aspx", rel: "nofollow" },
                                "Advertise with Us"
                            )
                        )
                    ),
                    _react2.default.createElement(
                        "p",
                        null,
                        "Download Mobile App"
                    ),
                    _react2.default.createElement("a", { href: "https://play.google.com/store/apps/details?id=com.bikewale.app&referrer=utm_source%3DBikeWaleMobileWebsite%26utm_medium%3DFooter%26utm_campaign%3DBikeWale%2520MobileWebsite%2520Footer", target: "_blank", className: "bwmsprite google-play-logo", rel: "noopener nofollow" }),
                    _react2.default.createElement("br", null),
                    _react2.default.createElement(
                        "a",
                        { href: "/?site=desktop", target: "_blank" },
                        "View Desktop Version"
                    )
                ),
                _react2.default.createElement(
                    "div",
                    { className: "font11 padding-top10" },
                    _react2.default.createElement(
                        "div",
                        { className: "grid-4" },
                        "\xA9 BikeWale India"
                    ),
                    _react2.default.createElement(
                        "div",
                        { className: "grid-8 text-right" },
                        _react2.default.createElement(
                            "a",
                            { href: "https://www.bikewale.com/visitoragreement.aspx", rel: "nofollow" },
                            "Visitor Agreement"
                        ),
                        "\xA0&\xA0",
                        _react2.default.createElement(
                            "a",
                            { href: "https://www.bikewale.com/privacypolicy.aspx", rel: "nofollow" },
                            "Privacy Policy"
                        )
                    ),
                    _react2.default.createElement("div", { className: "clear" })
                )
            );
        }
    }]);

    return Footer;
}(_react2.default.Component);

exports.default = Footer;

/***/ }),
/* 25 */
/***/ (function(module, exports, __webpack_require__) {

var __WEBPACK_AMD_DEFINE_ARRAY__, __WEBPACK_AMD_DEFINE_RESULT__;/*!
  Copyright (c) 2017 Jed Watson.
  Licensed under the MIT License (MIT), see
  http://jedwatson.github.io/classnames
*/
/* global define */

(function () {
	'use strict';

	var hasOwn = {}.hasOwnProperty;

	function classNames () {
		var classes = [];

		for (var i = 0; i < arguments.length; i++) {
			var arg = arguments[i];
			if (!arg) continue;

			var argType = typeof arg;

			if (argType === 'string' || argType === 'number') {
				classes.push(arg);
			} else if (Array.isArray(arg) && arg.length) {
				var inner = classNames.apply(null, arg);
				if (inner) {
					classes.push(inner);
				}
			} else if (argType === 'object') {
				for (var key in arg) {
					if (hasOwn.call(arg, key) && arg[key]) {
						classes.push(key);
					}
				}
			}
		}

		return classes.join(' ');
	}

	if (typeof module !== 'undefined' && module.exports) {
		classNames.default = classNames;
		module.exports = classNames;
	} else if (true) {
		// register as 'classnames', consistent with npm package name
		!(__WEBPACK_AMD_DEFINE_ARRAY__ = [], __WEBPACK_AMD_DEFINE_RESULT__ = (function () {
			return classNames;
		}).apply(exports, __WEBPACK_AMD_DEFINE_ARRAY__),
				__WEBPACK_AMD_DEFINE_RESULT__ !== undefined && (module.exports = __WEBPACK_AMD_DEFINE_RESULT__));
	} else {
		window.classNames = classNames;
	}
}());


/***/ }),
/* 26 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/**
 * Copyright 2014-2015, Facebook, Inc.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree. An additional grant
 * of patent rights can be found in the PATENTS file in the same directory.
 */



/**
 * Similar to invariant but only logs a warning if the condition is not met.
 * This can be used to log issues in development environments in critical
 * paths. Removing the logging code for production environments will keep the
 * same logic and follow the same code paths.
 */

var warning = function() {};

if (true) {
  warning = function(condition, format, args) {
    var len = arguments.length;
    args = new Array(len > 2 ? len - 2 : 0);
    for (var key = 2; key < len; key++) {
      args[key - 2] = arguments[key];
    }
    if (format === undefined) {
      throw new Error(
        '`warning(condition, format, ...args)` requires a warning ' +
        'message argument'
      );
    }

    if (format.length < 10 || (/^[s\W]*$/).test(format)) {
      throw new Error(
        'The warning format should be able to uniquely identify this ' +
        'warning. Please, use a more descriptive format than: ' + format
      );
    }

    if (!condition) {
      var argIndex = 0;
      var message = 'Warning: ' +
        format.replace(/%s/g, function() {
          return args[argIndex++];
        });
      if (typeof console !== 'undefined') {
        console.error(message);
      }
      try {
        // This error was thrown as a convenience so that you can use this stack
        // to find the callsite that caused this warning to fire.
        throw new Error(message);
      } catch(x) {}
    }
  };
}

module.exports = warning;


/***/ }),
/* 27 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "a", function() { return createLocation; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "b", function() { return locationsAreEqual; });
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_resolve_pathname__ = __webpack_require__(97);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_value_equal__ = __webpack_require__(98);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2__PathUtils__ = __webpack_require__(20);
var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };





var createLocation = function createLocation(path, state, key, currentLocation) {
  var location = void 0;
  if (typeof path === 'string') {
    // Two-arg form: push(path, state)
    location = Object(__WEBPACK_IMPORTED_MODULE_2__PathUtils__["d" /* parsePath */])(path);
    location.state = state;
  } else {
    // One-arg form: push(location)
    location = _extends({}, path);

    if (location.pathname === undefined) location.pathname = '';

    if (location.search) {
      if (location.search.charAt(0) !== '?') location.search = '?' + location.search;
    } else {
      location.search = '';
    }

    if (location.hash) {
      if (location.hash.charAt(0) !== '#') location.hash = '#' + location.hash;
    } else {
      location.hash = '';
    }

    if (state !== undefined && location.state === undefined) location.state = state;
  }

  try {
    location.pathname = decodeURI(location.pathname);
  } catch (e) {
    if (e instanceof URIError) {
      throw new URIError('Pathname "' + location.pathname + '" could not be decoded. ' + 'This is likely caused by an invalid percent-encoding.');
    } else {
      throw e;
    }
  }

  if (key) location.key = key;

  if (currentLocation) {
    // Resolve incomplete/relative pathname relative to current location.
    if (!location.pathname) {
      location.pathname = currentLocation.pathname;
    } else if (location.pathname.charAt(0) !== '/') {
      location.pathname = Object(__WEBPACK_IMPORTED_MODULE_0_resolve_pathname__["a" /* default */])(location.pathname, currentLocation.pathname);
    }
  } else {
    // When there is no prior location and pathname is empty, set it to /
    if (!location.pathname) {
      location.pathname = '/';
    }
  }

  return location;
};

var locationsAreEqual = function locationsAreEqual(a, b) {
  return a.pathname === b.pathname && a.search === b.search && a.hash === b.hash && a.key === b.key && Object(__WEBPACK_IMPORTED_MODULE_1_value_equal__["a" /* default */])(a.state, b.state);
};

/***/ }),
/* 28 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var originalPushState = null;
var originalReplaceState = null;

var scrollPosition = { x: -1, y: -1 };

if (typeof Object.assign != 'function') {
  Object.assign = function (target, varArgs) {
    // .length of function is 2
    'use strict';

    if (target == null) {
      // TypeError if undefined or null
      throw new TypeError('Cannot convert undefined or null to object');
    }

    var to = Object(target);

    for (var index = 1; index < arguments.length; index++) {
      var nextSource = arguments[index];

      if (nextSource != null) {
        // Skip over if undefined or null
        for (var nextKey in nextSource) {
          // Avoid bugs when hasOwnProperty is shadowed
          if (Object.prototype.hasOwnProperty.call(nextSource, nextKey)) {
            to[nextKey] = nextSource[nextKey];
          }
        }
      }
    }
    return to;
  };
}

function customPushState() {
  var newStateOfCurrentPage = Object.assign({}, window.history.state, {
    scrollToX: window.scrollX,
    scrollToY: window.scrollY

  });

  originalReplaceState.call(window.history, newStateOfCurrentPage, '');
  originalPushState.apply(window.history, arguments);
}

function customReplaceState(state) {
  var newState = Object.assign({}, {
    scrollToX: window.history.state && window.history.state.scrollToX >= 0 ? window.history.state.scrollToX >= 0 : -1,
    scrollToY: window.history.state && window.history.state.scrollToY >= 0 ? window.history.state.scrollToY >= 0 : -1
  }, state);

  for (var _len = arguments.length, otherArgs = Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
    otherArgs[_key - 1] = arguments[_key];
  }

  originalReplaceState.apply(window.history, [newState].concat(otherArgs));
}

function onPopState() {

  var state = window.history.state;
  if (state && Number.isFinite(state.scrollToX) && Number.isFinite(state.scrollToY)) {
    scrollPosition.x = state.scrollToX;
    scrollPosition.y = state.scrollToY;
  } else {
    resetScrollPosition();
  }
}

function wrapHistoryAPIFunction() {
  originalPushState = window.history.pushState;
  originalReplaceState = window.history.replaceState;

  window.history.pushState = customPushState;
  window.history.replaceState = customReplaceState;

  window.history.scrollRestoration = 'manual';
}

function resetScrollPosition() {

  scrollPosition.x = -1;
  scrollPosition.y = -1;
}

function isBrowserWithoutScrollSupport() {
  var ua = window.navigator.userAgent;
  if (ua.indexOf('UCBrowser') === -1 && !(ua.indexOf('Safari') != -1 && ua.indexOf('Chrome') === -1) && ua.indexOf('CriOS') === -1) {
    return false;
  }
  return true;
}

module.exports = {
  customPushState: customPushState,
  customReplaceState: customReplaceState,
  onPopState: onPopState,
  scrollPosition: scrollPosition,
  wrapHistoryAPIFunction: wrapHistoryAPIFunction,
  resetScrollPosition: resetScrollPosition,
  isBrowserWithoutScrollSupport: isBrowserWithoutScrollSupport
};

/***/ }),
/* 29 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var _commonUtils = __webpack_require__(3);

var _analyticsUtils = __webpack_require__(5);

var timer = null;
function resetTimer() {
	timer = null;
}
function startTimer(componentRenderCount, adCount) {
	// to be called only once by the api called first
	var startTime = new Date().getTime();
	timer = {
		adCount: adCount,
		componentRenderCount: componentRenderCount,
		startTime: startTime,
		endTime: 0
	};
	if (!(0, _commonUtils.isServer)()) {
		window.endTimer = endTimer;
	}
}

function endTimer(event) {
	if (timer) {
		switch (event) {
			case "component-render":
				timer.componentRenderCount = timer.componentRenderCount <= 0 ? 0 : timer.componentRenderCount - 1;
				break;
			case "ad":
				timer.adCount = timer.adCount <= 0 ? 0 : timer.adCount - 1;
				break;
		}
		if (timer.componentRenderCount == 0 && timer.adCount == 0) {
			timer.endTime = event === "component-render" ? new Date().getTime() + 50 : new Date().getTime();
			logTiming(timer);
			timer = null;
		}
	}
}

function logTiming() {
	if (timer) {
		var loadTime = timer.endTime - timer.startTime;
		if (window.FirstRenderWithAppshell) {
			window.FirstRenderWithAppshell = false;
		} else {
			_analyticsUtils.cwTracking.trackCustomData("BWPerformance", "MsitePWA", "navigationStart=" + timer.startTime + "|loadEventEnd=" + timer.endTime);
			if (window.FirstRenderWithAppshell == undefined || window.FirstRenderWithAppshell == null) window.FirstRenderWithAppshell = false;
		}
	}
}

module.exports = {
	resetTimer: resetTimer,
	startTimer: startTimer,
	endTimer: endTimer,
	logTiming: logTiming
};

/***/ }),
/* 30 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


function moreImagesUrl() {
    return "/m/images/";
}

function createImageUrl(hostUrl, originalImagePath, resolution) {
    return !hostUrl || !originalImagePath ? 'https://imgd.aeplcdn.com/160x89/bikewaleimg/images/noimage.png?q=70' : hostUrl + resolution + originalImagePath;
}

module.exports = {
    moreImagesUrl: moreImagesUrl,
    createImageUrl: createImageUrl
};

/***/ }),
/* 31 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
    value: true
});

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var Spinner = function Spinner() {
    return _react2.default.createElement(
        "div",
        { className: "speedometer__container loader-content loader-fixed" },
        _react2.default.createElement("span", { className: "loader" })
    );
};

exports.default = Spinner;

/***/ }),
/* 32 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_warning__ = __webpack_require__(26);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_warning___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_0_warning__);


var createTransitionManager = function createTransitionManager() {
  var prompt = null;

  var setPrompt = function setPrompt(nextPrompt) {
    __WEBPACK_IMPORTED_MODULE_0_warning___default()(prompt == null, 'A history supports only one prompt at a time');

    prompt = nextPrompt;

    return function () {
      if (prompt === nextPrompt) prompt = null;
    };
  };

  var confirmTransitionTo = function confirmTransitionTo(location, action, getUserConfirmation, callback) {
    // TODO: If another transition starts while we're still confirming
    // the previous one, we may end up in a weird state. Figure out the
    // best way to handle this.
    if (prompt != null) {
      var result = typeof prompt === 'function' ? prompt(location, action) : prompt;

      if (typeof result === 'string') {
        if (typeof getUserConfirmation === 'function') {
          getUserConfirmation(result, callback);
        } else {
          __WEBPACK_IMPORTED_MODULE_0_warning___default()(false, 'A history needs a getUserConfirmation function in order to use a prompt message');

          callback(true);
        }
      } else {
        // Return false from a transition hook to cancel the transition.
        callback(result !== false);
      }
    } else {
      callback(true);
    }
  };

  var listeners = [];

  var appendListener = function appendListener(fn) {
    var isActive = true;

    var listener = function listener() {
      if (isActive) fn.apply(undefined, arguments);
    };

    listeners.push(listener);

    return function () {
      isActive = false;
      listeners = listeners.filter(function (item) {
        return item !== listener;
      });
    };
  };

  var notifyListeners = function notifyListeners() {
    for (var _len = arguments.length, args = Array(_len), _key = 0; _key < _len; _key++) {
      args[_key] = arguments[_key];
    }

    listeners.forEach(function (listener) {
      return listener.apply(undefined, args);
    });
  };

  return {
    setPrompt: setPrompt,
    confirmTransitionTo: confirmTransitionTo,
    appendListener: appendListener,
    notifyListeners: notifyListeners
  };
};

/* harmony default export */ __webpack_exports__["a"] = (createTransitionManager);

/***/ }),
/* 33 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_react_router_es_Router__ = __webpack_require__(34);
// Written in this round about way for babel-transform-imports


/* harmony default export */ __webpack_exports__["a"] = (__WEBPACK_IMPORTED_MODULE_0_react_router_es_Router__["a" /* default */]);

/***/ }),
/* 34 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_warning__ = __webpack_require__(13);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_warning___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_0_warning__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_invariant__ = __webpack_require__(7);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_invariant___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_1_invariant__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2_react__ = __webpack_require__(0);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_3_prop_types__ = __webpack_require__(1);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_3_prop_types___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_3_prop_types__);
var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }






/**
 * The public API for putting history on context.
 */

var Router = function (_React$Component) {
  _inherits(Router, _React$Component);

  function Router() {
    var _temp, _this, _ret;

    _classCallCheck(this, Router);

    for (var _len = arguments.length, args = Array(_len), _key = 0; _key < _len; _key++) {
      args[_key] = arguments[_key];
    }

    return _ret = (_temp = (_this = _possibleConstructorReturn(this, _React$Component.call.apply(_React$Component, [this].concat(args))), _this), _this.state = {
      match: _this.computeMatch(_this.props.history.location.pathname)
    }, _temp), _possibleConstructorReturn(_this, _ret);
  }

  Router.prototype.getChildContext = function getChildContext() {
    return {
      router: _extends({}, this.context.router, {
        history: this.props.history,
        route: {
          location: this.props.history.location,
          match: this.state.match
        }
      })
    };
  };

  Router.prototype.computeMatch = function computeMatch(pathname) {
    return {
      path: "/",
      url: "/",
      params: {},
      isExact: pathname === "/"
    };
  };

  Router.prototype.componentWillMount = function componentWillMount() {
    var _this2 = this;

    var _props = this.props,
        children = _props.children,
        history = _props.history;


    __WEBPACK_IMPORTED_MODULE_1_invariant___default()(children == null || __WEBPACK_IMPORTED_MODULE_2_react__["default"].Children.count(children) === 1, "A <Router> may have only one child element");

    // Do this here so we can setState when a <Redirect> changes the
    // location in componentWillMount. This happens e.g. when doing
    // server rendering using a <StaticRouter>.
    this.unlisten = history.listen(function () {
      _this2.setState({
        match: _this2.computeMatch(history.location.pathname)
      });
    });
  };

  Router.prototype.componentWillReceiveProps = function componentWillReceiveProps(nextProps) {
    __WEBPACK_IMPORTED_MODULE_0_warning___default()(this.props.history === nextProps.history, "You cannot change <Router history>");
  };

  Router.prototype.componentWillUnmount = function componentWillUnmount() {
    this.unlisten();
  };

  Router.prototype.render = function render() {
    var children = this.props.children;

    return children ? __WEBPACK_IMPORTED_MODULE_2_react__["default"].Children.only(children) : null;
  };

  return Router;
}(__WEBPACK_IMPORTED_MODULE_2_react__["default"].Component);

Router.propTypes = {
  history: __WEBPACK_IMPORTED_MODULE_3_prop_types___default.a.object.isRequired,
  children: __WEBPACK_IMPORTED_MODULE_3_prop_types___default.a.node
};
Router.contextTypes = {
  router: __WEBPACK_IMPORTED_MODULE_3_prop_types___default.a.object
};
Router.childContextTypes = {
  router: __WEBPACK_IMPORTED_MODULE_3_prop_types___default.a.object.isRequired
};


/* harmony default export */ __webpack_exports__["a"] = (Router);

/***/ }),
/* 35 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_path_to_regexp__ = __webpack_require__(53);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_path_to_regexp___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_0_path_to_regexp__);


var patternCache = {};
var cacheLimit = 10000;
var cacheCount = 0;

var compilePath = function compilePath(pattern, options) {
  var cacheKey = "" + options.end + options.strict + options.sensitive;
  var cache = patternCache[cacheKey] || (patternCache[cacheKey] = {});

  if (cache[pattern]) return cache[pattern];

  var keys = [];
  var re = __WEBPACK_IMPORTED_MODULE_0_path_to_regexp___default()(pattern, keys, options);
  var compiledPattern = { re: re, keys: keys };

  if (cacheCount < cacheLimit) {
    cache[pattern] = compiledPattern;
    cacheCount++;
  }

  return compiledPattern;
};

/**
 * Public API for matching a URL pathname to a path pattern.
 */
var matchPath = function matchPath(pathname) {
  var options = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {};
  var parent = arguments[2];

  if (typeof options === "string") options = { path: options };

  var _options = options,
      path = _options.path,
      _options$exact = _options.exact,
      exact = _options$exact === undefined ? false : _options$exact,
      _options$strict = _options.strict,
      strict = _options$strict === undefined ? false : _options$strict,
      _options$sensitive = _options.sensitive,
      sensitive = _options$sensitive === undefined ? false : _options$sensitive;


  if (path == null) return parent;

  var _compilePath = compilePath(path, { end: exact, strict: strict, sensitive: sensitive }),
      re = _compilePath.re,
      keys = _compilePath.keys;

  var match = re.exec(pathname);

  if (!match) return null;

  var url = match[0],
      values = match.slice(1);

  var isExact = pathname === url;

  if (exact && !isExact) return null;

  return {
    path: path, // the path pattern used to match
    url: path === "/" && url === "" ? "/" : url, // the matched portion of the URL
    isExact: isExact, // whether or not we matched exactly
    params: keys.reduce(function (memo, key, index) {
      memo[key.name] = values[index];
      return memo;
    }, {})
  };
};

/* harmony default export */ __webpack_exports__["a"] = (matchPath);

/***/ }),
/* 36 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var _commonUtils = __webpack_require__(3);

function addAdToGTcmd(adUnitPath, adDimension, adDivId, adTarget) {
  googletag.cmd.push(function () {
    for (var tagKey in adTarget) {
      if (adTarget[tagKey]) {
        googletag.pubads().setTargeting(tagKey, (0, _commonUtils.RemoveSpecialCharacters)(adTarget[tagKey]));
      } else {
        googletag.pubads().clearTargeting(tagKey);
      }
    }

    var slot = googletag.defineSlot(adUnitPath, adDimension, adDivId);
    if (!slot) return;
    slot.addService(googletag.pubads());
    googletag.pubads().enableAsyncRendering();
    googletag.pubads().updateCorrelator();
    googletag.pubads().enableSingleRequest(false);

    googletag.pubads().collapseEmptyDivs();
    googletag.enableServices();
  });
}

function adSlotTargeting() {
  var objTargetingData = bwcache.get("useradtargeting", true);
  if (!objTargetingData || Object.keys(objTargetingData).length == 0) {
    var xhr = new XMLHttpRequest();
    xhr.onreadystatechange = function () {
      if (xhr.response && xhr.response != "") {
        objTargetingData = JSON.parse(xhr.response).targetingData;
        bwcache.set("useradtargeting", objTargetingData, true);
      }
    };
    xhr.open("GET", "/api/userprofiletargeting/", false);
    xhr.send();
  }
  return objTargetingData;
}

function addAdSlot(adUnitPath, adDimension, adDivId, tags) {
  try {
    if (!googletag && !googletag.cmd) return;

    var adExists = false; // remains false if the ad slot has already been defined irrespective of whether googletag has loaded

    // event listener slotrenderended is added in appshell itself as it is not specific to any particuler ad
    if (googletag.apiReady) {
      var slots = googletag.pubads().getSlots();
      for (var i = 0; i < slots.length; i++) {
        var ad = slots[i];
        if (ad.getAdUnitPath() === adUnitPath) {
          adExists = true;
          break;
        }
      }
    }

    if (!adExists) {
      var adTarget = Object.assign({}, tags, adSlotTargeting());
      addAdToGTcmd(adUnitPath, adDimension, adDivId, adTarget);
    }
  } catch (e) {}
}

function removeAdSlot(adUnitPath) {
  try {
    if (typeof googletag == "undefined" || !googletag.apiReady) {
      return;
    }
    var slots = googletag.pubads().getSlots();
    for (var i = 0; i < slots.length; i++) {
      var ad = slots[i];
      if (ad.getAdUnitPath() === adUnitPath) {
        googletag.destroySlots([ad]);
        break;
      }
    }
  } catch (e) {}
}

function refreshGPTAds() {
  try {
    setTimeout(function () {
      if (typeof googletag != "undefined") {
        googletag.cmd.push(function () {
          googletag.pubads().refresh();
        });
      }
    }, 100);
  } catch (e) {}
}

module.exports = {
  addAdSlot: addAdSlot,
  removeAdSlot: removeAdSlot,
  refreshGPTAds: refreshGPTAds
};

/***/ }),
/* 37 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
var lockScroll = exports.lockScroll = function lockScroll() {
  var doc = document.documentElement;
  var body = document.body;

  if (body.clientHeight > window.innerHeight) {
    var scrollTopPosition = window.pageYOffset || doc.scrollTop;

    if (scrollTopPosition < 0) {
      scrollTopPosition = 0;
    }

    doc.style.overflowY = "hidden";
    doc.style.position = "fixed";
    doc.style.top = -scrollTopPosition + "px";
  }
};

var unlockScroll = exports.unlockScroll = function unlockScroll() {
  var doc = document.documentElement;
  var scrollTopPosition = parseInt(doc.style.top);

  doc.style.overflowY = "visible";
  doc.style.position = "";
  window.scrollTo(0, -scrollTopPosition);
};

/***/ }),
/* 38 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});

/* Return the closest element matching a selector up the DOM tree
 * https://github.com/jonathantneal/closest
 */

exports.default = function () {
  if (typeof window !== 'undefined') {
    var ElementProto = window.Element.prototype;
    if (typeof ElementProto.matches !== 'function') {
      ElementProto.matches = ElementProto.msMatchesSelector || ElementProto.mozMatchesSelector || ElementProto.webkitMatchesSelector || function matches(selector) {
        var element = this;
        var elements = (element.document || element.ownerDocument).querySelectorAll(selector);
        var index = 0;

        while (elements[index] && elements[index] !== element) {
          ++index;
        }

        return Boolean(elements[index]);
      };
    }

    if (typeof ElementProto.closest !== 'function') {
      ElementProto.closest = function closest(selector) {
        var element = this;

        while (element && element.nodeType === 1) {
          if (element.matches(selector)) {
            return element;
          }

          element = element.parentNode;
        }

        return null;
      };
    }
  }
  return null;
}();

/***/ }),
/* 39 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony export (immutable) */ __webpack_exports__["a"] = warning;
/**
 * Prints a warning in the console if it exists.
 *
 * @param {String} message The warning message.
 * @returns {void}
 */
function warning(message) {
  /* eslint-disable no-console */
  if (typeof console !== 'undefined' && typeof console.error === 'function') {
    console.error(message);
  }
  /* eslint-enable no-console */
  try {
    // This error was thrown as a convenience so that if you enable
    // "break on all exceptions" in your console,
    // it would pause the execution at this line.
    throw new Error(message);
    /* eslint-disable no-empty */
  } catch (e) {}
  /* eslint-enable no-empty */
}

/***/ }),
/* 40 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0__baseGetTag_js__ = __webpack_require__(177);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1__getPrototype_js__ = __webpack_require__(182);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2__isObjectLike_js__ = __webpack_require__(184);




/** `Object#toString` result references. */
var objectTag = '[object Object]';

/** Used for built-in method references. */
var funcProto = Function.prototype,
    objectProto = Object.prototype;

/** Used to resolve the decompiled source of functions. */
var funcToString = funcProto.toString;

/** Used to check objects for own properties. */
var hasOwnProperty = objectProto.hasOwnProperty;

/** Used to infer the `Object` constructor. */
var objectCtorString = funcToString.call(Object);

/**
 * Checks if `value` is a plain object, that is, an object created by the
 * `Object` constructor or one with a `[[Prototype]]` of `null`.
 *
 * @static
 * @memberOf _
 * @since 0.8.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is a plain object, else `false`.
 * @example
 *
 * function Foo() {
 *   this.a = 1;
 * }
 *
 * _.isPlainObject(new Foo);
 * // => false
 *
 * _.isPlainObject([1, 2, 3]);
 * // => false
 *
 * _.isPlainObject({ 'x': 0, 'y': 0 });
 * // => true
 *
 * _.isPlainObject(Object.create(null));
 * // => true
 */
function isPlainObject(value) {
  if (!Object(__WEBPACK_IMPORTED_MODULE_2__isObjectLike_js__["a" /* default */])(value) || Object(__WEBPACK_IMPORTED_MODULE_0__baseGetTag_js__["a" /* default */])(value) != objectTag) {
    return false;
  }
  var proto = Object(__WEBPACK_IMPORTED_MODULE_1__getPrototype_js__["a" /* default */])(value);
  if (proto === null) {
    return true;
  }
  var Ctor = hasOwnProperty.call(proto, 'constructor') && proto.constructor;
  return typeof Ctor == 'function' && Ctor instanceof Ctor &&
    funcToString.call(Ctor) == objectCtorString;
}

/* harmony default export */ __webpack_exports__["a"] = (isPlainObject);


/***/ }),
/* 41 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
    value: true
});
exports.openEmiCalculator = exports.updateDownPaymentSlider = undefined;

var _emiActionTypes = __webpack_require__(42);

var dispatchTimerId = undefined;
var updateDownpaymentSliderValue = function updateDownpaymentSliderValue(values, userChange) {
    values = values.map(function (x) {
        return parseInt(x);
    });
    return {
        type: _emiActionTypes.emiCalculatorAction.UPDATE_DOWNPAYMENT_SLIDER_VALUE,
        values: values,
        userChange: userChange
    };
};

var shouldDownpaymentSliderUpdate = function shouldDownpaymentSliderUpdate(values, slider) {
    var update = true;
    if (slider.values[0] === values[0]) {
        update = false;
    }
    return update;
};

var updateDownPaymentSlider = exports.updateDownPaymentSlider = function updateDownPaymentSlider(_ref) {
    var values = _ref.values,
        userChange = _ref.userChange;
    return function (dispatch, getState) {
        var downpaymentSliderMap = getState(['Emi', 'VehicleDownPayment', 'slider']);
        if (shouldDownpaymentSliderUpdate(values, downpaymentSliderMap)) {
            clearTimeout(dispatchTimerId);
            dispatchTimerId = setTimeout(function () {
                dispatch(updateDownpaymentSliderValue(values, userChange));
            }, 200);
        }
    };
};

var openEmiCalculator = exports.openEmiCalculator = function openEmiCalculator(onRoadPrice) {
    return function (dispatch) {
        var minDnPay = .1 * onRoadPrice;
        var maxDnPay = .4 * onRoadPrice;
        var defaultDnPay = .3 * onRoadPrice;
        var payload = {
            min: parseInt(minDnPay),
            max: parseInt(maxDnPay),
            values: [parseInt(defaultDnPay)],
            onRoadPrice: onRoadPrice
        };
        dispatch({
            type: _emiActionTypes.emiCalculatorAction.OPEN_EMICALCULATOR,
            payload: payload
        });
    };
};

/***/ }),
/* 42 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var emiCalculatorAction = {
	OPEN_EMICALCULATOR: 'OPEN_EMICALCULATOR',
	UPDATE_DOWNPAYMENT_SLIDER_VALUE: 'UPDATE_DOWNPAYMENT_SLIDER_VALUE',
	UPDATE_TENURE_SLIDER_VALUE: 'UPDATE_TENURE_SLIDER_VALUE',
	UPDATE_INTEREST_SLIDER_VALUE: 'UPDATE_INTEREST_SLIDER_VALUE',
	UPDATE_PIE_VALUE: 'UPDATE_PIE_VALUE'
};

module.exports = {
	emiCalculatorAction: emiCalculatorAction
};

/***/ }),
/* 43 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
	value: true
});

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

var _propTypes = __webpack_require__(1);

var _propTypes2 = _interopRequireDefault(_propTypes);

var _commonUtils = __webpack_require__(3);

var _sliderConstants = __webpack_require__(211);

var SliderConstants = _interopRequireWildcard(_sliderConstants);

var _linear = __webpack_require__(85);

var _linear2 = _interopRequireDefault(_linear);

function _interopRequireWildcard(obj) { if (obj && obj.__esModule) { return obj; } else { var newObj = {}; if (obj != null) { for (var key in obj) { if (Object.prototype.hasOwnProperty.call(obj, key)) newObj[key] = obj[key]; } } newObj.default = obj; return newObj; } }

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _defineProperty(obj, key, value) { if (key in obj) { Object.defineProperty(obj, key, { value: value, enumerable: true, configurable: true, writable: true }); } else { obj[key] = value; } return obj; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var has = Object.prototype.hasOwnProperty;

var PropTypeArrOfNumber = _propTypes2.default.arrayOf(_propTypes2.default.number);
var PropTypeReactComponent = _propTypes2.default.oneOfType([_propTypes2.default.func, _propTypes2.default.string]);

function getClassName(props) {
	var orientation = props.orientation === 'vertical' ? 'rheostat-vertical' : 'rheostat-horizontal';

	return ['rheostat', orientation].concat(props.className.split(' ')).join(' ');
}

function getHandleFor(ev) {
	return Number(ev.currentTarget.getAttribute('data-handle-key'));
}

function killEvent(ev) {
	ev.stopPropagation();
	if (window.isBrowserUC) {
		ev.preventDefault();
	}
}

function Button(props) {
	var showTooltip = props['data-tooltip'];


	return _react2.default.createElement(
		'button',
		_extends({}, props, { type: 'button' }),
		showTooltip ? _react2.default.createElement(
			'span',
			{ className: 'rheostat-handle-tooltip' },
			props.children
		) : ''
	);
}

var propTypes = {
	// the algorithm to use
	algorithm: _propTypes2.default.shape({
		getValue: _propTypes2.default.func,
		getPosition: _propTypes2.default.func
	}),
	// any children you pass in
	children: _propTypes2.default.node,
	// standard class name you'd like to apply to the root element
	className: _propTypes2.default.string,
	// prevent the slider from moving when clicked
	disabled: _propTypes2.default.bool,
	// a custom handle you can pass in
	handle: PropTypeReactComponent,
	// a custom handle tooltip
	handleTooltip: _propTypes2.default.bool,
	// a custom method to format tooltip value
	handleTooltipLabel: _propTypes2.default.func,
	// the maximum possible value
	max: _propTypes2.default.number,
	// the minimum possible value
	min: _propTypes2.default.number,
	// called on click
	onClick: _propTypes2.default.func,
	// called whenever the user is done changing values on the slider
	onChange: _propTypes2.default.func,
	// called on key press
	onKeyPress: _propTypes2.default.func,
	// called when you finish dragging a handle
	onSliderDragEnd: _propTypes2.default.func,
	// called every time the slider is dragged and the value changes
	onSliderDragMove: _propTypes2.default.func,
	// called when you start dragging a handle
	onSliderDragStart: _propTypes2.default.func,
	// called whenever the user is actively changing the values on the slider
	// (dragging, clicked, keypress)
	onValuesUpdated: _propTypes2.default.func,
	// the orientation
	orientation: _propTypes2.default.oneOf(['horizontal', 'vertical']),
	// a component for rendering the pits
	pitComponent: PropTypeReactComponent,
	// the points that pits are rendered on
	pitPoints: PropTypeArrOfNumber,
	// a custom function to format pit points
	pitPointLabel: _propTypes2.default.func,
	// a custom progress bar you can pass in
	progressBar: PropTypeReactComponent,
	// should we snap?
	snap: _propTypes2.default.bool,
	// the points we should snap to
	snapPoints: PropTypeArrOfNumber,
	// snap handle on drag move
	snapOnDragMove: _propTypes2.default.bool,
	// disable snap on click
	disableSnapOnClick: _propTypes2.default.bool,
	// the values
	values: PropTypeArrOfNumber
};

var defaultProps = {
	algorithm: _linear2.default,
	className: '',
	children: null,
	disabled: false,
	handle: Button,
	handleTooltip: true,
	handleTooltipLabel: null,
	max: SliderConstants.PERCENT_FULL,
	min: SliderConstants.PERCENT_EMPTY,
	onClick: null,
	onChange: null,
	onKeyPress: null,
	onSliderDragEnd: null,
	onSliderDragMove: null,
	onSliderDragStart: null,
	onValuesUpdated: null,
	orientation: 'horizontal',
	pitComponent: null,
	pitPoints: [],
	pitPointLabel: null,
	progressBar: 'div',
	snap: false,
	snapPoints: [],
	snapOnDragMove: false,
	disableSnapOnClick: false,
	values: [SliderConstants.PERCENT_EMPTY]
};

var Rheostat = function (_React$Component) {
	_inherits(Rheostat, _React$Component);

	function Rheostat(props) {
		_classCallCheck(this, Rheostat);

		var _this = _possibleConstructorReturn(this, (Rheostat.__proto__ || Object.getPrototypeOf(Rheostat)).call(this, props));

		if (typeof window.isBrowserUC === 'undefined') {
			(0, _commonUtils.detectBrowser)();
		}
		var _this$props = _this.props,
		    algorithm = _this$props.algorithm,
		    max = _this$props.max,
		    min = _this$props.min,
		    values = _this$props.values;


		_this.state = {
			className: getClassName(_this.props),
			handlePos: values.map(function (value) {
				return algorithm.getPosition(value, min, max);
			}),
			handleDimensions: 0,
			sliderBox: {},
			slidingIndex: null,
			values: values
		};

		_this.getPublicState = _this.getPublicState.bind(_this);
		_this.getSliderBoundingBox = _this.getSliderBoundingBox.bind(_this);
		_this.getProgressStyle = _this.getProgressStyle.bind(_this);
		_this.getMinValue = _this.getMinValue.bind(_this);
		_this.getMaxValue = _this.getMaxValue.bind(_this);
		_this.getHandleDimensions = _this.getHandleDimensions.bind(_this);
		_this.getClosestSnapPoint = _this.getClosestSnapPoint.bind(_this);
		_this.getSnapPosition = _this.getSnapPosition.bind(_this);
		_this.getNextPositionForKey = _this.getNextPositionForKey.bind(_this);
		_this.getNextState = _this.getNextState.bind(_this);
		_this.handleClick = _this.handleClick.bind(_this);
		_this.getClosestHandle = _this.getClosestHandle.bind(_this);
		_this.setStartSlide = _this.setStartSlide.bind(_this);
		_this.startMouseSlide = _this.startMouseSlide.bind(_this);
		_this.startTouchSlide = _this.startTouchSlide.bind(_this);
		_this.handleMouseSlide = _this.handleMouseSlide.bind(_this);
		_this.handleTouchSlide = _this.handleTouchSlide.bind(_this);
		_this.handleSlide = _this.handleSlide.bind(_this);
		_this.endSlide = _this.endSlide.bind(_this);
		_this.handleKeydown = _this.handleKeydown.bind(_this);
		_this.validatePosition = _this.validatePosition.bind(_this);
		_this.validateValues = _this.validateValues.bind(_this);
		_this.canMove = _this.canMove.bind(_this);
		_this.fireChangeEvent = _this.fireChangeEvent.bind(_this);
		_this.slideTo = _this.slideTo.bind(_this);
		_this.updateNewValues = _this.updateNewValues.bind(_this);
		_this.setRef = _this.setRef.bind(_this);
		return _this;
	}

	_createClass(Rheostat, [{
		key: 'componentWillReceiveProps',
		value: function componentWillReceiveProps(nextProps) {
			var _props = this.props,
			    className = _props.className,
			    disabled = _props.disabled,
			    min = _props.min,
			    max = _props.max,
			    orientation = _props.orientation;
			var _state = this.state,
			    values = _state.values,
			    slidingIndex = _state.slidingIndex;


			var minMaxChanged = nextProps.min !== min || nextProps.max !== max;

			var valuesChanged = values.length !== nextProps.values.length || values.some(function (value, idx) {
				return nextProps.values[idx] !== value;
			});

			var orientationChanged = nextProps.className !== className || nextProps.orientation !== orientation;

			var willBeDisabled = nextProps.disabled && !disabled;

			if (orientationChanged) {
				this.setState({
					className: getClassName(nextProps)
				});
			}

			if (minMaxChanged || valuesChanged) this.updateNewValues(nextProps);

			if (willBeDisabled && slidingIndex !== null) {
				this.endSlide();
			}
		}
	}, {
		key: 'getPublicState',
		value: function getPublicState() {
			var _props2 = this.props,
			    min = _props2.min,
			    max = _props2.max;
			var values = this.state.values;


			return { max: max, min: min, values: values };
		}

		// istanbul ignore next

	}, {
		key: 'getSliderBoundingBox',
		value: function getSliderBoundingBox() {
			var rheostat = this.rheostat;
			var node = rheostat.getDOMNode ? rheostat.getDOMNode() : rheostat;
			var rect = node.getBoundingClientRect();

			return {
				height: rect.height || node.clientHeight,
				left: rect.left,
				top: rect.top,
				width: rect.width || node.clientWidth
			};
		}
	}, {
		key: 'getProgressStyle',
		value: function getProgressStyle(idx) {
			var handlePos = this.state.handlePos;


			var value = handlePos[idx];

			if (idx === 0) {
				return this.props.orientation === 'vertical' ? { height: value + '%', top: 0 } : { left: 0, width: value + '%' };
			}

			var prevValue = handlePos[idx - 1];
			var diffValue = value - prevValue;

			return this.props.orientation === 'vertical' ? { height: diffValue + '%', top: prevValue + '%' } : { left: prevValue + '%', width: diffValue + '%' };
		}
	}, {
		key: 'getMinValue',
		value: function getMinValue(idx) {
			return this.state.values[idx - 1] ? Math.max(this.props.min, this.state.values[idx - 1]) : this.props.min;
		}
	}, {
		key: 'getMaxValue',
		value: function getMaxValue(idx) {
			return this.state.values[idx + 1] ? Math.min(this.props.max, this.state.values[idx + 1]) : this.props.max;
		}

		// istanbul ignore next

	}, {
		key: 'getHandleDimensions',
		value: function getHandleDimensions(ev, sliderBox) {
			var handleNode = ev.currentTarget || null;

			if (!handleNode) return 0;

			return this.props.orientation === 'vertical' ? handleNode.clientHeight / sliderBox.height * SliderConstants.PERCENT_FULL / 2 : handleNode.clientWidth / sliderBox.width * SliderConstants.PERCENT_FULL / 2;
		}
	}, {
		key: 'getClosestSnapPoint',
		value: function getClosestSnapPoint(value) {
			if (!this.props.snapPoints.length) return value;

			return this.props.snapPoints.reduce(function (snapTo, snap) {
				return Math.abs(snapTo - value) < Math.abs(snap - value) ? snapTo : snap;
			});
		}
	}, {
		key: 'getSnapPosition',
		value: function getSnapPosition(positionPercent) {
			if (!this.props.snap) return positionPercent;

			var _props3 = this.props,
			    algorithm = _props3.algorithm,
			    max = _props3.max,
			    min = _props3.min;


			var value = algorithm.getValue(positionPercent, min, max);

			var snapValue = this.getClosestSnapPoint(value);

			return algorithm.getPosition(snapValue, min, max);
		}
	}, {
		key: 'getNextPositionForKey',
		value: function getNextPositionForKey(idx, keyCode) {
			var _stepMultiplier;

			var _state2 = this.state,
			    handlePos = _state2.handlePos,
			    values = _state2.values;
			var _props4 = this.props,
			    algorithm = _props4.algorithm,
			    max = _props4.max,
			    min = _props4.min,
			    snapPoints = _props4.snapPoints;


			var shouldSnap = this.props.snap;

			var proposedValue = values[idx];
			var proposedPercentage = handlePos[idx];
			var originalPercentage = proposedPercentage;
			var stepValue = 1;

			if (max >= 100) {
				proposedPercentage = Math.round(proposedPercentage);
			} else {
				stepValue = 100 / (max - min);
			}

			var currentIndex = null;

			if (shouldSnap) {
				currentIndex = snapPoints.indexOf(this.getClosestSnapPoint(values[idx]));
			}

			var stepMultiplier = (_stepMultiplier = {}, _defineProperty(_stepMultiplier, SliderConstants.KEYS.LEFT, function (v) {
				return v * -1;
			}), _defineProperty(_stepMultiplier, SliderConstants.KEYS.RIGHT, function (v) {
				return v * 1;
			}), _defineProperty(_stepMultiplier, SliderConstants.KEYS.UP, function (v) {
				return v * 1;
			}), _defineProperty(_stepMultiplier, SliderConstants.KEYS.DOWN, function (v) {
				return v * -1;
			}), _defineProperty(_stepMultiplier, SliderConstants.KEYS.PAGE_DOWN, function (v) {
				return v > 1 ? -v : v * -10;
			}), _defineProperty(_stepMultiplier, SliderConstants.KEYS.PAGE_UP, function (v) {
				return v > 1 ? v : v * 10;
			}), _stepMultiplier);

			if (has.call(stepMultiplier, keyCode)) {
				proposedPercentage += stepMultiplier[keyCode](stepValue);

				if (shouldSnap) {
					if (proposedPercentage > originalPercentage) {
						// move cursor right unless overflow
						if (currentIndex < snapPoints.length - 1) {
							proposedValue = snapPoints[currentIndex + 1];
						}
						// move cursor left unless there is overflow
					} else if (currentIndex > 0) {
						proposedValue = snapPoints[currentIndex - 1];
					}
				}
			} else if (keyCode === SliderConstants.KEYS.HOME) {
				proposedPercentage = SliderConstants.PERCENT_EMPTY;

				if (shouldSnap) {
					proposedValue = snapPoints[0];
				}
			} else if (keyCode === SliderConstants.KEYS.END) {
				proposedPercentage = SliderConstants.PERCENT_FULL;

				if (shouldSnap) {
					proposedValue = snapPoints[snapPoints.length - 1];
				}
			} else {
				return null;
			}

			return shouldSnap ? algorithm.getPosition(proposedValue, min, max) : proposedPercentage;
		}
	}, {
		key: 'getNextState',
		value: function getNextState(idx, proposedPosition) {
			var _this2 = this;

			var handlePos = this.state.handlePos;
			var _props5 = this.props,
			    max = _props5.max,
			    min = _props5.min;


			var actualPosition = this.validatePosition(idx, proposedPosition);

			var nextHandlePos = handlePos.map(function (pos, index) {
				return index === idx ? actualPosition : pos;
			});

			return {
				handlePos: nextHandlePos,
				values: nextHandlePos.map(function (pos) {
					return _this2.props.algorithm.getValue(pos, min, max);
				})
			};
		}
	}, {
		key: 'getClosestHandle',
		value: function getClosestHandle(positionPercent) {
			var handlePos = this.state.handlePos;


			return handlePos.reduce(function (closestIdx, node, idx) {
				var challenger = Math.abs(handlePos[idx] - positionPercent);
				var current = Math.abs(handlePos[closestIdx] - positionPercent);
				return challenger < current ? idx : closestIdx;
			}, 0);
		}

		// istanbul ignore next

	}, {
		key: 'setStartSlide',
		value: function setStartSlide(ev, x, y) {
			var sliderBox = this.getSliderBoundingBox();

			this.setState({
				handleDimensions: this.getHandleDimensions(ev, sliderBox),
				sliderBox: sliderBox,
				slidingIndex: getHandleFor(ev)
			});
		}
	}, {
		key: 'setRef',
		value: function setRef(ref) {
			this.rheostat = ref;
		}

		// istanbul ignore next

	}, {
		key: 'startMouseSlide',
		value: function startMouseSlide(ev) {
			this.setStartSlide(ev, ev.clientX, ev.clientY);

			if (typeof document.addEventListener === 'function') {
				document.addEventListener('mousemove', this.handleMouseSlide, false);
				document.addEventListener('mouseup', this.endSlide, false);
			} else {
				document.attachEvent('onmousemove', this.handleMouseSlide);
				document.attachEvent('onmouseup', this.endSlide);
			}

			killEvent(ev);
		}

		// istanbul ignore next

	}, {
		key: 'startTouchSlide',
		value: function startTouchSlide(ev) {
			if (ev.changedTouches.length > 1) return;

			var touch = ev.changedTouches[0];

			this.setStartSlide(ev, touch.clientX, touch.clientY);

			document.addEventListener('touchmove', this.handleTouchSlide, false);
			document.addEventListener('touchend', this.endSlide, false);

			if (this.props.onSliderDragStart) this.props.onSliderDragStart();
			killEvent(ev);
		}

		// istanbul ignore next

	}, {
		key: 'handleMouseSlide',
		value: function handleMouseSlide(ev) {
			if (this.state.slidingIndex === null) return;
			this.handleSlide(ev.clientX, ev.clientY);
			killEvent(ev);
		}

		// istanbul ignore next

	}, {
		key: 'handleTouchSlide',
		value: function handleTouchSlide(ev) {
			if (this.state.slidingIndex === null) return;

			if (ev.changedTouches.length > 1) {
				this.endSlide();
				return;
			}

			var touch = ev.changedTouches[0];
			this.handleSlide(touch.clientX, touch.clientY);
			killEvent(ev);
		}

		// istanbul ignore next

	}, {
		key: 'handleSlide',
		value: function handleSlide(x, y) {
			var _this3 = this;

			var _state3 = this.state,
			    idx = _state3.slidingIndex,
			    sliderBox = _state3.sliderBox;


			var positionPercent = this.props.orientation === 'vertical' ? (y - sliderBox.top) / sliderBox.height * SliderConstants.PERCENT_FULL : (x - sliderBox.left) / sliderBox.width * SliderConstants.PERCENT_FULL;

			var handleId = this.getClosestHandle(positionPercent);

			if (this.props.snapOnDragMove) {
				var validPositionPercent = this.getSnapPosition(positionPercent);

				this.slideTo(handleId, validPositionPercent, function () {
					return _this3.fireChangeEvent();
				});
			} else {
				this.slideTo(handleId, positionPercent);
			}

			if (this.canMove(idx, positionPercent)) {
				// update mouse positions
				this.setState({ x: x, y: y });
				if (this.props.onSliderDragMove) this.props.onSliderDragMove(this.getPublicState());
			}
		}

		// istanbul ignore next

	}, {
		key: 'endSlide',
		value: function endSlide(ev) {
			var _this4 = this;

			var idx = this.state.slidingIndex;

			this.setState({ slidingIndex: null });

			if (typeof document.removeEventListener === 'function') {
				document.removeEventListener('mouseup', this.endSlide, false);
				document.removeEventListener('touchend', this.endSlide, false);
				document.removeEventListener('touchmove', this.handleTouchSlide, false);
				document.removeEventListener('mousemove', this.handleMouseSlide, false);
			} else {
				document.detachEvent('onmousemove', this.handleMouseSlide);
				document.detachEvent('onmouseup', this.endSlide);
			}

			if (this.props.onSliderDragEnd) this.props.onSliderDragEnd();
			if (this.props.snap) {
				var positionPercent = this.getSnapPosition(this.state.handlePos[idx]);
				this.slideTo(idx, positionPercent, function () {
					return _this4.fireChangeEvent();
				});
			} else {
				this.fireChangeEvent();
			}

			killEvent(ev);
		}

		// istanbul ignore next

	}, {
		key: 'handleClick',
		value: function handleClick(ev) {
			var _this5 = this;

			if (ev.target.getAttribute('data-handle-key')) {}
			//return


			// Calculate the position of the slider on the page so we can determine
			// the position where you click in relativity.
			var sliderBox = this.getSliderBoundingBox();

			var positionDecimal = this.props.orientation === 'vertical' ? (ev.clientY - sliderBox.top) / sliderBox.height : (ev.clientX - sliderBox.left) / sliderBox.width;

			var positionPercent = positionDecimal * SliderConstants.PERCENT_FULL;

			var handleId = this.getClosestHandle(positionPercent);

			if (positionPercent === this.state.handlePos[handleId]) {
				return;
			}

			if (this.props.disableSnapOnClick) {
				// Move the handle there
				this.slideTo(handleId, positionPercent, function () {
					return _this5.fireChangeEvent();
				});
			} else {
				var validPositionPercent = this.getSnapPosition(positionPercent);

				// Move the handle there
				this.slideTo(handleId, validPositionPercent, function () {
					return _this5.fireChangeEvent();
				});
			}

			if (this.props.onClick) this.props.onClick();
		}

		// istanbul ignore next

	}, {
		key: 'handleKeydown',
		value: function handleKeydown(ev) {
			var _this6 = this;

			var idx = getHandleFor(ev);

			if (ev.keyCode === SliderConstants.KEYS.ESC) {
				ev.currentTarget.blur();
				return;
			}

			var proposedPercentage = this.getNextPositionForKey(idx, ev.keyCode);

			if (proposedPercentage === null) return;

			if (this.canMove(idx, proposedPercentage)) {
				this.slideTo(idx, proposedPercentage, function () {
					return _this6.fireChangeEvent();
				});
				if (this.props.onKeyPress) this.props.onKeyPress();
			}

			killEvent(ev);
		}

		// Make sure the proposed position respects the bounds and
		// does not collide with other handles too much.

	}, {
		key: 'validatePosition',
		value: function validatePosition(idx, proposedPosition) {
			var _state4 = this.state,
			    handlePos = _state4.handlePos,
			    handleDimensions = _state4.handleDimensions;


			return Math.max(Math.min(proposedPosition, handlePos[idx + 1] !== undefined ? handlePos[idx + 1] - handleDimensions : SliderConstants.PERCENT_FULL // 100% is the highest value
			), handlePos[idx - 1] !== undefined ? handlePos[idx - 1] + handleDimensions : SliderConstants.PERCENT_EMPTY // 0% is the lowest value
			);
		}
	}, {
		key: 'validateValues',
		value: function validateValues(proposedValues, props) {
			var _ref = props || this.props,
			    max = _ref.max,
			    min = _ref.min;

			return proposedValues.map(function (value, idx, values) {
				var realValue = Math.max(Math.min(value, max), min);

				if (values.length && realValue < values[idx - 1]) {
					return values[idx - 1];
				}

				return realValue;
			});
		}

		// Can we move the slider to the given position?

	}, {
		key: 'canMove',
		value: function canMove(idx, proposedPosition) {
			var _state5 = this.state,
			    handlePos = _state5.handlePos,
			    handleDimensions = _state5.handleDimensions;


			if (proposedPosition < SliderConstants.PERCENT_EMPTY) return false;
			if (proposedPosition > SliderConstants.PERCENT_FULL) return false;

			var nextHandlePosition = handlePos[idx + 1] !== undefined ? handlePos[idx + 1] - handleDimensions : Infinity;

			if (proposedPosition > nextHandlePosition) return false;

			var prevHandlePosition = handlePos[idx - 1] !== undefined ? handlePos[idx - 1] + handleDimensions : -Infinity;

			if (proposedPosition < prevHandlePosition) return false;

			return true;
		}

		// istanbul ignore next

	}, {
		key: 'fireChangeEvent',
		value: function fireChangeEvent() {
			var onChange = this.props.onChange;

			if (onChange) onChange(this.getPublicState());
		}

		// istanbul ignore next

	}, {
		key: 'slideTo',
		value: function slideTo(idx, proposedPosition, onAfterSet) {
			var _this7 = this;

			var nextState = this.getNextState(idx, proposedPosition);

			this.setState(nextState, function () {
				var onValuesUpdated = _this7.props.onValuesUpdated;

				if (onValuesUpdated) onValuesUpdated(_this7.getPublicState());
				if (onAfterSet) onAfterSet();
			});
		}

		// istanbul ignore next

	}, {
		key: 'updateNewValues',
		value: function updateNewValues(nextProps) {
			var _this8 = this;

			var slidingIndex = this.state.slidingIndex;

			// Don't update while the slider is sliding

			if (slidingIndex !== null) {
				return;
			}

			var max = nextProps.max,
			    min = nextProps.min,
			    values = nextProps.values;
			var algorithm = this.props.algorithm;


			var nextValues = this.validateValues(values, nextProps);

			this.setState({
				handlePos: nextValues.map(function (value) {
					return algorithm.getPosition(value, min, max);
				}),
				values: nextValues
			}, function () {
				return _this8.fireChangeEvent();
			});
		}
	}, {
		key: 'getTooltipLabel',
		value: function getTooltipLabel(value) {
			var handleTooltipLabel = this.props.handleTooltipLabel;


			if (handleTooltipLabel) {
				return handleTooltipLabel(value);
			} else {
				return value;
			}
		}
	}, {
		key: 'getPitPointsLabel',
		value: function getPitPointsLabel(value) {
			var pitPointLabel = this.props.pitPointLabel;


			if (pitPointLabel) {
				return pitPointLabel(value);
			} else {
				return value;
			}
		}
	}, {
		key: 'render',
		value: function render() {
			var _this9 = this;

			var _props6 = this.props,
			    algorithm = _props6.algorithm,
			    children = _props6.children,
			    disabled = _props6.disabled,
			    Handle = _props6.handle,
			    handleTooltip = _props6.handleTooltip,
			    max = _props6.max,
			    min = _props6.min,
			    orientation = _props6.orientation,
			    PitComponent = _props6.pitComponent,
			    pitPoints = _props6.pitPoints,
			    pitPointLabel = _props6.pitPointLabel,
			    ProgressBar = _props6.progressBar;
			var _state6 = this.state,
			    className = _state6.className,
			    handlePos = _state6.handlePos,
			    values = _state6.values;


			return _react2.default.createElement(
				'div',
				{
					className: className,
					ref: this.setRef,
					onClick: !disabled && this.handleClick,
					style: { position: 'relative' }
				},
				_react2.default.createElement('div', { className: 'rheostat-background' }),
				handlePos.map(function (pos, idx) {
					var handleStyle = orientation === 'vertical' ? { top: pos + '%', position: 'absolute' } : { left: pos + '%', position: 'absolute' };

					return _react2.default.createElement(
						Handle,
						{
							'aria-valuemax': _this9.getMaxValue(idx),
							'aria-valuemin': _this9.getMinValue(idx),
							'aria-valuenow': values[idx],
							'aria-disabled': disabled,
							'data-handle-key': idx,
							className: 'rheostat-handle',
							key: 'handle-' + idx,
							onClick: _this9.killEvent,
							onKeyDown: !disabled && _this9.handleKeydown,
							onMouseDown: !disabled && _this9.startMouseSlide,
							onTouchStart: !disabled && _this9.startTouchSlide,
							role: 'slider',
							style: handleStyle,
							tabIndex: 0,
							'data-tooltip': handleTooltip
						},
						handleTooltip ? _this9.getTooltipLabel(values[idx]) : ''
					);
				}),
				handlePos.map(function (node, idx, arr) {
					if (idx === 0 && arr.length > 1) {
						return null;
					}

					return _react2.default.createElement(ProgressBar, {
						className: 'rheostat-progress',
						key: 'progress-bar-' + idx,
						style: _this9.getProgressStyle(idx)
					});
				}),
				PitComponent && pitPoints.map(function (n) {
					var pos = algorithm.getPosition(n, min, max);
					var pitStyle = orientation === 'vertical' ? { top: pos + '%', position: 'absolute' } : { left: pos + '%', position: 'absolute' };

					return _react2.default.createElement(
						PitComponent,
						{
							key: 'pit-' + n,
							style: pitStyle
						},
						_this9.getPitPointsLabel(n)
					);
				}),
				children
			);
		}
	}]);

	return Rheostat;
}(_react2.default.Component);

Rheostat.propTypes = propTypes;
Rheostat.defaultProps = defaultProps;

exports.default = Rheostat;

/***/ }),
/* 44 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
	value: true
});

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

var _propTypes = __webpack_require__(1);

var _propTypes2 = _interopRequireDefault(_propTypes);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var propTypes = {
	// pit element style object
	style: _propTypes2.default.object,
	// label for pit element
	children: _propTypes2.default.number
};

function PitComponent(_ref) {
	var style = _ref.style,
	    children = _ref.children;

	return _react2.default.createElement(
		'span',
		{
			className: 'pit-item',
			style: _extends({}, style)
		},
		children
	);
}

exports.default = PitComponent;

/***/ }),
/* 45 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 *
 * 
 */

function makeEmptyFunction(arg) {
  return function () {
    return arg;
  };
}

/**
 * This function accepts and discards inputs; it has no side effects. This is
 * primarily useful idiomatically for overridable function endpoints which
 * always need to be callable, since JS lacks a null-call idiom ala Cocoa.
 */
var emptyFunction = function emptyFunction() {};

emptyFunction.thatReturns = makeEmptyFunction;
emptyFunction.thatReturnsFalse = makeEmptyFunction(false);
emptyFunction.thatReturnsTrue = makeEmptyFunction(true);
emptyFunction.thatReturnsNull = makeEmptyFunction(null);
emptyFunction.thatReturnsThis = function () {
  return this;
};
emptyFunction.thatReturnsArgument = function (arg) {
  return arg;
};

module.exports = emptyFunction;

/***/ }),
/* 46 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 *
 */



/**
 * Use invariant() to assert state which your program assumes to be true.
 *
 * Provide sprintf-style format (only %s is supported) and arguments
 * to provide information about what broke and what you were
 * expecting.
 *
 * The invariant message will be stripped in production, but the invariant
 * will remain to ensure logic does not differ in production.
 */

var validateFormat = function validateFormat(format) {};

if (true) {
  validateFormat = function validateFormat(format) {
    if (format === undefined) {
      throw new Error('invariant requires an error message argument');
    }
  };
}

function invariant(condition, format, a, b, c, d, e, f) {
  validateFormat(format);

  if (!condition) {
    var error;
    if (format === undefined) {
      error = new Error('Minified exception occurred; use the non-minified dev environment ' + 'for the full error message and additional helpful warnings.');
    } else {
      var args = [a, b, c, d, e, f];
      var argIndex = 0;
      error = new Error(format.replace(/%s/g, function () {
        return args[argIndex++];
      }));
      error.name = 'Invariant Violation';
    }

    error.framesToPop = 1; // we don't care about invariant's own frame
    throw error;
  }
}

module.exports = invariant;

/***/ }),
/* 47 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/**
 * Copyright (c) 2014-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 *
 */



var emptyFunction = __webpack_require__(45);

/**
 * Similar to invariant but only logs a warning if the condition is not met.
 * This can be used to log issues in development environments in critical
 * paths. Removing the logging code for production environments will keep the
 * same logic and follow the same code paths.
 */

var warning = emptyFunction;

if (true) {
  var printWarning = function printWarning(format) {
    for (var _len = arguments.length, args = Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
      args[_key - 1] = arguments[_key];
    }

    var argIndex = 0;
    var message = 'Warning: ' + format.replace(/%s/g, function () {
      return args[argIndex++];
    });
    if (typeof console !== 'undefined') {
      console.error(message);
    }
    try {
      // --- Welcome to debugging React ---
      // This error was thrown as a convenience so that you can use this stack
      // to find the callsite that caused this warning to fire.
      throw new Error(message);
    } catch (x) {}
  };

  warning = function warning(condition, format) {
    if (format === undefined) {
      throw new Error('`warning(condition, format, ...args)` requires a warning ' + 'message argument');
    }

    if (format.indexOf('Failed Composite propType: ') === 0) {
      return; // Ignore CompositeComponent proptype check.
    }

    if (!condition) {
      for (var _len2 = arguments.length, args = Array(_len2 > 2 ? _len2 - 2 : 0), _key2 = 2; _key2 < _len2; _key2++) {
        args[_key2 - 2] = arguments[_key2];
      }

      printWarning.apply(undefined, [format].concat(args));
    }
  };
}

module.exports = warning;

/***/ }),
/* 48 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */



var ReactPropTypesSecret = 'SECRET_DO_NOT_PASS_THIS_OR_YOU_WILL_BE_FIRED';

module.exports = ReactPropTypesSecret;


/***/ }),
/* 49 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "b", function() { return canUseDOM; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "a", function() { return addEventListener; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "e", function() { return removeEventListener; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "c", function() { return getConfirmation; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "g", function() { return supportsHistory; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "h", function() { return supportsPopStateOnHashChange; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "f", function() { return supportsGoWithoutReloadUsingHash; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "d", function() { return isExtraneousPopstateEvent; });
var canUseDOM = !!(typeof window !== 'undefined' && window.document && window.document.createElement);

var addEventListener = function addEventListener(node, event, listener) {
  return node.addEventListener ? node.addEventListener(event, listener, false) : node.attachEvent('on' + event, listener);
};

var removeEventListener = function removeEventListener(node, event, listener) {
  return node.removeEventListener ? node.removeEventListener(event, listener, false) : node.detachEvent('on' + event, listener);
};

var getConfirmation = function getConfirmation(message, callback) {
  return callback(window.confirm(message));
}; // eslint-disable-line no-alert

/**
 * Returns true if the HTML5 history API is supported. Taken from Modernizr.
 *
 * https://github.com/Modernizr/Modernizr/blob/master/LICENSE
 * https://github.com/Modernizr/Modernizr/blob/master/feature-detects/history.js
 * changed to avoid false negatives for Windows Phones: https://github.com/reactjs/react-router/issues/586
 */
var supportsHistory = function supportsHistory() {
  var ua = window.navigator.userAgent;

  if ((ua.indexOf('Android 2.') !== -1 || ua.indexOf('Android 4.0') !== -1) && ua.indexOf('Mobile Safari') !== -1 && ua.indexOf('Chrome') === -1 && ua.indexOf('Windows Phone') === -1) return false;

  return window.history && 'pushState' in window.history;
};

/**
 * Returns true if browser fires popstate on hash change.
 * IE10 and IE11 do not.
 */
var supportsPopStateOnHashChange = function supportsPopStateOnHashChange() {
  return window.navigator.userAgent.indexOf('Trident') === -1;
};

/**
 * Returns false if using go(n) with hash history causes a full page reload.
 */
var supportsGoWithoutReloadUsingHash = function supportsGoWithoutReloadUsingHash() {
  return window.navigator.userAgent.indexOf('Firefox') === -1;
};

/**
 * Returns true if a given popstate event is an extraneous WebKit event.
 * Accounts for the fact that Chrome on iOS fires real popstate events
 * containing undefined state when pressing the back button.
 */
var isExtraneousPopstateEvent = function isExtraneousPopstateEvent(event) {
  return event.state === undefined && navigator.userAgent.indexOf('CriOS') === -1;
};

/***/ }),
/* 50 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_react__ = __webpack_require__(0);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_prop_types__ = __webpack_require__(1);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_prop_types___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_1_prop_types__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2_invariant__ = __webpack_require__(7);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2_invariant___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_2_invariant__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_3_history__ = __webpack_require__(16);
var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

function _objectWithoutProperties(obj, keys) { var target = {}; for (var i in obj) { if (keys.indexOf(i) >= 0) continue; if (!Object.prototype.hasOwnProperty.call(obj, i)) continue; target[i] = obj[i]; } return target; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }






var isModifiedEvent = function isModifiedEvent(event) {
  return !!(event.metaKey || event.altKey || event.ctrlKey || event.shiftKey);
};

/**
 * The public API for rendering a history-aware <a>.
 */

var Link = function (_React$Component) {
  _inherits(Link, _React$Component);

  function Link() {
    var _temp, _this, _ret;

    _classCallCheck(this, Link);

    for (var _len = arguments.length, args = Array(_len), _key = 0; _key < _len; _key++) {
      args[_key] = arguments[_key];
    }

    return _ret = (_temp = (_this = _possibleConstructorReturn(this, _React$Component.call.apply(_React$Component, [this].concat(args))), _this), _this.handleClick = function (event) {
      if (_this.props.onClick) _this.props.onClick(event);

      if (!event.defaultPrevented && // onClick prevented default
      event.button === 0 && // ignore everything but left clicks
      !_this.props.target && // let browser handle "target=_blank" etc.
      !isModifiedEvent(event) // ignore clicks with modifier keys
      ) {
          event.preventDefault();

          var history = _this.context.router.history;
          var _this$props = _this.props,
              replace = _this$props.replace,
              to = _this$props.to;


          if (replace) {
            history.replace(to);
          } else {
            history.push(to);
          }
        }
    }, _temp), _possibleConstructorReturn(_this, _ret);
  }

  Link.prototype.render = function render() {
    var _props = this.props,
        replace = _props.replace,
        to = _props.to,
        innerRef = _props.innerRef,
        props = _objectWithoutProperties(_props, ["replace", "to", "innerRef"]); // eslint-disable-line no-unused-vars

    __WEBPACK_IMPORTED_MODULE_2_invariant___default()(this.context.router, "You should not use <Link> outside a <Router>");

    __WEBPACK_IMPORTED_MODULE_2_invariant___default()(to !== undefined, 'You must specify the "to" property');

    var history = this.context.router.history;

    var location = typeof to === "string" ? Object(__WEBPACK_IMPORTED_MODULE_3_history__["c" /* createLocation */])(to, null, null, history.location) : to;

    var href = history.createHref(location);
    return __WEBPACK_IMPORTED_MODULE_0_react__["default"].createElement("a", _extends({}, props, { onClick: this.handleClick, href: href, ref: innerRef }));
  };

  return Link;
}(__WEBPACK_IMPORTED_MODULE_0_react__["default"].Component);

Link.propTypes = {
  onClick: __WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.func,
  target: __WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.string,
  replace: __WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.bool,
  to: __WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.oneOfType([__WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.string, __WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.object]).isRequired,
  innerRef: __WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.oneOfType([__WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.string, __WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.func])
};
Link.defaultProps = {
  replace: false
};
Link.contextTypes = {
  router: __WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.shape({
    history: __WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.shape({
      push: __WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.func.isRequired,
      replace: __WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.func.isRequired,
      createHref: __WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.func.isRequired
    }).isRequired
  }).isRequired
};


/* harmony default export */ __webpack_exports__["a"] = (Link);

/***/ }),
/* 51 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_react_router_es_Route__ = __webpack_require__(52);
// Written in this round about way for babel-transform-imports


/* harmony default export */ __webpack_exports__["a"] = (__WEBPACK_IMPORTED_MODULE_0_react_router_es_Route__["a" /* default */]);

/***/ }),
/* 52 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_warning__ = __webpack_require__(13);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_warning___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_0_warning__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_invariant__ = __webpack_require__(7);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_invariant___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_1_invariant__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2_react__ = __webpack_require__(0);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_3_prop_types__ = __webpack_require__(1);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_3_prop_types___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_3_prop_types__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_4__matchPath__ = __webpack_require__(35);
var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }







var isEmptyChildren = function isEmptyChildren(children) {
  return __WEBPACK_IMPORTED_MODULE_2_react__["default"].Children.count(children) === 0;
};

/**
 * The public API for matching a single path and rendering.
 */

var Route = function (_React$Component) {
  _inherits(Route, _React$Component);

  function Route() {
    var _temp, _this, _ret;

    _classCallCheck(this, Route);

    for (var _len = arguments.length, args = Array(_len), _key = 0; _key < _len; _key++) {
      args[_key] = arguments[_key];
    }

    return _ret = (_temp = (_this = _possibleConstructorReturn(this, _React$Component.call.apply(_React$Component, [this].concat(args))), _this), _this.state = {
      match: _this.computeMatch(_this.props, _this.context.router)
    }, _temp), _possibleConstructorReturn(_this, _ret);
  }

  Route.prototype.getChildContext = function getChildContext() {
    return {
      router: _extends({}, this.context.router, {
        route: {
          location: this.props.location || this.context.router.route.location,
          match: this.state.match
        }
      })
    };
  };

  Route.prototype.computeMatch = function computeMatch(_ref, router) {
    var computedMatch = _ref.computedMatch,
        location = _ref.location,
        path = _ref.path,
        strict = _ref.strict,
        exact = _ref.exact,
        sensitive = _ref.sensitive;

    if (computedMatch) return computedMatch; // <Switch> already computed the match for us

    __WEBPACK_IMPORTED_MODULE_1_invariant___default()(router, "You should not use <Route> or withRouter() outside a <Router>");

    var route = router.route;

    var pathname = (location || route.location).pathname;

    return Object(__WEBPACK_IMPORTED_MODULE_4__matchPath__["a" /* default */])(pathname, { path: path, strict: strict, exact: exact, sensitive: sensitive }, route.match);
  };

  Route.prototype.componentWillMount = function componentWillMount() {
    __WEBPACK_IMPORTED_MODULE_0_warning___default()(!(this.props.component && this.props.render), "You should not use <Route component> and <Route render> in the same route; <Route render> will be ignored");

    __WEBPACK_IMPORTED_MODULE_0_warning___default()(!(this.props.component && this.props.children && !isEmptyChildren(this.props.children)), "You should not use <Route component> and <Route children> in the same route; <Route children> will be ignored");

    __WEBPACK_IMPORTED_MODULE_0_warning___default()(!(this.props.render && this.props.children && !isEmptyChildren(this.props.children)), "You should not use <Route render> and <Route children> in the same route; <Route children> will be ignored");
  };

  Route.prototype.componentWillReceiveProps = function componentWillReceiveProps(nextProps, nextContext) {
    __WEBPACK_IMPORTED_MODULE_0_warning___default()(!(nextProps.location && !this.props.location), '<Route> elements should not change from uncontrolled to controlled (or vice versa). You initially used no "location" prop and then provided one on a subsequent render.');

    __WEBPACK_IMPORTED_MODULE_0_warning___default()(!(!nextProps.location && this.props.location), '<Route> elements should not change from controlled to uncontrolled (or vice versa). You provided a "location" prop initially but omitted it on a subsequent render.');

    this.setState({
      match: this.computeMatch(nextProps, nextContext.router)
    });
  };

  Route.prototype.render = function render() {
    var match = this.state.match;
    var _props = this.props,
        children = _props.children,
        component = _props.component,
        render = _props.render;
    var _context$router = this.context.router,
        history = _context$router.history,
        route = _context$router.route,
        staticContext = _context$router.staticContext;

    var location = this.props.location || route.location;
    var props = { match: match, location: location, history: history, staticContext: staticContext };

    if (component) return match ? __WEBPACK_IMPORTED_MODULE_2_react__["default"].createElement(component, props) : null;

    if (render) return match ? render(props) : null;

    if (typeof children === "function") return children(props);

    if (children && !isEmptyChildren(children)) return __WEBPACK_IMPORTED_MODULE_2_react__["default"].Children.only(children);

    return null;
  };

  return Route;
}(__WEBPACK_IMPORTED_MODULE_2_react__["default"].Component);

Route.propTypes = {
  computedMatch: __WEBPACK_IMPORTED_MODULE_3_prop_types___default.a.object, // private, from <Switch>
  path: __WEBPACK_IMPORTED_MODULE_3_prop_types___default.a.string,
  exact: __WEBPACK_IMPORTED_MODULE_3_prop_types___default.a.bool,
  strict: __WEBPACK_IMPORTED_MODULE_3_prop_types___default.a.bool,
  sensitive: __WEBPACK_IMPORTED_MODULE_3_prop_types___default.a.bool,
  component: __WEBPACK_IMPORTED_MODULE_3_prop_types___default.a.func,
  render: __WEBPACK_IMPORTED_MODULE_3_prop_types___default.a.func,
  children: __WEBPACK_IMPORTED_MODULE_3_prop_types___default.a.oneOfType([__WEBPACK_IMPORTED_MODULE_3_prop_types___default.a.func, __WEBPACK_IMPORTED_MODULE_3_prop_types___default.a.node]),
  location: __WEBPACK_IMPORTED_MODULE_3_prop_types___default.a.object
};
Route.contextTypes = {
  router: __WEBPACK_IMPORTED_MODULE_3_prop_types___default.a.shape({
    history: __WEBPACK_IMPORTED_MODULE_3_prop_types___default.a.object.isRequired,
    route: __WEBPACK_IMPORTED_MODULE_3_prop_types___default.a.object.isRequired,
    staticContext: __WEBPACK_IMPORTED_MODULE_3_prop_types___default.a.object
  })
};
Route.childContextTypes = {
  router: __WEBPACK_IMPORTED_MODULE_3_prop_types___default.a.object.isRequired
};


/* harmony default export */ __webpack_exports__["a"] = (Route);

/***/ }),
/* 53 */
/***/ (function(module, exports, __webpack_require__) {

var isarray = __webpack_require__(105)

/**
 * Expose `pathToRegexp`.
 */
module.exports = pathToRegexp
module.exports.parse = parse
module.exports.compile = compile
module.exports.tokensToFunction = tokensToFunction
module.exports.tokensToRegExp = tokensToRegExp

/**
 * The main path matching regexp utility.
 *
 * @type {RegExp}
 */
var PATH_REGEXP = new RegExp([
  // Match escaped characters that would otherwise appear in future matches.
  // This allows the user to escape special characters that won't transform.
  '(\\\\.)',
  // Match Express-style parameters and un-named parameters with a prefix
  // and optional suffixes. Matches appear as:
  //
  // "/:test(\\d+)?" => ["/", "test", "\d+", undefined, "?", undefined]
  // "/route(\\d+)"  => [undefined, undefined, undefined, "\d+", undefined, undefined]
  // "/*"            => ["/", undefined, undefined, undefined, undefined, "*"]
  '([\\/.])?(?:(?:\\:(\\w+)(?:\\(((?:\\\\.|[^\\\\()])+)\\))?|\\(((?:\\\\.|[^\\\\()])+)\\))([+*?])?|(\\*))'
].join('|'), 'g')

/**
 * Parse a string for the raw tokens.
 *
 * @param  {string}  str
 * @param  {Object=} options
 * @return {!Array}
 */
function parse (str, options) {
  var tokens = []
  var key = 0
  var index = 0
  var path = ''
  var defaultDelimiter = options && options.delimiter || '/'
  var res

  while ((res = PATH_REGEXP.exec(str)) != null) {
    var m = res[0]
    var escaped = res[1]
    var offset = res.index
    path += str.slice(index, offset)
    index = offset + m.length

    // Ignore already escaped sequences.
    if (escaped) {
      path += escaped[1]
      continue
    }

    var next = str[index]
    var prefix = res[2]
    var name = res[3]
    var capture = res[4]
    var group = res[5]
    var modifier = res[6]
    var asterisk = res[7]

    // Push the current path onto the tokens.
    if (path) {
      tokens.push(path)
      path = ''
    }

    var partial = prefix != null && next != null && next !== prefix
    var repeat = modifier === '+' || modifier === '*'
    var optional = modifier === '?' || modifier === '*'
    var delimiter = res[2] || defaultDelimiter
    var pattern = capture || group

    tokens.push({
      name: name || key++,
      prefix: prefix || '',
      delimiter: delimiter,
      optional: optional,
      repeat: repeat,
      partial: partial,
      asterisk: !!asterisk,
      pattern: pattern ? escapeGroup(pattern) : (asterisk ? '.*' : '[^' + escapeString(delimiter) + ']+?')
    })
  }

  // Match any characters still remaining.
  if (index < str.length) {
    path += str.substr(index)
  }

  // If the path exists, push it onto the end.
  if (path) {
    tokens.push(path)
  }

  return tokens
}

/**
 * Compile a string to a template function for the path.
 *
 * @param  {string}             str
 * @param  {Object=}            options
 * @return {!function(Object=, Object=)}
 */
function compile (str, options) {
  return tokensToFunction(parse(str, options))
}

/**
 * Prettier encoding of URI path segments.
 *
 * @param  {string}
 * @return {string}
 */
function encodeURIComponentPretty (str) {
  return encodeURI(str).replace(/[\/?#]/g, function (c) {
    return '%' + c.charCodeAt(0).toString(16).toUpperCase()
  })
}

/**
 * Encode the asterisk parameter. Similar to `pretty`, but allows slashes.
 *
 * @param  {string}
 * @return {string}
 */
function encodeAsterisk (str) {
  return encodeURI(str).replace(/[?#]/g, function (c) {
    return '%' + c.charCodeAt(0).toString(16).toUpperCase()
  })
}

/**
 * Expose a method for transforming tokens into the path function.
 */
function tokensToFunction (tokens) {
  // Compile all the tokens into regexps.
  var matches = new Array(tokens.length)

  // Compile all the patterns before compilation.
  for (var i = 0; i < tokens.length; i++) {
    if (typeof tokens[i] === 'object') {
      matches[i] = new RegExp('^(?:' + tokens[i].pattern + ')$')
    }
  }

  return function (obj, opts) {
    var path = ''
    var data = obj || {}
    var options = opts || {}
    var encode = options.pretty ? encodeURIComponentPretty : encodeURIComponent

    for (var i = 0; i < tokens.length; i++) {
      var token = tokens[i]

      if (typeof token === 'string') {
        path += token

        continue
      }

      var value = data[token.name]
      var segment

      if (value == null) {
        if (token.optional) {
          // Prepend partial segment prefixes.
          if (token.partial) {
            path += token.prefix
          }

          continue
        } else {
          throw new TypeError('Expected "' + token.name + '" to be defined')
        }
      }

      if (isarray(value)) {
        if (!token.repeat) {
          throw new TypeError('Expected "' + token.name + '" to not repeat, but received `' + JSON.stringify(value) + '`')
        }

        if (value.length === 0) {
          if (token.optional) {
            continue
          } else {
            throw new TypeError('Expected "' + token.name + '" to not be empty')
          }
        }

        for (var j = 0; j < value.length; j++) {
          segment = encode(value[j])

          if (!matches[i].test(segment)) {
            throw new TypeError('Expected all "' + token.name + '" to match "' + token.pattern + '", but received `' + JSON.stringify(segment) + '`')
          }

          path += (j === 0 ? token.prefix : token.delimiter) + segment
        }

        continue
      }

      segment = token.asterisk ? encodeAsterisk(value) : encode(value)

      if (!matches[i].test(segment)) {
        throw new TypeError('Expected "' + token.name + '" to match "' + token.pattern + '", but received "' + segment + '"')
      }

      path += token.prefix + segment
    }

    return path
  }
}

/**
 * Escape a regular expression string.
 *
 * @param  {string} str
 * @return {string}
 */
function escapeString (str) {
  return str.replace(/([.+*?=^!:${}()[\]|\/\\])/g, '\\$1')
}

/**
 * Escape the capturing group by escaping special characters and meaning.
 *
 * @param  {string} group
 * @return {string}
 */
function escapeGroup (group) {
  return group.replace(/([=!:$\/()])/g, '\\$1')
}

/**
 * Attach the keys as a property of the regexp.
 *
 * @param  {!RegExp} re
 * @param  {Array}   keys
 * @return {!RegExp}
 */
function attachKeys (re, keys) {
  re.keys = keys
  return re
}

/**
 * Get the flags for a regexp from the options.
 *
 * @param  {Object} options
 * @return {string}
 */
function flags (options) {
  return options.sensitive ? '' : 'i'
}

/**
 * Pull out keys from a regexp.
 *
 * @param  {!RegExp} path
 * @param  {!Array}  keys
 * @return {!RegExp}
 */
function regexpToRegexp (path, keys) {
  // Use a negative lookahead to match only capturing groups.
  var groups = path.source.match(/\((?!\?)/g)

  if (groups) {
    for (var i = 0; i < groups.length; i++) {
      keys.push({
        name: i,
        prefix: null,
        delimiter: null,
        optional: false,
        repeat: false,
        partial: false,
        asterisk: false,
        pattern: null
      })
    }
  }

  return attachKeys(path, keys)
}

/**
 * Transform an array into a regexp.
 *
 * @param  {!Array}  path
 * @param  {Array}   keys
 * @param  {!Object} options
 * @return {!RegExp}
 */
function arrayToRegexp (path, keys, options) {
  var parts = []

  for (var i = 0; i < path.length; i++) {
    parts.push(pathToRegexp(path[i], keys, options).source)
  }

  var regexp = new RegExp('(?:' + parts.join('|') + ')', flags(options))

  return attachKeys(regexp, keys)
}

/**
 * Create a path regexp from string input.
 *
 * @param  {string}  path
 * @param  {!Array}  keys
 * @param  {!Object} options
 * @return {!RegExp}
 */
function stringToRegexp (path, keys, options) {
  return tokensToRegExp(parse(path, options), keys, options)
}

/**
 * Expose a function for taking tokens and returning a RegExp.
 *
 * @param  {!Array}          tokens
 * @param  {(Array|Object)=} keys
 * @param  {Object=}         options
 * @return {!RegExp}
 */
function tokensToRegExp (tokens, keys, options) {
  if (!isarray(keys)) {
    options = /** @type {!Object} */ (keys || options)
    keys = []
  }

  options = options || {}

  var strict = options.strict
  var end = options.end !== false
  var route = ''

  // Iterate over the tokens and create our regexp string.
  for (var i = 0; i < tokens.length; i++) {
    var token = tokens[i]

    if (typeof token === 'string') {
      route += escapeString(token)
    } else {
      var prefix = escapeString(token.prefix)
      var capture = '(?:' + token.pattern + ')'

      keys.push(token)

      if (token.repeat) {
        capture += '(?:' + prefix + capture + ')*'
      }

      if (token.optional) {
        if (!token.partial) {
          capture = '(?:' + prefix + '(' + capture + '))?'
        } else {
          capture = prefix + '(' + capture + ')?'
        }
      } else {
        capture = prefix + '(' + capture + ')'
      }

      route += capture
    }
  }

  var delimiter = escapeString(options.delimiter || '/')
  var endsWithDelimiter = route.slice(-delimiter.length) === delimiter

  // In non-strict mode we allow a slash at the end of match. If the path to
  // match already ends with a slash, we remove it for consistency. The slash
  // is valid at the end of a path match, not in the middle. This is important
  // in non-ending mode, where "/test/" shouldn't match "/test//route".
  if (!strict) {
    route = (endsWithDelimiter ? route.slice(0, -delimiter.length) : route) + '(?:' + delimiter + '(?=$))?'
  }

  if (end) {
    route += '$'
  } else {
    // In non-ending mode, we need the capturing groups to match as much as
    // possible by using a positive lookahead to the end or next path segment.
    route += strict && endsWithDelimiter ? '' : '(?=' + delimiter + '|$)'
  }

  return attachKeys(new RegExp('^' + route, flags(options)), keys)
}

/**
 * Normalize the given path string, returning a regular expression.
 *
 * An empty array can be passed in for the keys, which will hold the
 * placeholder key descriptions. For example, using `/user/:id`, `keys` will
 * contain `[{ name: 'id', delimiter: '/', optional: false, repeat: false }]`.
 *
 * @param  {(string|RegExp|Array)} path
 * @param  {(Array|Object)=}       keys
 * @param  {Object=}               options
 * @return {!RegExp}
 */
function pathToRegexp (path, keys, options) {
  if (!isarray(keys)) {
    options = /** @type {!Object} */ (keys || options)
    keys = []
  }

  options = options || {}

  if (path instanceof RegExp) {
    return regexpToRegexp(path, /** @type {!Array} */ (keys))
  }

  if (isarray(path)) {
    return arrayToRegexp(/** @type {!Array} */ (path), /** @type {!Array} */ (keys), options)
  }

  return stringToRegexp(/** @type {string} */ (path), /** @type {!Array} */ (keys), options)
}


/***/ }),
/* 54 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_path_to_regexp__ = __webpack_require__(53);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_path_to_regexp___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_0_path_to_regexp__);


var patternCache = {};
var cacheLimit = 10000;
var cacheCount = 0;

var compileGenerator = function compileGenerator(pattern) {
  var cacheKey = pattern;
  var cache = patternCache[cacheKey] || (patternCache[cacheKey] = {});

  if (cache[pattern]) return cache[pattern];

  var compiledGenerator = __WEBPACK_IMPORTED_MODULE_0_path_to_regexp___default.a.compile(pattern);

  if (cacheCount < cacheLimit) {
    cache[pattern] = compiledGenerator;
    cacheCount++;
  }

  return compiledGenerator;
};

/**
 * Public API for generating a URL pathname from a pattern and parameters.
 */
var generatePath = function generatePath() {
  var pattern = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : "/";
  var params = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {};

  if (pattern === "/") {
    return pattern;
  }
  var generator = compileGenerator(pattern);
  return generator(params, { pretty: true });
};

/* harmony default export */ __webpack_exports__["a"] = (generatePath);

/***/ }),
/* 55 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


/**
 * Copyright 2015, Yahoo! Inc.
 * Copyrights licensed under the New BSD License. See the accompanying LICENSE file for terms.
 */
var REACT_STATICS = {
    childContextTypes: true,
    contextTypes: true,
    defaultProps: true,
    displayName: true,
    getDefaultProps: true,
    getDerivedStateFromProps: true,
    mixins: true,
    propTypes: true,
    type: true
};

var KNOWN_STATICS = {
    name: true,
    length: true,
    prototype: true,
    caller: true,
    callee: true,
    arguments: true,
    arity: true
};

var defineProperty = Object.defineProperty;
var getOwnPropertyNames = Object.getOwnPropertyNames;
var getOwnPropertySymbols = Object.getOwnPropertySymbols;
var getOwnPropertyDescriptor = Object.getOwnPropertyDescriptor;
var getPrototypeOf = Object.getPrototypeOf;
var objectPrototype = getPrototypeOf && getPrototypeOf(Object);

function hoistNonReactStatics(targetComponent, sourceComponent, blacklist) {
    if (typeof sourceComponent !== 'string') { // don't hoist over string (html) components

        if (objectPrototype) {
            var inheritedComponent = getPrototypeOf(sourceComponent);
            if (inheritedComponent && inheritedComponent !== objectPrototype) {
                hoistNonReactStatics(targetComponent, inheritedComponent, blacklist);
            }
        }

        var keys = getOwnPropertyNames(sourceComponent);

        if (getOwnPropertySymbols) {
            keys = keys.concat(getOwnPropertySymbols(sourceComponent));
        }

        for (var i = 0; i < keys.length; ++i) {
            var key = keys[i];
            if (!REACT_STATICS[key] && !KNOWN_STATICS[key] && (!blacklist || !blacklist[key])) {
                var descriptor = getOwnPropertyDescriptor(sourceComponent, key);
                try { // Avoid failures from read-only properties
                    defineProperty(targetComponent, key, descriptor);
                } catch (e) {}
            }
        }

        return targetComponent;
    }

    return targetComponent;
}

module.exports = hoistNonReactStatics;


/***/ }),
/* 56 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
    value: true
});

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

var _reactLazyLoad = __webpack_require__(6);

var _reactLazyLoad2 = _interopRequireDefault(_reactLazyLoad);

var _popUpUtils = __webpack_require__(11);

var _analyticsUtils = __webpack_require__(5);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var NewBikes = function (_React$Component) {
    _inherits(NewBikes, _React$Component);

    function NewBikes(props) {
        _classCallCheck(this, NewBikes);

        var _this = _possibleConstructorReturn(this, (NewBikes.__proto__ || Object.getPrototypeOf(NewBikes)).call(this, props));

        _this.checkOnRoadLinkClick = _this.checkOnRoadLinkClick.bind(_this);
        return _this;
    }

    _createClass(NewBikes, [{
        key: 'checkOnRoadLinkClick',
        value: function checkOnRoadLinkClick(item, event) {
            var category = gaObj.name;
            (0, _analyticsUtils.triggerGA)(category, "Get_On_Road_Price_Click", item.ModelName);
            event.preventDefault();
            (0, _popUpUtils.setDataforPopularBikesWidget)(event, item);
            (0, _popUpUtils.setPriceQuoteFlag)();
        }
    }, {
        key: 'renderListItem',
        value: function renderListItem(item, index) {
            var priceHtml = null;
            var button = null;
            if (item.Price == "") {
                priceHtml = _react2.default.createElement(
                    'span',
                    { className: 'value-size-16' },
                    item.PriceSuffix
                );
            } else {
                priceHtml = _react2.default.createElement(
                    'div',
                    { className: 'value-size-16' },
                    _react2.default.createElement(
                        'span',
                        null,
                        '\u20B9'
                    ),
                    '\xA0',
                    _react2.default.createElement(
                        'span',
                        null,
                        item.Price,
                        ' ',
                        item.PriceSuffix
                    )
                );
            }
            if (item.IsNew) {
                button = _react2.default.createElement(
                    'div',
                    { 'class': 'card-btn-block' },
                    _react2.default.createElement(
                        'a',
                        { 'data-makename': item.MakeName, 'data-modelname': item.ModelName, 'data-modelid': item.ModelId, className: 'btn btn-card btn-full-width btn-white getquotation', onClick: this.checkOnRoadLinkClick.bind(this, item) },
                        'Check on-road price'
                    )
                );
            }

            return _react2.default.createElement(
                'li',
                { key: item.Name, className: 'carousel-slide' },
                _react2.default.createElement(
                    'div',
                    { className: 'carousel-card' },
                    _react2.default.createElement(
                        'a',
                        { href: item.DetailPageUrl, title: item.Name, className: 'card-target-block' },
                        _react2.default.createElement(
                            'div',
                            { className: 'card-image-block' },
                            _react2.default.createElement(
                                _reactLazyLoad2.default,
                                { offsetVertical: 0 },
                                _react2.default.createElement('img', { src: item.ImgUrl, alt: item.Name })
                            )
                        ),
                        _react2.default.createElement(
                            'div',
                            { className: 'card-details-block' },
                            _react2.default.createElement(
                                'h3',
                                { className: 'h3-title-target target-link text-truncate' },
                                item.Name
                            ),
                            _react2.default.createElement(
                                'p',
                                { className: 'text-truncate key-size-11' },
                                item.PriceDescription
                            ),
                            priceHtml
                        ),
                        button
                    )
                )
            );
        }
    }, {
        key: 'render',
        value: function render() {

            if (!this.props.newBikesData || !this.props.newBikesData.BikesList || this.props.newBikesData.BikesList.length === 0) {
                return false;
            }
            var newBikesData = this.props.newBikesData;
            return _react2.default.createElement(
                'div',
                { className: 'container bg-white box-shadow section-bottom-margin carousel-bottom-padding carousel-top-padding' },
                _react2.default.createElement(
                    'div',
                    { className: 'carousel-heading-content' },
                    _react2.default.createElement(
                        'div',
                        { className: 'swiper-heading-left-grid inline-block' },
                        _react2.default.createElement(
                            'h2',
                            null,
                            newBikesData.Heading
                        )
                    ),
                    _react2.default.createElement(
                        'div',
                        { className: 'swiper-heading-right-grid inline-block text-right' },
                        _react2.default.createElement(
                            'a',
                            { href: newBikesData.CompleteListUrl, title: newBikesData.CompleteListUrlAlternateLabel, className: 'btn view-all-target-btn' },
                            'View all'
                        )
                    )
                ),
                _react2.default.createElement(
                    'ul',
                    { className: 'carousel-wrapper' },
                    newBikesData.BikesList.map(function (item, index) {
                        return this.renderListItem(item, index);
                    }.bind(this))
                )
            );
        }
    }]);

    return NewBikes;
}(_react2.default.Component);

exports.default = NewBikes;

/***/ }),
/* 57 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var SocialMediaSlug = function (_React$Component) {
    _inherits(SocialMediaSlug, _React$Component);

    function SocialMediaSlug() {
        _classCallCheck(this, SocialMediaSlug);

        return _possibleConstructorReturn(this, (SocialMediaSlug.__proto__ || Object.getPrototypeOf(SocialMediaSlug)).apply(this, arguments));
    }

    _createClass(SocialMediaSlug, [{
        key: 'shareLink',
        value: function shareLink(platform) {
            var cururl = window.location.href;
            var url;
            switch (platform) {
                case "facebook":
                    url = 'https://www.facebook.com/sharer/sharer.php?u=';
                    break;
                case "twitter":
                    url = 'https://twitter.com/home?status=';
                    break;
                case "googleplus":
                    url = 'https://plus.google.com/share?url=';
                    break;
                case "whatsapp":
                    var text = document.getElementsByTagName("title")[0].innerHTML;
                    var message = encodeURIComponent(text) + " - " + encodeURIComponent(cururl);
                    var whatsapp_url = "whatsapp://send?text=" + message;
                    url = whatsapp_url;
                    window.open(url, '_blank');
                    return;
            }
            url += cururl;
            window.open(url, '_blank');
        }
    }, {
        key: 'render',
        value: function render() {

            return _react2.default.createElement(
                'div',
                { className: 'social-media-divider' },
                _react2.default.createElement(
                    'p',
                    { className: 'margin-bottom10 font14 text-light-grey' },
                    'Share this story'
                ),
                _react2.default.createElement(
                    'ul',
                    { className: 'social-wrapper' },
                    _react2.default.createElement(
                        'li',
                        { className: 'whatsapp-container rounded-corner2 share-btn', onClick: this.shareLink.bind(this, "whatsapp") },
                        _react2.default.createElement('span', { className: 'social-icons-sprite whatsapp-icon' })
                    ),
                    _react2.default.createElement(
                        'li',
                        { className: 'fb-container rounded-corner2', onClick: this.shareLink.bind(this, "facebook") },
                        _react2.default.createElement('span', { className: 'social-icons-sprite fb-icon' })
                    ),
                    _react2.default.createElement(
                        'li',
                        { className: 'tweet-container rounded-corner2', onClick: this.shareLink.bind(this, "twitter") },
                        _react2.default.createElement('span', { className: 'social-icons-sprite tweet-icon' })
                    ),
                    _react2.default.createElement(
                        'li',
                        { className: 'gplus-container rounded-corner2', onClick: this.shareLink.bind(this, "googleplus") },
                        _react2.default.createElement('span', { className: 'social-icons-sprite gplus-icon' })
                    )
                ),
                _react2.default.createElement('div', { className: 'clear' })
            );
        }
    }]);

    return SocialMediaSlug;
}(_react2.default.Component);

module.exports = SocialMediaSlug;

/***/ }),
/* 58 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


module.exports = __webpack_require__(138);

/***/ }),
/* 59 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.getTrackLeft = exports.getTrackAnimateCSS = exports.getTrackCSS = undefined;

var _reactDom = __webpack_require__(0);

var _reactDom2 = _interopRequireDefault(_reactDom);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var checkSpecKeys = function checkSpecKeys(spec, keysArray) {
  return keysArray.reduce(function (value, key) {
    return value && spec.hasOwnProperty(key);
  }, true) ? null : console.error('Keys Missing', spec);
};

var getTrackCSS = exports.getTrackCSS = function getTrackCSS(spec) {
  checkSpecKeys(spec, ['left', 'variableWidth', 'slideCount', 'slidesToShow', 'slideWidth']);

  var trackWidth;

  if (spec.variableWidth) {
    trackWidth = (spec.slideCount + 2 * spec.slidesToShow) * spec.slideWidth;
  } else if (spec.centerMode) {
    trackWidth = (spec.slideCount + 2 * (spec.slidesToShow + 1)) * spec.slideWidth;
  } else {
    trackWidth = (spec.slideCount + 2 * spec.slidesToShow) * spec.slideWidth;
  }

  var style = {
    opacity: 1,
    width: trackWidth,
    WebkitTransform: 'translate3d(' + spec.left + 'px, 0px, 0px)',
    transform: 'translate3d(' + spec.left + 'px, 0px, 0px)',
    transition: '',
    WebkitTransition: '',
    msTransform: 'translateX(' + spec.left + 'px)'
  };

  // Fallback for IE8
  if (!window.addEventListener && window.attachEvent) {
    style.marginLeft = spec.left + 'px';
  }

  return style;
};

var getTrackAnimateCSS = exports.getTrackAnimateCSS = function getTrackAnimateCSS(spec) {
  checkSpecKeys(spec, ['left', 'variableWidth', 'slideCount', 'slidesToShow', 'slideWidth', 'speed', 'cssEase']);

  var style = getTrackCSS(spec);
  // useCSS is true by default so it can be undefined
  style.WebkitTransition = '-webkit-transform ' + spec.speed + 'ms ' + spec.cssEase;
  style.transition = 'transform ' + spec.speed + 'ms ' + spec.cssEase;
  return style;
};

var getTrackLeft = exports.getTrackLeft = function getTrackLeft(spec) {

  checkSpecKeys(spec, ['slideIndex', 'trackRef', 'infinite', 'centerMode', 'slideCount', 'slidesToShow', 'slidesToScroll', 'slideWidth', 'listWidth', 'variableWidth']);

  var slideOffset = 0;
  var targetLeft;
  var targetSlide;

  if (spec.fade) {
    return 0;
  }

  if (spec.infinite) {
    if (spec.slideCount > spec.slidesToShow) {
      slideOffset = spec.slideWidth * spec.slidesToShow * -1;
    }
    if (spec.slideCount % spec.slidesToScroll !== 0) {
      if (spec.slideIndex + spec.slidesToScroll > spec.slideCount && spec.slideCount > spec.slidesToShow) {
        if (spec.slideIndex > spec.slideCount) {
          slideOffset = (spec.slidesToShow - (spec.slideIndex - spec.slideCount)) * spec.slideWidth * -1;
        } else {
          slideOffset = spec.slideCount % spec.slidesToScroll * spec.slideWidth * -1;
        }
      }
    }
  } else {

    if (spec.slideCount % spec.slidesToScroll !== 0) {
      if (spec.slideIndex + spec.slidesToScroll > spec.slideCount && spec.slideCount > spec.slidesToShow) {
        var slidesToOffset = spec.slidesToShow - spec.slideCount % spec.slidesToScroll;
        slideOffset = slidesToOffset * spec.slideWidth;
      }
    }
  }

  if (spec.centerMode) {
    if (spec.infinite) {
      slideOffset += spec.slideWidth * Math.floor(spec.slidesToShow / 2);
    } else {
      slideOffset = spec.slideWidth * Math.floor(spec.slidesToShow / 2);
    }
  }

  targetLeft = spec.slideIndex * spec.slideWidth * -1 + slideOffset;

  if (spec.variableWidth === true) {
    var targetSlideIndex;
    if (spec.slideCount <= spec.slidesToShow || spec.infinite === false) {
      targetSlide = _reactDom2.default.findDOMNode(spec.trackRef).childNodes[spec.slideIndex];
    } else {
      targetSlideIndex = spec.slideIndex + spec.slidesToShow;
      targetSlide = _reactDom2.default.findDOMNode(spec.trackRef).childNodes[targetSlideIndex];
    }
    targetLeft = targetSlide ? targetSlide.offsetLeft * -1 : 0;
    if (spec.centerMode === true) {
      if (spec.infinite === false) {
        targetSlide = _reactDom2.default.findDOMNode(spec.trackRef).children[spec.slideIndex];
      } else {
        targetSlide = _reactDom2.default.findDOMNode(spec.trackRef).children[spec.slideIndex + spec.slidesToShow + 1];
      }

      targetLeft = targetSlide ? targetSlide.offsetLeft * -1 : 0;
      targetLeft += (spec.listWidth - targetSlide.offsetWidth) / 2;
    }
  }

  return targetLeft;
};

/***/ }),
/* 60 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

var _reactDom = __webpack_require__(0);

var _reactDom2 = _interopRequireDefault(_reactDom);

var _trackHelper = __webpack_require__(59);

var _objectAssign = __webpack_require__(19);

var _objectAssign2 = _interopRequireDefault(_objectAssign);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var helpers = {
  initialize: function initialize(props) {
    var slideCount = _react2.default.Children.count(props.children);
    var listWidth = this.getWidth(_reactDom2.default.findDOMNode(this.list));
    var trackWidth = this.getWidth(_reactDom2.default.findDOMNode(this.track));
    var slideWidth = trackWidth / props.slidesToShow;

    var currentSlide = props.rtl ? slideCount - 1 - props.initialSlide : props.initialSlide;

    this.setState({
      slideCount: slideCount,
      slideWidth: slideWidth,
      listWidth: listWidth,
      trackWidth: trackWidth,
      currentSlide: currentSlide
    }, function () {

      var targetLeft = (0, _trackHelper.getTrackLeft)((0, _objectAssign2.default)({
        slideIndex: this.state.currentSlide,
        trackRef: this.track
      }, props, this.state));
      // getCSS function needs previously set state
      var trackStyle = (0, _trackHelper.getTrackCSS)((0, _objectAssign2.default)({ left: targetLeft }, props, this.state));

      this.setState({ trackStyle: trackStyle });

      this.autoPlay(); // once we're set up, trigger the initial autoplay.
    });
  },
  update: function update(props) {
    // This method has mostly same code as initialize method.
    // Refactor it
    var slideCount = _react2.default.Children.count(props.children);
    var listWidth = this.getWidth(_reactDom2.default.findDOMNode(this.list));
    var trackWidth = this.getWidth(_reactDom2.default.findDOMNode(this.track));
    var slideWidth = this.getWidth(_reactDom2.default.findDOMNode(this)) / props.slidesToShow;

    // pause slider if autoplay is set to false
    if (!props.autoplay) this.pause();

    this.setState({
      slideCount: slideCount,
      slideWidth: slideWidth,
      listWidth: listWidth,
      trackWidth: trackWidth
    }, function () {

      var targetLeft = (0, _trackHelper.getTrackLeft)((0, _objectAssign2.default)({
        slideIndex: this.state.currentSlide,
        trackRef: this.track
      }, props, this.state));
      // getCSS function needs previously set state
      var trackStyle = (0, _trackHelper.getTrackCSS)((0, _objectAssign2.default)({ left: targetLeft }, props, this.state));

      this.setState({ trackStyle: trackStyle });
    });
  },
  getWidth: function getWidth(elem) {
    return elem.getBoundingClientRect().width || elem.offsetWidth;
  },
  adaptHeight: function adaptHeight() {
    if (this.props.adaptiveHeight) {
      var selector = '[data-index="' + this.state.currentSlide + '"]';
      if (this.list) {
        var slickList = _reactDom2.default.findDOMNode(this.list);
        slickList.style.height = slickList.querySelector(selector).offsetHeight + 'px';
      }
    }
  },
  slideHandler: function slideHandler(index) {
    var _this = this;

    // Functionality of animateSlide and postSlide is merged into this function
    // console.log('slideHandler', index);
    var targetSlide, currentSlide;
    var targetLeft, currentLeft;
    var callback;

    if (this.props.waitForAnimate && this.state.animating) {
      return;
    }

    if (this.props.fade) {
      currentSlide = this.state.currentSlide;

      // Don't change slide if it's not infite and current slide is the first or last slide.
      if (this.props.infinite === false && (index < 0 || index >= this.state.slideCount)) {
        return;
      }

      //  Shifting targetSlide back into the range
      if (index < 0) {
        targetSlide = index + this.state.slideCount;
      } else if (index >= this.state.slideCount) {
        targetSlide = index - this.state.slideCount;
      } else {
        targetSlide = index;
      }

      if (this.props.lazyLoad && this.state.lazyLoadedList.indexOf(targetSlide) < 0) {
        this.setState({
          lazyLoadedList: this.state.lazyLoadedList.concat(targetSlide)
        });
      }

      callback = function callback() {
        _this.setState({
          animating: false
        });
        if (_this.props.afterChange) {
          _this.props.afterChange(targetSlide);
        }
        delete _this.animationEndCallback;
      };

      this.setState({
        animating: true,
        currentSlide: targetSlide
      }, function () {
        this.animationEndCallback = setTimeout(callback, this.props.speed);
      });

      if (this.props.beforeChange) {
        this.props.beforeChange(this.state.currentSlide, targetSlide);
      }

      this.autoPlay();
      return;
    }

    targetSlide = index;
    if (targetSlide < 0) {
      if (this.props.infinite === false) {
        currentSlide = 0;
      } else if (this.state.slideCount % this.props.slidesToScroll !== 0) {
        currentSlide = this.state.slideCount - this.state.slideCount % this.props.slidesToScroll;
      } else {
        currentSlide = this.state.slideCount + targetSlide;
      }
    } else if (targetSlide >= this.state.slideCount) {
      if (this.props.infinite === false) {
        currentSlide = this.state.slideCount - this.props.slidesToShow;
      } else if (this.state.slideCount % this.props.slidesToScroll !== 0) {
        currentSlide = 0;
      } else {
        currentSlide = targetSlide - this.state.slideCount;
      }
    } else {
      currentSlide = targetSlide;
    }

    targetLeft = (0, _trackHelper.getTrackLeft)((0, _objectAssign2.default)({
      slideIndex: targetSlide,
      trackRef: this.track
    }, this.props, this.state));

    currentLeft = (0, _trackHelper.getTrackLeft)((0, _objectAssign2.default)({
      slideIndex: currentSlide,
      trackRef: this.track
    }, this.props, this.state));

    if (this.props.infinite === false) {
      targetLeft = currentLeft;
    }

    if (this.props.beforeChange) {
      this.props.beforeChange(this.state.currentSlide, currentSlide);
    }

    if (this.props.lazyLoad) {
      var loaded = true;
      var slidesToLoad = [];
      for (var i = targetSlide; i < targetSlide + this.props.slidesToShow; i++) {
        loaded = loaded && this.state.lazyLoadedList.indexOf(i) >= 0;
        if (!loaded) {
          slidesToLoad.push(i);
        }
      }
      if (!loaded) {
        this.setState({
          lazyLoadedList: this.state.lazyLoadedList.concat(slidesToLoad)
        });
      }
    }

    // Slide Transition happens here.
    // animated transition happens to target Slide and
    // non - animated transition happens to current Slide
    // If CSS transitions are false, directly go the current slide.

    if (this.props.useCSS === false) {

      this.setState({
        currentSlide: currentSlide,
        trackStyle: (0, _trackHelper.getTrackCSS)((0, _objectAssign2.default)({ left: currentLeft }, this.props, this.state))
      }, function () {
        if (this.props.afterChange) {
          this.props.afterChange(currentSlide);
        }
      });
    } else {

      var nextStateChanges = {
        animating: false,
        currentSlide: currentSlide,
        trackStyle: (0, _trackHelper.getTrackCSS)((0, _objectAssign2.default)({ left: currentLeft }, this.props, this.state)),
        swipeLeft: null
      };

      callback = function callback() {
        _this.setState(nextStateChanges);
        if (_this.props.afterChange) {
          _this.props.afterChange(currentSlide);
        }
        delete _this.animationEndCallback;
      };

      this.setState({
        animating: true,
        currentSlide: currentSlide,
        trackStyle: (0, _trackHelper.getTrackAnimateCSS)((0, _objectAssign2.default)({ left: targetLeft }, this.props, this.state))
      }, function () {
        this.animationEndCallback = setTimeout(callback, this.props.speed);
      });
    }

    this.autoPlay();
  },
  swipeDirection: function swipeDirection(touchObject) {
    var xDist, yDist, r, swipeAngle;

    xDist = touchObject.startX - touchObject.curX;
    yDist = touchObject.startY - touchObject.curY;
    r = Math.atan2(yDist, xDist);

    swipeAngle = Math.round(r * 180 / Math.PI);
    if (swipeAngle < 0) {
      swipeAngle = 360 - Math.abs(swipeAngle);
    }
    if (swipeAngle <= 45 && swipeAngle >= 0 || swipeAngle <= 360 && swipeAngle >= 315) {
      return this.props.rtl === false ? 'left' : 'right';
    }
    if (swipeAngle >= 135 && swipeAngle <= 225) {
      return this.props.rtl === false ? 'right' : 'left';
    }

    return 'vertical';
  },
  autoPlay: function autoPlay() {
    var _this2 = this;

    if (this.state.autoPlayTimer) {
      return;
    }
    var play = function play() {
      if (_this2.state.mounted) {
        var nextIndex = _this2.props.rtl ? _this2.state.currentSlide - _this2.props.slidesToScroll : _this2.state.currentSlide + _this2.props.slidesToScroll;
        _this2.slideHandler(nextIndex);
      }
    };
    if (this.props.autoplay) {
      this.setState({
        autoPlayTimer: window.setInterval(play, this.props.autoplaySpeed)
      });
    }
  },
  pause: function pause() {
    if (this.state.autoPlayTimer) {
      window.clearInterval(this.state.autoPlayTimer);
      this.setState({
        autoPlayTimer: null
      });
    }
  }
};

exports.default = helpers;

/***/ }),
/* 61 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var defaultProps = {
    className: '',
    accessibility: true,
    adaptiveHeight: false,
    arrows: true,
    autoplay: false,
    autoplaySpeed: 3000,
    centerMode: false,
    centerPadding: '50px',
    cssEase: 'ease',
    dots: false,
    dotsClass: 'slick-dots',
    draggable: true,
    easing: 'linear',
    edgeFriction: 0.35,
    fade: false,
    focusOnSelect: false,
    infinite: true,
    initialSlide: 0,
    lazyLoad: false,
    pauseOnHover: true,
    responsive: null,
    rtl: false,
    slide: 'div',
    slidesToShow: 1,
    slidesToScroll: 1,
    speed: 500,
    swipe: true,
    swipeToSlide: false,
    touchMove: true,
    touchThreshold: 5,
    useCSS: true,
    variableWidth: false,
    vertical: false,
    waitForAnimate: true,
    afterChange: null,
    beforeChange: null,
    edgeEvent: null,
    init: null,
    swipeEvent: null,
    // nextArrow, prevArrow are react componets
    nextArrow: null,
    prevArrow: null
};

module.exports = defaultProps;

/***/ }),
/* 62 */
/***/ (function(module, exports, __webpack_require__) {

var camel2hyphen = __webpack_require__(145);

var isDimension = function (feature) {
  var re = /[height|width]$/;
  return re.test(feature);
};

var obj2mq = function (obj) {
  var mq = '';
  var features = Object.keys(obj);
  features.forEach(function (feature, index) {
    var value = obj[feature];
    feature = camel2hyphen(feature);
    // Add px to dimension features
    if (isDimension(feature) && typeof value === 'number') {
      value = value + 'px';
    }
    if (value === true) {
      mq += feature;
    } else if (value === false) {
      mq += 'not ' + feature;
    } else {
      mq += '(' + feature + ': ' + value + ')';
    }
    if (index < features.length-1) {
      mq += ' and '
    }
  });
  return mq;
};

var json2mq = function (query) {
  var mq = '';
  if (typeof query === 'string') {
    return query;
  }
  // Handling array of media queries
  if (query instanceof Array) {
    query.forEach(function (q, index) {
      mq += obj2mq(q);
      if (index < query.length-1) {
        mq += ', '
      }
    });
    return mq;
  }
  // Handling single media query
  return obj2mq(query);
};

module.exports = json2mq;

/***/ }),
/* 63 */
/***/ (function(module, exports) {

/**
 * Helper function for iterating over a collection
 *
 * @param collection
 * @param fn
 */
function each(collection, fn) {
    var i      = 0,
        length = collection.length,
        cont;

    for(i; i < length; i++) {
        cont = fn(collection[i], i);
        if(cont === false) {
            break; //allow early exit
        }
    }
}

/**
 * Helper function for determining whether target object is an array
 *
 * @param target the object under test
 * @return {Boolean} true if array, false otherwise
 */
function isArray(target) {
    return Object.prototype.toString.apply(target) === '[object Array]';
}

/**
 * Helper function for determining whether target object is a function
 *
 * @param target the object under test
 * @return {Boolean} true if function, false otherwise
 */
function isFunction(target) {
    return typeof target === 'function';
}

module.exports = {
    isFunction : isFunction,
    isArray : isArray,
    each : each
};


/***/ }),
/* 64 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

var _reactLazyLoad = __webpack_require__(6);

var _reactLazyLoad2 = _interopRequireDefault(_reactLazyLoad);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var CarouselBikeItem = function (_React$Component) {
    _inherits(CarouselBikeItem, _React$Component);

    function CarouselBikeItem() {
        _classCallCheck(this, CarouselBikeItem);

        return _possibleConstructorReturn(this, (CarouselBikeItem.__proto__ || Object.getPrototypeOf(CarouselBikeItem)).apply(this, arguments));
    }

    _createClass(CarouselBikeItem, [{
        key: 'render',
        value: function render() {
            if (!this.props.item) return null;
            var item = this.props.item;
            var priceHtml = null;
            if (item.Price == "") {
                priceHtml = _react2.default.createElement(
                    'span',
                    { className: 'value-size-16' },
                    item.PriceSuffix
                );
            } else {
                priceHtml = _react2.default.createElement(
                    'div',
                    { className: 'value-size-16' },
                    _react2.default.createElement(
                        'span',
                        null,
                        '\u20B9'
                    ),
                    '\xA0',
                    _react2.default.createElement(
                        'span',
                        null,
                        item.Price,
                        ' ',
                        item.PriceSuffix
                    )
                );
            }

            return _react2.default.createElement(
                'li',
                { key: item.Name, className: 'carousel-slide' },
                _react2.default.createElement(
                    'div',
                    { className: 'carousel-card' },
                    _react2.default.createElement(
                        'a',
                        { href: item.DetailPageUrl, title: item.Name, className: 'card-target-block' },
                        _react2.default.createElement(
                            'div',
                            { className: 'card-image-block' },
                            _react2.default.createElement(
                                _reactLazyLoad2.default,
                                { offsetVertical: 0 },
                                _react2.default.createElement('img', { src: item.ImgUrl, alt: item.Name })
                            )
                        ),
                        _react2.default.createElement(
                            'div',
                            { className: 'card-details-block' },
                            _react2.default.createElement(
                                'h3',
                                { className: 'h3-title-target target-link text-truncate' },
                                item.Name
                            ),
                            _react2.default.createElement(
                                'p',
                                { className: 'text-truncate key-size-11' },
                                item.PriceDescription
                            ),
                            priceHtml
                        )
                    )
                )
            );
        }
    }]);

    return CarouselBikeItem;
}(_react2.default.Component);

module.exports = CarouselBikeItem;

/***/ }),
/* 65 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

var _reactLazyLoad = __webpack_require__(6);

var _reactLazyLoad2 = _interopRequireDefault(_reactLazyLoad);

var _reactRouterDom = __webpack_require__(2);

var _VideosCommonFunc = __webpack_require__(14);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var CarouselVideoItem = function (_React$Component) {
    _inherits(CarouselVideoItem, _React$Component);

    function CarouselVideoItem(props) {
        _classCallCheck(this, CarouselVideoItem);

        return _possibleConstructorReturn(this, (CarouselVideoItem.__proto__ || Object.getPrototypeOf(CarouselVideoItem)).call(this, props));
    }

    _createClass(CarouselVideoItem, [{
        key: 'onClickItem',
        value: function onClickItem(item, event) {
            if (this.props.onClickItem && typeof this.props.onClickItem === 'function') {
                event.preventDefault();
                this.props.onClickItem(item);
            }
        }
    }, {
        key: 'render',
        value: function render() {
            if (!this.props.item) return null;
            var item = this.props.item;
            return _react2.default.createElement(
                'li',
                { key: item.BasicId, className: 'carousel-slide' },
                _react2.default.createElement(
                    'div',
                    { className: 'carousel-card' },
                    _react2.default.createElement(
                        _reactRouterDom.Link,
                        { to: (0, _VideosCommonFunc.formVideoUrl)(item.VideoTitleUrl, item.BasicId), title: item.VideoTitle, className: 'card-target-block', onClick: this.onClickItem.bind(this, item) },
                        _react2.default.createElement(
                            'div',
                            { className: 'card-image-block' },
                            _react2.default.createElement(
                                _reactLazyLoad2.default,
                                null,
                                _react2.default.createElement('img', { className: 'swiper-lazy', src: (0, _VideosCommonFunc.formVideoImageUrl)(item.VideoId, 'mqdefault.jpg'), alt: item.VideoTitle })
                            ),
                            _react2.default.createElement('span', { className: 'swiper-lazy-preloader' }),
                            _react2.default.createElement(
                                'span',
                                { className: 'black-overlay' },
                                _react2.default.createElement('span', { className: 'bwmsprite video-play-icon' })
                            )
                        ),
                        _react2.default.createElement(
                            'div',
                            { className: 'card-details-block' },
                            _react2.default.createElement(
                                'h3',
                                { className: 'font12 text-black margin-bottom5 text-truncate-2' },
                                item.VideoTitle
                            ),
                            _react2.default.createElement(
                                'div',
                                { className: 'views-count-container' },
                                _react2.default.createElement('span', { className: 'bwmsprite calender-grey-sm-icon' }),
                                _react2.default.createElement(
                                    'span',
                                    { className: 'article-stats-content' },
                                    item.DisplayDate
                                )
                            ),
                            _react2.default.createElement(
                                'div',
                                { className: 'views-count-container' },
                                _react2.default.createElement('span', { className: 'bwmsprite video-views-icon' }),
                                _react2.default.createElement(
                                    'span',
                                    { className: 'article-stats-content' },
                                    item.Views
                                )
                            ),
                            _react2.default.createElement('div', { className: 'clear' })
                        )
                    )
                )
            );
        }
    }]);

    return CarouselVideoItem;
}(_react2.default.Component);

module.exports = CarouselVideoItem;

/***/ }),
/* 66 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

var _propTypes = __webpack_require__(1);

var _propTypes2 = _interopRequireDefault(_propTypes);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var propTypes = {
  // the height of data preview
  previewHeight: _propTypes2.default.string.isRequired,
  // any children you pass in
  children: _propTypes2.default.node.isRequired,
  // a custom class name
  className: _propTypes2.default.string,
  // a flag to set default state of data
  expanded: _propTypes2.default.bool,
  // the text to show for expand
  expandText: _propTypes2.default.string,
  // the text to show for collapse
  collapseText: _propTypes2.default.string,
  // option to set if expanded data can be collapsed
  collapse: _propTypes2.default.bool,
  // a flag to set ellipsis
  ellipsis: _propTypes2.default.bool,
  // custom text to show before expand text
  ellipsisText: _propTypes2.default.string,
  // called whenever the user clicks expand button
  onExpandClick: _propTypes2.default.func,
  // called whenever the user clicks collapse button
  onCollapseClick: _propTypes2.default.func,
  // called whenever the user clicks expand and collapse button
  onClick: _propTypes2.default.func
};

var defaultProps = {
  className: "",
  expanded: false,
  expandText: "Expand",
  collapseText: "Collapse",
  collapse: true,
  ellipsis: true,
  ellipsisText: "...",
  onExpandClick: null,
  onCollapseClick: null,
  onClick: null
};

var ExpandCollapse = function (_React$Component) {
  _inherits(ExpandCollapse, _React$Component);

  function ExpandCollapse(props) {
    _classCallCheck(this, ExpandCollapse);

    var _this = _possibleConstructorReturn(this, (ExpandCollapse.__proto__ || Object.getPrototypeOf(ExpandCollapse)).call(this, props));

    _this.state = {
      expanded: _this.props.expanded,
      shouldExpand: true
    };

    _this.handleClick = _this.handleClick.bind(_this);
    _this.shouldDataExpand = _this.shouldDataExpand.bind(_this);
    _this.getContentHeight = _this.getContentHeight.bind(_this);
    _this.getButton = _this.getButton.bind(_this);
    _this.getButtonText = _this.getButtonText.bind(_this);
    _this.getClassName = _this.getClassName.bind(_this);
    _this.setScrollPosition = _this.setScrollPosition.bind(_this);
    _this.setRef = _this.setRef.bind(_this);
    return _this;
  }

  _createClass(ExpandCollapse, [{
    key: "componentDidMount",
    value: function componentDidMount() {
      this.shouldDataExpand();
    }
  }, {
    key: "handleClick",
    value: function handleClick(event) {
      this.setState({
        expanded: !this.state.expanded
      }, function () {
        var expanded = this.state.expanded;
        var _props = this.props,
            onExpandClick = _props.onExpandClick,
            onCollapseClick = _props.onCollapseClick,
            onClick = _props.onClick;


        if (onExpandClick) {
          if (expanded) {
            onExpandClick(event);
          }
        }

        if (onCollapseClick) {
          if (!expanded) {
            onCollapseClick(event);
          }
        }

        if (onClick) {
          onClick(event);
        }

        this.setScrollPosition();
      });
    }
  }, {
    key: "setScrollPosition",
    value: function setScrollPosition() {
      var expanded = this.state.expanded;


      if (!expanded) {
        var contentRect = this.toggleContent.getBoundingClientRect();
        if (contentRect.top < 0) {
          var offsetTop = Math.abs(contentRect.top + (window.pageYOffset || document.documentElement.scrollTop));

          window.scrollTo(0, offsetTop);
        }
      }
    }
  }, {
    key: "shouldDataExpand",
    value: function shouldDataExpand() {
      var expanded = this.state.expanded;


      var contentRect = this.toggleContent.getBoundingClientRect();
      var contentBodyRect = this.toggleContent.querySelector(".react-expand-collapse__body").getBoundingClientRect();

      if (contentRect.height > contentBodyRect.height && !expanded) {
        this.setState({
          shouldExpand: false
        });
      }
    }
  }, {
    key: "getContentHeight",
    value: function getContentHeight() {
      var _state = this.state,
          expanded = _state.expanded,
          shouldExpand = _state.shouldExpand;
      var previewHeight = this.props.previewHeight;


      if (expanded || !shouldExpand) {
        return "auto";
      }

      return previewHeight;
    }
  }, {
    key: "getButton",
    value: function getButton() {
      var _state2 = this.state,
          expanded = _state2.expanded,
          shouldExpand = _state2.shouldExpand;
      var collapse = this.props.collapse;


      if (shouldExpand) {
        if (!collapse && expanded) {
          return "";
        }

        var buttonText = this.getButtonText();

        return _react2.default.createElement(
          "span",
          {
            className: "react-expand-collapse__button",
            onClick: this.handleClick,
            "aria-label": buttonText,
            "aria-expanded": expanded,
            role: "button"
          },
          buttonText
        );
      }

      return "";
    }
  }, {
    key: "getButtonText",
    value: function getButtonText() {
      var expanded = this.state.expanded;
      var _props2 = this.props,
          expandText = _props2.expandText,
          collapseText = _props2.collapseText,
          ellipsis = _props2.ellipsis,
          ellipsisText = _props2.ellipsisText;


      var text = expanded ? collapseText : expandText;

      if (ellipsis) {
        text = !expanded ? "" + (ellipsisText + " " + text) : text;
      }

      return text;
    }
  }, {
    key: "getClassName",
    value: function getClassName() {
      var expanded = this.state.expanded;
      var className = this.props.className;


      var expandedClass = expanded ? "react-expand-collapse--expanded" : "";

      var classes = ["react-expand-collapse__content", expandedClass].concat(className.split(" ")).join(" ");

      return classes;
    }
  }, {
    key: "setRef",
    value: function setRef(ref) {
      this.toggleContent = ref;
    }
  }, {
    key: "render",
    value: function render() {
      var className = this.getClassName();
      var contentHeight = this.getContentHeight();
      var button = this.getButton();

      return _react2.default.createElement(
        "div",
        {
          className: className,
          ref: this.setRef,
          style: { height: contentHeight }
        },
        _react2.default.createElement(
          "div",
          { className: "react-expand-collapse__body" },
          this.props.children
        ),
        button
      );
    }
  }]);

  return ExpandCollapse;
}(_react2.default.Component);

ExpandCollapse.propTypes = propTypes;
ExpandCollapse.defaultProps = defaultProps;

exports.default = ExpandCollapse;

/***/ }),
/* 67 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "b", function() { return subscriptionShape; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "a", function() { return storeShape; });
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_prop_types__ = __webpack_require__(1);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_prop_types___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_0_prop_types__);


var subscriptionShape = __WEBPACK_IMPORTED_MODULE_0_prop_types___default.a.shape({
  trySubscribe: __WEBPACK_IMPORTED_MODULE_0_prop_types___default.a.func.isRequired,
  tryUnsubscribe: __WEBPACK_IMPORTED_MODULE_0_prop_types___default.a.func.isRequired,
  notifyNestedSubs: __WEBPACK_IMPORTED_MODULE_0_prop_types___default.a.func.isRequired,
  isSubscribed: __WEBPACK_IMPORTED_MODULE_0_prop_types___default.a.func.isRequired
});

var storeShape = __WEBPACK_IMPORTED_MODULE_0_prop_types___default.a.shape({
  subscribe: __WEBPACK_IMPORTED_MODULE_0_prop_types___default.a.func.isRequired,
  dispatch: __WEBPACK_IMPORTED_MODULE_0_prop_types___default.a.func.isRequired,
  getState: __WEBPACK_IMPORTED_MODULE_0_prop_types___default.a.func.isRequired
});

/***/ }),
/* 68 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony export (immutable) */ __webpack_exports__["a"] = connectAdvanced;
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_hoist_non_react_statics__ = __webpack_require__(55);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_hoist_non_react_statics___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_0_hoist_non_react_statics__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_invariant__ = __webpack_require__(7);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_invariant___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_1_invariant__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2_react__ = __webpack_require__(0);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_3__utils_Subscription__ = __webpack_require__(173);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_4__utils_PropTypes__ = __webpack_require__(67);
var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

function _objectWithoutProperties(obj, keys) { var target = {}; for (var i in obj) { if (keys.indexOf(i) >= 0) continue; if (!Object.prototype.hasOwnProperty.call(obj, i)) continue; target[i] = obj[i]; } return target; }








var hotReloadingVersion = 0;
var dummyState = {};
function noop() {}
function makeSelectorStateful(sourceSelector, store) {
  // wrap the selector in an object that tracks its results between runs.
  var selector = {
    run: function runComponentSelector(props) {
      try {
        var nextProps = sourceSelector(store.getState(), props);
        if (nextProps !== selector.props || selector.error) {
          selector.shouldComponentUpdate = true;
          selector.props = nextProps;
          selector.error = null;
        }
      } catch (error) {
        selector.shouldComponentUpdate = true;
        selector.error = error;
      }
    }
  };

  return selector;
}

function connectAdvanced(
/*
  selectorFactory is a func that is responsible for returning the selector function used to
  compute new props from state, props, and dispatch. For example:
     export default connectAdvanced((dispatch, options) => (state, props) => ({
      thing: state.things[props.thingId],
      saveThing: fields => dispatch(actionCreators.saveThing(props.thingId, fields)),
    }))(YourComponent)
   Access to dispatch is provided to the factory so selectorFactories can bind actionCreators
  outside of their selector as an optimization. Options passed to connectAdvanced are passed to
  the selectorFactory, along with displayName and WrappedComponent, as the second argument.
   Note that selectorFactory is responsible for all caching/memoization of inbound and outbound
  props. Do not use connectAdvanced directly without memoizing results between calls to your
  selector, otherwise the Connect component will re-render on every state or props change.
*/
selectorFactory) {
  var _contextTypes, _childContextTypes;

  var _ref = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {},
      _ref$getDisplayName = _ref.getDisplayName,
      getDisplayName = _ref$getDisplayName === undefined ? function (name) {
    return 'ConnectAdvanced(' + name + ')';
  } : _ref$getDisplayName,
      _ref$methodName = _ref.methodName,
      methodName = _ref$methodName === undefined ? 'connectAdvanced' : _ref$methodName,
      _ref$renderCountProp = _ref.renderCountProp,
      renderCountProp = _ref$renderCountProp === undefined ? undefined : _ref$renderCountProp,
      _ref$shouldHandleStat = _ref.shouldHandleStateChanges,
      shouldHandleStateChanges = _ref$shouldHandleStat === undefined ? true : _ref$shouldHandleStat,
      _ref$storeKey = _ref.storeKey,
      storeKey = _ref$storeKey === undefined ? 'store' : _ref$storeKey,
      _ref$withRef = _ref.withRef,
      withRef = _ref$withRef === undefined ? false : _ref$withRef,
      connectOptions = _objectWithoutProperties(_ref, ['getDisplayName', 'methodName', 'renderCountProp', 'shouldHandleStateChanges', 'storeKey', 'withRef']);

  var subscriptionKey = storeKey + 'Subscription';
  var version = hotReloadingVersion++;

  var contextTypes = (_contextTypes = {}, _contextTypes[storeKey] = __WEBPACK_IMPORTED_MODULE_4__utils_PropTypes__["a" /* storeShape */], _contextTypes[subscriptionKey] = __WEBPACK_IMPORTED_MODULE_4__utils_PropTypes__["b" /* subscriptionShape */], _contextTypes);
  var childContextTypes = (_childContextTypes = {}, _childContextTypes[subscriptionKey] = __WEBPACK_IMPORTED_MODULE_4__utils_PropTypes__["b" /* subscriptionShape */], _childContextTypes);

  return function wrapWithConnect(WrappedComponent) {
    __WEBPACK_IMPORTED_MODULE_1_invariant___default()(typeof WrappedComponent == 'function', 'You must pass a component to the function returned by ' + (methodName + '. Instead received ' + JSON.stringify(WrappedComponent)));

    var wrappedComponentName = WrappedComponent.displayName || WrappedComponent.name || 'Component';

    var displayName = getDisplayName(wrappedComponentName);

    var selectorFactoryOptions = _extends({}, connectOptions, {
      getDisplayName: getDisplayName,
      methodName: methodName,
      renderCountProp: renderCountProp,
      shouldHandleStateChanges: shouldHandleStateChanges,
      storeKey: storeKey,
      withRef: withRef,
      displayName: displayName,
      wrappedComponentName: wrappedComponentName,
      WrappedComponent: WrappedComponent
    });

    var Connect = function (_Component) {
      _inherits(Connect, _Component);

      function Connect(props, context) {
        _classCallCheck(this, Connect);

        var _this = _possibleConstructorReturn(this, _Component.call(this, props, context));

        _this.version = version;
        _this.state = {};
        _this.renderCount = 0;
        _this.store = props[storeKey] || context[storeKey];
        _this.propsMode = Boolean(props[storeKey]);
        _this.setWrappedInstance = _this.setWrappedInstance.bind(_this);

        __WEBPACK_IMPORTED_MODULE_1_invariant___default()(_this.store, 'Could not find "' + storeKey + '" in either the context or props of ' + ('"' + displayName + '". Either wrap the root component in a <Provider>, ') + ('or explicitly pass "' + storeKey + '" as a prop to "' + displayName + '".'));

        _this.initSelector();
        _this.initSubscription();
        return _this;
      }

      Connect.prototype.getChildContext = function getChildContext() {
        var _ref2;

        // If this component received store from props, its subscription should be transparent
        // to any descendants receiving store+subscription from context; it passes along
        // subscription passed to it. Otherwise, it shadows the parent subscription, which allows
        // Connect to control ordering of notifications to flow top-down.
        var subscription = this.propsMode ? null : this.subscription;
        return _ref2 = {}, _ref2[subscriptionKey] = subscription || this.context[subscriptionKey], _ref2;
      };

      Connect.prototype.componentDidMount = function componentDidMount() {
        if (!shouldHandleStateChanges) return;

        // componentWillMount fires during server side rendering, but componentDidMount and
        // componentWillUnmount do not. Because of this, trySubscribe happens during ...didMount.
        // Otherwise, unsubscription would never take place during SSR, causing a memory leak.
        // To handle the case where a child component may have triggered a state change by
        // dispatching an action in its componentWillMount, we have to re-run the select and maybe
        // re-render.
        this.subscription.trySubscribe();
        this.selector.run(this.props);
        if (this.selector.shouldComponentUpdate) this.forceUpdate();
      };

      Connect.prototype.componentWillReceiveProps = function componentWillReceiveProps(nextProps) {
        this.selector.run(nextProps);
      };

      Connect.prototype.shouldComponentUpdate = function shouldComponentUpdate() {
        return this.selector.shouldComponentUpdate;
      };

      Connect.prototype.componentWillUnmount = function componentWillUnmount() {
        if (this.subscription) this.subscription.tryUnsubscribe();
        this.subscription = null;
        this.notifyNestedSubs = noop;
        this.store = null;
        this.selector.run = noop;
        this.selector.shouldComponentUpdate = false;
      };

      Connect.prototype.getWrappedInstance = function getWrappedInstance() {
        __WEBPACK_IMPORTED_MODULE_1_invariant___default()(withRef, 'To access the wrapped instance, you need to specify ' + ('{ withRef: true } in the options argument of the ' + methodName + '() call.'));
        return this.wrappedInstance;
      };

      Connect.prototype.setWrappedInstance = function setWrappedInstance(ref) {
        this.wrappedInstance = ref;
      };

      Connect.prototype.initSelector = function initSelector() {
        var sourceSelector = selectorFactory(this.store.dispatch, selectorFactoryOptions);
        this.selector = makeSelectorStateful(sourceSelector, this.store);
        this.selector.run(this.props);
      };

      Connect.prototype.initSubscription = function initSubscription() {
        if (!shouldHandleStateChanges) return;

        // parentSub's source should match where store came from: props vs. context. A component
        // connected to the store via props shouldn't use subscription from context, or vice versa.
        var parentSub = (this.propsMode ? this.props : this.context)[subscriptionKey];
        this.subscription = new __WEBPACK_IMPORTED_MODULE_3__utils_Subscription__["a" /* default */](this.store, parentSub, this.onStateChange.bind(this));

        // `notifyNestedSubs` is duplicated to handle the case where the component is  unmounted in
        // the middle of the notification loop, where `this.subscription` will then be null. An
        // extra null check every change can be avoided by copying the method onto `this` and then
        // replacing it with a no-op on unmount. This can probably be avoided if Subscription's
        // listeners logic is changed to not call listeners that have been unsubscribed in the
        // middle of the notification loop.
        this.notifyNestedSubs = this.subscription.notifyNestedSubs.bind(this.subscription);
      };

      Connect.prototype.onStateChange = function onStateChange() {
        this.selector.run(this.props);

        if (!this.selector.shouldComponentUpdate) {
          this.notifyNestedSubs();
        } else {
          this.componentDidUpdate = this.notifyNestedSubsOnComponentDidUpdate;
          this.setState(dummyState);
        }
      };

      Connect.prototype.notifyNestedSubsOnComponentDidUpdate = function notifyNestedSubsOnComponentDidUpdate() {
        // `componentDidUpdate` is conditionally implemented when `onStateChange` determines it
        // needs to notify nested subs. Once called, it unimplements itself until further state
        // changes occur. Doing it this way vs having a permanent `componentDidUpdate` that does
        // a boolean check every time avoids an extra method call most of the time, resulting
        // in some perf boost.
        this.componentDidUpdate = undefined;
        this.notifyNestedSubs();
      };

      Connect.prototype.isSubscribed = function isSubscribed() {
        return Boolean(this.subscription) && this.subscription.isSubscribed();
      };

      Connect.prototype.addExtraProps = function addExtraProps(props) {
        if (!withRef && !renderCountProp && !(this.propsMode && this.subscription)) return props;
        // make a shallow copy so that fields added don't leak to the original selector.
        // this is especially important for 'ref' since that's a reference back to the component
        // instance. a singleton memoized selector would then be holding a reference to the
        // instance, preventing the instance from being garbage collected, and that would be bad
        var withExtras = _extends({}, props);
        if (withRef) withExtras.ref = this.setWrappedInstance;
        if (renderCountProp) withExtras[renderCountProp] = this.renderCount++;
        if (this.propsMode && this.subscription) withExtras[subscriptionKey] = this.subscription;
        return withExtras;
      };

      Connect.prototype.render = function render() {
        var selector = this.selector;
        selector.shouldComponentUpdate = false;

        if (selector.error) {
          throw selector.error;
        } else {
          return Object(__WEBPACK_IMPORTED_MODULE_2_react__["createElement"])(WrappedComponent, this.addExtraProps(selector.props));
        }
      };

      return Connect;
    }(__WEBPACK_IMPORTED_MODULE_2_react__["Component"]);

    Connect.WrappedComponent = WrappedComponent;
    Connect.displayName = displayName;
    Connect.childContextTypes = childContextTypes;
    Connect.contextTypes = contextTypes;
    Connect.propTypes = contextTypes;

    if (true) {
      Connect.prototype.componentWillUpdate = function componentWillUpdate() {
        var _this2 = this;

        // We are hot reloading!
        if (this.version !== version) {
          this.version = version;
          this.initSelector();

          // If any connected descendants don't hot reload (and resubscribe in the process), their
          // listeners will be lost when we unsubscribe. Unfortunately, by copying over all
          // listeners, this does mean that the old versions of connected descendants will still be
          // notified of state changes; however, their onStateChange function is a no-op so this
          // isn't a huge deal.
          var oldListeners = [];

          if (this.subscription) {
            oldListeners = this.subscription.listeners.get();
            this.subscription.tryUnsubscribe();
          }
          this.initSubscription();
          if (shouldHandleStateChanges) {
            this.subscription.trySubscribe();
            oldListeners.forEach(function (listener) {
              return _this2.subscription.listeners.subscribe(listener);
            });
          }
        }
      };
    }

    return __WEBPACK_IMPORTED_MODULE_0_hoist_non_react_statics___default()(Connect, WrappedComponent);
  };
}

/***/ }),
/* 69 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "a", function() { return ActionTypes; });
/* harmony export (immutable) */ __webpack_exports__["b"] = createStore;
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_lodash_es_isPlainObject__ = __webpack_require__(40);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_symbol_observable__ = __webpack_require__(185);



/**
 * These are private action types reserved by Redux.
 * For any unknown actions, you must return the current state.
 * If the current state is undefined, you must return the initial state.
 * Do not reference these action types directly in your code.
 */
var ActionTypes = {
  INIT: '@@redux/INIT'

  /**
   * Creates a Redux store that holds the state tree.
   * The only way to change the data in the store is to call `dispatch()` on it.
   *
   * There should only be a single store in your app. To specify how different
   * parts of the state tree respond to actions, you may combine several reducers
   * into a single reducer function by using `combineReducers`.
   *
   * @param {Function} reducer A function that returns the next state tree, given
   * the current state tree and the action to handle.
   *
   * @param {any} [preloadedState] The initial state. You may optionally specify it
   * to hydrate the state from the server in universal apps, or to restore a
   * previously serialized user session.
   * If you use `combineReducers` to produce the root reducer function, this must be
   * an object with the same shape as `combineReducers` keys.
   *
   * @param {Function} [enhancer] The store enhancer. You may optionally specify it
   * to enhance the store with third-party capabilities such as middleware,
   * time travel, persistence, etc. The only store enhancer that ships with Redux
   * is `applyMiddleware()`.
   *
   * @returns {Store} A Redux store that lets you read the state, dispatch actions
   * and subscribe to changes.
   */
};function createStore(reducer, preloadedState, enhancer) {
  var _ref2;

  if (typeof preloadedState === 'function' && typeof enhancer === 'undefined') {
    enhancer = preloadedState;
    preloadedState = undefined;
  }

  if (typeof enhancer !== 'undefined') {
    if (typeof enhancer !== 'function') {
      throw new Error('Expected the enhancer to be a function.');
    }

    return enhancer(createStore)(reducer, preloadedState);
  }

  if (typeof reducer !== 'function') {
    throw new Error('Expected the reducer to be a function.');
  }

  var currentReducer = reducer;
  var currentState = preloadedState;
  var currentListeners = [];
  var nextListeners = currentListeners;
  var isDispatching = false;

  function ensureCanMutateNextListeners() {
    if (nextListeners === currentListeners) {
      nextListeners = currentListeners.slice();
    }
  }

  /**
   * Reads the state tree managed by the store.
   *
   * @returns {any} The current state tree of your application.
   */
  function getState() {
    return currentState;
  }

  /**
   * Adds a change listener. It will be called any time an action is dispatched,
   * and some part of the state tree may potentially have changed. You may then
   * call `getState()` to read the current state tree inside the callback.
   *
   * You may call `dispatch()` from a change listener, with the following
   * caveats:
   *
   * 1. The subscriptions are snapshotted just before every `dispatch()` call.
   * If you subscribe or unsubscribe while the listeners are being invoked, this
   * will not have any effect on the `dispatch()` that is currently in progress.
   * However, the next `dispatch()` call, whether nested or not, will use a more
   * recent snapshot of the subscription list.
   *
   * 2. The listener should not expect to see all state changes, as the state
   * might have been updated multiple times during a nested `dispatch()` before
   * the listener is called. It is, however, guaranteed that all subscribers
   * registered before the `dispatch()` started will be called with the latest
   * state by the time it exits.
   *
   * @param {Function} listener A callback to be invoked on every dispatch.
   * @returns {Function} A function to remove this change listener.
   */
  function subscribe(listener) {
    if (typeof listener !== 'function') {
      throw new Error('Expected listener to be a function.');
    }

    var isSubscribed = true;

    ensureCanMutateNextListeners();
    nextListeners.push(listener);

    return function unsubscribe() {
      if (!isSubscribed) {
        return;
      }

      isSubscribed = false;

      ensureCanMutateNextListeners();
      var index = nextListeners.indexOf(listener);
      nextListeners.splice(index, 1);
    };
  }

  /**
   * Dispatches an action. It is the only way to trigger a state change.
   *
   * The `reducer` function, used to create the store, will be called with the
   * current state tree and the given `action`. Its return value will
   * be considered the **next** state of the tree, and the change listeners
   * will be notified.
   *
   * The base implementation only supports plain object actions. If you want to
   * dispatch a Promise, an Observable, a thunk, or something else, you need to
   * wrap your store creating function into the corresponding middleware. For
   * example, see the documentation for the `redux-thunk` package. Even the
   * middleware will eventually dispatch plain object actions using this method.
   *
   * @param {Object} action A plain object representing “what changed”. It is
   * a good idea to keep actions serializable so you can record and replay user
   * sessions, or use the time travelling `redux-devtools`. An action must have
   * a `type` property which may not be `undefined`. It is a good idea to use
   * string constants for action types.
   *
   * @returns {Object} For convenience, the same action object you dispatched.
   *
   * Note that, if you use a custom middleware, it may wrap `dispatch()` to
   * return something else (for example, a Promise you can await).
   */
  function dispatch(action) {
    if (!Object(__WEBPACK_IMPORTED_MODULE_0_lodash_es_isPlainObject__["a" /* default */])(action)) {
      throw new Error('Actions must be plain objects. ' + 'Use custom middleware for async actions.');
    }

    if (typeof action.type === 'undefined') {
      throw new Error('Actions may not have an undefined "type" property. ' + 'Have you misspelled a constant?');
    }

    if (isDispatching) {
      throw new Error('Reducers may not dispatch actions.');
    }

    try {
      isDispatching = true;
      currentState = currentReducer(currentState, action);
    } finally {
      isDispatching = false;
    }

    var listeners = currentListeners = nextListeners;
    for (var i = 0; i < listeners.length; i++) {
      var listener = listeners[i];
      listener();
    }

    return action;
  }

  /**
   * Replaces the reducer currently used by the store to calculate the state.
   *
   * You might need this if your app implements code splitting and you want to
   * load some of the reducers dynamically. You might also need this if you
   * implement a hot reloading mechanism for Redux.
   *
   * @param {Function} nextReducer The reducer for the store to use instead.
   * @returns {void}
   */
  function replaceReducer(nextReducer) {
    if (typeof nextReducer !== 'function') {
      throw new Error('Expected the nextReducer to be a function.');
    }

    currentReducer = nextReducer;
    dispatch({ type: ActionTypes.INIT });
  }

  /**
   * Interoperability point for observable/reactive libraries.
   * @returns {observable} A minimal observable of state changes.
   * For more information, see the observable proposal:
   * https://github.com/tc39/proposal-observable
   */
  function observable() {
    var _ref;

    var outerSubscribe = subscribe;
    return _ref = {
      /**
       * The minimal observable subscription method.
       * @param {Object} observer Any object that can be used as an observer.
       * The observer object should have a `next` method.
       * @returns {subscription} An object with an `unsubscribe` method that can
       * be used to unsubscribe the observable from the store, and prevent further
       * emission of values from the observable.
       */
      subscribe: function subscribe(observer) {
        if (typeof observer !== 'object') {
          throw new TypeError('Expected the observer to be an object.');
        }

        function observeState() {
          if (observer.next) {
            observer.next(getState());
          }
        }

        observeState();
        var unsubscribe = outerSubscribe(observeState);
        return { unsubscribe: unsubscribe };
      }
    }, _ref[__WEBPACK_IMPORTED_MODULE_1_symbol_observable__["a" /* default */]] = function () {
      return this;
    }, _ref;
  }

  // When a store is created, an "INIT" action is dispatched so that every
  // reducer returns their initial state. This effectively populates
  // the initial state tree.
  dispatch({ type: ActionTypes.INIT });

  return _ref2 = {
    dispatch: dispatch,
    subscribe: subscribe,
    getState: getState,
    replaceReducer: replaceReducer
  }, _ref2[__WEBPACK_IMPORTED_MODULE_1_symbol_observable__["a" /* default */]] = observable, _ref2;
}

/***/ }),
/* 70 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0__root_js__ = __webpack_require__(178);


/** Built-in value references. */
var Symbol = __WEBPACK_IMPORTED_MODULE_0__root_js__["a" /* default */].Symbol;

/* harmony default export */ __webpack_exports__["a"] = (Symbol);


/***/ }),
/* 71 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony export (immutable) */ __webpack_exports__["a"] = warning;
/**
 * Prints a warning in the console if it exists.
 *
 * @param {String} message The warning message.
 * @returns {void}
 */
function warning(message) {
  /* eslint-disable no-console */
  if (typeof console !== 'undefined' && typeof console.error === 'function') {
    console.error(message);
  }
  /* eslint-enable no-console */
  try {
    // This error was thrown as a convenience so that if you enable
    // "break on all exceptions" in your console,
    // it would pause the execution at this line.
    throw new Error(message);
    /* eslint-disable no-empty */
  } catch (e) {}
  /* eslint-enable no-empty */
}

/***/ }),
/* 72 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony export (immutable) */ __webpack_exports__["a"] = compose;
/**
 * Composes single-argument functions from right to left. The rightmost
 * function can take multiple arguments as it provides the signature for
 * the resulting composite function.
 *
 * @param {...Function} funcs The functions to compose.
 * @returns {Function} A function obtained by composing the argument functions
 * from right to left. For example, compose(f, g, h) is identical to doing
 * (...args) => f(g(h(...args))).
 */

function compose() {
  for (var _len = arguments.length, funcs = Array(_len), _key = 0; _key < _len; _key++) {
    funcs[_key] = arguments[_key];
  }

  if (funcs.length === 0) {
    return function (arg) {
      return arg;
    };
  }

  if (funcs.length === 1) {
    return funcs[0];
  }

  return funcs.reduce(function (a, b) {
    return function () {
      return a(b.apply(undefined, arguments));
    };
  });
}

/***/ }),
/* 73 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony export (immutable) */ __webpack_exports__["a"] = wrapMapToPropsConstant;
/* unused harmony export getDependsOnOwnProps */
/* harmony export (immutable) */ __webpack_exports__["b"] = wrapMapToPropsFunc;
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0__utils_verifyPlainObject__ = __webpack_require__(74);


function wrapMapToPropsConstant(getConstant) {
  return function initConstantSelector(dispatch, options) {
    var constant = getConstant(dispatch, options);

    function constantSelector() {
      return constant;
    }
    constantSelector.dependsOnOwnProps = false;
    return constantSelector;
  };
}

// dependsOnOwnProps is used by createMapToPropsProxy to determine whether to pass props as args
// to the mapToProps function being wrapped. It is also used by makePurePropsSelector to determine
// whether mapToProps needs to be invoked when props have changed.
// 
// A length of one signals that mapToProps does not depend on props from the parent component.
// A length of zero is assumed to mean mapToProps is getting args via arguments or ...args and
// therefore not reporting its length accurately..
function getDependsOnOwnProps(mapToProps) {
  return mapToProps.dependsOnOwnProps !== null && mapToProps.dependsOnOwnProps !== undefined ? Boolean(mapToProps.dependsOnOwnProps) : mapToProps.length !== 1;
}

// Used by whenMapStateToPropsIsFunction and whenMapDispatchToPropsIsFunction,
// this function wraps mapToProps in a proxy function which does several things:
// 
//  * Detects whether the mapToProps function being called depends on props, which
//    is used by selectorFactory to decide if it should reinvoke on props changes.
//    
//  * On first call, handles mapToProps if returns another function, and treats that
//    new function as the true mapToProps for subsequent calls.
//    
//  * On first call, verifies the first result is a plain object, in order to warn
//    the developer that their mapToProps function is not returning a valid result.
//    
function wrapMapToPropsFunc(mapToProps, methodName) {
  return function initProxySelector(dispatch, _ref) {
    var displayName = _ref.displayName;

    var proxy = function mapToPropsProxy(stateOrDispatch, ownProps) {
      return proxy.dependsOnOwnProps ? proxy.mapToProps(stateOrDispatch, ownProps) : proxy.mapToProps(stateOrDispatch);
    };

    // allow detectFactoryAndVerify to get ownProps
    proxy.dependsOnOwnProps = true;

    proxy.mapToProps = function detectFactoryAndVerify(stateOrDispatch, ownProps) {
      proxy.mapToProps = mapToProps;
      proxy.dependsOnOwnProps = getDependsOnOwnProps(mapToProps);
      var props = proxy(stateOrDispatch, ownProps);

      if (typeof props === 'function') {
        proxy.mapToProps = props;
        proxy.dependsOnOwnProps = getDependsOnOwnProps(props);
        props = proxy(stateOrDispatch, ownProps);
      }

      if (true) Object(__WEBPACK_IMPORTED_MODULE_0__utils_verifyPlainObject__["a" /* default */])(props, displayName, methodName);

      return props;
    };

    return proxy;
  };
}

/***/ }),
/* 74 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony export (immutable) */ __webpack_exports__["a"] = verifyPlainObject;
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_lodash_es_isPlainObject__ = __webpack_require__(40);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1__warning__ = __webpack_require__(39);



function verifyPlainObject(value, displayName, methodName) {
  if (!Object(__WEBPACK_IMPORTED_MODULE_0_lodash_es_isPlainObject__["a" /* default */])(value)) {
    Object(__WEBPACK_IMPORTED_MODULE_1__warning__["a" /* default */])(methodName + '() in ' + displayName + ' must return a plain object. Instead received ' + value + '.');
  }
}

/***/ }),
/* 75 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.resetVersionSelection = exports.resetBikeVersionListFailure = exports.fetchBikeVersionList = exports.resetMakeModelListFailure = exports.fetchMakeModelList = exports.selectBikeVersion = exports.selectModel = exports.resetSelectedBikeDetailFailure = exports.fetchSelectedBikeDetail = exports.closeSelectBikePopup = exports.openSelectBikePopup = undefined;

var _SelectBikePopup = __webpack_require__(195);

var openBikePopup = function openBikePopup() {
  return {
    type: _SelectBikePopup.selectBikePopup.OPEN_BIKE_POPUP
  };
};

var closeBikePopup = function closeBikePopup() {
  return {
    type: _SelectBikePopup.selectBikePopup.CLOSE_BIKE_POPUP
  };
};

var getSelectedBikeDetail = function getSelectedBikeDetail(dispatch, modelId) {
  if (modelId > 0) {
    var xhr = new XMLHttpRequest();
    xhr.onreadystatechange = function () {
      if (xhr.readyState == 4) {
        if (xhr.status == 200) dispatch({ type: _SelectBikePopup.selectBikePopup.FETCH_MODEL_DETAIL_SUCCESS, payload: JSON.parse(xhr.responseText) });else dispatch({ type: _SelectBikePopup.selectBikePopup.FETCH_MODEL_DETAIL_FAILURE });
      }
    };
    xhr.open('GET', '/api/model/?modelId=' + modelId);
    xhr.send();
    dispatch({ type: _SelectBikePopup.selectBikePopup.FETCH_MODEL_DETAIL });
  }
};

var getBikeVersionList = function getBikeVersionList(dispatch, modelId, cityId) {
  var xhr = new XMLHttpRequest();
  xhr.onreadystatechange = function () {
    if (xhr.readyState == 4) {
      if (xhr.status == 200) {
        dispatch({ type: _SelectBikePopup.selectBikePopup.FETCH_VERSIONLIST_SUCCESS, payload: { versionList: JSON.parse(xhr.responseText), modelId: modelId, cityId: cityId } });
      } else {
        dispatch({ type: _SelectBikePopup.selectBikePopup.FETCH_VERSIONLIST_FAILURE });
      }
    }
  };
  xhr.open('GET', '/api/pwa/PQVersionList/?modelid=' + modelId + '&cityId=' + cityId);
  xhr.send();
  dispatch({ type: _SelectBikePopup.selectBikePopup.FETCHING_VERSIONLIST, payload: modelId });
};

var setModel = function setModel(modelId) {
  return {
    type: _SelectBikePopup.selectBikePopup.SELECT_MODEL,
    payload: { modelId: modelId }
  };
};

var setBikeVersion = function setBikeVersion(versionId) {
  return {
    type: _SelectBikePopup.selectBikePopup.SET_BIKE_VERSION,
    payload: { versionId: versionId }
  };
};

var getMakeModelList = function getMakeModelList(dispatch) {
  var xhr = new XMLHttpRequest();
  xhr.onreadystatechange = function () {
    if (xhr.readyState == 4) {
      if (xhr.status == 200) dispatch({ type: _SelectBikePopup.selectBikePopup.FETCH_BIKELIST_SUCCESS, payload: JSON.parse(xhr.responseText) });else dispatch({ type: _SelectBikePopup.selectBikePopup.FETCH_BIKELIST_FAILURE });
    }
  };
  xhr.open('GET', '/api/pwa/model/all/v2/2/');
  xhr.send();
};

var openSelectBikePopup = exports.openSelectBikePopup = function openSelectBikePopup() {
  return function (dispatch) {
    dispatch(openBikePopup());
  };
};

var closeSelectBikePopup = exports.closeSelectBikePopup = function closeSelectBikePopup() {
  return function (dispatch) {
    dispatch(closeBikePopup());
  };
};

var fetchSelectedBikeDetail = exports.fetchSelectedBikeDetail = function fetchSelectedBikeDetail(modelId) {
  return function (dispatch) {
    return getSelectedBikeDetail(dispatch, modelId);
  };
};

var resetSelectedBikeDetailFailure = exports.resetSelectedBikeDetailFailure = function resetSelectedBikeDetailFailure() {
  return function (dispatch) {
    dispatch({ type: _SelectBikePopup.selectBikePopup.RESET_MODEL_DETAIL_FAILURE });
  };
};

var selectModel = exports.selectModel = function selectModel(modelId) {
  return function (dispatch) {
    dispatch(setModel(modelId));
  };
};

var selectBikeVersion = exports.selectBikeVersion = function selectBikeVersion(versionId) {
  return function (dispatch) {
    dispatch(setBikeVersion(versionId));
  };
};
var fetchMakeModelList = exports.fetchMakeModelList = function fetchMakeModelList() {
  return function (dispatch) {
    return getMakeModelList(dispatch);
  };
};

var resetMakeModelListFailure = exports.resetMakeModelListFailure = function resetMakeModelListFailure() {
  return function (dispatch) {
    dispatch({ type: _SelectBikePopup.selectBikePopup.RESET_BIKELIST_FAILURE });
  };
};

var fetchBikeVersionList = exports.fetchBikeVersionList = function fetchBikeVersionList(modelId, cityId) {
  return function (dispatch) {
    return getBikeVersionList(dispatch, modelId, cityId);
  };
};

var resetBikeVersionListFailure = exports.resetBikeVersionListFailure = function resetBikeVersionListFailure() {
  return function (dispatch) {
    dispatch({ type: _SelectBikePopup.selectBikePopup.RESET_VERSIONLIST_FAILURE });
  };
};
var resetVersionSelection = exports.resetVersionSelection = function resetVersionSelection() {
  return function (dispatch) {
    dispatch({ type: _SelectBikePopup.selectBikePopup.RESET_VERSION_SELECTION });
  };
};

/***/ }),
/* 76 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.selectCity = exports.selectCityNext = exports.closeSelectCityPopup = exports.openSelectCityPopup = exports.resetCityFailure = exports.fetchCity = undefined;

var _FinanceCityPopup = __webpack_require__(77);

var getCity = function getCity(modelId) {
  return function (dispatch) {
    var url = '/api/pwa/cities/model/' + modelId + '/';
    var xhr = new XMLHttpRequest();
    xhr.onreadystatechange = function () {
      if (xhr.readyState == 4) {
        if (xhr.status == 200) {
          dispatch({ type: _FinanceCityPopup.financeCityPopup.FETCH_CITY_SUCCESS, payload: { cities: JSON.parse(xhr.responseText), modelId: modelId } });
        } else {
          dispatch({ type: _FinanceCityPopup.financeCityPopup.FETCH_CITY_FAILURE, payload: { modelId: modelId } });
        }
      }
    };
    xhr.open('GET', url);
    xhr.send();
    dispatch({ type: _FinanceCityPopup.financeCityPopup.FETCH_CITY });
  };
};

var openCityPopup = function openCityPopup() {
  return {
    type: _FinanceCityPopup.financeCityPopup.OPEN_CITY_POPUP
  };
};

var closeCityPopup = function closeCityPopup() {
  return {
    type: _FinanceCityPopup.financeCityPopup.CLOSE_CITY_POPUP
  };
};

var cityNext = function cityNext() {
  return {
    type: _FinanceCityPopup.financeCityPopup.CITY_NEXT
  };
};

var setCity = function setCity(payload) {
  return {
    type: _FinanceCityPopup.financeCityPopup.SET_CITY,
    payload: payload
  };
};

var fetchCity = exports.fetchCity = function fetchCity(modelId) {
  return function (dispatch) {
    dispatch(getCity(modelId));
  };
};

var resetCityFailure = exports.resetCityFailure = function resetCityFailure() {
  return function (dispatch) {
    dispatch({ type: _FinanceCityPopup.financeCityPopup.RESET_CITY_FAILURE });
  };
};

var openSelectCityPopup = exports.openSelectCityPopup = function openSelectCityPopup() {
  return function (dispatch) {
    dispatch(openCityPopup());
  };
};

var closeSelectCityPopup = exports.closeSelectCityPopup = function closeSelectCityPopup() {
  return function (dispatch) {
    dispatch(closeCityPopup());
  };
};

var selectCityNext = exports.selectCityNext = function selectCityNext() {
  return function (dispatch) {
    dispatch(cityNext());
  };
};

var selectCity = exports.selectCity = function selectCity(payload) {
  return function (dispatch) {
    dispatch(setCity(payload));
  };
};

/***/ }),
/* 77 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
    value: true
});
var financeCityPopup = exports.financeCityPopup = {
    FETCH_CITY: 'FETCH_CITY',
    FETCH_CITY_SUCCESS: 'FETCH_CITY_SUCCESS',
    FETCH_CITY_FAILURE: 'FETCH_CITY_FAILURE',
    OPEN_CITY_POPUP: 'OPEN_CITY_POPUP',
    CLOSE_CITY_POPUP: 'CLOSE_CITY_POPUP',
    SET_CITY: 'SET_CITY',
    CITY_NEXT: 'CITY_NEXT',
    RESET_CITY_FAILURE: 'RESET_CITY_FAILURE'
};

/***/ }),
/* 78 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.updateSimilarBikesEmi = exports.fetchSimilarBikes = undefined;

var _SimilarBikesEMI = __webpack_require__(196);

var _immutable = __webpack_require__(17);

var getSimilarBikesData = function getSimilarBikesData(dispatch, modelEmiObj) {
  var xhr = new XMLHttpRequest();
  xhr.onreadystatechange = function () {
    if (xhr.readyState === 4) {
      if (xhr.status === 200) {
        var bikeList = JSON.parse(xhr.responseText);
        var payload = {
          data: bikeList.map(function (json) {
            return {
              makeId: json.makeId,
              makeName: json.makeName,
              modelId: json.modelId,
              modelName: json.modelName,
              modelImage: json.hostUrl + '310x174' + json.originalImagePath,
              onRoadPriceAmount: json.onRoadPrice
            };
          }),
          modelEmiObj: modelEmiObj
        };
        dispatch({
          type: _SimilarBikesEMI.similarBikesEMI.FETCH_SIMILAR_BIKES_EMI,
          payload: payload });
      } else {
        dispatch({
          type: _SimilarBikesEMI.similarBikesEMI.FETCH_SIMILAR_BIKES_FAILED
        });
      }
    }
  };
  var url = '/api/pwa/similarbikes/model/' + modelEmiObj.modelId + '/finance/?topCount=9&cityId=' + modelEmiObj.cityId;
  xhr.open('GET', url);
  xhr.send();
  dispatch({
    type: _SimilarBikesEMI.similarBikesEMI.FETCHING_SIMILAR_BIKES
  });
};

var fetchSimilarBikes = exports.fetchSimilarBikes = function fetchSimilarBikes(modelEmiObj) {
  return function (dispatch) {
    getSimilarBikesData(dispatch, modelEmiObj);
  };
};
var updateSimilarBikesEmi = exports.updateSimilarBikesEmi = function updateSimilarBikesEmi(similarBikesList, modelEmiObj) {
  return function (dispatch) {
    dispatch({
      type: _SimilarBikesEMI.similarBikesEMI.UPDATE_EMI,
      payload: {
        similarBikesList: similarBikesList,
        modelEmiObj: modelEmiObj
      }
    });
  };
};

/***/ }),
/* 79 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

var _propTypes = __webpack_require__(1);

var _propTypes2 = _interopRequireDefault(_propTypes);

var _Collapsible = __webpack_require__(201);

var _Collapsible2 = _interopRequireDefault(_Collapsible);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; } /*
                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                * https://github.com/glennflanagan/react-responsive-accordion
                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                * - Modify class name
                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                */

var propTypes = {
  transitionTime: _propTypes2.default.number,
  easing: _propTypes2.default.string,
  startPosition: _propTypes2.default.number,
  classParentString: _propTypes2.default.string,
  onTriggerClick: _propTypes2.default.func,
  closeable: _propTypes2.default.bool,
  allOpen: _propTypes2.default.bool,
  items: _propTypes2.default.arrayOf(_propTypes2.default.shape({
    props: _propTypes2.default.shape({
      'data-trigger': _propTypes2.default.oneOfType([_propTypes2.default.string, _propTypes2.default.element]).isRequired,
      'data-triggerWhenOpen': _propTypes2.default.oneOfType([_propTypes2.default.string, _propTypes2.default.element]),
      'data-triggerDisabled': _propTypes2.default.bool
    })
  }))
};

var defaultProps = {
  transitionTime: 200,
  easing: 'linear',
  startPosition: -1,
  classParentString: 'collapsible',
  onTriggerClick: null,
  closeable: false,
  allOpen: false
};

var Accordion = function (_React$Component) {
  _inherits(Accordion, _React$Component);

  function Accordion(props) {
    _classCallCheck(this, Accordion);

    var _this = _possibleConstructorReturn(this, (Accordion.__proto__ || Object.getPrototypeOf(Accordion)).call(this, props));

    _this.state = {
      openPosition: _this.props.startPosition
    };

    _this.handleTriggerClick = _this.handleTriggerClick.bind(_this);
    return _this;
  }

  _createClass(Accordion, [{
    key: 'handleTriggerClick',
    value: function handleTriggerClick(position, event) {
      var closeAll = false;

      if (this.props.closeable) {
        closeAll = !this.state.closeAll && position === this.state.openPosition;
      }

      this.setState({
        openPosition: position,
        closeAll: closeAll
      });

      if (this.props.onTriggerClick) {
        this.props.onTriggerClick(position, event);
      }
    }
  }, {
    key: 'render',
    value: function render() {
      var _this2 = this;

      if (!this.props.items) {
        return null;
      }
      var nodes = this.props.items.map(function (node, index) {

        var triggerWhenOpen = node.props['data-trigger-when-open'] ? node.props['data-trigger-when-open'] : node.props['data-trigger'];
        var triggerDisabled = node.props['data-trigger-disabled'] || false;
        var triggerOnOpen = node.props['data-onOpen'] ? node.props['data-onOpen'] : null;

        return _react2.default.createElement(
          _Collapsible2.default,
          {
            key: "collapsible" + index,
            handleTriggerClick: _this2.handleTriggerClick,
            open: !_this2.state.closeAll && _this2.state.openPosition === index || _this2.props.allOpen,
            onOpen: triggerOnOpen,
            trigger: node.props['data-trigger'],
            triggerWhenOpen: triggerWhenOpen,
            triggerDisabled: triggerDisabled,
            transitionTime: _this2.props.transitionTime,
            transitionCloseTime: _this2.props.transitionCloseTime,
            easing: _this2.props.easing,
            classParentString: _this2.props.classParentString,
            accordionPosition: index
          },
          node
        );
      });

      return _react2.default.createElement(
        'div',
        null,
        nodes
      );
    }
  }]);

  return Accordion;
}(_react2.default.Component);

Accordion.propTypes = propTypes;
Accordion.defaultProps = defaultProps;

exports.default = Accordion;

/***/ }),
/* 80 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

var _propTypes = __webpack_require__(1);

var _propTypes2 = _interopRequireDefault(_propTypes);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var propTypes = {
  // custom component specific class
  type: _propTypes2.default.string,
  // custom image class to provide component specific style
  imageClass: _propTypes2.default.string,
  // no result title
  title: _propTypes2.default.string,
  // no result subtitle
  subtitle: _propTypes2.default.string
};

var defaultProps = {
  type: '',
  imageClass: null,
  title: 'No result found',
  subtitle: null
};

var NoResult = function (_React$Component) {
  _inherits(NoResult, _React$Component);

  function NoResult(props) {
    _classCallCheck(this, NoResult);

    return _possibleConstructorReturn(this, (NoResult.__proto__ || Object.getPrototypeOf(NoResult)).call(this, props));
  }

  _createClass(NoResult, [{
    key: 'render',
    value: function render() {
      return _react2.default.createElement(
        'div',
        { className: "no-result__content " + this.props.type },
        this.props.imageClass ? _react2.default.createElement('div', { className: this.props.imageClass }) : '',
        _react2.default.createElement(
          'p',
          { className: 'no-result__title' },
          this.props.title
        ),
        this.props.subtitle ? _react2.default.createElement(
          'p',
          { className: 'no-result__subtitle' },
          this.props.subtitle
        ) : '',
        this.props.children
      );
    }
  }]);

  return NoResult;
}(_react2.default.Component);

NoResult.propTypes = propTypes;
NoResult.defaultProps = defaultProps;

exports.default = NoResult;

/***/ }),
/* 81 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var _closestPolyfill = __webpack_require__(38);

var _closestPolyfill2 = _interopRequireDefault(_closestPolyfill);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function Popup(popupContent) {
  var self = this;

  self.content = popupContent;
  self.head = self.content.querySelector('.popup__head');
  self.headHeight = self.head.clientHeight;
  self.searchBoxHeight = self.content.querySelector('.popup__search-box').clientHeight;
  self.heightThreshold = self.headHeight - self.searchBoxHeight;
}

var handlePopupScroll = function handlePopupScroll(popup) {
  if (popup.content.scrollTop > popup.heightThreshold) {
    popup.head.style.position = 'fixed';
    popup.head.style.height = popup.searchBoxHeight + 'px';
  } else {
    resetPopupHead(popup);
  }
};

var resetPopupHead = function resetPopupHead(popup) {
  popup.head.style.position = '';
  popup.head.style.height = '';
};

var handlePopupClose = function handlePopupClose(popupContent, popup) {
  popupContent.scrollTop = 0;
  resetPopupHead(popup);
};

// scroll popup to the clicked collapsible element
var focusCollapsible = function focusCollapsible(popupContent, event) {
  var eventRect = event.currentTarget.getBoundingClientRect();
  var collapsibleTriggerHeight = event.currentTarget.closest('.collapsible').querySelector('.collapsible__trigger').offsetHeight;
  var searchBoxHeight = popupContent.querySelector('.popup__search-box').offsetHeight;
  var autocompleteBoxHeight = popupContent.querySelector('.autocomplete-box').offsetHeight;

  popupContent.scrollTop = eventRect.top + popupContent.scrollTop - (searchBoxHeight + collapsibleTriggerHeight + autocompleteBoxHeight / 1.8);
};

// scroll popup content on autocomplete search
var focusPopupContent = function focusPopupContent(popupContent) {
  var popupHead = popupContent.querySelector('.popup__head');
  var autocompleteBox = popupHead.querySelector('.autocomplete-box');
  var autocompleteBoxHeight = autocompleteBox ? autocompleteBox.offsetHeight : 0;

  if (popupContent.scrollTop > popupHead.offsetHeight) {
    popupContent.scrollTop = popupHead.offsetHeight - autocompleteBoxHeight;
  }
};

var addPopupEvents = function addPopupEvents(popupContent) {
  var popup = new Popup(popupContent);

  resetPopupHead(popup);
  popupContent.addEventListener('scroll', handlePopupScroll.bind(undefined, popup));
  popupContent.querySelector('.popup__close').addEventListener('click', handlePopupClose.bind(undefined, popupContent, popup));
};

var removePopupEvents = function removePopupEvents(popupContent) {
  popupContent.removeEventListener('scroll', handlePopupScroll);
  popupContent.querySelector('.popup__close').removeEventListener('click', handlePopupClose);
};

module.exports = {
  addPopupEvents: addPopupEvents,
  removePopupEvents: removePopupEvents,
  focusCollapsible: focusCollapsible,
  focusPopupContent: focusPopupContent
};

/***/ }),
/* 82 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

var _propTypes = __webpack_require__(1);

var _propTypes2 = _interopRequireDefault(_propTypes);

var _progressBarConstants = __webpack_require__(83);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var propTypes = {
  // count
  stepNumber: _propTypes2.default.number,
  // status
  status: _propTypes2.default.number,
  //to check it is active or not
  isActive: _propTypes2.default.bool
};

var defaultProps = {
  stepNumber: -1,
  status: _progressBarConstants.progressBarStatus.DISABLE,
  isActive: false //here, true is on focus
};

var ProgressBarItem = function (_React$Component) {
  _inherits(ProgressBarItem, _React$Component);

  function ProgressBarItem(props) {
    _classCallCheck(this, ProgressBarItem);

    var _this = _possibleConstructorReturn(this, (ProgressBarItem.__proto__ || Object.getPrototypeOf(ProgressBarItem)).call(this, props));

    _this.getClassName = _this.getClassName.bind(_this);
    return _this;
  }

  _createClass(ProgressBarItem, [{
    key: 'getClassName',
    value: function getClassName(status) {
      switch (status) {
        case _progressBarConstants.progressBarStatus.DISABLE:
          return 'disabled';
        case _progressBarConstants.progressBarStatus.DONE:
          return 'done';
        default:
          return 'disabled';
      }
    }
  }, {
    key: 'render',
    value: function render() {
      var _props = this.props,
          stepNumber = _props.stepNumber,
          status = _props.status,
          isActive = _props.isActive;

      var classStatus = this.getClassName(status);
      var classActive = classActive ? ' focused' : ' ';
      return _react2.default.createElement(
        'div',
        { className: 'progress-bar__item' },
        _react2.default.createElement(
          'div',
          { className: 'progress-bar-item__content ' + classStatus + ' ' + classActive },
          stepNumber != -1 ? _react2.default.createElement(
            'span',
            { className: 'progress-bar-item__step-count' },
            _react2.default.createElement(
              'span',
              { className: 'progress-step-count__label' },
              stepNumber
            )
          ) : '',
          _react2.default.createElement(
            'span',
            { className: 'progress-bar-item__step-title' },
            this.props.children
          )
        )
      );
    }
  }]);

  return ProgressBarItem;
}(_react2.default.Component);

ProgressBarItem.propTypes = propTypes;
ProgressBarItem.defaultProps = defaultProps;

exports.default = ProgressBarItem;

/***/ }),
/* 83 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
var progressBarStatus = exports.progressBarStatus = {
  DISABLE: 1,
  DONE: 2
};

/***/ }),
/* 84 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

var _reactRedux = __webpack_require__(12);

var _redux = __webpack_require__(10);

var _immutableWrapperContainer = __webpack_require__(9);

var _formatMonthsYears = __webpack_require__(209);

var _formatAmount = __webpack_require__(15);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var EMICalculatorHeader = function (_React$Component) {
  _inherits(EMICalculatorHeader, _React$Component);

  function EMICalculatorHeader(props) {
    _classCallCheck(this, EMICalculatorHeader);

    var _this = _possibleConstructorReturn(this, (EMICalculatorHeader.__proto__ || Object.getPrototypeOf(EMICalculatorHeader)).call(this, props));

    _this.state = {
      monthsToYears: (0, _formatMonthsYears.monthsToYears)(_this.props.tenure)
    };
    return _this;
  }

  _createClass(EMICalculatorHeader, [{
    key: 'componentWillReceiveProps',
    value: function componentWillReceiveProps(nextProps) {
      this.setState({
        monthsToYears: (0, _formatMonthsYears.monthsToYears)(nextProps.tenure)
      });
    }
  }, {
    key: 'render',
    value: function render() {
      var _props = this.props,
          emiCalculation = _props.emiCalculation,
          onRoadPrice = _props.onRoadPrice,
          city = _props.city;
      var monthsToYears = this.state.monthsToYears;


      var finalEmi = (0, _formatAmount.formatToINR)(emiCalculation);
      var orpAmount = (0, _formatAmount.formatToINR)(onRoadPrice);

      return _react2.default.createElement(
        'div',
        { className: 'emi-calci-header' },
        _react2.default.createElement(
          'div',
          { className: 'emi-calci-top-header' },
          _react2.default.createElement(
            'div',
            { className: 'emi-calci__orp-text' },
            'On-road price, ',
            city.cityName
          ),
          _react2.default.createElement(
            'div',
            { className: 'emi-calci__orp-data' },
            _react2.default.createElement(
              'span',
              null,
              orpAmount
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { className: 'emi-cost-container' },
          _react2.default.createElement(
            'div',
            { className: 'emi-text' },
            'EMI'
          ),
          _react2.default.createElement(
            'div',
            { className: 'emi-total-cost' },
            _react2.default.createElement(
              'span',
              { className: 'final-emi-text' },
              finalEmi
            ),
            _react2.default.createElement(
              'span',
              { className: 'emi-total-year' },
              'per month for ',
              _react2.default.createElement(
                'span',
                { className: 'emi-total-year-text' },
                monthsToYears
              )
            )
          )
        )
      );
    }
  }]);

  return EMICalculatorHeader;
}(_react2.default.Component);

exports.default = EMICalculatorHeader;

/***/ }),
/* 85 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
	value: true
});
exports.default = {
	getPosition: function getPosition(value, min, max) {
		return (value - min) / (max - min) * 100;
	},
	getValue: function getValue(pos, min, max) {
		var decimal = pos / 100;

		if (pos === 0) {
			return min;
		} else if (pos === 100) {
			return max;
		}

		return Math.round((max - min) * decimal + min);
	}
};

/***/ }),
/* 86 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

var _propTypes = __webpack_require__(1);

var _propTypes2 = _interopRequireDefault(_propTypes);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var propTypes = {
  // a custom class
  className: _propTypes2.default.string,
  // visibility flag
  isActive: _propTypes2.default.bool,
  // tooltip message to display
  message: _propTypes2.default.string,
  // tooltip placement
  placement: _propTypes2.default.string,
  // a custom object to pass style
  style: _propTypes2.default.object,
  // arrow
  arrow: _propTypes2.default.bool,
  // a custom function to call on tooltip open
  onOpen: _propTypes2.default.func,
  // a custom function to call on tooltip close
  onClose: _propTypes2.default.func
};

var defaultProps = {
  className: '',
  isActive: false,
  message: '',
  placement: 'top',
  style: {
    width: 260
  },
  arrow: true,
  onOpen: null,
  onClose: null
};

var Tooltip = function (_React$Component) {
  _inherits(Tooltip, _React$Component);

  function Tooltip(props) {
    _classCallCheck(this, Tooltip);

    var _this = _possibleConstructorReturn(this, (Tooltip.__proto__ || Object.getPrototypeOf(Tooltip)).call(this, props));

    _this.state = {
      isActive: _this.props.isActive
    };

    _this.handleClick = _this.handleClick.bind(_this);
    _this.handleClose = _this.handleClose.bind(_this);
    return _this;
  }

  _createClass(Tooltip, [{
    key: 'handleClick',
    value: function handleClick() {
      var closeButton = document.querySelectorAll('.tooltip-box__close');

      for (var i = 0; i < closeButton.length; i++) {
        closeButton[i].click();
      }

      this.setState({
        isActive: true
      });

      if (this.props.onOpen) {
        this.props.onOpen();
      }
    }
  }, {
    key: 'handleClose',
    value: function handleClose() {
      this.setState({
        isActive: false
      });

      if (this.props.onClose) {
        this.props.onClose();
      }
    }
  }, {
    key: 'render',
    value: function render() {
      var isActive = this.state.isActive;
      var _props = this.props,
          className = _props.className,
          placement = _props.placement,
          style = _props.style,
          arrow = _props.arrow,
          message = _props.message,
          children = _props.children;


      var tooltipPlacement = "tooltip--" + placement;

      return _react2.default.createElement(
        'span',
        { className: 'tooltip__content' },
        _react2.default.createElement(
          'span',
          { onClick: this.handleClick },
          children
        ),
        isActive && _react2.default.createElement(
          'div',
          { className: 'tooltip-box ' + className + ' ' + tooltipPlacement, style: style },
          _react2.default.createElement(
            'div',
            { className: 'tooltip-box__content' },
            _react2.default.createElement('span', { className: 'tooltip-box__close', onClick: this.handleClose }),
            _react2.default.createElement(
              'div',
              { className: 'tooltip-box__message' },
              message
            )
          ),
          arrow && _react2.default.createElement('span', { className: 'tooltip-box__arrow' })
        )
      );
    }
  }]);

  return Tooltip;
}(_react2.default.Component);

Tooltip.propTypes = propTypes;
Tooltip.defaultProps = defaultProps;

exports.default = Tooltip;

/***/ }),
/* 87 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
        value: true
});
exports.default = {
        getPosition: function getPosition(snapPoints, value, min, max) {
                var percentOfEachInterval = 100 / (snapPoints.length - 1);
                // find index of snapPoints having value >= param value
                var index = snapPoints.findIndex(function (x) {
                        return x >= value;
                });

                // handle base cases
                if (index == 0) return 0; // if value <= min
                if (index < 0 || index == snapPoints.length) return 100; // if value > max or value = snapPoints.length

                var valueExceedingLowerSnap = value - snapPoints[index - 1];

                var snapInterval = snapPoints[index] - snapPoints[index - 1];

                var percentWeightInInterval = valueExceedingLowerSnap / snapInterval * 100;

                var pos = (index - 1) * 100 / (snapPoints.length - 1) + percentWeightInInterval * percentOfEachInterval / 100;

                return pos;
        },
        getValue: function getValue(snapPoints, pos, min, max) {
                var percentOfEachInterval = 100 / (snapPoints.length - 1);
                // handle base cases
                if (pos <= 0) {
                        return min;
                } else if (pos >= 100) {
                        return max;
                }

                var lowerSnapIndex = Math.floor(pos / percentOfEachInterval);

                var percentOfScaleExceedingLowerSnap = pos - lowerSnapIndex * percentOfEachInterval;

                var value = snapPoints[lowerSnapIndex] + percentOfScaleExceedingLowerSnap / percentOfEachInterval * (snapPoints[lowerSnapIndex + 1] - snapPoints[lowerSnapIndex]);

                return (value * 100 / 100).toFixed(1);
        }
};

/***/ }),
/* 88 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


//var Components = require('expose-loader?Components!./components')

var ServerRouterWrapper = __webpack_require__(89);

/***/ }),
/* 89 */
/***/ (function(module, exports, __webpack_require__) {

/* WEBPACK VAR INJECTION */(function(global) {module.exports = global["ServerRouterWrapper"] = __webpack_require__(90);
/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(18)))

/***/ }),
/* 90 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

var _reactRouterDom = __webpack_require__(2);

var _ServerAppComponent = __webpack_require__(118);

var _ServerAppComponent2 = _interopRequireDefault(_ServerAppComponent);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var ServerRouterWrapper = function (_React$Component) {
  _inherits(ServerRouterWrapper, _React$Component);

  function ServerRouterWrapper() {
    _classCallCheck(this, ServerRouterWrapper);

    return _possibleConstructorReturn(this, (ServerRouterWrapper.__proto__ || Object.getPrototypeOf(ServerRouterWrapper)).apply(this, arguments));
  }

  _createClass(ServerRouterWrapper, [{
    key: 'render',
    value: function render() {

      var url = this.props.Url;
      return _react2.default.createElement(
        _reactRouterDom.StaticRouter,
        { location: url },
        _react2.default.createElement(_ServerAppComponent2.default, { childComponentProps: this.props })
      );
    }
  }]);

  return ServerRouterWrapper;
}(_react2.default.Component);

module.exports = ServerRouterWrapper;

/***/ }),
/* 91 */
/***/ (function(module, exports) {

// shim for using process in browser
var process = module.exports = {};

// cached from whatever global is present so that test runners that stub it
// don't break things.  But we need to wrap it in a try catch in case it is
// wrapped in strict mode code which doesn't define any globals.  It's inside a
// function because try/catches deoptimize in certain engines.

var cachedSetTimeout;
var cachedClearTimeout;

function defaultSetTimout() {
    throw new Error('setTimeout has not been defined');
}
function defaultClearTimeout () {
    throw new Error('clearTimeout has not been defined');
}
(function () {
    try {
        if (typeof setTimeout === 'function') {
            cachedSetTimeout = setTimeout;
        } else {
            cachedSetTimeout = defaultSetTimout;
        }
    } catch (e) {
        cachedSetTimeout = defaultSetTimout;
    }
    try {
        if (typeof clearTimeout === 'function') {
            cachedClearTimeout = clearTimeout;
        } else {
            cachedClearTimeout = defaultClearTimeout;
        }
    } catch (e) {
        cachedClearTimeout = defaultClearTimeout;
    }
} ())
function runTimeout(fun) {
    if (cachedSetTimeout === setTimeout) {
        //normal enviroments in sane situations
        return setTimeout(fun, 0);
    }
    // if setTimeout wasn't available but was latter defined
    if ((cachedSetTimeout === defaultSetTimout || !cachedSetTimeout) && setTimeout) {
        cachedSetTimeout = setTimeout;
        return setTimeout(fun, 0);
    }
    try {
        // when when somebody has screwed with setTimeout but no I.E. maddness
        return cachedSetTimeout(fun, 0);
    } catch(e){
        try {
            // When we are in I.E. but the script has been evaled so I.E. doesn't trust the global object when called normally
            return cachedSetTimeout.call(null, fun, 0);
        } catch(e){
            // same as above but when it's a version of I.E. that must have the global object for 'this', hopfully our context correct otherwise it will throw a global error
            return cachedSetTimeout.call(this, fun, 0);
        }
    }


}
function runClearTimeout(marker) {
    if (cachedClearTimeout === clearTimeout) {
        //normal enviroments in sane situations
        return clearTimeout(marker);
    }
    // if clearTimeout wasn't available but was latter defined
    if ((cachedClearTimeout === defaultClearTimeout || !cachedClearTimeout) && clearTimeout) {
        cachedClearTimeout = clearTimeout;
        return clearTimeout(marker);
    }
    try {
        // when when somebody has screwed with setTimeout but no I.E. maddness
        return cachedClearTimeout(marker);
    } catch (e){
        try {
            // When we are in I.E. but the script has been evaled so I.E. doesn't  trust the global object when called normally
            return cachedClearTimeout.call(null, marker);
        } catch (e){
            // same as above but when it's a version of I.E. that must have the global object for 'this', hopfully our context correct otherwise it will throw a global error.
            // Some versions of I.E. have different rules for clearTimeout vs setTimeout
            return cachedClearTimeout.call(this, marker);
        }
    }



}
var queue = [];
var draining = false;
var currentQueue;
var queueIndex = -1;

function cleanUpNextTick() {
    if (!draining || !currentQueue) {
        return;
    }
    draining = false;
    if (currentQueue.length) {
        queue = currentQueue.concat(queue);
    } else {
        queueIndex = -1;
    }
    if (queue.length) {
        drainQueue();
    }
}

function drainQueue() {
    if (draining) {
        return;
    }
    var timeout = runTimeout(cleanUpNextTick);
    draining = true;

    var len = queue.length;
    while(len) {
        currentQueue = queue;
        queue = [];
        while (++queueIndex < len) {
            if (currentQueue) {
                currentQueue[queueIndex].run();
            }
        }
        queueIndex = -1;
        len = queue.length;
    }
    currentQueue = null;
    draining = false;
    runClearTimeout(timeout);
}

process.nextTick = function (fun) {
    var args = new Array(arguments.length - 1);
    if (arguments.length > 1) {
        for (var i = 1; i < arguments.length; i++) {
            args[i - 1] = arguments[i];
        }
    }
    queue.push(new Item(fun, args));
    if (queue.length === 1 && !draining) {
        runTimeout(drainQueue);
    }
};

// v8 likes predictible objects
function Item(fun, array) {
    this.fun = fun;
    this.array = array;
}
Item.prototype.run = function () {
    this.fun.apply(null, this.array);
};
process.title = 'browser';
process.browser = true;
process.env = {};
process.argv = [];
process.version = ''; // empty string to avoid regexp issues
process.versions = {};

function noop() {}

process.on = noop;
process.addListener = noop;
process.once = noop;
process.off = noop;
process.removeListener = noop;
process.removeAllListeners = noop;
process.emit = noop;
process.prependListener = noop;
process.prependOnceListener = noop;

process.listeners = function (name) { return [] }

process.binding = function (name) {
    throw new Error('process.binding is not supported');
};

process.cwd = function () { return '/' };
process.chdir = function (dir) {
    throw new Error('process.chdir is not supported');
};
process.umask = function() { return 0; };


/***/ }),
/* 92 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */



var emptyFunction = __webpack_require__(45);
var invariant = __webpack_require__(46);
var warning = __webpack_require__(47);
var assign = __webpack_require__(19);

var ReactPropTypesSecret = __webpack_require__(48);
var checkPropTypes = __webpack_require__(93);

module.exports = function(isValidElement, throwOnDirectAccess) {
  /* global Symbol */
  var ITERATOR_SYMBOL = typeof Symbol === 'function' && Symbol.iterator;
  var FAUX_ITERATOR_SYMBOL = '@@iterator'; // Before Symbol spec.

  /**
   * Returns the iterator method function contained on the iterable object.
   *
   * Be sure to invoke the function with the iterable as context:
   *
   *     var iteratorFn = getIteratorFn(myIterable);
   *     if (iteratorFn) {
   *       var iterator = iteratorFn.call(myIterable);
   *       ...
   *     }
   *
   * @param {?object} maybeIterable
   * @return {?function}
   */
  function getIteratorFn(maybeIterable) {
    var iteratorFn = maybeIterable && (ITERATOR_SYMBOL && maybeIterable[ITERATOR_SYMBOL] || maybeIterable[FAUX_ITERATOR_SYMBOL]);
    if (typeof iteratorFn === 'function') {
      return iteratorFn;
    }
  }

  /**
   * Collection of methods that allow declaration and validation of props that are
   * supplied to React components. Example usage:
   *
   *   var Props = require('ReactPropTypes');
   *   var MyArticle = React.createClass({
   *     propTypes: {
   *       // An optional string prop named "description".
   *       description: Props.string,
   *
   *       // A required enum prop named "category".
   *       category: Props.oneOf(['News','Photos']).isRequired,
   *
   *       // A prop named "dialog" that requires an instance of Dialog.
   *       dialog: Props.instanceOf(Dialog).isRequired
   *     },
   *     render: function() { ... }
   *   });
   *
   * A more formal specification of how these methods are used:
   *
   *   type := array|bool|func|object|number|string|oneOf([...])|instanceOf(...)
   *   decl := ReactPropTypes.{type}(.isRequired)?
   *
   * Each and every declaration produces a function with the same signature. This
   * allows the creation of custom validation functions. For example:
   *
   *  var MyLink = React.createClass({
   *    propTypes: {
   *      // An optional string or URI prop named "href".
   *      href: function(props, propName, componentName) {
   *        var propValue = props[propName];
   *        if (propValue != null && typeof propValue !== 'string' &&
   *            !(propValue instanceof URI)) {
   *          return new Error(
   *            'Expected a string or an URI for ' + propName + ' in ' +
   *            componentName
   *          );
   *        }
   *      }
   *    },
   *    render: function() {...}
   *  });
   *
   * @internal
   */

  var ANONYMOUS = '<<anonymous>>';

  // Important!
  // Keep this list in sync with production version in `./factoryWithThrowingShims.js`.
  var ReactPropTypes = {
    array: createPrimitiveTypeChecker('array'),
    bool: createPrimitiveTypeChecker('boolean'),
    func: createPrimitiveTypeChecker('function'),
    number: createPrimitiveTypeChecker('number'),
    object: createPrimitiveTypeChecker('object'),
    string: createPrimitiveTypeChecker('string'),
    symbol: createPrimitiveTypeChecker('symbol'),

    any: createAnyTypeChecker(),
    arrayOf: createArrayOfTypeChecker,
    element: createElementTypeChecker(),
    instanceOf: createInstanceTypeChecker,
    node: createNodeChecker(),
    objectOf: createObjectOfTypeChecker,
    oneOf: createEnumTypeChecker,
    oneOfType: createUnionTypeChecker,
    shape: createShapeTypeChecker,
    exact: createStrictShapeTypeChecker,
  };

  /**
   * inlined Object.is polyfill to avoid requiring consumers ship their own
   * https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Object/is
   */
  /*eslint-disable no-self-compare*/
  function is(x, y) {
    // SameValue algorithm
    if (x === y) {
      // Steps 1-5, 7-10
      // Steps 6.b-6.e: +0 != -0
      return x !== 0 || 1 / x === 1 / y;
    } else {
      // Step 6.a: NaN == NaN
      return x !== x && y !== y;
    }
  }
  /*eslint-enable no-self-compare*/

  /**
   * We use an Error-like object for backward compatibility as people may call
   * PropTypes directly and inspect their output. However, we don't use real
   * Errors anymore. We don't inspect their stack anyway, and creating them
   * is prohibitively expensive if they are created too often, such as what
   * happens in oneOfType() for any type before the one that matched.
   */
  function PropTypeError(message) {
    this.message = message;
    this.stack = '';
  }
  // Make `instanceof Error` still work for returned errors.
  PropTypeError.prototype = Error.prototype;

  function createChainableTypeChecker(validate) {
    if (true) {
      var manualPropTypeCallCache = {};
      var manualPropTypeWarningCount = 0;
    }
    function checkType(isRequired, props, propName, componentName, location, propFullName, secret) {
      componentName = componentName || ANONYMOUS;
      propFullName = propFullName || propName;

      if (secret !== ReactPropTypesSecret) {
        if (throwOnDirectAccess) {
          // New behavior only for users of `prop-types` package
          invariant(
            false,
            'Calling PropTypes validators directly is not supported by the `prop-types` package. ' +
            'Use `PropTypes.checkPropTypes()` to call them. ' +
            'Read more at http://fb.me/use-check-prop-types'
          );
        } else if ("development" !== 'production' && typeof console !== 'undefined') {
          // Old behavior for people using React.PropTypes
          var cacheKey = componentName + ':' + propName;
          if (
            !manualPropTypeCallCache[cacheKey] &&
            // Avoid spamming the console because they are often not actionable except for lib authors
            manualPropTypeWarningCount < 3
          ) {
            warning(
              false,
              'You are manually calling a React.PropTypes validation ' +
              'function for the `%s` prop on `%s`. This is deprecated ' +
              'and will throw in the standalone `prop-types` package. ' +
              'You may be seeing this warning due to a third-party PropTypes ' +
              'library. See https://fb.me/react-warning-dont-call-proptypes ' + 'for details.',
              propFullName,
              componentName
            );
            manualPropTypeCallCache[cacheKey] = true;
            manualPropTypeWarningCount++;
          }
        }
      }
      if (props[propName] == null) {
        if (isRequired) {
          if (props[propName] === null) {
            return new PropTypeError('The ' + location + ' `' + propFullName + '` is marked as required ' + ('in `' + componentName + '`, but its value is `null`.'));
          }
          return new PropTypeError('The ' + location + ' `' + propFullName + '` is marked as required in ' + ('`' + componentName + '`, but its value is `undefined`.'));
        }
        return null;
      } else {
        return validate(props, propName, componentName, location, propFullName);
      }
    }

    var chainedCheckType = checkType.bind(null, false);
    chainedCheckType.isRequired = checkType.bind(null, true);

    return chainedCheckType;
  }

  function createPrimitiveTypeChecker(expectedType) {
    function validate(props, propName, componentName, location, propFullName, secret) {
      var propValue = props[propName];
      var propType = getPropType(propValue);
      if (propType !== expectedType) {
        // `propValue` being instance of, say, date/regexp, pass the 'object'
        // check, but we can offer a more precise error message here rather than
        // 'of type `object`'.
        var preciseType = getPreciseType(propValue);

        return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` of type ' + ('`' + preciseType + '` supplied to `' + componentName + '`, expected ') + ('`' + expectedType + '`.'));
      }
      return null;
    }
    return createChainableTypeChecker(validate);
  }

  function createAnyTypeChecker() {
    return createChainableTypeChecker(emptyFunction.thatReturnsNull);
  }

  function createArrayOfTypeChecker(typeChecker) {
    function validate(props, propName, componentName, location, propFullName) {
      if (typeof typeChecker !== 'function') {
        return new PropTypeError('Property `' + propFullName + '` of component `' + componentName + '` has invalid PropType notation inside arrayOf.');
      }
      var propValue = props[propName];
      if (!Array.isArray(propValue)) {
        var propType = getPropType(propValue);
        return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` of type ' + ('`' + propType + '` supplied to `' + componentName + '`, expected an array.'));
      }
      for (var i = 0; i < propValue.length; i++) {
        var error = typeChecker(propValue, i, componentName, location, propFullName + '[' + i + ']', ReactPropTypesSecret);
        if (error instanceof Error) {
          return error;
        }
      }
      return null;
    }
    return createChainableTypeChecker(validate);
  }

  function createElementTypeChecker() {
    function validate(props, propName, componentName, location, propFullName) {
      var propValue = props[propName];
      if (!isValidElement(propValue)) {
        var propType = getPropType(propValue);
        return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` of type ' + ('`' + propType + '` supplied to `' + componentName + '`, expected a single ReactElement.'));
      }
      return null;
    }
    return createChainableTypeChecker(validate);
  }

  function createInstanceTypeChecker(expectedClass) {
    function validate(props, propName, componentName, location, propFullName) {
      if (!(props[propName] instanceof expectedClass)) {
        var expectedClassName = expectedClass.name || ANONYMOUS;
        var actualClassName = getClassName(props[propName]);
        return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` of type ' + ('`' + actualClassName + '` supplied to `' + componentName + '`, expected ') + ('instance of `' + expectedClassName + '`.'));
      }
      return null;
    }
    return createChainableTypeChecker(validate);
  }

  function createEnumTypeChecker(expectedValues) {
    if (!Array.isArray(expectedValues)) {
       true ? warning(false, 'Invalid argument supplied to oneOf, expected an instance of array.') : void 0;
      return emptyFunction.thatReturnsNull;
    }

    function validate(props, propName, componentName, location, propFullName) {
      var propValue = props[propName];
      for (var i = 0; i < expectedValues.length; i++) {
        if (is(propValue, expectedValues[i])) {
          return null;
        }
      }

      var valuesString = JSON.stringify(expectedValues);
      return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` of value `' + propValue + '` ' + ('supplied to `' + componentName + '`, expected one of ' + valuesString + '.'));
    }
    return createChainableTypeChecker(validate);
  }

  function createObjectOfTypeChecker(typeChecker) {
    function validate(props, propName, componentName, location, propFullName) {
      if (typeof typeChecker !== 'function') {
        return new PropTypeError('Property `' + propFullName + '` of component `' + componentName + '` has invalid PropType notation inside objectOf.');
      }
      var propValue = props[propName];
      var propType = getPropType(propValue);
      if (propType !== 'object') {
        return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` of type ' + ('`' + propType + '` supplied to `' + componentName + '`, expected an object.'));
      }
      for (var key in propValue) {
        if (propValue.hasOwnProperty(key)) {
          var error = typeChecker(propValue, key, componentName, location, propFullName + '.' + key, ReactPropTypesSecret);
          if (error instanceof Error) {
            return error;
          }
        }
      }
      return null;
    }
    return createChainableTypeChecker(validate);
  }

  function createUnionTypeChecker(arrayOfTypeCheckers) {
    if (!Array.isArray(arrayOfTypeCheckers)) {
       true ? warning(false, 'Invalid argument supplied to oneOfType, expected an instance of array.') : void 0;
      return emptyFunction.thatReturnsNull;
    }

    for (var i = 0; i < arrayOfTypeCheckers.length; i++) {
      var checker = arrayOfTypeCheckers[i];
      if (typeof checker !== 'function') {
        warning(
          false,
          'Invalid argument supplied to oneOfType. Expected an array of check functions, but ' +
          'received %s at index %s.',
          getPostfixForTypeWarning(checker),
          i
        );
        return emptyFunction.thatReturnsNull;
      }
    }

    function validate(props, propName, componentName, location, propFullName) {
      for (var i = 0; i < arrayOfTypeCheckers.length; i++) {
        var checker = arrayOfTypeCheckers[i];
        if (checker(props, propName, componentName, location, propFullName, ReactPropTypesSecret) == null) {
          return null;
        }
      }

      return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` supplied to ' + ('`' + componentName + '`.'));
    }
    return createChainableTypeChecker(validate);
  }

  function createNodeChecker() {
    function validate(props, propName, componentName, location, propFullName) {
      if (!isNode(props[propName])) {
        return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` supplied to ' + ('`' + componentName + '`, expected a ReactNode.'));
      }
      return null;
    }
    return createChainableTypeChecker(validate);
  }

  function createShapeTypeChecker(shapeTypes) {
    function validate(props, propName, componentName, location, propFullName) {
      var propValue = props[propName];
      var propType = getPropType(propValue);
      if (propType !== 'object') {
        return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` of type `' + propType + '` ' + ('supplied to `' + componentName + '`, expected `object`.'));
      }
      for (var key in shapeTypes) {
        var checker = shapeTypes[key];
        if (!checker) {
          continue;
        }
        var error = checker(propValue, key, componentName, location, propFullName + '.' + key, ReactPropTypesSecret);
        if (error) {
          return error;
        }
      }
      return null;
    }
    return createChainableTypeChecker(validate);
  }

  function createStrictShapeTypeChecker(shapeTypes) {
    function validate(props, propName, componentName, location, propFullName) {
      var propValue = props[propName];
      var propType = getPropType(propValue);
      if (propType !== 'object') {
        return new PropTypeError('Invalid ' + location + ' `' + propFullName + '` of type `' + propType + '` ' + ('supplied to `' + componentName + '`, expected `object`.'));
      }
      // We need to check all keys in case some are required but missing from
      // props.
      var allKeys = assign({}, props[propName], shapeTypes);
      for (var key in allKeys) {
        var checker = shapeTypes[key];
        if (!checker) {
          return new PropTypeError(
            'Invalid ' + location + ' `' + propFullName + '` key `' + key + '` supplied to `' + componentName + '`.' +
            '\nBad object: ' + JSON.stringify(props[propName], null, '  ') +
            '\nValid keys: ' +  JSON.stringify(Object.keys(shapeTypes), null, '  ')
          );
        }
        var error = checker(propValue, key, componentName, location, propFullName + '.' + key, ReactPropTypesSecret);
        if (error) {
          return error;
        }
      }
      return null;
    }

    return createChainableTypeChecker(validate);
  }

  function isNode(propValue) {
    switch (typeof propValue) {
      case 'number':
      case 'string':
      case 'undefined':
        return true;
      case 'boolean':
        return !propValue;
      case 'object':
        if (Array.isArray(propValue)) {
          return propValue.every(isNode);
        }
        if (propValue === null || isValidElement(propValue)) {
          return true;
        }

        var iteratorFn = getIteratorFn(propValue);
        if (iteratorFn) {
          var iterator = iteratorFn.call(propValue);
          var step;
          if (iteratorFn !== propValue.entries) {
            while (!(step = iterator.next()).done) {
              if (!isNode(step.value)) {
                return false;
              }
            }
          } else {
            // Iterator will provide entry [k,v] tuples rather than values.
            while (!(step = iterator.next()).done) {
              var entry = step.value;
              if (entry) {
                if (!isNode(entry[1])) {
                  return false;
                }
              }
            }
          }
        } else {
          return false;
        }

        return true;
      default:
        return false;
    }
  }

  function isSymbol(propType, propValue) {
    // Native Symbol.
    if (propType === 'symbol') {
      return true;
    }

    // 19.4.3.5 Symbol.prototype[@@toStringTag] === 'Symbol'
    if (propValue['@@toStringTag'] === 'Symbol') {
      return true;
    }

    // Fallback for non-spec compliant Symbols which are polyfilled.
    if (typeof Symbol === 'function' && propValue instanceof Symbol) {
      return true;
    }

    return false;
  }

  // Equivalent of `typeof` but with special handling for array and regexp.
  function getPropType(propValue) {
    var propType = typeof propValue;
    if (Array.isArray(propValue)) {
      return 'array';
    }
    if (propValue instanceof RegExp) {
      // Old webkits (at least until Android 4.0) return 'function' rather than
      // 'object' for typeof a RegExp. We'll normalize this here so that /bla/
      // passes PropTypes.object.
      return 'object';
    }
    if (isSymbol(propType, propValue)) {
      return 'symbol';
    }
    return propType;
  }

  // This handles more types than `getPropType`. Only used for error messages.
  // See `createPrimitiveTypeChecker`.
  function getPreciseType(propValue) {
    if (typeof propValue === 'undefined' || propValue === null) {
      return '' + propValue;
    }
    var propType = getPropType(propValue);
    if (propType === 'object') {
      if (propValue instanceof Date) {
        return 'date';
      } else if (propValue instanceof RegExp) {
        return 'regexp';
      }
    }
    return propType;
  }

  // Returns a string that is postfixed to a warning about an invalid type.
  // For example, "undefined" or "of type array"
  function getPostfixForTypeWarning(value) {
    var type = getPreciseType(value);
    switch (type) {
      case 'array':
      case 'object':
        return 'an ' + type;
      case 'boolean':
      case 'date':
      case 'regexp':
        return 'a ' + type;
      default:
        return type;
    }
  }

  // Returns class name of the object, if any.
  function getClassName(propValue) {
    if (!propValue.constructor || !propValue.constructor.name) {
      return ANONYMOUS;
    }
    return propValue.constructor.name;
  }

  ReactPropTypes.checkPropTypes = checkPropTypes;
  ReactPropTypes.PropTypes = ReactPropTypes;

  return ReactPropTypes;
};


/***/ }),
/* 93 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */



if (true) {
  var invariant = __webpack_require__(46);
  var warning = __webpack_require__(47);
  var ReactPropTypesSecret = __webpack_require__(48);
  var loggedTypeFailures = {};
}

/**
 * Assert that the values match with the type specs.
 * Error messages are memorized and will only be shown once.
 *
 * @param {object} typeSpecs Map of name to a ReactPropType
 * @param {object} values Runtime values that need to be type-checked
 * @param {string} location e.g. "prop", "context", "child context"
 * @param {string} componentName Name of the component for error messages.
 * @param {?Function} getStack Returns the component stack.
 * @private
 */
function checkPropTypes(typeSpecs, values, location, componentName, getStack) {
  if (true) {
    for (var typeSpecName in typeSpecs) {
      if (typeSpecs.hasOwnProperty(typeSpecName)) {
        var error;
        // Prop type validation may throw. In case they do, we don't want to
        // fail the render phase where it didn't fail before. So we log it.
        // After these have been cleaned up, we'll let them throw.
        try {
          // This is intentionally an invariant that gets caught. It's the same
          // behavior as without this statement except with a better message.
          invariant(typeof typeSpecs[typeSpecName] === 'function', '%s: %s type `%s` is invalid; it must be a function, usually from ' + 'the `prop-types` package, but received `%s`.', componentName || 'React class', location, typeSpecName, typeof typeSpecs[typeSpecName]);
          error = typeSpecs[typeSpecName](values, typeSpecName, componentName, location, null, ReactPropTypesSecret);
        } catch (ex) {
          error = ex;
        }
        warning(!error || error instanceof Error, '%s: type specification of %s `%s` is invalid; the type checker ' + 'function must return `null` or an `Error` but returned a %s. ' + 'You may have forgotten to pass an argument to the type checker ' + 'creator (arrayOf, instanceOf, objectOf, oneOf, oneOfType, and ' + 'shape all require an argument).', componentName || 'React class', location, typeSpecName, typeof error);
        if (error instanceof Error && !(error.message in loggedTypeFailures)) {
          // Only monitor this failure once because there tends to be a lot of the
          // same error.
          loggedTypeFailures[error.message] = true;

          var stack = getStack ? getStack() : '';

          warning(false, 'Failed %s type: %s%s', location, error.message, stack != null ? stack : '');
        }
      }
    }
  }
}

module.exports = checkPropTypes;


/***/ }),
/* 94 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "c", function() { return h; });
/* unused harmony export createElement */
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "b", function() { return cloneElement; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "a", function() { return Component; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "e", function() { return render; });
/* unused harmony export rerender */
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "d", function() { return options; });
/** Virtual DOM Node */
function VNode() {}

/** Global options
 *	@public
 *	@namespace options {Object}
 */
var options = {

	/** If `true`, `prop` changes trigger synchronous component updates.
  *	@name syncComponentUpdates
  *	@type Boolean
  *	@default true
  */
	//syncComponentUpdates: true,

	/** Processes all created VNodes.
  *	@param {VNode} vnode	A newly-created VNode to normalize/process
  */
	//vnode(vnode) { }

	/** Hook invoked after a component is mounted. */
	// afterMount(component) { }

	/** Hook invoked after the DOM is updated with a component's latest render. */
	// afterUpdate(component) { }

	/** Hook invoked immediately before a component is unmounted. */
	// beforeUnmount(component) { }
};

var stack = [];

var EMPTY_CHILDREN = [];

/**
 * JSX/hyperscript reviver.
 * @see http://jasonformat.com/wtf-is-jsx
 * Benchmarks: https://esbench.com/bench/57ee8f8e330ab09900a1a1a0
 *
 * Note: this is exported as both `h()` and `createElement()` for compatibility reasons.
 *
 * Creates a VNode (virtual DOM element). A tree of VNodes can be used as a lightweight representation
 * of the structure of a DOM tree. This structure can be realized by recursively comparing it against
 * the current _actual_ DOM structure, and applying only the differences.
 *
 * `h()`/`createElement()` accepts an element name, a list of attributes/props,
 * and optionally children to append to the element.
 *
 * @example The following DOM tree
 *
 * `<div id="foo" name="bar">Hello!</div>`
 *
 * can be constructed using this function as:
 *
 * `h('div', { id: 'foo', name : 'bar' }, 'Hello!');`
 *
 * @param {string} nodeName	An element name. Ex: `div`, `a`, `span`, etc.
 * @param {Object} attributes	Any attributes/props to set on the created element.
 * @param rest			Additional arguments are taken to be children to append. Can be infinitely nested Arrays.
 *
 * @public
 */
function h(nodeName, attributes) {
	var children = EMPTY_CHILDREN,
	    lastSimple,
	    child,
	    simple,
	    i;
	for (i = arguments.length; i-- > 2;) {
		stack.push(arguments[i]);
	}
	if (attributes && attributes.children != null) {
		if (!stack.length) stack.push(attributes.children);
		delete attributes.children;
	}
	while (stack.length) {
		if ((child = stack.pop()) && child.pop !== undefined) {
			for (i = child.length; i--;) {
				stack.push(child[i]);
			}
		} else {
			if (typeof child === 'boolean') child = null;

			if (simple = typeof nodeName !== 'function') {
				if (child == null) child = '';else if (typeof child === 'number') child = String(child);else if (typeof child !== 'string') simple = false;
			}

			if (simple && lastSimple) {
				children[children.length - 1] += child;
			} else if (children === EMPTY_CHILDREN) {
				children = [child];
			} else {
				children.push(child);
			}

			lastSimple = simple;
		}
	}

	var p = new VNode();
	p.nodeName = nodeName;
	p.children = children;
	p.attributes = attributes == null ? undefined : attributes;
	p.key = attributes == null ? undefined : attributes.key;

	// if a "vnode hook" is defined, pass every created VNode to it
	if (options.vnode !== undefined) options.vnode(p);

	return p;
}

/**
 *  Copy all properties from `props` onto `obj`.
 *  @param {Object} obj		Object onto which properties should be copied.
 *  @param {Object} props	Object from which to copy properties.
 *  @returns obj
 *  @private
 */
function extend(obj, props) {
  for (var i in props) {
    obj[i] = props[i];
  }return obj;
}

/**
 * Call a function asynchronously, as soon as possible. Makes
 * use of HTML Promise to schedule the callback if available,
 * otherwise falling back to `setTimeout` (mainly for IE<11).
 *
 * @param {Function} callback
 */
var defer = typeof Promise == 'function' ? Promise.resolve().then.bind(Promise.resolve()) : setTimeout;

/**
 * Clones the given VNode, optionally adding attributes/props and replacing its children.
 * @param {VNode} vnode		The virtual DOM element to clone
 * @param {Object} props	Attributes/props to add when cloning
 * @param {VNode} rest		Any additional arguments will be used as replacement children.
 */
function cloneElement(vnode, props) {
  return h(vnode.nodeName, extend(extend({}, vnode.attributes), props), arguments.length > 2 ? [].slice.call(arguments, 2) : vnode.children);
}

// DOM properties that should NOT have "px" added when numeric
var IS_NON_DIMENSIONAL = /acit|ex(?:s|g|n|p|$)|rph|ows|mnc|ntw|ine[ch]|zoo|^ord/i;

/** Managed queue of dirty components to be re-rendered */

var items = [];

function enqueueRender(component) {
	if (!component._dirty && (component._dirty = true) && items.push(component) == 1) {
		(options.debounceRendering || defer)(rerender);
	}
}

function rerender() {
	var p,
	    list = items;
	items = [];
	while (p = list.pop()) {
		if (p._dirty) renderComponent(p);
	}
}

/**
 * Check if two nodes are equivalent.
 *
 * @param {Node} node			DOM Node to compare
 * @param {VNode} vnode			Virtual DOM node to compare
 * @param {boolean} [hydrating=false]	If true, ignores component constructors when comparing.
 * @private
 */
function isSameNodeType(node, vnode, hydrating) {
  if (typeof vnode === 'string' || typeof vnode === 'number') {
    return node.splitText !== undefined;
  }
  if (typeof vnode.nodeName === 'string') {
    return !node._componentConstructor && isNamedNode(node, vnode.nodeName);
  }
  return hydrating || node._componentConstructor === vnode.nodeName;
}

/**
 * Check if an Element has a given nodeName, case-insensitively.
 *
 * @param {Element} node	A DOM Element to inspect the name of.
 * @param {String} nodeName	Unnormalized name to compare against.
 */
function isNamedNode(node, nodeName) {
  return node.normalizedNodeName === nodeName || node.nodeName.toLowerCase() === nodeName.toLowerCase();
}

/**
 * Reconstruct Component-style `props` from a VNode.
 * Ensures default/fallback values from `defaultProps`:
 * Own-properties of `defaultProps` not present in `vnode.attributes` are added.
 *
 * @param {VNode} vnode
 * @returns {Object} props
 */
function getNodeProps(vnode) {
  var props = extend({}, vnode.attributes);
  props.children = vnode.children;

  var defaultProps = vnode.nodeName.defaultProps;
  if (defaultProps !== undefined) {
    for (var i in defaultProps) {
      if (props[i] === undefined) {
        props[i] = defaultProps[i];
      }
    }
  }

  return props;
}

/** Create an element with the given nodeName.
 *	@param {String} nodeName
 *	@param {Boolean} [isSvg=false]	If `true`, creates an element within the SVG namespace.
 *	@returns {Element} node
 */
function createNode(nodeName, isSvg) {
	var node = isSvg ? document.createElementNS('http://www.w3.org/2000/svg', nodeName) : document.createElement(nodeName);
	node.normalizedNodeName = nodeName;
	return node;
}

/** Remove a child node from its parent if attached.
 *	@param {Element} node		The node to remove
 */
function removeNode(node) {
	var parentNode = node.parentNode;
	if (parentNode) parentNode.removeChild(node);
}

/** Set a named attribute on the given Node, with special behavior for some names and event handlers.
 *	If `value` is `null`, the attribute/handler will be removed.
 *	@param {Element} node	An element to mutate
 *	@param {string} name	The name/key to set, such as an event or attribute name
 *	@param {any} old	The last value that was set for this name/node pair
 *	@param {any} value	An attribute value, such as a function to be used as an event handler
 *	@param {Boolean} isSvg	Are we currently diffing inside an svg?
 *	@private
 */
function setAccessor(node, name, old, value, isSvg) {
	if (name === 'className') name = 'class';

	if (name === 'key') {
		// ignore
	} else if (name === 'ref') {
		if (old) old(null);
		if (value) value(node);
	} else if (name === 'class' && !isSvg) {
		node.className = value || '';
	} else if (name === 'style') {
		if (!value || typeof value === 'string' || typeof old === 'string') {
			node.style.cssText = value || '';
		}
		if (value && typeof value === 'object') {
			if (typeof old !== 'string') {
				for (var i in old) {
					if (!(i in value)) node.style[i] = '';
				}
			}
			for (var i in value) {
				node.style[i] = typeof value[i] === 'number' && IS_NON_DIMENSIONAL.test(i) === false ? value[i] + 'px' : value[i];
			}
		}
	} else if (name === 'dangerouslySetInnerHTML') {
		if (value) node.innerHTML = value.__html || '';
	} else if (name[0] == 'o' && name[1] == 'n') {
		var useCapture = name !== (name = name.replace(/Capture$/, ''));
		name = name.toLowerCase().substring(2);
		if (value) {
			if (!old) node.addEventListener(name, eventProxy, useCapture);
		} else {
			node.removeEventListener(name, eventProxy, useCapture);
		}
		(node._listeners || (node._listeners = {}))[name] = value;
	} else if (name !== 'list' && name !== 'type' && !isSvg && name in node) {
		setProperty(node, name, value == null ? '' : value);
		if (value == null || value === false) node.removeAttribute(name);
	} else {
		var ns = isSvg && name !== (name = name.replace(/^xlink:?/, ''));
		if (value == null || value === false) {
			if (ns) node.removeAttributeNS('http://www.w3.org/1999/xlink', name.toLowerCase());else node.removeAttribute(name);
		} else if (typeof value !== 'function') {
			if (ns) node.setAttributeNS('http://www.w3.org/1999/xlink', name.toLowerCase(), value);else node.setAttribute(name, value);
		}
	}
}

/** Attempt to set a DOM property to the given value.
 *	IE & FF throw for certain property-value combinations.
 */
function setProperty(node, name, value) {
	try {
		node[name] = value;
	} catch (e) {}
}

/** Proxy an event to hooked event handlers
 *	@private
 */
function eventProxy(e) {
	return this._listeners[e.type](options.event && options.event(e) || e);
}

/** Queue of components that have been mounted and are awaiting componentDidMount */
var mounts = [];

/** Diff recursion count, used to track the end of the diff cycle. */
var diffLevel = 0;

/** Global flag indicating if the diff is currently within an SVG */
var isSvgMode = false;

/** Global flag indicating if the diff is performing hydration */
var hydrating = false;

/** Invoke queued componentDidMount lifecycle methods */
function flushMounts() {
	var c;
	while (c = mounts.pop()) {
		if (options.afterMount) options.afterMount(c);
		if (c.componentDidMount) c.componentDidMount();
	}
}

/** Apply differences in a given vnode (and it's deep children) to a real DOM Node.
 *	@param {Element} [dom=null]		A DOM node to mutate into the shape of the `vnode`
 *	@param {VNode} vnode			A VNode (with descendants forming a tree) representing the desired DOM structure
 *	@returns {Element} dom			The created/mutated element
 *	@private
 */
function diff(dom, vnode, context, mountAll, parent, componentRoot) {
	// diffLevel having been 0 here indicates initial entry into the diff (not a subdiff)
	if (!diffLevel++) {
		// when first starting the diff, check if we're diffing an SVG or within an SVG
		isSvgMode = parent != null && parent.ownerSVGElement !== undefined;

		// hydration is indicated by the existing element to be diffed not having a prop cache
		hydrating = dom != null && !('__preactattr_' in dom);
	}

	var ret = idiff(dom, vnode, context, mountAll, componentRoot);

	// append the element if its a new parent
	if (parent && ret.parentNode !== parent) parent.appendChild(ret);

	// diffLevel being reduced to 0 means we're exiting the diff
	if (! --diffLevel) {
		hydrating = false;
		// invoke queued componentDidMount lifecycle methods
		if (!componentRoot) flushMounts();
	}

	return ret;
}

/** Internals of `diff()`, separated to allow bypassing diffLevel / mount flushing. */
function idiff(dom, vnode, context, mountAll, componentRoot) {
	var out = dom,
	    prevSvgMode = isSvgMode;

	// empty values (null, undefined, booleans) render as empty Text nodes
	if (vnode == null || typeof vnode === 'boolean') vnode = '';

	// Fast case: Strings & Numbers create/update Text nodes.
	if (typeof vnode === 'string' || typeof vnode === 'number') {

		// update if it's already a Text node:
		if (dom && dom.splitText !== undefined && dom.parentNode && (!dom._component || componentRoot)) {
			/* istanbul ignore if */ /* Browser quirk that can't be covered: https://github.com/developit/preact/commit/fd4f21f5c45dfd75151bd27b4c217d8003aa5eb9 */
			if (dom.nodeValue != vnode) {
				dom.nodeValue = vnode;
			}
		} else {
			// it wasn't a Text node: replace it with one and recycle the old Element
			out = document.createTextNode(vnode);
			if (dom) {
				if (dom.parentNode) dom.parentNode.replaceChild(out, dom);
				recollectNodeTree(dom, true);
			}
		}

		out['__preactattr_'] = true;

		return out;
	}

	// If the VNode represents a Component, perform a component diff:
	var vnodeName = vnode.nodeName;
	if (typeof vnodeName === 'function') {
		return buildComponentFromVNode(dom, vnode, context, mountAll);
	}

	// Tracks entering and exiting SVG namespace when descending through the tree.
	isSvgMode = vnodeName === 'svg' ? true : vnodeName === 'foreignObject' ? false : isSvgMode;

	// If there's no existing element or it's the wrong type, create a new one:
	vnodeName = String(vnodeName);
	if (!dom || !isNamedNode(dom, vnodeName)) {
		out = createNode(vnodeName, isSvgMode);

		if (dom) {
			// move children into the replacement node
			while (dom.firstChild) {
				out.appendChild(dom.firstChild);
			} // if the previous Element was mounted into the DOM, replace it inline
			if (dom.parentNode) dom.parentNode.replaceChild(out, dom);

			// recycle the old element (skips non-Element node types)
			recollectNodeTree(dom, true);
		}
	}

	var fc = out.firstChild,
	    props = out['__preactattr_'],
	    vchildren = vnode.children;

	if (props == null) {
		props = out['__preactattr_'] = {};
		for (var a = out.attributes, i = a.length; i--;) {
			props[a[i].name] = a[i].value;
		}
	}

	// Optimization: fast-path for elements containing a single TextNode:
	if (!hydrating && vchildren && vchildren.length === 1 && typeof vchildren[0] === 'string' && fc != null && fc.splitText !== undefined && fc.nextSibling == null) {
		if (fc.nodeValue != vchildren[0]) {
			fc.nodeValue = vchildren[0];
		}
	}
	// otherwise, if there are existing or new children, diff them:
	else if (vchildren && vchildren.length || fc != null) {
			innerDiffNode(out, vchildren, context, mountAll, hydrating || props.dangerouslySetInnerHTML != null);
		}

	// Apply attributes/props from VNode to the DOM Element:
	diffAttributes(out, vnode.attributes, props);

	// restore previous SVG mode: (in case we're exiting an SVG namespace)
	isSvgMode = prevSvgMode;

	return out;
}

/** Apply child and attribute changes between a VNode and a DOM Node to the DOM.
 *	@param {Element} dom			Element whose children should be compared & mutated
 *	@param {Array} vchildren		Array of VNodes to compare to `dom.childNodes`
 *	@param {Object} context			Implicitly descendant context object (from most recent `getChildContext()`)
 *	@param {Boolean} mountAll
 *	@param {Boolean} isHydrating	If `true`, consumes externally created elements similar to hydration
 */
function innerDiffNode(dom, vchildren, context, mountAll, isHydrating) {
	var originalChildren = dom.childNodes,
	    children = [],
	    keyed = {},
	    keyedLen = 0,
	    min = 0,
	    len = originalChildren.length,
	    childrenLen = 0,
	    vlen = vchildren ? vchildren.length : 0,
	    j,
	    c,
	    f,
	    vchild,
	    child;

	// Build up a map of keyed children and an Array of unkeyed children:
	if (len !== 0) {
		for (var i = 0; i < len; i++) {
			var _child = originalChildren[i],
			    props = _child['__preactattr_'],
			    key = vlen && props ? _child._component ? _child._component.__key : props.key : null;
			if (key != null) {
				keyedLen++;
				keyed[key] = _child;
			} else if (props || (_child.splitText !== undefined ? isHydrating ? _child.nodeValue.trim() : true : isHydrating)) {
				children[childrenLen++] = _child;
			}
		}
	}

	if (vlen !== 0) {
		for (var i = 0; i < vlen; i++) {
			vchild = vchildren[i];
			child = null;

			// attempt to find a node based on key matching
			var key = vchild.key;
			if (key != null) {
				if (keyedLen && keyed[key] !== undefined) {
					child = keyed[key];
					keyed[key] = undefined;
					keyedLen--;
				}
			}
			// attempt to pluck a node of the same type from the existing children
			else if (!child && min < childrenLen) {
					for (j = min; j < childrenLen; j++) {
						if (children[j] !== undefined && isSameNodeType(c = children[j], vchild, isHydrating)) {
							child = c;
							children[j] = undefined;
							if (j === childrenLen - 1) childrenLen--;
							if (j === min) min++;
							break;
						}
					}
				}

			// morph the matched/found/created DOM child to match vchild (deep)
			child = idiff(child, vchild, context, mountAll);

			f = originalChildren[i];
			if (child && child !== dom && child !== f) {
				if (f == null) {
					dom.appendChild(child);
				} else if (child === f.nextSibling) {
					removeNode(f);
				} else {
					dom.insertBefore(child, f);
				}
			}
		}
	}

	// remove unused keyed children:
	if (keyedLen) {
		for (var i in keyed) {
			if (keyed[i] !== undefined) recollectNodeTree(keyed[i], false);
		}
	}

	// remove orphaned unkeyed children:
	while (min <= childrenLen) {
		if ((child = children[childrenLen--]) !== undefined) recollectNodeTree(child, false);
	}
}

/** Recursively recycle (or just unmount) a node and its descendants.
 *	@param {Node} node						DOM node to start unmount/removal from
 *	@param {Boolean} [unmountOnly=false]	If `true`, only triggers unmount lifecycle, skips removal
 */
function recollectNodeTree(node, unmountOnly) {
	var component = node._component;
	if (component) {
		// if node is owned by a Component, unmount that component (ends up recursing back here)
		unmountComponent(component);
	} else {
		// If the node's VNode had a ref function, invoke it with null here.
		// (this is part of the React spec, and smart for unsetting references)
		if (node['__preactattr_'] != null && node['__preactattr_'].ref) node['__preactattr_'].ref(null);

		if (unmountOnly === false || node['__preactattr_'] == null) {
			removeNode(node);
		}

		removeChildren(node);
	}
}

/** Recollect/unmount all children.
 *	- we use .lastChild here because it causes less reflow than .firstChild
 *	- it's also cheaper than accessing the .childNodes Live NodeList
 */
function removeChildren(node) {
	node = node.lastChild;
	while (node) {
		var next = node.previousSibling;
		recollectNodeTree(node, true);
		node = next;
	}
}

/** Apply differences in attributes from a VNode to the given DOM Element.
 *	@param {Element} dom		Element with attributes to diff `attrs` against
 *	@param {Object} attrs		The desired end-state key-value attribute pairs
 *	@param {Object} old			Current/previous attributes (from previous VNode or element's prop cache)
 */
function diffAttributes(dom, attrs, old) {
	var name;

	// remove attributes no longer present on the vnode by setting them to undefined
	for (name in old) {
		if (!(attrs && attrs[name] != null) && old[name] != null) {
			setAccessor(dom, name, old[name], old[name] = undefined, isSvgMode);
		}
	}

	// add new & update changed attributes
	for (name in attrs) {
		if (name !== 'children' && name !== 'innerHTML' && (!(name in old) || attrs[name] !== (name === 'value' || name === 'checked' ? dom[name] : old[name]))) {
			setAccessor(dom, name, old[name], old[name] = attrs[name], isSvgMode);
		}
	}
}

/** Retains a pool of Components for re-use, keyed on component name.
 *	Note: since component names are not unique or even necessarily available, these are primarily a form of sharding.
 *	@private
 */
var components = {};

/** Reclaim a component for later re-use by the recycler. */
function collectComponent(component) {
	var name = component.constructor.name;
	(components[name] || (components[name] = [])).push(component);
}

/** Create a component. Normalizes differences between PFC's and classful Components. */
function createComponent(Ctor, props, context) {
	var list = components[Ctor.name],
	    inst;

	if (Ctor.prototype && Ctor.prototype.render) {
		inst = new Ctor(props, context);
		Component.call(inst, props, context);
	} else {
		inst = new Component(props, context);
		inst.constructor = Ctor;
		inst.render = doRender;
	}

	if (list) {
		for (var i = list.length; i--;) {
			if (list[i].constructor === Ctor) {
				inst.nextBase = list[i].nextBase;
				list.splice(i, 1);
				break;
			}
		}
	}
	return inst;
}

/** The `.render()` method for a PFC backing instance. */
function doRender(props, state, context) {
	return this.constructor(props, context);
}

/** Set a component's `props` (generally derived from JSX attributes).
 *	@param {Object} props
 *	@param {Object} [opts]
 *	@param {boolean} [opts.renderSync=false]	If `true` and {@link options.syncComponentUpdates} is `true`, triggers synchronous rendering.
 *	@param {boolean} [opts.render=true]			If `false`, no render will be triggered.
 */
function setComponentProps(component, props, opts, context, mountAll) {
	if (component._disable) return;
	component._disable = true;

	if (component.__ref = props.ref) delete props.ref;
	if (component.__key = props.key) delete props.key;

	if (!component.base || mountAll) {
		if (component.componentWillMount) component.componentWillMount();
	} else if (component.componentWillReceiveProps) {
		component.componentWillReceiveProps(props, context);
	}

	if (context && context !== component.context) {
		if (!component.prevContext) component.prevContext = component.context;
		component.context = context;
	}

	if (!component.prevProps) component.prevProps = component.props;
	component.props = props;

	component._disable = false;

	if (opts !== 0) {
		if (opts === 1 || options.syncComponentUpdates !== false || !component.base) {
			renderComponent(component, 1, mountAll);
		} else {
			enqueueRender(component);
		}
	}

	if (component.__ref) component.__ref(component);
}

/** Render a Component, triggering necessary lifecycle events and taking High-Order Components into account.
 *	@param {Component} component
 *	@param {Object} [opts]
 *	@param {boolean} [opts.build=false]		If `true`, component will build and store a DOM node if not already associated with one.
 *	@private
 */
function renderComponent(component, opts, mountAll, isChild) {
	if (component._disable) return;

	var props = component.props,
	    state = component.state,
	    context = component.context,
	    previousProps = component.prevProps || props,
	    previousState = component.prevState || state,
	    previousContext = component.prevContext || context,
	    isUpdate = component.base,
	    nextBase = component.nextBase,
	    initialBase = isUpdate || nextBase,
	    initialChildComponent = component._component,
	    skip = false,
	    rendered,
	    inst,
	    cbase;

	// if updating
	if (isUpdate) {
		component.props = previousProps;
		component.state = previousState;
		component.context = previousContext;
		if (opts !== 2 && component.shouldComponentUpdate && component.shouldComponentUpdate(props, state, context) === false) {
			skip = true;
		} else if (component.componentWillUpdate) {
			component.componentWillUpdate(props, state, context);
		}
		component.props = props;
		component.state = state;
		component.context = context;
	}

	component.prevProps = component.prevState = component.prevContext = component.nextBase = null;
	component._dirty = false;

	if (!skip) {
		rendered = component.render(props, state, context);

		// context to pass to the child, can be updated via (grand-)parent component
		if (component.getChildContext) {
			context = extend(extend({}, context), component.getChildContext());
		}

		var childComponent = rendered && rendered.nodeName,
		    toUnmount,
		    base;

		if (typeof childComponent === 'function') {
			// set up high order component link

			var childProps = getNodeProps(rendered);
			inst = initialChildComponent;

			if (inst && inst.constructor === childComponent && childProps.key == inst.__key) {
				setComponentProps(inst, childProps, 1, context, false);
			} else {
				toUnmount = inst;

				component._component = inst = createComponent(childComponent, childProps, context);
				inst.nextBase = inst.nextBase || nextBase;
				inst._parentComponent = component;
				setComponentProps(inst, childProps, 0, context, false);
				renderComponent(inst, 1, mountAll, true);
			}

			base = inst.base;
		} else {
			cbase = initialBase;

			// destroy high order component link
			toUnmount = initialChildComponent;
			if (toUnmount) {
				cbase = component._component = null;
			}

			if (initialBase || opts === 1) {
				if (cbase) cbase._component = null;
				base = diff(cbase, rendered, context, mountAll || !isUpdate, initialBase && initialBase.parentNode, true);
			}
		}

		if (initialBase && base !== initialBase && inst !== initialChildComponent) {
			var baseParent = initialBase.parentNode;
			if (baseParent && base !== baseParent) {
				baseParent.replaceChild(base, initialBase);

				if (!toUnmount) {
					initialBase._component = null;
					recollectNodeTree(initialBase, false);
				}
			}
		}

		if (toUnmount) {
			unmountComponent(toUnmount);
		}

		component.base = base;
		if (base && !isChild) {
			var componentRef = component,
			    t = component;
			while (t = t._parentComponent) {
				(componentRef = t).base = base;
			}
			base._component = componentRef;
			base._componentConstructor = componentRef.constructor;
		}
	}

	if (!isUpdate || mountAll) {
		mounts.unshift(component);
	} else if (!skip) {
		// Ensure that pending componentDidMount() hooks of child components
		// are called before the componentDidUpdate() hook in the parent.
		// Note: disabled as it causes duplicate hooks, see https://github.com/developit/preact/issues/750
		// flushMounts();

		if (component.componentDidUpdate) {
			component.componentDidUpdate(previousProps, previousState, previousContext);
		}
		if (options.afterUpdate) options.afterUpdate(component);
	}

	if (component._renderCallbacks != null) {
		while (component._renderCallbacks.length) {
			component._renderCallbacks.pop().call(component);
		}
	}

	if (!diffLevel && !isChild) flushMounts();
}

/** Apply the Component referenced by a VNode to the DOM.
 *	@param {Element} dom	The DOM node to mutate
 *	@param {VNode} vnode	A Component-referencing VNode
 *	@returns {Element} dom	The created/mutated element
 *	@private
 */
function buildComponentFromVNode(dom, vnode, context, mountAll) {
	var c = dom && dom._component,
	    originalComponent = c,
	    oldDom = dom,
	    isDirectOwner = c && dom._componentConstructor === vnode.nodeName,
	    isOwner = isDirectOwner,
	    props = getNodeProps(vnode);
	while (c && !isOwner && (c = c._parentComponent)) {
		isOwner = c.constructor === vnode.nodeName;
	}

	if (c && isOwner && (!mountAll || c._component)) {
		setComponentProps(c, props, 3, context, mountAll);
		dom = c.base;
	} else {
		if (originalComponent && !isDirectOwner) {
			unmountComponent(originalComponent);
			dom = oldDom = null;
		}

		c = createComponent(vnode.nodeName, props, context);
		if (dom && !c.nextBase) {
			c.nextBase = dom;
			// passing dom/oldDom as nextBase will recycle it if unused, so bypass recycling on L229:
			oldDom = null;
		}
		setComponentProps(c, props, 1, context, mountAll);
		dom = c.base;

		if (oldDom && dom !== oldDom) {
			oldDom._component = null;
			recollectNodeTree(oldDom, false);
		}
	}

	return dom;
}

/** Remove a component from the DOM and recycle it.
 *	@param {Component} component	The Component instance to unmount
 *	@private
 */
function unmountComponent(component) {
	if (options.beforeUnmount) options.beforeUnmount(component);

	var base = component.base;

	component._disable = true;

	if (component.componentWillUnmount) component.componentWillUnmount();

	component.base = null;

	// recursively tear down & recollect high-order component children:
	var inner = component._component;
	if (inner) {
		unmountComponent(inner);
	} else if (base) {
		if (base['__preactattr_'] && base['__preactattr_'].ref) base['__preactattr_'].ref(null);

		component.nextBase = base;

		removeNode(base);
		collectComponent(component);

		removeChildren(base);
	}

	if (component.__ref) component.__ref(null);
}

/** Base Component class.
 *	Provides `setState()` and `forceUpdate()`, which trigger rendering.
 *	@public
 *
 *	@example
 *	class MyFoo extends Component {
 *		render(props, state) {
 *			return <div />;
 *		}
 *	}
 */
function Component(props, context) {
	this._dirty = true;

	/** @public
  *	@type {object}
  */
	this.context = context;

	/** @public
  *	@type {object}
  */
	this.props = props;

	/** @public
  *	@type {object}
  */
	this.state = this.state || {};
}

extend(Component.prototype, {

	/** Returns a `boolean` indicating if the component should re-render when receiving the given `props` and `state`.
  *	@param {object} nextProps
  *	@param {object} nextState
  *	@param {object} nextContext
  *	@returns {Boolean} should the component re-render
  *	@name shouldComponentUpdate
  *	@function
  */

	/** Update component state by copying properties from `state` to `this.state`.
  *	@param {object} state		A hash of state properties to update with new values
  *	@param {function} callback	A function to be called once component state is updated
  */
	setState: function setState(state, callback) {
		var s = this.state;
		if (!this.prevState) this.prevState = extend({}, s);
		extend(s, typeof state === 'function' ? state(s, this.props) : state);
		if (callback) (this._renderCallbacks = this._renderCallbacks || []).push(callback);
		enqueueRender(this);
	},


	/** Immediately perform a synchronous re-render of the component.
  *	@param {function} callback		A function to be called after component is re-rendered.
  *	@private
  */
	forceUpdate: function forceUpdate(callback) {
		if (callback) (this._renderCallbacks = this._renderCallbacks || []).push(callback);
		renderComponent(this, 2);
	},


	/** Accepts `props` and `state`, and returns a new Virtual DOM tree to build.
  *	Virtual DOM is generally constructed via [JSX](http://jasonformat.com/wtf-is-jsx).
  *	@param {object} props		Props (eg: JSX attributes) received from parent element/component
  *	@param {object} state		The component's current state
  *	@param {object} context		Context object (if a parent component has provided context)
  *	@returns VNode
  */
	render: function render() {}
});

/** Render JSX into a `parent` Element.
 *	@param {VNode} vnode		A (JSX) VNode to render
 *	@param {Element} parent		DOM element to render into
 *	@param {Element} [merge]	Attempt to re-use an existing DOM tree rooted at `merge`
 *	@public
 *
 *	@example
 *	// render a div into <body>:
 *	render(<div id="hello">hello!</div>, document.body);
 *
 *	@example
 *	// render a "Thing" component into #foo:
 *	const Thing = ({ name }) => <span>{ name }</span>;
 *	render(<Thing name="one" />, document.querySelector('#foo'));
 */
function render(vnode, parent, merge) {
  return diff(merge, vnode, {}, false, parent, false);
}

var preact = {
	h: h,
	createElement: h,
	cloneElement: cloneElement,
	Component: Component,
	render: render,
	rerender: rerender,
	options: options
};

/* unused harmony default export */ var _unused_webpack_default_export = (preact);

//# sourceMappingURL=preact.esm.js.map


/***/ }),
/* 95 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_warning__ = __webpack_require__(13);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_warning___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_0_warning__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_react__ = __webpack_require__(0);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2_prop_types__ = __webpack_require__(1);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2_prop_types___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_2_prop_types__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_3_history__ = __webpack_require__(16);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_4__Router__ = __webpack_require__(33);
function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }







/**
 * The public API for a <Router> that uses HTML5 history.
 */

var BrowserRouter = function (_React$Component) {
  _inherits(BrowserRouter, _React$Component);

  function BrowserRouter() {
    var _temp, _this, _ret;

    _classCallCheck(this, BrowserRouter);

    for (var _len = arguments.length, args = Array(_len), _key = 0; _key < _len; _key++) {
      args[_key] = arguments[_key];
    }

    return _ret = (_temp = (_this = _possibleConstructorReturn(this, _React$Component.call.apply(_React$Component, [this].concat(args))), _this), _this.history = Object(__WEBPACK_IMPORTED_MODULE_3_history__["a" /* createBrowserHistory */])(_this.props), _temp), _possibleConstructorReturn(_this, _ret);
  }

  BrowserRouter.prototype.componentWillMount = function componentWillMount() {
    __WEBPACK_IMPORTED_MODULE_0_warning___default()(!this.props.history, "<BrowserRouter> ignores the history prop. To use a custom history, " + "use `import { Router }` instead of `import { BrowserRouter as Router }`.");
  };

  BrowserRouter.prototype.render = function render() {
    return __WEBPACK_IMPORTED_MODULE_1_react__["default"].createElement(__WEBPACK_IMPORTED_MODULE_4__Router__["a" /* default */], { history: this.history, children: this.props.children });
  };

  return BrowserRouter;
}(__WEBPACK_IMPORTED_MODULE_1_react__["default"].Component);

BrowserRouter.propTypes = {
  basename: __WEBPACK_IMPORTED_MODULE_2_prop_types___default.a.string,
  forceRefresh: __WEBPACK_IMPORTED_MODULE_2_prop_types___default.a.bool,
  getUserConfirmation: __WEBPACK_IMPORTED_MODULE_2_prop_types___default.a.func,
  keyLength: __WEBPACK_IMPORTED_MODULE_2_prop_types___default.a.number,
  children: __WEBPACK_IMPORTED_MODULE_2_prop_types___default.a.node
};


/* harmony default export */ __webpack_exports__["a"] = (BrowserRouter);

/***/ }),
/* 96 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_warning__ = __webpack_require__(26);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_warning___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_0_warning__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_invariant__ = __webpack_require__(7);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_invariant___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_1_invariant__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2__LocationUtils__ = __webpack_require__(27);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_3__PathUtils__ = __webpack_require__(20);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_4__createTransitionManager__ = __webpack_require__(32);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_5__DOMUtils__ = __webpack_require__(49);
var _typeof = typeof Symbol === "function" && typeof Symbol.iterator === "symbol" ? function (obj) { return typeof obj; } : function (obj) { return obj && typeof Symbol === "function" && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj; };

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };








var PopStateEvent = 'popstate';
var HashChangeEvent = 'hashchange';

var getHistoryState = function getHistoryState() {
  try {
    return window.history.state || {};
  } catch (e) {
    // IE 11 sometimes throws when accessing window.history.state
    // See https://github.com/ReactTraining/history/pull/289
    return {};
  }
};

/**
 * Creates a history object that uses the HTML5 history API including
 * pushState, replaceState, and the popstate event.
 */
var createBrowserHistory = function createBrowserHistory() {
  var props = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {};

  __WEBPACK_IMPORTED_MODULE_1_invariant___default()(__WEBPACK_IMPORTED_MODULE_5__DOMUtils__["b" /* canUseDOM */], 'Browser history needs a DOM');

  var globalHistory = window.history;
  var canUseHistory = Object(__WEBPACK_IMPORTED_MODULE_5__DOMUtils__["g" /* supportsHistory */])();
  var needsHashChangeListener = !Object(__WEBPACK_IMPORTED_MODULE_5__DOMUtils__["h" /* supportsPopStateOnHashChange */])();

  var _props$forceRefresh = props.forceRefresh,
      forceRefresh = _props$forceRefresh === undefined ? false : _props$forceRefresh,
      _props$getUserConfirm = props.getUserConfirmation,
      getUserConfirmation = _props$getUserConfirm === undefined ? __WEBPACK_IMPORTED_MODULE_5__DOMUtils__["c" /* getConfirmation */] : _props$getUserConfirm,
      _props$keyLength = props.keyLength,
      keyLength = _props$keyLength === undefined ? 6 : _props$keyLength;

  var basename = props.basename ? Object(__WEBPACK_IMPORTED_MODULE_3__PathUtils__["g" /* stripTrailingSlash */])(Object(__WEBPACK_IMPORTED_MODULE_3__PathUtils__["a" /* addLeadingSlash */])(props.basename)) : '';

  var getDOMLocation = function getDOMLocation(historyState) {
    var _ref = historyState || {},
        key = _ref.key,
        state = _ref.state;

    var _window$location = window.location,
        pathname = _window$location.pathname,
        search = _window$location.search,
        hash = _window$location.hash;


    var path = pathname + search + hash;

    __WEBPACK_IMPORTED_MODULE_0_warning___default()(!basename || Object(__WEBPACK_IMPORTED_MODULE_3__PathUtils__["c" /* hasBasename */])(path, basename), 'You are attempting to use a basename on a page whose URL path does not begin ' + 'with the basename. Expected path "' + path + '" to begin with "' + basename + '".');

    if (basename) path = Object(__WEBPACK_IMPORTED_MODULE_3__PathUtils__["e" /* stripBasename */])(path, basename);

    return Object(__WEBPACK_IMPORTED_MODULE_2__LocationUtils__["a" /* createLocation */])(path, state, key);
  };

  var createKey = function createKey() {
    return Math.random().toString(36).substr(2, keyLength);
  };

  var transitionManager = Object(__WEBPACK_IMPORTED_MODULE_4__createTransitionManager__["a" /* default */])();

  var setState = function setState(nextState) {
    _extends(history, nextState);

    history.length = globalHistory.length;

    transitionManager.notifyListeners(history.location, history.action);
  };

  var handlePopState = function handlePopState(event) {
    // Ignore extraneous popstate events in WebKit.
    if (Object(__WEBPACK_IMPORTED_MODULE_5__DOMUtils__["d" /* isExtraneousPopstateEvent */])(event)) return;

    handlePop(getDOMLocation(event.state));
  };

  var handleHashChange = function handleHashChange() {
    handlePop(getDOMLocation(getHistoryState()));
  };

  var forceNextPop = false;

  var handlePop = function handlePop(location) {
    if (forceNextPop) {
      forceNextPop = false;
      setState();
    } else {
      var action = 'POP';

      transitionManager.confirmTransitionTo(location, action, getUserConfirmation, function (ok) {
        if (ok) {
          setState({ action: action, location: location });
        } else {
          revertPop(location);
        }
      });
    }
  };

  var revertPop = function revertPop(fromLocation) {
    var toLocation = history.location;

    // TODO: We could probably make this more reliable by
    // keeping a list of keys we've seen in sessionStorage.
    // Instead, we just default to 0 for keys we don't know.

    var toIndex = allKeys.indexOf(toLocation.key);

    if (toIndex === -1) toIndex = 0;

    var fromIndex = allKeys.indexOf(fromLocation.key);

    if (fromIndex === -1) fromIndex = 0;

    var delta = toIndex - fromIndex;

    if (delta) {
      forceNextPop = true;
      go(delta);
    }
  };

  var initialLocation = getDOMLocation(getHistoryState());
  var allKeys = [initialLocation.key];

  // Public interface

  var createHref = function createHref(location) {
    return basename + Object(__WEBPACK_IMPORTED_MODULE_3__PathUtils__["b" /* createPath */])(location);
  };

  var push = function push(path, state) {
    __WEBPACK_IMPORTED_MODULE_0_warning___default()(!((typeof path === 'undefined' ? 'undefined' : _typeof(path)) === 'object' && path.state !== undefined && state !== undefined), 'You should avoid providing a 2nd state argument to push when the 1st ' + 'argument is a location-like object that already has state; it is ignored');

    var action = 'PUSH';
    var location = Object(__WEBPACK_IMPORTED_MODULE_2__LocationUtils__["a" /* createLocation */])(path, state, createKey(), history.location);

    transitionManager.confirmTransitionTo(location, action, getUserConfirmation, function (ok) {
      if (!ok) return;

      var href = createHref(location);
      var key = location.key,
          state = location.state;


      if (canUseHistory) {
        globalHistory.pushState({ key: key, state: state }, null, href);

        if (forceRefresh) {
          window.location.href = href;
        } else {
          var prevIndex = allKeys.indexOf(history.location.key);
          var nextKeys = allKeys.slice(0, prevIndex === -1 ? 0 : prevIndex + 1);

          nextKeys.push(location.key);
          allKeys = nextKeys;

          setState({ action: action, location: location });
        }
      } else {
        __WEBPACK_IMPORTED_MODULE_0_warning___default()(state === undefined, 'Browser history cannot push state in browsers that do not support HTML5 history');

        window.location.href = href;
      }
    });
  };

  var replace = function replace(path, state) {
    __WEBPACK_IMPORTED_MODULE_0_warning___default()(!((typeof path === 'undefined' ? 'undefined' : _typeof(path)) === 'object' && path.state !== undefined && state !== undefined), 'You should avoid providing a 2nd state argument to replace when the 1st ' + 'argument is a location-like object that already has state; it is ignored');

    var action = 'REPLACE';
    var location = Object(__WEBPACK_IMPORTED_MODULE_2__LocationUtils__["a" /* createLocation */])(path, state, createKey(), history.location);

    transitionManager.confirmTransitionTo(location, action, getUserConfirmation, function (ok) {
      if (!ok) return;

      var href = createHref(location);
      var key = location.key,
          state = location.state;


      if (canUseHistory) {
        globalHistory.replaceState({ key: key, state: state }, null, href);

        if (forceRefresh) {
          window.location.replace(href);
        } else {
          var prevIndex = allKeys.indexOf(history.location.key);

          if (prevIndex !== -1) allKeys[prevIndex] = location.key;

          setState({ action: action, location: location });
        }
      } else {
        __WEBPACK_IMPORTED_MODULE_0_warning___default()(state === undefined, 'Browser history cannot replace state in browsers that do not support HTML5 history');

        window.location.replace(href);
      }
    });
  };

  var go = function go(n) {
    globalHistory.go(n);
  };

  var goBack = function goBack() {
    return go(-1);
  };

  var goForward = function goForward() {
    return go(1);
  };

  var listenerCount = 0;

  var checkDOMListeners = function checkDOMListeners(delta) {
    listenerCount += delta;

    if (listenerCount === 1) {
      Object(__WEBPACK_IMPORTED_MODULE_5__DOMUtils__["a" /* addEventListener */])(window, PopStateEvent, handlePopState);

      if (needsHashChangeListener) Object(__WEBPACK_IMPORTED_MODULE_5__DOMUtils__["a" /* addEventListener */])(window, HashChangeEvent, handleHashChange);
    } else if (listenerCount === 0) {
      Object(__WEBPACK_IMPORTED_MODULE_5__DOMUtils__["e" /* removeEventListener */])(window, PopStateEvent, handlePopState);

      if (needsHashChangeListener) Object(__WEBPACK_IMPORTED_MODULE_5__DOMUtils__["e" /* removeEventListener */])(window, HashChangeEvent, handleHashChange);
    }
  };

  var isBlocked = false;

  var block = function block() {
    var prompt = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : false;

    var unblock = transitionManager.setPrompt(prompt);

    if (!isBlocked) {
      checkDOMListeners(1);
      isBlocked = true;
    }

    return function () {
      if (isBlocked) {
        isBlocked = false;
        checkDOMListeners(-1);
      }

      return unblock();
    };
  };

  var listen = function listen(listener) {
    var unlisten = transitionManager.appendListener(listener);
    checkDOMListeners(1);

    return function () {
      checkDOMListeners(-1);
      unlisten();
    };
  };

  var history = {
    length: globalHistory.length,
    action: 'POP',
    location: initialLocation,
    createHref: createHref,
    push: push,
    replace: replace,
    go: go,
    goBack: goBack,
    goForward: goForward,
    block: block,
    listen: listen
  };

  return history;
};

/* harmony default export */ __webpack_exports__["a"] = (createBrowserHistory);

/***/ }),
/* 97 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
function isAbsolute(pathname) {
  return pathname.charAt(0) === '/';
}

// About 1.5x faster than the two-arg version of Array#splice()
function spliceOne(list, index) {
  for (var i = index, k = i + 1, n = list.length; k < n; i += 1, k += 1) {
    list[i] = list[k];
  }

  list.pop();
}

// This implementation is based heavily on node's url.parse
function resolvePathname(to) {
  var from = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : '';

  var toParts = to && to.split('/') || [];
  var fromParts = from && from.split('/') || [];

  var isToAbs = to && isAbsolute(to);
  var isFromAbs = from && isAbsolute(from);
  var mustEndAbs = isToAbs || isFromAbs;

  if (to && isAbsolute(to)) {
    // to is absolute
    fromParts = toParts;
  } else if (toParts.length) {
    // to is relative, drop the filename
    fromParts.pop();
    fromParts = fromParts.concat(toParts);
  }

  if (!fromParts.length) return '/';

  var hasTrailingSlash = void 0;
  if (fromParts.length) {
    var last = fromParts[fromParts.length - 1];
    hasTrailingSlash = last === '.' || last === '..' || last === '';
  } else {
    hasTrailingSlash = false;
  }

  var up = 0;
  for (var i = fromParts.length; i >= 0; i--) {
    var part = fromParts[i];

    if (part === '.') {
      spliceOne(fromParts, i);
    } else if (part === '..') {
      spliceOne(fromParts, i);
      up++;
    } else if (up) {
      spliceOne(fromParts, i);
      up--;
    }
  }

  if (!mustEndAbs) for (; up--; up) {
    fromParts.unshift('..');
  }if (mustEndAbs && fromParts[0] !== '' && (!fromParts[0] || !isAbsolute(fromParts[0]))) fromParts.unshift('');

  var result = fromParts.join('/');

  if (hasTrailingSlash && result.substr(-1) !== '/') result += '/';

  return result;
}

/* harmony default export */ __webpack_exports__["a"] = (resolvePathname);

/***/ }),
/* 98 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
var _typeof = typeof Symbol === "function" && typeof Symbol.iterator === "symbol" ? function (obj) { return typeof obj; } : function (obj) { return obj && typeof Symbol === "function" && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj; };

function valueEqual(a, b) {
  if (a === b) return true;

  if (a == null || b == null) return false;

  if (Array.isArray(a)) {
    return Array.isArray(b) && a.length === b.length && a.every(function (item, index) {
      return valueEqual(item, b[index]);
    });
  }

  var aType = typeof a === 'undefined' ? 'undefined' : _typeof(a);
  var bType = typeof b === 'undefined' ? 'undefined' : _typeof(b);

  if (aType !== bType) return false;

  if (aType === 'object') {
    var aValue = a.valueOf();
    var bValue = b.valueOf();

    if (aValue !== a || bValue !== b) return valueEqual(aValue, bValue);

    var aKeys = Object.keys(a);
    var bKeys = Object.keys(b);

    if (aKeys.length !== bKeys.length) return false;

    return aKeys.every(function (key) {
      return valueEqual(a[key], b[key]);
    });
  }

  return false;
}

/* harmony default export */ __webpack_exports__["a"] = (valueEqual);

/***/ }),
/* 99 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_warning__ = __webpack_require__(26);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_warning___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_0_warning__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_invariant__ = __webpack_require__(7);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_invariant___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_1_invariant__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2__LocationUtils__ = __webpack_require__(27);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_3__PathUtils__ = __webpack_require__(20);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_4__createTransitionManager__ = __webpack_require__(32);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_5__DOMUtils__ = __webpack_require__(49);
var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };








var HashChangeEvent = 'hashchange';

var HashPathCoders = {
  hashbang: {
    encodePath: function encodePath(path) {
      return path.charAt(0) === '!' ? path : '!/' + Object(__WEBPACK_IMPORTED_MODULE_3__PathUtils__["f" /* stripLeadingSlash */])(path);
    },
    decodePath: function decodePath(path) {
      return path.charAt(0) === '!' ? path.substr(1) : path;
    }
  },
  noslash: {
    encodePath: __WEBPACK_IMPORTED_MODULE_3__PathUtils__["f" /* stripLeadingSlash */],
    decodePath: __WEBPACK_IMPORTED_MODULE_3__PathUtils__["a" /* addLeadingSlash */]
  },
  slash: {
    encodePath: __WEBPACK_IMPORTED_MODULE_3__PathUtils__["a" /* addLeadingSlash */],
    decodePath: __WEBPACK_IMPORTED_MODULE_3__PathUtils__["a" /* addLeadingSlash */]
  }
};

var getHashPath = function getHashPath() {
  // We can't use window.location.hash here because it's not
  // consistent across browsers - Firefox will pre-decode it!
  var href = window.location.href;
  var hashIndex = href.indexOf('#');
  return hashIndex === -1 ? '' : href.substring(hashIndex + 1);
};

var pushHashPath = function pushHashPath(path) {
  return window.location.hash = path;
};

var replaceHashPath = function replaceHashPath(path) {
  var hashIndex = window.location.href.indexOf('#');

  window.location.replace(window.location.href.slice(0, hashIndex >= 0 ? hashIndex : 0) + '#' + path);
};

var createHashHistory = function createHashHistory() {
  var props = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {};

  __WEBPACK_IMPORTED_MODULE_1_invariant___default()(__WEBPACK_IMPORTED_MODULE_5__DOMUtils__["b" /* canUseDOM */], 'Hash history needs a DOM');

  var globalHistory = window.history;
  var canGoWithoutReload = Object(__WEBPACK_IMPORTED_MODULE_5__DOMUtils__["f" /* supportsGoWithoutReloadUsingHash */])();

  var _props$getUserConfirm = props.getUserConfirmation,
      getUserConfirmation = _props$getUserConfirm === undefined ? __WEBPACK_IMPORTED_MODULE_5__DOMUtils__["c" /* getConfirmation */] : _props$getUserConfirm,
      _props$hashType = props.hashType,
      hashType = _props$hashType === undefined ? 'slash' : _props$hashType;

  var basename = props.basename ? Object(__WEBPACK_IMPORTED_MODULE_3__PathUtils__["g" /* stripTrailingSlash */])(Object(__WEBPACK_IMPORTED_MODULE_3__PathUtils__["a" /* addLeadingSlash */])(props.basename)) : '';

  var _HashPathCoders$hashT = HashPathCoders[hashType],
      encodePath = _HashPathCoders$hashT.encodePath,
      decodePath = _HashPathCoders$hashT.decodePath;


  var getDOMLocation = function getDOMLocation() {
    var path = decodePath(getHashPath());

    __WEBPACK_IMPORTED_MODULE_0_warning___default()(!basename || Object(__WEBPACK_IMPORTED_MODULE_3__PathUtils__["c" /* hasBasename */])(path, basename), 'You are attempting to use a basename on a page whose URL path does not begin ' + 'with the basename. Expected path "' + path + '" to begin with "' + basename + '".');

    if (basename) path = Object(__WEBPACK_IMPORTED_MODULE_3__PathUtils__["e" /* stripBasename */])(path, basename);

    return Object(__WEBPACK_IMPORTED_MODULE_2__LocationUtils__["a" /* createLocation */])(path);
  };

  var transitionManager = Object(__WEBPACK_IMPORTED_MODULE_4__createTransitionManager__["a" /* default */])();

  var setState = function setState(nextState) {
    _extends(history, nextState);

    history.length = globalHistory.length;

    transitionManager.notifyListeners(history.location, history.action);
  };

  var forceNextPop = false;
  var ignorePath = null;

  var handleHashChange = function handleHashChange() {
    var path = getHashPath();
    var encodedPath = encodePath(path);

    if (path !== encodedPath) {
      // Ensure we always have a properly-encoded hash.
      replaceHashPath(encodedPath);
    } else {
      var location = getDOMLocation();
      var prevLocation = history.location;

      if (!forceNextPop && Object(__WEBPACK_IMPORTED_MODULE_2__LocationUtils__["b" /* locationsAreEqual */])(prevLocation, location)) return; // A hashchange doesn't always == location change.

      if (ignorePath === Object(__WEBPACK_IMPORTED_MODULE_3__PathUtils__["b" /* createPath */])(location)) return; // Ignore this change; we already setState in push/replace.

      ignorePath = null;

      handlePop(location);
    }
  };

  var handlePop = function handlePop(location) {
    if (forceNextPop) {
      forceNextPop = false;
      setState();
    } else {
      var action = 'POP';

      transitionManager.confirmTransitionTo(location, action, getUserConfirmation, function (ok) {
        if (ok) {
          setState({ action: action, location: location });
        } else {
          revertPop(location);
        }
      });
    }
  };

  var revertPop = function revertPop(fromLocation) {
    var toLocation = history.location;

    // TODO: We could probably make this more reliable by
    // keeping a list of paths we've seen in sessionStorage.
    // Instead, we just default to 0 for paths we don't know.

    var toIndex = allPaths.lastIndexOf(Object(__WEBPACK_IMPORTED_MODULE_3__PathUtils__["b" /* createPath */])(toLocation));

    if (toIndex === -1) toIndex = 0;

    var fromIndex = allPaths.lastIndexOf(Object(__WEBPACK_IMPORTED_MODULE_3__PathUtils__["b" /* createPath */])(fromLocation));

    if (fromIndex === -1) fromIndex = 0;

    var delta = toIndex - fromIndex;

    if (delta) {
      forceNextPop = true;
      go(delta);
    }
  };

  // Ensure the hash is encoded properly before doing anything else.
  var path = getHashPath();
  var encodedPath = encodePath(path);

  if (path !== encodedPath) replaceHashPath(encodedPath);

  var initialLocation = getDOMLocation();
  var allPaths = [Object(__WEBPACK_IMPORTED_MODULE_3__PathUtils__["b" /* createPath */])(initialLocation)];

  // Public interface

  var createHref = function createHref(location) {
    return '#' + encodePath(basename + Object(__WEBPACK_IMPORTED_MODULE_3__PathUtils__["b" /* createPath */])(location));
  };

  var push = function push(path, state) {
    __WEBPACK_IMPORTED_MODULE_0_warning___default()(state === undefined, 'Hash history cannot push state; it is ignored');

    var action = 'PUSH';
    var location = Object(__WEBPACK_IMPORTED_MODULE_2__LocationUtils__["a" /* createLocation */])(path, undefined, undefined, history.location);

    transitionManager.confirmTransitionTo(location, action, getUserConfirmation, function (ok) {
      if (!ok) return;

      var path = Object(__WEBPACK_IMPORTED_MODULE_3__PathUtils__["b" /* createPath */])(location);
      var encodedPath = encodePath(basename + path);
      var hashChanged = getHashPath() !== encodedPath;

      if (hashChanged) {
        // We cannot tell if a hashchange was caused by a PUSH, so we'd
        // rather setState here and ignore the hashchange. The caveat here
        // is that other hash histories in the page will consider it a POP.
        ignorePath = path;
        pushHashPath(encodedPath);

        var prevIndex = allPaths.lastIndexOf(Object(__WEBPACK_IMPORTED_MODULE_3__PathUtils__["b" /* createPath */])(history.location));
        var nextPaths = allPaths.slice(0, prevIndex === -1 ? 0 : prevIndex + 1);

        nextPaths.push(path);
        allPaths = nextPaths;

        setState({ action: action, location: location });
      } else {
        __WEBPACK_IMPORTED_MODULE_0_warning___default()(false, 'Hash history cannot PUSH the same path; a new entry will not be added to the history stack');

        setState();
      }
    });
  };

  var replace = function replace(path, state) {
    __WEBPACK_IMPORTED_MODULE_0_warning___default()(state === undefined, 'Hash history cannot replace state; it is ignored');

    var action = 'REPLACE';
    var location = Object(__WEBPACK_IMPORTED_MODULE_2__LocationUtils__["a" /* createLocation */])(path, undefined, undefined, history.location);

    transitionManager.confirmTransitionTo(location, action, getUserConfirmation, function (ok) {
      if (!ok) return;

      var path = Object(__WEBPACK_IMPORTED_MODULE_3__PathUtils__["b" /* createPath */])(location);
      var encodedPath = encodePath(basename + path);
      var hashChanged = getHashPath() !== encodedPath;

      if (hashChanged) {
        // We cannot tell if a hashchange was caused by a REPLACE, so we'd
        // rather setState here and ignore the hashchange. The caveat here
        // is that other hash histories in the page will consider it a POP.
        ignorePath = path;
        replaceHashPath(encodedPath);
      }

      var prevIndex = allPaths.indexOf(Object(__WEBPACK_IMPORTED_MODULE_3__PathUtils__["b" /* createPath */])(history.location));

      if (prevIndex !== -1) allPaths[prevIndex] = path;

      setState({ action: action, location: location });
    });
  };

  var go = function go(n) {
    __WEBPACK_IMPORTED_MODULE_0_warning___default()(canGoWithoutReload, 'Hash history go(n) causes a full page reload in this browser');

    globalHistory.go(n);
  };

  var goBack = function goBack() {
    return go(-1);
  };

  var goForward = function goForward() {
    return go(1);
  };

  var listenerCount = 0;

  var checkDOMListeners = function checkDOMListeners(delta) {
    listenerCount += delta;

    if (listenerCount === 1) {
      Object(__WEBPACK_IMPORTED_MODULE_5__DOMUtils__["a" /* addEventListener */])(window, HashChangeEvent, handleHashChange);
    } else if (listenerCount === 0) {
      Object(__WEBPACK_IMPORTED_MODULE_5__DOMUtils__["e" /* removeEventListener */])(window, HashChangeEvent, handleHashChange);
    }
  };

  var isBlocked = false;

  var block = function block() {
    var prompt = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : false;

    var unblock = transitionManager.setPrompt(prompt);

    if (!isBlocked) {
      checkDOMListeners(1);
      isBlocked = true;
    }

    return function () {
      if (isBlocked) {
        isBlocked = false;
        checkDOMListeners(-1);
      }

      return unblock();
    };
  };

  var listen = function listen(listener) {
    var unlisten = transitionManager.appendListener(listener);
    checkDOMListeners(1);

    return function () {
      checkDOMListeners(-1);
      unlisten();
    };
  };

  var history = {
    length: globalHistory.length,
    action: 'POP',
    location: initialLocation,
    createHref: createHref,
    push: push,
    replace: replace,
    go: go,
    goBack: goBack,
    goForward: goForward,
    block: block,
    listen: listen
  };

  return history;
};

/* harmony default export */ __webpack_exports__["a"] = (createHashHistory);

/***/ }),
/* 100 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_warning__ = __webpack_require__(26);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_warning___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_0_warning__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1__PathUtils__ = __webpack_require__(20);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2__LocationUtils__ = __webpack_require__(27);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_3__createTransitionManager__ = __webpack_require__(32);
var _typeof = typeof Symbol === "function" && typeof Symbol.iterator === "symbol" ? function (obj) { return typeof obj; } : function (obj) { return obj && typeof Symbol === "function" && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj; };

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };






var clamp = function clamp(n, lowerBound, upperBound) {
  return Math.min(Math.max(n, lowerBound), upperBound);
};

/**
 * Creates a history object that stores locations in memory.
 */
var createMemoryHistory = function createMemoryHistory() {
  var props = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {};
  var getUserConfirmation = props.getUserConfirmation,
      _props$initialEntries = props.initialEntries,
      initialEntries = _props$initialEntries === undefined ? ['/'] : _props$initialEntries,
      _props$initialIndex = props.initialIndex,
      initialIndex = _props$initialIndex === undefined ? 0 : _props$initialIndex,
      _props$keyLength = props.keyLength,
      keyLength = _props$keyLength === undefined ? 6 : _props$keyLength;


  var transitionManager = Object(__WEBPACK_IMPORTED_MODULE_3__createTransitionManager__["a" /* default */])();

  var setState = function setState(nextState) {
    _extends(history, nextState);

    history.length = history.entries.length;

    transitionManager.notifyListeners(history.location, history.action);
  };

  var createKey = function createKey() {
    return Math.random().toString(36).substr(2, keyLength);
  };

  var index = clamp(initialIndex, 0, initialEntries.length - 1);
  var entries = initialEntries.map(function (entry) {
    return typeof entry === 'string' ? Object(__WEBPACK_IMPORTED_MODULE_2__LocationUtils__["a" /* createLocation */])(entry, undefined, createKey()) : Object(__WEBPACK_IMPORTED_MODULE_2__LocationUtils__["a" /* createLocation */])(entry, undefined, entry.key || createKey());
  });

  // Public interface

  var createHref = __WEBPACK_IMPORTED_MODULE_1__PathUtils__["b" /* createPath */];

  var push = function push(path, state) {
    __WEBPACK_IMPORTED_MODULE_0_warning___default()(!((typeof path === 'undefined' ? 'undefined' : _typeof(path)) === 'object' && path.state !== undefined && state !== undefined), 'You should avoid providing a 2nd state argument to push when the 1st ' + 'argument is a location-like object that already has state; it is ignored');

    var action = 'PUSH';
    var location = Object(__WEBPACK_IMPORTED_MODULE_2__LocationUtils__["a" /* createLocation */])(path, state, createKey(), history.location);

    transitionManager.confirmTransitionTo(location, action, getUserConfirmation, function (ok) {
      if (!ok) return;

      var prevIndex = history.index;
      var nextIndex = prevIndex + 1;

      var nextEntries = history.entries.slice(0);
      if (nextEntries.length > nextIndex) {
        nextEntries.splice(nextIndex, nextEntries.length - nextIndex, location);
      } else {
        nextEntries.push(location);
      }

      setState({
        action: action,
        location: location,
        index: nextIndex,
        entries: nextEntries
      });
    });
  };

  var replace = function replace(path, state) {
    __WEBPACK_IMPORTED_MODULE_0_warning___default()(!((typeof path === 'undefined' ? 'undefined' : _typeof(path)) === 'object' && path.state !== undefined && state !== undefined), 'You should avoid providing a 2nd state argument to replace when the 1st ' + 'argument is a location-like object that already has state; it is ignored');

    var action = 'REPLACE';
    var location = Object(__WEBPACK_IMPORTED_MODULE_2__LocationUtils__["a" /* createLocation */])(path, state, createKey(), history.location);

    transitionManager.confirmTransitionTo(location, action, getUserConfirmation, function (ok) {
      if (!ok) return;

      history.entries[history.index] = location;

      setState({ action: action, location: location });
    });
  };

  var go = function go(n) {
    var nextIndex = clamp(history.index + n, 0, history.entries.length - 1);

    var action = 'POP';
    var location = history.entries[nextIndex];

    transitionManager.confirmTransitionTo(location, action, getUserConfirmation, function (ok) {
      if (ok) {
        setState({
          action: action,
          location: location,
          index: nextIndex
        });
      } else {
        // Mimic the behavior of DOM histories by
        // causing a render after a cancelled POP.
        setState();
      }
    });
  };

  var goBack = function goBack() {
    return go(-1);
  };

  var goForward = function goForward() {
    return go(1);
  };

  var canGo = function canGo(n) {
    var nextIndex = history.index + n;
    return nextIndex >= 0 && nextIndex < history.entries.length;
  };

  var block = function block() {
    var prompt = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : false;
    return transitionManager.setPrompt(prompt);
  };

  var listen = function listen(listener) {
    return transitionManager.appendListener(listener);
  };

  var history = {
    length: entries.length,
    action: 'POP',
    location: entries[index],
    index: index,
    entries: entries,
    createHref: createHref,
    push: push,
    replace: replace,
    go: go,
    goBack: goBack,
    goForward: goForward,
    canGo: canGo,
    block: block,
    listen: listen
  };

  return history;
};

/* harmony default export */ __webpack_exports__["a"] = (createMemoryHistory);

/***/ }),
/* 101 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_warning__ = __webpack_require__(13);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_warning___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_0_warning__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_react__ = __webpack_require__(0);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2_prop_types__ = __webpack_require__(1);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2_prop_types___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_2_prop_types__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_3_history__ = __webpack_require__(16);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_4__Router__ = __webpack_require__(33);
function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }







/**
 * The public API for a <Router> that uses window.location.hash.
 */

var HashRouter = function (_React$Component) {
  _inherits(HashRouter, _React$Component);

  function HashRouter() {
    var _temp, _this, _ret;

    _classCallCheck(this, HashRouter);

    for (var _len = arguments.length, args = Array(_len), _key = 0; _key < _len; _key++) {
      args[_key] = arguments[_key];
    }

    return _ret = (_temp = (_this = _possibleConstructorReturn(this, _React$Component.call.apply(_React$Component, [this].concat(args))), _this), _this.history = Object(__WEBPACK_IMPORTED_MODULE_3_history__["b" /* createHashHistory */])(_this.props), _temp), _possibleConstructorReturn(_this, _ret);
  }

  HashRouter.prototype.componentWillMount = function componentWillMount() {
    __WEBPACK_IMPORTED_MODULE_0_warning___default()(!this.props.history, "<HashRouter> ignores the history prop. To use a custom history, " + "use `import { Router }` instead of `import { HashRouter as Router }`.");
  };

  HashRouter.prototype.render = function render() {
    return __WEBPACK_IMPORTED_MODULE_1_react__["default"].createElement(__WEBPACK_IMPORTED_MODULE_4__Router__["a" /* default */], { history: this.history, children: this.props.children });
  };

  return HashRouter;
}(__WEBPACK_IMPORTED_MODULE_1_react__["default"].Component);

HashRouter.propTypes = {
  basename: __WEBPACK_IMPORTED_MODULE_2_prop_types___default.a.string,
  getUserConfirmation: __WEBPACK_IMPORTED_MODULE_2_prop_types___default.a.func,
  hashType: __WEBPACK_IMPORTED_MODULE_2_prop_types___default.a.oneOf(["hashbang", "noslash", "slash"]),
  children: __WEBPACK_IMPORTED_MODULE_2_prop_types___default.a.node
};


/* harmony default export */ __webpack_exports__["a"] = (HashRouter);

/***/ }),
/* 102 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_react_router_es_MemoryRouter__ = __webpack_require__(103);
// Written in this round about way for babel-transform-imports


/* harmony default export */ __webpack_exports__["a"] = (__WEBPACK_IMPORTED_MODULE_0_react_router_es_MemoryRouter__["a" /* default */]);

/***/ }),
/* 103 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_warning__ = __webpack_require__(13);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_warning___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_0_warning__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_react__ = __webpack_require__(0);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2_prop_types__ = __webpack_require__(1);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2_prop_types___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_2_prop_types__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_3_history__ = __webpack_require__(16);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_4__Router__ = __webpack_require__(34);
function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }







/**
 * The public API for a <Router> that stores location in memory.
 */

var MemoryRouter = function (_React$Component) {
  _inherits(MemoryRouter, _React$Component);

  function MemoryRouter() {
    var _temp, _this, _ret;

    _classCallCheck(this, MemoryRouter);

    for (var _len = arguments.length, args = Array(_len), _key = 0; _key < _len; _key++) {
      args[_key] = arguments[_key];
    }

    return _ret = (_temp = (_this = _possibleConstructorReturn(this, _React$Component.call.apply(_React$Component, [this].concat(args))), _this), _this.history = Object(__WEBPACK_IMPORTED_MODULE_3_history__["d" /* createMemoryHistory */])(_this.props), _temp), _possibleConstructorReturn(_this, _ret);
  }

  MemoryRouter.prototype.componentWillMount = function componentWillMount() {
    __WEBPACK_IMPORTED_MODULE_0_warning___default()(!this.props.history, "<MemoryRouter> ignores the history prop. To use a custom history, " + "use `import { Router }` instead of `import { MemoryRouter as Router }`.");
  };

  MemoryRouter.prototype.render = function render() {
    return __WEBPACK_IMPORTED_MODULE_1_react__["default"].createElement(__WEBPACK_IMPORTED_MODULE_4__Router__["a" /* default */], { history: this.history, children: this.props.children });
  };

  return MemoryRouter;
}(__WEBPACK_IMPORTED_MODULE_1_react__["default"].Component);

MemoryRouter.propTypes = {
  initialEntries: __WEBPACK_IMPORTED_MODULE_2_prop_types___default.a.array,
  initialIndex: __WEBPACK_IMPORTED_MODULE_2_prop_types___default.a.number,
  getUserConfirmation: __WEBPACK_IMPORTED_MODULE_2_prop_types___default.a.func,
  keyLength: __WEBPACK_IMPORTED_MODULE_2_prop_types___default.a.number,
  children: __WEBPACK_IMPORTED_MODULE_2_prop_types___default.a.node
};


/* harmony default export */ __webpack_exports__["a"] = (MemoryRouter);

/***/ }),
/* 104 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_react__ = __webpack_require__(0);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_prop_types__ = __webpack_require__(1);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_prop_types___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_1_prop_types__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2__Route__ = __webpack_require__(51);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_3__Link__ = __webpack_require__(50);
var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var _typeof = typeof Symbol === "function" && typeof Symbol.iterator === "symbol" ? function (obj) { return typeof obj; } : function (obj) { return obj && typeof Symbol === "function" && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj; };

function _objectWithoutProperties(obj, keys) { var target = {}; for (var i in obj) { if (keys.indexOf(i) >= 0) continue; if (!Object.prototype.hasOwnProperty.call(obj, i)) continue; target[i] = obj[i]; } return target; }






/**
 * A <Link> wrapper that knows if it's "active" or not.
 */
var NavLink = function NavLink(_ref) {
  var to = _ref.to,
      exact = _ref.exact,
      strict = _ref.strict,
      location = _ref.location,
      activeClassName = _ref.activeClassName,
      className = _ref.className,
      activeStyle = _ref.activeStyle,
      style = _ref.style,
      getIsActive = _ref.isActive,
      ariaCurrent = _ref["aria-current"],
      rest = _objectWithoutProperties(_ref, ["to", "exact", "strict", "location", "activeClassName", "className", "activeStyle", "style", "isActive", "aria-current"]);

  var path = (typeof to === "undefined" ? "undefined" : _typeof(to)) === "object" ? to.pathname : to;

  // Regex taken from: https://github.com/pillarjs/path-to-regexp/blob/master/index.js#L202
  var escapedPath = path && path.replace(/([.+*?=^!:${}()[\]|/\\])/g, "\\$1");

  return __WEBPACK_IMPORTED_MODULE_0_react__["default"].createElement(__WEBPACK_IMPORTED_MODULE_2__Route__["a" /* default */], {
    path: escapedPath,
    exact: exact,
    strict: strict,
    location: location,
    children: function children(_ref2) {
      var location = _ref2.location,
          match = _ref2.match;

      var isActive = !!(getIsActive ? getIsActive(match, location) : match);

      return __WEBPACK_IMPORTED_MODULE_0_react__["default"].createElement(__WEBPACK_IMPORTED_MODULE_3__Link__["a" /* default */], _extends({
        to: to,
        className: isActive ? [className, activeClassName].filter(function (i) {
          return i;
        }).join(" ") : className,
        style: isActive ? _extends({}, style, activeStyle) : style,
        "aria-current": isActive && ariaCurrent || null
      }, rest));
    }
  });
};

NavLink.propTypes = {
  to: __WEBPACK_IMPORTED_MODULE_3__Link__["a" /* default */].propTypes.to,
  exact: __WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.bool,
  strict: __WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.bool,
  location: __WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.object,
  activeClassName: __WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.string,
  className: __WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.string,
  activeStyle: __WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.object,
  style: __WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.object,
  isActive: __WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.func,
  "aria-current": __WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.oneOf(["page", "step", "location", "date", "time", "true"])
};

NavLink.defaultProps = {
  activeClassName: "active",
  "aria-current": "page"
};

/* harmony default export */ __webpack_exports__["a"] = (NavLink);

/***/ }),
/* 105 */
/***/ (function(module, exports) {

module.exports = Array.isArray || function (arr) {
  return Object.prototype.toString.call(arr) == '[object Array]';
};


/***/ }),
/* 106 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_react_router_es_Prompt__ = __webpack_require__(107);
// Written in this round about way for babel-transform-imports


/* harmony default export */ __webpack_exports__["a"] = (__WEBPACK_IMPORTED_MODULE_0_react_router_es_Prompt__["a" /* default */]);

/***/ }),
/* 107 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_react__ = __webpack_require__(0);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_prop_types__ = __webpack_require__(1);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_prop_types___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_1_prop_types__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2_invariant__ = __webpack_require__(7);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2_invariant___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_2_invariant__);
function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }





/**
 * The public API for prompting the user before navigating away
 * from a screen with a component.
 */

var Prompt = function (_React$Component) {
  _inherits(Prompt, _React$Component);

  function Prompt() {
    _classCallCheck(this, Prompt);

    return _possibleConstructorReturn(this, _React$Component.apply(this, arguments));
  }

  Prompt.prototype.enable = function enable(message) {
    if (this.unblock) this.unblock();

    this.unblock = this.context.router.history.block(message);
  };

  Prompt.prototype.disable = function disable() {
    if (this.unblock) {
      this.unblock();
      this.unblock = null;
    }
  };

  Prompt.prototype.componentWillMount = function componentWillMount() {
    __WEBPACK_IMPORTED_MODULE_2_invariant___default()(this.context.router, "You should not use <Prompt> outside a <Router>");

    if (this.props.when) this.enable(this.props.message);
  };

  Prompt.prototype.componentWillReceiveProps = function componentWillReceiveProps(nextProps) {
    if (nextProps.when) {
      if (!this.props.when || this.props.message !== nextProps.message) this.enable(nextProps.message);
    } else {
      this.disable();
    }
  };

  Prompt.prototype.componentWillUnmount = function componentWillUnmount() {
    this.disable();
  };

  Prompt.prototype.render = function render() {
    return null;
  };

  return Prompt;
}(__WEBPACK_IMPORTED_MODULE_0_react__["default"].Component);

Prompt.propTypes = {
  when: __WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.bool,
  message: __WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.oneOfType([__WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.func, __WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.string]).isRequired
};
Prompt.defaultProps = {
  when: true
};
Prompt.contextTypes = {
  router: __WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.shape({
    history: __WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.shape({
      block: __WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.func.isRequired
    }).isRequired
  }).isRequired
};


/* harmony default export */ __webpack_exports__["a"] = (Prompt);

/***/ }),
/* 108 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_react_router_es_Redirect__ = __webpack_require__(109);
// Written in this round about way for babel-transform-imports


/* harmony default export */ __webpack_exports__["a"] = (__WEBPACK_IMPORTED_MODULE_0_react_router_es_Redirect__["a" /* default */]);

/***/ }),
/* 109 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_react__ = __webpack_require__(0);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_prop_types__ = __webpack_require__(1);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_prop_types___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_1_prop_types__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2_warning__ = __webpack_require__(13);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2_warning___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_2_warning__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_3_invariant__ = __webpack_require__(7);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_3_invariant___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_3_invariant__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_4_history__ = __webpack_require__(16);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_5__generatePath__ = __webpack_require__(54);
var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }








/**
 * The public API for updating the location programmatically
 * with a component.
 */

var Redirect = function (_React$Component) {
  _inherits(Redirect, _React$Component);

  function Redirect() {
    _classCallCheck(this, Redirect);

    return _possibleConstructorReturn(this, _React$Component.apply(this, arguments));
  }

  Redirect.prototype.isStatic = function isStatic() {
    return this.context.router && this.context.router.staticContext;
  };

  Redirect.prototype.componentWillMount = function componentWillMount() {
    __WEBPACK_IMPORTED_MODULE_3_invariant___default()(this.context.router, "You should not use <Redirect> outside a <Router>");

    if (this.isStatic()) this.perform();
  };

  Redirect.prototype.componentDidMount = function componentDidMount() {
    if (!this.isStatic()) this.perform();
  };

  Redirect.prototype.componentDidUpdate = function componentDidUpdate(prevProps) {
    var prevTo = Object(__WEBPACK_IMPORTED_MODULE_4_history__["c" /* createLocation */])(prevProps.to);
    var nextTo = Object(__WEBPACK_IMPORTED_MODULE_4_history__["c" /* createLocation */])(this.props.to);

    if (Object(__WEBPACK_IMPORTED_MODULE_4_history__["f" /* locationsAreEqual */])(prevTo, nextTo)) {
      __WEBPACK_IMPORTED_MODULE_2_warning___default()(false, "You tried to redirect to the same route you're currently on: " + ("\"" + nextTo.pathname + nextTo.search + "\""));
      return;
    }

    this.perform();
  };

  Redirect.prototype.computeTo = function computeTo(_ref) {
    var computedMatch = _ref.computedMatch,
        to = _ref.to;

    if (computedMatch) {
      if (typeof to === "string") {
        return Object(__WEBPACK_IMPORTED_MODULE_5__generatePath__["a" /* default */])(to, computedMatch.params);
      } else {
        return _extends({}, to, {
          pathname: Object(__WEBPACK_IMPORTED_MODULE_5__generatePath__["a" /* default */])(to.pathname, computedMatch.params)
        });
      }
    }

    return to;
  };

  Redirect.prototype.perform = function perform() {
    var history = this.context.router.history;
    var push = this.props.push;

    var to = this.computeTo(this.props);

    if (push) {
      history.push(to);
    } else {
      history.replace(to);
    }
  };

  Redirect.prototype.render = function render() {
    return null;
  };

  return Redirect;
}(__WEBPACK_IMPORTED_MODULE_0_react__["default"].Component);

Redirect.propTypes = {
  computedMatch: __WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.object, // private, from <Switch>
  push: __WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.bool,
  from: __WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.string,
  to: __WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.oneOfType([__WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.string, __WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.object]).isRequired
};
Redirect.defaultProps = {
  push: false
};
Redirect.contextTypes = {
  router: __WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.shape({
    history: __WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.shape({
      push: __WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.func.isRequired,
      replace: __WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.func.isRequired
    }).isRequired,
    staticContext: __WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.object
  }).isRequired
};


/* harmony default export */ __webpack_exports__["a"] = (Redirect);

/***/ }),
/* 110 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_react_router_es_StaticRouter__ = __webpack_require__(111);
// Written in this round about way for babel-transform-imports


/* harmony default export */ __webpack_exports__["a"] = (__WEBPACK_IMPORTED_MODULE_0_react_router_es_StaticRouter__["a" /* default */]);

/***/ }),
/* 111 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_warning__ = __webpack_require__(13);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_warning___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_0_warning__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_invariant__ = __webpack_require__(7);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_invariant___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_1_invariant__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2_react__ = __webpack_require__(0);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_3_prop_types__ = __webpack_require__(1);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_3_prop_types___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_3_prop_types__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_4_history__ = __webpack_require__(16);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_5__Router__ = __webpack_require__(34);
var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

function _objectWithoutProperties(obj, keys) { var target = {}; for (var i in obj) { if (keys.indexOf(i) >= 0) continue; if (!Object.prototype.hasOwnProperty.call(obj, i)) continue; target[i] = obj[i]; } return target; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }








var addLeadingSlash = function addLeadingSlash(path) {
  return path.charAt(0) === "/" ? path : "/" + path;
};

var addBasename = function addBasename(basename, location) {
  if (!basename) return location;

  return _extends({}, location, {
    pathname: addLeadingSlash(basename) + location.pathname
  });
};

var stripBasename = function stripBasename(basename, location) {
  if (!basename) return location;

  var base = addLeadingSlash(basename);

  if (location.pathname.indexOf(base) !== 0) return location;

  return _extends({}, location, {
    pathname: location.pathname.substr(base.length)
  });
};

var createURL = function createURL(location) {
  return typeof location === "string" ? location : Object(__WEBPACK_IMPORTED_MODULE_4_history__["e" /* createPath */])(location);
};

var staticHandler = function staticHandler(methodName) {
  return function () {
    __WEBPACK_IMPORTED_MODULE_1_invariant___default()(false, "You cannot %s with <StaticRouter>", methodName);
  };
};

var noop = function noop() {};

/**
 * The public top-level API for a "static" <Router>, so-called because it
 * can't actually change the current location. Instead, it just records
 * location changes in a context object. Useful mainly in testing and
 * server-rendering scenarios.
 */

var StaticRouter = function (_React$Component) {
  _inherits(StaticRouter, _React$Component);

  function StaticRouter() {
    var _temp, _this, _ret;

    _classCallCheck(this, StaticRouter);

    for (var _len = arguments.length, args = Array(_len), _key = 0; _key < _len; _key++) {
      args[_key] = arguments[_key];
    }

    return _ret = (_temp = (_this = _possibleConstructorReturn(this, _React$Component.call.apply(_React$Component, [this].concat(args))), _this), _this.createHref = function (path) {
      return addLeadingSlash(_this.props.basename + createURL(path));
    }, _this.handlePush = function (location) {
      var _this$props = _this.props,
          basename = _this$props.basename,
          context = _this$props.context;

      context.action = "PUSH";
      context.location = addBasename(basename, Object(__WEBPACK_IMPORTED_MODULE_4_history__["c" /* createLocation */])(location));
      context.url = createURL(context.location);
    }, _this.handleReplace = function (location) {
      var _this$props2 = _this.props,
          basename = _this$props2.basename,
          context = _this$props2.context;

      context.action = "REPLACE";
      context.location = addBasename(basename, Object(__WEBPACK_IMPORTED_MODULE_4_history__["c" /* createLocation */])(location));
      context.url = createURL(context.location);
    }, _this.handleListen = function () {
      return noop;
    }, _this.handleBlock = function () {
      return noop;
    }, _temp), _possibleConstructorReturn(_this, _ret);
  }

  StaticRouter.prototype.getChildContext = function getChildContext() {
    return {
      router: {
        staticContext: this.props.context
      }
    };
  };

  StaticRouter.prototype.componentWillMount = function componentWillMount() {
    __WEBPACK_IMPORTED_MODULE_0_warning___default()(!this.props.history, "<StaticRouter> ignores the history prop. To use a custom history, " + "use `import { Router }` instead of `import { StaticRouter as Router }`.");
  };

  StaticRouter.prototype.render = function render() {
    var _props = this.props,
        basename = _props.basename,
        context = _props.context,
        location = _props.location,
        props = _objectWithoutProperties(_props, ["basename", "context", "location"]);

    var history = {
      createHref: this.createHref,
      action: "POP",
      location: stripBasename(basename, Object(__WEBPACK_IMPORTED_MODULE_4_history__["c" /* createLocation */])(location)),
      push: this.handlePush,
      replace: this.handleReplace,
      go: staticHandler("go"),
      goBack: staticHandler("goBack"),
      goForward: staticHandler("goForward"),
      listen: this.handleListen,
      block: this.handleBlock
    };

    return __WEBPACK_IMPORTED_MODULE_2_react__["default"].createElement(__WEBPACK_IMPORTED_MODULE_5__Router__["a" /* default */], _extends({}, props, { history: history }));
  };

  return StaticRouter;
}(__WEBPACK_IMPORTED_MODULE_2_react__["default"].Component);

StaticRouter.propTypes = {
  basename: __WEBPACK_IMPORTED_MODULE_3_prop_types___default.a.string,
  context: __WEBPACK_IMPORTED_MODULE_3_prop_types___default.a.object.isRequired,
  location: __WEBPACK_IMPORTED_MODULE_3_prop_types___default.a.oneOfType([__WEBPACK_IMPORTED_MODULE_3_prop_types___default.a.string, __WEBPACK_IMPORTED_MODULE_3_prop_types___default.a.object])
};
StaticRouter.defaultProps = {
  basename: "",
  location: "/"
};
StaticRouter.childContextTypes = {
  router: __WEBPACK_IMPORTED_MODULE_3_prop_types___default.a.object.isRequired
};


/* harmony default export */ __webpack_exports__["a"] = (StaticRouter);

/***/ }),
/* 112 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_react_router_es_Switch__ = __webpack_require__(113);
// Written in this round about way for babel-transform-imports


/* harmony default export */ __webpack_exports__["a"] = (__WEBPACK_IMPORTED_MODULE_0_react_router_es_Switch__["a" /* default */]);

/***/ }),
/* 113 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_react__ = __webpack_require__(0);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_prop_types__ = __webpack_require__(1);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_prop_types___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_1_prop_types__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2_warning__ = __webpack_require__(13);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2_warning___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_2_warning__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_3_invariant__ = __webpack_require__(7);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_3_invariant___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_3_invariant__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_4__matchPath__ = __webpack_require__(35);
function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }







/**
 * The public API for rendering the first <Route> that matches.
 */

var Switch = function (_React$Component) {
  _inherits(Switch, _React$Component);

  function Switch() {
    _classCallCheck(this, Switch);

    return _possibleConstructorReturn(this, _React$Component.apply(this, arguments));
  }

  Switch.prototype.componentWillMount = function componentWillMount() {
    __WEBPACK_IMPORTED_MODULE_3_invariant___default()(this.context.router, "You should not use <Switch> outside a <Router>");
  };

  Switch.prototype.componentWillReceiveProps = function componentWillReceiveProps(nextProps) {
    __WEBPACK_IMPORTED_MODULE_2_warning___default()(!(nextProps.location && !this.props.location), '<Switch> elements should not change from uncontrolled to controlled (or vice versa). You initially used no "location" prop and then provided one on a subsequent render.');

    __WEBPACK_IMPORTED_MODULE_2_warning___default()(!(!nextProps.location && this.props.location), '<Switch> elements should not change from controlled to uncontrolled (or vice versa). You provided a "location" prop initially but omitted it on a subsequent render.');
  };

  Switch.prototype.render = function render() {
    var route = this.context.router.route;
    var children = this.props.children;

    var location = this.props.location || route.location;

    var match = void 0,
        child = void 0;
    __WEBPACK_IMPORTED_MODULE_0_react__["default"].Children.forEach(children, function (element) {
      if (match == null && __WEBPACK_IMPORTED_MODULE_0_react__["default"].isValidElement(element)) {
        var _element$props = element.props,
            pathProp = _element$props.path,
            exact = _element$props.exact,
            strict = _element$props.strict,
            sensitive = _element$props.sensitive,
            from = _element$props.from;

        var path = pathProp || from;

        child = element;
        match = Object(__WEBPACK_IMPORTED_MODULE_4__matchPath__["a" /* default */])(location.pathname, { path: path, exact: exact, strict: strict, sensitive: sensitive }, route.match);
      }
    });

    return match ? __WEBPACK_IMPORTED_MODULE_0_react__["default"].cloneElement(child, { location: location, computedMatch: match }) : null;
  };

  return Switch;
}(__WEBPACK_IMPORTED_MODULE_0_react__["default"].Component);

Switch.contextTypes = {
  router: __WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.shape({
    route: __WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.object.isRequired
  }).isRequired
};
Switch.propTypes = {
  children: __WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.node,
  location: __WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.object
};


/* harmony default export */ __webpack_exports__["a"] = (Switch);

/***/ }),
/* 114 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_react_router_es_generatePath__ = __webpack_require__(54);
// Written in this round about way for babel-transform-imports


/* harmony default export */ __webpack_exports__["a"] = (__WEBPACK_IMPORTED_MODULE_0_react_router_es_generatePath__["a" /* default */]);

/***/ }),
/* 115 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_react_router_es_matchPath__ = __webpack_require__(35);
// Written in this round about way for babel-transform-imports


/* harmony default export */ __webpack_exports__["a"] = (__WEBPACK_IMPORTED_MODULE_0_react_router_es_matchPath__["a" /* default */]);

/***/ }),
/* 116 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_react_router_es_withRouter__ = __webpack_require__(117);
// Written in this round about way for babel-transform-imports


/* harmony default export */ __webpack_exports__["a"] = (__WEBPACK_IMPORTED_MODULE_0_react_router_es_withRouter__["a" /* default */]);

/***/ }),
/* 117 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_react__ = __webpack_require__(0);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_prop_types__ = __webpack_require__(1);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_prop_types___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_1_prop_types__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2_hoist_non_react_statics__ = __webpack_require__(55);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2_hoist_non_react_statics___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_2_hoist_non_react_statics__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_3__Route__ = __webpack_require__(52);
var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

function _objectWithoutProperties(obj, keys) { var target = {}; for (var i in obj) { if (keys.indexOf(i) >= 0) continue; if (!Object.prototype.hasOwnProperty.call(obj, i)) continue; target[i] = obj[i]; } return target; }






/**
 * A public higher-order component to access the imperative API
 */
var withRouter = function withRouter(Component) {
  var C = function C(props) {
    var wrappedComponentRef = props.wrappedComponentRef,
        remainingProps = _objectWithoutProperties(props, ["wrappedComponentRef"]);

    return __WEBPACK_IMPORTED_MODULE_0_react__["default"].createElement(__WEBPACK_IMPORTED_MODULE_3__Route__["a" /* default */], {
      children: function children(routeComponentProps) {
        return __WEBPACK_IMPORTED_MODULE_0_react__["default"].createElement(Component, _extends({}, remainingProps, routeComponentProps, {
          ref: wrappedComponentRef
        }));
      }
    });
  };

  C.displayName = "withRouter(" + (Component.displayName || Component.name) + ")";
  C.WrappedComponent = Component;
  C.propTypes = {
    wrappedComponentRef: __WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.func
  };

  return __WEBPACK_IMPORTED_MODULE_2_hoist_non_react_statics___default()(C, Component);
};

/* harmony default export */ __webpack_exports__["a"] = (withRouter);

/***/ }),
/* 118 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

var _reactRouterDom = __webpack_require__(2);

var _Navigation = __webpack_require__(119);

var _Navigation2 = _interopRequireDefault(_Navigation);

var _NavigationDrawer = __webpack_require__(120);

var _NavigationDrawer2 = _interopRequireDefault(_NavigationDrawer);

var _AdUnit = __webpack_require__(21);

var _AdUnit2 = _interopRequireDefault(_AdUnit);

var _constants = __webpack_require__(4);

var _ArticleListComponent = __webpack_require__(121);

var _ArticleListComponent2 = _interopRequireDefault(_ArticleListComponent);

var _ArticleDetailComponent = __webpack_require__(131);

var _ArticleDetailComponent2 = _interopRequireDefault(_ArticleDetailComponent);

var _VideoLandingComponent_Server = __webpack_require__(154);

var _VideoLandingComponent_Server2 = _interopRequireDefault(_VideoLandingComponent_Server);

var _VideosByCategoryComponent = __webpack_require__(161);

var _VideosByCategoryComponent2 = _interopRequireDefault(_VideosByCategoryComponent);

var _VideoDetailComponent = __webpack_require__(163);

var _VideoDetailComponent2 = _interopRequireDefault(_VideoDetailComponent);

var _FinanceComponent = __webpack_require__(167);

var _FinanceComponent2 = _interopRequireDefault(_FinanceComponent);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var ServerAppComponent = function (_React$Component) {
	_inherits(ServerAppComponent, _React$Component);

	function ServerAppComponent() {
		_classCallCheck(this, ServerAppComponent);

		return _possibleConstructorReturn(this, (ServerAppComponent.__proto__ || Object.getPrototypeOf(ServerAppComponent)).apply(this, arguments));
	}

	_createClass(ServerAppComponent, [{
		key: 'render',
		value: function render() {
			var _this2 = this;

			return _react2.default.createElement(
				'div',
				null,
				_react2.default.createElement(_Navigation2.default, null),
				_react2.default.createElement(
					'div',
					{ className: 'body-content' },
					_react2.default.createElement(
						_reactRouterDom.Switch,
						null,
						_react2.default.createElement(_reactRouterDom.Route, { exact: true, path: '/m/(news|expert-reviews)/', component: function component() {
								return _react2.default.createElement(_ArticleListComponent2.default, _this2.props.childComponentProps);
							} }),
						_react2.default.createElement(_reactRouterDom.Route, { exact: true, path: '/m/(news|expert-reviews)/page/:pageNo/', component: function component() {
								return _react2.default.createElement(_ArticleListComponent2.default, _this2.props.childComponentProps);
							} }),
						_react2.default.createElement(_reactRouterDom.Route, { exact: true, path: '/m/news/:basicId(\\d+)-:title.html', component: function component() {
								return _react2.default.createElement(_ArticleDetailComponent2.default, _this2.props.childComponentProps);
							} }),
						_react2.default.createElement(_reactRouterDom.Route, { exact: true, path: '/m/expert-reviews/:title-:basicId(\\d+).html', component: function component() {
								return _react2.default.createElement(_ArticleDetailComponent2.default, _this2.props.childComponentProps);
							} }),
						_react2.default.createElement(_reactRouterDom.Route, { exact: true, path: '/m/bike-videos/', component: function component() {
								return _react2.default.createElement(_VideoLandingComponent_Server2.default, _this2.props.childComponentProps);
							} }),
						_react2.default.createElement(_reactRouterDom.Route, { exact: true, path: '/m/bike-videos/category/*-:categoryId(\\d+)/', component: function component() {
								return _react2.default.createElement(_VideosByCategoryComponent2.default, _this2.props.childComponentProps);
							} }),
						_react2.default.createElement(_reactRouterDom.Route, { exact: true, path: '/m/bike-videos/:title-:basicId(\\d+)/', component: function component() {
								return _react2.default.createElement(_VideoDetailComponent2.default, _this2.props.childComponentProps);
							} }),
						_react2.default.createElement(_reactRouterDom.Route, { exact: true, path: '/m/bike-loan-emi-calculator/', component: function component() {
								return _react2.default.createElement(_FinanceComponent2.default, _this2.props.childComponentProps);
							} })
					)
				),
				_react2.default.createElement(_NavigationDrawer2.default, null),
				_react2.default.createElement('div', { id: 'peripheralComponents' }),
				_react2.default.createElement('div', { className: 'blackOut-window' })
			);
		}
	}]);

	return ServerAppComponent;
}(_react2.default.Component);

module.exports = ServerAppComponent;

/***/ }),
/* 119 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
    value: true
});

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

var _reactRouterDom = __webpack_require__(2);

var _analyticsUtils = __webpack_require__(5);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var Navigation = function (_React$Component) {
    _inherits(Navigation, _React$Component);

    function Navigation() {
        _classCallCheck(this, Navigation);

        return _possibleConstructorReturn(this, (Navigation.__proto__ || Object.getPrototypeOf(Navigation)).apply(this, arguments));
    }

    _createClass(Navigation, [{
        key: 'openNavDrawer',
        value: function openNavDrawer() {
            document.body.className = 'nav-drawer-active';

            var categ = (0, _analyticsUtils.GetCatForNav)();
            if (categ != null) {
                (0, _analyticsUtils.triggerGA)(categ, 'Hamburger_Menu_Icon', 'Icon_Click');
            }
        }
    }, {
        key: 'lockPopup',
        value: function lockPopup() {
            document.body.classList.add('lock-browser-scroll');
            document.getElementsByClassName('blackOut-window')[0].style.display = 'block';
        }
    }, {
        key: 'openGlobalSearchPopUp',
        value: function openGlobalSearchPopUp() {
            try {
                document.getElementById('global-search-popup').style.display = 'block';
                document.getElementById('globalSearch').focus();
                this.lockPopup();
            } catch (err) {}
        }
    }, {
        key: 'openGlobalCityPopUp',
        value: function openGlobalCityPopUp() {
            try {
                document.getElementById('globalcity-popup').style.display = 'block';

                this.lockPopup();
                document.getElementById('error-icon').classList.add('hide');
                document.getElementById('bw-blackbg-tooltip').classList.add('hide');
                document.getElementById("globalCityPopUp").classList.remove('border-red');

                document.getElementById("loaderGlobalCity").style.display = 'none';

                window.location.hash = "globalCity";
            } catch (err) {}
        }
    }, {
        key: 'render',
        value: function render() {

            return _react2.default.createElement(
                'div',
                { className: 'header-navbar' },
                _react2.default.createElement(
                    'div',
                    { className: 'leftfloat' },
                    _react2.default.createElement(
                        'div',
                        { id: 'navbar-btn', className: 'inline-block cur-pointer', onClick: this.openNavDrawer },
                        _react2.default.createElement('span', { className: 'bwmsprite nav-icon' })
                    ),
                    _react2.default.createElement(
                        'a',
                        { href: '/m/', id: 'bwheader-logo', className: 'header-link inline-block' },
                        _react2.default.createElement('img', { src: 'data:image/png;base64,', alt: 'BikeWale' })
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { className: 'rightfloat' },
                    _react2.default.createElement(
                        'div',
                        { className: 'global-search cur-pointer', id: 'global-search', onClick: this.openGlobalSearchPopUp.bind(this) },
                        _react2.default.createElement('span', { className: 'bwmsprite search-bold-icon' })
                    ),
                    _react2.default.createElement(
                        'div',
                        { className: 'global-location cur-pointer', onClick: this.openGlobalCityPopUp.bind(this) },
                        _react2.default.createElement('span', { className: 'bwmsprite map-loc-icon' })
                    )
                ),
                _react2.default.createElement('div', { className: 'clear' })
            );
        }
    }]);

    return Navigation;
}(_react2.default.Component);

exports.default = Navigation;

/***/ }),
/* 120 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
    value: true
});

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

var _reactRouterDom = __webpack_require__(2);

var _analyticsUtils = __webpack_require__(5);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var NavigationDrawer = function (_React$Component) {
    _inherits(NavigationDrawer, _React$Component);

    function NavigationDrawer(props) {
        _classCallCheck(this, NavigationDrawer);

        var _this = _possibleConstructorReturn(this, (NavigationDrawer.__proto__ || Object.getPrototypeOf(NavigationDrawer)).call(this, props));

        _this.pushAnalytics = _this.pushAnalytics.bind(_this);
        _this.toggleNestedNav = _this.toggleNestedNav.bind(_this);
        _this.pushAnalytics = _this.pushAnalytics.bind(_this);
        _this.pushAnalyticsAndCloseDrawer = _this.pushAnalyticsAndCloseDrawer.bind(_this);
        _this.renderNestedListItem = _this.renderNestedListItem.bind(_this);
        _this.renderNestedListItemWithLinkTag = _this.renderNestedListItemWithLinkTag.bind(_this);

        return _this;
    }

    _createClass(NavigationDrawer, [{
        key: 'removeActiveItemClassName',
        value: function removeActiveItemClassName(element) {
            if (element.classList.contains('item-active')) element.className = '';else element.className = 'item-active';
        }
    }, {
        key: 'toggleNestedNav',
        value: function toggleNestedNav(event) {
            var listItem = event.currentTarget.parentElement;
            this.removeActiveItemClassName(listItem);
        }
    }, {
        key: 'closeNavDrawer',
        value: function closeNavDrawer(event) {
            document.body.className = '';
        }
    }, {
        key: 'closeNestedNavigation',
        value: function closeNestedNavigation() {
            var element = document.getElementById('nav-drawer').getElementsByClassName('item-active');
            if (element && element[0]) {
                this.removeActiveItemClassName(element[0]);
            }
        }
    }, {
        key: 'pushAnalytics',
        value: function pushAnalytics(label) {
            if (typeof label === "string") {
                (0, _analyticsUtils.pushNavMenuAnalytics)(label);
            } else {
                (0, _analyticsUtils.pushNavMenuAnalytics)(event.currentTarget.innerHTML);
            }
        }
    }, {
        key: 'pushAnalyticsAndCloseDrawer',
        value: function pushAnalyticsAndCloseDrawer(label) {
            if (typeof label === "string") {
                (0, _analyticsUtils.pushNavMenuAnalytics)(label);
            } else {
                (0, _analyticsUtils.pushNavMenuAnalytics)(event.currentTarget.innerHTML);
            }
            if (event.target.nodeName) {
                this.closeNestedNavigation();
                this.closeNavDrawer();
            }
        }
    }, {
        key: 'renderNestedListItemWithLinkTag',
        value: function renderNestedListItemWithLinkTag(link, text) {
            return _react2.default.createElement(
                'li',
                null,
                _react2.default.createElement(
                    _reactRouterDom.Link,
                    { to: link, onClick: this.pushAnalyticsAndCloseDrawer },
                    text
                )
            );
        }
    }, {
        key: 'renderNestedListItem',
        value: function renderNestedListItem(link, text) {
            return _react2.default.createElement(
                'li',
                null,
                _react2.default.createElement(
                    'a',
                    { href: link, onClick: this.pushAnalyticsAndCloseDrawer },
                    text
                )
            );
        }
    }, {
        key: 'render',
        value: function render() {

            return _react2.default.createElement(
                'div',
                null,
                _react2.default.createElement(
                    'nav',
                    { id: 'nav-drawer', className: 'transition-ease' },
                    _react2.default.createElement(
                        'ul',
                        { className: 'nav-drawer-list padding-top10' },
                        _react2.default.createElement(
                            'li',
                            null,
                            _react2.default.createElement(
                                'a',
                                { href: '/m/', className: 'nav-item', onClick: this.pushAnalyticsAndCloseDrawer.bind(this, "Home") },
                                _react2.default.createElement('span', { className: 'bwmsprite home-icon' }),
                                _react2.default.createElement(
                                    'span',
                                    null,
                                    'Home'
                                )
                            )
                        ),
                        _react2.default.createElement(
                            'li',
                            null,
                            _react2.default.createElement(
                                'div',
                                { className: 'nav-item', onClick: this.toggleNestedNav },
                                _react2.default.createElement(
                                    'div',
                                    { onClick: this.pushAnalytics.bind(this, "New Bikes") },
                                    _react2.default.createElement('span', { className: 'bwmsprite newBikes-icon' }),
                                    _react2.default.createElement(
                                        'span',
                                        null,
                                        'New Bikes'
                                    ),
                                    _react2.default.createElement('span', { className: 'bwmsprite fa-angle-down' })
                                )
                            ),
                            _react2.default.createElement(
                                'ul',
                                { className: 'nested-nav-list' },
                                this.renderNestedListItem("/m/new-bikes-in-india/", "Find New Bikes"),
                                this.renderNestedListItem("/m/comparebikes/", "Compare Bikes"),
                                this.renderNestedListItem("/m/pricequote/", "Check On-Road Price"),
                                this.renderNestedListItem("/m/dealer-showrooms/", "Locate Dealer"),
                                this.renderNestedListItem("/m/service-centers/", "Locate Service Center"),
                                this.renderNestedListItem("/m/upcoming-bikes/", "Upcoming Bikes"),
                                this.renderNestedListItem("/m/new-bike-launches/", "New Launches"),
                                this.renderNestedListItem("/m/bikebooking/", "Book Your Bike")
                            )
                        ),
                        _react2.default.createElement(
                            'li',
                            null,
                            _react2.default.createElement(
                                'div',
                                { className: 'nav-item', onClick: this.toggleNestedNav },
                                _react2.default.createElement(
                                    'div',
                                    { onClick: this.pushAnalytics.bind(this, "New Scooters") },
                                    _react2.default.createElement('span', { className: 'bwmsprite scooter-icon' }),
                                    _react2.default.createElement(
                                        'span',
                                        null,
                                        'New Scooters'
                                    ),
                                    _react2.default.createElement('span', { className: 'bwmsprite fa-angle-down' })
                                )
                            ),
                            _react2.default.createElement(
                                'ul',
                                { className: 'nested-nav-list' },
                                this.renderNestedListItem("/m/honda-scooters/", "Honda Scooters"),
                                this.renderNestedListItem("/m/hero-scooters/", "Hero Scooters"),
                                this.renderNestedListItem("/m/tvs-scooters/", "TVS Scooters"),
                                this.renderNestedListItem("/m/yamaha-scooters/", "Yamaha Scooters"),
                                this.renderNestedListItem("/m/scooters/", "All Scooters")
                            )
                        ),
                        _react2.default.createElement(
                            'li',
                            null,
                            _react2.default.createElement(
                                'div',
                                { className: 'nav-item', onClick: this.toggleNestedNav },
                                _react2.default.createElement(
                                    'div',
                                    { onClick: this.pushAnalytics.bind(this, "Buy & Sell Used Bikes") },
                                    _react2.default.createElement('span', { className: 'bwmsprite usedBikes-icon' }),
                                    _react2.default.createElement(
                                        'span',
                                        null,
                                        'Buy & Sell Used Bikes'
                                    ),
                                    _react2.default.createElement('span', { className: 'bwmsprite fa-angle-down' })
                                )
                            ),
                            _react2.default.createElement(
                                'ul',
                                { className: 'nested-nav-list' },
                                this.renderNestedListItem("/m/used/", "Find Used Bikes"),
                                this.renderNestedListItem("/m/used/bikes-in-india/", "All Used Bikes"),
                                this.renderNestedListItem("/m/used/sell/", "Sell Your Bike")
                            )
                        ),
                        _react2.default.createElement(
                            'li',
                            null,
                            _react2.default.createElement(
                                'a',
                                { href: '/m/reviews/', className: 'nav-item', onClick: this.pushAnalyticsAndCloseDrawer.bind(this, "Reviews") },
                                _react2.default.createElement('span', { className: 'bwmsprite reviews-icon' }),
                                _react2.default.createElement(
                                    'span',
                                    null,
                                    'Reviews'
                                )
                            )
                        ),
                        _react2.default.createElement(
                            'li',
                            null,
                            _react2.default.createElement(
                                'div',
                                { className: 'nav-item', onClick: this.toggleNestedNav },
                                _react2.default.createElement(
                                    'div',
                                    { onClick: this.pushAnalytics.bind(this, "News, Videos & Tips") },
                                    _react2.default.createElement('span', { className: 'bwmsprite news-icon' }),
                                    _react2.default.createElement(
                                        'span',
                                        null,
                                        'News, Videos & Tips'
                                    ),
                                    _react2.default.createElement('span', { className: 'bwmsprite fa-angle-down' })
                                )
                            ),
                            _react2.default.createElement(
                                'ul',
                                { className: 'nested-nav-list' },
                                this.renderNestedListItemWithLinkTag("/m/news/", "News"),
                                this.renderNestedListItemWithLinkTag("/m/expert-reviews/", "Expert Reviews"),
                                this.renderNestedListItem("/m/features/", "Features"),
                                this.renderNestedListItem("/m/bike-care/", "Bike Care"),
                                this.renderNestedListItemWithLinkTag("/m/bike-videos/", "Videos")
                            )
                        ),
                        _react2.default.createElement(
                            'li',
                            null,
                            _react2.default.createElement(
                                'a',
                                { href: '/featured/trackday-2018/', className: 'nav-item', onClick: this.pushAnalyticsAndCloseDrawer.bind(this, "Track Day 2018") },
                                _react2.default.createElement('span', { className: 'bwmsprite track-day' }),
                                _react2.default.createElement(
                                    'span',
                                    null,
                                    'Track Day 2018'
                                )
                            )
                        ),
                        _react2.default.createElement(
                            'li',
                            null,
                            _react2.default.createElement(
                                _reactRouterDom.Link,
                                { to: '/m/bike-loan-emi-calculator/', className: 'nav-item', onClick: this.pushAnalyticsAndCloseDrawer.bind(this, "EMI Calculator") },
                                _react2.default.createElement('span', { className: 'bwmsprite forum-icon' }),
                                _react2.default.createElement(
                                    'span',
                                    null,
                                    'EMI Calculator'
                                )
                            )
                        ),
                        _react2.default.createElement(
                            'li',
                            null,
                            _react2.default.createElement(
                                'a',
                                { href: '/m/users/login.aspx', className: 'nav-item', onClick: this.pushAnalytics.bind() },
                                _react2.default.createElement('span', { className: 'bwmsprite myBikeWale-icon' }),
                                _react2.default.createElement(
                                    'span',
                                    null,
                                    'Login'
                                )
                            )
                        )
                    ),
                    _react2.default.createElement(
                        'div',
                        { id: 'nav-app-content', className: 'transition-ease' },
                        _react2.default.createElement(
                            'p',
                            { className: 'font12 text-bold inline-block' },
                            'India\u2019s #1 Bike Research Destination'
                        ),
                        _react2.default.createElement(
                            'a',
                            { href: 'https://play.google.com/store/apps/details?id=com.bikewale.app&referrer=utm_source%3DMobilesite%26utm_medium%3DDrawer%26utm_campaign=BikeWale%2520MobilesiteDrawer', target: '_blank', className: 'btn btn-orange nav-app-install-btn text-bold inline-block', rel: 'noopener nofollow' },
                            'Install'
                        )
                    )
                ),
                _react2.default.createElement('div', { className: 'black-overlay', onClick: this.closeNavDrawer })
            );
        }
    }]);

    return NavigationDrawer;
}(_react2.default.Component);

exports.default = (0, _reactRouterDom.withRouter)(NavigationDrawer);

/***/ }),
/* 121 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
    value: true
});

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

var _reactRouterDom = __webpack_require__(2);

var _propTypes = __webpack_require__(1);

var _propTypes2 = _interopRequireDefault(_propTypes);

var _ArticleList = __webpack_require__(122);

var _ArticleList2 = _interopRequireDefault(_ArticleList);

var _NewsPagination = __webpack_require__(130);

var _NewsPagination2 = _interopRequireDefault(_NewsPagination);

var _SpinnerRelative = __webpack_require__(8);

var _SpinnerRelative2 = _interopRequireDefault(_SpinnerRelative);

var _Breadcrumb = __webpack_require__(23);

var _Breadcrumb2 = _interopRequireDefault(_Breadcrumb);

var _NewBikes = __webpack_require__(56);

var _NewBikes2 = _interopRequireDefault(_NewBikes);

var _Footer = __webpack_require__(24);

var _Footer2 = _interopRequireDefault(_Footer);

var _AdUnit = __webpack_require__(21);

var _AdUnit2 = _interopRequireDefault(_AdUnit);

var _constants = __webpack_require__(4);

var _commonUtils = __webpack_require__(3);

var _popUpUtils = __webpack_require__(11);

var _scrollUtils = __webpack_require__(28);

var _googleAdUtils = __webpack_require__(36);

var _timing = __webpack_require__(29);

var _NewsCommon = __webpack_require__(22);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

if (false) {
    require('../../../stylesheet/news.sass');
}

var ArticleListComponent = function (_React$Component) {
    _inherits(ArticleListComponent, _React$Component);

    function ArticleListComponent(props) {
        _classCallCheck(this, ArticleListComponent);

        var _this = _possibleConstructorReturn(this, (ArticleListComponent.__proto__ || Object.getPrototypeOf(ArticleListComponent)).call(this, props));

        if ((0, _commonUtils.isServer)()) {

            if (_this.props.ArticleListData && _this.props.ArticleListData.ArticleList) {
                _this.props.ArticleListData.Status = _constants.Status.Fetched;
                _this.props.ArticleListData.PageNo = Math.floor(parseInt(_this.props.ArticleListData.ArticleList.StartIndex) / parseInt(_constants.NewsArticlesPerPage)) + 1; // corner case - last page with less than NewsArticlesPerPage articles
            }
            if (_this.props.NewBikesListData && _this.props.NewBikesListData.NewBikesList) {
                _this.props.NewBikesListData.Status = _constants.Status.Fetched;
            }
        }

        var globalCity = (0, _popUpUtils.getGlobalCity)();
        _this.globalCityName = globalCity && globalCity.name.length > 0 ? globalCity.name : '';

        _this.updateArticleList = _this.updateArticleList.bind(_this);
        _this.onArticleClickEvent = _this.onArticleClickEvent.bind(_this);

        if (typeof gaObj != "undefined") {
            gaObj = _constants.GA_PAGE_MAPPING["ListPage"];
        }

        return _this;
    }

    _createClass(ArticleListComponent, [{
        key: 'componentDidUpdate',
        value: function componentDidUpdate() {
            var pageNoFromData = this.props.ArticleListData.PageNo; // if no pageno then -1
            var pageNoFromUrl = this.props.match.params["pageNo"] ? this.props.match.params["pageNo"] : 1;
            if (pageNoFromData == pageNoFromUrl) {
                this.logger();
                this.scrollToPosition();
            }
        }
    }, {
        key: 'componentDidMount',
        value: function componentDidMount() {
            this.logger();

            if (this.props.ArticleListData && this.props.ArticleListData.Status == _constants.Status.Fetched) {
                //bikesList has been fetched
                if (this.props.NewBikesListData && this.props.NewBikesListData.Status == _constants.Status.Fetched) {
                    return;
                } else {
                    // bikesList to be fetched
                    this.props.fetchNewBikesListData();
                }
            } else {
                //both data needs to be fetched
                this.props.fetchArticleList(this.props.ArticleListData.PageNo); // pageNo is initially -1
                this.props.fetchNewBikesListData();
            }

            if ((0, _scrollUtils.isBrowserWithoutScrollSupport)()) {
                window.scrollTo(0, 0);
            }
        }
    }, {
        key: 'componentWillReceiveProps',
        value: function componentWillReceiveProps(nextProps) {
            try {
                var prevUrlParam = this.props.match.params;
                var nextUrlParam = nextProps.match.params;
                if (prevUrlParam != nextUrlParam) {
                    nextProps.fetchArticleList(-1);
                    nextProps.fetchNewBikesListData();
                    if ((0, _scrollUtils.isBrowserWithoutScrollSupport)()) {
                        window.scrollTo(0, 0);
                    }
                    return;
                }
                //componentWillRecieveProps is called on first load in UC Browser and iOS Chrome, not in other browsers
                if (prevUrlParam["pageNo"] === nextUrlParam["pageNo"]) {
                    // condition 1 : new url has been pushed
                    return;
                }
                var newHashValue = this.props.location.hash;
                var oldHashValue = nextProps.location.hash;
                if (newHashValue && newHashValue.indexOf('#') >= 0 || oldHashValue && oldHashValue.indexOf('#') >= 0) {
                    // condition 2 :  global city popup  clicked -- should not call apis
                    return;
                }

                if (nextProps && nextProps.ArticleListData.Status != _constants.Status.IsFetching) {
                    // condition 3 : back / forward button has been clicked, so all the dataobjects have fetched state from previous url. hence now new api have to be hit
                    nextProps.fetchArticleList(-1);
                    nextProps.fetchNewBikesListData();
                    if ((0, _scrollUtils.isBrowserWithoutScrollSupport)()) {
                        window.scrollTo(0, 0);
                    }
                }
            } catch (e) {}
        }
    }, {
        key: 'componentWillUnmount',
        value: function componentWillUnmount() {
            this.props.resetArticleListData();
        }
    }, {
        key: 'updateArticleList',
        value: function updateArticleList(PageNo) {
            this.props.fetchArticleList(PageNo);
            this.props.fetchNewBikesListData();
            if ((0, _scrollUtils.isBrowserWithoutScrollSupport)()) {
                window.scrollTo(0, 0);
            }
        }
    }, {
        key: 'onArticleClickEvent',
        value: function onArticleClickEvent(ArticleInitialData) {
            if (!ArticleInitialData || !ArticleInitialData.ArticleUrl) return;
            this.props.history.push(ArticleInitialData.ArticleUrl);
            this.props.fetchArticleDetail(ArticleInitialData);
        }
    }, {
        key: 'scrollToPosition',
        value: function scrollToPosition() {
            if (_scrollUtils.scrollPosition.x >= 0 && _scrollUtils.scrollPosition.y >= 0) {
                // needs to be scrolled
                if (this.props.ArticleListData && this.props.ArticleListData.Status == _constants.Status.Fetched) {
                    // checks whether ready to scroll
                    window.scrollTo(_scrollUtils.scrollPosition.x, _scrollUtils.scrollPosition.y);
                    (0, _scrollUtils.resetScrollPosition)();
                }
            }
        }
    }, {
        key: 'logger',
        value: function logger() {
            try {
                if (this.props && this.props.ArticleListData.Status == _constants.Status.Fetched && this.props.NewBikesListData.Status == _constants.Status.Fetched) {
                    (0, _timing.endTimer)("component-render");
                }
            } catch (err) {}
        }
    }, {
        key: 'renderNewBikesList',
        value: function renderNewBikesList() {
            if (!this.props.NewBikesListData || this.props.NewBikesListData.Status !== _constants.Status.Fetched) {
                return false;
            }
            return this.props.NewBikesListData.NewBikesList.map(function (newBikes) {
                return _react2.default.createElement(_NewBikes2.default, { key: newBikes.Heading, newBikesData: newBikes });
            });
        }
    }, {
        key: 'render',
        value: function render() {
            var componentData = this.props.ArticleListData;

            var loadingState = _react2.default.createElement(
                'div',
                null,
                _react2.default.createElement(_SpinnerRelative2.default, null)
            );

            if (!componentData || componentData.Status == _constants.Status.Reset || componentData.Status == _constants.Status.IsFetching || componentData.Status == _constants.Status.Error) {
                return loadingState;
            }

            var targetTags = {
                City: this.globalCityName
            };

            var adSlotTop = _react2.default.createElement(_AdUnit2.default, { uniqueKey: componentData.PageNo, tags: targetTags, adSlot: _constants.AD_PATH_NEWS_MOBILE_TOP_320_50, adDimension: _constants.AD_DIMENSION_320_50, adContainerId: _constants.AD_DIV_REVIEWS_TOP_320_50 });
            var adSlotMiddle = _react2.default.createElement(_AdUnit2.default, { uniqueKey: componentData.PageNo, tags: targetTags, adSlot: _constants.AD_PATH_NEWS_MOBILE_MIDDLE_300_250, adDimension: _constants.AD_DIMENSION_300_250, adContainerId: _constants.AD_DIV_REVIEWS_MIDDLE_300_250 });
            var adSlotBottom = _react2.default.createElement(_AdUnit2.default, { uniqueKey: componentData.PageNo, tags: targetTags, adSlot: _constants.AD_PATH_NEWS_MOBILE_BOTTOM_320_50, adDimension: _constants.AD_DIMENSION_320_50, adContainerId: _constants.AD_DIV_REVIEWS_BOTTOM_320_50 });
            var isNews = (0, _NewsCommon.extractPageCategoryFromURL)() == "news";
            var pageUrl = isNews ? "news" : "expert-reviews";
            var breadcrumbTitle = isNews ? "News" : "Reviews";
            return _react2.default.createElement(
                'div',
                null,
                adSlotTop,
                _react2.default.createElement(
                    'div',
                    { className: 'container bg-white box-shadow section-bottom-margin' },
                    _react2.default.createElement(
                        'h1',
                        { className: 'box-shadow card-heading' },
                        componentData.ArticleList.PageTitle
                    ),
                    _react2.default.createElement(_ArticleList2.default, { articleList: componentData.ArticleList.Articles,
                        pageNo: componentData.PageNo,
                        onArticleClickEvent: this.onArticleClickEvent,
                        isCategoryNameShown: isNews }),
                    _react2.default.createElement(_NewsPagination2.default, { startIndex: componentData.ArticleList.StartIndex,
                        endIndex: componentData.ArticleList.EndIndex,
                        pageNo: componentData.PageNo,
                        articleCount: componentData.ArticleList.RecordCount,
                        articlePerPage: _constants.NewsArticlesPerPage,
                        updateArticleList: this.updateArticleList,
                        pageUrl: pageUrl })
                ),
                _react2.default.createElement(
                    'div',
                    { className: 'margin-bottom15' },
                    adSlotMiddle
                ),
                this.renderNewBikesList(),
                _react2.default.createElement(
                    'div',
                    { className: 'margin-bottom15' },
                    adSlotBottom
                ),
                _react2.default.createElement(_Breadcrumb2.default, { breadcrumb: [{ Href: '/m/', Title: 'Home' }, { Href: '', Title: breadcrumbTitle }] }),
                _react2.default.createElement(_Footer2.default, null)
            );
        }
    }]);

    return ArticleListComponent;
}(_react2.default.Component);

exports.default = (0, _reactRouterDom.withRouter)(ArticleListComponent);

/***/ }),
/* 122 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
    value: true
});

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

var _reactRouterDom = __webpack_require__(2);

var _commonUtils = __webpack_require__(3);

var _NewsCommon = __webpack_require__(22);

var _reactLazyLoad = __webpack_require__(6);

var _reactLazyLoad2 = _interopRequireDefault(_reactLazyLoad);

var _UserReviewSlug = __webpack_require__(129);

var _UserReviewSlug2 = _interopRequireDefault(_UserReviewSlug);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var ArticleList = function (_React$Component) {
    _inherits(ArticleList, _React$Component);

    function ArticleList() {
        _classCallCheck(this, ArticleList);

        return _possibleConstructorReturn(this, (ArticleList.__proto__ || Object.getPrototypeOf(ArticleList)).apply(this, arguments));
    }

    _createClass(ArticleList, [{
        key: 'onArticleClickEvent',
        value: function onArticleClickEvent(article, event) {
            event.preventDefault();
            var articleInitialData = (0, _NewsCommon.mapNewsArticleDataToInitialData)(article);

            this.props.onArticleClickEvent(articleInitialData);
        }
    }, {
        key: 'renderArticleContent',
        value: function renderArticleContent(article, index) {
            var imageUrl = !article.HostUrl || !article.SmallPicUrl ? 'https://imgd.aeplcdn.com/160x89/bikewaleimg/images/noimage.png?q=70' : article.HostUrl + article.SmallPicUrl;
            var articleCategoryComponent = function articleCategoryComponent(isCategoryNameShown) {
                if (isCategoryNameShown) {
                    return _react2.default.createElement(
                        'div',
                        { className: 'article-desc-wrapper' },
                        _react2.default.createElement(
                            'span',
                            { className: 'article-category' },
                            article.CategoryName
                        ),
                        _react2.default.createElement(
                            'h2',
                            { className: 'font14' },
                            article.Title
                        )
                    );
                } else {
                    return _react2.default.createElement(
                        'div',
                        { className: 'article-desc-wrapper' },
                        _react2.default.createElement(
                            'h2',
                            { className: 'font14' },
                            'Expert Review: ',
                            article.Title
                        )
                    );
                }
            };
            return _react2.default.createElement(
                'div',
                { className: 'article-item-content' },
                _react2.default.createElement(
                    'div',
                    { className: 'article-wrapper' },
                    _react2.default.createElement(
                        'div',
                        { className: 'article-image-wrapper' },
                        _react2.default.createElement(
                            _reactLazyLoad2.default,
                            { offsetVertical: 0 },
                            _react2.default.createElement('img', { src: imageUrl, alt: article.Title, title: article.Title })
                        )
                    ),
                    articleCategoryComponent(this.props.isCategoryNameShown)
                ),
                _react2.default.createElement(
                    'div',
                    { className: 'article-stats-wrapper font12 leftfloat text-light-grey' },
                    _react2.default.createElement('span', { className: 'bwmsprite calender-grey-icon' }),
                    _react2.default.createElement(
                        'span',
                        { className: 'inline-block' },
                        article.DisplayDate
                    )
                ),
                _react2.default.createElement(
                    'div',
                    { className: 'article-stats-wrapper font12 leftfloat text-light-grey' },
                    _react2.default.createElement('span', { className: 'bwmsprite author-grey-icon' }),
                    _react2.default.createElement(
                        'span',
                        { className: 'inline-block' },
                        article.AuthorName
                    )
                ),
                _react2.default.createElement('div', { className: 'clear' })
            );
        }
    }, {
        key: 'renderArticleLinkTag',
        value: function renderArticleLinkTag(article, index) {
            if (!article) {
                return null;
            }

            if ((0, _NewsCommon.isReactCategory)(article.CategoryName)) {
                return _react2.default.createElement(
                    'li',
                    { key: article.BasicId },
                    _react2.default.createElement(
                        _reactRouterDom.Link,
                        { to: article.ArticleUrl, title: article.Title, onClick: this.onArticleClickEvent.bind(this, article) },
                        this.renderArticleContent(article, index)
                    )
                );
            } else {
                return _react2.default.createElement(
                    'li',
                    { key: article.BasicId },
                    _react2.default.createElement(
                        'a',
                        { href: article.ArticleUrl, title: article.Title },
                        this.renderArticleContent(article, index)
                    )
                );
            }
        }
    }, {
        key: 'render',
        value: function render() {
            var articleComponents = [];
            for (var i = 0; i < this.props.articleList.length; i++) {
                if (i == _commonUtils.CMSUserReviewSlugPosition && this.props.pageNo == 1) {
                    if ((0, _commonUtils.isServer)() || !(0, _commonUtils.isCMSUserReviewSlugClosed)()) {
                        articleComponents.push(_react2.default.createElement(_UserReviewSlug2.default, null));
                    }
                }
                articleComponents.push(this.renderArticleLinkTag(this.props.articleList[i], i));
            }
            return _react2.default.createElement(
                'ul',
                { className: 'article-list' },
                articleComponents
            );
        }
    }]);

    return ArticleList;
}(_react2.default.Component);

exports.default = ArticleList;

/***/ }),
/* 123 */
/***/ (function(module, exports, __webpack_require__) {

var __WEBPACK_AMD_DEFINE_FACTORY__, __WEBPACK_AMD_DEFINE_RESULT__;(function(root,factory){
    if (true) {
        !(__WEBPACK_AMD_DEFINE_FACTORY__ = (factory),
				__WEBPACK_AMD_DEFINE_RESULT__ = (typeof __WEBPACK_AMD_DEFINE_FACTORY__ === 'function' ?
				(__WEBPACK_AMD_DEFINE_FACTORY__.call(exports, __webpack_require__, exports, module)) :
				__WEBPACK_AMD_DEFINE_FACTORY__),
				__WEBPACK_AMD_DEFINE_RESULT__ !== undefined && (module.exports = __WEBPACK_AMD_DEFINE_RESULT__));
    } else if (typeof exports === 'object') {
        module.exports = factory();
    } else {
        root.eventListener = factory();
  }
}(this, function () {
	function wrap(standard, fallback) {
		return function (el, evtName, listener, useCapture) {
			if (el[standard]) {
				el[standard](evtName, listener, useCapture);
			} else if (el[fallback]) {
				el[fallback]('on' + evtName, listener);
			}
		}
	}

    return {
		add: wrap('addEventListener', 'attachEvent'),
		remove: wrap('removeEventListener', 'detachEvent')
	};
}));

/***/ }),
/* 124 */
/***/ (function(module, exports, __webpack_require__) {

/* WEBPACK VAR INJECTION */(function(global) {/**
 * lodash (Custom Build) <https://lodash.com/>
 * Build: `lodash modularize exports="npm" -o ./`
 * Copyright jQuery Foundation and other contributors <https://jquery.org/>
 * Released under MIT license <https://lodash.com/license>
 * Based on Underscore.js 1.8.3 <http://underscorejs.org/LICENSE>
 * Copyright Jeremy Ashkenas, DocumentCloud and Investigative Reporters & Editors
 */

/** Used as the `TypeError` message for "Functions" methods. */
var FUNC_ERROR_TEXT = 'Expected a function';

/** Used as references for various `Number` constants. */
var NAN = 0 / 0;

/** `Object#toString` result references. */
var symbolTag = '[object Symbol]';

/** Used to match leading and trailing whitespace. */
var reTrim = /^\s+|\s+$/g;

/** Used to detect bad signed hexadecimal string values. */
var reIsBadHex = /^[-+]0x[0-9a-f]+$/i;

/** Used to detect binary string values. */
var reIsBinary = /^0b[01]+$/i;

/** Used to detect octal string values. */
var reIsOctal = /^0o[0-7]+$/i;

/** Built-in method references without a dependency on `root`. */
var freeParseInt = parseInt;

/** Detect free variable `global` from Node.js. */
var freeGlobal = typeof global == 'object' && global && global.Object === Object && global;

/** Detect free variable `self`. */
var freeSelf = typeof self == 'object' && self && self.Object === Object && self;

/** Used as a reference to the global object. */
var root = freeGlobal || freeSelf || Function('return this')();

/** Used for built-in method references. */
var objectProto = Object.prototype;

/**
 * Used to resolve the
 * [`toStringTag`](http://ecma-international.org/ecma-262/7.0/#sec-object.prototype.tostring)
 * of values.
 */
var objectToString = objectProto.toString;

/* Built-in method references for those with the same name as other `lodash` methods. */
var nativeMax = Math.max,
    nativeMin = Math.min;

/**
 * Gets the timestamp of the number of milliseconds that have elapsed since
 * the Unix epoch (1 January 1970 00:00:00 UTC).
 *
 * @static
 * @memberOf _
 * @since 2.4.0
 * @category Date
 * @returns {number} Returns the timestamp.
 * @example
 *
 * _.defer(function(stamp) {
 *   console.log(_.now() - stamp);
 * }, _.now());
 * // => Logs the number of milliseconds it took for the deferred invocation.
 */
var now = function() {
  return root.Date.now();
};

/**
 * Creates a debounced function that delays invoking `func` until after `wait`
 * milliseconds have elapsed since the last time the debounced function was
 * invoked. The debounced function comes with a `cancel` method to cancel
 * delayed `func` invocations and a `flush` method to immediately invoke them.
 * Provide `options` to indicate whether `func` should be invoked on the
 * leading and/or trailing edge of the `wait` timeout. The `func` is invoked
 * with the last arguments provided to the debounced function. Subsequent
 * calls to the debounced function return the result of the last `func`
 * invocation.
 *
 * **Note:** If `leading` and `trailing` options are `true`, `func` is
 * invoked on the trailing edge of the timeout only if the debounced function
 * is invoked more than once during the `wait` timeout.
 *
 * If `wait` is `0` and `leading` is `false`, `func` invocation is deferred
 * until to the next tick, similar to `setTimeout` with a timeout of `0`.
 *
 * See [David Corbacho's article](https://css-tricks.com/debouncing-throttling-explained-examples/)
 * for details over the differences between `_.debounce` and `_.throttle`.
 *
 * @static
 * @memberOf _
 * @since 0.1.0
 * @category Function
 * @param {Function} func The function to debounce.
 * @param {number} [wait=0] The number of milliseconds to delay.
 * @param {Object} [options={}] The options object.
 * @param {boolean} [options.leading=false]
 *  Specify invoking on the leading edge of the timeout.
 * @param {number} [options.maxWait]
 *  The maximum time `func` is allowed to be delayed before it's invoked.
 * @param {boolean} [options.trailing=true]
 *  Specify invoking on the trailing edge of the timeout.
 * @returns {Function} Returns the new debounced function.
 * @example
 *
 * // Avoid costly calculations while the window size is in flux.
 * jQuery(window).on('resize', _.debounce(calculateLayout, 150));
 *
 * // Invoke `sendMail` when clicked, debouncing subsequent calls.
 * jQuery(element).on('click', _.debounce(sendMail, 300, {
 *   'leading': true,
 *   'trailing': false
 * }));
 *
 * // Ensure `batchLog` is invoked once after 1 second of debounced calls.
 * var debounced = _.debounce(batchLog, 250, { 'maxWait': 1000 });
 * var source = new EventSource('/stream');
 * jQuery(source).on('message', debounced);
 *
 * // Cancel the trailing debounced invocation.
 * jQuery(window).on('popstate', debounced.cancel);
 */
function debounce(func, wait, options) {
  var lastArgs,
      lastThis,
      maxWait,
      result,
      timerId,
      lastCallTime,
      lastInvokeTime = 0,
      leading = false,
      maxing = false,
      trailing = true;

  if (typeof func != 'function') {
    throw new TypeError(FUNC_ERROR_TEXT);
  }
  wait = toNumber(wait) || 0;
  if (isObject(options)) {
    leading = !!options.leading;
    maxing = 'maxWait' in options;
    maxWait = maxing ? nativeMax(toNumber(options.maxWait) || 0, wait) : maxWait;
    trailing = 'trailing' in options ? !!options.trailing : trailing;
  }

  function invokeFunc(time) {
    var args = lastArgs,
        thisArg = lastThis;

    lastArgs = lastThis = undefined;
    lastInvokeTime = time;
    result = func.apply(thisArg, args);
    return result;
  }

  function leadingEdge(time) {
    // Reset any `maxWait` timer.
    lastInvokeTime = time;
    // Start the timer for the trailing edge.
    timerId = setTimeout(timerExpired, wait);
    // Invoke the leading edge.
    return leading ? invokeFunc(time) : result;
  }

  function remainingWait(time) {
    var timeSinceLastCall = time - lastCallTime,
        timeSinceLastInvoke = time - lastInvokeTime,
        result = wait - timeSinceLastCall;

    return maxing ? nativeMin(result, maxWait - timeSinceLastInvoke) : result;
  }

  function shouldInvoke(time) {
    var timeSinceLastCall = time - lastCallTime,
        timeSinceLastInvoke = time - lastInvokeTime;

    // Either this is the first call, activity has stopped and we're at the
    // trailing edge, the system time has gone backwards and we're treating
    // it as the trailing edge, or we've hit the `maxWait` limit.
    return (lastCallTime === undefined || (timeSinceLastCall >= wait) ||
      (timeSinceLastCall < 0) || (maxing && timeSinceLastInvoke >= maxWait));
  }

  function timerExpired() {
    var time = now();
    if (shouldInvoke(time)) {
      return trailingEdge(time);
    }
    // Restart the timer.
    timerId = setTimeout(timerExpired, remainingWait(time));
  }

  function trailingEdge(time) {
    timerId = undefined;

    // Only invoke if we have `lastArgs` which means `func` has been
    // debounced at least once.
    if (trailing && lastArgs) {
      return invokeFunc(time);
    }
    lastArgs = lastThis = undefined;
    return result;
  }

  function cancel() {
    if (timerId !== undefined) {
      clearTimeout(timerId);
    }
    lastInvokeTime = 0;
    lastArgs = lastCallTime = lastThis = timerId = undefined;
  }

  function flush() {
    return timerId === undefined ? result : trailingEdge(now());
  }

  function debounced() {
    var time = now(),
        isInvoking = shouldInvoke(time);

    lastArgs = arguments;
    lastThis = this;
    lastCallTime = time;

    if (isInvoking) {
      if (timerId === undefined) {
        return leadingEdge(lastCallTime);
      }
      if (maxing) {
        // Handle invocations in a tight loop.
        timerId = setTimeout(timerExpired, wait);
        return invokeFunc(lastCallTime);
      }
    }
    if (timerId === undefined) {
      timerId = setTimeout(timerExpired, wait);
    }
    return result;
  }
  debounced.cancel = cancel;
  debounced.flush = flush;
  return debounced;
}

/**
 * Checks if `value` is the
 * [language type](http://www.ecma-international.org/ecma-262/7.0/#sec-ecmascript-language-types)
 * of `Object`. (e.g. arrays, functions, objects, regexes, `new Number(0)`, and `new String('')`)
 *
 * @static
 * @memberOf _
 * @since 0.1.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is an object, else `false`.
 * @example
 *
 * _.isObject({});
 * // => true
 *
 * _.isObject([1, 2, 3]);
 * // => true
 *
 * _.isObject(_.noop);
 * // => true
 *
 * _.isObject(null);
 * // => false
 */
function isObject(value) {
  var type = typeof value;
  return !!value && (type == 'object' || type == 'function');
}

/**
 * Checks if `value` is object-like. A value is object-like if it's not `null`
 * and has a `typeof` result of "object".
 *
 * @static
 * @memberOf _
 * @since 4.0.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is object-like, else `false`.
 * @example
 *
 * _.isObjectLike({});
 * // => true
 *
 * _.isObjectLike([1, 2, 3]);
 * // => true
 *
 * _.isObjectLike(_.noop);
 * // => false
 *
 * _.isObjectLike(null);
 * // => false
 */
function isObjectLike(value) {
  return !!value && typeof value == 'object';
}

/**
 * Checks if `value` is classified as a `Symbol` primitive or object.
 *
 * @static
 * @memberOf _
 * @since 4.0.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is a symbol, else `false`.
 * @example
 *
 * _.isSymbol(Symbol.iterator);
 * // => true
 *
 * _.isSymbol('abc');
 * // => false
 */
function isSymbol(value) {
  return typeof value == 'symbol' ||
    (isObjectLike(value) && objectToString.call(value) == symbolTag);
}

/**
 * Converts `value` to a number.
 *
 * @static
 * @memberOf _
 * @since 4.0.0
 * @category Lang
 * @param {*} value The value to process.
 * @returns {number} Returns the number.
 * @example
 *
 * _.toNumber(3.2);
 * // => 3.2
 *
 * _.toNumber(Number.MIN_VALUE);
 * // => 5e-324
 *
 * _.toNumber(Infinity);
 * // => Infinity
 *
 * _.toNumber('3.2');
 * // => 3.2
 */
function toNumber(value) {
  if (typeof value == 'number') {
    return value;
  }
  if (isSymbol(value)) {
    return NAN;
  }
  if (isObject(value)) {
    var other = typeof value.valueOf == 'function' ? value.valueOf() : value;
    value = isObject(other) ? (other + '') : other;
  }
  if (typeof value != 'string') {
    return value === 0 ? value : +value;
  }
  value = value.replace(reTrim, '');
  var isBinary = reIsBinary.test(value);
  return (isBinary || reIsOctal.test(value))
    ? freeParseInt(value.slice(2), isBinary ? 2 : 8)
    : (reIsBadHex.test(value) ? NAN : +value);
}

module.exports = debounce;

/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(18)))

/***/ }),
/* 125 */
/***/ (function(module, exports, __webpack_require__) {

/* WEBPACK VAR INJECTION */(function(global) {/**
 * lodash (Custom Build) <https://lodash.com/>
 * Build: `lodash modularize exports="npm" -o ./`
 * Copyright jQuery Foundation and other contributors <https://jquery.org/>
 * Released under MIT license <https://lodash.com/license>
 * Based on Underscore.js 1.8.3 <http://underscorejs.org/LICENSE>
 * Copyright Jeremy Ashkenas, DocumentCloud and Investigative Reporters & Editors
 */

/** Used as the `TypeError` message for "Functions" methods. */
var FUNC_ERROR_TEXT = 'Expected a function';

/** Used as references for various `Number` constants. */
var NAN = 0 / 0;

/** `Object#toString` result references. */
var symbolTag = '[object Symbol]';

/** Used to match leading and trailing whitespace. */
var reTrim = /^\s+|\s+$/g;

/** Used to detect bad signed hexadecimal string values. */
var reIsBadHex = /^[-+]0x[0-9a-f]+$/i;

/** Used to detect binary string values. */
var reIsBinary = /^0b[01]+$/i;

/** Used to detect octal string values. */
var reIsOctal = /^0o[0-7]+$/i;

/** Built-in method references without a dependency on `root`. */
var freeParseInt = parseInt;

/** Detect free variable `global` from Node.js. */
var freeGlobal = typeof global == 'object' && global && global.Object === Object && global;

/** Detect free variable `self`. */
var freeSelf = typeof self == 'object' && self && self.Object === Object && self;

/** Used as a reference to the global object. */
var root = freeGlobal || freeSelf || Function('return this')();

/** Used for built-in method references. */
var objectProto = Object.prototype;

/**
 * Used to resolve the
 * [`toStringTag`](http://ecma-international.org/ecma-262/7.0/#sec-object.prototype.tostring)
 * of values.
 */
var objectToString = objectProto.toString;

/* Built-in method references for those with the same name as other `lodash` methods. */
var nativeMax = Math.max,
    nativeMin = Math.min;

/**
 * Gets the timestamp of the number of milliseconds that have elapsed since
 * the Unix epoch (1 January 1970 00:00:00 UTC).
 *
 * @static
 * @memberOf _
 * @since 2.4.0
 * @category Date
 * @returns {number} Returns the timestamp.
 * @example
 *
 * _.defer(function(stamp) {
 *   console.log(_.now() - stamp);
 * }, _.now());
 * // => Logs the number of milliseconds it took for the deferred invocation.
 */
var now = function() {
  return root.Date.now();
};

/**
 * Creates a debounced function that delays invoking `func` until after `wait`
 * milliseconds have elapsed since the last time the debounced function was
 * invoked. The debounced function comes with a `cancel` method to cancel
 * delayed `func` invocations and a `flush` method to immediately invoke them.
 * Provide `options` to indicate whether `func` should be invoked on the
 * leading and/or trailing edge of the `wait` timeout. The `func` is invoked
 * with the last arguments provided to the debounced function. Subsequent
 * calls to the debounced function return the result of the last `func`
 * invocation.
 *
 * **Note:** If `leading` and `trailing` options are `true`, `func` is
 * invoked on the trailing edge of the timeout only if the debounced function
 * is invoked more than once during the `wait` timeout.
 *
 * If `wait` is `0` and `leading` is `false`, `func` invocation is deferred
 * until to the next tick, similar to `setTimeout` with a timeout of `0`.
 *
 * See [David Corbacho's article](https://css-tricks.com/debouncing-throttling-explained-examples/)
 * for details over the differences between `_.debounce` and `_.throttle`.
 *
 * @static
 * @memberOf _
 * @since 0.1.0
 * @category Function
 * @param {Function} func The function to debounce.
 * @param {number} [wait=0] The number of milliseconds to delay.
 * @param {Object} [options={}] The options object.
 * @param {boolean} [options.leading=false]
 *  Specify invoking on the leading edge of the timeout.
 * @param {number} [options.maxWait]
 *  The maximum time `func` is allowed to be delayed before it's invoked.
 * @param {boolean} [options.trailing=true]
 *  Specify invoking on the trailing edge of the timeout.
 * @returns {Function} Returns the new debounced function.
 * @example
 *
 * // Avoid costly calculations while the window size is in flux.
 * jQuery(window).on('resize', _.debounce(calculateLayout, 150));
 *
 * // Invoke `sendMail` when clicked, debouncing subsequent calls.
 * jQuery(element).on('click', _.debounce(sendMail, 300, {
 *   'leading': true,
 *   'trailing': false
 * }));
 *
 * // Ensure `batchLog` is invoked once after 1 second of debounced calls.
 * var debounced = _.debounce(batchLog, 250, { 'maxWait': 1000 });
 * var source = new EventSource('/stream');
 * jQuery(source).on('message', debounced);
 *
 * // Cancel the trailing debounced invocation.
 * jQuery(window).on('popstate', debounced.cancel);
 */
function debounce(func, wait, options) {
  var lastArgs,
      lastThis,
      maxWait,
      result,
      timerId,
      lastCallTime,
      lastInvokeTime = 0,
      leading = false,
      maxing = false,
      trailing = true;

  if (typeof func != 'function') {
    throw new TypeError(FUNC_ERROR_TEXT);
  }
  wait = toNumber(wait) || 0;
  if (isObject(options)) {
    leading = !!options.leading;
    maxing = 'maxWait' in options;
    maxWait = maxing ? nativeMax(toNumber(options.maxWait) || 0, wait) : maxWait;
    trailing = 'trailing' in options ? !!options.trailing : trailing;
  }

  function invokeFunc(time) {
    var args = lastArgs,
        thisArg = lastThis;

    lastArgs = lastThis = undefined;
    lastInvokeTime = time;
    result = func.apply(thisArg, args);
    return result;
  }

  function leadingEdge(time) {
    // Reset any `maxWait` timer.
    lastInvokeTime = time;
    // Start the timer for the trailing edge.
    timerId = setTimeout(timerExpired, wait);
    // Invoke the leading edge.
    return leading ? invokeFunc(time) : result;
  }

  function remainingWait(time) {
    var timeSinceLastCall = time - lastCallTime,
        timeSinceLastInvoke = time - lastInvokeTime,
        result = wait - timeSinceLastCall;

    return maxing ? nativeMin(result, maxWait - timeSinceLastInvoke) : result;
  }

  function shouldInvoke(time) {
    var timeSinceLastCall = time - lastCallTime,
        timeSinceLastInvoke = time - lastInvokeTime;

    // Either this is the first call, activity has stopped and we're at the
    // trailing edge, the system time has gone backwards and we're treating
    // it as the trailing edge, or we've hit the `maxWait` limit.
    return (lastCallTime === undefined || (timeSinceLastCall >= wait) ||
      (timeSinceLastCall < 0) || (maxing && timeSinceLastInvoke >= maxWait));
  }

  function timerExpired() {
    var time = now();
    if (shouldInvoke(time)) {
      return trailingEdge(time);
    }
    // Restart the timer.
    timerId = setTimeout(timerExpired, remainingWait(time));
  }

  function trailingEdge(time) {
    timerId = undefined;

    // Only invoke if we have `lastArgs` which means `func` has been
    // debounced at least once.
    if (trailing && lastArgs) {
      return invokeFunc(time);
    }
    lastArgs = lastThis = undefined;
    return result;
  }

  function cancel() {
    if (timerId !== undefined) {
      clearTimeout(timerId);
    }
    lastInvokeTime = 0;
    lastArgs = lastCallTime = lastThis = timerId = undefined;
  }

  function flush() {
    return timerId === undefined ? result : trailingEdge(now());
  }

  function debounced() {
    var time = now(),
        isInvoking = shouldInvoke(time);

    lastArgs = arguments;
    lastThis = this;
    lastCallTime = time;

    if (isInvoking) {
      if (timerId === undefined) {
        return leadingEdge(lastCallTime);
      }
      if (maxing) {
        // Handle invocations in a tight loop.
        timerId = setTimeout(timerExpired, wait);
        return invokeFunc(lastCallTime);
      }
    }
    if (timerId === undefined) {
      timerId = setTimeout(timerExpired, wait);
    }
    return result;
  }
  debounced.cancel = cancel;
  debounced.flush = flush;
  return debounced;
}

/**
 * Creates a throttled function that only invokes `func` at most once per
 * every `wait` milliseconds. The throttled function comes with a `cancel`
 * method to cancel delayed `func` invocations and a `flush` method to
 * immediately invoke them. Provide `options` to indicate whether `func`
 * should be invoked on the leading and/or trailing edge of the `wait`
 * timeout. The `func` is invoked with the last arguments provided to the
 * throttled function. Subsequent calls to the throttled function return the
 * result of the last `func` invocation.
 *
 * **Note:** If `leading` and `trailing` options are `true`, `func` is
 * invoked on the trailing edge of the timeout only if the throttled function
 * is invoked more than once during the `wait` timeout.
 *
 * If `wait` is `0` and `leading` is `false`, `func` invocation is deferred
 * until to the next tick, similar to `setTimeout` with a timeout of `0`.
 *
 * See [David Corbacho's article](https://css-tricks.com/debouncing-throttling-explained-examples/)
 * for details over the differences between `_.throttle` and `_.debounce`.
 *
 * @static
 * @memberOf _
 * @since 0.1.0
 * @category Function
 * @param {Function} func The function to throttle.
 * @param {number} [wait=0] The number of milliseconds to throttle invocations to.
 * @param {Object} [options={}] The options object.
 * @param {boolean} [options.leading=true]
 *  Specify invoking on the leading edge of the timeout.
 * @param {boolean} [options.trailing=true]
 *  Specify invoking on the trailing edge of the timeout.
 * @returns {Function} Returns the new throttled function.
 * @example
 *
 * // Avoid excessively updating the position while scrolling.
 * jQuery(window).on('scroll', _.throttle(updatePosition, 100));
 *
 * // Invoke `renewToken` when the click event is fired, but not more than once every 5 minutes.
 * var throttled = _.throttle(renewToken, 300000, { 'trailing': false });
 * jQuery(element).on('click', throttled);
 *
 * // Cancel the trailing throttled invocation.
 * jQuery(window).on('popstate', throttled.cancel);
 */
function throttle(func, wait, options) {
  var leading = true,
      trailing = true;

  if (typeof func != 'function') {
    throw new TypeError(FUNC_ERROR_TEXT);
  }
  if (isObject(options)) {
    leading = 'leading' in options ? !!options.leading : leading;
    trailing = 'trailing' in options ? !!options.trailing : trailing;
  }
  return debounce(func, wait, {
    'leading': leading,
    'maxWait': wait,
    'trailing': trailing
  });
}

/**
 * Checks if `value` is the
 * [language type](http://www.ecma-international.org/ecma-262/7.0/#sec-ecmascript-language-types)
 * of `Object`. (e.g. arrays, functions, objects, regexes, `new Number(0)`, and `new String('')`)
 *
 * @static
 * @memberOf _
 * @since 0.1.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is an object, else `false`.
 * @example
 *
 * _.isObject({});
 * // => true
 *
 * _.isObject([1, 2, 3]);
 * // => true
 *
 * _.isObject(_.noop);
 * // => true
 *
 * _.isObject(null);
 * // => false
 */
function isObject(value) {
  var type = typeof value;
  return !!value && (type == 'object' || type == 'function');
}

/**
 * Checks if `value` is object-like. A value is object-like if it's not `null`
 * and has a `typeof` result of "object".
 *
 * @static
 * @memberOf _
 * @since 4.0.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is object-like, else `false`.
 * @example
 *
 * _.isObjectLike({});
 * // => true
 *
 * _.isObjectLike([1, 2, 3]);
 * // => true
 *
 * _.isObjectLike(_.noop);
 * // => false
 *
 * _.isObjectLike(null);
 * // => false
 */
function isObjectLike(value) {
  return !!value && typeof value == 'object';
}

/**
 * Checks if `value` is classified as a `Symbol` primitive or object.
 *
 * @static
 * @memberOf _
 * @since 4.0.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is a symbol, else `false`.
 * @example
 *
 * _.isSymbol(Symbol.iterator);
 * // => true
 *
 * _.isSymbol('abc');
 * // => false
 */
function isSymbol(value) {
  return typeof value == 'symbol' ||
    (isObjectLike(value) && objectToString.call(value) == symbolTag);
}

/**
 * Converts `value` to a number.
 *
 * @static
 * @memberOf _
 * @since 4.0.0
 * @category Lang
 * @param {*} value The value to process.
 * @returns {number} Returns the number.
 * @example
 *
 * _.toNumber(3.2);
 * // => 3.2
 *
 * _.toNumber(Number.MIN_VALUE);
 * // => 5e-324
 *
 * _.toNumber(Infinity);
 * // => Infinity
 *
 * _.toNumber('3.2');
 * // => 3.2
 */
function toNumber(value) {
  if (typeof value == 'number') {
    return value;
  }
  if (isSymbol(value)) {
    return NAN;
  }
  if (isObject(value)) {
    var other = typeof value.valueOf == 'function' ? value.valueOf() : value;
    value = isObject(other) ? (other + '') : other;
  }
  if (typeof value != 'string') {
    return value === 0 ? value : +value;
  }
  value = value.replace(reTrim, '');
  var isBinary = reIsBinary.test(value);
  return (isBinary || reIsOctal.test(value))
    ? freeParseInt(value.slice(2), isBinary ? 2 : 8)
    : (reIsBadHex.test(value) ? NAN : +value);
}

module.exports = throttle;

/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(18)))

/***/ }),
/* 126 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
var style = function style(element, prop) {
  return typeof getComputedStyle !== 'undefined' ? getComputedStyle(element, null).getPropertyValue(prop) : element.style[prop];
};

var overflow = function overflow(element) {
  return style(element, 'overflow') + style(element, 'overflow-y') + style(element, 'overflow-x');
};

var scrollParent = function scrollParent(element) {
  if (!(element instanceof HTMLElement)) {
    return window;
  }

  var parent = element;

  while (parent) {
    if (parent === document.body || parent === document.documentElement) {
      break;
    }

    if (!parent.parentNode) {
      break;
    }

    if (/(scroll|auto)/.test(overflow(parent))) {
      return parent;
    }

    parent = parent.parentNode;
  }

  return window;
};

exports.default = scrollParent;

/***/ }),
/* 127 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = inViewport;

var _getElementPosition = __webpack_require__(128);

var _getElementPosition2 = _interopRequireDefault(_getElementPosition);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var isHidden = function isHidden(element) {
  return element.offsetParent === null;
};

function inViewport(element, container, customOffset) {
  if (isHidden(element)) {
    return false;
  }

  var top = void 0;
  var bottom = void 0;
  var left = void 0;
  var right = void 0;

  if (typeof container === 'undefined' || container === window) {
    top = window.pageYOffset;
    left = window.pageXOffset;
    bottom = top + window.innerHeight;
    right = left + window.innerWidth;
  } else {
    var containerPosition = (0, _getElementPosition2.default)(container);

    top = containerPosition.top;
    left = containerPosition.left;
    bottom = top + container.offsetHeight;
    right = left + container.offsetWidth;
  }

  var elementPosition = (0, _getElementPosition2.default)(element);

  return top <= elementPosition.top + element.offsetHeight + customOffset.top && bottom >= elementPosition.top - customOffset.bottom && left <= elementPosition.left + element.offsetWidth + customOffset.left && right >= elementPosition.left - customOffset.right;
}

/***/ }),
/* 128 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = getElementPosition;
/*
* Finds element's position relative to the whole document,
* rather than to the viewport as it is the case with .getBoundingClientRect().
*/
function getElementPosition(element) {
  var rect = element.getBoundingClientRect();

  return {
    top: rect.top + window.pageYOffset,
    left: rect.left + window.pageXOffset
  };
}

/***/ }),
/* 129 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

var _reactLazyLoad = __webpack_require__(6);

var _reactLazyLoad2 = _interopRequireDefault(_reactLazyLoad);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var UserReviewSlug = function (_React$Component) {
	_inherits(UserReviewSlug, _React$Component);

	function UserReviewSlug(props) {
		_classCallCheck(this, UserReviewSlug);

		var _this = _possibleConstructorReturn(this, (UserReviewSlug.__proto__ || Object.getPrototypeOf(UserReviewSlug)).call(this, props));

		_this.state = {
			visible: true
		};
		_this.closeUserReviewSlug = _this.closeUserReviewSlug.bind(_this);
		return _this;
	}

	_createClass(UserReviewSlug, [{
		key: 'closeUserReviewSlug',
		value: function closeUserReviewSlug() {
			bwcache.set('showeditcmsreviewslug', '1', true);
			this.setState({
				visible: false
			});
		}
	}, {
		key: 'render',
		value: function render() {

			return _react2.default.createElement(
				'li',
				{ className: "list-item__slug " + (this.state.visible ? '' : 'hide'), id: 'writereviewslug' },
				_react2.default.createElement('span', { className: 'bwmsprite list-slug__close', onClick: this.closeUserReviewSlug }),
				_react2.default.createElement(
					'div',
					{ className: 'list-slug__wrapper' },
					_react2.default.createElement(
						'div',
						{ className: 'slug-content--top-align' },
						_react2.default.createElement(
							_reactLazyLoad2.default,
							null,
							_react2.default.createElement('img', { className: 'list-slug__image', src: 'https://imgd.aeplcdn.com/0x0/bw/static/sprites/m/review-contest-slug-120.png', alt: 'Bike Review Contest' })
						)
					),
					_react2.default.createElement(
						'div',
						{ className: 'list-slug__details slug-content--top-align' },
						_react2.default.createElement(
							'p',
							{ className: 'list-slug__heading' },
							'Bike review contest'
						),
						_react2.default.createElement(
							'p',
							{ className: 'list-slug__subheading' },
							'Write a detailed review & stand a chance to win Amazon vouchers worth ',
							_react2.default.createElement(
								'span',
								null,
								'\u20B9'
							),
							'\xA02,000'
						),
						_react2.default.createElement(
							'a',
							{ href: '/m/bike-review-contest/?csrc=13', title: 'Bike Review Contest', className: 'list-slug__target' },
							'Participate now',
							_react2.default.createElement('span', { className: 'bwmsprite target-arrow' })
						)
					)
				)
			);
		}
	}]);

	return UserReviewSlug;
}(_react2.default.Component);

module.exports = UserReviewSlug;

/***/ }),
/* 130 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
    value: true
});

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

var _reactRouterDom = __webpack_require__(2);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var getDisplayPageNos = function getDisplayPageNos(articleCount, articlePerPage, pageNo, maxNoOfPages) {

    if (maxNoOfPages <= 5) {
        var displayPageNos = [];
        for (var i = 1; i <= maxNoOfPages; i++) {
            displayPageNos.push(i);
        }return displayPageNos;
    }

    pageNo = parseInt(pageNo);
    if (pageNo <= 3) {
        return [1, 2, 3, 4, 5];
    }

    var displayPageNos = [pageNo - 2, pageNo - 1, pageNo];
    for (var i = pageNo + 1; i <= pageNo + 2 && i <= maxNoOfPages; i++) {
        displayPageNos.push(i);
    }return displayPageNos;
};

var NewsPagination = function (_React$Component) {
    _inherits(NewsPagination, _React$Component);

    function NewsPagination() {
        _classCallCheck(this, NewsPagination);

        return _possibleConstructorReturn(this, (NewsPagination.__proto__ || Object.getPrototypeOf(NewsPagination)).apply(this, arguments));
    }

    _createClass(NewsPagination, [{
        key: 'onClickLink',
        value: function onClickLink(pageNo) {

            this.props.updateArticleList(pageNo);
        }
    }, {
        key: 'getClassNameForPaginationArrow',
        value: function getClassNameForPaginationArrow(pageNo, comparePageNo) {
            if (pageNo == comparePageNo) return;
        }
    }, {
        key: 'renderPageList',
        value: function renderPageList(pageNo, displayPageNos) {

            return displayPageNos.map(function (displayPageNo) {
                if (displayPageNo == pageNo) {
                    return _react2.default.createElement(
                        'li',
                        { key: displayPageNo, className: 'active' },
                        pageNo
                    );
                } else {
                    return _react2.default.createElement(
                        'li',
                        { key: displayPageNo },
                        _react2.default.createElement(
                            _reactRouterDom.Link,
                            { to: '/m/' + this.props.pageUrl + '/page/' + displayPageNo + '/', onClick: this.onClickLink.bind(this, displayPageNo) },
                            displayPageNo
                        )
                    );
                }
            }.bind(this));
        }
    }, {
        key: 'renderPreviousPageArrow',
        value: function renderPreviousPageArrow(pageNo) {
            if (pageNo == 1) {
                return _react2.default.createElement(
                    'span',
                    { className: 'pagination-control-prev inactive' },
                    _react2.default.createElement(_reactRouterDom.Link, { to: '', className: 'bwmsprite bwsprite prev-page-icon' })
                );
            } else {
                var goToPageNo = parseInt(pageNo) - 1;
                return _react2.default.createElement(
                    'span',
                    { className: 'pagination-control-prev' },
                    _react2.default.createElement(_reactRouterDom.Link, { to: '/m/' + this.props.pageUrl + '/page/' + goToPageNo + '/', className: 'bwmsprite bwsprite prev-page-icon', onClick: this.onClickLink.bind(this, goToPageNo) })
                );
            }
        }
    }, {
        key: 'renderNextPageArrow',
        value: function renderNextPageArrow(pageNo, maxNoOfPages) {
            if (pageNo == maxNoOfPages) {
                return _react2.default.createElement(
                    'span',
                    { className: 'pagination-control-next inactive' },
                    _react2.default.createElement(_reactRouterDom.Link, { to: '', className: 'bwmsprite bwsprite next-page-icon' })
                );
            } else {
                var goToPageNo = parseInt(pageNo) + 1;
                return _react2.default.createElement(
                    'span',
                    { className: 'pagination-control-next' },
                    _react2.default.createElement(_reactRouterDom.Link, { to: '/m/' + this.props.pageUrl + '/page/' + goToPageNo + '/', className: 'bwmsprite bwsprite next-page-icon', onClick: this.onClickLink.bind(this, goToPageNo) })
                );
            }
        }
    }, {
        key: 'render',
        value: function render() {
            var _props = this.props,
                articleCount = _props.articleCount,
                endIndex = _props.endIndex,
                pageNo = _props.pageNo,
                startIndex = _props.startIndex,
                articlePerPage = _props.articlePerPage;

            var maxNoOfPages = Math.ceil(articleCount / articlePerPage);
            var displayPageNos = getDisplayPageNos(articleCount, articlePerPage, pageNo, maxNoOfPages);
            return _react2.default.createElement(
                'div',
                { className: 'pagination-container' },
                _react2.default.createElement(
                    'div',
                    { className: 'grid-5 omega' },
                    _react2.default.createElement(
                        'span',
                        { className: 'text-bold text-default' },
                        startIndex,
                        '-',
                        endIndex
                    ),
                    ' of ',
                    _react2.default.createElement(
                        'span',
                        { className: 'text-bold text-default' },
                        articleCount
                    ),
                    ' articles'
                ),
                _react2.default.createElement(
                    'div',
                    { className: 'pagination-list-content grid-7 alpha omega position-rel rightfloat' },
                    _react2.default.createElement(
                        'ul',
                        { className: 'pagination-list' },
                        this.renderPageList(pageNo, displayPageNos)
                    ),
                    this.renderPreviousPageArrow(pageNo),
                    this.renderNextPageArrow(pageNo, maxNoOfPages)
                ),
                _react2.default.createElement('div', { className: 'clear' })
            );
        }
    }]);

    return NewsPagination;
}(_react2.default.Component);

exports.default = NewsPagination;

/***/ }),
/* 131 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
    value: true
});

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

var _reactRouterDom = __webpack_require__(2);

var _propTypes = __webpack_require__(1);

var _propTypes2 = _interopRequireDefault(_propTypes);

var _SocialMediaSlug = __webpack_require__(57);

var _SocialMediaSlug2 = _interopRequireDefault(_SocialMediaSlug);

var _ArticleDetailContent = __webpack_require__(132);

var _ArticleDetailContent2 = _interopRequireDefault(_ArticleDetailContent);

var _ArticleDetailPagination = __webpack_require__(133);

var _ArticleDetailPagination2 = _interopRequireDefault(_ArticleDetailPagination);

var _ArticleDetailTitle = __webpack_require__(134);

var _ArticleDetailTitle2 = _interopRequireDefault(_ArticleDetailTitle);

var _SpinnerRelative = __webpack_require__(8);

var _SpinnerRelative2 = _interopRequireDefault(_SpinnerRelative);

var _Breadcrumb = __webpack_require__(23);

var _Breadcrumb2 = _interopRequireDefault(_Breadcrumb);

var _NewBikes = __webpack_require__(56);

var _NewBikes2 = _interopRequireDefault(_NewBikes);

var _ModelSlug = __webpack_require__(135);

var _ModelSlug2 = _interopRequireDefault(_ModelSlug);

var _Footer = __webpack_require__(24);

var _Footer2 = _interopRequireDefault(_Footer);

var _commonUtils = __webpack_require__(3);

var _contentTrackingUtils = __webpack_require__(136);

var _NewsCommon = __webpack_require__(22);

var _googleAdUtils = __webpack_require__(36);

var _scrollUtils = __webpack_require__(28);

var _popUpUtils = __webpack_require__(11);

var _timing = __webpack_require__(29);

var _AdUnit = __webpack_require__(21);

var _AdUnit2 = _interopRequireDefault(_AdUnit);

var _constants = __webpack_require__(4);

var _ArticleDetailImageCarousel = __webpack_require__(137);

var _ArticleDetailImageCarousel2 = _interopRequireDefault(_ArticleDetailImageCarousel);

var _CarouselBrand = __webpack_require__(153);

var _CarouselBrand2 = _interopRequireDefault(_CarouselBrand);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var ArticleDetail = function (_React$Component) {
    _inherits(ArticleDetail, _React$Component);

    function ArticleDetail(props) {
        _classCallCheck(this, ArticleDetail);

        var _this = _possibleConstructorReturn(this, (ArticleDetail.__proto__ || Object.getPrototypeOf(ArticleDetail)).call(this, props));

        if ((0, _commonUtils.isServer)()) {
            if (_this.props.ArticleDetailData && _this.props.ArticleDetailData.ArticleDetail) {
                _this.props.ArticleDetailData.Status = _constants.Status.Fetched;
            }
            if (_this.props.NewBikesListData && _this.props.NewBikesListData.NewBikesList) {
                _this.props.NewBikesListData.Status = _constants.Status.Fetched;
            }
            if (_this.props.RelatedModelObject && _this.props.RelatedModelObject.ModelObject) {
                _this.props.RelatedModelObject.Status = _constants.Status.Fetched;
            }
        }

        var globalCity = (0, _popUpUtils.getGlobalCity)();
        _this.globalCityName = globalCity && globalCity.name.length > 0 ? globalCity.name : '';
        _this.pageCategory = (0, _NewsCommon.extractPageCategoryFromURL)();
        _this.extractBasicIdFromArticleUrl = _this.extractBasicIdFromArticleUrl.bind(_this);
        if (typeof gaObj != "undefined") {
            gaObj = _constants.GA_PAGE_MAPPING["DetailsPage"];
        }
        return _this;
    }

    _createClass(ArticleDetail, [{
        key: 'componentDidUpdate',
        value: function componentDidUpdate() {
            var basicIdFromData = this.props.ArticleDetailData && this.props.ArticleDetailData.ArticleDetail ? this.props.ArticleDetailData.ArticleDetail.BasicId : null;
            var basicIdFromUrl = this.props.match.params["basicId"] ? this.props.match.params["basicId"] : -1;
            if (basicIdFromData == basicIdFromUrl) {
                this.logger();
                this.scrollToPosition();
            }
        }
    }, {
        key: 'componentDidMount',
        value: function componentDidMount() {

            this.logger();

            var basicId = this.extractBasicIdFromArticleUrl();
            if (!basicId) {
                return;
            }
            _contentTrackingUtils.ContentTracking.tracking.setUpTracking(1, 'content-details');
            _contentTrackingUtils.ContentTracking.page.registerEvent();

            if (this.props.ArticleDetailData && this.props.ArticleDetailData.Status == _constants.Status.Fetched) {
                // data to be further rendered after server render
                if (this.props.RelatedModelObject && this.props.RelatedModelObject.Status !== _constants.Status.Fetched) {
                    this.props.fetchRelatedModelObject(basicId);
                }
                if (this.props.NewBikesListData && this.props.NewBikesListData.Status !== _constants.Status.Fetched) {
                    this.props.fetchNewBikesListData(basicId);
                }
            } else {
                // data that is purely client rendered
                var articleInitialData = this.props.ArticleDetailData.InitialDataDict == null ? null : this.props.ArticleDetailData.InitialDataDict[window.location.pathname];
                if (!articleInitialData) {
                    this.props.fetchArticleDetail(basicId); //initialdata is never null as initialData is always set from previous page or entire store is server rendered
                }
                //if initialdata != null then fetchArticleDetail already performed by previous page
                this.props.fetchRelatedModelObject(basicId);
                this.props.fetchNewBikesListData(basicId);
            }

            if ((0, _scrollUtils.isBrowserWithoutScrollSupport)()) {
                window.scrollTo(0, 0);
            }
        }
    }, {
        key: 'componentWillReceiveProps',
        value: function componentWillReceiveProps(nextProps) {
            try {
                var prevUrlParam = this.props.match.params;
                var nextUrlParam = nextProps.match.params;
                //componentWillRecieveProps is called on first load in UC Browser and iOS Chrome, not in other browsers
                if (prevUrlParam["basicId"] === nextUrlParam["basicId"]) {
                    // condition 1 : new url has been pushed
                    if (nextProps.ArticleDetailData && nextProps.ArticleDetailData.ArticleDetail) nextProps.ArticleDetailData.InitialDataDict[window.location.pathname] = nextProps.ArticleDetailData.ArticleDetail;
                    return;
                }
                var newHashValue = this.props.location.hash;
                var oldHashValue = nextProps.location.hash;
                if (newHashValue && newHashValue.indexOf('#') >= 0 || oldHashValue && oldHashValue.indexOf('#') >= 0) {
                    // condition 2 :  global city popup  clicked -- should not call apis
                    return;
                }

                if (nextProps && nextProps.ArticleDetailData.Status != _constants.Status.IsFetching) {
                    // condition 3 : back / forward button has been clicked, so all the dataobjects have fetched state from previous url. hence now new api have to be hit
                    var articleInitialData = nextProps.ArticleDetailData.InitialDataDict[window.location.pathname];
                    var basicId = articleInitialData == null ? this.extractBasicIdFromArticleUrl() : articleInitialData.BasicId;
                    if (!articleInitialData) {
                        // not previously visited
                        nextProps.fetchArticleDetail(basicId);
                    } else {
                        nextProps.fetchArticleDetail(articleInitialData);
                    }
                    nextProps.fetchRelatedModelObject(basicId);
                    nextProps.fetchNewBikesListData(basicId);
                    if ((0, _scrollUtils.isBrowserWithoutScrollSupport)()) {
                        window.scrollTo(0, 0);
                    }
                }
            } catch (e) {}
        }
    }, {
        key: 'scrollToPosition',
        value: function scrollToPosition() {
            if (_scrollUtils.scrollPosition.x >= 0 && _scrollUtils.scrollPosition.y >= 0) {
                // needs to be scrolled
                if (this.props.ArticleDetailData && this.props.ArticleDetailData.Status == _constants.Status.Fetched) {
                    // checks whether ready to scroll
                    window.scrollTo(_scrollUtils.scrollPosition.x, _scrollUtils.scrollPosition.y);
                    (0, _scrollUtils.resetScrollPosition)();
                }
            }
        }
    }, {
        key: 'logger',
        value: function logger() {
            try {
                if (this.props && this.props.ArticleDetailData.Status == _constants.Status.Fetched && this.props.RelatedModelObject.Status == _constants.Status.Fetched && this.props.NewBikesListData.Status == _constants.Status.Fetched) {
                    (0, _timing.endTimer)("component-render");
                }
            } catch (err) {}
        }
    }, {
        key: 'extractBasicIdFromArticleUrl',
        value: function extractBasicIdFromArticleUrl() {
            var basicId, regexp;
            var url = window.location.pathname;
            if (this.pageCategory === "news") {
                regexp = /\/m\/news\/(\d+)-.*\.html/;
            } else {
                regexp = /\/m\/expert-reviews\/.*-(\d+)\.html/;
            }
            var matches = url.match(regexp);
            if (matches) {
                return matches[1];
            }
        }
    }, {
        key: 'onArticlePaginationClickEvent',
        value: function onArticlePaginationClickEvent(articleInitialData) {
            this.props.history.push(articleInitialData.ArticleUrl);
            this.props.fetchArticleDetail(articleInitialData);
            this.props.fetchNewBikesListData(articleInitialData.BasicId);
            this.props.fetchRelatedModelObject(articleInitialData.BasicId);
            if ((0, _scrollUtils.isBrowserWithoutScrollSupport)()) {
                window.scrollTo(0, 0);
            }
        }
    }, {
        key: 'renderImage',
        value: function renderImage(title, src) {
            return _react2.default.createElement(
                'div',
                { className: 'article-content-image-wrapper' },
                _react2.default.createElement('img', { alt: title, title: title, src: src })
            );
        }
    }, {
        key: 'renderArticleContent',
        value: function renderArticleContent(articleDetail, initialData) {
            if (articleDetail) {
                var imageUrl = !articleDetail.HostUrl || !articleDetail.LargePicUrl ? 'https://imgd.aeplcdn.com/640x348/bikewaleimg/images/noimage.png?q=70' : articleDetail.HostUrl + articleDetail.LargePicUrl;
                var imageTag = this.pageCategory === "news" ? this.renderImage(articleDetail.Title, imageUrl) : null;
                var bottomContent, imageCarousel;
                var eventLable = this.pageCategory === "news" ? "News_Detail" : "Expert_Review_Detail";

                if (articleDetail.BottomContent) {
                    bottomContent = _react2.default.createElement(_ArticleDetailContent2.default, { htmlContent: articleDetail.BottomContent });
                }
                if (articleDetail.ImageGallery) {
                    imageCarousel = _react2.default.createElement(_ArticleDetailImageCarousel2.default, { imageGallery: articleDetail.ImageGallery, title: articleDetail.Title });
                }
                return _react2.default.createElement(
                    'div',
                    null,
                    _react2.default.createElement(
                        'div',
                        { className: 'content-details related-details', 'data-id': initialData.BasicId, 'data-category-id': initialData.CategoryId, 'data-event-label': eventLable },
                        _react2.default.createElement(
                            'div',
                            { className: 'article-content' },
                            imageTag,
                            _react2.default.createElement(_ArticleDetailContent2.default, { htmlContent: articleDetail.TopContent }),
                            this.renderModelSlug(),
                            bottomContent
                        )
                    ),
                    _react2.default.createElement(_SocialMediaSlug2.default, null),
                    imageCarousel,
                    _react2.default.createElement(_ArticleDetailPagination2.default, { prevArticle: articleDetail.PrevArticle, nextArticle: articleDetail.NextArticle, onArticlePaginationClickEvent: this.onArticlePaginationClickEvent.bind(this) })
                );
            } else {
                return _react2.default.createElement(_SpinnerRelative2.default, null);
            }
        }
    }, {
        key: 'renderNewBikesList',
        value: function renderNewBikesList() {
            if (!this.props.NewBikesListData || this.props.NewBikesListData.Status !== _constants.Status.Fetched || !this.props.NewBikesListData.NewBikesList || this.props.NewBikesListData.NewBikesList.length === 0) {
                return false;
            }
            return this.props.NewBikesListData.NewBikesList.map(function (newBikes) {
                return _react2.default.createElement(_NewBikes2.default, { key: newBikes.Heading, newBikesData: newBikes });
            });
        }
    }, {
        key: 'renderModelSlug',
        value: function renderModelSlug() {
            if (!this.props.RelatedModelObject || this.props.RelatedModelObject.Status !== _constants.Status.Fetched || !this.props.RelatedModelObject.ModelObject) return false;
            return _react2.default.createElement(_ModelSlug2.default, { modelInfo: this.props.RelatedModelObject });
        }
    }, {
        key: 'renderBreadcrumb',
        value: function renderBreadcrumb(title) {
            if (this.props.ArticleDetailData && this.props.ArticleDetailData.Status == _constants.Status.Fetched) {
                var pageCategory = (0, _NewsCommon.extractPageCategoryFromURL)();
                if (pageCategory === "news") {
                    return _react2.default.createElement(_Breadcrumb2.default, { breadcrumb: [{ Href: '/m/', Title: 'Home' }, { Href: '/m/news/', Title: 'News', isReactLink: true }, { Href: '', Title: title }] });
                } else {
                    return _react2.default.createElement(_Breadcrumb2.default, { breadcrumb: [{ Href: '/m/', Title: 'Home' }, { Href: '/m/expert-reviews/', Title: 'Expert Reviews', isReactLink: true }, { Href: '', Title: title }] });
                }
            } else {
                return false;
            }
        }
    }, {
        key: 'renderFooter',
        value: function renderFooter() {
            if (this.props.ArticleDetailData && this.props.ArticleDetailData.Status == _constants.Status.Fetched) {
                return _react2.default.createElement(_Footer2.default, null);
            } else {
                return false;
            }
        }
    }, {
        key: 'renderPopularBrandList',
        value: function renderPopularBrandList() {
            if (!this.props.NewBikesListData || this.props.NewBikesListData.Status !== _constants.Status.Fetched || !this.props.NewBikesListData.BikeMakeList || this.props.NewBikesListData.BikeMakeList.length === 0) {
                return false;
            }
            return _react2.default.createElement(_CarouselBrand2.default, { brandList: this.props.NewBikesListData.BikeMakeList[0] });
        }
    }, {
        key: 'render',
        value: function render() {

            var componentData = this.props.ArticleDetailData;
            if (!componentData) {
                return null;
            }
            var articleInitialData;

            var loadingState = _react2.default.createElement(
                'div',
                null,
                _react2.default.createElement(_SpinnerRelative2.default, null)
            );

            if (!componentData.InitialDataDict) {
                articleInitialData = null;
            } else {
                var url = window.location.pathname;
                articleInitialData = componentData.InitialDataDict[url];
            }

            if (!articleInitialData) {

                if (componentData.Status == _constants.Status.Reset || componentData.Status == _constants.Status.IsFetching || componentData.Status == _constants.Status.Error) {
                    return loadingState;
                }

                //no initial data -> hard refresh
                articleInitialData = (0, _NewsCommon.mapNewsArticleDataToInitialData)(componentData.ArticleDetail);
                if (!articleInitialData) {
                    // articleDetail does not have data
                    return loadingState;
                }
            }

            var articleDetail = componentData.ArticleDetail;
            var adSlotTop = null;
            var adSlotBottom = null;

            if (articleDetail) {
                var targetTags = {
                    City: this.globalCityName,
                    Make: articleDetail.MakeName,
                    Model: articleDetail.ModelName,
                    Tags: articleDetail.Tags
                };

                adSlotTop = _react2.default.createElement(_AdUnit2.default, { uniqueKey: articleDetail.Title, tags: targetTags, adSlot: _constants.AD_PATH_NEWS_MOBILE_TOP_320_50, adDimension: _constants.AD_DIMENSION_320_50, adContainerId: _constants.AD_DIV_REVIEWS_TOP_320_50 });

                adSlotBottom = _react2.default.createElement(_AdUnit2.default, { uniqueKey: articleDetail.Title, tags: targetTags, adSlot: _constants.AD_PATH_NEWS_MOBILE_BOTTOM_320_50, adDimension: _constants.AD_DIMENSION_320_50, adContainerId: _constants.AD_DIV_REVIEWS_BOTTOM_320_50 });
            }

            var documentTitle = articleInitialData.Title == "" ? "BikeWale" : articleInitialData.Title + " - BikeWale";

            //To be changed

            return _react2.default.createElement(
                'div',
                null,
                adSlotTop,
                _react2.default.createElement(
                    'div',
                    { className: 'container bg-white box-shadow section-bottom-margin article-details-container' },
                    _react2.default.createElement(_ArticleDetailTitle2.default, { title: articleInitialData.Title, authorName: articleInitialData.AuthorName, authorMaskingName: articleInitialData.AuthorMaskingName, displayDate: articleInitialData.DisplayDateTime }),
                    _react2.default.createElement(
                        'div',
                        { className: 'article-content-padding' },
                        this.renderArticleContent(articleDetail, articleInitialData)
                    )
                ),
                this.renderNewBikesList(),
                this.renderPopularBrandList(),
                _react2.default.createElement(
                    'div',
                    { className: 'margin-bottom15' },
                    adSlotBottom
                ),
                this.renderBreadcrumb(articleInitialData.Title),
                this.renderFooter()
            );
        }
    }]);

    return ArticleDetail;
}(_react2.default.Component);

exports.default = (0, _reactRouterDom.withRouter)(ArticleDetail);

/***/ }),
/* 132 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

var _SpinnerRelative = __webpack_require__(8);

var _SpinnerRelative2 = _interopRequireDefault(_SpinnerRelative);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var ArticleDetailContent = function (_React$Component) {
	_inherits(ArticleDetailContent, _React$Component);

	function ArticleDetailContent() {
		_classCallCheck(this, ArticleDetailContent);

		return _possibleConstructorReturn(this, (ArticleDetailContent.__proto__ || Object.getPrototypeOf(ArticleDetailContent)).apply(this, arguments));
	}

	_createClass(ArticleDetailContent, [{
		key: 'render',
		value: function render() {
			if (this.props.htmlContent) {
				return _react2.default.createElement('div', { dangerouslySetInnerHTML: { __html: this.props.htmlContent } });
			} else return _react2.default.createElement(_SpinnerRelative2.default, null);

			return false;
		}
	}]);

	return ArticleDetailContent;
}(_react2.default.Component);

module.exports = ArticleDetailContent;

/***/ }),
/* 133 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

var _reactRouterDom = __webpack_require__(2);

var _NewsCommon = __webpack_require__(22);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var ArticleDetailPagination = function (_React$Component) {
	_inherits(ArticleDetailPagination, _React$Component);

	function ArticleDetailPagination() {
		_classCallCheck(this, ArticleDetailPagination);

		return _possibleConstructorReturn(this, (ArticleDetailPagination.__proto__ || Object.getPrototypeOf(ArticleDetailPagination)).apply(this, arguments));
	}

	_createClass(ArticleDetailPagination, [{
		key: 'onArticlePaginationClickEvent',
		value: function onArticlePaginationClickEvent(article, event) {
			event.preventDefault();

			var articleInitialData = (0, _NewsCommon.mapNewsArticleDataToInitialData)(article);
			this.props.onArticlePaginationClickEvent(articleInitialData);
		}
	}, {
		key: 'renderLinkContent',
		value: function renderLinkContent(article, articleTypeText) {
			if (articleTypeText == "Previous") {
				return _react2.default.createElement(
					'div',
					null,
					_react2.default.createElement('span', { className: 'bwmsprite prev-arrow' }),
					_react2.default.createElement(
						'div',
						{ className: 'next-prev-article-box inline-block padding-left5' },
						_react2.default.createElement(
							'span',
							{ className: 'font12 text-light' },
							'Previous'
						),
						_react2.default.createElement('br', null),
						_react2.default.createElement(
							'span',
							{ className: 'next-prev-article-title next-page-title' },
							article.Title
						)
					)
				);
			} else if (articleTypeText == "Next") {
				return _react2.default.createElement(
					'div',
					null,
					_react2.default.createElement(
						'div',
						{ className: 'next-prev-article-box inline-block padding-right5' },
						_react2.default.createElement(
							'span',
							{ className: 'font12 text-light' },
							'Next'
						),
						_react2.default.createElement(
							'span',
							{ className: 'next-prev-article-title next-page-title' },
							article.Title
						)
					),
					_react2.default.createElement('span', { className: 'bwmsprite next-arrow' })
				);
			}
		}
	}, {
		key: 'renderLinkForAdjacentArticle',
		value: function renderLinkForAdjacentArticle(article, articleTypeText) {
			if (!article) {
				return false;
			}
			if ((0, _NewsCommon.isReactCategory)(article.CategoryName)) {
				return _react2.default.createElement(
					_reactRouterDom.Link,
					{ to: article.ArticleUrl, title: article.Title, className: 'text-default next-prev-article-target', onClick: this.onArticlePaginationClickEvent.bind(this, article) },
					this.renderLinkContent(article, articleTypeText)
				);
			} else {
				return _react2.default.createElement(
					'a',
					{ href: article.ArticleUrl, title: article.Title, className: 'text-default next-prev-article-target' },
					this.renderLinkContent(article, articleTypeText)
				);
			}
		}
	}, {
		key: 'render',
		value: function render() {
			var prevArticle = this.props.prevArticle;
			var nextArticle = this.props.nextArticle;

			return _react2.default.createElement(
				'div',
				{ className: 'border-solid-top padding-top10' },
				_react2.default.createElement(
					'div',
					{ className: 'grid-6 alpha border-solid-right' },
					this.renderLinkForAdjacentArticle(prevArticle, "Previous")
				),
				_react2.default.createElement(
					'div',
					{ className: 'grid-6 omega rightfloat' },
					this.renderLinkForAdjacentArticle(nextArticle, "Next")
				),
				_react2.default.createElement('div', { className: 'clear' })
			);
		}
	}]);

	return ArticleDetailPagination;
}(_react2.default.Component);

module.exports = ArticleDetailPagination;

/***/ }),
/* 134 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var ArticleDetailTitle = function (_React$Component) {
	_inherits(ArticleDetailTitle, _React$Component);

	function ArticleDetailTitle() {
		_classCallCheck(this, ArticleDetailTitle);

		return _possibleConstructorReturn(this, (ArticleDetailTitle.__proto__ || Object.getPrototypeOf(ArticleDetailTitle)).apply(this, arguments));
	}

	_createClass(ArticleDetailTitle, [{
		key: 'render',
		value: function render() {
			var authorLink = "";
			if (this.props.authorMaskingName) {
				authorLink = _react2.default.createElement(
					'a',
					{ href: '/m/authors/' + this.props.authorMaskingName + '/' },
					_react2.default.createElement('span', { className: 'bwmsprite author-grey-sm-icon' }),
					_react2.default.createElement(
						'span',
						{ className: 'article-stats-content margin-left5 text-link' },
						this.props.authorName
					)
				);
			} else {
				authorLink = _react2.default.createElement(
					'div',
					null,
					_react2.default.createElement('span', { className: 'bwmsprite author-grey-sm-icon' }),
					_react2.default.createElement(
						'span',
						{ className: 'article-stats-content' },
						this.props.authorName
					)
				);
			}
			return _react2.default.createElement(
				'div',
				{ className: 'box-shadow article-head card-heading' },
				_react2.default.createElement(
					'h1',
					{ className: 'margin-bottom10' },
					this.props.title
				),
				_react2.default.createElement(
					'div',
					{ className: 'grid-6 alpha' },
					_react2.default.createElement('span', { className: 'bwmsprite calender-grey-sm-icon' }),
					_react2.default.createElement(
						'span',
						{ className: 'article-stats-content' },
						this.props.displayDate
					)
				),
				_react2.default.createElement(
					'div',
					{ className: 'grid-6 alpha padding-right5' },
					authorLink
				),
				_react2.default.createElement('div', { className: 'clear' })
			);
		}
	}]);

	return ArticleDetailTitle;
}(_react2.default.Component);

module.exports = ArticleDetailTitle;

/***/ }),
/* 135 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

var _constants = __webpack_require__(4);

var _reactLazyLoad = __webpack_require__(6);

var _reactLazyLoad2 = _interopRequireDefault(_reactLazyLoad);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var ModelSlug = function (_React$Component) {
	_inherits(ModelSlug, _React$Component);

	function ModelSlug() {
		_classCallCheck(this, ModelSlug);

		return _possibleConstructorReturn(this, (ModelSlug.__proto__ || Object.getPrototypeOf(ModelSlug)).apply(this, arguments));
	}

	_createClass(ModelSlug, [{
		key: 'renderUsedBikesLink',
		value: function renderUsedBikesLink() {
			if (!this.props.modelInfo || !this.props.modelInfo.ModelObject || !this.props.modelInfo.ModelObject.UsedBikesLink) {
				return false;
			}
			var usedBikesLink = this.props.modelInfo.ModelObject.UsedBikesLink;
			return _react2.default.createElement(
				'div',
				null,
				_react2.default.createElement('div', { className: 'border-solid-bottom margin-top5 margin-bottom10' }),
				_react2.default.createElement(
					'a',
					{ href: usedBikesLink.UsedBikesLinkUrl, title: 'Used', className: 'block text-default' },
					_react2.default.createElement(
						'span',
						{ className: 'used-target-label inline-block' },
						_react2.default.createElement(
							'span',
							{ className: 'font14 text-bold' },
							usedBikesLink.DescriptionLabel
						),
						_react2.default.createElement('br', null),
						_react2.default.createElement(
							'span',
							{ className: 'font12 text-light-grey' },
							usedBikesLink.PricePrefix,
							' ',
							_react2.default.createElement(
								'span',
								null,
								'\u20B9'
							),
							' ',
							usedBikesLink.Price
						)
					),
					_react2.default.createElement('span', { className: 'bwmsprite next-grey-icon' })
				)
			);
		}
	}, {
		key: 'getClassNameForBikeInfo',
		value: function getClassNameForBikeInfo(type) {
			switch (type) {
				case 'Expert Reviews':
					return 'reviews-sm';
				case 'Images':
					return 'photos-sm';
				case 'Specs':
					return 'specs-sm';
				case 'News':
					return 'news-sm';
				case 'Videos':
					return 'videos-sm';
				case 'Dealers':
					return 'dealers-sm';
				case 'User Reviews':
					return 'user-reviews-sm';
			}
			return '';
		}
	}, {
		key: 'renderMoreDetailsListItem',
		value: function renderMoreDetailsListItem(item) {
			if (!item) return false;
			var className = this.getClassNameForBikeInfo(item.Type);
			if (className == '') return false;
			return _react2.default.createElement(
				'li',
				{ key: item.Type },
				_react2.default.createElement(
					'a',
					{ href: item.DetailUrl, title: item.Title },
					_react2.default.createElement('span', { className: "bwmsprite " + className }),
					_react2.default.createElement(
						'span',
						{ className: 'icon-label' },
						item.Type
					)
				)
			);
		}
	}, {
		key: 'renderMoreDetailsList',
		value: function renderMoreDetailsList() {
			if (!this.props.modelInfo || !this.props.modelInfo.ModelObject || !this.props.modelInfo.ModelObject.MoreDetailsList) {
				return false;
			}

			var moreDetailsList = this.props.modelInfo.ModelObject.MoreDetailsList;
			return _react2.default.createElement(
				'ul',
				{ className: 'item-more-details-list' },
				moreDetailsList.map(function (item) {
					return this.renderMoreDetailsListItem(item);
				}.bind(this))
			);
		}
	}, {
		key: 'render',
		value: function render() {
			if (!this.props.modelInfo || this.props.modelInfo.Status !== _constants.Status.Fetched || !this.props.modelInfo.ModelObject) {
				return false;
			}
			var modelObject = this.props.modelInfo.ModelObject;
			if (!modelObject) {
				return false;
			}
			var ribbonTagComponent = null;
			if (modelObject.Upcoming.toLowerCase() == "true") ribbonTagComponent = _react2.default.createElement(
				'p',
				{ className: 'model-ribbon-tag upcoming-ribbon' },
				'Upcoming'
			);else if (modelObject.Discontinued.toLowerCase() == "true") ribbonTagComponent = _react2.default.createElement(
				'p',
				{ className: 'model-ribbon-tag discontinued-ribbon' },
				'Discontinued'
			);

			return _react2.default.createElement(
				'div',
				{ className: 'model-more-info-section model-slug-type-news' },
				_react2.default.createElement(
					'div',
					{ className: 'margin-bottom10' },
					ribbonTagComponent,
					_react2.default.createElement('div', { className: 'clear' }),
					_react2.default.createElement(
						'a',
						{ href: modelObject.ModelDetailUrl, className: 'item-image-content vertical-top', title: modelObject.ModelName },
						_react2.default.createElement(
							_reactLazyLoad2.default,
							{ offsetVertical: 0 },
							_react2.default.createElement('img', { src: modelObject.ImageUrl, alt: modelObject.ModelName })
						)
					),
					_react2.default.createElement(
						'div',
						{ className: 'bike-details-block vertical-top' },
						_react2.default.createElement(
							'a',
							{ href: modelObject.ModelDetailUrl, className: 'block margin-bottom5', title: modelObject.ModelName },
							_react2.default.createElement(
								'h3',
								{ className: 'text-truncate' },
								modelObject.ModelName
							)
						),
						_react2.default.createElement(
							'p',
							{ className: 'key-size-12 text-truncate' },
							modelObject.PriceDescription
						),
						_react2.default.createElement(
							'div',
							{ className: 'value-size-18' },
							_react2.default.createElement(
								'span',
								null,
								'\u20B9'
							),
							_react2.default.createElement(
								'span',
								null,
								' ',
								modelObject.Price
							)
						)
					)
				),
				this.renderMoreDetailsList(),
				_react2.default.createElement('div', { className: 'clear' }),
				this.renderUsedBikesLink()
			);
		}
	}]);

	return ModelSlug;
}(_react2.default.Component);

module.exports = ModelSlug;

/***/ }),
/* 136 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var _analyticsUtils = __webpack_require__(5);

var _constants = __webpack_require__(4);

var ContentTracking = {
    contentElement: undefined,
    isDetails: false,
    category: undefined,
    isScroll: false,
    trackingStatus: {},
    pageviewStatus: {},
    trackList: [0, 10, 25, 50, 75, 100],
    articleUrl: undefined,

    page: {

        getBasicInfo: function getBasicInfo(element, len) {

            var currentEle = element[len];
            var categoryId = currentEle.getAttribute('data-category-id');
            var Id = currentEle.getAttribute('data-id');
            var eventLabel = currentEle.getAttribute('data-event-label') + '_' + Id;
            if (typeof gaObj != "undefined") {
                gaObj = _constants.GA_PAGE_MAPPING["DetailsPage"];
            }
            var eventCategory = gaObj.name;
            return { id: Id, categoryId: categoryId, eventLabel: eventLabel, eventCategory: eventCategory };
        },

        visibleContent: function visibleContent(contentTopPosition, contentHeight) {
            var visibleHeight = document.documentElement.clientHeight - contentTopPosition;
            return visibleHeight > contentHeight ? contentHeight : visibleHeight;
        },

        contentFlow: function contentFlow(percentage, basicId, eventLabel, eventCategory) {
            var j = 0,
                checkpoint;
            for (var i = ContentTracking.trackList.length - 1; i >= 0; i--) {
                checkpoint = ContentTracking.trackList[i];
                if (checkpoint <= percentage && !ContentTracking.trackingStatus[basicId][checkpoint]) {
                    j = i;
                    break;
                }
            }
            for (var i = j; i >= 0; i--) {
                checkpoint = ContentTracking.trackList[i];
                if (!ContentTracking.trackingStatus[basicId][checkpoint]) {
                    ContentTracking.trackingStatus[basicId][checkpoint] = true;
                    var eventAction = checkpoint === 0 ? "Page_Load" : checkpoint.toString() + "_Viewed";
                    (0, _analyticsUtils.triggerNonInteractiveGA)(eventCategory, eventAction, eventLabel);
                }
            }
        },

        percetageStatus: function percetageStatus() {
            var perstatus = undefined;
            if (ContentTracking.isDetails) {
                perstatus = {
                    0: false, 10: false, 25: false, 50: false, 75: false, 100: false
                };
            }
            return perstatus;
        },

        registerEvent: function registerEvent() {

            window.addEventListener('scroll', function () {
                ContentTracking.tracking.start();
            });

            window.addEventListener('load', function () {
                ContentTracking.tracking.start();
                ContentTracking.tracking.setDefaultIdInLocalStorage(document.getElementsByClassName('content-details')[0].getAttribute('data-id'));
            });
        }
    },

    tracking: {

        setUpTracking: function setUpTracking(isDetails, contentElement) {

            ContentTracking.contentElement = contentElement;
            ContentTracking.isDetails = isDetails;
        },

        createObject: function createObject(Id) {
            if (ContentTracking.trackingStatus[Id] === undefined) {
                ContentTracking.trackingStatus[Id] = ContentTracking.page.percetageStatus();
            }
        },

        start: function start() {
            var articleObj = document.getElementsByClassName(ContentTracking.contentElement);
            if (articleObj.length > 0) {
                var objLen = articleObj.length - 1;
                var content = ContentTracking.page.getBasicInfo(articleObj, objLen);
                ContentTracking.tracking.createObject(content.id + '_' + content.categoryId);
                if (!ContentTracking.trackingStatus[content.id + '_' + content.categoryId][100]) {
                    var indexArticleObj = articleObj[objLen];
                    var contentTopPosition = indexArticleObj.getBoundingClientRect().top - 60;
                    var contentHeight = indexArticleObj.clientHeight;
                    var visibleHeight = ContentTracking.page.visibleContent(contentTopPosition, contentHeight);
                    var eventLabel = content.eventLabel;
                    var eventCategory = content.eventCategory;
                    ContentTracking.page.contentFlow(visibleHeight * 100 / contentHeight, content.id + "_" + content.categoryId, eventLabel, eventCategory);
                }
            }
        },
        setDefaultIdInLocalStorage: function setDefaultIdInLocalStorage(basicId) {
            if (window.localStorage && basicId != undefined) {
                if (localStorage.BasicId != undefined && localStorage.BasicId.split('|').indexOf(basicId) === -1) localStorage.BasicId += "|" + basicId;else localStorage.BasicId = basicId;
            }
        }

    }
};

module.exports = {
    ContentTracking: ContentTracking
};

/***/ }),
/* 137 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
	value: true
});

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

var _reactSlick = __webpack_require__(58);

var _reactSlick2 = _interopRequireDefault(_reactSlick);

var _Gallery = __webpack_require__(152);

var _Gallery2 = _interopRequireDefault(_Gallery);

var _WidgetsCommon = __webpack_require__(30);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var ArticleDetailImageCarousel = function (_React$Component) {
	_inherits(ArticleDetailImageCarousel, _React$Component);

	function ArticleDetailImageCarousel(props) {
		_classCallCheck(this, ArticleDetailImageCarousel);

		var _this = _possibleConstructorReturn(this, (ArticleDetailImageCarousel.__proto__ || Object.getPrototypeOf(ArticleDetailImageCarousel)).call(this, props));

		_this.state = {
			images: _this.props.imageGallery ? _this.props.imageGallery.ModelImages : [],
			totalCount: _this.props.imageGallery ? _this.props.imageGallery.RecordCount : 0,
			title: _this.props.title,
			isGalleryActive: false,
			slideIndex: 0
		};

		_this.handleThumbnailCarouselBeforeChange = _this.handleThumbnailCarouselBeforeChange.bind(_this);
		return _this;
	}

	_createClass(ArticleDetailImageCarousel, [{
		key: 'componentDidMount',
		value: function componentDidMount() {
			var _this2 = this;

			var imageSlider = this.refs.imageSlider.innerSlider.base;
			var imageSliderSlides = imageSlider.querySelectorAll('.slick-slide');

			var _loop = function _loop(i) {
				var self = _this2;
				imageSliderSlides[i].addEventListener('click', function () {
					self.focusSlider(i);
				});
			};

			for (var i = 0; i < imageSliderSlides.length; i++) {
				_loop(i);
			}
		}
	}, {
		key: 'getImageSlides',
		value: function getImageSlides() {
			var list = this.state.images.map(function (image) {
				return _react2.default.createElement(
					'div',
					{ style: { width: 90 } },
					_react2.default.createElement('img', { title: image.ImageName, alt: image.ImageName, src: (0, _WidgetsCommon.createImageUrl)(image.HostUrl, image.OriginalImgPath, '110x61') })
				);
			});

			return list;
		}
	}, {
		key: 'handleThumbnailCarouselBeforeChange',
		value: function handleThumbnailCarouselBeforeChange(oldindex, newindex) {
			if (event.currentTarget.classList.contains('slick-slide')) {
				var currentSlideIndex = newindex;

				this.setState({
					isGalleryActive: true,
					slideIndex: currentSlideIndex
				});
			}
		}
	}, {
		key: 'focusSlider',
		value: function focusSlider(slideIndex) {
			this.handleThumbnailCarouselBeforeChange(0, slideIndex);
		}
	}, {
		key: 'render',
		value: function render() {
			var imageSliderSettings = {
				className: 'slider__thumbnail',
				focusOnSelect: true,
				infinite: false,
				slidesToShow: 1,
				slidesToScroll: 1,
				variableWidth: true,
				beforeChange: this.handleThumbnailCarouselBeforeChange
			};

			var gallery = {
				heading: this.state.title,
				images: this.state.images,
				totalCount: this.state.totalCount,
				isActive: this.state.isGalleryActive,
				slideIndex: this.state.slideIndex
			};
			if (gallery.totalCount == 0) {
				return false;
			}
			return _react2.default.createElement(
				'div',
				{ className: 'article-image-content' },
				_react2.default.createElement(
					'h3',
					{ className: 'article-image-heading' },
					'Images'
				),
				_react2.default.createElement(
					'div',
					{ ref: 'imageSliderContainer', className: 'article-image-slider' },
					_react2.default.createElement(
						_reactSlick2.default,
						_extends({ ref: 'imageSlider' }, imageSliderSettings),
						this.getImageSlides()
					)
				),
				this.state.isGalleryActive ? _react2.default.createElement(_Gallery2.default, gallery) : ''
			);
		}
	}]);

	return ArticleDetailImageCarousel;
}(_react2.default.Component);

exports.default = ArticleDetailImageCarousel;

/***/ }),
/* 138 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

var _innerSlider = __webpack_require__(139);

var _objectAssign = __webpack_require__(19);

var _objectAssign2 = _interopRequireDefault(_objectAssign);

var _json2mq = __webpack_require__(62);

var _json2mq2 = _interopRequireDefault(_json2mq);

var _reactResponsiveMixin = __webpack_require__(146);

var _reactResponsiveMixin2 = _interopRequireDefault(_reactResponsiveMixin);

var _defaultProps = __webpack_require__(61);

var _defaultProps2 = _interopRequireDefault(_defaultProps);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var Slider = _react2.default.createClass({
  displayName: 'Slider',

  mixins: [_reactResponsiveMixin2.default],
  innerSlider: null,
  innerSliderRefHandler: function innerSliderRefHandler(ref) {
    this.innerSlider = ref;
  },
  getInitialState: function getInitialState() {
    return {
      breakpoint: null
    };
  },
  componentWillMount: function componentWillMount() {
    var _this = this;

    if (this.props.responsive) {
      var breakpoints = this.props.responsive.map(function (breakpt) {
        return breakpt.breakpoint;
      });
      breakpoints.sort(function (x, y) {
        return x - y;
      });

      breakpoints.forEach(function (breakpoint, index) {
        var bQuery;
        if (index === 0) {
          bQuery = (0, _json2mq2.default)({ minWidth: 0, maxWidth: breakpoint });
        } else {
          bQuery = (0, _json2mq2.default)({ minWidth: breakpoints[index - 1], maxWidth: breakpoint });
        }
        _this.media(bQuery, function () {
          _this.setState({ breakpoint: breakpoint });
        });
      });

      // Register media query for full screen. Need to support resize from small to large
      var query = (0, _json2mq2.default)({ minWidth: breakpoints.slice(-1)[0] });

      this.media(query, function () {
        _this.setState({ breakpoint: null });
      });
    }
  },

  slickPrev: function slickPrev() {
    this.innerSlider.slickPrev();
  },

  slickNext: function slickNext() {
    this.innerSlider.slickNext();
  },

  slickGoTo: function slickGoTo(slide) {
    this.innerSlider.slickGoTo(slide);
  },

  render: function render() {
    var _this2 = this;

    var settings;
    var newProps;
    if (this.state.breakpoint) {
      newProps = this.props.responsive.filter(function (resp) {
        return resp.breakpoint === _this2.state.breakpoint;
      });
      settings = newProps[0].settings === 'unslick' ? 'unslick' : (0, _objectAssign2.default)({}, this.props, newProps[0].settings);
    } else {
      settings = (0, _objectAssign2.default)({}, _defaultProps2.default, this.props);
    }

    var children = this.props.children;
    if (!Array.isArray(children)) {
      children = [children];
    }

    // Children may contain false or null, so we should filter them
    children = children.filter(function (child) {
      return !!child;
    });

    if (settings === 'unslick') {
      // if 'unslick' responsive breakpoint setting used, just return the <Slider> tag nested HTML
      return _react2.default.createElement(
        'div',
        null,
        children
      );
    } else {
      return _react2.default.createElement(
        _innerSlider.InnerSlider,
        _extends({ ref: this.innerSliderRefHandler }, settings),
        children
      );
    }
  }
});

module.exports = Slider;

/***/ }),
/* 139 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.InnerSlider = undefined;

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

var _eventHandlers = __webpack_require__(140);

var _eventHandlers2 = _interopRequireDefault(_eventHandlers);

var _helpers = __webpack_require__(60);

var _helpers2 = _interopRequireDefault(_helpers);

var _initialState = __webpack_require__(141);

var _initialState2 = _interopRequireDefault(_initialState);

var _defaultProps = __webpack_require__(61);

var _defaultProps2 = _interopRequireDefault(_defaultProps);

var _classnames = __webpack_require__(25);

var _classnames2 = _interopRequireDefault(_classnames);

var _track = __webpack_require__(142);

var _dots = __webpack_require__(143);

var _arrows = __webpack_require__(144);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var InnerSlider = exports.InnerSlider = _react2.default.createClass({
  displayName: 'InnerSlider',

  mixins: [_helpers2.default, _eventHandlers2.default],
  list: null,
  track: null,
  listRefHandler: function listRefHandler(ref) {
    this.list = ref;
  },
  trackRefHandler: function trackRefHandler(ref) {
    this.track = ref;
  },
  getInitialState: function getInitialState() {
    return _extends({}, _initialState2.default, {
      currentSlide: this.props.initialSlide
    });
  },
  getDefaultProps: function getDefaultProps() {
    return _defaultProps2.default;
  },
  componentWillMount: function componentWillMount() {
    if (this.props.init) {
      this.props.init();
    }
    this.setState({
      mounted: true
    });
    var lazyLoadedList = [];
    for (var i = 0; i < _react2.default.Children.count(this.props.children); i++) {
      if (i >= this.state.currentSlide && i < this.state.currentSlide + this.props.slidesToShow) {
        lazyLoadedList.push(i);
      }
    }

    if (this.props.lazyLoad && this.state.lazyLoadedList.length === 0) {
      this.setState({
        lazyLoadedList: lazyLoadedList
      });
    }
  },
  componentDidMount: function componentDidMount() {
    // Hack for autoplay -- Inspect Later
    this.initialize(this.props);
    this.adaptHeight();
    if (window.addEventListener) {
      window.addEventListener('resize', this.onWindowResized);
    } else {
      window.attachEvent('onresize', this.onWindowResized);
    }
  },
  componentWillUnmount: function componentWillUnmount() {
    if (this.animationEndCallback) {
      clearTimeout(this.animationEndCallback);
    }
    if (window.addEventListener) {
      window.removeEventListener('resize', this.onWindowResized);
    } else {
      window.detachEvent('onresize', this.onWindowResized);
    }
    if (this.state.autoPlayTimer) {
      window.clearInterval(this.state.autoPlayTimer);
    }
  },
  componentWillReceiveProps: function componentWillReceiveProps(nextProps) {
    if (this.props.slickGoTo != nextProps.slickGoTo) {
      if (true) {
        console.warn('react-slick deprecation warning: slickGoTo prop is deprecated and it will be removed in next release. Use slickGoTo method instead');
      }
      this.changeSlide({
        message: 'index',
        index: nextProps.slickGoTo,
        currentSlide: this.state.currentSlide
      });
    } else if (this.state.currentSlide >= nextProps.children.length) {
      this.update(nextProps);
      this.changeSlide({
        message: 'index',
        index: nextProps.children.length - nextProps.slidesToShow,
        currentSlide: this.state.currentSlide
      });
    } else {
      this.update(nextProps);
    }
  },
  componentDidUpdate: function componentDidUpdate() {
    this.adaptHeight();
  },
  onWindowResized: function onWindowResized() {
    this.update(this.props);
    // animating state should be cleared while resizing, otherwise autoplay stops working
    this.setState({
      animating: false
    });
  },
  slickPrev: function slickPrev() {
    this.changeSlide({ message: 'previous' });
  },
  slickNext: function slickNext() {
    this.changeSlide({ message: 'next' });
  },
  slickGoTo: function slickGoTo(slide) {
    typeof slide === 'number' && this.changeSlide({
      message: 'index',
      index: slide,
      currentSlide: this.state.currentSlide
    });
  },
  render: function render() {
    var className = (0, _classnames2.default)('slick-initialized', 'slick-slider', this.props.className);

    var trackProps = {
      fade: this.props.fade,
      cssEase: this.props.cssEase,
      speed: this.props.speed,
      infinite: this.props.infinite,
      centerMode: this.props.centerMode,
      focusOnSelect: this.props.focusOnSelect ? this.selectHandler : new Function(),
      currentSlide: this.state.currentSlide,
      lazyLoad: this.props.lazyLoad,
      lazyLoadedList: this.state.lazyLoadedList,
      rtl: this.props.rtl,
      slideWidth: this.state.slideWidth,
      slidesToShow: this.props.slidesToShow,
      slidesToScroll: this.props.slidesToScroll,
      slideCount: this.state.slideCount,
      trackStyle: this.state.trackStyle,
      variableWidth: this.props.variableWidth
    };

    var dots;

    if (this.props.dots === true && this.state.slideCount >= this.props.slidesToShow) {
      var dotProps = {
        dotsClass: this.props.dotsClass,
        slideCount: this.state.slideCount,
        slidesToShow: this.props.slidesToShow,
        currentSlide: this.state.currentSlide,
        slidesToScroll: this.props.slidesToScroll,
        clickHandler: this.changeSlide
      };

      dots = _react2.default.createElement(_dots.Dots, dotProps);
    }

    var prevArrow, nextArrow;

    var arrowProps = {
      infinite: this.props.infinite,
      centerMode: this.props.centerMode,
      currentSlide: this.state.currentSlide,
      slideCount: this.state.slideCount,
      slidesToShow: this.props.slidesToShow,
      prevArrow: this.props.prevArrow,
      nextArrow: this.props.nextArrow,
      clickHandler: this.changeSlide
    };

    if (this.props.arrows) {
      prevArrow = _react2.default.createElement(_arrows.PrevArrow, arrowProps);
      nextArrow = _react2.default.createElement(_arrows.NextArrow, arrowProps);
    }

    var centerPaddingStyle = null;

    if (this.props.vertical === false) {
      if (this.props.centerMode === true) {
        centerPaddingStyle = {
          padding: '0px ' + this.props.centerPadding
        };
      }
    } else {
      if (this.props.centerMode === true) {
        centerPaddingStyle = {
          padding: this.props.centerPadding + ' 0px'
        };
      }
    }

    return _react2.default.createElement(
      'div',
      { className: className, onMouseEnter: this.onInnerSliderEnter, onMouseLeave: this.onInnerSliderLeave },
      prevArrow,
      _react2.default.createElement(
        'div',
        {
          ref: this.listRefHandler,
          className: 'slick-list',
          style: centerPaddingStyle,
          onMouseDown: this.swipeStart,
          onMouseMove: this.state.dragging ? this.swipeMove : null,
          onMouseUp: this.swipeEnd,
          onMouseLeave: this.state.dragging ? this.swipeEnd : null,
          onTouchStart: this.swipeStart,
          onTouchMove: this.state.dragging ? this.swipeMove : null,
          onTouchEnd: this.swipeEnd,
          onTouchCancel: this.state.dragging ? this.swipeEnd : null,
          onKeyDown: this.props.accessibility ? this.keyHandler : null },
        _react2.default.createElement(
          _track.Track,
          _extends({ ref: this.trackRefHandler }, trackProps),
          this.props.children
        )
      ),
      nextArrow,
      dots
    );
  }
});

/***/ }),
/* 140 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;

var _trackHelper = __webpack_require__(59);

var _helpers = __webpack_require__(60);

var _helpers2 = _interopRequireDefault(_helpers);

var _objectAssign = __webpack_require__(19);

var _objectAssign2 = _interopRequireDefault(_objectAssign);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var EventHandlers = {
  // Event handler for previous and next
  changeSlide: function changeSlide(options) {
    var indexOffset, previousInt, slideOffset, unevenOffset, targetSlide;
    var _props = this.props;
    var slidesToScroll = _props.slidesToScroll;
    var slidesToShow = _props.slidesToShow;
    var _state = this.state;
    var slideCount = _state.slideCount;
    var currentSlide = _state.currentSlide;

    unevenOffset = slideCount % slidesToScroll !== 0;
    indexOffset = unevenOffset ? 0 : (slideCount - currentSlide) % slidesToScroll;

    if (options.message === 'previous') {
      slideOffset = indexOffset === 0 ? slidesToScroll : slidesToShow - indexOffset;
      targetSlide = currentSlide - slideOffset;
      if (this.props.lazyLoad) {
        previousInt = currentSlide - slideOffset;
        targetSlide = previousInt === -1 ? slideCount - 1 : previousInt;
      }
    } else if (options.message === 'next') {
      slideOffset = indexOffset === 0 ? slidesToScroll : indexOffset;
      targetSlide = currentSlide + slideOffset;
      if (this.props.lazyLoad) {
        targetSlide = (currentSlide + slidesToScroll) % slideCount + indexOffset;
      }
    } else if (options.message === 'dots' || options.message === 'children') {
      // Click on dots
      targetSlide = options.index * options.slidesToScroll;
      if (targetSlide === options.currentSlide) {
        return;
      }
    } else if (options.message === 'index') {
      targetSlide = parseInt(options.index);
      if (targetSlide === options.currentSlide) {
        return;
      }
    }

    this.slideHandler(targetSlide);
  },

  // Accessiblity handler for previous and next
  keyHandler: function keyHandler(e) {
    //Dont slide if the cursor is inside the form fields and arrow keys are pressed
    if (!e.target.tagName.match('TEXTAREA|INPUT|SELECT')) {
      if (e.keyCode === 37 && this.props.accessibility === true) {
        this.changeSlide({
          message: this.props.rtl === true ? 'next' : 'previous'
        });
      } else if (e.keyCode === 39 && this.props.accessibility === true) {
        this.changeSlide({
          message: this.props.rtl === true ? 'previous' : 'next'
        });
      }
    }
  },
  // Focus on selecting a slide (click handler on track)
  selectHandler: function selectHandler(options) {
    this.changeSlide(options);
  },
  swipeStart: function swipeStart(e) {
    var touches, posX, posY;

    if (this.props.swipe === false || 'ontouchend' in document && this.props.swipe === false) {
      return;
    } else if (this.props.draggable === false && e.type.indexOf('mouse') !== -1) {
      return;
    }
    posX = e.touches !== undefined ? e.touches[0].pageX : e.clientX;
    posY = e.touches !== undefined ? e.touches[0].pageY : e.clientY;
    this.setState({
      dragging: true,
      touchObject: {
        startX: posX,
        startY: posY,
        curX: posX,
        curY: posY
      }
    });
  },
  swipeMove: function swipeMove(e) {
    if (!this.state.dragging) {
      e.preventDefault();
      return;
    }
    if (this.state.animating) {
      return;
    }
    var swipeLeft;
    var curLeft, positionOffset;
    var touchObject = this.state.touchObject;

    curLeft = (0, _trackHelper.getTrackLeft)((0, _objectAssign2.default)({
      slideIndex: this.state.currentSlide,
      trackRef: this.track
    }, this.props, this.state));
    touchObject.curX = e.touches ? e.touches[0].pageX : e.clientX;
    touchObject.curY = e.touches ? e.touches[0].pageY : e.clientY;
    touchObject.swipeLength = Math.round(Math.sqrt(Math.pow(touchObject.curX - touchObject.startX, 2)));

    positionOffset = (this.props.rtl === false ? 1 : -1) * (touchObject.curX > touchObject.startX ? 1 : -1);

    var currentSlide = this.state.currentSlide;
    var dotCount = Math.ceil(this.state.slideCount / this.props.slidesToScroll);
    var swipeDirection = this.swipeDirection(this.state.touchObject);
    var touchSwipeLength = touchObject.swipeLength;

    if (this.props.infinite === false) {
      if (currentSlide === 0 && swipeDirection === 'right' || currentSlide + 1 >= dotCount && swipeDirection === 'left') {
        touchSwipeLength = touchObject.swipeLength * this.props.edgeFriction;

        if (this.state.edgeDragged === false && this.props.edgeEvent) {
          this.props.edgeEvent(swipeDirection);
          this.setState({ edgeDragged: true });
        }
      }
    }

    if (this.state.swiped === false && this.props.swipeEvent) {
      this.props.swipeEvent(swipeDirection);
      this.setState({ swiped: true });
    }

    swipeLeft = curLeft + touchSwipeLength * positionOffset;
    this.setState({
      touchObject: touchObject,
      swipeLeft: swipeLeft,
      trackStyle: (0, _trackHelper.getTrackCSS)((0, _objectAssign2.default)({ left: swipeLeft }, this.props, this.state))
    });

    if (Math.abs(touchObject.curX - touchObject.startX) < Math.abs(touchObject.curY - touchObject.startY) * 0.8) {
      return;
    }
    if (touchObject.swipeLength > 4) {
      e.preventDefault();
    }
  },
  swipeEnd: function swipeEnd(e) {
    if (!this.state.dragging) {
      e.preventDefault();
      return;
    }
    var touchObject = this.state.touchObject;
    var minSwipe = this.state.listWidth / this.props.touchThreshold;
    var swipeDirection = this.swipeDirection(touchObject);

    // reset the state of touch related state variables.
    this.setState({
      dragging: false,
      edgeDragged: false,
      swiped: false,
      swipeLeft: null,
      touchObject: {}
    });
    // Fix for #13
    if (!touchObject.swipeLength) {
      return;
    }
    if (touchObject.swipeLength > minSwipe) {
      e.preventDefault();
      if (swipeDirection === 'left') {
        this.slideHandler(this.state.currentSlide + this.props.slidesToScroll);
      } else if (swipeDirection === 'right') {
        this.slideHandler(this.state.currentSlide - this.props.slidesToScroll);
      } else {
        this.slideHandler(this.state.currentSlide);
      }
    } else {
      // Adjust the track back to it's original position.
      var currentLeft = (0, _trackHelper.getTrackLeft)((0, _objectAssign2.default)({
        slideIndex: this.state.currentSlide,
        trackRef: this.track
      }, this.props, this.state));

      this.setState({
        trackStyle: (0, _trackHelper.getTrackAnimateCSS)((0, _objectAssign2.default)({ left: currentLeft }, this.props, this.state))
      });
    }
  },
  onInnerSliderEnter: function onInnerSliderEnter(e) {
    if (this.props.autoplay && this.props.pauseOnHover) {
      this.pause();
    }
  },
  onInnerSliderLeave: function onInnerSliderLeave(e) {
    if (this.props.autoplay && this.props.pauseOnHover) {
      this.autoPlay();
    }
  }
};

exports.default = EventHandlers;

/***/ }),
/* 141 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var initialState = {
    animating: false,
    dragging: false,
    autoPlayTimer: null,
    currentDirection: 0,
    currentLeft: null,
    currentSlide: 0,
    direction: 1,
    // listWidth: null,
    // listHeight: null,
    // loadIndex: 0,
    slideCount: null,
    slideWidth: null,
    // sliding: false,
    // slideOffset: 0,
    swipeLeft: null,
    touchObject: {
        startX: 0,
        startY: 0,
        curX: 0,
        curY: 0
    },

    lazyLoadedList: [],

    // added for react
    initialized: false,
    edgeDragged: false,
    swiped: false, // used by swipeEvent. differentites between touch and swipe.
    trackStyle: {},
    trackWidth: 0

    // Removed
    // transformsEnabled: false,
    // $nextArrow: null,
    // $prevArrow: null,
    // $dots: null,
    // $list: null,
    // $slideTrack: null,
    // $slides: null,
};

module.exports = initialState;

/***/ }),
/* 142 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.Track = undefined;

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

var _objectAssign = __webpack_require__(19);

var _objectAssign2 = _interopRequireDefault(_objectAssign);

var _classnames = __webpack_require__(25);

var _classnames2 = _interopRequireDefault(_classnames);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var getSlideClasses = function getSlideClasses(spec) {
  var slickActive, slickCenter, slickCloned;
  var centerOffset, index;

  if (spec.rtl) {
    index = spec.slideCount - 1 - spec.index;
  } else {
    index = spec.index;
  }

  slickCloned = index < 0 || index >= spec.slideCount;
  if (spec.centerMode) {
    centerOffset = Math.floor(spec.slidesToShow / 2);
    slickCenter = (index - spec.currentSlide) % spec.slideCount === 0;
    if (index > spec.currentSlide - centerOffset - 1 && index <= spec.currentSlide + centerOffset) {
      slickActive = true;
    }
  } else {
    slickActive = spec.currentSlide <= index && index < spec.currentSlide + spec.slidesToShow;
  }
  return (0, _classnames2.default)({
    'slick-slide': true,
    'slick-active': slickActive,
    'slick-center': slickCenter,
    'slick-cloned': slickCloned
  });
};

var getSlideStyle = function getSlideStyle(spec) {
  var style = {};

  if (spec.variableWidth === undefined || spec.variableWidth === false) {
    style.width = spec.slideWidth;
  }

  if (spec.fade) {
    style.position = 'relative';
    style.left = -spec.index * spec.slideWidth;
    style.opacity = spec.currentSlide === spec.index ? 1 : 0;
    style.transition = 'opacity ' + spec.speed + 'ms ' + spec.cssEase;
    style.WebkitTransition = 'opacity ' + spec.speed + 'ms ' + spec.cssEase;
  }

  return style;
};

var getKey = function getKey(child, fallbackKey) {
  // key could be a zero
  return child.key === null || child.key === undefined ? fallbackKey : child.key;
};

var renderSlides = function renderSlides(spec) {
  var key;
  var slides = [];
  var preCloneSlides = [];
  var postCloneSlides = [];
  var count = _react2.default.Children.count(spec.children);

  _react2.default.Children.forEach(spec.children, function (elem, index) {
    var child = void 0;
    var childOnClickOptions = {
      message: 'children',
      index: index,
      slidesToScroll: spec.slidesToScroll,
      currentSlide: spec.currentSlide
    };

    if (!spec.lazyLoad | (spec.lazyLoad && spec.lazyLoadedList.indexOf(index) >= 0)) {
      child = elem;
    } else {
      child = _react2.default.createElement('div', null);
    }
    var childStyle = getSlideStyle((0, _objectAssign2.default)({}, spec, { index: index }));
    var slickClasses = getSlideClasses((0, _objectAssign2.default)({ index: index }, spec));
    var cssClasses;

    if (child.props.className) {
      cssClasses = (0, _classnames2.default)(slickClasses, child.props.className);
    } else {
      cssClasses = slickClasses;
    }

    var onClick = function onClick(e) {
      child.props && child.props.onClick && child.props.onClick(e);
      spec.focusOnSelect(childOnClickOptions);
    };

    slides.push(_react2.default.cloneElement(child, {
      key: 'original' + getKey(child, index),
      'data-index': index,
      className: cssClasses,
      tabIndex: '-1',
      style: (0, _objectAssign2.default)({ outline: 'none' }, child.props.style || {}, childStyle),
      onClick: onClick
    }));

    // variableWidth doesn't wrap properly.
    if (spec.infinite && spec.fade === false) {
      var infiniteCount = spec.variableWidth ? spec.slidesToShow + 1 : spec.slidesToShow;

      if (index >= count - infiniteCount) {
        key = -(count - index);
        preCloneSlides.push(_react2.default.cloneElement(child, {
          key: 'precloned' + getKey(child, key),
          'data-index': key,
          className: cssClasses,
          style: (0, _objectAssign2.default)({}, child.props.style || {}, childStyle),
          onClick: onClick
        }));
      }

      if (index < infiniteCount) {
        key = count + index;
        postCloneSlides.push(_react2.default.cloneElement(child, {
          key: 'postcloned' + getKey(child, key),
          'data-index': key,
          className: cssClasses,
          style: (0, _objectAssign2.default)({}, child.props.style || {}, childStyle),
          onClick: onClick
        }));
      }
    }
  });

  if (spec.rtl) {
    return preCloneSlides.concat(slides, postCloneSlides).reverse();
  } else {
    return preCloneSlides.concat(slides, postCloneSlides);
  }
};

var Track = exports.Track = _react2.default.createClass({
  displayName: 'Track',

  render: function render() {
    var slides = renderSlides.call(this, this.props);
    return _react2.default.createElement(
      'div',
      { className: 'slick-track', style: this.props.trackStyle },
      slides
    );
  }
});

/***/ }),
/* 143 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.Dots = undefined;

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

var _classnames = __webpack_require__(25);

var _classnames2 = _interopRequireDefault(_classnames);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var getDotCount = function getDotCount(spec) {
  var dots;
  dots = Math.ceil(spec.slideCount / spec.slidesToScroll);
  return dots;
};

var Dots = exports.Dots = _react2.default.createClass({
  displayName: 'Dots',


  clickHandler: function clickHandler(options, e) {
    // In Autoplay the focus stays on clicked button even after transition
    // to next slide. That only goes away by click somewhere outside
    e.preventDefault();
    this.props.clickHandler(options);
  },
  render: function render() {
    var _this = this;

    var dotCount = getDotCount({
      slideCount: this.props.slideCount,
      slidesToScroll: this.props.slidesToScroll
    });

    // Apply join & split to Array to pre-fill it for IE8
    //
    // Credit: http://stackoverflow.com/a/13735425/1849458
    var dots = Array.apply(null, Array(dotCount + 1).join('0').split('')).map(function (x, i) {

      var leftBound = i * _this.props.slidesToScroll;
      var rightBound = i * _this.props.slidesToScroll + (_this.props.slidesToScroll - 1);
      var className = (0, _classnames2.default)({
        'slick-active': _this.props.currentSlide >= leftBound && _this.props.currentSlide <= rightBound
      });

      var dotOptions = {
        message: 'dots',
        index: i,
        slidesToScroll: _this.props.slidesToScroll,
        currentSlide: _this.props.currentSlide
      };

      return _react2.default.createElement(
        'li',
        { key: i, className: className },
        _react2.default.createElement(
          'button',
          { onClick: _this.clickHandler.bind(_this, dotOptions) },
          i + 1
        )
      );
    });

    return _react2.default.createElement(
      'ul',
      { className: this.props.dotsClass, style: { display: 'block' } },
      dots
    );
  }
});

/***/ }),
/* 144 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


exports.__esModule = true;
exports.NextArrow = exports.PrevArrow = undefined;

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

var _classnames = __webpack_require__(25);

var _classnames2 = _interopRequireDefault(_classnames);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var PrevArrow = exports.PrevArrow = _react2.default.createClass({
  displayName: 'PrevArrow',


  clickHandler: function clickHandler(options, e) {
    if (e) {
      e.preventDefault();
    }
    this.props.clickHandler(options, e);
  },
  render: function render() {
    var prevClasses = { 'slick-arrow': true, 'slick-prev': true };
    var prevHandler = this.clickHandler.bind(this, { message: 'previous' });

    if (!this.props.infinite && (this.props.currentSlide === 0 || this.props.slideCount <= this.props.slidesToShow)) {
      prevClasses['slick-disabled'] = true;
      prevHandler = null;
    }

    var prevArrowProps = {
      key: '0',
      'data-role': 'none',
      className: (0, _classnames2.default)(prevClasses),
      style: { display: 'block' },
      onClick: prevHandler
    };
    var prevArrow;

    if (this.props.prevArrow) {
      prevArrow = _react2.default.cloneElement(this.props.prevArrow, prevArrowProps);
    } else {
      prevArrow = _react2.default.createElement(
        'button',
        _extends({ key: '0', type: 'button' }, prevArrowProps),
        ' Previous'
      );
    }

    return prevArrow;
  }
});

var NextArrow = exports.NextArrow = _react2.default.createClass({
  displayName: 'NextArrow',

  clickHandler: function clickHandler(options, e) {
    if (e) {
      e.preventDefault();
    }
    this.props.clickHandler(options, e);
  },
  render: function render() {
    var nextClasses = { 'slick-arrow': true, 'slick-next': true };
    var nextHandler = this.clickHandler.bind(this, { message: 'next' });

    if (!this.props.infinite) {
      if (this.props.centerMode && this.props.currentSlide >= this.props.slideCount - 1) {
        nextClasses['slick-disabled'] = true;
        nextHandler = null;
      } else {
        if (this.props.currentSlide >= this.props.slideCount - this.props.slidesToShow) {
          nextClasses['slick-disabled'] = true;
          nextHandler = null;
        }
      }

      if (this.props.slideCount <= this.props.slidesToShow) {
        nextClasses['slick-disabled'] = true;
        nextHandler = null;
      }
    }

    var nextArrowProps = {
      key: '1',
      'data-role': 'none',
      className: (0, _classnames2.default)(nextClasses),
      style: { display: 'block' },
      onClick: nextHandler
    };

    var nextArrow;

    if (this.props.nextArrow) {
      nextArrow = _react2.default.cloneElement(this.props.nextArrow, nextArrowProps);
    } else {
      nextArrow = _react2.default.createElement(
        'button',
        _extends({ key: '1', type: 'button' }, nextArrowProps),
        ' Next'
      );
    }

    return nextArrow;
  }
});

/***/ }),
/* 145 */
/***/ (function(module, exports) {

var camel2hyphen = function (str) {
  return str
          .replace(/[A-Z]/g, function (match) {
            return '-' + match.toLowerCase();
          })
          .toLowerCase();
};

module.exports = camel2hyphen;

/***/ }),
/* 146 */
/***/ (function(module, exports, __webpack_require__) {

var canUseDOM = __webpack_require__(147);
var enquire = canUseDOM && __webpack_require__(148);
var json2mq = __webpack_require__(62);

var ResponsiveMixin = {
  media: function (query, handler) {
    query = json2mq(query);
    if (typeof handler === 'function') {
      handler = {
        match: handler
      };
    }
    enquire.register(query, handler);

    // Queue the handlers to unregister them at unmount  
    if (! this._responsiveMediaHandlers) {
      this._responsiveMediaHandlers = [];
    }
    this._responsiveMediaHandlers.push({query: query, handler: handler});
  },
  componentWillUnmount: function () {
    if (this._responsiveMediaHandlers) {
      this._responsiveMediaHandlers.forEach(function(obj) {
        enquire.unregister(obj.query, obj.handler);
      });
    }
  }
};

module.exports = ResponsiveMixin;

/***/ }),
/* 147 */
/***/ (function(module, exports) {

var canUseDOM = !!(
  typeof window !== 'undefined' &&
  window.document &&
  window.document.createElement
);

module.exports = canUseDOM;

/***/ }),
/* 148 */
/***/ (function(module, exports, __webpack_require__) {

var MediaQueryDispatch = __webpack_require__(149);
module.exports = new MediaQueryDispatch();


/***/ }),
/* 149 */
/***/ (function(module, exports, __webpack_require__) {

var MediaQuery = __webpack_require__(150);
var Util = __webpack_require__(63);
var each = Util.each;
var isFunction = Util.isFunction;
var isArray = Util.isArray;

/**
 * Allows for registration of query handlers.
 * Manages the query handler's state and is responsible for wiring up browser events
 *
 * @constructor
 */
function MediaQueryDispatch () {
    if(!window.matchMedia) {
        throw new Error('matchMedia not present, legacy browsers require a polyfill');
    }

    this.queries = {};
    this.browserIsIncapable = !window.matchMedia('only all').matches;
}

MediaQueryDispatch.prototype = {

    constructor : MediaQueryDispatch,

    /**
     * Registers a handler for the given media query
     *
     * @param {string} q the media query
     * @param {object || Array || Function} options either a single query handler object, a function, or an array of query handlers
     * @param {function} options.match fired when query matched
     * @param {function} [options.unmatch] fired when a query is no longer matched
     * @param {function} [options.setup] fired when handler first triggered
     * @param {boolean} [options.deferSetup=false] whether setup should be run immediately or deferred until query is first matched
     * @param {boolean} [shouldDegrade=false] whether this particular media query should always run on incapable browsers
     */
    register : function(q, options, shouldDegrade) {
        var queries         = this.queries,
            isUnconditional = shouldDegrade && this.browserIsIncapable;

        if(!queries[q]) {
            queries[q] = new MediaQuery(q, isUnconditional);
        }

        //normalise to object in an array
        if(isFunction(options)) {
            options = { match : options };
        }
        if(!isArray(options)) {
            options = [options];
        }
        each(options, function(handler) {
            if (isFunction(handler)) {
                handler = { match : handler };
            }
            queries[q].addHandler(handler);
        });

        return this;
    },

    /**
     * unregisters a query and all it's handlers, or a specific handler for a query
     *
     * @param {string} q the media query to target
     * @param {object || function} [handler] specific handler to unregister
     */
    unregister : function(q, handler) {
        var query = this.queries[q];

        if(query) {
            if(handler) {
                query.removeHandler(handler);
            }
            else {
                query.clear();
                delete this.queries[q];
            }
        }

        return this;
    }
};

module.exports = MediaQueryDispatch;


/***/ }),
/* 150 */
/***/ (function(module, exports, __webpack_require__) {

var QueryHandler = __webpack_require__(151);
var each = __webpack_require__(63).each;

/**
 * Represents a single media query, manages it's state and registered handlers for this query
 *
 * @constructor
 * @param {string} query the media query string
 * @param {boolean} [isUnconditional=false] whether the media query should run regardless of whether the conditions are met. Primarily for helping older browsers deal with mobile-first design
 */
function MediaQuery(query, isUnconditional) {
    this.query = query;
    this.isUnconditional = isUnconditional;
    this.handlers = [];
    this.mql = window.matchMedia(query);

    var self = this;
    this.listener = function(mql) {
        // Chrome passes an MediaQueryListEvent object, while other browsers pass MediaQueryList directly
        self.mql = mql.currentTarget || mql;
        self.assess();
    };
    this.mql.addListener(this.listener);
}

MediaQuery.prototype = {

    constuctor : MediaQuery,

    /**
     * add a handler for this query, triggering if already active
     *
     * @param {object} handler
     * @param {function} handler.match callback for when query is activated
     * @param {function} [handler.unmatch] callback for when query is deactivated
     * @param {function} [handler.setup] callback for immediate execution when a query handler is registered
     * @param {boolean} [handler.deferSetup=false] should the setup callback be deferred until the first time the handler is matched?
     */
    addHandler : function(handler) {
        var qh = new QueryHandler(handler);
        this.handlers.push(qh);

        this.matches() && qh.on();
    },

    /**
     * removes the given handler from the collection, and calls it's destroy methods
     *
     * @param {object || function} handler the handler to remove
     */
    removeHandler : function(handler) {
        var handlers = this.handlers;
        each(handlers, function(h, i) {
            if(h.equals(handler)) {
                h.destroy();
                return !handlers.splice(i,1); //remove from array and exit each early
            }
        });
    },

    /**
     * Determine whether the media query should be considered a match
     *
     * @return {Boolean} true if media query can be considered a match, false otherwise
     */
    matches : function() {
        return this.mql.matches || this.isUnconditional;
    },

    /**
     * Clears all handlers and unbinds events
     */
    clear : function() {
        each(this.handlers, function(handler) {
            handler.destroy();
        });
        this.mql.removeListener(this.listener);
        this.handlers.length = 0; //clear array
    },

    /*
        * Assesses the query, turning on all handlers if it matches, turning them off if it doesn't match
        */
    assess : function() {
        var action = this.matches() ? 'on' : 'off';

        each(this.handlers, function(handler) {
            handler[action]();
        });
    }
};

module.exports = MediaQuery;


/***/ }),
/* 151 */
/***/ (function(module, exports) {

/**
 * Delegate to handle a media query being matched and unmatched.
 *
 * @param {object} options
 * @param {function} options.match callback for when the media query is matched
 * @param {function} [options.unmatch] callback for when the media query is unmatched
 * @param {function} [options.setup] one-time callback triggered the first time a query is matched
 * @param {boolean} [options.deferSetup=false] should the setup callback be run immediately, rather than first time query is matched?
 * @constructor
 */
function QueryHandler(options) {
    this.options = options;
    !options.deferSetup && this.setup();
}

QueryHandler.prototype = {

    constructor : QueryHandler,

    /**
     * coordinates setup of the handler
     *
     * @function
     */
    setup : function() {
        if(this.options.setup) {
            this.options.setup();
        }
        this.initialised = true;
    },

    /**
     * coordinates setup and triggering of the handler
     *
     * @function
     */
    on : function() {
        !this.initialised && this.setup();
        this.options.match && this.options.match();
    },

    /**
     * coordinates the unmatch event for the handler
     *
     * @function
     */
    off : function() {
        this.options.unmatch && this.options.unmatch();
    },

    /**
     * called when a handler is to be destroyed.
     * delegates to the destroy or unmatch callbacks, depending on availability.
     *
     * @function
     */
    destroy : function() {
        this.options.destroy ? this.options.destroy() : this.off();
    },

    /**
     * determines equality by reference.
     * if object is supplied compare options, if function, compare match callback
     *
     * @function
     * @param {object || function} [target] the target for comparison
     */
    equals : function(target) {
        return this.options === target || this.options.match === target;
    }

};

module.exports = QueryHandler;


/***/ }),
/* 152 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
	value: true
});

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

var _reactSlick = __webpack_require__(58);

var _reactSlick2 = _interopRequireDefault(_reactSlick);

var _WidgetsCommon = __webpack_require__(30);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var defaultProps = {
	heading: '',
	isActive: false,
	slideIndex: 0
};

var Gallery = function (_React$Component) {
	_inherits(Gallery, _React$Component);

	function Gallery(props) {
		_classCallCheck(this, Gallery);

		var _this = _possibleConstructorReturn(this, (Gallery.__proto__ || Object.getPrototypeOf(Gallery)).call(this, props));

		_this.state = {
			heading: _this.props.heading,
			isActive: _this.props.isActive,
			slideIndex: _this.props.slideIndex
		};

		_this.handleGalleryClose = _this.handleGalleryClose.bind(_this);
		_this.handleMainSliderBeforeChange = _this.handleMainSliderBeforeChange.bind(_this);
		_this.handleThumbnailSliderBeforeChange = _this.handleThumbnailSliderBeforeChange.bind(_this);
		return _this;
	}

	_createClass(Gallery, [{
		key: 'componentWillReceiveProps',
		value: function componentWillReceiveProps(nextProps) {
			this.focusMainSlider(nextProps.slideIndex);

			this.setState({
				isActive: nextProps.isActive,
				slideIndex: nextProps.slideIndex
			});
		}
	}, {
		key: 'componentDidMount',
		value: function componentDidMount() {
			var _this2 = this;

			var thumbnailSlider = this.refs.thumbnailSlider.innerSlider.base;
			var thumbnailSliderSlides = thumbnailSlider.querySelectorAll('.slick-slide');

			var _loop = function _loop(i) {
				var self = _this2;
				thumbnailSliderSlides[i].addEventListener('click', function () {
					self.focusMainSlider(i);
				});
			};

			for (var i = 0; i < thumbnailSliderSlides.length; i++) {
				_loop(i);
			}
		}
	}, {
		key: 'handleGalleryClose',
		value: function handleGalleryClose() {
			this.setState({
				isActive: false
			});
		}
	}, {
		key: 'getMainCarouselSlides',
		value: function getMainCarouselSlides() {
			var list = this.props.images.map(function (image) {
				var title = (image.ModelName ? image.ModelName : '') + ' ' + (image.ImageCategory ? image.ImageCategory : '');
				return _react2.default.createElement(
					'div',
					null,
					_react2.default.createElement('img', { title: title, alt: image.ImageCategory, src: (0, _WidgetsCommon.createImageUrl)(image.HostUrl, image.OriginalImgPath, '476x268') })
				);
			});

			return list;
		}
	}, {
		key: 'getThumbnailSlides',
		value: function getThumbnailSlides() {
			var list = this.props.images.map(function (image) {
				var title = (image.ModelName ? image.ModelName : '') + ' ' + (image.ImageCategory ? image.ImageCategory : '');
				return _react2.default.createElement(
					'div',
					{ style: { width: 90 } },
					_react2.default.createElement('img', { title: title, alt: image.ImageCategory, src: (0, _WidgetsCommon.createImageUrl)(image.HostUrl, image.OriginalImgPath, '110x61') })
				);
			});

			return list;
		}
	}, {
		key: 'handleMainSliderBeforeChange',
		value: function handleMainSliderBeforeChange(oldindex, newindex) {
			var currentSlideIndex = newindex >= 0 ? newindex : this.state.slideIndex;
			var currentSlideTitle = this.props.images[currentSlideIndex].title;

			this.refs.thumbnailSlider.slickGoTo(currentSlideIndex);

			this.setState({
				slideIndex: currentSlideIndex,
				slideTitle: currentSlideTitle
			});
		}
	}, {
		key: 'handleThumbnailSliderBeforeChange',
		value: function handleThumbnailSliderBeforeChange(oldindex, newindex) {
			if (event) {
				if (event.currentTarget.classList.contains('slick-slide')) {
					var currentSlideIndex = newindex;

					// reset click event to trigger 'before change' function loop
					event = undefined;

					this.focusMainSlider(currentSlideIndex);
				}
			}
		}
	}, {
		key: 'focusMainSlider',
		value: function focusMainSlider(slideIndex) {
			this.refs.mainSlider.slickGoTo(slideIndex);
		}
	}, {
		key: 'render',
		value: function render() {
			if (!this.props.images && this.props.images.length === 0) {
				return false;
			}
			var mainSliderSettings = {
				className: 'slider__main',
				initialSlide: this.state.slideIndex,
				infinite: false,
				lazyLoad: true,
				slidesToShow: 1,
				slidesToScroll: 1,
				beforeChange: this.handleMainSliderBeforeChange
			};

			var thumbnailSliderSettings = {
				arrows: false,
				className: 'slider__thumbnail',
				focusOnSelect: true,
				initialSlide: this.state.slideIndex,
				infinite: false,
				slidesToShow: 1,
				slidesToScroll: 1,
				variableWidth: true,
				beforeChange: this.handleThumbnailSliderBeforeChange
			};

			var activeClass = this.state.isActive ? 'gallery--active' : '';
			var modelName = this.props.images[this.state.slideIndex].ModelName;
			var imageCategory = this.props.images[this.state.slideIndex].ImageCategory;
			var slideTitle = (modelName ? modelName : '') + ' ' + (imageCategory ? imageCategory : '');

			return _react2.default.createElement(
				'div',
				{ className: "article-gallery-container " + activeClass },
				_react2.default.createElement(
					'h3',
					{ className: 'gallery__heading' },
					this.state.heading
				),
				_react2.default.createElement('span', { onClick: this.handleGalleryClose, className: 'gallery__close' }),
				_react2.default.createElement(
					'div',
					{ className: 'gallery__body' },
					_react2.default.createElement(
						'div',
						{ className: 'gallery-body__header' },
						_react2.default.createElement(
							'span',
							{ className: 'gallery-slider__title' },
							slideTitle
						),
						_react2.default.createElement(
							'span',
							{ className: 'gallery-slider__count' },
							this.state.slideIndex + 1,
							' of ',
							this.props.totalCount
						),
						_react2.default.createElement('div', { className: 'clear' })
					),
					_react2.default.createElement(
						_reactSlick2.default,
						_extends({ ref: 'mainSlider' }, mainSliderSettings),
						this.getMainCarouselSlides()
					),
					_react2.default.createElement(
						_reactSlick2.default,
						_extends({ ref: 'thumbnailSlider' }, thumbnailSliderSettings),
						this.getThumbnailSlides()
					)
				)
			);
		}
	}]);

	return Gallery;
}(_react2.default.Component);

Gallery.defaultProps = defaultProps;

exports.default = Gallery;

/***/ }),
/* 153 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
	value: true
});

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var CarouselBrand = function (_React$Component) {
	_inherits(CarouselBrand, _React$Component);

	function CarouselBrand(props) {
		_classCallCheck(this, CarouselBrand);

		var _this = _possibleConstructorReturn(this, (CarouselBrand.__proto__ || Object.getPrototypeOf(CarouselBrand)).call(this, props));

		_this.renderBrand = _this.renderBrand.bind(_this);
		_this.renderBrandList = _this.renderBrandList.bind(_this);
		return _this;
	}

	_createClass(CarouselBrand, [{
		key: "renderBrand",
		value: function renderBrand(brand) {
			return _react2.default.createElement(
				"li",
				{ className: "carousel-slide" },
				_react2.default.createElement(
					"div",
					{ className: "carousel-card" },
					_react2.default.createElement(
						"a",
						{ href: "/m/" + brand.MaskingName + "-scooters/", title: brand.MakeName + " scooters", className: "card-target-block" },
						_react2.default.createElement(
							"div",
							{ className: "brand-logo-image" },
							_react2.default.createElement(
								"span",
								{ "class": "brand-type" },
								_react2.default.createElement("span", { "class": "brandlogosprite brand-".concat(brand.MakeId.toString()) })
							)
						),
						_react2.default.createElement(
							"div",
							{ className: "card-details-block" },
							_react2.default.createElement(
								"p",
								{ "class": "brand-details__title" },
								brand.MakeName
							),
							_react2.default.createElement(
								"h3",
								{ "class": "brand-details__subtitle" },
								brand.TotalCount,
								" scooters"
							)
						)
					)
				)
			);
		}
	}, {
		key: "renderBrandList",
		value: function renderBrandList(brandList) {
			return _react2.default.createElement(
				"ul",
				{ className: "carousel-wrapper brand-type-carousel" },
				brandList.map(function (brand) {
					return this.renderBrand(brand);
				}.bind(this))
			);
		}
	}, {
		key: "render",
		value: function render() {
			var brandWidget = this.props.brandList;
			if (!brandWidget || !brandWidget.MakeList || brandWidget.MakeList.length == 0) {
				return;
			}
			return _react2.default.createElement(
				"div",
				{ className: "container bg-white box-shadow section-bottom-margin carousel-bottom-padding carousel-top-padding" },
				_react2.default.createElement(
					"div",
					{ className: "carousel-heading-content" },
					_react2.default.createElement(
						"div",
						{ className: "swiper-heading-left-grid inline-block" },
						_react2.default.createElement(
							"h2",
							null,
							brandWidget.Heading
						)
					),
					_react2.default.createElement(
						"div",
						{ className: "swiper-heading-right-grid inline-block text-right" },
						_react2.default.createElement(
							"a",
							{ href: brandWidget.CompleteListUrl, title: brandWidget.CompleteListUrlAlternateLabel, className: "btn view-all-target-btn" },
							brandWidget.CompleteListUrlLabel
						)
					)
				),
				this.renderBrandList(brandWidget.MakeList)
			);
		}
	}]);

	return CarouselBrand;
}(_react2.default.Component);

exports.default = CarouselBrand;

/***/ }),
/* 154 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

var _TopVideosComponent = __webpack_require__(155);

var _TopVideosComponent2 = _interopRequireDefault(_TopVideosComponent);

var _OtherVideosComponent = __webpack_require__(158);

var _OtherVideosComponent2 = _interopRequireDefault(_OtherVideosComponent);

var _timing = __webpack_require__(29);

var _scrollUtils = __webpack_require__(28);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var childComponentCount;
var childComponent;
// 0th index -> top videos
// 1st index -> other videos

// export function to reset api count

var VideoLandingComponent_Server = function (_React$Component) {
	_inherits(VideoLandingComponent_Server, _React$Component);

	function VideoLandingComponent_Server() {
		_classCallCheck(this, VideoLandingComponent_Server);

		return _possibleConstructorReturn(this, (VideoLandingComponent_Server.__proto__ || Object.getPrototypeOf(VideoLandingComponent_Server)).apply(this, arguments));
	}

	_createClass(VideoLandingComponent_Server, [{
		key: 'render',
		value: function render() {
			var TopVideos = !this.props || !this.props.TopVideos ? null : this.props.TopVideos;
			var OtherVideos = !this.props || !this.props.OtherVideos ? null : this.props.OtherVideos;

			return _react2.default.createElement(
				'div',
				null,
				_react2.default.createElement(_TopVideosComponent2.default, { TopVideos: TopVideos }),
				_react2.default.createElement(_OtherVideosComponent2.default, { OtherVideos: OtherVideos })
			);
		}
	}]);

	return VideoLandingComponent_Server;
}(_react2.default.Component);

module.exports = VideoLandingComponent_Server;

/***/ }),
/* 155 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

var _constants = __webpack_require__(4);

var _Spinner = __webpack_require__(31);

var _Spinner2 = _interopRequireDefault(_Spinner);

var _JumbotronVideos = __webpack_require__(156);

var _JumbotronVideos2 = _interopRequireDefault(_JumbotronVideos);

var _ExpertReviewsVideoList = __webpack_require__(157);

var _ExpertReviewsVideoList2 = _interopRequireDefault(_ExpertReviewsVideoList);

var _commonUtils = __webpack_require__(3);

var _VideosCommonFunc = __webpack_require__(14);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var TopVideosComponent = function (_React$Component) {
	_inherits(TopVideosComponent, _React$Component);

	function TopVideosComponent(props) {
		_classCallCheck(this, TopVideosComponent);

		var _this = _possibleConstructorReturn(this, (TopVideosComponent.__proto__ || Object.getPrototypeOf(TopVideosComponent)).call(this, props));

		if ((0, _commonUtils.isServer)()) {
			_this.props.Status = _constants.Status.Fetched;
		}
		_this.onClickVideoUrl = _this.onClickVideoUrl.bind(_this);
		_this.onClickMoreVideoUrl = _this.onClickMoreVideoUrl.bind(_this);
		_this.callParentLogAndScrollHandler = _this.callParentLogAndScrollHandler.bind(_this);
		return _this;
	}

	_createClass(TopVideosComponent, [{
		key: 'onClickMoreVideoUrl',
		value: function onClickMoreVideoUrl(sectionTitle, moreVideosUrl) {
			(0, _VideosCommonFunc.pushVideosByCategoryUrl)(this, sectionTitle, moreVideosUrl);
		}
	}, {
		key: 'onClickVideoUrl',
		value: function onClickVideoUrl(video) {
			(0, _VideosCommonFunc.pushVideoDetailUrl)(this, video);
		}
	}, {
		key: 'componentDidMount',
		value: function componentDidMount() {
			this.callParentLogAndScrollHandler();
			if (this.props.Status !== _constants.Status.Fetched && this.props.Status !== _constants.Status.IsFetching) {
				this.props.fetchTopVideosData();
			}
		}
	}, {
		key: 'componentDidUpdate',
		value: function componentDidUpdate(prevProps, prevState) {
			this.callParentLogAndScrollHandler();
		}
	}, {
		key: 'callParentLogAndScrollHandler',
		value: function callParentLogAndScrollHandler() {
			if (this.props.Status == _constants.Status.Fetched) {
				if (typeof this.props.logAndScrollHandler == 'function') {
					this.props.logAndScrollHandler('TopVideosComponent');
				}
			}
		}
	}, {
		key: 'render',
		value: function render() {
			if (this.props.Status !== _constants.Status.Fetched || !this.props.TopVideos) return _react2.default.createElement(_Spinner2.default, null);
			var topVideos = this.props.TopVideos;

			return _react2.default.createElement(
				'div',
				null,
				_react2.default.createElement(_JumbotronVideos2.default, { LandingFirstVideo: topVideos.LandingFirstVideos, onClickVideoUrl: this.onClickVideoUrl }),
				_react2.default.createElement(_ExpertReviewsVideoList2.default, { VideoListData: topVideos.ExpertReviews, onClickVideoUrl: this.onClickVideoUrl, onClickMoreVideoUrl: this.onClickMoreVideoUrl })
			);
		}
	}]);

	return TopVideosComponent;
}(_react2.default.Component);

module.exports = TopVideosComponent;

/***/ }),
/* 156 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

var _reactLazyLoad = __webpack_require__(6);

var _reactLazyLoad2 = _interopRequireDefault(_reactLazyLoad);

var _reactRouterDom = __webpack_require__(2);

var _VideosCommonFunc = __webpack_require__(14);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var JumbotronVideos = function (_React$Component) {
	_inherits(JumbotronVideos, _React$Component);

	function JumbotronVideos(props) {
		_classCallCheck(this, JumbotronVideos);

		var _this = _possibleConstructorReturn(this, (JumbotronVideos.__proto__ || Object.getPrototypeOf(JumbotronVideos)).call(this, props));

		_this.renderFirstVideo = _this.renderFirstVideo.bind(_this);
		_this.renderOtherVideoList = _this.renderOtherVideoList.bind(_this);

		return _this;
	}

	_createClass(JumbotronVideos, [{
		key: 'onClickVideoUrl',
		value: function onClickVideoUrl(video, event) {

			if (this.props.onClickVideoUrl && typeof this.props.onClickVideoUrl === 'function') {
				event.preventDefault();
				this.props.onClickVideoUrl(video);
			}
		}
	}, {
		key: 'renderFirstVideo',
		value: function renderFirstVideo(video) {
			if (!video) {
				return null;
			}
			return _react2.default.createElement(
				'div',
				{ className: 'jumbotron' },
				_react2.default.createElement(
					_reactRouterDom.Link,
					{ to: (0, _VideosCommonFunc.formVideoUrl)(video.VideoTitleUrl, video.BasicId), className: 'jumbotron__link', onClick: this.onClickVideoUrl.bind(this, video) },
					_react2.default.createElement(
						_reactLazyLoad2.default,
						null,
						_react2.default.createElement('img', { src: (0, _VideosCommonFunc.formVideoImageUrl)(video.VideoId, 'sddefault.jpg'), alt: video.VideoTitle, title: video.VideoTitle, border: '0', className: 'jumbotron__image' })
					),
					_react2.default.createElement(
						'span',
						{ className: 'jumbotron__head' },
						video.VideoTitle
					)
				)
			);
		}
	}, {
		key: 'renderOtherVideoItem',
		value: function renderOtherVideoItem(video) {
			if (!video) return null;
			var videoUrl = (0, _VideosCommonFunc.formVideoUrl)(video.VideoTitleUrl, video.BasicId);
			return _react2.default.createElement(
				'li',
				{ className: 'misc-container' },
				_react2.default.createElement(
					_reactRouterDom.Link,
					{ to: videoUrl, className: 'misc-list-image', onClick: this.onClickVideoUrl.bind(this, video) },
					_react2.default.createElement(
						'div',
						{ className: 'misc-container__image' },
						_react2.default.createElement(
							_reactLazyLoad2.default,
							null,
							_react2.default.createElement('img', { src: (0, _VideosCommonFunc.formVideoImageUrl)(video.VideoId, 'default.jpg'), alt: video.VideoTitle, title: video.VideoTitle, border: '0' })
						)
					)
				),
				_react2.default.createElement(
					_reactRouterDom.Link,
					{ to: videoUrl, className: 'misc-list-title', onClick: this.onClickVideoUrl.bind(this, video) },
					video.VideoTitle
				)
			);
		}
	}, {
		key: 'renderOtherVideoList',
		value: function renderOtherVideoList(videoList) {
			if (!videoList || !(videoList.length > 1)) {
				return null;
			}
			return _react2.default.createElement(
				'ul',
				{ className: 'miscWrapper jumbotron-list bottom-shadow margin-bottom20' },
				videoList.map(function (video, index) {
					if (index == 0) return null;
					return this.renderOtherVideoItem(video);
				}.bind(this))
			);
		}
	}, {
		key: 'render',
		value: function render() {
			if (!this.props.LandingFirstVideo || !(this.props.LandingFirstVideo.length > 0)) {
				return null;
			}
			return _react2.default.createElement(
				'div',
				{ className: 'container' },
				this.renderFirstVideo(this.props.LandingFirstVideo[0]),
				this.renderOtherVideoList(this.props.LandingFirstVideo)
			);
		}
	}]);

	return JumbotronVideos;
}(_react2.default.Component);

module.exports = JumbotronVideos;

/***/ }),
/* 157 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

var _reactLazyLoad = __webpack_require__(6);

var _reactLazyLoad2 = _interopRequireDefault(_reactLazyLoad);

var _VideosCommonFunc = __webpack_require__(14);

var _reactRouterDom = __webpack_require__(2);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var ExpertReviewsVideoList = function (_React$Component) {
	_inherits(ExpertReviewsVideoList, _React$Component);

	function ExpertReviewsVideoList(props) {
		_classCallCheck(this, ExpertReviewsVideoList);

		var _this = _possibleConstructorReturn(this, (ExpertReviewsVideoList.__proto__ || Object.getPrototypeOf(ExpertReviewsVideoList)).call(this, props));

		_this.onClickVideoUrl = _this.onClickVideoUrl.bind(_this);
		return _this;
	}

	_createClass(ExpertReviewsVideoList, [{
		key: 'onClickVideoUrl',
		value: function onClickVideoUrl(video, event) {

			if (this.props.onClickVideoUrl && typeof this.props.onClickVideoUrl === 'function') {
				event.preventDefault();
				this.props.onClickVideoUrl(video);
			}
		}
	}, {
		key: 'onClickMoreVideoUrl',
		value: function onClickMoreVideoUrl(event) {

			if (this.props.onClickMoreVideoUrl && typeof this.props.onClickMoreVideoUrl === 'function') {
				event.preventDefault();
				this.props.onClickMoreVideoUrl(this.props.VideoListData.SectionTitle, this.props.VideoListData.MoreVideosUrl);
			}
		}
	}, {
		key: 'renderVideoItem',
		value: function renderVideoItem(video) {
			if (!video) return null;
			var videoUrl = (0, _VideosCommonFunc.formVideoUrl)(video.VideoTitleUrl, video.BasicId);
			return _react2.default.createElement(
				'div',
				{ className: 'carousel-slide' },
				_react2.default.createElement(
					'div',
					{ className: 'carousel-card' },
					_react2.default.createElement(
						_reactRouterDom.Link,
						{ to: videoUrl, onClick: this.onClickVideoUrl.bind(this, video), className: 'block' },
						_react2.default.createElement(
							'div',
							{ className: 'card-image-block' },
							_react2.default.createElement(
								_reactLazyLoad2.default,
								null,
								_react2.default.createElement('img', { src: (0, _VideosCommonFunc.formVideoImageUrl)(video.VideoId, 'sddefault.jpg'), alt: video.VideoTitle, title: video.VideoTitle, border: '0' })
							)
						),
						_react2.default.createElement('span', { className: 'swiper-lazy-preloader' })
					),
					_react2.default.createElement(
						'div',
						{ className: 'card-details-block' },
						_react2.default.createElement(
							_reactRouterDom.Link,
							{ to: videoUrl, className: 'text-default font14 text-bold text-truncate-2', onClick: this.onClickVideoUrl.bind(this, video) },
							video.VideoTitle
						),
						_react2.default.createElement(
							'p',
							{ className: 'font12 text-xlight-grey margin-top10 margin-bottom10' },
							video.DisplayDate
						),
						_react2.default.createElement(
							'p',
							{ className: 'font14 text-light-grey margin-bottom10 line-height17 wrap-text text-truncate-2' },
							!video.ShortDescription ? video.Description : video.ShortDescription
						),
						_react2.default.createElement(
							'div',
							{ className: 'grid-6 alpha omega border-light-right font14' },
							_react2.default.createElement('span', { className: 'bwmsprite video-views-icon margin-right5' }),
							_react2.default.createElement(
								'span',
								{ className: 'text-default' },
								video.Views
							)
						),
						_react2.default.createElement(
							'div',
							{ className: 'grid-6 omega padding-left10 font14' },
							_react2.default.createElement('span', { className: 'bwmsprite video-likes-icon margin-right5' }),
							_react2.default.createElement(
								'span',
								{ className: 'text-default' },
								video.Likes
							)
						),
						_react2.default.createElement('div', { className: 'clear' })
					)
				)
			);
		}
	}, {
		key: 'render',
		value: function render() {
			if (!this.props.VideoListData || !this.props.VideoListData.Videos || !(this.props.VideoListData.Videos.length > 0)) return null;
			return _react2.default.createElement(
				'section',
				{ className: 'bg-white' },
				_react2.default.createElement(
					'div',
					{ id: 'expertReviewsWrapper', className: 'container bottom-shadow margin-bottom25' },
					_react2.default.createElement(
						'h2',
						{ className: 'text-center padding-top20 padding-bottom15' },
						this.props.VideoListData.SectionTitle
					),
					_react2.default.createElement(
						'div',
						{ className: 'carousel-wrapper carousel-type-video' },
						this.props.VideoListData.Videos.map(function (video) {
							return this.renderVideoItem(video);
						}.bind(this))
					),
					_react2.default.createElement(
						'div',
						{ className: 'padding-left10 view-all-btn-container margin-top10 padding-bottom20' },
						_react2.default.createElement(
							_reactRouterDom.Link,
							{ to: (0, _VideosCommonFunc.formMoreVideosUrl)(this.props.VideoListData.MoreVideosUrl), title: this.props.VideoListData.SectionTitle + " Bike Videos", className: 'btn view-all-target-btn', onClick: this.onClickMoreVideoUrl.bind(this) },
							'View more videos',
							_react2.default.createElement('span', { className: 'bwmsprite teal-right' })
						)
					)
				)
			);
		}
	}]);

	return ExpertReviewsVideoList;
}(_react2.default.Component);

module.exports = ExpertReviewsVideoList;

/***/ }),
/* 158 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

var _constants = __webpack_require__(4);

var _commonUtils = __webpack_require__(3);

var _ModelBrandsList = __webpack_require__(159);

var _ModelBrandsList2 = _interopRequireDefault(_ModelBrandsList);

var _VideoCarouselByCategory = __webpack_require__(160);

var _VideoCarouselByCategory2 = _interopRequireDefault(_VideoCarouselByCategory);

var _immutableWrapperContainer = __webpack_require__(9);

var _VideosCommonFunc = __webpack_require__(14);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var OtherVideosComponent = function (_React$Component) {
	_inherits(OtherVideosComponent, _React$Component);

	function OtherVideosComponent(props) {
		_classCallCheck(this, OtherVideosComponent);

		var _this = _possibleConstructorReturn(this, (OtherVideosComponent.__proto__ || Object.getPrototypeOf(OtherVideosComponent)).call(this, props));

		if ((0, _commonUtils.isServer)()) {
			_this.props.Status = _constants.Status.Fetched;
		}
		_this.onClickMoreVideoUrl = _this.onClickMoreVideoUrl.bind(_this);
		_this.onClickVideoUrl = _this.onClickVideoUrl.bind(_this);
		_this.callParentLogAndScrollHandler = _this.callParentLogAndScrollHandler.bind(_this);
		return _this;
	}

	_createClass(OtherVideosComponent, [{
		key: 'callParentLogAndScrollHandler',
		value: function callParentLogAndScrollHandler() {
			if (this.props.Status == _constants.Status.Fetched) {
				if (typeof this.props.logAndScrollHandler == 'function') {
					this.props.logAndScrollHandler('OtherVideosComponent'); // 1 for signifying second api inset of 2 on videos page
				}
			}
		}
	}, {
		key: 'onClickMoreVideoUrl',
		value: function onClickMoreVideoUrl(sectionTitle, moreVideosUrl) {
			(0, _VideosCommonFunc.pushVideosByCategoryUrl)(this, sectionTitle, moreVideosUrl);
			//TODO call next api
		}
	}, {
		key: 'onClickVideoUrl',
		value: function onClickVideoUrl(video) {
			(0, _VideosCommonFunc.pushVideoDetailUrl)(this, video);
			//call apis of video page
		}
	}, {
		key: 'componentDidUpdate',
		value: function componentDidUpdate() {
			this.callParentLogAndScrollHandler();
		}
	}, {
		key: 'componentDidMount',
		value: function componentDidMount() {
			this.callParentLogAndScrollHandler();
			if (this.props.Status !== _constants.Status.Fetched && this.props.Status !== _constants.Status.IsFetching) {
				this.props.fetchOtherVideosData();
			}
		}
	}, {
		key: 'render',
		value: function render() {
			if (this.props.Status !== _constants.Status.Fetched || !this.props.OtherVideos) return null;

			return _react2.default.createElement(
				'div',
				null,
				_react2.default.createElement(_VideoCarouselByCategory2.default, { VideoListData: this.props.OtherVideos.FirstRide, onClickVideoUrl: this.onClickVideoUrl, onClickMoreVideoUrl: this.onClickMoreVideoUrl }),
				_react2.default.createElement(_VideoCarouselByCategory2.default, { VideoListData: this.props.OtherVideos.LaunchAlert, onClickVideoUrl: this.onClickVideoUrl, onClickMoreVideoUrl: this.onClickMoreVideoUrl }),
				_react2.default.createElement(_VideoCarouselByCategory2.default, { VideoListData: this.props.OtherVideos.FirstLook, onClickVideoUrl: this.onClickVideoUrl, onClickMoreVideoUrl: this.onClickMoreVideoUrl }),
				_react2.default.createElement(_ModelBrandsList2.default, { Brands: this.props.OtherVideos.Brands }),
				_react2.default.createElement(_VideoCarouselByCategory2.default, { VideoListData: this.props.OtherVideos.PowerDriftBlockbuster, onClickVideoUrl: this.onClickVideoUrl, onClickMoreVideoUrl: this.onClickMoreVideoUrl }),
				_react2.default.createElement(_VideoCarouselByCategory2.default, { VideoListData: this.props.OtherVideos.MotorSports, onClickVideoUrl: this.onClickVideoUrl, onClickMoreVideoUrl: this.onClickMoreVideoUrl }),
				_react2.default.createElement(_VideoCarouselByCategory2.default, { VideoListData: this.props.OtherVideos.PowerDriftSpecials, onClickVideoUrl: this.onClickVideoUrl, onClickMoreVideoUrl: this.onClickMoreVideoUrl }),
				_react2.default.createElement(_VideoCarouselByCategory2.default, { VideoListData: this.props.OtherVideos.PowerDriftTopMusic, onClickVideoUrl: this.onClickVideoUrl, onClickMoreVideoUrl: this.onClickMoreVideoUrl }),
				_react2.default.createElement(_VideoCarouselByCategory2.default, { VideoListData: this.props.OtherVideos.Miscellaneous, onClickVideoUrl: this.onClickVideoUrl, onClickMoreVideoUrl: this.onClickMoreVideoUrl })
			);
		}
	}]);

	return OtherVideosComponent;
}(_react2.default.Component);

module.exports = OtherVideosComponent;

/***/ }),
/* 159 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var ModelBrandsList = function (_React$Component) {
	_inherits(ModelBrandsList, _React$Component);

	function ModelBrandsList(props) {
		_classCallCheck(this, ModelBrandsList);

		var _this = _possibleConstructorReturn(this, (ModelBrandsList.__proto__ || Object.getPrototypeOf(ModelBrandsList)).call(this, props));

		_this.state = {
			showOtherBrands: false
		};
		_this.renderBrandItem = _this.renderBrandItem.bind(_this);
		_this.renderTopBrands = _this.renderTopBrands.bind(_this);
		_this.renderOtherBrands = _this.renderOtherBrands.bind(_this);
		_this.renderShowMoreBrandsButton = _this.renderShowMoreBrandsButton.bind(_this);
		_this.moreBrandButtonClick = _this.moreBrandButtonClick.bind(_this);
		return _this;
	}

	_createClass(ModelBrandsList, [{
		key: 'moreBrandButtonClick',
		value: function moreBrandButtonClick() {
			this.setState({
				showOtherBrands: !this.state.showOtherBrands
			});
		}
	}, {
		key: 'renderBrandItem',
		value: function renderBrandItem(brand) {
			if (!brand) return null;
			return _react2.default.createElement(
				'li',
				null,
				_react2.default.createElement(
					'a',
					{ href: '/m' + brand.Href, title: brand.Title },
					_react2.default.createElement(
						'span',
						{ className: 'brand-type' },
						_react2.default.createElement('span', { className: "lazy brandlogosprite brand-" + brand.MakeId })
					),
					_react2.default.createElement(
						'span',
						{ className: 'brand-type-title' },
						brand.MakeName
					)
				)
			);
		}
	}, {
		key: 'renderTopBrands',
		value: function renderTopBrands(topBrands) {
			if (!topBrands) return null;
			return _react2.default.createElement(
				'ul',
				{ className: 'text-center' },
				topBrands.map(function (brand) {
					return this.renderBrandItem(brand);
				}.bind(this))
			);
		}
	}, {
		key: 'renderOtherBrands',
		value: function renderOtherBrands(otherBrands) {
			if (!otherBrands || !(otherBrands.length > 0)) return null;
			var hideClassName = this.state.showOtherBrands ? '' : 'hide';
			return _react2.default.createElement(
				'ul',
				{ className: "brand-style-moreBtn brandTypeMore border-top1 padding-top25 text-center " + hideClassName },
				otherBrands.map(function (brand) {
					return this.renderBrandItem(brand);
				}.bind(this))
			);
		}
	}, {
		key: 'renderShowMoreBrandsButton',
		value: function renderShowMoreBrandsButton(otherBrands) {
			if (!otherBrands || otherBrands.length == 0) {
				return null;
			}

			return _react2.default.createElement(
				'div',
				{ className: 'view-all-btn-container' },
				_react2.default.createElement(
					'a',
					{ href: 'javascript:void(0)', onClick: this.moreBrandButtonClick.bind(), className: 'view-brandType btn view-all-target-btn rotate-arrow', rel: 'nofollow' },
					_react2.default.createElement(
						'span',
						{ className: 'btn-label' },
						this.state.showOtherBrands ? 'View less brands' : 'View more brands'
					),
					_react2.default.createElement('span', { className: 'bwmsprite teal-right' })
				)
			);
		}
	}, {
		key: 'render',
		value: function render() {
			if (!this.props.Brands || !this.props.Brands.TopBrands || !(this.props.Brands.TopBrands.length > 0)) return null;
			return _react2.default.createElement(
				'div',
				null,
				_react2.default.createElement(
					'h2',
					{ className: 'text-center padding-top10 padding-bottom15' },
					'Browse videos by brands'
				),
				_react2.default.createElement(
					'div',
					{ className: 'container bg-white box-shadow card-bottom-margin brand-container collapsible-brand-content' },
					_react2.default.createElement(
						'div',
						{ id: 'brand-type-container', className: 'brand-type-container' },
						this.renderTopBrands(this.props.Brands.TopBrands),
						this.renderOtherBrands(this.props.Brands.OtherBrands)
					),
					this.renderShowMoreBrandsButton(this.props.Brands.OtherBrands)
				)
			);
		}
	}]);

	return ModelBrandsList;
}(_react2.default.Component);

module.exports = ModelBrandsList;

/***/ }),
/* 160 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

var _reactLazyLoad = __webpack_require__(6);

var _reactLazyLoad2 = _interopRequireDefault(_reactLazyLoad);

var _VideosCommonFunc = __webpack_require__(14);

var _reactRouterDom = __webpack_require__(2);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var VideoCarouselByCategory = function (_React$Component) {
	_inherits(VideoCarouselByCategory, _React$Component);

	function VideoCarouselByCategory() {
		_classCallCheck(this, VideoCarouselByCategory);

		return _possibleConstructorReturn(this, (VideoCarouselByCategory.__proto__ || Object.getPrototypeOf(VideoCarouselByCategory)).apply(this, arguments));
	}

	_createClass(VideoCarouselByCategory, [{
		key: 'onClickMoreVideoUrl',
		value: function onClickMoreVideoUrl(event) {
			if (this.props.onClickMoreVideoUrl && typeof this.props.onClickMoreVideoUrl === 'function') {
				event.preventDefault();
				this.props.onClickMoreVideoUrl(this.props.VideoListData.SectionTitle, this.props.VideoListData.MoreVideosUrl);
			}
		}
	}, {
		key: 'onClickVideoUrl',
		value: function onClickVideoUrl(video, event) {

			if (this.props.onClickVideoUrl && typeof this.props.onClickVideoUrl === 'function') {
				event.preventDefault();
				this.props.onClickVideoUrl(video);
			}
		}
	}, {
		key: 'renderVideoItem',
		value: function renderVideoItem(video) {
			return _react2.default.createElement(
				'div',
				{ className: 'carousel-slide' },
				_react2.default.createElement(
					'div',
					{ className: 'carousel-card' },
					_react2.default.createElement(
						_reactRouterDom.Link,
						{ to: (0, _VideosCommonFunc.formVideoUrl)(video.VideoTitleUrl, video.BasicId), className: 'card-target-block', onClick: this.onClickVideoUrl.bind(this, video) },
						_react2.default.createElement(
							'div',
							{ className: 'card-image-block' },
							_react2.default.createElement(
								_reactLazyLoad2.default,
								null,
								_react2.default.createElement('img', { className: 'swiper-lazy', src: (0, _VideosCommonFunc.formVideoImageUrl)(video.VideoId, 'default.jpg'), alt: video.VideoTitle, title: video.VideoTitle, border: '0' })
							)
						),
						_react2.default.createElement('span', { className: 'swiper-lazy-preloader' }),
						_react2.default.createElement(
							'div',
							{ className: 'card-details-block' },
							_react2.default.createElement(
								'div',
								{ className: 'card-list-title text-truncate-2' },
								video.VideoTitle
							),
							_react2.default.createElement(
								'p',
								{ className: 'card-list__info' },
								video.DisplayDate
							),
							_react2.default.createElement(
								'div',
								{ className: 'views-count-container' },
								_react2.default.createElement('span', { className: 'bwmsprite video-views-icon' }),
								_react2.default.createElement(
									'span',
									{ className: 'text-default' },
									video.Views
								)
							),
							_react2.default.createElement(
								'div',
								{ className: 'views-count-container padding-left10 border-light-left' },
								_react2.default.createElement('span', { className: 'bwmsprite video-likes-icon' }),
								_react2.default.createElement(
									'span',
									{ className: 'text-default' },
									video.Likes
								)
							),
							_react2.default.createElement('div', { className: 'clear' })
						)
					)
				)
			);
		}
	}, {
		key: 'render',
		value: function render() {
			if (!this.props.VideoListData || !this.props.VideoListData.Videos || !(this.props.VideoListData.Videos.length > 0)) return null;

			return _react2.default.createElement(
				'section',
				null,
				_react2.default.createElement(
					'div',
					{ className: 'container' },
					_react2.default.createElement(
						'div',
						{ className: 'carousel-heading-content' },
						_react2.default.createElement(
							'h2',
							{ className: 'carousel-heading__title-center' },
							this.props.VideoListData.SectionTitle
						)
					),
					_react2.default.createElement(
						'div',
						{ className: 'carousel-wrapper carousel-type-video' },
						this.props.VideoListData.Videos.map(function (video) {
							return this.renderVideoItem(video);
						}.bind(this))
					),
					_react2.default.createElement(
						'div',
						{ className: 'padding-left10 view-all-btn-container margin-top10 padding-bottom20' },
						_react2.default.createElement(
							_reactRouterDom.Link,
							{ to: (0, _VideosCommonFunc.formMoreVideosUrl)(this.props.VideoListData.MoreVideosUrl), title: this.props.VideoListData.SectionTitle + " Bike Videos", className: 'btn view-all-target-btn', onClick: this.onClickMoreVideoUrl.bind(this) },
							'View more videos',
							_react2.default.createElement('span', { className: 'bwmsprite teal-right' })
						)
					)
				)
			);
		}
	}]);

	return VideoCarouselByCategory;
}(_react2.default.Component);

module.exports = VideoCarouselByCategory;

/***/ }),
/* 161 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

var _reactRouterDom = __webpack_require__(2);

var _Footer = __webpack_require__(24);

var _Footer2 = _interopRequireDefault(_Footer);

var _reactLazyLoad = __webpack_require__(6);

var _reactLazyLoad2 = _interopRequireDefault(_reactLazyLoad);

var _commonUtils = __webpack_require__(3);

var _CategoryHeader = __webpack_require__(162);

var _CategoryHeader2 = _interopRequireDefault(_CategoryHeader);

var _Spinner = __webpack_require__(31);

var _Spinner2 = _interopRequireDefault(_Spinner);

var _Breadcrumb = __webpack_require__(23);

var _Breadcrumb2 = _interopRequireDefault(_Breadcrumb);

var _constants = __webpack_require__(4);

var _VideosCommonFunc = __webpack_require__(14);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var VideosByCategoryComponent = function (_React$Component) {
	_inherits(VideosByCategoryComponent, _React$Component);

	function VideosByCategoryComponent(props) {
		_classCallCheck(this, VideosByCategoryComponent);

		var _this = _possibleConstructorReturn(this, (VideosByCategoryComponent.__proto__ || Object.getPrototypeOf(VideosByCategoryComponent)).call(this, props));

		_this.onClickVideoUrl = _this.onClickVideoUrl.bind(_this);

		if (typeof gaObj != "undefined") {
			gaObj = _constants.GA_PAGE_MAPPING["VideoCategoryPage"];
		}
		return _this;
	}

	_createClass(VideosByCategoryComponent, [{
		key: 'componentDidMount',
		value: function componentDidMount() {
			if (this.props.VideosByCategory.Status !== _constants.Status.Fetched && this.props.VideosByCategory.Status !== _constants.Status.IsFetching) {
				//call actioncreator
				this.props.fetchVideosByCategory(this.props.match.params.categoryId, null);
			}
			document.getElementsByTagName("body")[0].classList.add("hide-bw-header");
		}
	}, {
		key: 'componentWillUnmount',
		value: function componentWillUnmount() {
			document.getElementsByTagName("body")[0].classList.remove("hide-bw-header");
		}
	}, {
		key: 'onClickVideoUrl',
		value: function onClickVideoUrl(video, event) {
			event.preventDefault();
			(0, _VideosCommonFunc.pushVideoDetailUrl)(this, video);
		}
	}, {
		key: 'renderVideoItem',
		value: function renderVideoItem(video) {
			if (!video) return null;
			var videoUrl = (0, _VideosCommonFunc.formVideoUrl)(video.VideoTitleUrl, video.BasicId);
			return _react2.default.createElement(
				'li',
				null,
				_react2.default.createElement(
					'div',
					{ className: 'misc-container margin-bottom10' },
					_react2.default.createElement(
						_reactRouterDom.Link,
						{ to: videoUrl, className: 'misc-list-image', onClick: this.onClickVideoUrl.bind(this, video) },
						_react2.default.createElement(
							'div',
							{ className: 'misc-container__image' },
							_react2.default.createElement(
								_reactLazyLoad2.default,
								null,
								_react2.default.createElement('img', { src: (0, _VideosCommonFunc.formVideoImageUrl)(video.VideoId, 'mqdefault.jpg'), alt: video.VideoTitle, title: video.VideoTitle, border: '0' })
							)
						)
					),
					_react2.default.createElement(
						_reactRouterDom.Link,
						{ to: videoUrl, title: video.VideoTitle, className: 'misc-list-title', onClick: this.onClickVideoUrl.bind(this, video) },
						video.VideoTitle
					)
				),
				_react2.default.createElement(
					'div',
					{ className: 'views-count-container' },
					_react2.default.createElement('span', { className: 'bwmsprite video-views-icon' }),
					_react2.default.createElement(
						'span',
						{ className: 'text-light-grey' },
						'Views:'
					),
					_react2.default.createElement(
						'span',
						null,
						video.Views
					)
				),
				_react2.default.createElement(
					'div',
					{ className: 'views-count-container padding-left20 border-light-left' },
					_react2.default.createElement('span', { className: 'bwmsprite video-likes-icon' }),
					_react2.default.createElement(
						'span',
						{ className: 'text-light-grey' },
						'Likes:'
					),
					_react2.default.createElement(
						'span',
						null,
						video.Likes
					)
				),
				_react2.default.createElement('div', { className: 'clear' })
			);
		}
	}, {
		key: 'renderVideoList',
		value: function renderVideoList(videos) {
			if (videos && videos.length > 0) {
				return _react2.default.createElement(
					'section',
					{ className: 'bg-white padding-top50 bottom-shadow margin-bottom30' },
					_react2.default.createElement(
						'div',
						{ className: 'container' },
						_react2.default.createElement(
							'ul',
							{ id: 'listVideos1', className: 'miscWrapper' },
							videos.map(function (video) {
								return this.renderVideoItem(video);
							}.bind(this))
						)
					)
				);
			} else {
				return _react2.default.createElement(_Spinner2.default, null);
			}
		}
	}, {
		key: 'render',
		value: function render() {
			if (!this.props.VideosByCategory) return null;
			var title = !this.props.VideosByCategory.SectionTitle ? '' : this.props.VideosByCategory.SectionTitle + ' Video';
			return _react2.default.createElement(
				'div',
				null,
				_react2.default.createElement(_CategoryHeader2.default, { PageHeading: title }),
				this.renderVideoList(this.props.VideosByCategory ? this.props.VideosByCategory.Videos : null),
				!this.props.VideosByCategory || !this.props.VideosByCategory.Videos || this.props.VideosByCategory.Videos.length == 0 ? null : _react2.default.createElement(
					'div',
					null,
					_react2.default.createElement(_Breadcrumb2.default, { breadcrumb: [{ Href: '/m/', Title: 'Home' }, { Href: '/m/bike-videos/', Title: 'Videos', isReactLink: true }, { Href: '', Title: title }] }),
					_react2.default.createElement(_Footer2.default, null)
				)
			);
		}
	}]);

	return VideosByCategoryComponent;
}(_react2.default.Component);

module.exports = VideosByCategoryComponent;

/***/ }),
/* 162 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var CategoryHeader = function (_React$Component) {
	_inherits(CategoryHeader, _React$Component);

	function CategoryHeader() {
		_classCallCheck(this, CategoryHeader);

		return _possibleConstructorReturn(this, (CategoryHeader.__proto__ || Object.getPrototypeOf(CategoryHeader)).apply(this, arguments));
	}

	_createClass(CategoryHeader, [{
		key: "onBackButtonClick",
		value: function onBackButtonClick() {
			window.history.back();
		}
	}, {
		key: "render",
		value: function render() {
			if (!this.props.PageHeading) return null;
			return _react2.default.createElement(
				"header",
				{ id: "categoryHeader", className: "fixed-header" },
				_react2.default.createElement(
					"div",
					{ className: "category-back-btn" },
					_react2.default.createElement("span", { className: "bwmsprite fa-arrow-back", onClick: this.onBackButtonClick.bind() })
				),
				_react2.default.createElement(
					"h1",
					{ className: "header-title" },
					this.props.PageHeading
				)
			);
		}
	}]);

	return CategoryHeader;
}(_react2.default.Component);

module.exports = CategoryHeader;

/***/ }),
/* 163 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

var _reactRouterDom = __webpack_require__(2);

var _VideoDetail = __webpack_require__(164);

var _VideoDetail2 = _interopRequireDefault(_VideoDetail);

var _VideoModelSlug = __webpack_require__(165);

var _VideoModelSlug2 = _interopRequireDefault(_VideoModelSlug);

var _commonUtils = __webpack_require__(3);

var _scrollUtils = __webpack_require__(28);

var _timing = __webpack_require__(29);

var _constants = __webpack_require__(4);

var _Spinner = __webpack_require__(31);

var _Spinner2 = _interopRequireDefault(_Spinner);

var _Breadcrumb = __webpack_require__(23);

var _Breadcrumb2 = _interopRequireDefault(_Breadcrumb);

var _Footer = __webpack_require__(24);

var _Footer2 = _interopRequireDefault(_Footer);

var _SpinnerRelative = __webpack_require__(8);

var _SpinnerRelative2 = _interopRequireDefault(_SpinnerRelative);

var _CarouselBikeItem = __webpack_require__(64);

var _CarouselBikeItem2 = _interopRequireDefault(_CarouselBikeItem);

var _CarouselVideoItem = __webpack_require__(65);

var _CarouselVideoItem2 = _interopRequireDefault(_CarouselVideoItem);

var _CarouselContainer = __webpack_require__(166);

var _CarouselContainer2 = _interopRequireDefault(_CarouselContainer);

var _VideosCommonFunc = __webpack_require__(14);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var VideoDetailComponent = function (_React$Component) {
	_inherits(VideoDetailComponent, _React$Component);

	function VideoDetailComponent(props) {
		_classCallCheck(this, VideoDetailComponent);

		var _this = _possibleConstructorReturn(this, (VideoDetailComponent.__proto__ || Object.getPrototypeOf(VideoDetailComponent)).call(this, props));

		_this.fetchModelSlug = _this.fetchModelSlug.bind(_this);
		_this.fetchVideoDetailApi = _this.fetchVideoDetailApi.bind(_this);
		_this.fetchRelatedInfo = _this.fetchRelatedInfo.bind(_this);
		_this.logger = _this.logger.bind(_this);
		_this.scrollToPosition = _this.scrollToPosition.bind(_this);
		_this.renderCarousel = _this.renderCarousel.bind(_this);

		if (typeof gaObj != "undefined") {
			gaObj = _constants.GA_PAGE_MAPPING["VideoDetailsPage"];
		}
		return _this;
	}

	_createClass(VideoDetailComponent, [{
		key: 'fetchVideoDetailApi',
		value: function fetchVideoDetailApi() {
			if (this.props.VideoInfoStatus !== _constants.Status.Fetched && this.props.VideoInfoStatus !== _constants.Status.IsFetching) {
				this.props.fetchVideoDetail(this.props.match.params.basicId);
			}
		}
	}, {
		key: 'fetchModelSlug',
		value: function fetchModelSlug(props) {
			if (props.ModelInfoStatus !== _constants.Status.IsFetching && props.ModelInfoStatus !== _constants.Status.Fetched) {
				props.fetchModelSlug(props.match.params.basicId);
			}
		}
	}, {
		key: 'fetchRelatedInfo',
		value: function fetchRelatedInfo(props) {
			if (props.RelatedInfoApi && props.VideoInfoStatus === _constants.Status.Fetched && props.RelatedInfoStatus !== _constants.Status.Fetched && props.RelatedInfoStatus !== _constants.Status.IsFetching) {
				props.fetchRelatedInfo(props.RelatedInfoApi);
			}
		}
	}, {
		key: 'componentWillReceiveProps',
		value: function componentWillReceiveProps(nextProps) {
			try {

				var prevUrlParam = this.props.match.params;
				var nextUrlParam = nextProps.match.params;

				if (prevUrlParam.basicId === nextUrlParam.basicId) {
					if (nextProps) {
						// hit api for relatedinfoapi
						this.fetchRelatedInfo(nextProps);
					}
					return;
				}
				if (nextProps.VideoDetail && nextUrlParam.basicId !== nextProps.VideoDetail.BasicId) {
					// same component on screen but new basicid
					var initialDataDict = !nextProps.InitialDataDict || nextProps.InitialDataDict.length == 0 ? null : nextProps.InitialDataDict[nextUrlParam.basicId];
					this.props.fetchVideoDetail(initialDataDict || nextUrlParam.basicId);
					this.props.fetchModelSlug(nextUrlParam.basicId);
					if ((0, _scrollUtils.isBrowserWithoutScrollSupport)()) {
						window.scrollTo(0, 0);
					}
				} else if (nextProps && nextProps.VideoInfoStatus != _constants.Status.IsFetching) {
					// different basicid loaded but videodetail is null
					var initialDataDict = !nextProps.InitialDataDict || nextProps.InitialDataDict.length == 0 ? null : nextProps.InitialDataDict[nextUrlParam.basicId];
					this.props.fetchVideoDetail(initialDataDict || nextUrlParam.basicId);
					this.props.fetchModelSlug(nextUrlParam.basicId);
					if ((0, _scrollUtils.isBrowserWithoutScrollSupport)()) {
						window.scrollTo(0, 0);
					}
				}
			} catch (err) {
				console.log(err);
			}
		}
	}, {
		key: 'componentDidUpdate',
		value: function componentDidUpdate() {
			var BasicIsFromUrl = this.props.match.params.basicId;
			var basicIdFromStore = this.props.VideoDetail ? this.props.VideoDetail.BasicId : null;
			// var nextUrlParam = nextProps.match.params;
			if (BasicIsFromUrl == basicIdFromStore) {
				this.logger();
				this.scrollToPosition();
			}
		}
	}, {
		key: 'scrollToPosition',
		value: function scrollToPosition() {
			if (_scrollUtils.scrollPosition.x >= 0 && _scrollUtils.scrollPosition.y >= 0) {
				// needs to be scrolled
				if (this.props && this.props.VideoInfoStatus == _constants.Status.Fetched && this.props.ModelInfoStatus == _constants.Status.Fetched && this.props.RelatedInfoStatus == _constants.Status.Fetched) {
					// checks whether ready to scroll
					window.scrollTo(_scrollUtils.scrollPosition.x, _scrollUtils.scrollPosition.y);
					(0, _scrollUtils.resetScrollPosition)();
				}
			}
		}
	}, {
		key: 'componentDidMount',
		value: function componentDidMount() {
			this.logger();
			this.fetchVideoDetailApi();
			this.fetchModelSlug(this.props);
			if ((0, _scrollUtils.isBrowserWithoutScrollSupport)()) {
				window.scrollTo(0, 0);
			}
		}
	}, {
		key: 'componentWillUnmount',
		value: function componentWillUnmount() {
			this.props.resetVideoDetail();
		}
	}, {
		key: 'logger',
		value: function logger() {
			try {
				if (this.props && this.props.VideoInfoStatus == _constants.Status.Fetched && this.props.ModelInfoStatus == _constants.Status.Fetched && this.props.RelatedInfoStatus == _constants.Status.Fetched) {
					(0, _timing.endTimer)("component-render");
				}
			} catch (err) {}
		}
	}, {
		key: 'renderCarousel',
		value: function renderCarousel(relatedInfo, relatedInfoApi) {
			var carouselComponentList = [];
			for (var i = 0; i < relatedInfoApi.length; i++) {
				var data = relatedInfo[i];
				var carouselData = {
					Heading: data.Heading,
					CompleteListUrl: data.CompleteListUrl,
					CompleteListUrlAlternateLabel: data.CompleteListUrlAlternateLabel
				};
				var carouselComponent = null;
				switch (relatedInfoApi[i].Type) {
					case 0:
						carouselData.List = data.BikesList;
						carouselData.WrapperCssClass = "new-bike-carousel";
						carouselComponent = _react2.default.createElement(_CarouselContainer2.default, { carouselData: carouselData, childComponent: _CarouselBikeItem2.default });
						break;
					case 1:
						carouselData.List = data.VideosList;
						carouselData.WrapperCssClass = "video-swiper";
						carouselComponent = _react2.default.createElement(_CarouselContainer2.default, { carouselData: carouselData, childComponent: _CarouselVideoItem2.default });
						break;
				}

				if (carouselComponent) {
					carouselComponentList.push(carouselComponent);
				}
			}
			return carouselComponentList;
		}
	}, {
		key: 'render',
		value: function render() {
			var video = this.props.VideoDetail;
			if ((0, _commonUtils.isServer)()) {
				video = this.props.VideoInfo.VideoInfo;
			}

			if (!(0, _commonUtils.isServer)() && !video && !this.props.match.params && this.props.match.params.basicId !== video.BasicId) {
				return _react2.default.createElement(_SpinnerRelative2.default, null);
			}
			var initialData = !this.props.InitialDataDict ? null : this.props.InitialDataDict[this.props.match.params.basicId];

			if (this.props.VideoInfoStatus !== _constants.Status.Fetched && !(0, _commonUtils.isServer)()) {
				if (!initialData) return _react2.default.createElement(_Spinner2.default, null);
			}
			var videoUrl = null,
			    videoTitle = null,
			    displayDate = null;
			if (initialData) {

				videoUrl = initialData.VideoUrl;
				videoTitle = initialData.VideoTitle;
				displayDate = initialData.DisplayDate;
			} else {
				videoUrl = video.VideoUrl;
				videoTitle = video.VideoTitle;
				displayDate = video.DisplayDate;
			}
			var modelInfo = this.props.ModelInfo;
			var relatedInfo = this.props.RelatedInfo;
			var relatedInfoApi = this.props.RelatedInfoApi;
			if ((0, _commonUtils.isServer)()) {
				relatedInfoApi = this.props.VideoInfo.RelatedInfoApi;
			}
			return _react2.default.createElement(
				'div',
				null,
				_react2.default.createElement(_VideoDetail2.default, { videoDetail: video, initialData: { videoUrl: videoUrl, videoTitle: videoTitle, displayDate: displayDate } }),
				(0, _commonUtils.isServer)() || this.props.ModelInfoStatus == _constants.Status.Fetched && modelInfo ? _react2.default.createElement(_VideoModelSlug2.default, { modelInfo: modelInfo }) : null,
				(0, _commonUtils.isServer)() || this.props.RelatedInfoStatus == _constants.Status.Fetched && relatedInfo && relatedInfoApi ? this.renderCarousel(relatedInfo, relatedInfoApi) : null,
				(0, _commonUtils.isServer)() || this.props.VideoInfoStatus === _constants.Status.Fetched ? _react2.default.createElement(
					'div',
					null,
					_react2.default.createElement(_Breadcrumb2.default, { breadcrumb: [{ Href: '/m/', Title: 'Home' }, { Href: '/m/bike-videos/', Title: 'Videos', isReactLink: true }, { Href: '/m/bike-videos/category' + video.SectionUrl, Title: video.SectionTitle, isReactLink: true }, { Href: '', Title: videoTitle }] }),
					_react2.default.createElement(_Footer2.default, null)
				) : null
			);
		}
	}]);

	return VideoDetailComponent;
}(_react2.default.Component);

module.exports = (0, _reactRouterDom.withRouter)(VideoDetailComponent);

/***/ }),
/* 164 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

var _constants = __webpack_require__(4);

var _SocialMediaSlug = __webpack_require__(57);

var _SocialMediaSlug2 = _interopRequireDefault(_SocialMediaSlug);

var _Spinner = __webpack_require__(31);

var _Spinner2 = _interopRequireDefault(_Spinner);

var _SpinnerRelative = __webpack_require__(8);

var _SpinnerRelative2 = _interopRequireDefault(_SpinnerRelative);

var _commonUtils = __webpack_require__(3);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var VideoDetail = function (_React$Component) {
	_inherits(VideoDetail, _React$Component);

	function VideoDetail(props) {
		_classCallCheck(this, VideoDetail);

		var _this = _possibleConstructorReturn(this, (VideoDetail.__proto__ || Object.getPrototypeOf(VideoDetail)).call(this, props));

		_this.state = {
			shownShortDesc: true
		};
		if ((0, _commonUtils.isServer)()) {
			_this.props.Status = _constants.Status.Fetched;
		}
		_this.showShortDesc = _this.showShortDesc.bind(_this);

		return _this;
	}

	_createClass(VideoDetail, [{
		key: 'showShortDesc',
		value: function showShortDesc(event) {
			this.setState({
				shownShortDesc: !this.state.shownShortDesc
			});
		}
	}, {
		key: 'renderDescription',
		value: function renderDescription(description, shortDescription) {
			if (!shortDescription) {
				return _react2.default.createElement(
					'div',
					{ className: 'model-preview-main-content' },
					description
				);
			}

			if (this.state.shownShortDesc) {
				return _react2.default.createElement(
					'div',
					null,
					_react2.default.createElement(
						'div',
						{ className: 'model-preview-main-content' },
						shortDescription
					),
					_react2.default.createElement(
						'a',
						{ href: 'javascript:void(0)', className: 'font14 read-more-model-preview', rel: 'nofollow', onClick: this.showShortDesc.bind() },
						'Read more'
					)
				);
			} else {
				return _react2.default.createElement(
					'div',
					null,
					_react2.default.createElement(
						'div',
						{ className: 'model-preview-more-content' },
						description
					),
					_react2.default.createElement(
						'a',
						{ href: 'javascript:void(0)', className: 'font14 read-more-model-preview', rel: 'nofollow', onClick: this.showShortDesc.bind() },
						'Collapse'
					)
				);
			}
		}
	}, {
		key: 'componentDidUpdate',
		value: function componentDidUpdate() {
			try {
				var container = document.getElementById("g-ytsubscribe");
				if (container && container.childNodes && container.childNodes.length == 0 && gapi) {
					var options = {
						'channel': 'powerdriftofficial',
						'layout': 'default',
						'count': 'hidden'
					};
					gapi.ytsubscribe.render(container, options);
				}
			} catch (err) {}
		}
	}, {
		key: 'render',
		value: function render() {
			var video = this.props.videoDetail;
			// if(!video) return null;
			var initialData = this.props.initialData;
			var videoUrl = !initialData ? "" : initialData.videoUrl;
			var videoTitle = !initialData ? "" : initialData.videoTitle;
			var displayDate = !initialData ? "" : initialData.displayDate;
			if (!videoUrl || !videoTitle) return null;

			return _react2.default.createElement(
				'div',
				null,
				_react2.default.createElement(
					'section',
					{ className: 'bg-white' },
					_react2.default.createElement(
						'div',
						{ className: 'container margin-bottom10' },
						_react2.default.createElement(
							'div',
							{ id: 'embedVideo' },
							_react2.default.createElement('iframe', { height: '180', src: videoUrl, frameborder: '0', allowfullscreen: true })
						),
						_react2.default.createElement(
							'div',
							{ className: 'border-solid-bottom video-detail' },
							_react2.default.createElement(
								'h1',
								{ className: 'font18' },
								videoTitle
							),
							_react2.default.createElement(
								'p',
								{ className: 'card-list__info' },
								displayDate
							),
							!video ? _react2.default.createElement(_SpinnerRelative2.default, null) : _react2.default.createElement(
								'div',
								null,
								_react2.default.createElement(
									'div',
									{ className: 'card-list__details' },
									this.renderDescription(video.Description, video.ShortDescription)
								),
								_react2.default.createElement(
									'div',
									{ className: 'views-count-container font14 leftfloat border-light-right' },
									_react2.default.createElement('span', { className: 'bwmsprite video-views-icon margin-right5' }),
									_react2.default.createElement(
										'span',
										{ className: 'text-default comma' },
										video.Views
									)
								),
								_react2.default.createElement(
									'div',
									{ className: 'views-count-container font14 leftfloat padding-left10 border-light-right' },
									_react2.default.createElement('span', { className: 'bwmsprite video-likes-icon margin-right5' }),
									_react2.default.createElement(
										'span',
										{ className: 'text-default comma' },
										video.Likes
									)
								),
								_react2.default.createElement(
									'div',
									{ className: 'font14 leftfloat padding-left10 powerdrift-sub-btn' },
									_react2.default.createElement('script', { defer: true, src: 'https://apis.google.com/js/platform.js?onload=onLoadCallback' }),
									_react2.default.createElement('div', { id: 'g-ytsubscribe', className: 'g-ytsubscribe', 'data-channel': 'powerdriftofficial', 'data-layout': 'default', 'data-count': 'hidden' })
								),
								_react2.default.createElement('div', { className: 'clear' }),
								_react2.default.createElement(_SocialMediaSlug2.default, null),
								_react2.default.createElement('div', { className: 'clear' })
							)
						)
					)
				)
			);
		}
	}]);

	return VideoDetail;
}(_react2.default.Component);

module.exports = VideoDetail;

// <a href="javascript:void(0);"
// onclick={this.renderYT.bind()}>Click here for a button!</a>

/***/ }),
/* 165 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

var _constants = __webpack_require__(4);

var _reactLazyLoad = __webpack_require__(6);

var _reactLazyLoad2 = _interopRequireDefault(_reactLazyLoad);

var _commonUtils = __webpack_require__(3);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var VideoModelSlug = function (_React$Component) {
	_inherits(VideoModelSlug, _React$Component);

	function VideoModelSlug(props) {
		_classCallCheck(this, VideoModelSlug);

		var _this = _possibleConstructorReturn(this, (VideoModelSlug.__proto__ || Object.getPrototypeOf(VideoModelSlug)).call(this, props));

		if ((0, _commonUtils.isServer)()) {
			_this.props.Status = _constants.Status.Fetched;
		}
		_this.renderUsedBikesLink = _this.renderUsedBikesLink.bind(_this);

		return _this;
	}

	_createClass(VideoModelSlug, [{
		key: 'renderUsedBikesLink',
		value: function renderUsedBikesLink(model, usedBikesLink) {

			if (!usedBikesLink) return null;

			return _react2.default.createElement(
				'div',
				null,
				_react2.default.createElement('div', { className: 'border-solid-bottom margin-top5 margin-bottom10' }),
				_react2.default.createElement(
					'a',
					{ href: usedBikesLink.UsedBikesLinkUrl, title: "Used " + model.ModelName, className: 'block text-default' },
					_react2.default.createElement(
						'span',
						{ className: 'used-target-label inline-block' },
						_react2.default.createElement(
							'span',
							{ className: 'font14 text-bold' },
							usedBikesLink.DescriptionLabel
						),
						_react2.default.createElement('br', null),
						_react2.default.createElement(
							'span',
							{ className: 'font12 text-light-grey' },
							usedBikesLink.PricePrefix,
							' ',
							_react2.default.createElement(
								'span',
								null,
								'\u20B9'
							),
							' ',
							usedBikesLink.Price
						)
					),
					_react2.default.createElement('span', { className: 'bwmsprite next-grey-icon' })
				)
			);
		}
	}, {
		key: 'getClassNameForBikeInfo',
		value: function getClassNameForBikeInfo(type) {
			switch (type) {
				case 'Expert Reviews':
					return 'reviews-sm';
				case 'Images':
					return 'photos-sm';
				case 'Specs':
					return 'specs-sm';
				case 'News':
					return 'news-sm';
				case 'Videos':
					return 'videos-sm';
				case 'Dealers':
					return 'dealers-sm';
				case 'User Reviews':
					return 'user-reviews-sm';
			}
			return '';
		}
	}, {
		key: 'renderMoreDetailsListItem',
		value: function renderMoreDetailsListItem(item) {
			if (!item) return false;
			var className = this.getClassNameForBikeInfo(item.Type);
			if (className == '') return false;
			return _react2.default.createElement(
				'li',
				{ key: item.Type },
				_react2.default.createElement(
					'a',
					{ href: item.DetailUrl, title: item.Title },
					_react2.default.createElement('span', { className: "bwmsprite " + className }),
					_react2.default.createElement(
						'span',
						{ className: 'icon-label' },
						item.Type
					)
				)
			);
		}
	}, {
		key: 'renderMoreDetailsList',
		value: function renderMoreDetailsList(moreDetailsList) {
			if (!moreDetailsList || !(moreDetailsList.length > 0)) return false;
			return _react2.default.createElement(
				'ul',
				{ className: 'item-more-details-list' },
				moreDetailsList.map(function (item) {
					return this.renderMoreDetailsListItem(item);
				}.bind(this))
			);
		}
	}, {
		key: 'render',
		value: function render() {

			var model = this.props.modelInfo;
			if (!model) return null;

			var ribbonTagComponent = null;

			if (model.Upcoming.toLowerCase() == "true") ribbonTagComponent = _react2.default.createElement(
				'p',
				{ className: 'model-ribbon-tag upcoming-ribbon' },
				'Upcoming'
			);else if (model.Discontinued.toLowerCase() == "true") ribbonTagComponent = _react2.default.createElement(
				'p',
				{ className: 'model-ribbon-tag discontinued-ribbon' },
				'Discontinued'
			);

			var ratingComponent = null;
			if (model.Rating && model.Rating.Count > 0) {
				ratingComponent = _react2.default.createElement(
					'div',
					{ id: 'reviewRatingsDiv', className: 'margin-bottom10 position-rel position--left-neg-5' },
					_react2.default.createElement(
						'span',
						{ className: "rate-count-" + Math.round(model.Rating.Rating) },
						_react2.default.createElement('span', { className: 'bwmsprite star-icon star-size-16' }),
						_react2.default.createElement(
							'span',
							{ className: 'font14 text-bold inline-block' },
							model.Rating.Rating
						)
					),
					_react2.default.createElement(
						'span',
						{ className: 'font11 text-xx-light inline-block padding-left5' },
						'(',
						model.Rating.Count + ' ' + (model.Rating.Count > 1 ? 'ratings' : 'rating'),
						')'
					),
					model.Rating.ReviewCount > 0 ? _react2.default.createElement(
						'a',
						{ className: 'text-xt-light review-left-divider inline-block font13', href: model.Rating.ReviewUrl, title: model.ModelName + " user reviews" },
						model.Rating.ReviewCount + " " + (model.Rating.ReviewCount > 1 ? "reviews" : "review")
					) : null
				);
			}

			return _react2.default.createElement(
				'div',
				{ className: 'bg-white box-shadow section-bottom-margin padding-15-20' },
				_react2.default.createElement(
					'div',
					{ className: 'model-more-info-section' },
					ribbonTagComponent,
					_react2.default.createElement('div', { className: 'clear' }),
					_react2.default.createElement(
						'a',
						{ href: model.ModelDetailUrl, className: 'block text-default margin-bottom5', title: model.ModelName },
						_react2.default.createElement(
							'h2',
							null,
							model.ModelName
						)
					),
					ratingComponent,
					_react2.default.createElement(
						'div',
						{ className: 'misc-container margin-bottom10' },
						_react2.default.createElement('div', { className: 'clear' }),
						_react2.default.createElement(
							'a',
							{ href: model.ModelDetailUrl, className: 'misc-list-image vertical-top', title: model.ModelName },
							_react2.default.createElement(
								'div',
								{ className: 'misc-container__image' },
								_react2.default.createElement(
									_reactLazyLoad2.default,
									null,
									_react2.default.createElement('img', { src: model.ImageUrl, alt: model.ModelName })
								)
							)
						),
						_react2.default.createElement(
							'div',
							{ className: 'misc-details-block' },
							_react2.default.createElement(
								'p',
								{ className: "price-label-size-12" + (model.Upcoming.toLowerCase() == "true" ? "" : " text-truncate") },
								model.PriceDescription
							),
							_react2.default.createElement(
								'div',
								null,
								_react2.default.createElement(
									'span',
									{ className: 'price-value-size-18' },
									'\u20B9'
								),
								'\xA0',
								_react2.default.createElement(
									'span',
									{ className: 'price-value-size-18 version-price' },
									model.Price
								)
							)
						)
					),
					_react2.default.createElement(
						'a',
						{ href: model.ModelDetailUrl, title: model.ModelName, className: 'btn btn-white btn-180-34 margin-bottom15' },
						'View model details ',
						_react2.default.createElement('span', { className: 'bwmsprite btn-red-arrow' })
					),
					this.renderMoreDetailsList(),
					_react2.default.createElement('div', { className: 'clear' }),
					this.renderUsedBikesLink(model, model.UsedBikesLink)
				)
			);
		}
	}]);

	return VideoModelSlug;
}(_react2.default.Component);

module.exports = VideoModelSlug;

/***/ }),
/* 166 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

var _reactLazyLoad = __webpack_require__(6);

var _reactLazyLoad2 = _interopRequireDefault(_reactLazyLoad);

var _CarouselBikeItem = __webpack_require__(64);

var _CarouselBikeItem2 = _interopRequireDefault(_CarouselBikeItem);

var _CarouselVideoItem = __webpack_require__(65);

var _CarouselVideoItem2 = _interopRequireDefault(_CarouselVideoItem);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var CarouselContainer = function (_React$Component) {
    _inherits(CarouselContainer, _React$Component);

    function CarouselContainer(props) {
        _classCallCheck(this, CarouselContainer);

        return _possibleConstructorReturn(this, (CarouselContainer.__proto__ || Object.getPrototypeOf(CarouselContainer)).call(this, props));
    }

    _createClass(CarouselContainer, [{
        key: 'render',
        value: function render() {

            if (!this.props.carouselData || !this.props.carouselData.List || this.props.carouselData.List.length == 0 || !this.props.childComponent) return null;
            var carouselData = this.props.carouselData;
            var ChildComponent = this.props.childComponent;

            return _react2.default.createElement(
                'div',
                { className: 'container bg-white box-shadow section-bottom-margin carousel-bottom-padding carousel-top-padding' },
                _react2.default.createElement(
                    'div',
                    { className: 'carousel-heading-content' },
                    _react2.default.createElement(
                        'div',
                        { className: 'swiper-heading-left-grid inline-block' },
                        _react2.default.createElement(
                            'h2',
                            null,
                            carouselData.Heading
                        )
                    ),
                    _react2.default.createElement(
                        'div',
                        { className: 'swiper-heading-right-grid inline-block text-right' },
                        _react2.default.createElement(
                            'a',
                            { href: carouselData.CompleteListUrl, title: carouselData.CompleteListUrlAlternateLabel, className: 'btn view-all-target-btn' },
                            'View all'
                        )
                    )
                ),
                _react2.default.createElement(
                    'ul',
                    { className: "carousel-wrapper " + carouselData.wrapperCssClass },
                    carouselData.List.map(function (item) {
                        return _react2.default.createElement(ChildComponent, { item: item });
                    }.bind(this))
                )
            );
        }
    }]);

    return CarouselContainer;
}(_react2.default.Component);

module.exports = CarouselContainer;

/***/ }),
/* 167 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
	value: true
});

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

var _Tabs = __webpack_require__(168);

var _Tabs2 = _interopRequireDefault(_Tabs);

var _Disclaimer = __webpack_require__(236);

var _Disclaimer2 = _interopRequireDefault(_Disclaimer);

var _Breadcrumb = __webpack_require__(23);

var _Breadcrumb2 = _interopRequireDefault(_Breadcrumb);

var _AdUnit = __webpack_require__(21);

var _AdUnit2 = _interopRequireDefault(_AdUnit);

var _Footer = __webpack_require__(24);

var _Footer2 = _interopRequireDefault(_Footer);

var _constants = __webpack_require__(4);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

if (false) {
	require('../../../stylesheet/finance.sass');
}

var FinanceComponent = function (_React$Component) {
	_inherits(FinanceComponent, _React$Component);

	function FinanceComponent(props) {
		_classCallCheck(this, FinanceComponent);

		var _this = _possibleConstructorReturn(this, (FinanceComponent.__proto__ || Object.getPrototypeOf(FinanceComponent)).call(this, props));

		if (typeof gaObj != "undefined") {
			gaObj = _constants.GA_PAGE_MAPPING["FinancePage"];
		}
		return _this;
	}

	_createClass(FinanceComponent, [{
		key: 'render',
		value: function render() {
			return _react2.default.createElement(
				'div',
				{ className: 'finance-content' },
				_react2.default.createElement(_AdUnit2.default, { uniqueKey: 'finance-page', tags: null, adSlot: '/1017752/BikeWale_Finance_Top_320x50', adDimension: [320, 50], adContainerId: 'div-gpt-ad-1525945337139-0' }),
				_react2.default.createElement(
					'div',
					{ className: 'finance-content__body' },
					_react2.default.createElement(
						'div',
						{ className: 'finance-content__head' },
						_react2.default.createElement(
							'h1',
							{ className: 'finance-head__title' },
							'EMI Calculator - Calculate Your Bike Loan EMI'
						)
					),
					_react2.default.createElement(_Tabs2.default, null),
					_react2.default.createElement(_Disclaimer2.default, null),
					_react2.default.createElement(_AdUnit2.default, { uniqueKey: 'finance-page', tags: null, adSlot: '/1017752/BikeWale_Finance_Bottom_320x50', adDimension: [[320, 50], [320, 100]], adContainerId: 'div-gpt-ad-1525945337139-2' }),
					_react2.default.createElement(_Breadcrumb2.default, { breadcrumb: [{ Href: '/m/', Title: 'Home' }, { Href: '', Title: 'EMI Calculator' }] })
				),
				_react2.default.createElement(_Footer2.default, null)
			);
		}
	}]);

	return FinanceComponent;
}(_react2.default.Component);

exports.default = FinanceComponent;

/***/ }),
/* 168 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

var _reactExpandCollapse = __webpack_require__(66);

var _reactExpandCollapse2 = _interopRequireDefault(_reactExpandCollapse);

var _scrollSpyTabs = __webpack_require__(169);

var _EMITabContainer = __webpack_require__(171);

var _EMITabContainer2 = _interopRequireDefault(_EMITabContainer);

var _Documentation = __webpack_require__(233);

var _Documentation2 = _interopRequireDefault(_Documentation);

var _FAQ = __webpack_require__(234);

var _FAQ2 = _interopRequireDefault(_FAQ);

var _AdUnit = __webpack_require__(21);

var _AdUnit2 = _interopRequireDefault(_AdUnit);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var Tabs = function (_React$Component) {
  _inherits(Tabs, _React$Component);

  function Tabs(props) {
    _classCallCheck(this, Tabs);

    var _this = _possibleConstructorReturn(this, (Tabs.__proto__ || Object.getPrototypeOf(Tabs)).call(this, props));

    _this.state = {
      selectBikePopupStatus: false
    };

    _this.setRef = _this.setRef.bind(_this);
    return _this;
  }

  _createClass(Tabs, [{
    key: 'componentDidMount',
    value: function componentDidMount() {
      (0, _scrollSpyTabs.addTabEvents)(this.tabsContainer);
    }
  }, {
    key: 'componentWillUnmount',
    value: function componentWillUnmount() {
      (0, _scrollSpyTabs.removeTabEvents)(this.tabsContainer);
    }
  }, {
    key: 'setRef',
    value: function setRef(ref) {
      this.tabsContainer = ref;
    }
  }, {
    key: 'render',
    value: function render() {
      return _react2.default.createElement(
        'div',
        { ref: this.setRef, className: 'tabs__container' },
        _react2.default.createElement(
          'div',
          { id: 'quickLinksTab', className: 'tabs__placeholder' },
          _react2.default.createElement(
            'div',
            { className: 'tabs__content' },
            _react2.default.createElement(
              'ul',
              { className: 'tabs__list' },
              _react2.default.createElement(
                'li',
                { className: 'tabs-list__item', 'data-tab': 'overviewTab' },
                'Overview'
              ),
              _react2.default.createElement(
                'li',
                { className: 'tabs-list__item', 'data-tab': 'emiTab' },
                'EMI Calculator'
              ),
              _react2.default.createElement(
                'li',
                { className: 'tabs-list__item', 'data-tab': 'documentationTab' },
                'Documentation'
              ),
              _react2.default.createElement(
                'li',
                { className: 'tabs-list__item', 'data-tab': 'faqTab' },
                'FAQ'
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { className: 'tabs-panel__item', 'data-tab-panel': 'overviewTab' },
          _react2.default.createElement(
            'div',
            { className: 'overview__content' },
            _react2.default.createElement(
              _reactExpandCollapse2.default,
              { previewHeight: '66px', expandText: 'Read more' },
              'Bike loan EMI calculation was never this easy. Just select the bike you wish to avail loan for and which city do you wish to purchase the bike. Bike loan EMI calculation was never this easy. Just select the bike you wish to avail loan for and which city do you wish to purchase the bike. Bike loan EMI calculation was never this easy. Just select the bike you wish to avail loan for and which city do you wish to purchase the bike.'
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { className: 'tabs-panel__item', 'data-tab-panel': 'emiTab' },
          _react2.default.createElement(
            'div',
            { className: 'emi-calculator' },
            _react2.default.createElement(_EMITabContainer2.default, null)
          )
        ),
        _react2.default.createElement(
          'div',
          { className: 'tabs-panel__item', 'data-tab-panel': 'documentationTab' },
          _react2.default.createElement(
            'div',
            { className: 'finance-documentation' },
            _react2.default.createElement(_Documentation2.default, null)
          )
        ),
        _react2.default.createElement(_AdUnit2.default, { uniqueKey: 'finance-page', tags: null, adSlot: '/1017752/BikeWale_Finance_BTF_300x250', adDimension: [300, 250], adContainerId: 'div-gpt-ad-1525945337139-1' }),
        _react2.default.createElement(
          'div',
          { className: 'tabs-panel__item', 'data-tab-panel': 'faqTab' },
          _react2.default.createElement(
            'div',
            { className: 'finance-faq' },
            _react2.default.createElement(_FAQ2.default, null)
          )
        )
      );
    }
  }]);

  return Tabs;
}(_react2.default.Component);

exports.default = Tabs;

/***/ }),
/* 169 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var _closestPolyfill = __webpack_require__(38);

var _closestPolyfill2 = _interopRequireDefault(_closestPolyfill);

var _scrollTo = __webpack_require__(170);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _toConsumableArray(arr) { if (Array.isArray(arr)) { for (var i = 0, arr2 = Array(arr.length); i < arr.length; i++) { arr2[i] = arr[i]; } return arr2; } else { return Array.from(arr); } }

function centerTabLink(targetLink) {
  var tabList = targetLink.closest('.tabs__list');
  var listWidth = tabList.offsetWidth;
  var targetLinkWidth = targetLink.offsetWidth;
  var targetLinkIndex = Array.prototype.indexOf.call(tabList.children, targetLink);

  var tabLinks = [].concat(_toConsumableArray(tabList.querySelectorAll('.tabs-list__item')));
  var offsetLeft = 0;

  // Add up the width of all the elements before target link
  for (var i = 0; i < targetLinkIndex; i++) {
    offsetLeft += tabLinks[i].offsetWidth;
  }

  var scrollXPosition = Math.max(0, offsetLeft - (listWidth - targetLinkWidth) / 2);
  tabList.closest('.tabs__content').scrollLeft = scrollXPosition;
}

function handleTabLinkClick(targetLink) {
  var tabName = targetLink.getAttribute('data-tab');
  centerTabLink(targetLink);

  var tabsContainer = targetLink.closest('.tabs__container');
  var tabListHeight = tabsContainer.querySelector('.tabs__content').offsetHeight;

  var targetPanel = tabsContainer.querySelector('.tabs-panel__item[data-tab-panel="' + tabName + '"]');
  var panelOffsetTop = targetPanel.getBoundingClientRect().top + (window.pageYOffset || document.documentElement.scrollTop) - tabListHeight;
  var scrollPosition = Math.ceil(panelOffsetTop);

  (0, _scrollTo.scrollTop)(window, scrollPosition);
}

function handleStickyTabs(tabsContainer) {
  var tabsRect = tabsContainer.getBoundingClientRect();
  var tabListHeight = tabsContainer.querySelector('.tabs__content').offsetHeight;

  if (tabsRect.top < 0 && tabsRect.height + tabsRect.top - tabListHeight * 2 > 0) {
    tabsContainer.classList.add('tabs--fixed');
  } else {
    tabsContainer.classList.remove('tabs--fixed');
  }
}

function handleTabLinkHighlight(tabsContainer) {
  var tabPanels = [].concat(_toConsumableArray(tabsContainer.querySelectorAll('.tabs-panel__item')));

  tabPanels.forEach(function (panel) {
    var windowScrollPosition = window.pageYOffset || document.documentElement.scrollTop;

    var tabListHeight = tabsContainer.querySelector('.tabs__content').offsetHeight;
    var panelTopPosition = panel.getBoundingClientRect().top + windowScrollPosition - tabListHeight;
    var panelBottomPosition = panelTopPosition + panel.offsetHeight;

    if (windowScrollPosition >= panelTopPosition && windowScrollPosition <= panelBottomPosition) {
      var prevActiveTabLink = tabsContainer.querySelector('.tab--active');
      if (prevActiveTabLink) {
        prevActiveTabLink.classList.remove('tab--active');
      }

      var panelName = panel.getAttribute('data-tab-panel');
      var nextActiveTabLink = tabsContainer.querySelector('.tabs-list__item[data-tab="' + panelName + '"]');
      nextActiveTabLink.classList.add('tab--active');
      centerTabLink(nextActiveTabLink);
    }
  });
}

function setDefaultTabLink(tabsContainer) {
  if (!tabsContainer.querySelector('.tab--active')) {
    tabsContainer.querySelector('.tabs-list__item').classList.add('tab--active');
  }
}

function scrollEvents(tabsContainer) {
  handleStickyTabs(tabsContainer);
  handleTabLinkHighlight(tabsContainer);
}

function addTabEvents(tabsContainer) {
  // Attach tab links click event
  var tabLinks = [].concat(_toConsumableArray(tabsContainer.querySelectorAll('.tabs-list__item')));

  tabLinks.forEach(function (tab) {
    tab.addEventListener('click', function () {
      handleTabLinkClick(this);
    });
  });

  setDefaultTabLink(tabsContainer);

  // Attach sticky tabs and tab link highlight event
  window.addEventListener('scroll', scrollEvents.bind(this, tabsContainer));
}

function removeTabEvents(tabsContainer) {
  window.removeEventListener('scroll', scrollEvents.bind(this, tabsContainer));
}

module.exports = {
  addTabEvents: addTabEvents,
  removeTabEvents: removeTabEvents
};

/***/ }),
/* 170 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});

function _toConsumableArray(arr) { if (Array.isArray(arr)) { for (var i = 0, arr2 = Array(arr.length); i < arr.length; i++) { arr2[i] = arr[i]; } return arr2; } else { return Array.from(arr); } }

/*
 * ScrollTo animation using pure javascript
 * https://gist.github.com/andjosh/6764939
 * 
 * debounce
 * https://davidwalsh.name/javascript-debounce-function
 */

var scrollTop = exports.scrollTop = function scrollTop(element, to) {
  var duration = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : 500;

  var isElementWindow = element === window ? true : false;

  var start = isElementWindow ? window.pageYOffset || document.documentElement.scrollTop : element.scrollTop,
      change = to - start,
      currentTime = 0,
      increment = 20;

  var animateScroll = function animateScroll() {
    currentTime += increment;
    var val = Math.easeInOutQuad(currentTime, start, change, duration);
    if (isElementWindow) {
      element.scrollTo(0, val);
    } else {
      element.scrollTop = val;
    }

    if (currentTime < duration) {
      setTimeout(animateScroll, increment);
    }
  };

  animateScroll();
};

var inView = exports.inView = function inView(element) {
  var isViewSelected = false;
  [].concat(_toConsumableArray(element.children)).forEach(function (item) {

    if (isElementVisible(item.firstChild, 'horizontal', 50)) {
      if (isViewSelected != true) {
        [].concat(_toConsumableArray(item.parentElement.children)).forEach(function (child) {
          child.classList.remove('inview--active');
        });

        item.classList.add("inview--active");
        isViewSelected = true;
      }
    }
  });
};

/*
  direction: horizontal, vertical
  threshold: visiblity of card on viewport eg, 50, 75
*/
var isElementVisible = exports.isElementVisible = function isElementVisible(element, direction, threshold) {
  var elementRect = element.getBoundingClientRect();
  var thresholdValue;
  if (typeof threshold === 'number') {
    thresholdValue = threshold ? elementRect.width * (parseInt(threshold) / 100) : 0;
  } else {
    console.log('please provide threshold value as int in isElementVisible');
  }

  switch (direction) {
    case 'horizontal':
      return elementRect.left + thresholdValue > 0 && elementRect.right - thresholdValue < window.innerWidth;
    case 'vertical':
      return elementRect.top + thresholdValue > 0 && elementRect.bottom - thresholdValue < window.innerHeight;
    default:
      return elementRect.left + thresholdValue > 0 && elementRect.right - thresholdValue < window.innerWidth && elementRect.top + thresholdValue > 0 && elementRect.bottom - thresholdValue < window.innerHeight;
  }
};
var scrollLeft = exports.scrollLeft = function scrollLeft(element, to) {
  var duration = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : 500;

  var start = element.scrollLeft,
      change = to - start,
      currentTime = 0,
      increment = 20;

  var animateScroll = function animateScroll() {
    currentTime += increment;
    var val = Math.easeInOutQuad(currentTime, start, change, duration);
    element.scrollLeft = val;

    if (currentTime < duration) {
      setTimeout(animateScroll, increment);
    }
  };

  animateScroll();
};

var scrollIntoView = exports.scrollIntoView = function scrollIntoView(element, event) {
  if (event == null) {
    return null;
  }
  var elementRect = null;
  if (event.currentTarget != null) {
    elementRect = event.currentTarget.getBoundingClientRect();
  } else {
    elementRect = event.getBoundingClientRect();
  }

  if (elementRect.left < 0 || elementRect.right > window.innerWidth) {
    var leftPosition = element.scrollLeft + elementRect.left - 20;

    scrollLeft(element, leftPosition);
  }
};

//t = current time
//b = start value
//c = change in value
//d = duration
Math.easeInOutQuad = function (t, b, c, d) {
  t /= d / 2;
  if (t < 1) {
    return c / 2 * t * t + b;
  }
  t--;
  return -c / 2 * (t * (t - 2) - 1) + b;
};

/***/ }),
/* 171 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});

var _reactRedux = __webpack_require__(12);

var _redux = __webpack_require__(10);

var _immutableWrapperContainer = __webpack_require__(9);

var _SelectBikePopup = __webpack_require__(75);

var _FinanceCityPopup = __webpack_require__(76);

var _SimilarBikesEMI = __webpack_require__(78);

var _emiDownPaymentSlider = __webpack_require__(41);

var _toast = __webpack_require__(197);

var _EMITab = __webpack_require__(199);

var _EMITab2 = _interopRequireDefault(_EMITab);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var mapStateToProps = function mapStateToProps(store) {
  return {
    selectBikePopup: store.getIn(['Finance', 'SelectBikePopup']),
    FinanceCityPopup: store.getIn(['Finance', 'FinanceCityPopup']),
    SimilarBikesEMI: store.getIn(['Finance', 'SimilarBikesEMI']),
    sliderDp: store.getIn(['Emi', 'VehicleDownPayment', 'slider']),
    sliderTenure: store.getIn(['Emi', 'VehicleTenure', 'slider']),
    sliderInt: store.getIn(['Emi', 'VehicleInterest', 'slider'])
  };
};

var mapDispatchToProps = function mapDispatchToProps(dispatch) {
  return {
    openSelectBikePopup: (0, _redux.bindActionCreators)(_SelectBikePopup.openSelectBikePopup, dispatch),
    closeSelectBikePopup: (0, _redux.bindActionCreators)(_SelectBikePopup.closeSelectBikePopup, dispatch),
    fetchMakeModelList: (0, _redux.bindActionCreators)(_SelectBikePopup.fetchMakeModelList, dispatch),
    resetMakeModelListFailure: (0, _redux.bindActionCreators)(_SelectBikePopup.resetMakeModelListFailure, dispatch),
    fetchSelectedBikeDetail: (0, _redux.bindActionCreators)(_SelectBikePopup.fetchSelectedBikeDetail, dispatch),
    resetSelectedBikeDetailFailure: (0, _redux.bindActionCreators)(_SelectBikePopup.resetSelectedBikeDetailFailure, dispatch),
    fetchBikeVersionList: (0, _redux.bindActionCreators)(_SelectBikePopup.fetchBikeVersionList, dispatch),
    resetBikeVersionListFailure: (0, _redux.bindActionCreators)(_SelectBikePopup.resetBikeVersionListFailure, dispatch),
    openSelectCityPopup: (0, _redux.bindActionCreators)(_FinanceCityPopup.openSelectCityPopup, dispatch),
    closeSelectCityPopup: (0, _redux.bindActionCreators)(_FinanceCityPopup.closeSelectCityPopup, dispatch),
    fetchCity: (0, _redux.bindActionCreators)(_FinanceCityPopup.fetchCity, dispatch),
    resetCityFailure: (0, _redux.bindActionCreators)(_FinanceCityPopup.resetCityFailure, dispatch),
    selectCity: (0, _redux.bindActionCreators)(_FinanceCityPopup.selectCity, dispatch),
    fetchSimilarBikes: (0, _redux.bindActionCreators)(_SimilarBikesEMI.fetchSimilarBikes, dispatch),
    selectModel: (0, _redux.bindActionCreators)(_SelectBikePopup.selectModel, dispatch),
    updateSimilarBikesEmi: (0, _redux.bindActionCreators)(_SimilarBikesEMI.updateSimilarBikesEmi, dispatch),
    openEmiCalculator: (0, _redux.bindActionCreators)(_emiDownPaymentSlider.openEmiCalculator, dispatch),
    initToast: (0, _redux.bindActionCreators)(_toast.initToast, dispatch),
    clearToast: (0, _redux.bindActionCreators)(_toast.clearToast, dispatch),
    resetVersionSelection: (0, _redux.bindActionCreators)(_SelectBikePopup.resetVersionSelection, dispatch)
  };
};

exports.default = (0, _reactRedux.connect)(mapStateToProps, mapDispatchToProps)((0, _immutableWrapperContainer.toJS)(_EMITab2.default));

/***/ }),
/* 172 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony export (immutable) */ __webpack_exports__["a"] = createProvider;
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_react__ = __webpack_require__(0);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_prop_types__ = __webpack_require__(1);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_prop_types___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_1_prop_types__);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2__utils_PropTypes__ = __webpack_require__(67);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_3__utils_warning__ = __webpack_require__(39);
function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }






var didWarnAboutReceivingStore = false;
function warnAboutReceivingStore() {
  if (didWarnAboutReceivingStore) {
    return;
  }
  didWarnAboutReceivingStore = true;

  Object(__WEBPACK_IMPORTED_MODULE_3__utils_warning__["a" /* default */])('<Provider> does not support changing `store` on the fly. ' + 'It is most likely that you see this error because you updated to ' + 'Redux 2.x and React Redux 2.x which no longer hot reload reducers ' + 'automatically. See https://github.com/reactjs/react-redux/releases/' + 'tag/v2.0.0 for the migration instructions.');
}

function createProvider() {
  var _Provider$childContex;

  var storeKey = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : 'store';
  var subKey = arguments[1];

  var subscriptionKey = subKey || storeKey + 'Subscription';

  var Provider = function (_Component) {
    _inherits(Provider, _Component);

    Provider.prototype.getChildContext = function getChildContext() {
      var _ref;

      return _ref = {}, _ref[storeKey] = this[storeKey], _ref[subscriptionKey] = null, _ref;
    };

    function Provider(props, context) {
      _classCallCheck(this, Provider);

      var _this = _possibleConstructorReturn(this, _Component.call(this, props, context));

      _this[storeKey] = props.store;
      return _this;
    }

    Provider.prototype.render = function render() {
      return __WEBPACK_IMPORTED_MODULE_0_react__["Children"].only(this.props.children);
    };

    return Provider;
  }(__WEBPACK_IMPORTED_MODULE_0_react__["Component"]);

  if (true) {
    Provider.prototype.componentWillReceiveProps = function (nextProps) {
      if (this[storeKey] !== nextProps.store) {
        warnAboutReceivingStore();
      }
    };
  }

  Provider.propTypes = {
    store: __WEBPACK_IMPORTED_MODULE_2__utils_PropTypes__["a" /* storeShape */].isRequired,
    children: __WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.element.isRequired
  };
  Provider.childContextTypes = (_Provider$childContex = {}, _Provider$childContex[storeKey] = __WEBPACK_IMPORTED_MODULE_2__utils_PropTypes__["a" /* storeShape */].isRequired, _Provider$childContex[subscriptionKey] = __WEBPACK_IMPORTED_MODULE_2__utils_PropTypes__["b" /* subscriptionShape */], _Provider$childContex);

  return Provider;
}

/* harmony default export */ __webpack_exports__["b"] = (createProvider());

/***/ }),
/* 173 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "a", function() { return Subscription; });
function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

// encapsulates the subscription logic for connecting a component to the redux store, as
// well as nesting subscriptions of descendant components, so that we can ensure the
// ancestor components re-render before descendants

var CLEARED = null;
var nullListeners = {
  notify: function notify() {}
};

function createListenerCollection() {
  // the current/next pattern is copied from redux's createStore code.
  // TODO: refactor+expose that code to be reusable here?
  var current = [];
  var next = [];

  return {
    clear: function clear() {
      next = CLEARED;
      current = CLEARED;
    },
    notify: function notify() {
      var listeners = current = next;
      for (var i = 0; i < listeners.length; i++) {
        listeners[i]();
      }
    },
    get: function get() {
      return next;
    },
    subscribe: function subscribe(listener) {
      var isSubscribed = true;
      if (next === current) next = current.slice();
      next.push(listener);

      return function unsubscribe() {
        if (!isSubscribed || current === CLEARED) return;
        isSubscribed = false;

        if (next === current) next = current.slice();
        next.splice(next.indexOf(listener), 1);
      };
    }
  };
}

var Subscription = function () {
  function Subscription(store, parentSub, onStateChange) {
    _classCallCheck(this, Subscription);

    this.store = store;
    this.parentSub = parentSub;
    this.onStateChange = onStateChange;
    this.unsubscribe = null;
    this.listeners = nullListeners;
  }

  Subscription.prototype.addNestedSub = function addNestedSub(listener) {
    this.trySubscribe();
    return this.listeners.subscribe(listener);
  };

  Subscription.prototype.notifyNestedSubs = function notifyNestedSubs() {
    this.listeners.notify();
  };

  Subscription.prototype.isSubscribed = function isSubscribed() {
    return Boolean(this.unsubscribe);
  };

  Subscription.prototype.trySubscribe = function trySubscribe() {
    if (!this.unsubscribe) {
      this.unsubscribe = this.parentSub ? this.parentSub.addNestedSub(this.onStateChange) : this.store.subscribe(this.onStateChange);

      this.listeners = createListenerCollection();
    }
  };

  Subscription.prototype.tryUnsubscribe = function tryUnsubscribe() {
    if (this.unsubscribe) {
      this.unsubscribe();
      this.unsubscribe = null;
      this.listeners.clear();
      this.listeners = nullListeners;
    }
  };

  return Subscription;
}();



/***/ }),
/* 174 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* unused harmony export createConnect */
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0__components_connectAdvanced__ = __webpack_require__(68);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1__utils_shallowEqual__ = __webpack_require__(175);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2__mapDispatchToProps__ = __webpack_require__(176);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_3__mapStateToProps__ = __webpack_require__(191);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_4__mergeProps__ = __webpack_require__(192);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_5__selectorFactory__ = __webpack_require__(193);
var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

function _objectWithoutProperties(obj, keys) { var target = {}; for (var i in obj) { if (keys.indexOf(i) >= 0) continue; if (!Object.prototype.hasOwnProperty.call(obj, i)) continue; target[i] = obj[i]; } return target; }








/*
  connect is a facade over connectAdvanced. It turns its args into a compatible
  selectorFactory, which has the signature:

    (dispatch, options) => (nextState, nextOwnProps) => nextFinalProps
  
  connect passes its args to connectAdvanced as options, which will in turn pass them to
  selectorFactory each time a Connect component instance is instantiated or hot reloaded.

  selectorFactory returns a final props selector from its mapStateToProps,
  mapStateToPropsFactories, mapDispatchToProps, mapDispatchToPropsFactories, mergeProps,
  mergePropsFactories, and pure args.

  The resulting final props selector is called by the Connect component instance whenever
  it receives new props or store state.
 */

function match(arg, factories, name) {
  for (var i = factories.length - 1; i >= 0; i--) {
    var result = factories[i](arg);
    if (result) return result;
  }

  return function (dispatch, options) {
    throw new Error('Invalid value of type ' + typeof arg + ' for ' + name + ' argument when connecting component ' + options.wrappedComponentName + '.');
  };
}

function strictEqual(a, b) {
  return a === b;
}

// createConnect with default args builds the 'official' connect behavior. Calling it with
// different options opens up some testing and extensibility scenarios
function createConnect() {
  var _ref = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {},
      _ref$connectHOC = _ref.connectHOC,
      connectHOC = _ref$connectHOC === undefined ? __WEBPACK_IMPORTED_MODULE_0__components_connectAdvanced__["a" /* default */] : _ref$connectHOC,
      _ref$mapStateToPropsF = _ref.mapStateToPropsFactories,
      mapStateToPropsFactories = _ref$mapStateToPropsF === undefined ? __WEBPACK_IMPORTED_MODULE_3__mapStateToProps__["a" /* default */] : _ref$mapStateToPropsF,
      _ref$mapDispatchToPro = _ref.mapDispatchToPropsFactories,
      mapDispatchToPropsFactories = _ref$mapDispatchToPro === undefined ? __WEBPACK_IMPORTED_MODULE_2__mapDispatchToProps__["a" /* default */] : _ref$mapDispatchToPro,
      _ref$mergePropsFactor = _ref.mergePropsFactories,
      mergePropsFactories = _ref$mergePropsFactor === undefined ? __WEBPACK_IMPORTED_MODULE_4__mergeProps__["a" /* default */] : _ref$mergePropsFactor,
      _ref$selectorFactory = _ref.selectorFactory,
      selectorFactory = _ref$selectorFactory === undefined ? __WEBPACK_IMPORTED_MODULE_5__selectorFactory__["a" /* default */] : _ref$selectorFactory;

  return function connect(mapStateToProps, mapDispatchToProps, mergeProps) {
    var _ref2 = arguments.length > 3 && arguments[3] !== undefined ? arguments[3] : {},
        _ref2$pure = _ref2.pure,
        pure = _ref2$pure === undefined ? true : _ref2$pure,
        _ref2$areStatesEqual = _ref2.areStatesEqual,
        areStatesEqual = _ref2$areStatesEqual === undefined ? strictEqual : _ref2$areStatesEqual,
        _ref2$areOwnPropsEqua = _ref2.areOwnPropsEqual,
        areOwnPropsEqual = _ref2$areOwnPropsEqua === undefined ? __WEBPACK_IMPORTED_MODULE_1__utils_shallowEqual__["a" /* default */] : _ref2$areOwnPropsEqua,
        _ref2$areStatePropsEq = _ref2.areStatePropsEqual,
        areStatePropsEqual = _ref2$areStatePropsEq === undefined ? __WEBPACK_IMPORTED_MODULE_1__utils_shallowEqual__["a" /* default */] : _ref2$areStatePropsEq,
        _ref2$areMergedPropsE = _ref2.areMergedPropsEqual,
        areMergedPropsEqual = _ref2$areMergedPropsE === undefined ? __WEBPACK_IMPORTED_MODULE_1__utils_shallowEqual__["a" /* default */] : _ref2$areMergedPropsE,
        extraOptions = _objectWithoutProperties(_ref2, ['pure', 'areStatesEqual', 'areOwnPropsEqual', 'areStatePropsEqual', 'areMergedPropsEqual']);

    var initMapStateToProps = match(mapStateToProps, mapStateToPropsFactories, 'mapStateToProps');
    var initMapDispatchToProps = match(mapDispatchToProps, mapDispatchToPropsFactories, 'mapDispatchToProps');
    var initMergeProps = match(mergeProps, mergePropsFactories, 'mergeProps');

    return connectHOC(selectorFactory, _extends({
      // used in error messages
      methodName: 'connect',

      // used to compute Connect's displayName from the wrapped component's displayName.
      getDisplayName: function getDisplayName(name) {
        return 'Connect(' + name + ')';
      },

      // if mapStateToProps is falsy, the Connect component doesn't subscribe to store state changes
      shouldHandleStateChanges: Boolean(mapStateToProps),

      // passed through to selectorFactory
      initMapStateToProps: initMapStateToProps,
      initMapDispatchToProps: initMapDispatchToProps,
      initMergeProps: initMergeProps,
      pure: pure,
      areStatesEqual: areStatesEqual,
      areOwnPropsEqual: areOwnPropsEqual,
      areStatePropsEqual: areStatePropsEqual,
      areMergedPropsEqual: areMergedPropsEqual

    }, extraOptions));
  };
}

/* harmony default export */ __webpack_exports__["a"] = (createConnect());

/***/ }),
/* 175 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony export (immutable) */ __webpack_exports__["a"] = shallowEqual;
var hasOwn = Object.prototype.hasOwnProperty;

function is(x, y) {
  if (x === y) {
    return x !== 0 || y !== 0 || 1 / x === 1 / y;
  } else {
    return x !== x && y !== y;
  }
}

function shallowEqual(objA, objB) {
  if (is(objA, objB)) return true;

  if (typeof objA !== 'object' || objA === null || typeof objB !== 'object' || objB === null) {
    return false;
  }

  var keysA = Object.keys(objA);
  var keysB = Object.keys(objB);

  if (keysA.length !== keysB.length) return false;

  for (var i = 0; i < keysA.length; i++) {
    if (!hasOwn.call(objB, keysA[i]) || !is(objA[keysA[i]], objB[keysA[i]])) {
      return false;
    }
  }

  return true;
}

/***/ }),
/* 176 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* unused harmony export whenMapDispatchToPropsIsFunction */
/* unused harmony export whenMapDispatchToPropsIsMissing */
/* unused harmony export whenMapDispatchToPropsIsObject */
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_redux__ = __webpack_require__(10);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1__wrapMapToProps__ = __webpack_require__(73);



function whenMapDispatchToPropsIsFunction(mapDispatchToProps) {
  return typeof mapDispatchToProps === 'function' ? Object(__WEBPACK_IMPORTED_MODULE_1__wrapMapToProps__["b" /* wrapMapToPropsFunc */])(mapDispatchToProps, 'mapDispatchToProps') : undefined;
}

function whenMapDispatchToPropsIsMissing(mapDispatchToProps) {
  return !mapDispatchToProps ? Object(__WEBPACK_IMPORTED_MODULE_1__wrapMapToProps__["a" /* wrapMapToPropsConstant */])(function (dispatch) {
    return { dispatch: dispatch };
  }) : undefined;
}

function whenMapDispatchToPropsIsObject(mapDispatchToProps) {
  return mapDispatchToProps && typeof mapDispatchToProps === 'object' ? Object(__WEBPACK_IMPORTED_MODULE_1__wrapMapToProps__["a" /* wrapMapToPropsConstant */])(function (dispatch) {
    return Object(__WEBPACK_IMPORTED_MODULE_0_redux__["bindActionCreators"])(mapDispatchToProps, dispatch);
  }) : undefined;
}

/* harmony default export */ __webpack_exports__["a"] = ([whenMapDispatchToPropsIsFunction, whenMapDispatchToPropsIsMissing, whenMapDispatchToPropsIsObject]);

/***/ }),
/* 177 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0__Symbol_js__ = __webpack_require__(70);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1__getRawTag_js__ = __webpack_require__(180);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2__objectToString_js__ = __webpack_require__(181);




/** `Object#toString` result references. */
var nullTag = '[object Null]',
    undefinedTag = '[object Undefined]';

/** Built-in value references. */
var symToStringTag = __WEBPACK_IMPORTED_MODULE_0__Symbol_js__["a" /* default */] ? __WEBPACK_IMPORTED_MODULE_0__Symbol_js__["a" /* default */].toStringTag : undefined;

/**
 * The base implementation of `getTag` without fallbacks for buggy environments.
 *
 * @private
 * @param {*} value The value to query.
 * @returns {string} Returns the `toStringTag`.
 */
function baseGetTag(value) {
  if (value == null) {
    return value === undefined ? undefinedTag : nullTag;
  }
  return (symToStringTag && symToStringTag in Object(value))
    ? Object(__WEBPACK_IMPORTED_MODULE_1__getRawTag_js__["a" /* default */])(value)
    : Object(__WEBPACK_IMPORTED_MODULE_2__objectToString_js__["a" /* default */])(value);
}

/* harmony default export */ __webpack_exports__["a"] = (baseGetTag);


/***/ }),
/* 178 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0__freeGlobal_js__ = __webpack_require__(179);


/** Detect free variable `self`. */
var freeSelf = typeof self == 'object' && self && self.Object === Object && self;

/** Used as a reference to the global object. */
var root = __WEBPACK_IMPORTED_MODULE_0__freeGlobal_js__["a" /* default */] || freeSelf || Function('return this')();

/* harmony default export */ __webpack_exports__["a"] = (root);


/***/ }),
/* 179 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* WEBPACK VAR INJECTION */(function(global) {/** Detect free variable `global` from Node.js. */
var freeGlobal = typeof global == 'object' && global && global.Object === Object && global;

/* harmony default export */ __webpack_exports__["a"] = (freeGlobal);

/* WEBPACK VAR INJECTION */}.call(__webpack_exports__, __webpack_require__(18)))

/***/ }),
/* 180 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0__Symbol_js__ = __webpack_require__(70);


/** Used for built-in method references. */
var objectProto = Object.prototype;

/** Used to check objects for own properties. */
var hasOwnProperty = objectProto.hasOwnProperty;

/**
 * Used to resolve the
 * [`toStringTag`](http://ecma-international.org/ecma-262/7.0/#sec-object.prototype.tostring)
 * of values.
 */
var nativeObjectToString = objectProto.toString;

/** Built-in value references. */
var symToStringTag = __WEBPACK_IMPORTED_MODULE_0__Symbol_js__["a" /* default */] ? __WEBPACK_IMPORTED_MODULE_0__Symbol_js__["a" /* default */].toStringTag : undefined;

/**
 * A specialized version of `baseGetTag` which ignores `Symbol.toStringTag` values.
 *
 * @private
 * @param {*} value The value to query.
 * @returns {string} Returns the raw `toStringTag`.
 */
function getRawTag(value) {
  var isOwn = hasOwnProperty.call(value, symToStringTag),
      tag = value[symToStringTag];

  try {
    value[symToStringTag] = undefined;
    var unmasked = true;
  } catch (e) {}

  var result = nativeObjectToString.call(value);
  if (unmasked) {
    if (isOwn) {
      value[symToStringTag] = tag;
    } else {
      delete value[symToStringTag];
    }
  }
  return result;
}

/* harmony default export */ __webpack_exports__["a"] = (getRawTag);


/***/ }),
/* 181 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/** Used for built-in method references. */
var objectProto = Object.prototype;

/**
 * Used to resolve the
 * [`toStringTag`](http://ecma-international.org/ecma-262/7.0/#sec-object.prototype.tostring)
 * of values.
 */
var nativeObjectToString = objectProto.toString;

/**
 * Converts `value` to a string using `Object.prototype.toString`.
 *
 * @private
 * @param {*} value The value to convert.
 * @returns {string} Returns the converted string.
 */
function objectToString(value) {
  return nativeObjectToString.call(value);
}

/* harmony default export */ __webpack_exports__["a"] = (objectToString);


/***/ }),
/* 182 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0__overArg_js__ = __webpack_require__(183);


/** Built-in value references. */
var getPrototype = Object(__WEBPACK_IMPORTED_MODULE_0__overArg_js__["a" /* default */])(Object.getPrototypeOf, Object);

/* harmony default export */ __webpack_exports__["a"] = (getPrototype);


/***/ }),
/* 183 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/**
 * Creates a unary function that invokes `func` with its argument transformed.
 *
 * @private
 * @param {Function} func The function to wrap.
 * @param {Function} transform The argument transform.
 * @returns {Function} Returns the new function.
 */
function overArg(func, transform) {
  return function(arg) {
    return func(transform(arg));
  };
}

/* harmony default export */ __webpack_exports__["a"] = (overArg);


/***/ }),
/* 184 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/**
 * Checks if `value` is object-like. A value is object-like if it's not `null`
 * and has a `typeof` result of "object".
 *
 * @static
 * @memberOf _
 * @since 4.0.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is object-like, else `false`.
 * @example
 *
 * _.isObjectLike({});
 * // => true
 *
 * _.isObjectLike([1, 2, 3]);
 * // => true
 *
 * _.isObjectLike(_.noop);
 * // => false
 *
 * _.isObjectLike(null);
 * // => false
 */
function isObjectLike(value) {
  return value != null && typeof value == 'object';
}

/* harmony default export */ __webpack_exports__["a"] = (isObjectLike);


/***/ }),
/* 185 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* WEBPACK VAR INJECTION */(function(global, module) {/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0__ponyfill_js__ = __webpack_require__(187);
/* global window */


var root;

if (typeof self !== 'undefined') {
  root = self;
} else if (typeof window !== 'undefined') {
  root = window;
} else if (typeof global !== 'undefined') {
  root = global;
} else if (true) {
  root = module;
} else {
  root = Function('return this')();
}

var result = Object(__WEBPACK_IMPORTED_MODULE_0__ponyfill_js__["a" /* default */])(root);
/* harmony default export */ __webpack_exports__["a"] = (result);

/* WEBPACK VAR INJECTION */}.call(__webpack_exports__, __webpack_require__(18), __webpack_require__(186)(module)))

/***/ }),
/* 186 */
/***/ (function(module, exports) {

module.exports = function(originalModule) {
	if(!originalModule.webpackPolyfill) {
		var module = Object.create(originalModule);
		// module.parent = undefined by default
		if(!module.children) module.children = [];
		Object.defineProperty(module, "loaded", {
			enumerable: true,
			get: function() {
				return module.l;
			}
		});
		Object.defineProperty(module, "id", {
			enumerable: true,
			get: function() {
				return module.i;
			}
		});
		Object.defineProperty(module, "exports", {
			enumerable: true,
		});
		module.webpackPolyfill = 1;
	}
	return module;
};


/***/ }),
/* 187 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony export (immutable) */ __webpack_exports__["a"] = symbolObservablePonyfill;
function symbolObservablePonyfill(root) {
	var result;
	var Symbol = root.Symbol;

	if (typeof Symbol === 'function') {
		if (Symbol.observable) {
			result = Symbol.observable;
		} else {
			result = Symbol('observable');
			Symbol.observable = result;
		}
	} else {
		result = '@@observable';
	}

	return result;
};


/***/ }),
/* 188 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony export (immutable) */ __webpack_exports__["a"] = combineReducers;
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0__createStore__ = __webpack_require__(69);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_lodash_es_isPlainObject__ = __webpack_require__(40);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_2__utils_warning__ = __webpack_require__(71);




function getUndefinedStateErrorMessage(key, action) {
  var actionType = action && action.type;
  var actionName = actionType && '"' + actionType.toString() + '"' || 'an action';

  return 'Given action ' + actionName + ', reducer "' + key + '" returned undefined. ' + 'To ignore an action, you must explicitly return the previous state. ' + 'If you want this reducer to hold no value, you can return null instead of undefined.';
}

function getUnexpectedStateShapeWarningMessage(inputState, reducers, action, unexpectedKeyCache) {
  var reducerKeys = Object.keys(reducers);
  var argumentName = action && action.type === __WEBPACK_IMPORTED_MODULE_0__createStore__["a" /* ActionTypes */].INIT ? 'preloadedState argument passed to createStore' : 'previous state received by the reducer';

  if (reducerKeys.length === 0) {
    return 'Store does not have a valid reducer. Make sure the argument passed ' + 'to combineReducers is an object whose values are reducers.';
  }

  if (!Object(__WEBPACK_IMPORTED_MODULE_1_lodash_es_isPlainObject__["a" /* default */])(inputState)) {
    return 'The ' + argumentName + ' has unexpected type of "' + {}.toString.call(inputState).match(/\s([a-z|A-Z]+)/)[1] + '". Expected argument to be an object with the following ' + ('keys: "' + reducerKeys.join('", "') + '"');
  }

  var unexpectedKeys = Object.keys(inputState).filter(function (key) {
    return !reducers.hasOwnProperty(key) && !unexpectedKeyCache[key];
  });

  unexpectedKeys.forEach(function (key) {
    unexpectedKeyCache[key] = true;
  });

  if (unexpectedKeys.length > 0) {
    return 'Unexpected ' + (unexpectedKeys.length > 1 ? 'keys' : 'key') + ' ' + ('"' + unexpectedKeys.join('", "') + '" found in ' + argumentName + '. ') + 'Expected to find one of the known reducer keys instead: ' + ('"' + reducerKeys.join('", "') + '". Unexpected keys will be ignored.');
  }
}

function assertReducerShape(reducers) {
  Object.keys(reducers).forEach(function (key) {
    var reducer = reducers[key];
    var initialState = reducer(undefined, { type: __WEBPACK_IMPORTED_MODULE_0__createStore__["a" /* ActionTypes */].INIT });

    if (typeof initialState === 'undefined') {
      throw new Error('Reducer "' + key + '" returned undefined during initialization. ' + 'If the state passed to the reducer is undefined, you must ' + 'explicitly return the initial state. The initial state may ' + 'not be undefined. If you don\'t want to set a value for this reducer, ' + 'you can use null instead of undefined.');
    }

    var type = '@@redux/PROBE_UNKNOWN_ACTION_' + Math.random().toString(36).substring(7).split('').join('.');
    if (typeof reducer(undefined, { type: type }) === 'undefined') {
      throw new Error('Reducer "' + key + '" returned undefined when probed with a random type. ' + ('Don\'t try to handle ' + __WEBPACK_IMPORTED_MODULE_0__createStore__["a" /* ActionTypes */].INIT + ' or other actions in "redux/*" ') + 'namespace. They are considered private. Instead, you must return the ' + 'current state for any unknown actions, unless it is undefined, ' + 'in which case you must return the initial state, regardless of the ' + 'action type. The initial state may not be undefined, but can be null.');
    }
  });
}

/**
 * Turns an object whose values are different reducer functions, into a single
 * reducer function. It will call every child reducer, and gather their results
 * into a single state object, whose keys correspond to the keys of the passed
 * reducer functions.
 *
 * @param {Object} reducers An object whose values correspond to different
 * reducer functions that need to be combined into one. One handy way to obtain
 * it is to use ES6 `import * as reducers` syntax. The reducers may never return
 * undefined for any action. Instead, they should return their initial state
 * if the state passed to them was undefined, and the current state for any
 * unrecognized action.
 *
 * @returns {Function} A reducer function that invokes every reducer inside the
 * passed object, and builds a state object with the same shape.
 */
function combineReducers(reducers) {
  var reducerKeys = Object.keys(reducers);
  var finalReducers = {};
  for (var i = 0; i < reducerKeys.length; i++) {
    var key = reducerKeys[i];

    if (true) {
      if (typeof reducers[key] === 'undefined') {
        Object(__WEBPACK_IMPORTED_MODULE_2__utils_warning__["a" /* default */])('No reducer provided for key "' + key + '"');
      }
    }

    if (typeof reducers[key] === 'function') {
      finalReducers[key] = reducers[key];
    }
  }
  var finalReducerKeys = Object.keys(finalReducers);

  var unexpectedKeyCache = void 0;
  if (true) {
    unexpectedKeyCache = {};
  }

  var shapeAssertionError = void 0;
  try {
    assertReducerShape(finalReducers);
  } catch (e) {
    shapeAssertionError = e;
  }

  return function combination() {
    var state = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {};
    var action = arguments[1];

    if (shapeAssertionError) {
      throw shapeAssertionError;
    }

    if (true) {
      var warningMessage = getUnexpectedStateShapeWarningMessage(state, finalReducers, action, unexpectedKeyCache);
      if (warningMessage) {
        Object(__WEBPACK_IMPORTED_MODULE_2__utils_warning__["a" /* default */])(warningMessage);
      }
    }

    var hasChanged = false;
    var nextState = {};
    for (var _i = 0; _i < finalReducerKeys.length; _i++) {
      var _key = finalReducerKeys[_i];
      var reducer = finalReducers[_key];
      var previousStateForKey = state[_key];
      var nextStateForKey = reducer(previousStateForKey, action);
      if (typeof nextStateForKey === 'undefined') {
        var errorMessage = getUndefinedStateErrorMessage(_key, action);
        throw new Error(errorMessage);
      }
      nextState[_key] = nextStateForKey;
      hasChanged = hasChanged || nextStateForKey !== previousStateForKey;
    }
    return hasChanged ? nextState : state;
  };
}

/***/ }),
/* 189 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony export (immutable) */ __webpack_exports__["a"] = bindActionCreators;
function bindActionCreator(actionCreator, dispatch) {
  return function () {
    return dispatch(actionCreator.apply(undefined, arguments));
  };
}

/**
 * Turns an object whose values are action creators, into an object with the
 * same keys, but with every function wrapped into a `dispatch` call so they
 * may be invoked directly. This is just a convenience method, as you can call
 * `store.dispatch(MyActionCreators.doSomething())` yourself just fine.
 *
 * For convenience, you can also pass a single function as the first argument,
 * and get a function in return.
 *
 * @param {Function|Object} actionCreators An object whose values are action
 * creator functions. One handy way to obtain it is to use ES6 `import * as`
 * syntax. You may also pass a single function.
 *
 * @param {Function} dispatch The `dispatch` function available on your Redux
 * store.
 *
 * @returns {Function|Object} The object mimicking the original object, but with
 * every action creator wrapped into the `dispatch` call. If you passed a
 * function as `actionCreators`, the return value will also be a single
 * function.
 */
function bindActionCreators(actionCreators, dispatch) {
  if (typeof actionCreators === 'function') {
    return bindActionCreator(actionCreators, dispatch);
  }

  if (typeof actionCreators !== 'object' || actionCreators === null) {
    throw new Error('bindActionCreators expected an object or a function, instead received ' + (actionCreators === null ? 'null' : typeof actionCreators) + '. ' + 'Did you write "import ActionCreators from" instead of "import * as ActionCreators from"?');
  }

  var keys = Object.keys(actionCreators);
  var boundActionCreators = {};
  for (var i = 0; i < keys.length; i++) {
    var key = keys[i];
    var actionCreator = actionCreators[key];
    if (typeof actionCreator === 'function') {
      boundActionCreators[key] = bindActionCreator(actionCreator, dispatch);
    }
  }
  return boundActionCreators;
}

/***/ }),
/* 190 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony export (immutable) */ __webpack_exports__["a"] = applyMiddleware;
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0__compose__ = __webpack_require__(72);
var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };



/**
 * Creates a store enhancer that applies middleware to the dispatch method
 * of the Redux store. This is handy for a variety of tasks, such as expressing
 * asynchronous actions in a concise manner, or logging every action payload.
 *
 * See `redux-thunk` package as an example of the Redux middleware.
 *
 * Because middleware is potentially asynchronous, this should be the first
 * store enhancer in the composition chain.
 *
 * Note that each middleware will be given the `dispatch` and `getState` functions
 * as named arguments.
 *
 * @param {...Function} middlewares The middleware chain to be applied.
 * @returns {Function} A store enhancer applying the middleware.
 */
function applyMiddleware() {
  for (var _len = arguments.length, middlewares = Array(_len), _key = 0; _key < _len; _key++) {
    middlewares[_key] = arguments[_key];
  }

  return function (createStore) {
    return function (reducer, preloadedState, enhancer) {
      var store = createStore(reducer, preloadedState, enhancer);
      var _dispatch = store.dispatch;
      var chain = [];

      var middlewareAPI = {
        getState: store.getState,
        dispatch: function dispatch(action) {
          return _dispatch(action);
        }
      };
      chain = middlewares.map(function (middleware) {
        return middleware(middlewareAPI);
      });
      _dispatch = __WEBPACK_IMPORTED_MODULE_0__compose__["a" /* default */].apply(undefined, chain)(store.dispatch);

      return _extends({}, store, {
        dispatch: _dispatch
      });
    };
  };
}

/***/ }),
/* 191 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* unused harmony export whenMapStateToPropsIsFunction */
/* unused harmony export whenMapStateToPropsIsMissing */
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0__wrapMapToProps__ = __webpack_require__(73);


function whenMapStateToPropsIsFunction(mapStateToProps) {
  return typeof mapStateToProps === 'function' ? Object(__WEBPACK_IMPORTED_MODULE_0__wrapMapToProps__["b" /* wrapMapToPropsFunc */])(mapStateToProps, 'mapStateToProps') : undefined;
}

function whenMapStateToPropsIsMissing(mapStateToProps) {
  return !mapStateToProps ? Object(__WEBPACK_IMPORTED_MODULE_0__wrapMapToProps__["a" /* wrapMapToPropsConstant */])(function () {
    return {};
  }) : undefined;
}

/* harmony default export */ __webpack_exports__["a"] = ([whenMapStateToPropsIsFunction, whenMapStateToPropsIsMissing]);

/***/ }),
/* 192 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* unused harmony export defaultMergeProps */
/* unused harmony export wrapMergePropsFunc */
/* unused harmony export whenMergePropsIsFunction */
/* unused harmony export whenMergePropsIsOmitted */
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0__utils_verifyPlainObject__ = __webpack_require__(74);
var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };



function defaultMergeProps(stateProps, dispatchProps, ownProps) {
  return _extends({}, ownProps, stateProps, dispatchProps);
}

function wrapMergePropsFunc(mergeProps) {
  return function initMergePropsProxy(dispatch, _ref) {
    var displayName = _ref.displayName,
        pure = _ref.pure,
        areMergedPropsEqual = _ref.areMergedPropsEqual;

    var hasRunOnce = false;
    var mergedProps = void 0;

    return function mergePropsProxy(stateProps, dispatchProps, ownProps) {
      var nextMergedProps = mergeProps(stateProps, dispatchProps, ownProps);

      if (hasRunOnce) {
        if (!pure || !areMergedPropsEqual(nextMergedProps, mergedProps)) mergedProps = nextMergedProps;
      } else {
        hasRunOnce = true;
        mergedProps = nextMergedProps;

        if (true) Object(__WEBPACK_IMPORTED_MODULE_0__utils_verifyPlainObject__["a" /* default */])(mergedProps, displayName, 'mergeProps');
      }

      return mergedProps;
    };
  };
}

function whenMergePropsIsFunction(mergeProps) {
  return typeof mergeProps === 'function' ? wrapMergePropsFunc(mergeProps) : undefined;
}

function whenMergePropsIsOmitted(mergeProps) {
  return !mergeProps ? function () {
    return defaultMergeProps;
  } : undefined;
}

/* harmony default export */ __webpack_exports__["a"] = ([whenMergePropsIsFunction, whenMergePropsIsOmitted]);

/***/ }),
/* 193 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* unused harmony export impureFinalPropsSelectorFactory */
/* unused harmony export pureFinalPropsSelectorFactory */
/* harmony export (immutable) */ __webpack_exports__["a"] = finalPropsSelectorFactory;
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0__verifySubselectors__ = __webpack_require__(194);
function _objectWithoutProperties(obj, keys) { var target = {}; for (var i in obj) { if (keys.indexOf(i) >= 0) continue; if (!Object.prototype.hasOwnProperty.call(obj, i)) continue; target[i] = obj[i]; } return target; }



function impureFinalPropsSelectorFactory(mapStateToProps, mapDispatchToProps, mergeProps, dispatch) {
  return function impureFinalPropsSelector(state, ownProps) {
    return mergeProps(mapStateToProps(state, ownProps), mapDispatchToProps(dispatch, ownProps), ownProps);
  };
}

function pureFinalPropsSelectorFactory(mapStateToProps, mapDispatchToProps, mergeProps, dispatch, _ref) {
  var areStatesEqual = _ref.areStatesEqual,
      areOwnPropsEqual = _ref.areOwnPropsEqual,
      areStatePropsEqual = _ref.areStatePropsEqual;

  var hasRunAtLeastOnce = false;
  var state = void 0;
  var ownProps = void 0;
  var stateProps = void 0;
  var dispatchProps = void 0;
  var mergedProps = void 0;

  function handleFirstCall(firstState, firstOwnProps) {
    state = firstState;
    ownProps = firstOwnProps;
    stateProps = mapStateToProps(state, ownProps);
    dispatchProps = mapDispatchToProps(dispatch, ownProps);
    mergedProps = mergeProps(stateProps, dispatchProps, ownProps);
    hasRunAtLeastOnce = true;
    return mergedProps;
  }

  function handleNewPropsAndNewState() {
    stateProps = mapStateToProps(state, ownProps);

    if (mapDispatchToProps.dependsOnOwnProps) dispatchProps = mapDispatchToProps(dispatch, ownProps);

    mergedProps = mergeProps(stateProps, dispatchProps, ownProps);
    return mergedProps;
  }

  function handleNewProps() {
    if (mapStateToProps.dependsOnOwnProps) stateProps = mapStateToProps(state, ownProps);

    if (mapDispatchToProps.dependsOnOwnProps) dispatchProps = mapDispatchToProps(dispatch, ownProps);

    mergedProps = mergeProps(stateProps, dispatchProps, ownProps);
    return mergedProps;
  }

  function handleNewState() {
    var nextStateProps = mapStateToProps(state, ownProps);
    var statePropsChanged = !areStatePropsEqual(nextStateProps, stateProps);
    stateProps = nextStateProps;

    if (statePropsChanged) mergedProps = mergeProps(stateProps, dispatchProps, ownProps);

    return mergedProps;
  }

  function handleSubsequentCalls(nextState, nextOwnProps) {
    var propsChanged = !areOwnPropsEqual(nextOwnProps, ownProps);
    var stateChanged = !areStatesEqual(nextState, state);
    state = nextState;
    ownProps = nextOwnProps;

    if (propsChanged && stateChanged) return handleNewPropsAndNewState();
    if (propsChanged) return handleNewProps();
    if (stateChanged) return handleNewState();
    return mergedProps;
  }

  return function pureFinalPropsSelector(nextState, nextOwnProps) {
    return hasRunAtLeastOnce ? handleSubsequentCalls(nextState, nextOwnProps) : handleFirstCall(nextState, nextOwnProps);
  };
}

// TODO: Add more comments

// If pure is true, the selector returned by selectorFactory will memoize its results,
// allowing connectAdvanced's shouldComponentUpdate to return false if final
// props have not changed. If false, the selector will always return a new
// object and shouldComponentUpdate will always return true.

function finalPropsSelectorFactory(dispatch, _ref2) {
  var initMapStateToProps = _ref2.initMapStateToProps,
      initMapDispatchToProps = _ref2.initMapDispatchToProps,
      initMergeProps = _ref2.initMergeProps,
      options = _objectWithoutProperties(_ref2, ['initMapStateToProps', 'initMapDispatchToProps', 'initMergeProps']);

  var mapStateToProps = initMapStateToProps(dispatch, options);
  var mapDispatchToProps = initMapDispatchToProps(dispatch, options);
  var mergeProps = initMergeProps(dispatch, options);

  if (true) {
    Object(__WEBPACK_IMPORTED_MODULE_0__verifySubselectors__["a" /* default */])(mapStateToProps, mapDispatchToProps, mergeProps, options.displayName);
  }

  var selectorFactory = options.pure ? pureFinalPropsSelectorFactory : impureFinalPropsSelectorFactory;

  return selectorFactory(mapStateToProps, mapDispatchToProps, mergeProps, dispatch, options);
}

/***/ }),
/* 194 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
/* harmony export (immutable) */ __webpack_exports__["a"] = verifySubselectors;
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0__utils_warning__ = __webpack_require__(39);


function verify(selector, methodName, displayName) {
  if (!selector) {
    throw new Error('Unexpected value for ' + methodName + ' in ' + displayName + '.');
  } else if (methodName === 'mapStateToProps' || methodName === 'mapDispatchToProps') {
    if (!selector.hasOwnProperty('dependsOnOwnProps')) {
      Object(__WEBPACK_IMPORTED_MODULE_0__utils_warning__["a" /* default */])('The selector for ' + methodName + ' of ' + displayName + ' did not specify a value for dependsOnOwnProps.');
    }
  }
}

function verifySubselectors(mapStateToProps, mapDispatchToProps, mergeProps, displayName) {
  verify(mapStateToProps, 'mapStateToProps', displayName);
  verify(mapDispatchToProps, 'mapDispatchToProps', displayName);
  verify(mergeProps, 'mergeProps', displayName);
}

/***/ }),
/* 195 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
var selectBikePopup = exports.selectBikePopup = {
  OPEN_BIKE_POPUP: 'OPEN_BIKE_POPUP',
  CLOSE_BIKE_POPUP: 'CLOSE_BIKE_POPUP',
  SELECT_MODEL: 'SELECT_MODEL',
  SET_BIKE_VERSION: 'SET_BIKE_VERSION',
  FETCH_BIKELIST_SUCCESS: 'FETCH_BIKELIST_SUCCESS',
  FETCH_BIKELIST_FAILURE: 'FETCH_BIKELIST_FAILURE',
  RESET_BIKELIST_FAILURE: 'RESET_BIKELIST_FAILURE',
  FETCHING_VERSIONLIST: 'FETCHING_VERSIONLIST',
  FETCH_VERSIONLIST_SUCCESS: 'FETCH_VERSIONLIST_SUCCESS',
  FETCH_VERSIONLIST_FAILURE: 'FETCH_VERSIONLIST_FAILURE',
  RESET_VERSIONLIST_FAILURE: 'RESET_VERSIONLIST_FAILURE',
  FETCH_MODEL_DETAIL: 'FETCH_MODEL_DETAIL',
  FETCH_MODEL_DETAIL_SUCCESS: 'FETCH_MODEL_DETAIL_SUCCESS',
  FETCH_MODEL_DETAIL_FAILURE: 'FETCH_MODEL_DETAIL_FAILURE',
  RESET_MODEL_DETAIL_FAILURE: 'RESET_MODEL_DETAIL_FAILURE',
  RESET_VERSION_SELECTION: 'RESET_VERSION_SELECTION'
};

/***/ }),
/* 196 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
var similarBikesEMI = exports.similarBikesEMI = {
  FETCH_SIMILAR_BIKES_EMI: 'FETCH_SIMILAR_BIKES_EMI',
  UPDATE_EMI: 'UPDATE_EMI',
  FETCH_SIMILAR_BIKES_FAILED: 'FETCH_SIMILAR_BIKES_FAILED',
  FETCHING_SIMILAR_BIKES: 'FETCHING_SIMILAR_BIKES'
};

/***/ }),
/* 197 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
	value: true
});
exports.clearToast = exports.initToast = exports.removeToast = exports.setToast = undefined;

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var _toast = __webpack_require__(198);

var _immutable = __webpack_require__(17);

var setToast = exports.setToast = function setToast(data) {
	return {
		type: _toast.toast.SET_TOAST,
		message: data.message,
		toastTimerId: data.toastTimerId,
		toastStyle: data.toastStyle
	};
};

var removeToast = exports.removeToast = function removeToast() {
	return {
		type: _toast.toast.REMOVE_TOAST
	};
};

var initToast = exports.initToast = function initToast(data) {
	return function (dispatch, getState) {
		var toast = getState().get('Toast').toJS()['Toast'];

		var toastStyle = void 0;

		// show toast message relative to click position
		if (data.event) {
			var event = data.event;

			var offset = {
				top: window.pageYOffset || document.documentElement.scrollTop
			};

			var clickPositionY = event.pageY - offset.top + 20;
			var availableHeight = window.innerHeight - clickPositionY;

			if (availableHeight < 100) {
				toastStyle = _extends({}, toastStyle, {
					top: 'auto',
					bottom: availableHeight + 50 // 50px offset height i.e average height of toast message
				});
			} else {
				toastStyle = _extends({}, toastStyle, {
					top: clickPositionY + 'px',
					bottom: 'auto'
				});
			}
		}

		if (data.style) {
			toastStyle = _extends({}, toastStyle, data.style);
		}

		// set toast message duration
		var toastDuration = toast && toast.duration ? toast.duration : 3000;

		if (data.duration) {
			toastDuration = data.duration;
		}
		if (toast != undefined) clearTimeout(toast.toastTimerId); // clear previous toast
		else {}

		var toastTimerId = setTimeout(function () {
			dispatch(removeToast());
		}, toastDuration);

		dispatch(setToast(_extends({}, data, {
			toastTimerId: toastTimerId,
			toastStyle: toastStyle
		})));
	};
};

var clearToast = exports.clearToast = function clearToast() {
	return function (dispatch, getState) {
		var toast = getState().get('Toast').toJS()['Toast'];
		if (toast != undefined) {
			clearTimeout(toast.toastTimerId);
			dispatch(removeToast());
		}
	};
};

/***/ }),
/* 198 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
    value: true
});
var toast = exports.toast = {
    SET_TOAST: 'SET_TOAST',
    REMOVE_TOAST: 'REMOVE_TOAST'
};

/***/ }),
/* 199 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

var _SelectBikePopup = __webpack_require__(200);

var _SelectBikePopup2 = _interopRequireDefault(_SelectBikePopup);

var _EMISteps = __webpack_require__(202);

var _EMISteps2 = _interopRequireDefault(_EMISteps);

var _EMICalculator = __webpack_require__(208);

var _EMICalculator2 = _interopRequireDefault(_EMICalculator);

var _SelectCityPopup = __webpack_require__(221);

var _SelectCityPopup2 = _interopRequireDefault(_SelectCityPopup);

var _ModelInfoContainer = __webpack_require__(227);

var _ModelInfoContainer2 = _interopRequireDefault(_ModelInfoContainer);

var _SwiperContainer = __webpack_require__(231);

var _SwiperContainer2 = _interopRequireDefault(_SwiperContainer);

var _SwiperSimilarBikesEMI = __webpack_require__(232);

var _SwiperSimilarBikesEMI2 = _interopRequireDefault(_SwiperSimilarBikesEMI);

var _popUpUtils = __webpack_require__(11);

var _analyticsUtils = __webpack_require__(5);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var EMITab = function (_React$Component) {
  _inherits(EMITab, _React$Component);

  function EMITab(props) {
    _classCallCheck(this, EMITab);

    var _this = _possibleConstructorReturn(this, (EMITab.__proto__ || Object.getPrototypeOf(EMITab)).call(this, props));

    _initialiseProps.call(_this);

    _this.getSelectedBikeId = _this.getSelectedBikeId.bind(_this);
    _this.getSelectedCityId = _this.getSelectedCityId.bind(_this);
    _this.handleCityClick = _this.handleCityClick.bind(_this);
    _this.handleBikeClick = _this.handleBikeClick.bind(_this);
    _this.handleSimilarEMISwiperCardClick = _this.handleSimilarEMISwiperCardClick.bind(_this);
    _this.handleSelectBikeClick = _this.handleSelectBikeClick.bind(_this);
    _this.handleSelectCityClick = _this.handleSelectCityClick.bind(_this);
    _this.checkNetworkFailure = _this.checkNetworkFailure.bind(_this);
    _this.setCity = _this.setCity.bind(_this);
    _this.setToast = _this.setToast.bind(_this);
    _this.state = {
      shouldscroll: false,
      isGlobalCityInList: true,
      shouldFetchSimilarBikes: true,
      shouldOpenEmiCalculator: true,
      isFetching: false,
      currentSelectedBikeId: -1

    };
    return _this;
  }

  _createClass(EMITab, [{
    key: 'checkNetworkFailure',
    value: function checkNetworkFailure() {
      var _props = this.props,
          selectBikePopup = _props.selectBikePopup,
          FinanceCityPopup = _props.FinanceCityPopup,
          resetMakeModelListFailure = _props.resetMakeModelListFailure,
          resetSelectedBikeDetailFailure = _props.resetSelectedBikeDetailFailure,
          resetBikeVersionListFailure = _props.resetBikeVersionListFailure,
          resetCityFailure = _props.resetCityFailure;

      var networkFailureError = false;
      if (selectBikePopup.MakeModelListFetchError) {
        resetMakeModelListFailure();
        networkFailureError = true;
      } else if (selectBikePopup.VersionListFetchError) {
        resetBikeVersionListFailure();
        networkFailureError = true;
      } else if (selectBikePopup.ModelDetailFetchError) {
        resetSelectedBikeDetailFailure();
        networkFailureError = true;
      } else if (FinanceCityPopup.CityFetchError) {
        resetCityFailure();
        networkFailureError = true;
      }
      if (networkFailureError) {
        this.setToast('Something went wrong. Please try again.');
      }
    }
  }, {
    key: 'componentWillReceiveProps',
    value: function componentWillReceiveProps(nextProps) {
      var _props2 = this.props,
          selectBikePopup = _props2.selectBikePopup,
          openEmiCalculator = _props2.openEmiCalculator,
          updateSimilarBikesEmi = _props2.updateSimilarBikesEmi;

      var currentSelectBikePopupVersion = selectBikePopup.Selection.version;
      var nextPropsSelectBikePopupVersion = nextProps.selectBikePopup.Selection.version;
      if (nextPropsSelectBikePopupVersion.versionList.length > 0 && nextPropsSelectBikePopupVersion.selectedVersionIndex > -1 && (currentSelectBikePopupVersion.modelId != nextPropsSelectBikePopupVersion.modelId || currentSelectBikePopupVersion.selectedVersionIndex != nextPropsSelectBikePopupVersion.selectedVersionIndex || currentSelectBikePopupVersion.cityId != nextPropsSelectBikePopupVersion.cityId)) {
        openEmiCalculator(nextPropsSelectBikePopupVersion.versionList[nextPropsSelectBikePopupVersion.selectedVersionIndex].price);
        this.state.isFetching = false;
      }

      if (nextProps.SimilarBikesEMI.data.length > 0 && nextProps.sliderDp.values[0] > 0 && (nextProps.sliderDp.values[0] !== nextProps.SimilarBikesEMI.downPayment || nextProps.sliderInt.values[0] !== nextProps.SimilarBikesEMI.rateOfInt || nextProps.sliderTenure.values[0] !== nextProps.SimilarBikesEMI.tenure || nextProps.SimilarBikesEMI.modelId != this.props.SimilarBikesEMI.modelId || nextProps.SimilarBikesEMI.cityId != this.props.SimilarBikesEMI.cityId)) {
        updateSimilarBikesEmi(nextProps.SimilarBikesEMI, {
          downPayment: nextProps.sliderDp.values[0],
          tenure: nextProps.sliderTenure.values[0],
          rateOfInt: nextProps.sliderInt.values[0]
        });
      }
      this.checkNetworkFailure();
    }
  }, {
    key: 'componentDidUpdate',
    value: function componentDidUpdate(prevProps) {
      var _props3 = this.props,
          FinanceCityPopup = _props3.FinanceCityPopup,
          selectBikePopup = _props3.selectBikePopup;

      var currentCityId = this.getSelectedCityId(this.props);
      var currentGlobalCityId = FinanceCityPopup.currentGlobalCityId;
      if (this.state.shouldscroll) {
        if (!this.props.FinanceCityPopup.isActive) {
          if (this.refs.emiSteps != undefined) {
            this.refs.emiSteps.scrollCityToView();
          }
        }
        // Open city popup if current city not in fetched city list
        if (selectBikePopup != undefined && FinanceCityPopup.RelatedModelId == this.state.currentSelectedBikeId && this.state.currentSelectedBikeId > 0) {
          if (FinanceCityPopup.CityFetchError) {
            this.setState(_extends({}, this.state, { shouldscroll: false }));
            this.refs.selectBikePopup.closePopup(); // Close bike popup when fetching error occurs
          } else if (FinanceCityPopup.Popular.length > 0 || FinanceCityPopup.Other.length > 0) {
            // Check if current global city in new city list
            if (currentGlobalCityId > 0 && ((0, _popUpUtils.IsGlobalCityPresent)(FinanceCityPopup.Popular, currentGlobalCityId) || (0, _popUpUtils.IsGlobalCityPresent)(FinanceCityPopup.Other, currentGlobalCityId))) {
              this.setState(_extends({}, this.state, { shouldscroll: false, isGlobalCityInList: true }));
              if (currentCityId != currentGlobalCityId) this.setCity({
                cityId: currentGlobalCityId,
                cityName: FinanceCityPopup.currentGlobalCityName,
                userChange: false
              });
              this.refs.selectBikePopup.closePopup(); // close bike popup on when fetched cities have current city
            } else {
              this.refs.selectBikePopup.closePopup(); // close bike popup on when fetched cities don't have current city
              this.handleSelectCityClick(); // open select city popup
              if (currentCityId != -1) this.setCity({
                cityId: -1,
                cityName: "",
                userChange: false
              });
              this.props.resetVersionSelection();
              if (FinanceCityPopup.currentGlobalCityName != undefined && FinanceCityPopup.currentGlobalCityName != "") {
                this.setToast("Price of this bike is not available in " + FinanceCityPopup.currentGlobalCityName + ". Please choose another city.");
              }
              this.setState(_extends({}, this.state, { shouldscroll: false, isGlobalCityInList: false, isFetching: false }));
            }
          } else {
            this.setToast("Price of this bike is not available. Unfortunately, we won't be able to show EMI for this bike.");
            this.setState(_extends({}, this.state, { shouldscroll: false }));
          }
        }
      }
    }
  }, {
    key: 'componentWillUnmount',
    value: function componentWillUnmount() {
      this.props.clearToast();
    }
  }, {
    key: 'render',
    value: function render() {
      var _props4 = this.props,
          selectBikePopup = _props4.selectBikePopup,
          FinanceCityPopup = _props4.FinanceCityPopup,
          SimilarBikesEMI = _props4.SimilarBikesEMI,
          closeSelectBikePopup = _props4.closeSelectBikePopup,
          fetchMakeModelList = _props4.fetchMakeModelList,
          closeSelectCityPopup = _props4.closeSelectCityPopup;

      var isBikeAndCitySelected = this.getSelectedBikeId(this.props) !== -1 && this.getSelectedCityId(this.props) !== -1;
      return _react2.default.createElement(
        'div',
        null,
        _react2.default.createElement(
          'div',
          { className: 'emi-calculator__head' },
          _react2.default.createElement(
            'h2',
            { className: 'emi-calculator__title' },
            'EMI Calculator'
          ),
          _react2.default.createElement(
            'p',
            { className: 'emi-calculator__head-description' },
            'Know the tentative EMI for bike of your choice in 2 simple steps.'
          )
        ),
        _react2.default.createElement(_EMISteps2.default, { ref: 'emiSteps', onSelectBikeClick: this.handleSelectBikeClick, onSelectCityClick: this.handleSelectCityClick, model: selectBikePopup.Selection, onSelectBikeClose: closeSelectBikePopup, onSelectCityClose: closeSelectCityPopup, isShown: !isBikeAndCitySelected }),
        isBikeAndCitySelected == true && _react2.default.createElement(
          'div',
          null,
          _react2.default.createElement(_ModelInfoContainer2.default, { ref: 'modelInfoComponent' }),
          _react2.default.createElement(_EMICalculator2.default, { IsFetching: this.state.isFetching })
        ),
        SimilarBikesEMI != null && SimilarBikesEMI.data != null && SimilarBikesEMI.data.length > 0 && _react2.default.createElement(_SwiperContainer2.default, {
          type: 'carousel__similar-emi',
          heading: 'Other Bikes in similar range',
          data: SimilarBikesEMI,
          carouselCard: _SwiperSimilarBikesEMI2.default,
          onCarouselCardClick: this.handleSimilarEMISwiperCardClick
        }),
        FinanceCityPopup != null && _react2.default.createElement(_SelectCityPopup2.default, { isActive: FinanceCityPopup.isActive, data: _extends({}, FinanceCityPopup, { isGlobalCityInList: this.state.isGlobalCityInList }), onCloseClick: closeSelectCityPopup, onCityClick: this.handleCityClick }),
        selectBikePopup != null && _react2.default.createElement(_SelectBikePopup2.default, { ref: 'selectBikePopup', isActive: selectBikePopup.isActive, data: selectBikePopup, onCloseClick: closeSelectBikePopup, onBikeClick: this.handleBikeClick, fetchMakeModelList: fetchMakeModelList })
      );
    }
  }]);

  return EMITab;
}(_react2.default.Component);

var _initialiseProps = function _initialiseProps() {
  var _this2 = this;

  this.handleSelectBikeClick = function () {
    (0, _popUpUtils.openPopupWithHash)(_this2.props.openSelectBikePopup, _this2.props.closeSelectBikePopup, "SelectBike");
    if (_this2.state.currentSelectedBikeId <= 0) {
      if (typeof gaObj != 'undefined') {
        (0, _analyticsUtils.triggerGA)(gaObj.name, 'Select_Bike_Clicked', '');
      }
    } else {
      if (typeof gaObj != 'undefined') {
        (0, _analyticsUtils.triggerGA)(gaObj.name, 'Model_Change_Initiated', 'Existing Model - ' + _this2.props.selectBikePopup.Selection.modelName);
      }
    }
  };

  this.handleBikeClick = function (item) {
    _this2.props.selectModel(item.modelId);
    _this2.props.fetchCity(item.modelId);
    _this2.setState(_extends({}, _this2.state, { shouldscroll: true, currentSelectedBikeId: item.modelId }));
    _this2.props.fetchSelectedBikeDetail(item.modelId);
    var currentCityId = _this2.props.FinanceCityPopup.currentGlobalCityId;
    if (currentCityId > 0) {
      _this2.props.fetchBikeVersionList(item.modelId, currentCityId);
      _this2.props.fetchSimilarBikes({
        modelId: item.modelId,
        cityId: currentCityId,
        downPayment: _this2.props.sliderDp.values[0],
        tenure: _this2.props.sliderTenure.values[0],
        rateOfInt: _this2.props.sliderInt.values[0]
      });
      _this2.state.isFetching = true;
    }
  };

  this.handleSelectCityClick = function () {
    (0, _popUpUtils.openPopupWithHash)(_this2.props.openSelectCityPopup, _this2.props.closeSelectCityPopup, "SelectCity");
  };

  this.setCity = function (item) {
    var payload = {
      cityId: item.cityId,
      cityName: item.cityName,
      userChange: true
    };
    _this2.props.selectCity(payload);
  };

  this.handleCityClick = function (item) {
    _this2.setCity(item);
    _this2.setState(_extends({}, _this2.state, { isGlobalCityInList: true, shouldFetchSimilarBikes: true }));
    var currentBikeId = _this2.getSelectedBikeId(_this2.props);
    _this2.props.fetchBikeVersionList(currentBikeId, item.cityId);
    _this2.props.fetchSimilarBikes({
      modelId: currentBikeId,
      cityId: item.cityId,
      downPayment: _this2.props.sliderDp.values[0],
      tenure: _this2.props.sliderTenure.values[0],
      rateOfInt: _this2.props.sliderInt.values[0]
    });
    _this2.state.isFetching = true;
  };

  this.handleSimilarEMISwiperCardClick = function (modelObj, event) {
    var quickLinksTabElement = document.getElementById("quickLinksTab");
    window.scrollTo(0, _this2.refs.modelInfoComponent.base.getBoundingClientRect().top + (window.pageYOffset || document.documentElement.scrollTop) - quickLinksTabElement.offsetHeight);
    if (typeof gaObj != 'undefined') {
      (0, _analyticsUtils.triggerGA)(gaObj.name, 'Similar_EMI_Widget_Clicked', _this2.props.selectBikePopup.Selection.modelName + '_' + modelObj.modelName);
    }
    _this2.props.fetchSelectedBikeDetail(modelObj.modelId);
    var currentCityId = _this2.getSelectedCityId(_this2.props);
    _this2.props.fetchBikeVersionList(modelObj.modelId, currentCityId);
    _this2.props.fetchSimilarBikes({
      modelId: modelObj.modelId,
      cityId: currentCityId,
      downPayment: _this2.props.sliderDp.values[0],
      tenure: _this2.props.sliderTenure.values[0],
      rateOfInt: _this2.props.sliderInt.values[0]
    });
    _this2.state.shouldFetchSimilarBikes = true;
    _this2.state.isFetching = true;
  };

  this.setToast = function (message) {
    _this2.props.initToast({
      message: message,
      event: event,
      duration: 5000,
      style: { bottom: '50px', top: 'auto' }
    });
  };

  this.getSelectedBikeId = function (props) {
    return props != null && props.selectBikePopup != null && props.selectBikePopup.Selection != null && props.selectBikePopup.Selection.modelId > 0 ? props.selectBikePopup.Selection.modelId : -1;
  };

  this.getSelectedCityId = function (props) {
    return props != null && props.FinanceCityPopup != null && props.FinanceCityPopup.Selection != null && props.FinanceCityPopup.Selection.cityId > 0 ? props.FinanceCityPopup.Selection.cityId : -1;
  };
};

exports.default = EMITab;

/***/ }),
/* 200 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

var _Accordion = __webpack_require__(79);

var _Accordion2 = _interopRequireDefault(_Accordion);

var _NoResult = __webpack_require__(80);

var _NoResult2 = _interopRequireDefault(_NoResult);

var _SpinnerRelative = __webpack_require__(8);

var _SpinnerRelative2 = _interopRequireDefault(_SpinnerRelative);

var _scrollLock = __webpack_require__(37);

var _popupScroll = __webpack_require__(81);

var _popUpUtils = __webpack_require__(11);

var _analyticsUtils = __webpack_require__(5);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var SelectBikePopup = function (_React$Component) {
  _inherits(SelectBikePopup, _React$Component);

  function SelectBikePopup(props) {
    _classCallCheck(this, SelectBikePopup);

    var _this = _possibleConstructorReturn(this, (SelectBikePopup.__proto__ || Object.getPrototypeOf(SelectBikePopup)).call(this, props));

    _this.closePopup = function () {
      if (_this.props.onCloseClick) {
        (0, _popUpUtils.closePopupWithHash)(_this.props.onCloseClick);
      }
    };

    _this.resetSearchMode = function () {
      _this.setState(_extends({}, _this.state, { searchMode: false }));
    };

    _this.handleCloseClick = function () {
      _this.popupScrollPosition = _this.popupContent.scrollTop;
      if (gaObj != undefined) {
        (0, _analyticsUtils.triggerGA)(gaObj.name, 'Model_Popup_Cross_Clicked', _this.state.modelValue);
      }
      _this.closePopup();
    };

    _this.handleBikeSelection = function (chosenModel) {
      _this.popupScrollPosition = _this.popupContent.scrollTop;
      if (gaObj != undefined) {
        (0, _analyticsUtils.triggerGA)(gaObj.name, 'Model_Selected', chosenModel.modelName + '_' + _this.state.modelValue);
      }
      if (_this.state.currentModelId > 0) {
        if (gaObj != undefined) {
          (0, _analyticsUtils.triggerGA)(gaObj.name, 'Model_Selected_On_Edit_Flow', 'Existing Model - ' + _this.props.data.Selection.modelName);
        }
      }
      if (_this.state.currentModelId != chosenModel.modelId) {
        _this.setState(_extends({}, _this.state, { currentModelId: chosenModel.modelId, modelValue: chosenModel.modelName, isProcessing: true }));
        _this.props.onBikeClick(chosenModel);
      }
    };

    _this.handleClear = function () {
      _this.setState(_extends({}, _this.state, { modelValue: '', makeModelList: _this.props.data.MakeModelList, searchMode: false }));
    };

    _this.filterMakeModelList = function (event) {
      var inputName = event.target.value;
      var modelList = event && !event.data ? _this.props.data.MakeModelList : _this.state.makeModelList;
      var searchMode = event && inputName != null && inputName.length > 0 ? true : false;
      var makeModelList = [];
      if (modelList != null) {
        makeModelList = modelList.map(function (item) {
          var modelList = item.models.filter(function (bike) {
            return bike.modelName.toLowerCase().indexOf(inputName.toLowerCase()) !== -1;
          });
          if (modelList && modelList.length > 0) {
            return _extends({}, item, { models: modelList
            });
          } else {
            return null;
          }
        }).filter(function (n) {
          return n;
        });
      }
      _this.setState(_extends({}, _this.state, {
        makeModelList: makeModelList,
        modelValue: inputName,
        searchMode: searchMode
      }), function () {
        (0, _popupScroll.focusPopupContent)(_this.popupContent);
      });
    };

    _this.handleAccordionClick = function (event) {
      (0, _popupScroll.focusCollapsible)(_this.popupContent, event);
    };

    _this.getList = function (makeModelList) {
      if (!makeModelList || !makeModelList.length) {
        return [];
      }
      var list = makeModelList.map(function (item) {
        if (!item) {
          return null;
        }
        return _react2.default.createElement(
          'div',
          { 'data-trigger': item.make.makeName, 'data-onOpen': this.handleAccordionClick.bind(this) },
          _react2.default.createElement(
            'ul',
            { className: 'panel-body__list' },
            item.models.map(function (bike) {
              var eleClassName = 'panel-bike-list__item ' + (bike.modelId == this.state.currentModelId ? " bike-list-item--active" : "");
              return _react2.default.createElement(
                'li',
                { key: bike.modelId, className: eleClassName, onClick: this.handleBikeSelection.bind(this, bike) },
                _react2.default.createElement(
                  'p',
                  { className: 'bike-list-item__label' },
                  bike.modelName
                )
              );
            }, this)
          )
        );
      }, _this);

      return list;
    };

    _this.setContentRef = function (ref) {
      _this.popupContent = ref;
    };

    _this.filterMakeModelList = _this.filterMakeModelList.bind(_this);
    _this.handleClear = _this.handleClear.bind(_this);
    _this.handleCloseClick = _this.handleCloseClick.bind(_this);
    _this.closePopup = _this.closePopup.bind(_this);
    _this.resetSearchMode = _this.resetSearchMode.bind(_this);
    _this.state = { currentModelId: _this.props.data.Selection.modelId, modelValue: _this.props.data.Selection.modelName, makeModelList: _this.props.data.MakeModelList, isProcessing: false };
    return _this;
  }

  _createClass(SelectBikePopup, [{
    key: 'componentWillReceiveProps',
    value: function componentWillReceiveProps(nextProps) {
      this.setState(_extends({}, this.state, { currentModelId: nextProps.data.Selection.modelId, makeModelList: nextProps.data.MakeModelList }));
    }
  }, {
    key: 'componentDidUpdate',
    value: function componentDidUpdate() {
      var isProcessing = this.state.isProcessing;
      if (this.props.isActive == false && this.state.isProcessing) isProcessing = !this.state.isProcessing;
      this.setState(_extends({}, this.state, { isProcessing: isProcessing }));
    }
  }, {
    key: 'componentWillMount',
    value: function componentWillMount() {
      if (!this.props.data.MakeModelList || !this.props.data.MakeModelList.length) {
        this.props.fetchMakeModelList();
      }
    }
  }, {
    key: 'componentDidMount',
    value: function componentDidMount() {
      (0, _popupScroll.addPopupEvents)(this.popupContent);
    }
  }, {
    key: 'componentWillUnmount',
    value: function componentWillUnmount() {
      (0, _popupScroll.removePopupEvents)(this.popupContent);
    }
  }, {
    key: 'shouldComponentUpdate',
    value: function shouldComponentUpdate(nextProps, nextState) {
      if (nextProps.isActive === this.props.isActive && nextState.isProcessing == this.state.isProcessing && nextState.modelValue == this.state.modelValue) {
        return false;
      }

      if (nextProps.isActive) {
        this.popupContent.scrollTop = this.popupScrollPosition;
      }

      return true;
    }
  }, {
    key: 'render',
    value: function render() {
      var _props = this.props,
          isActive = _props.isActive,
          data = _props.data;

      var MakeModelList = this.state.makeModelList;
      var popupActiveClassName = isActive ? 'popup--active' : '';
      var popupClasses = 'select-bike-popup popup-content ' + popupActiveClassName;
      var result = null;
      if (MakeModelList != undefined && MakeModelList.length != 0) {
        result = _react2.default.createElement(_Accordion2.default, {
          closeable: true,
          allOpen: this.state.searchMode,
          transitionCloseTime: 1,
          items: this.getList(MakeModelList)
        });
      } else {
        result = _react2.default.createElement(_NoResult2.default, {
          type: 'select-bike__no-bike-content',
          imageClass: 'select-bike__no-bike',
          title: 'No Matching Bikes Found'
        });
      }
      return _react2.default.createElement(
        'div',
        { className: popupClasses },
        _react2.default.createElement(
          'div',
          { ref: this.setContentRef, className: 'select-bike-popup__content' },
          _react2.default.createElement(
            'div',
            { className: 'popup__head' },
            _react2.default.createElement(
              'div',
              { className: 'popup-head__content' },
              _react2.default.createElement('span', { onClick: this.handleCloseClick, className: 'popup__close' }),
              _react2.default.createElement(
                'div',
                { className: 'popup__search-box' },
                _react2.default.createElement(
                  'p',
                  { className: 'popup-search__title' },
                  'Select Make and Model'
                ),
                _react2.default.createElement(
                  'div',
                  { className: 'autocomplete-box' },
                  _react2.default.createElement(
                    'div',
                    { className: 'autocomplete-field' },
                    _react2.default.createElement('input', { type: 'text', value: this.state.modelValue, className: 'form-control',
                      placeholder: 'Type to select Make and Model', onChange: this.filterMakeModelList, onFocus: this.filterMakeModelList, onBlur: this.resetSearchMode }),
                    _react2.default.createElement(
                      'span',
                      { className: 'autocomplete-box__clear', onClick: this.handleClear },
                      'Clear'
                    )
                  )
                )
              )
            )
          ),
          _react2.default.createElement(
            'div',
            { className: 'select-bike__body select-bike__accordion' },
            _react2.default.createElement(
              'div',
              { className: this.state.isProcessing ? "hide" : "show" },
              result
            ),
            _react2.default.createElement(
              'div',
              { className: !this.state.isProcessing ? "hide" : "show" },
              _react2.default.createElement(_SpinnerRelative2.default, null)
            )
          )
        )
      );
    }
  }]);

  return SelectBikePopup;
}(_react2.default.Component);

exports.default = SelectBikePopup;

/***/ }),
/* 201 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

var _propTypes = __webpack_require__(1);

var _propTypes2 = _interopRequireDefault(_propTypes);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; } /*
                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                * https://github.com/glennflanagan/react-collapsible
                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                * - Modify class names with BEM convention
                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                */

var propTypes = {
  transitionTime: _propTypes2.default.number,
  transitionCloseTime: _propTypes2.default.number,
  triggerTagName: _propTypes2.default.string,
  easing: _propTypes2.default.string,
  open: _propTypes2.default.bool,
  classParentString: _propTypes2.default.string,
  openedClassName: _propTypes2.default.string,
  triggerStyle: _propTypes2.default.object,
  triggerClassName: _propTypes2.default.string,
  triggerOpenedClassName: _propTypes2.default.string,
  contentOuterClassName: _propTypes2.default.string,
  contentInnerClassName: _propTypes2.default.string,
  accordionPosition: _propTypes2.default.oneOfType([_propTypes2.default.string, _propTypes2.default.number]),
  handleTriggerClick: _propTypes2.default.func,
  onOpen: _propTypes2.default.func,
  onClose: _propTypes2.default.func,
  onOpening: _propTypes2.default.func,
  onClosing: _propTypes2.default.func,
  trigger: _propTypes2.default.oneOfType([_propTypes2.default.string, _propTypes2.default.element]),
  triggerWhenOpen: _propTypes2.default.oneOfType([_propTypes2.default.string, _propTypes2.default.element]),
  triggerDisabled: _propTypes2.default.bool,
  lazyRender: _propTypes2.default.bool,
  overflowWhenOpen: _propTypes2.default.oneOf(['hidden', 'visible', 'auto', 'scroll', 'inherit', 'initial', 'unset']),
  triggerSibling: _propTypes2.default.oneOfType([_propTypes2.default.element, _propTypes2.default.func])
};

var defaultProps = {
  transitionTime: 200,
  transitionCloseTime: null,
  triggerTagName: 'span',
  easing: 'linear',
  open: false,
  classParentString: 'collapsible',
  triggerDisabled: false,
  lazyRender: false,
  overflowWhenOpen: 'hidden',
  openedClassName: '',
  triggerStyle: null,
  triggerClassName: '',
  triggerOpenedClassName: '',
  contentOuterClassName: '',
  contentInnerClassName: '',
  className: '',
  triggerSibling: null,
  onOpen: null,
  onClose: null,
  onOpening: null,
  onClosing: null
};

var Collapsible = function (_Component) {
  _inherits(Collapsible, _Component);

  function Collapsible(props) {
    _classCallCheck(this, Collapsible);

    // Bind class methods
    var _this = _possibleConstructorReturn(this, (Collapsible.__proto__ || Object.getPrototypeOf(Collapsible)).call(this, props));

    _this.handleTriggerClick = _this.handleTriggerClick.bind(_this);
    _this.handleTransitionEnd = _this.handleTransitionEnd.bind(_this);
    _this.continueOpenCollapsible = _this.continueOpenCollapsible.bind(_this);
    _this.setInnerRef = _this.setInnerRef.bind(_this);

    // Defaults the dropdown to be closed
    if (props.open) {
      _this.state = {
        isClosed: false,
        shouldSwitchAutoOnNextCycle: false,
        height: 'auto',
        transition: 'none',
        hasBeenOpened: true,
        overflow: props.overflowWhenOpen,
        inTransition: false
      };
    } else {
      _this.state = {
        isClosed: true,
        shouldSwitchAutoOnNextCycle: false,
        height: 0,
        transition: 'height ' + props.transitionTime + 'ms ' + props.easing,
        hasBeenOpened: false,
        overflow: 'hidden',
        inTransition: false
      };
    }
    return _this;
  }

  _createClass(Collapsible, [{
    key: 'componentDidUpdate',
    value: function componentDidUpdate(prevProps, prevState) {
      var _this2 = this;

      if (this.state.shouldOpenOnNextCycle) {
        this.continueOpenCollapsible();
      }

      if (prevState.height === 'auto' && this.state.shouldSwitchAutoOnNextCycle === true) {
        window.setTimeout(function () {
          // Set small timeout to ensure a true re-render
          _this2.setState({
            height: 0,
            overflow: 'hidden',
            isClosed: true,
            shouldSwitchAutoOnNextCycle: false
          });
        }, 50);
      }

      // If there has been a change in the open prop (controlled by accordion)
      if (prevProps.open !== this.props.open) {
        if (this.props.open === true) {
          this.openCollapsible();
          if (this.props.onOpening) {
            this.props.onOpening();
          }
        } else {
          this.closeCollapsible();
          if (this.props.onClosing) {
            this.props.onClosing();
          }
        }
      }
    }
  }, {
    key: 'closeCollapsible',
    value: function closeCollapsible() {
      this.setState({
        shouldSwitchAutoOnNextCycle: true,
        height: this.collapsibleInner.offsetHeight,
        transition: 'height ' + (this.props.transitionCloseTime ? this.props.transitionCloseTime : this.props.transitionTime) + 'ms ' + this.props.easing,
        inTransition: true
      });
    }
  }, {
    key: 'openCollapsible',
    value: function openCollapsible() {
      this.setState({
        inTransition: true,
        shouldOpenOnNextCycle: true
      });
    }
  }, {
    key: 'continueOpenCollapsible',
    value: function continueOpenCollapsible() {
      this.setState({
        height: this.collapsibleInner.offsetHeight,
        transition: 'height ' + this.props.transitionTime + 'ms ' + this.props.easing,
        isClosed: false,
        hasBeenOpened: true,
        inTransition: true,
        shouldOpenOnNextCycle: false
      });
    }
  }, {
    key: 'handleTriggerClick',
    value: function handleTriggerClick(event) {
      event.preventDefault();

      if (this.props.triggerDisabled) {
        return;
      }

      if (this.props.handleTriggerClick) {
        this.props.handleTriggerClick(this.props.accordionPosition, event);
      } else {
        if (this.state.isClosed === true) {
          this.openCollapsible();

          if (this.props.onOpening) {
            this.props.onOpening();
          }
        } else {
          this.closeCollapsible();

          if (this.props.onClosing) {
            this.props.onClosing();
          }
        }
      }
    }
  }, {
    key: 'renderNonClickableTriggerElement',
    value: function renderNonClickableTriggerElement() {
      var TriggerSibling = this.props.triggerSibling;


      if (TriggerSibling && typeof TriggerSibling === 'string') {
        return _react2.default.createElement(
          'span',
          { className: this.props.classParentString + '__trigger-sibling' },
          TriggerSibling
        );
      } else if (TriggerSibling) {
        return _react2.default.createElement(TriggerSibling, null);
      }

      return null;
    }
  }, {
    key: 'handleTransitionEnd',
    value: function handleTransitionEnd(event) {
      // Switch to height auto to make the container responsive
      if (!this.state.isClosed) {
        this.setState({
          height: 'auto',
          overflow: this.props.overflowWhenOpen,
          inTransition: false
        });

        if (this.props.onOpen) {
          this.props.onOpen(event);
        }
      } else {
        this.setState({
          inTransition: false
        });

        if (this.props.onClose) {
          this.props.onClose(event);
        }
      }
    }
  }, {
    key: 'setInnerRef',
    value: function setInnerRef(ref) {
      this.collapsibleInner = ref;
    }
  }, {
    key: 'render',
    value: function render() {
      var dropdownStyle = {
        height: this.state.height,
        WebkitTransition: this.state.transition,
        msTransition: this.state.transition,
        transition: this.state.transition,
        overflow: this.state.overflow
      };

      var _state = this.state,
          isClosed = _state.isClosed,
          hasBeenOpened = _state.hasBeenOpened,
          inTransition = _state.inTransition;


      var openClass = isClosed ? 'collapsible--closed' : 'collapsible--open';
      var disabledClass = this.props.triggerDisabled ? 'collapsible--disabled' : '';

      //If user wants different text when tray is open
      var trigger = isClosed === false && this.props.triggerWhenOpen !== undefined ? this.props.triggerWhenOpen : this.props.trigger;

      // If user wants a trigger wrapping element different than 'span'
      var TriggerElement = this.props.triggerTagName;

      // Don't render children until the first opening of the Collapsible if lazy rendering is enabled
      var children = this.props.lazyRender && !hasBeenOpened && isClosed && !inTransition ? null : this.props.children;

      // Construct CSS classes strings
      var triggerClassString = this.props.classParentString + '__trigger ' + openClass + ' ' + disabledClass + ' ' + (isClosed ? this.props.triggerClassName : this.props.triggerOpenedClassName);
      var parentClassString = this.props.classParentString + ' ' + (isClosed ? this.props.className : this.props.openedClassName);
      var outerClassString = this.props.classParentString + '__content-outer ' + this.props.contentOuterClassName;
      var innerClassString = this.props.classParentString + '__content-inner ' + this.props.contentInnerClassName;

      return _react2.default.createElement(
        'div',
        { className: parentClassString.trim() },
        _react2.default.createElement(
          'div',
          {
            className: triggerClassString.trim(),
            onClick: this.handleTriggerClick,
            style: this.props.triggerStyle && this.props.triggerStyle
          },
          trigger
        ),
        this.props.triggerSibling ? this.renderNonClickableTriggerElement() : '',
        _react2.default.createElement(
          'div',
          {
            className: outerClassString.trim(),
            style: dropdownStyle,
            onTransitionEnd: this.handleTransitionEnd
          },
          _react2.default.createElement(
            'div',
            {
              className: innerClassString.trim(),
              ref: this.setInnerRef
            },
            children
          )
        )
      );
    }
  }]);

  return Collapsible;
}(_react.Component);

Collapsible.propTypes = propTypes;
Collapsible.defaultProps = defaultProps;

exports.default = Collapsible;

/***/ }),
/* 202 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

var _propTypes = __webpack_require__(1);

var _propTypes2 = _interopRequireDefault(_propTypes);

var _ProgressBar = __webpack_require__(203);

var _ProgressBar2 = _interopRequireDefault(_ProgressBar);

var _ProgressBarItem = __webpack_require__(82);

var _ProgressBarItem2 = _interopRequireDefault(_ProgressBarItem);

var _ModelCard = __webpack_require__(204);

var _ModelCard2 = _interopRequireDefault(_ModelCard);

var _SelectionStepCard = __webpack_require__(205);

var _SelectionStepCard2 = _interopRequireDefault(_SelectionStepCard);

var _ScrollTo = __webpack_require__(206);

var _debounce = __webpack_require__(207);

var _WidgetsCommon = __webpack_require__(30);

var _progressBarConstants = __webpack_require__(83);

var _popUpUtils = __webpack_require__(11);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _toConsumableArray(arr) { if (Array.isArray(arr)) { for (var i = 0, arr2 = Array(arr.length); i < arr.length; i++) { arr2[i] = arr[i]; } return arr2; } else { return Array.from(arr); } }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var propTypes = {
  // handle select bike text click function
  handleSelectBikeClick: _propTypes2.default.func,
  // handle select city text click function
  handleSelectCityClick: _propTypes2.default.func,
  isShown: _propTypes2.default.bool
};

var defaultProps = {
  handleSelectBikeClick: null,
  handleSelectCityClick: null,
  isShown: false
};

var EMISteps = function (_React$Component) {
  _inherits(EMISteps, _React$Component);

  function EMISteps(props) {
    _classCallCheck(this, EMISteps);

    var _this = _possibleConstructorReturn(this, (EMISteps.__proto__ || Object.getPrototypeOf(EMISteps)).call(this, props));

    _this.renderMakeCard = function (modelData) {
      return modelData.makeName === '' ? _react2.default.createElement(
        'div',
        { className: 'select-step-card__placeholder select-card__make-selection', onClick: _this.handleMakeSelect },
        _react2.default.createElement(
          'div',
          { className: 'select-step-card__link' },
          _react2.default.createElement(
            'span',
            { className: 'selection-step__text' },
            'Search Make and Model'
          ),
          _react2.default.createElement('span', { className: 'selection-step__right-arrow' })
        )
      ) : _react2.default.createElement(_ModelCard2.default, { model: modelData, onClick: _this.handleMakeSelect });
    };

    _this.renderCityCard = function () {
      return _react2.default.createElement(
        'div',
        { ref: _this.setCitySelectRef, className: 'select-step-card__placeholder select-card__city-selection', onClick: _this.props.onSelectCityClick },
        _react2.default.createElement(
          'div',
          { className: 'select-step-card__link' },
          _react2.default.createElement(
            'span',
            { className: 'selection-step__text' },
            'Select your City'
          ),
          _react2.default.createElement('span', { className: 'selection-step__right-arrow' })
        )
      );
    };

    _this.setSelectionContainerRef = function (element) {
      _this.StepSelectionContainer = element;
    };

    _this.setSelectionStepsRef = function (element) {
      _this.StepSelection = element;
    };

    _this.setCitySelectRef = function (element) {
      _this.citySelect = element;
    };

    _this.scrollCityToView = function () {
      _this.setState(_extends({}, state, { shouldScroll: true }));
    };

    _this.handleMakeSelect = function (event) {
      _this.props.onSelectBikeClick();
    };

    _this.handleScroll = function (event) {
      (0, _debounce.debounce)(function () {
        if (_this.StepSelectionContainer.classList.contains('selection-step--overflow')) {
          (0, _ScrollTo.inView)(_this.StepSelection, _this.StepSelectionContainer);
          var activeCard = _this.StepSelection.querySelector(".selection-steps__card.inview--active");
          var activeIndex = [].concat(_toConsumableArray(_this.StepSelection.children)).indexOf(activeCard);
          [].concat(_toConsumableArray(document.querySelector('.progress-bar').children)).forEach(function (item) {
            item.firstChild.classList.remove('focused');
          });
          document.querySelector('.progress-bar__item:nth-of-type(' + (activeIndex + 1) + ') .progress-bar-item__content').classList.add('focused');
        }
      }, 250)();
    };

    _this.componentDidMount = function () {
      _this.StepSelectionContainer.addEventListener('scroll', _this.handleScroll);
    };

    _this.componentWillUnmount = function () {
      _this.StepSelectionContainer.removeEventListener('scroll', _this.handleScroll);
    };

    _this.componentDidUpdate = function () {
      if (_this.state.shouldScroll) (0, _ScrollTo.scrollIntoView)(_this.StepSelectionContainer, _this.citySelect);
    };

    _this.handleMakeSelect = _this.handleMakeSelect.bind(_this);
    _this.setCitySelectRef = _this.setCitySelectRef.bind(_this);
    _this.scrollCityToView = _this.scrollCityToView.bind(_this);
    _this.state = {
      shouldScroll: false,
      bike: _progressBarConstants.progressBarStatus.DISABLE,
      city: _progressBarConstants.progressBarStatus.DISABLE,
      activeStep: 1 //current active step
    };
    return _this;
  }

  _createClass(EMISteps, [{
    key: 'render',
    value: function render() {
      var model = this.props.model;
      var modelData = {
        makeName: model != null && model.makeName != null ? model.makeName : "",
        modelName: model != null && model.modelName != null ? model.modelName : "",
        modelImage: model != null && model.originalImagePath != null ? (0, _WidgetsCommon.createImageUrl)(model.hostUrl, model.originalImagePath, '310x174') : "",
        rating: model != null && model.rating != null ? model.rating : 0
      };
      var isOverflow = model && model.modelId > 0 ? 'selection-step--overflow' : '';
      var displayStyle = { 'display': this.props.isShown ? 'block' : 'none' };
      return _react2.default.createElement(
        'div',
        { className: 'emi-calculator__progress-container', style: displayStyle },
        _react2.default.createElement(
          _ProgressBar2.default,
          null,
          _react2.default.createElement(
            _ProgressBarItem2.default,
            { stepNumber: 1, status: this.state.bike, isActive: this.state.activeStep == 1 ? true : false },
            'Select bike'
          ),
          _react2.default.createElement(
            _ProgressBarItem2.default,
            { stepNumber: 2, status: this.state.city, isActive: this.state.activeStep == 2 ? true : false },
            'Select city'
          )
        ),
        _react2.default.createElement(
          'div',
          { ref: this.setSelectionContainerRef, className: "selection-steps__container " + isOverflow },
          _react2.default.createElement(
            'div',
            { ref: this.setSelectionStepsRef, className: 'emi-calulator__selection-steps' },
            _react2.default.createElement(
              _SelectionStepCard2.default,
              null,
              this.renderMakeCard(modelData)
            ),
            isOverflow && _react2.default.createElement(
              _SelectionStepCard2.default,
              null,
              this.renderCityCard()
            )
          )
        )
      );
    }
  }]);

  return EMISteps;
}(_react2.default.Component);

exports.default = EMISteps;

/***/ }),
/* 203 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

var _ProgressBarItem = __webpack_require__(82);

var _ProgressBarItem2 = _interopRequireDefault(_ProgressBarItem);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var ProgressBar = function (_React$Component) {
  _inherits(ProgressBar, _React$Component);

  function ProgressBar(props) {
    _classCallCheck(this, ProgressBar);

    return _possibleConstructorReturn(this, (ProgressBar.__proto__ || Object.getPrototypeOf(ProgressBar)).call(this, props));
  }

  _createClass(ProgressBar, [{
    key: 'render',
    value: function render() {
      return _react2.default.createElement(
        'div',
        { className: 'progress-bar' },
        this.props.children
      );
    }
  }]);

  return ProgressBar;
}(_react2.default.Component);

exports.default = ProgressBar;

/***/ }),
/* 204 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

var _propTypes = __webpack_require__(1);

var _propTypes2 = _interopRequireDefault(_propTypes);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var ModelCard = function ModelCard(props) {
  var model = props.model,
      onClick = props.onClick;


  return _react2.default.createElement(
    'div',
    { className: 'model-card__placeholder', onClick: onClick },
    _react2.default.createElement(
      'div',
      { className: 'model-card__content' },
      _react2.default.createElement(
        'div',
        { className: 'model-card__image' },
        _react2.default.createElement('img', { src: model.modelImage, alt: model.modelName })
      ),
      _react2.default.createElement(
        'div',
        { className: 'model-card__detail' },
        model.rating > 0 && _react2.default.createElement(
          'span',
          { className: 'rating-badge-sm', 'data-rate-bg': Math.floor(model.rating) },
          _react2.default.createElement('span', { className: 'rating-badge__star' }),
          _react2.default.createElement(
            'span',
            null,
            model.rating.toFixed(1)
          )
        ),
        _react2.default.createElement(
          'h3',
          null,
          _react2.default.createElement(
            'span',
            { className: 'model-card__make' },
            model.makeName
          ),
          _react2.default.createElement(
            'span',
            { className: 'model-card__model' },
            model.modelName
          )
        )
      )
    )
  );
};

ModelCard.propTypes = {
  model: _propTypes2.default.shape({
    makeName: _propTypes2.default.string,
    modelName: _propTypes2.default.string,
    modelImage: _propTypes2.default.string,
    rating: _propTypes2.default.number
  }),
  onClick: _propTypes2.default.func
};

ModelCard.defaultProps = {
  model: {}
};

exports.default = ModelCard;

/***/ }),
/* 205 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var SelectionStepCard = function (_React$Component) {
  _inherits(SelectionStepCard, _React$Component);

  function SelectionStepCard(props) {
    _classCallCheck(this, SelectionStepCard);

    return _possibleConstructorReturn(this, (SelectionStepCard.__proto__ || Object.getPrototypeOf(SelectionStepCard)).call(this, props));
  }

  _createClass(SelectionStepCard, [{
    key: "render",
    value: function render() {
      return _react2.default.createElement(
        "div",
        { className: "selection-steps__card" },
        _react2.default.createElement(
          "div",
          { className: "selection-step__card-content" },
          this.props.children
        )
      );
    }
  }]);

  return SelectionStepCard;
}(_react2.default.Component);

exports.default = SelectionStepCard;

/***/ }),
/* 206 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});

function _toConsumableArray(arr) { if (Array.isArray(arr)) { for (var i = 0, arr2 = Array(arr.length); i < arr.length; i++) { arr2[i] = arr[i]; } return arr2; } else { return Array.from(arr); } }

/*
 * ScrollTo animation using pure javascript
 * https://gist.github.com/andjosh/6764939
 * 
 * debounce
 * https://davidwalsh.name/javascript-debounce-function
 */

var scrollTop = exports.scrollTop = function scrollTop(element, to) {
  var duration = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : 500;

  var isElementWindow = element === window ? true : false;

  var start = isElementWindow ? window.pageYOffset || document.documentElement.scrollTop : element.scrollTop,
      change = to - start,
      currentTime = 0,
      increment = 20;

  var animateScroll = function animateScroll() {
    currentTime += increment;
    var val = Math.easeInOutQuad(currentTime, start, change, duration);
    if (isElementWindow) {
      element.scrollTo(0, val);
    } else {
      element.scrollTop = val;
    }

    if (currentTime < duration) {
      setTimeout(animateScroll, increment);
    }
  };

  animateScroll();
};

var inView = exports.inView = function inView(element) {
  var isViewSelected = false;
  [].concat(_toConsumableArray(element.children)).forEach(function (item) {

    if (isElementVisible(item.firstChild, 'horizontal', 50)) {
      if (isViewSelected != true) {
        [].concat(_toConsumableArray(item.parentElement.children)).forEach(function (child) {
          child.classList.remove('inview--active');
        });

        item.classList.add("inview--active");
        isViewSelected = true;
      }
    }
  });
};

/*
  direction: horizontal, vertical
  threshold: visiblity of card on viewport eg, 50, 75
*/
var isElementVisible = exports.isElementVisible = function isElementVisible(element, direction, threshold) {
  var elementRect = element.getBoundingClientRect();
  var thresholdValue;
  if (typeof threshold === 'number') {
    thresholdValue = threshold ? elementRect.width * (parseInt(threshold) / 100) : 0;
  } else {
    console.log('please provide threshold value as int in isElementVisible');
  }

  switch (direction) {
    case 'horizontal':
      return elementRect.left + thresholdValue > 0 && elementRect.right - thresholdValue < window.innerWidth;
    case 'vertical':
      return elementRect.top + thresholdValue > 0 && elementRect.bottom - thresholdValue < window.innerHeight;
    default:
      return elementRect.left + thresholdValue > 0 && elementRect.right - thresholdValue < window.innerWidth && elementRect.top + thresholdValue > 0 && elementRect.bottom - thresholdValue < window.innerHeight;
  }
};
var scrollLeft = exports.scrollLeft = function scrollLeft(element, to) {
  var duration = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : 500;

  var start = element.scrollLeft,
      change = to - start,
      currentTime = 0,
      increment = 20;

  var animateScroll = function animateScroll() {
    currentTime += increment;
    var val = Math.easeInOutQuad(currentTime, start, change, duration);
    element.scrollLeft = val;

    if (currentTime < duration) {
      setTimeout(animateScroll, increment);
    }
  };

  animateScroll();
};

var scrollIntoView = exports.scrollIntoView = function scrollIntoView(element, event) {
  if (event == null) {
    return null;
  }
  var elementRect = null;
  if (event.currentTarget != null) {
    elementRect = event.currentTarget.getBoundingClientRect();
  } else {
    elementRect = event.getBoundingClientRect();
  }

  if (elementRect.left < 0 || elementRect.right > window.innerWidth) {
    var leftPosition = element.scrollLeft + elementRect.left - 20;

    scrollLeft(element, leftPosition);
  }
};

//t = current time
//b = start value
//c = change in value
//d = duration
Math.easeInOutQuad = function (t, b, c, d) {
  t /= d / 2;
  if (t < 1) {
    return c / 2 * t * t + b;
  }
  t--;
  return -c / 2 * (t * (t - 2) - 1) + b;
};

/***/ }),
/* 207 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
	value: true
});
var debounce = exports.debounce = function debounce(func, wait, immediate) {
	var timeout;
	return function () {
		var context = this,
		    args = arguments;
		var later = function later() {
			timeout = null;
			if (!immediate) func.apply(context, args);
		};
		var callNow = immediate && !timeout;
		clearTimeout(timeout);
		timeout = setTimeout(later, wait);
		if (callNow) func.apply(context, args);
	};
};

/***/ }),
/* 208 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
	value: true
});
exports.EMICalculator = undefined;

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

var _reactRedux = __webpack_require__(12);

var _redux = __webpack_require__(10);

var _immutableWrapperContainer = __webpack_require__(9);

var _EMICalculatorHeader = __webpack_require__(84);

var _EMICalculatorHeader2 = _interopRequireDefault(_EMICalculatorHeader);

var _DownPaymentSlider = __webpack_require__(210);

var _DownPaymentSlider2 = _interopRequireDefault(_DownPaymentSlider);

var _TenureSlider = __webpack_require__(212);

var _TenureSlider2 = _interopRequireDefault(_TenureSlider);

var _InterestSlider = __webpack_require__(214);

var _InterestSlider2 = _interopRequireDefault(_InterestSlider);

var _SpinnerRelative = __webpack_require__(8);

var _SpinnerRelative2 = _interopRequireDefault(_SpinnerRelative);

var _EmiCalculations = __webpack_require__(217);

var _EmiPieBreakup = __webpack_require__(218);

var _EmiPieBreakup2 = _interopRequireDefault(_EmiPieBreakup);

var _formatAmount = __webpack_require__(15);

var _emiDownPaymentSlider = __webpack_require__(41);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

function getEMICalulatorData(sliderDp, sliderInt, sliderTenure) {
	var loanAmount = sliderDp.onRoadPrice - sliderDp.values[0];
	var emiCalculation = (0, _EmiCalculations.EmiCalculation)(loanAmount, sliderTenure.values[0], sliderInt.values[0]);
	var interestPayable = (0, _EmiCalculations.InterestPayable)(loanAmount, sliderTenure.values[0], sliderInt.values[0]);
	var totalBikeCost = (0, _EmiCalculations.TotalPrincipalAmt)(sliderDp.values[0], loanAmount, sliderTenure.values[0], sliderInt.values[0]);

	var downpayment = sliderDp.values[0];
	var tenure = sliderTenure.values[0];

	return {
		emiCalculation: emiCalculation,
		interestPayable: interestPayable,
		totalBikeCost: totalBikeCost,
		downpayment: downpayment,
		tenure: tenure
	};
}

var EMICalculator = exports.EMICalculator = function (_React$Component) {
	_inherits(EMICalculator, _React$Component);

	function EMICalculator(props) {
		_classCallCheck(this, EMICalculator);

		var _this = _possibleConstructorReturn(this, (EMICalculator.__proto__ || Object.getPrototypeOf(EMICalculator)).call(this, props));

		_this.handleDownPaymentSliderDragMove = function (sliderDp) {
			_this.setState(getEMICalulatorData(sliderDp, _this.props.sliderInt, _this.props.sliderTenure));
		};

		_this.handleTenureSliderDragMove = function (sliderTenure) {
			_this.setState(getEMICalulatorData(_this.props.sliderDp, _this.props.sliderInt, sliderTenure));
		};

		_this.handleInterestSliderDragMove = function (sliderInt) {
			_this.setState(getEMICalulatorData(_this.props.sliderDp, sliderInt, _this.props.sliderTenure));
		};

		_this.state = getEMICalulatorData(_this.props.sliderDp, _this.props.sliderInt, _this.props.sliderTenure);
		return _this;
	}

	_createClass(EMICalculator, [{
		key: 'componentWillReceiveProps',
		value: function componentWillReceiveProps(nextProps) {
			this.setState(getEMICalulatorData(nextProps.sliderDp, nextProps.sliderInt, nextProps.sliderTenure));
		}
	}, {
		key: 'render',
		value: function render() {
			if (this.props.sliderDp.onRoadPrice === 0) {
				return null;
			}

			var _state = this.state,
			    downpayment = _state.downpayment,
			    tenure = _state.tenure,
			    emiCalculation = _state.emiCalculation,
			    interestPayable = _state.interestPayable,
			    totalBikeCost = _state.totalBikeCost;
			var _props = this.props,
			    sliderDp = _props.sliderDp,
			    financeCitySelection = _props.financeCitySelection,
			    IsFetching = _props.IsFetching;


			var pieLoanAmt = sliderDp.onRoadPrice - sliderDp.values[0];
			var finalIntPayable = (0, _formatAmount.formatToINR)(parseInt(interestPayable));
			var finalTotalPrincipalAmt = (0, _formatAmount.formatToINR)(totalBikeCost);

			var pieChartData = {
				intStatePay: interestPayable,
				pieLoanAmt: pieLoanAmt,
				pieTotalPay: totalBikeCost,
				interestPay: finalIntPayable,
				TotalPrincipalAmt: finalTotalPrincipalAmt
			};

			return IsFetching ? _react2.default.createElement(
				'div',
				{ className: 'emi-outer-container' },
				_react2.default.createElement(
					'div',
					{ className: 'emi-spinner-container' },
					_react2.default.createElement(_SpinnerRelative2.default, null)
				),
				_react2.default.createElement('div', { className: 'emi-piechart-spinner' })
			) : _react2.default.createElement(
				'div',
				{ className: 'emi-outer-container' },
				_react2.default.createElement(
					'div',
					{ className: 'emi-calci-container' },
					_react2.default.createElement(_EMICalculatorHeader2.default, {
						emiCalculation: emiCalculation,
						tenure: tenure,
						onRoadPrice: sliderDp.onRoadPrice,
						city: financeCitySelection
					}),
					_react2.default.createElement(
						'div',
						{ className: 'emi-slider-container' },
						_react2.default.createElement(_DownPaymentSlider2.default, { onSliderDragMove: this.handleDownPaymentSliderDragMove }),
						_react2.default.createElement(
							'div',
							{ className: 'tenure-interest-slider' },
							_react2.default.createElement(
								'div',
								{ className: 'slider-input-container tenure-unit' },
								_react2.default.createElement(_TenureSlider2.default, { onSliderDragMove: this.handleTenureSliderDragMove })
							),
							_react2.default.createElement(
								'div',
								{ className: 'slider-input-container interest-unit' },
								_react2.default.createElement(_InterestSlider2.default, { onSliderDragMove: this.handleInterestSliderDragMove })
							)
						)
					)
				),
				_react2.default.createElement(
					'div',
					{ className: 'view-breakup-container' },
					_react2.default.createElement(_EmiPieBreakup2.default, {
						onRoadPrice: sliderDp.onRoadPrice,
						downpayment: downpayment,
						tenure: tenure,
						interestPayable: interestPayable,
						pieChartData: pieChartData
					})
				)
			);
		}
	}]);

	return EMICalculator;
}(_react2.default.Component);

var mapStateToProps = function mapStateToProps(state) {
	var sliderDp = state.getIn(['Emi', 'VehicleDownPayment', 'slider']);
	var sliderTenure = state.getIn(['Emi', 'VehicleTenure', 'slider']);
	var sliderInt = state.getIn(['Emi', 'VehicleInterest', 'slider']);
	var financeCitySelection = state.getIn(['Finance', 'FinanceCityPopup', 'Selection']);
	return {
		sliderDp: sliderDp,
		sliderTenure: sliderTenure,
		sliderInt: sliderInt,
		financeCitySelection: financeCitySelection
	};
};

exports.default = (0, _reactRedux.connect)(mapStateToProps, null)((0, _immutableWrapperContainer.toJS)(EMICalculator));

/***/ }),
/* 209 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


function monthsToYears(months) {
	var yearText = "";
	var monthText = "";
	var yearCal = Math.floor(months / 12);
	var monthCal = months % 12;
	if (monthCal < 2) {
		monthText = "month";
	} else {
		monthText = "months";
	}
	if (months > 11 && months < 24) {
		yearText = "Year";
	} else {
		yearText = "Years";
	}
	if (monthCal == 0) {
		return yearCal + " " + yearText;
	} else if (months > 11) {
		return yearCal + " " + yearText + " and " + monthCal + " " + monthText;
	} else {
		return monthCal + " " + monthText;
	}
}

module.exports = {
	monthsToYears: monthsToYears
};

/***/ }),
/* 210 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

var _reactRedux = __webpack_require__(12);

var _redux = __webpack_require__(10);

var _immutableWrapperContainer = __webpack_require__(9);

var _Rheostat = __webpack_require__(43);

var _Rheostat2 = _interopRequireDefault(_Rheostat);

var _RheostatPit = __webpack_require__(44);

var _RheostatPit2 = _interopRequireDefault(_RheostatPit);

var _Tooltip = __webpack_require__(86);

var _Tooltip2 = _interopRequireDefault(_Tooltip);

var _linear = __webpack_require__(85);

var _linear2 = _interopRequireDefault(_linear);

var _emiDownPaymentSlider = __webpack_require__(41);

var _formatAmount = __webpack_require__(15);

var _analyticsUtils = __webpack_require__(5);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var EMIDownPayment = function (_React$Component) {
  _inherits(EMIDownPayment, _React$Component);

  function EMIDownPayment(props) {
    _classCallCheck(this, EMIDownPayment);

    var _this = _possibleConstructorReturn(this, (EMIDownPayment.__proto__ || Object.getPrototypeOf(EMIDownPayment)).call(this, props));

    _this.handleSliderChange = function (_ref) {
      var values = _ref.values;
      var _this$props = _this.props,
          slider = _this$props.slider,
          onSliderDragMove = _this$props.onSliderDragMove,
          updateDownPaymentSlider = _this$props.updateDownPaymentSlider;

      updateDownPaymentSlider({ values: values, userChange: true });
      if (onSliderDragMove) {
        onSliderDragMove(_extends({}, slider, {
          values: values
        }));
      }
    };

    _this.handleSliderDragMove = function (_ref2) {
      var values = _ref2.values;
      var _this$props2 = _this.props,
          slider = _this$props2.slider,
          onSliderDragMove = _this$props2.onSliderDragMove;


      _this.setState({
        values: values
      });

      if (onSliderDragMove) {
        onSliderDragMove(_extends({}, slider, {
          values: values
        }));
      }
    };

    _this.handleOpen = function () {
      if (gaObj != undefined) {
        (0, _analyticsUtils.triggerGA)(gaObj.name, 'ToolTip_Clicked', 'Down Payment');
      }
    };

    _this.state = {
      values: _this.props.slider.values
    };
    return _this;
  }

  _createClass(EMIDownPayment, [{
    key: 'updateLoanText',
    value: function updateLoanText() {
      var loanAmountUpdated = this.props.slider.onRoadPrice - this.state.values[0];
      return loanAmountUpdated;
    }
  }, {
    key: 'componentWillReceiveProps',
    value: function componentWillReceiveProps(nextProps) {
      if (nextProps.slider.userChange) {
        if (gaObj != undefined) {
          (0, _analyticsUtils.triggerGA)(gaObj.name, 'Interacted_With_EMI_Calculator', 'Down Payment Slider');
        }
      }
    }
  }, {
    key: 'shouldComponentUpdate',
    value: function shouldComponentUpdate(nextProps, nextState) {
      return nextProps.slider.onRoadPrice != this.props.slider.onRoadPrice || nextProps.slider.values[0] != this.props.slider.values[0] || nextState.values[0] != this.state.values[0];
    }
  }, {
    key: 'render',
    value: function render() {
      var slider = this.props.slider;

      slider = _extends({}, slider, {
        snap: true,
        snapOnDragMove: true,
        algorithm: _linear2.default,
        className: 'rheostat-downpayment',
        pitComponent: _RheostatPit2.default,
        pitPoints: [slider.min, slider.max],
        onChange: this.handleSliderChange.bind(this),
        onSliderDragMove: this.handleSliderDragMove,
        handleTooltipLabel: _formatAmount.formatToINR
      });
      var vehicleLoanAmount = (0, _formatAmount.formatToINR)(this.updateLoanText());
      return _react2.default.createElement(
        'div',
        { className: 'slider-input-container downpayment-unit' },
        _react2.default.createElement(
          'span',
          { className: 'slider__unit-title' },
          'Down Payment'
        ),
        _react2.default.createElement(
          'div',
          { className: 'slider-section', ref: 'downPaymentSlider' },
          _react2.default.createElement(_Rheostat2.default, slider),
          _react2.default.createElement(
            'div',
            { className: 'slider__label' },
            _react2.default.createElement(
              'span',
              { className: 'slider-label__left' },
              'Min. Down Payment '
            ),
            _react2.default.createElement(
              _Tooltip2.default,
              { onOpen: this.handleOpen, message: 'Banks expect a min. down payment of 10% of on-road price even if you have the best credit profile.' },
              _react2.default.createElement('span', { className: 'slider__info-icon' })
            )
          ),
          _react2.default.createElement(
            'div',
            { className: 'vehicle-loan-text' },
            'Your loan amount will be ',
            _react2.default.createElement(
              'span',
              { className: 'vehicle-loan__amount' },
              vehicleLoanAmount
            )
          )
        )
      );
    }
  }]);

  return EMIDownPayment;
}(_react2.default.Component);

var mapStateToProps = function mapStateToProps(state) {
  var slider = state.getIn(['Emi', 'VehicleDownPayment', 'slider']);

  return {
    slider: slider
  };
};

var mapDispatchToProps = function mapDispatchToProps(dispatch, getState) {
  return {
    updateDownPaymentSlider: (0, _redux.bindActionCreators)(_emiDownPaymentSlider.updateDownPaymentSlider, dispatch)
  };
};

module.exports = (0, _reactRedux.connect)(mapStateToProps, mapDispatchToProps)((0, _immutableWrapperContainer.toJS)(EMIDownPayment));

/***/ }),
/* 211 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
	value: true
});
var KEYS = exports.KEYS = {
	DOWN: 40,
	END: 35,
	ESC: 27,
	HOME: 36,
	LEFT: 37,
	PAGE_DOWN: 34,
	PAGE_UP: 33,
	RIGHT: 39,
	UP: 38
};

var PERCENT_EMPTY = exports.PERCENT_EMPTY = 0;
var PERCENT_FULL = exports.PERCENT_FULL = 100;

/***/ }),
/* 212 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

var _reactRedux = __webpack_require__(12);

var _redux = __webpack_require__(10);

var _immutableWrapperContainer = __webpack_require__(9);

var _Rheostat = __webpack_require__(43);

var _Rheostat2 = _interopRequireDefault(_Rheostat);

var _RheostatPit = __webpack_require__(44);

var _RheostatPit2 = _interopRequireDefault(_RheostatPit);

var _fixPoints = __webpack_require__(87);

var _fixPoints2 = _interopRequireDefault(_fixPoints);

var _emiTenureSlider = __webpack_require__(213);

var _formatAmount = __webpack_require__(15);

var _analyticsUtils = __webpack_require__(5);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var EMITenure = function (_React$Component) {
  _inherits(EMITenure, _React$Component);

  function EMITenure(props) {
    _classCallCheck(this, EMITenure);

    var _this = _possibleConstructorReturn(this, (EMITenure.__proto__ || Object.getPrototypeOf(EMITenure)).call(this, props));

    _this.handleSliderChange = function (_ref) {
      var values = _ref.values;
      var updateTenureSlider = _this.props.updateTenureSlider;

      updateTenureSlider({ values: values, userChange: true });
    };

    _this.handleSliderDragMove = function (_ref2) {
      var values = _ref2.values;
      var _this$props = _this.props,
          slider = _this$props.slider,
          onSliderDragMove = _this$props.onSliderDragMove;


      if (onSliderDragMove) {
        onSliderDragMove(_extends({}, slider, {
          values: values
        }));
      }
    };

    return _this;
  }

  _createClass(EMITenure, [{
    key: 'componentWillReceiveProps',
    value: function componentWillReceiveProps(nextProps) {
      if (nextProps.slider.userChange) {
        if (gaObj != undefined) {
          (0, _analyticsUtils.triggerGA)(gaObj.name, 'Interacted_With_EMI_Calculator', 'Tenure Slider');
        }
      }
    }
  }, {
    key: 'render',
    value: function render() {
      var slider = this.props.slider;


      slider = _extends({}, slider, {
        className: 'slider-rheostat',
        pitComponent: _RheostatPit2.default,
        pitPoints: [slider.min, slider.max],
        handleTooltipLabel: _formatAmount.formatToRound,
        onChange: this.handleSliderChange,
        onSliderDragMove: this.handleSliderDragMove
      });
      return _react2.default.createElement(
        'div',
        { className: 'emi-calci-header slider-input-container' },
        _react2.default.createElement(
          'span',
          { className: 'slider__unit-title' },
          'Tenure ',
          _react2.default.createElement(
            'span',
            { className: 'slider__unit-text' },
            '(Months)'
          )
        ),
        _react2.default.createElement(
          'div',
          { className: 'slider-section', ref: 'tenureSlider' },
          _react2.default.createElement(_Rheostat2.default, slider)
        )
      );
    }
  }]);

  return EMITenure;
}(_react2.default.Component);

var mapStateToProps = function mapStateToProps(state) {
  var slider = state.getIn(['Emi', 'VehicleTenure', 'slider']);

  return {
    slider: slider
  };
};

var mapDispatchToProps = function mapDispatchToProps(dispatch, getState) {
  return {
    updateTenureSlider: (0, _redux.bindActionCreators)(_emiTenureSlider.updateTenureSlider, dispatch)
  };
};

module.exports = (0, _reactRedux.connect)(mapStateToProps, mapDispatchToProps)((0, _immutableWrapperContainer.toJS)(EMITenure));

/***/ }),
/* 213 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.updateTenureSlider = undefined;

var _emiActionTypes = __webpack_require__(42);

var _immutable = __webpack_require__(17);

var updateTenureSliderValue = function updateTenureSliderValue(values, userChange) {
  values = values.map(function (x) {
    return parseInt(x);
  });
  return function (dispatch) {
    dispatch({
      type: _emiActionTypes.emiCalculatorAction.UPDATE_TENURE_SLIDER_VALUE,
      values: values,
      userChange: userChange
    });
  };
};

var shouldTenureSliderUpdate = function shouldTenureSliderUpdate(values, slider) {
  var update = true;

  if (slider.values[0] === values[0]) {
    update = false;
  }

  return update;
};

var updateTenureSlider = exports.updateTenureSlider = function updateTenureSlider(_ref) {
  var values = _ref.values,
      userChange = _ref.userChange;
  return function (dispatch, getState) {
    var tenureSliderMap = getState(['Emi', 'VehicleTenure', 'slider']);
    if (shouldTenureSliderUpdate(values, tenureSliderMap)) {
      dispatch(updateTenureSliderValue(values, userChange));
    }
  };
};

/***/ }),
/* 214 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

var _reactRedux = __webpack_require__(12);

var _redux = __webpack_require__(10);

var _immutableWrapperContainer = __webpack_require__(9);

var _Rheostat = __webpack_require__(43);

var _Rheostat2 = _interopRequireDefault(_Rheostat);

var _RheostatPit = __webpack_require__(44);

var _RheostatPit2 = _interopRequireDefault(_RheostatPit);

var _Tooltip = __webpack_require__(86);

var _Tooltip2 = _interopRequireDefault(_Tooltip);

var _fixPoints = __webpack_require__(87);

var _fixPoints2 = _interopRequireDefault(_fixPoints);

var _DiffSnapPoints = __webpack_require__(215);

var _emiInterestSlider = __webpack_require__(216);

var _analyticsUtils = __webpack_require__(5);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var EMIInterest = function (_React$Component) {
	_inherits(EMIInterest, _React$Component);

	function EMIInterest(props) {
		_classCallCheck(this, EMIInterest);

		var _this = _possibleConstructorReturn(this, (EMIInterest.__proto__ || Object.getPrototypeOf(EMIInterest)).call(this, props));

		_this.handleSliderChange = function (_ref) {
			var values = _ref.values;
			var updateInterestSlider = _this.props.updateInterestSlider;

			updateInterestSlider({ values: values, userChange: true });
		};

		_this.handleSliderDragMove = function (_ref2) {
			var values = _ref2.values;
			var _this$props = _this.props,
			    slider = _this$props.slider,
			    onSliderDragMove = _this$props.onSliderDragMove;


			if (onSliderDragMove) {
				onSliderDragMove(_extends({}, slider, {
					values: values
				}));
			}
		};

		_this.handleOpen = function () {
			if (gaObj != undefined) {
				(0, _analyticsUtils.triggerGA)(gaObj.name, 'ToolTip_Clicked', 'Interest');
			}
		};

		return _this;
	}

	_createClass(EMIInterest, [{
		key: 'componentWillReceiveProps',
		value: function componentWillReceiveProps(nextProps) {
			if (nextProps.slider.userChange) {
				if (gaObj != undefined) {
					(0, _analyticsUtils.triggerGA)(gaObj.name, 'Interacted_With_EMI_Calculator', 'Interest Slider');
				}
			}
		}
	}, {
		key: 'shouldComponentUpdate',
		value: function shouldComponentUpdate(nextProps, nextState) {
			return nextProps.slider.values != this.props.slider.values;
		}
	}, {
		key: 'render',
		value: function render() {
			var slider = this.props.slider;


			slider = _extends({}, slider, {
				algorithm: {
					getPosition: _fixPoints2.default.getPosition.bind(null, slider.snapPoints),
					getValue: _fixPoints2.default.getValue.bind(null, slider.snapPoints)
				},
				className: 'slider-rheostat',
				pitComponent: _RheostatPit2.default,
				pitPoints: [slider.min, slider.max],
				onChange: this.handleSliderChange,
				onSliderDragMove: this.handleSliderDragMove
			});
			return _react2.default.createElement(
				'div',
				{ className: 'emi-calci-header slider-input-container' },
				_react2.default.createElement(
					'span',
					{ className: 'slider__unit-title' },
					'Interest ',
					_react2.default.createElement(
						'span',
						{ className: 'slider__unit-text' },
						'(%)'
					)
				),
				_react2.default.createElement(
					_Tooltip2.default,
					{ onOpen: this.handleOpen, placement: 'top-right', message: 'It is a flat interest rate - the interest rate is calculated on the full loan amount throughout the tenure without considering that monthly EMIs gradually reduce the principal amount' },
					_react2.default.createElement('span', { className: 'slider__info-icon' })
				),
				_react2.default.createElement(
					'div',
					{ className: 'slider-section', ref: 'interestSlider' },
					_react2.default.createElement(_Rheostat2.default, slider)
				)
			);
		}
	}]);

	return EMIInterest;
}(_react2.default.Component);

var mapStateToProps = function mapStateToProps(state) {
	var slider = state.getIn(['Emi', 'VehicleInterest', 'slider']);

	return {
		slider: slider
	};
};

var mapDispatchToProps = function mapDispatchToProps(dispatch, getState) {
	return {
		updateInterestSlider: (0, _redux.bindActionCreators)(_emiInterestSlider.updateInterestSlider, dispatch)
	};
};

module.exports = (0, _reactRedux.connect)(mapStateToProps, mapDispatchToProps)((0, _immutableWrapperContainer.toJS)(EMIInterest));

/***/ }),
/* 215 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
    value: true
});
var createNewSnapPoints = exports.createNewSnapPoints = function createNewSnapPoints(algoObj) {
    var snapPoints = [];

    for (var i = algoObj.startPoint; i <= algoObj.endPoint; i += algoObj.difference) {
        snapPoints.push(i);
    }

    return snapPoints;
};
var snapDifferences = [100, 500, 1000, 2500, 5000, 10000, 15000, 20000, 25000, 50000, 100000];
function adaptiveDifference(low, high, divisions) {
    return snapDifferences[snapDifferences.findIndex(function (x) {
        return x >= (high - low) / divisions;
    })];
}

var createSnapPointsWithBoundaryValues = exports.createSnapPointsWithBoundaryValues = function createSnapPointsWithBoundaryValues(algoObj) {
    var snapPoints = [];
    var difference = adaptiveDifference(algoObj.startPoint, algoObj.endPoint, algoObj.divisions);
    snapPoints.push(algoObj.startPoint);
    var startPoint = Math.round(algoObj.startPoint / difference) * difference;
    if (startPoint < algoObj.startPoint) {
        startPoint += difference;
    }
    for (var i = startPoint; i < algoObj.endPoint; i += difference) {
        snapPoints.push(i);
    }
    snapPoints.push(algoObj.endPoint);
    return snapPoints;
};

/***/ }),
/* 216 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.updateInterestSlider = undefined;

var _emiActionTypes = __webpack_require__(42);

var updateInterestSliderValue = function updateInterestSliderValue(values, userChange) {
  values = values.map(function (x) {
    return parseFloat(x);
  });
  return function (dispatch) {
    dispatch({
      type: _emiActionTypes.emiCalculatorAction.UPDATE_INTEREST_SLIDER_VALUE,
      values: values,
      userChange: userChange
    });
  };
};

var shouldInterestSliderUpdate = function shouldInterestSliderUpdate(values, slider) {
  var update = true;

  if (slider.values[0] === values[0]) {
    update = false;
  }

  return update;
};

var updateInterestSlider = exports.updateInterestSlider = function updateInterestSlider(_ref) {
  var values = _ref.values,
      userChange = _ref.userChange;
  return function (dispatch, getState) {
    var interestSliderMap = getState(['Emi', 'VehicleInterest', 'slider']);
    if (shouldInterestSliderUpdate(values, interestSliderMap)) {
      dispatch(updateInterestSliderValue(values, userChange));
    }
  };
};

/***/ }),
/* 217 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.EmiCalculation = EmiCalculation;
exports.InterestPayable = InterestPayable;
exports.TotalPrincipalAmt = TotalPrincipalAmt;
function EmiCalculation(loanAmount, tenure, rateOfInterest) {
  var monthlyInterestRatio = rateOfInterest / 1200;
  return parseInt(loanAmount * monthlyInterestRatio / (1 - Math.pow(1 + monthlyInterestRatio, -1 * tenure)));
}

function InterestPayable(loanAmount, tenure, interest) {
  return parseFloat(EmiCalculation(loanAmount, tenure, interest) * tenure) - loanAmount;
}

function TotalPrincipalAmt(downPayment, loanAmount, tenure, interest) {
  var intPayable = InterestPayable(loanAmount, tenure, interest);
  return parseInt(downPayment + loanAmount + intPayable);
}

/***/ }),
/* 218 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

var _reactRedux = __webpack_require__(12);

var _redux = __webpack_require__(10);

var _immutableWrapperContainer = __webpack_require__(9);

var _EmiPieChart = __webpack_require__(219);

var _EmiPieChart2 = _interopRequireDefault(_EmiPieChart);

var _formatAmount = __webpack_require__(15);

var _EMICalculatorHeader = __webpack_require__(84);

var _EMICalculatorHeader2 = _interopRequireDefault(_EMICalculatorHeader);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var PieBreakUp = function (_React$Component) {
	_inherits(PieBreakUp, _React$Component);

	function PieBreakUp(props) {
		_classCallCheck(this, PieBreakUp);

		return _possibleConstructorReturn(this, (PieBreakUp.__proto__ || Object.getPrototypeOf(PieBreakUp)).call(this, props));
	}

	_createClass(PieBreakUp, [{
		key: 'render',
		value: function render() {
			var _props = this.props,
			    onRoadPrice = _props.onRoadPrice,
			    downpayment = _props.downpayment,
			    interestPayable = _props.interestPayable,
			    tenure = _props.tenure,
			    pieChartData = _props.pieChartData;


			var loanAmount = parseFloat(onRoadPrice - downpayment);
			var pricipalLoan = (0, _formatAmount.formatToINR)(loanAmount);
			var pieInterestAmt = (0, _formatAmount.formatToINR)(interestPayable);

			return _react2.default.createElement(
				'div',
				null,
				_react2.default.createElement(
					'div',
					{ className: 'pie-breakup-data' },
					_react2.default.createElement(
						'div',
						{ className: 'pie-breakup_unit' },
						_react2.default.createElement(
							'div',
							{ className: 'pie-breakup__title' },
							_react2.default.createElement('span', { className: 'pie-breakup_bullet pie-breakup_bullet-interest' }),
							' Total Interest Payable'
						),
						_react2.default.createElement(
							'div',
							{ className: 'pie-breakup__amount' },
							pieInterestAmt
						)
					),
					_react2.default.createElement(
						'div',
						{ className: 'pie-breakup_unit' },
						_react2.default.createElement(
							'div',
							{ className: 'pie-breakup__title' },
							_react2.default.createElement('span', { className: 'pie-breakup_bullet pie-breakup_bullet-loan' }),
							' Principle Loan Amount'
						),
						_react2.default.createElement(
							'div',
							{ className: 'pie-breakup__amount' },
							pricipalLoan
						)
					),
					_react2.default.createElement(
						'div',
						null,
						_react2.default.createElement(
							'div',
							{ className: 'pie-breakup__title' },
							_react2.default.createElement('span', { className: 'pie-breakup__bullet-tenure' }),
							' Tenure',
							_react2.default.createElement(
								'span',
								{ className: 'pie-breakup_tenure' },
								parseInt(tenure),
								' Months'
							)
						)
					)
				),
				_react2.default.createElement(
					'div',
					{ className: 'pie-breakup-graph' },
					_react2.default.createElement(_EmiPieChart2.default, { pieChartData: pieChartData })
				)
			);
		}
	}]);

	return PieBreakUp;
}(_react2.default.Component);

module.exports = PieBreakUp;

/***/ }),
/* 219 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
	value: true
});

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

var _reactRedux = __webpack_require__(12);

var _redux = __webpack_require__(10);

var _immutableWrapperContainer = __webpack_require__(9);

var _formatAmount = __webpack_require__(15);

var _reactMinimalPieChart = __webpack_require__(220);

var _reactMinimalPieChart2 = _interopRequireDefault(_reactMinimalPieChart);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var EmiPieChart = function (_React$Component) {
	_inherits(EmiPieChart, _React$Component);

	function EmiPieChart(props) {
		_classCallCheck(this, EmiPieChart);

		var _this = _possibleConstructorReturn(this, (EmiPieChart.__proto__ || Object.getPrototypeOf(EmiPieChart)).call(this, props));

		_this.state = {
			intStatePay: _this.props.pieChartData.intStatePay,
			pieLoanAmt: _this.props.pieChartData.pieLoanAmt
		};
		return _this;
	}

	_createClass(EmiPieChart, [{
		key: 'componentWillReceiveProps',
		value: function componentWillReceiveProps(nextProps) {
			this.setState({
				intStatePay: nextProps.pieChartData.intStatePay,
				pieLoanAmt: nextProps.pieChartData.pieLoanAmt
			});
		}
	}, {
		key: 'render',
		value: function render() {
			var pieData = [{
				value: this.state.pieLoanAmt,
				color: '#fed1cd'
			}, {
				value: this.state.intStatePay,
				color: '#f45944'
			}];

			var finalAmountPay = (0, _formatAmount.formatToINR)(this.props.pieChartData.pieTotalPay);
			return _react2.default.createElement(
				'div',
				{ className: 'pie-graph-container' },
				_react2.default.createElement(
					'div',
					{ className: 'pie-graph__total-payment' },
					_react2.default.createElement(
						'div',
						{ className: 'pie-breakup__title' },
						'Total Payment'
					),
					_react2.default.createElement(
						'div',
						{ className: 'pie-center-amount' },
						finalAmountPay
					)
				),
				_react2.default.createElement(_reactMinimalPieChart2.default, {
					data: pieData,
					radius: 48,
					lineWidth: 10,
					startAngle: -90,
					rounded: true
				})
			);
		}
	}]);

	return EmiPieChart;
}(_react2.default.Component);

exports.default = EmiPieChart;

/***/ }),
/* 220 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
Object.defineProperty(__webpack_exports__, "__esModule", { value: true });
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_0_react__ = __webpack_require__(0);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_prop_types__ = __webpack_require__(1);
/* harmony import */ var __WEBPACK_IMPORTED_MODULE_1_prop_types___default = __webpack_require__.n(__WEBPACK_IMPORTED_MODULE_1_prop_types__);



// from http://stackoverflow.com/a/18473154

var partialCircle = function partialCircle(cx, cy, r, start, end) {
	var length = end - start;
	if (length === 0) return [];

	var fromX = r * Math.cos(start) + cx;
	var fromY = r * Math.sin(start) + cy;
	var toX = r * Math.cos(end) + cx;
	var toY = r * Math.sin(end) + cy;
	var large = Math.abs(length) <= Math.PI ? '0' : '1';
	var sweep = length < 0 ? '0' : '1';

	return [['M', fromX, fromY], ['A', r, r, 0, large, sweep, toX, toY]];
};

var svgPartialCircle = partialCircle;

var classCallCheck = function (instance, Constructor) {
  if (!(instance instanceof Constructor)) {
    throw new TypeError("Cannot call a class as a function");
  }
};









var _extends = Object.assign || function (target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i];

    for (var key in source) {
      if (Object.prototype.hasOwnProperty.call(source, key)) {
        target[key] = source[key];
      }
    }
  }

  return target;
};



var inherits = function (subClass, superClass) {
  if (typeof superClass !== "function" && superClass !== null) {
    throw new TypeError("Super expression must either be null or a function, not " + typeof superClass);
  }

  subClass.prototype = Object.create(superClass && superClass.prototype, {
    constructor: {
      value: subClass,
      enumerable: false,
      writable: true,
      configurable: true
    }
  });
  if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass;
};









var objectWithoutProperties = function (obj, keys) {
  var target = {};

  for (var i in obj) {
    if (keys.indexOf(i) >= 0) continue;
    if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;
    target[i] = obj[i];
  }

  return target;
};

var possibleConstructorReturn = function (self, call) {
  if (!self) {
    throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
  }

  return call && (typeof call === "object" || typeof call === "function") ? call : self;
};

var PI = Math.PI;
var degreesToRadians = function degreesToRadians(degrees) {
  return degrees * PI / 180;
};

var makePathCommands = function makePathCommands(cx, cy, startAngle, lengthAngle, radius) {
  var patchedLengthAngle = lengthAngle;

  if (patchedLengthAngle >= 360) patchedLengthAngle = 359.999;
  if (patchedLengthAngle <= -360) patchedLengthAngle = -359.999;

  return svgPartialCircle(cx, cy, // center X and Y
  radius, degreesToRadians(startAngle), degreesToRadians(startAngle + patchedLengthAngle)).map(function (command) {
    return command.join(' ');
  }).join(' ');
};

function ReactMinimalPieChartPath(_ref) {
  var cx = _ref.cx,
      cy = _ref.cy,
      startAngle = _ref.startAngle,
      lengthAngle = _ref.lengthAngle,
      radius = _ref.radius,
      lineWidth = _ref.lineWidth,
      reveal = _ref.reveal,
      props = objectWithoutProperties(_ref, ['cx', 'cy', 'startAngle', 'lengthAngle', 'radius', 'lineWidth', 'reveal']);

  var actualRadio = radius - lineWidth / 2;
  var pathCommands = makePathCommands(cx, cy, startAngle, lengthAngle, actualRadio);
  var strokeDasharray = void 0;
  var strokeDashoffset = void 0;

  // Animate/hide paths with "stroke-dasharray" + "stroke-dashoffset"
  // https://css-tricks.com/svg-line-animation-works/
  if (typeof reveal === 'number') {
    strokeDasharray = PI * actualRadio / 180 * Math.abs(lengthAngle);
    strokeDashoffset = strokeDasharray + strokeDasharray / 100 * reveal;
  }

  return __WEBPACK_IMPORTED_MODULE_0_react__["default"].createElement('path', _extends({
    d: pathCommands,
    strokeWidth: lineWidth,
    strokeDasharray: strokeDasharray,
    strokeDashoffset: strokeDashoffset
  }, props));
}

ReactMinimalPieChartPath.displayName = 'ReactMinimalPieChartPath';

ReactMinimalPieChartPath.propTypes = {
  cx: __WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.number.isRequired,
  cy: __WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.number.isRequired,
  startAngle: __WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.number,
  lengthAngle: __WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.number,
  radius: __WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.number,
  lineWidth: __WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.number,
  reveal: __WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.number
};

ReactMinimalPieChartPath.defaultProps = {
  startAngle: 0,
  lengthAngle: 0,
  lineWidth: 100,
  radius: 100
};

var VIEWBOX_SIZE = 100;
var VIEWBOX_HALF_SIZE = VIEWBOX_SIZE / 2;

var sumValues = function sumValues(data) {
  return data.reduce(function (acc, dataEntry) {
    return acc + dataEntry.value;
  }, 0);
};

var evaluateViewBoxSize = function evaluateViewBoxSize(ratio, baseSize) {
  // Wide ratio
  if (ratio > 1) {
    return baseSize + ' ' + baseSize / ratio;
  }
  // Narrow/squared ratio
  return baseSize * ratio + ' ' + baseSize;
};

// @TODO extract padding evaluation
var evaluateDegreesFromValues = function evaluateDegreesFromValues(data, totalAngle, totalValue, paddingAngle) {
  var total = totalValue || sumValues(data);

  // Remove segments padding from total degrees
  var degreesTakenByPadding = paddingAngle * data.length;
  var totalDegrees = Math.abs(totalAngle) - degreesTakenByPadding;

  if (totalDegrees > 360) totalDegrees = 360;
  if (totalAngle < 0) totalDegrees = -totalDegrees;

  // Append "degrees" into each data entry
  return data.map(function (dataEntry) {
    return Object.assign({ degrees: dataEntry.value / total * totalDegrees }, dataEntry);
  });
};

var makeSegmentTransitionStyle = function makeSegmentTransitionStyle(duration, easing) {
  return {
    transition: 'stroke-dashoffset ' + duration + 'ms ' + easing
  };
};

var makeSegments = function makeSegments(data, props, hide) {
  // Keep track of how many degrees have already been taken
  var lastSegmentAngle = props.startAngle;
  var segmentsPaddingAngle = props.paddingAngle * (props.lengthAngle / Math.abs(props.lengthAngle));
  var reveal = void 0;

  var style = props.animate ? makeSegmentTransitionStyle(props.animationDuration, props.animationEasing) : undefined;

  // Hide/reveal the segment?
  if (hide === true) {
    reveal = 0;
  } else if (typeof props.reveal === 'number') {
    reveal = props.reveal;
  } else if (hide === false) {
    reveal = 100;
  }

  return data.map(function (dataEntry, index) {
    var startAngle = lastSegmentAngle;
    lastSegmentAngle += dataEntry.degrees + segmentsPaddingAngle;

    return __WEBPACK_IMPORTED_MODULE_0_react__["default"].createElement(ReactMinimalPieChartPath, {
      key: dataEntry.key || index,
      cx: props.cx,
      cy: props.cy,
      startAngle: startAngle,
      lengthAngle: dataEntry.degrees,
      radius: props.radius,
      lineWidth: props.radius / 100 * props.lineWidth,
      reveal: reveal,
      style: style,
      stroke: dataEntry.color,
      strokeLinecap: props.rounded ? 'round' : undefined,
      fill: 'none'
    });
  });
};

var ReactMinimalPieChart = function (_PureComponent) {
  inherits(ReactMinimalPieChart, _PureComponent);

  function ReactMinimalPieChart(props) {
    classCallCheck(this, ReactMinimalPieChart);

    var _this = possibleConstructorReturn(this, _PureComponent.call(this, props));

    if (_this.props.animate === true) {
      _this.hideSegments = true;
    }
    return _this;
  }

  ReactMinimalPieChart.prototype.componentDidMount = function componentDidMount() {
    var _this2 = this;

    if (this.props.animate === true && requestAnimationFrame) {
      this.initialAnimationTimerId = setTimeout(function () {
        _this2.initialAnimationTimerId = null;
        _this2.initialAnimationRAFId = requestAnimationFrame(function () {
          _this2.initialAnimationRAFId = null, _this2.startAnimation();
        });
      });
    }
  };

  ReactMinimalPieChart.prototype.componentWillUnmount = function componentWillUnmount() {
    if (this.initialAnimationTimerId) {
      clearTimeout(this.initialAnimationTimerId);
    }
    if (this.initialAnimationRAFId) {
      cancelAnimationFrame(this.initialAnimationRAFId);
    }
  };

  ReactMinimalPieChart.prototype.startAnimation = function startAnimation() {
    this.hideSegments = false;
    this.forceUpdate();
  };

  ReactMinimalPieChart.prototype.render = function render() {
    if (this.props.data === undefined) {
      return null;
    }

    var normalizedData = evaluateDegreesFromValues(this.props.data, this.props.lengthAngle, this.props.totalValue, this.props.paddingAngle);

    return __WEBPACK_IMPORTED_MODULE_0_react__["default"].createElement(
      'div',
      {
        className: this.props.className,
        style: this.props.style
      },
      __WEBPACK_IMPORTED_MODULE_0_react__["default"].createElement(
        'svg',
        {
          viewBox: '0 0 ' + evaluateViewBoxSize(this.props.ratio, VIEWBOX_SIZE),
          width: '100%',
          height: '100%',
          style: { display: 'block' }
        },
        makeSegments(normalizedData, this.props, this.hideSegments)
      ),
      this.props.children
    );
  };

  return ReactMinimalPieChart;
}(__WEBPACK_IMPORTED_MODULE_0_react__["PureComponent"]);

ReactMinimalPieChart.displayName = 'ReactMinimalPieChart';

ReactMinimalPieChart.propTypes = {
  data: __WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.arrayOf(__WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.shape({
    value: __WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.number.isRequired,
    key: __WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.oneOfType([__WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.number, __WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.string]),
    color: __WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.string
  })),
  cx: __WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.number,
  cy: __WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.number,
  ratio: __WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.number,
  totalValue: __WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.number,
  style: __WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.objectOf(__WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.oneOfType([__WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.number, __WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.string])),
  startAngle: __WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.number,
  lengthAngle: __WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.number,
  paddingAngle: __WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.number,
  lineWidth: __WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.number,
  radius: __WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.number,
  rounded: __WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.bool,
  animate: __WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.bool,
  animationDuration: __WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.number,
  animationEasing: __WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.string,
  reveal: __WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.number,
  children: __WEBPACK_IMPORTED_MODULE_1_prop_types___default.a.node
};

ReactMinimalPieChart.defaultProps = {
  cx: VIEWBOX_HALF_SIZE,
  cy: VIEWBOX_HALF_SIZE,
  ratio: 1,
  startAngle: 0,
  lengthAngle: 360,
  paddingAngle: 0,
  lineWidth: 100,
  radius: VIEWBOX_HALF_SIZE,
  rounded: false,
  animate: false,
  animationDuration: 500,
  animationEasing: 'ease-out'
};

/* harmony default export */ __webpack_exports__["default"] = (ReactMinimalPieChart);
//# sourceMappingURL=index.js.map


/***/ }),
/* 221 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

var _Autocomplete = __webpack_require__(222);

var _Autocomplete2 = _interopRequireDefault(_Autocomplete);

var _PopularCityList = __webpack_require__(223);

var _PopularCityList2 = _interopRequireDefault(_PopularCityList);

var _ListGroup = __webpack_require__(225);

var _ListGroup2 = _interopRequireDefault(_ListGroup);

var _ListGroupItem = __webpack_require__(226);

var _ListGroupItem2 = _interopRequireDefault(_ListGroupItem);

var _NoResult = __webpack_require__(80);

var _NoResult2 = _interopRequireDefault(_NoResult);

var _SpinnerRelative = __webpack_require__(8);

var _SpinnerRelative2 = _interopRequireDefault(_SpinnerRelative);

var _scrollLock = __webpack_require__(37);

var _popUpUtils = __webpack_require__(11);

var _popupScroll = __webpack_require__(81);

var _analyticsUtils = __webpack_require__(5);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _toConsumableArray(arr) { if (Array.isArray(arr)) { for (var i = 0, arr2 = Array(arr.length); i < arr.length; i++) { arr2[i] = arr[i]; } return arr2; } else { return Array.from(arr); } }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var SelectCityPopup = function (_React$Component) {
  _inherits(SelectCityPopup, _React$Component);

  function SelectCityPopup(props) {
    _classCallCheck(this, SelectCityPopup);

    var _this = _possibleConstructorReturn(this, (SelectCityPopup.__proto__ || Object.getPrototypeOf(SelectCityPopup)).call(this, props));

    _initialiseProps.call(_this);

    _this.handleCloseClick = _this.handleCloseClick.bind(_this);
    _this.handleCityClick = _this.handleCityClick.bind(_this);
    _this.handleClearClick = _this.handleClearClick.bind(_this);
    _this.getOtherCityList = _this.getOtherCityList.bind(_this);
    _this.filterCityList = _this.filterCityList.bind(_this);
    _this.closePopup = _this.closePopup.bind(_this);
    var cityName = _this.getCityName(_this.props);
    _this.state = {
      Popular: _this.props.data.Popular,
      Other: _this.props.data.Other,
      cityValue: cityName
    };
    return _this;
  }

  _createClass(SelectCityPopup, [{
    key: 'componentWillReceiveProps',
    value: function componentWillReceiveProps(nextProps) {
      if (nextProps.data.Popular != this.state.Popular || nextProps.data.Other != this.state.Other) {
        var cityName = this.getCityName(nextProps);
        this.setState({
          Popular: nextProps.data.Popular,
          Other: nextProps.data.Other,
          cityValue: cityName
        });
      }
    }
  }, {
    key: 'componentDidMount',
    value: function componentDidMount() {
      (0, _popupScroll.addPopupEvents)(this.popupContent);
    }
  }, {
    key: 'componentWillUnmount',
    value: function componentWillUnmount() {
      (0, _popupScroll.removePopupEvents)(this.popupContent);
    }
  }, {
    key: 'shouldComponentUpdate',
    value: function shouldComponentUpdate(nextProps, nextState) {
      if (nextProps.isActive === this.props.isActive && nextProps.data.RelatedModelId == this.props.data.RelatedModelId && nextState.cityValue == this.state.cityValue) {
        return false;
      }

      if (nextProps.isActive) {
        this.popupContent.scrollTop = this.popupScrollPosition;
      }

      return true;
    }
  }, {
    key: 'render',
    value: function render() {
      var _props = this.props,
          isActive = _props.isActive,
          data = _props.data;


      var popupActiveClassName = isActive ? 'popup--active' : '';
      var popupClasses = 'select-city-popup popup-content ' + popupActiveClassName;
      var result = void 0;
      if (!data.IsFetching) {
        if (this.state.Popular.length > 0 || this.state.Other.length > 0) {
          result = _react2.default.createElement(
            'div',
            { className: 'select-city__body' },
            this.state.Popular.length > 0 && _react2.default.createElement(
              'div',
              { className: 'city-list-content' },
              _react2.default.createElement(
                'p',
                { className: 'city-list__heading' },
                'Popular cities'
              ),
              _react2.default.createElement(_PopularCityList2.default, {
                data: this.state.Popular,
                selection: data.Selection,
                onClick: this.handleCityClick
              })
            ),
            this.state.Other.length > 0 && _react2.default.createElement(
              'div',
              { className: 'city-list-content' },
              _react2.default.createElement(
                'p',
                { className: 'city-list__heading' },
                'Other cities'
              ),
              this.getOtherCityList()
            )
          );
        } else {
          result = _react2.default.createElement(_NoResult2.default, {
            type: 'select-bike__no-bike-content',
            imageClass: 'select-city__no-city',
            title: 'No Matching Cities Found'
          });
        }
      } else {
        result = _react2.default.createElement(_SpinnerRelative2.default, null);
      }
      return _react2.default.createElement(
        'div',
        { className: popupClasses, id: 'selectcity-popup' },
        _react2.default.createElement(
          'div',
          { ref: this.setContentRef, className: 'select-city-popup__content' },
          _react2.default.createElement(
            'div',
            { className: 'popup__head' },
            _react2.default.createElement(
              'div',
              { className: 'popup-head__content' },
              _react2.default.createElement('span', { onClick: this.handleCloseClick, className: 'popup__close' }),
              _react2.default.createElement(
                'div',
                { className: 'popup__search-box' },
                _react2.default.createElement(
                  'p',
                  { className: 'popup-search__title' },
                  'Select City'
                ),
                _react2.default.createElement(
                  'div',
                  { className: 'autocomplete-box' },
                  _react2.default.createElement(
                    'div',
                    { className: 'autocomplete-field' },
                    _react2.default.createElement('input', { type: 'text', value: this.state.cityValue, className: 'form-control', placeholder: 'Type to select city', id: 'selectcity-popup', onChange: this.filterCityList }),
                    data.Selection && data.Selection.cityId > 0 ? _react2.default.createElement(
                      'span',
                      { onClick: this.handleClearClick, className: 'autocomplete-box__clear' },
                      'Clear'
                    ) : null // <span className="select-city__locate-me"></span> Future implementation

                  )
                )
              )
            )
          ),
          result
        )
      );
    }
  }]);

  return SelectCityPopup;
}(_react2.default.Component);

var _initialiseProps = function _initialiseProps() {
  var _this2 = this;

  this.getCityName = function (props) {
    return props != null && props.data != null && props.data.Selection && props.data.isGlobalCityInList ? props.data.Selection.cityName : '';
  };

  this.filterCityList = function (event) {
    var updatedPopular = _this2.props.data.Popular;
    var updatedOther = _this2.props.data.Other;
    updatedPopular = updatedPopular.filter(function (item) {
      return item.cityName.toLowerCase().indexOf(event.target.value.toLowerCase()) !== -1;
    });
    updatedOther = updatedOther.filter(function (item) {
      return item.cityName.toLowerCase().indexOf(event.target.value.toLowerCase()) !== -1;
    });
    _this2.setState({
      cityValue: event.currentTarget.value,
      Popular: updatedPopular,
      Other: updatedOther
    }, function () {
      (0, _popupScroll.focusPopupContent)(_this2.popupContent);
    });
  };

  this.closePopup = function () {
    _this2.popupScrollPosition = _this2.popupContent.scrollTop;

    if (_this2.props.onCloseClick) {
      (0, _popUpUtils.closePopupWithHash)(_this2.props.onCloseClick);
    }
  };

  this.handleCityClick = function (item) {
    if (_this2.props.onCityClick) {
      _this2.props.onCityClick(item);
      if (gaObj != undefined) {
        (0, _analyticsUtils.triggerGA)(gaObj.name, 'City_Selected', item.cityName + '_' + _this2.state.cityValue);
      }
      _this2.setState.apply(_this2, _toConsumableArray(_this2.state).concat([{
        cityValue: item.cityName
      }]));
      if (_this2.props.data.Selection.cityId > 0) {
        if (gaObj != undefined) {
          (0, _analyticsUtils.triggerGA)(gaObj.name, 'City_Selected_On_Edit_Flow', 'Existing City - ' + _this2.props.data.Selection.cityName);
        }
      }
    }
    _this2.closePopup();
  };

  this.handleCloseClick = function () {
    if (gaObj != undefined) {
      (0, _analyticsUtils.triggerGA)(gaObj.name, 'City_Popup_Cross_Clicked', _this2.state.cityValue);
    }
    _this2.closePopup();
  };

  this.handleClearClick = function () {
    _this2.setState({
      cityValue: '',
      Popular: _this2.props.data.Popular,
      Other: _this2.props.data.Other
    });
  };

  this.getOtherCityList = function () {
    var data = _this2.props.data;


    var listItems = _this2.state.Other.map(function (item, index) {
      var active = data.Selection.cityId === item.cityId;

      return _react2.default.createElement(_ListGroupItem2.default, {
        key: item.cityId,
        id: item.cityId,
        name: item.cityName,
        active: active,
        onClick: _this2.handleCityClick.bind(_this2, item)
      });
    });

    return _react2.default.createElement(
      _ListGroup2.default,
      { type: 'other-city__list' },
      listItems
    );
  };

  this.setContentRef = function (ref) {
    _this2.popupContent = ref;
  };
};

exports.default = SelectCityPopup;

/***/ }),
/* 222 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var Autocomplete = function (_React$Component) {
	_inherits(Autocomplete, _React$Component);

	function Autocomplete(props) {
		_classCallCheck(this, Autocomplete);

		return _possibleConstructorReturn(this, (Autocomplete.__proto__ || Object.getPrototypeOf(Autocomplete)).call(this, props));
	}

	_createClass(Autocomplete, [{
		key: 'render',
		value: function render() {
			var _this2 = this;

			var inputProps = {};
			if (this.props.inputProps) {
				inputProps = this.props.inputProps;
			}

			return _react2.default.createElement(
				'div',
				{ style: this.props.wrapperStyle },
				_react2.default.createElement('input', { type: 'text',
					id: inputProps.id ? inputProps.id : '',
					placeholder: inputProps.placeholder ? inputProps.placeholder : '',
					className: inputProps.className ? inputProps.className : '',
					value: this.props.value ? this.props.value : '',
					onfocus: function onfocus() {
						typeof _this2.props.onClick == 'function' ? _this2.props.onClick() : null;
					},
					onchange: function onchange() {
						typeof _this2.props.onChange == 'function' ? _this2.props.onChange() : null;
					} }),
				typeof this.props.renderMenu == 'function' ? this.props.renderMenu(this.props.items, this.props.value) : null
			);
		}
	}]);

	return Autocomplete;
}(_react2.default.Component);

module.exports = Autocomplete;

/***/ }),
/* 223 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

var _propTypes = __webpack_require__(1);

var _propTypes2 = _interopRequireDefault(_propTypes);

var _ListItem = __webpack_require__(224);

var _ListItem2 = _interopRequireDefault(_ListItem);

var _constants = __webpack_require__(4);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var propTypes = {
  // list type
  type: _propTypes2.default.string
};

var defaultProps = {
  type: ''
};

var PopularCityList = function (_React$Component) {
  _inherits(PopularCityList, _React$Component);

  function PopularCityList(props) {
    _classCallCheck(this, PopularCityList);

    var _this = _possibleConstructorReturn(this, (PopularCityList.__proto__ || Object.getPrototypeOf(PopularCityList)).call(this, props));

    _this.handleCityClick = function (item) {
      if (_this.props.onClick) {
        _this.props.onClick(item);
      }
    };

    _this.getList = function () {
      var _this$props = _this.props,
          data = _this$props.data,
          selection = _this$props.selection;


      var listItems = data.map(function (item, index) {
        var element = {
          name: item.cityName,
          icon: _constants.HOST_URL + '0x0/bw/static/icons/city/' + item.cityId + '.svg'
        };

        var activeClass = '';
        if (selection.cityId === item.cityId) {
          activeClass = 'city--active ' + (selection.userChange ? "city--select" : "city--global");
        }

        return _react2.default.createElement(
          'li',
          {
            key: item.cityId,
            className: "city-popular-list__item " + activeClass,
            onClick: _this.handleCityClick.bind(_this, item)
          },
          _react2.default.createElement(_ListItem2.default, { item: element })
        );
      });

      return listItems;
    };

    return _this;
  }

  _createClass(PopularCityList, [{
    key: 'render',
    value: function render() {
      return _react2.default.createElement(
        'ul',
        { className: "city-popular__list " + this.props.type },
        this.getList()
      );
    }
  }]);

  return PopularCityList;
}(_react2.default.Component);

PopularCityList.propTypes = propTypes;
PopularCityList.defaultProps = defaultProps;

exports.default = PopularCityList;

/***/ }),
/* 224 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

var _propTypes = __webpack_require__(1);

var _propTypes2 = _interopRequireDefault(_propTypes);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var propTypes = {
  // data object
  item: _propTypes2.default.object
};

var defaultProps = {
  showDescription: false
};

var ListItem = function (_React$PureComponent) {
  _inherits(ListItem, _React$PureComponent);

  function ListItem(props) {
    _classCallCheck(this, ListItem);

    return _possibleConstructorReturn(this, (ListItem.__proto__ || Object.getPrototypeOf(ListItem)).call(this, props));
  }

  _createClass(ListItem, [{
    key: 'render',
    value: function render() {
      var _props = this.props,
          item = _props.item,
          showDescription = _props.showDescription;

      return _react2.default.createElement(
        'div',
        null,
        item.icon ? _react2.default.createElement(
          'div',
          { className: 'item__icon-content' },
          _react2.default.createElement(
            'div',
            { className: 'item-icon__image-content' },
            _react2.default.createElement('img', {
              className: 'item-icon-image',
              src: item.icon,
              alt: item.name
            })
          )
        ) : '',
        item.name ? _react2.default.createElement(
          'p',
          { className: 'item__heading' },
          item.name
        ) : '',
        item.description && showDescription ? _react2.default.createElement(
          'div',
          { className: 'item__description' },
          item.description
        ) : ''
      );
    }
  }]);

  return ListItem;
}(_react2.default.PureComponent);

ListItem.propTypes = propTypes;
ListItem.defaultProps = defaultProps;

exports.default = ListItem;

/***/ }),
/* 225 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

var _propTypes = __webpack_require__(1);

var _propTypes2 = _interopRequireDefault(_propTypes);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var propTypes = {
  // list type
  type: _propTypes2.default.string
};

var defaultProps = {
  type: ''
};

var ListGroup = function (_React$Component) {
  _inherits(ListGroup, _React$Component);

  function ListGroup(props) {
    _classCallCheck(this, ListGroup);

    return _possibleConstructorReturn(this, (ListGroup.__proto__ || Object.getPrototypeOf(ListGroup)).call(this, props));
  }

  _createClass(ListGroup, [{
    key: 'render',
    value: function render() {
      return _react2.default.createElement(
        'ul',
        { className: "list__group " + this.props.type },
        this.props.children
      );
    }
  }]);

  return ListGroup;
}(_react2.default.Component);

ListGroup.propTypes = propTypes;
ListGroup.defaultProps = defaultProps;

exports.default = ListGroup;

/***/ }),
/* 226 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

var _propTypes = __webpack_require__(1);

var _propTypes2 = _interopRequireDefault(_propTypes);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var propTypes = {
  // element id
  id: _propTypes2.default.number,
  // element name
  name: _propTypes2.default.string,
  // element active-inactive status
  active: _propTypes2.default.bool,
  // custom click handler
  onClick: _propTypes2.default.func
};

var defaultProps = {
  id: null,
  name: null,
  active: false
};

var ListGroupItem = function (_React$PureComponent) {
  _inherits(ListGroupItem, _React$PureComponent);

  function ListGroupItem(props) {
    _classCallCheck(this, ListGroupItem);

    var _this = _possibleConstructorReturn(this, (ListGroupItem.__proto__ || Object.getPrototypeOf(ListGroupItem)).call(this, props));

    _this.handleClick = _this.handleClick.bind(_this);
    return _this;
  }

  _createClass(ListGroupItem, [{
    key: 'handleClick',
    value: function handleClick() {
      if (this.props.onClick) {
        this.props.onClick();
      }
    }
  }, {
    key: 'render',
    value: function render() {
      var activeClass = this.props.active ? "list-group-item--active" : "";

      return _react2.default.createElement(
        'li',
        {
          key: this.props.id,
          className: "list-group__item " + activeClass,
          onClick: this.handleClick
        },
        _react2.default.createElement(
          'p',
          { className: 'list-group-item__title' },
          this.props.name
        )
      );
    }
  }]);

  return ListGroupItem;
}(_react2.default.PureComponent);

ListGroupItem.propTypes = propTypes;
ListGroupItem.defaultProps = defaultProps;

exports.default = ListGroupItem;

/***/ }),
/* 227 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});

var _reactRedux = __webpack_require__(12);

var _redux = __webpack_require__(10);

var _immutableWrapperContainer = __webpack_require__(9);

var _ModelInfoComponent = __webpack_require__(228);

var _ModelInfoComponent2 = _interopRequireDefault(_ModelInfoComponent);

var _SelectBikePopup = __webpack_require__(75);

var _FinanceCityPopup = __webpack_require__(76);

var _FinanceCityPopup2 = __webpack_require__(230);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var mapStateToProps = function mapStateToProps(store) {
  return {
    isLoaderShown: store.getIn(['Finance', 'SelectBikePopup', 'IsFetchingModelDetail']) || store.getIn(['Finance', 'FinanceCityPopup', 'IsFetching']),
    model: store.getIn(['Finance', 'SelectBikePopup', 'Selection']),
    city: store.getIn(['Finance', 'FinanceCityPopup', 'Selection'])
  };
};

var mapDispatchToProps = function mapDispatchToProps(dispatch) {
  return {
    openSelectBikePopup: (0, _redux.bindActionCreators)(_SelectBikePopup.openSelectBikePopup, dispatch),
    openSelectCityPopup: (0, _redux.bindActionCreators)(_FinanceCityPopup.openSelectCityPopup, dispatch),
    closeSelectBikePopup: (0, _redux.bindActionCreators)(_SelectBikePopup.closeSelectBikePopup, dispatch),
    closeSelectCityPopup: (0, _redux.bindActionCreators)(_FinanceCityPopup.closeSelectCityPopup, dispatch),
    selectBikeVersion: (0, _redux.bindActionCreators)(_SelectBikePopup.selectBikeVersion, dispatch)
  };
};

exports.default = (0, _reactRedux.connect)(mapStateToProps, mapDispatchToProps)((0, _immutableWrapperContainer.toJS)(_ModelInfoComponent2.default));

/***/ }),
/* 228 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

var _reactDropdown = __webpack_require__(229);

var _reactDropdown2 = _interopRequireDefault(_reactDropdown);

var _SpinnerRelative = __webpack_require__(8);

var _SpinnerRelative2 = _interopRequireDefault(_SpinnerRelative);

var _popUpUtils = __webpack_require__(11);

var _WidgetsCommon = __webpack_require__(30);

var _analyticsUtils = __webpack_require__(5);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var ModelInfo = function (_React$Component) {
  _inherits(ModelInfo, _React$Component);

  function ModelInfo(props) {
    _classCallCheck(this, ModelInfo);

    var _this = _possibleConstructorReturn(this, (ModelInfo.__proto__ || Object.getPrototypeOf(ModelInfo)).call(this, props));

    _this.handleVersionChange = function (option) {
      _this.props.selectBikeVersion(option.value);
    };

    _this.handleModelPopupClick = function () {
      if (gaObj != undefined) {
        (0, _analyticsUtils.triggerGA)(gaObj.name, 'Model_Change_Initiated', 'Existing Model - ' + _this.props.model.modelName);
      }
      (0, _popUpUtils.openPopupWithHash)(_this.props.openSelectBikePopup, _this.props.closeSelectBikePopup, "SelectBike");
    };

    _this.handelCityPopupClick = function () {
      if (gaObj != undefined) {
        (0, _analyticsUtils.triggerGA)(gaObj.name, 'City_Change_Initiated', 'Existing City - ' + _this.props.city.cityName);
      }
      (0, _popUpUtils.openPopupWithHash)(_this.props.openSelectCityPopup, _this.props.closeSelectCityPopup, "SelectCity");
    };

    _this.handleVersionChange = _this.handleVersionChange.bind(_this);
    _this.handleModelPopupClick = _this.handleModelPopupClick.bind(_this);
    _this.handelCityPopupClick = _this.handelCityPopupClick.bind(_this);
    return _this;
  }

  _createClass(ModelInfo, [{
    key: 'render',
    value: function render() {
      var _props = this.props,
          model = _props.model,
          city = _props.city,
          isLoaderShown = _props.isLoaderShown;


      if (!model) {
        return null;
      }
      if (isLoaderShown) {
        return _react2.default.createElement(_SpinnerRelative2.default, null);
      }
      var imageHostUrl = model.version.versionList != null && model.version.versionList.length > 0 && model.version.selectedVersionIndex > -1 ? model.version.versionList[model.version.selectedVersionIndex].hostUrl : model.hostUrl;
      var originalImagePath = model.version.versionList != null && model.version.versionList.length > 0 && model.version.selectedVersionIndex > -1 ? model.version.versionList[model.version.selectedVersionIndex].originalImagePath : model.originalImagePath;
      return _react2.default.createElement(
        'div',
        { className: 'model-info-card' },
        _react2.default.createElement(
          'div',
          { className: 'model-info-card__head' },
          _react2.default.createElement(
            'div',
            { className: 'model-info-card__image' },
            _react2.default.createElement('img', { src: (0, _WidgetsCommon.createImageUrl)(imageHostUrl, originalImagePath, '310x174'), alt: model.modelName })
          ),
          _react2.default.createElement(
            'div',
            { className: 'model-info-card__detail' },
            _react2.default.createElement(
              'h3',
              null,
              _react2.default.createElement(
                'span',
                { className: 'model-info-card__make' },
                model.makeName
              ),
              _react2.default.createElement(
                'span',
                { className: 'model-info-card__model' },
                model.modelName
              )
            )
          ),
          _react2.default.createElement('span', { onClick: this.handleModelPopupClick, className: 'model-info-card__edit' })
        ),
        _react2.default.createElement(
          'div',
          { className: 'model-info-card__body' },
          _react2.default.createElement(
            'div',
            { className: 'model-info__col model-info-col--dropdown' },
            _react2.default.createElement(
              'p',
              { className: 'model-info-col__label' },
              'Version'
            ),
            _react2.default.createElement(_reactDropdown2.default, {
              options: model.version.versionList,
              value: model.version.versionList != null && model.version.versionList.length > 0 && model.version.selectedVersionIndex > -1 ? model.version.versionList[model.version.selectedVersionIndex].label : "",
              placeholder: 'Version',
              placeholderClassName: 'model-info-col__value',
              arrowClassName: 'dropdown-version__arrow',
              onChange: this.handleVersionChange
            })
          ),
          _react2.default.createElement(
            'div',
            { className: 'model-info__col' },
            _react2.default.createElement(
              'p',
              { className: 'model-info-col__label' },
              'City'
            ),
            _react2.default.createElement(
              'p',
              { onClick: this.handelCityPopupClick, className: 'model-info-col__value model-info-col__city' },
              city.cityName
            )
          )
        )
      );
    }
  }]);

  return ModelInfo;
}(_react2.default.Component);

exports.default = ModelInfo;

/***/ }),
/* 229 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

var _reactDom = __webpack_require__(0);

var _reactDom2 = _interopRequireDefault(_reactDom);

var _classnames = __webpack_require__(25);

var _classnames2 = _interopRequireDefault(_classnames);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _defineProperty(obj, key, value) { if (key in obj) { Object.defineProperty(obj, key, { value: value, enumerable: true, configurable: true, writable: true }); } else { obj[key] = value; } return obj; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var DEFAULT_PLACEHOLDER_STRING = 'Select...';

var Dropdown = function (_Component) {
  _inherits(Dropdown, _Component);

  function Dropdown(props) {
    _classCallCheck(this, Dropdown);

    var _this = _possibleConstructorReturn(this, (Dropdown.__proto__ || Object.getPrototypeOf(Dropdown)).call(this, props));

    _this.state = {
      selected: props.value || {
        label: typeof props.placeholder === 'undefined' ? DEFAULT_PLACEHOLDER_STRING : props.placeholder,
        value: ''
      },
      isOpen: false
    };
    _this.mounted = true;
    _this.handleDocumentClick = _this.handleDocumentClick.bind(_this);
    _this.fireChangeEvent = _this.fireChangeEvent.bind(_this);
    return _this;
  }

  _createClass(Dropdown, [{
    key: 'componentWillReceiveProps',
    value: function componentWillReceiveProps(newProps) {
      if (newProps.value && newProps.value !== this.state.selected) {
        this.setState({ selected: newProps.value });
      } else if (!newProps.value) {
        this.setState({ selected: {
            label: typeof newProps.placeholder === 'undefined' ? DEFAULT_PLACEHOLDER_STRING : newProps.placeholder,
            value: ''
          } });
      }
    }
  }, {
    key: 'componentDidMount',
    value: function componentDidMount() {
      document.addEventListener('click', this.handleDocumentClick, false);
      document.addEventListener('touchend', this.handleDocumentClick, false);
    }
  }, {
    key: 'componentWillUnmount',
    value: function componentWillUnmount() {
      this.mounted = false;
      document.removeEventListener('click', this.handleDocumentClick, false);
      document.removeEventListener('touchend', this.handleDocumentClick, false);
    }
  }, {
    key: 'handleMouseDown',
    value: function handleMouseDown(event) {
      if (this.props.onFocus && typeof this.props.onFocus === 'function') {
        this.props.onFocus(this.state.isOpen);
      }
      if (event.type === 'mousedown' && event.button !== 0) return;
      event.stopPropagation();
      event.preventDefault();

      if (!this.props.disabled) {
        this.setState({
          isOpen: !this.state.isOpen
        });
      }
    }
  }, {
    key: 'setValue',
    value: function setValue(value, label) {
      var newState = {
        selected: {
          value: value,
          label: label
        },
        isOpen: false
      };
      this.fireChangeEvent(newState);
      this.setState(newState);
    }
  }, {
    key: 'fireChangeEvent',
    value: function fireChangeEvent(newState) {
      if (newState.selected !== this.state.selected && this.props.onChange) {
        this.props.onChange(newState.selected);
      }
    }
  }, {
    key: 'renderOption',
    value: function renderOption(option) {
      var _classes;

      var classes = (_classes = {}, _defineProperty(_classes, this.props.baseClassName + '-option', true), _defineProperty(_classes, option.className, !!option.className), _defineProperty(_classes, 'is-selected', option === this.state.selected), _classes);

      var optionClass = (0, _classnames2.default)(classes);

      var value = option.value;
      if (typeof value === 'undefined') {
        value = option.label || option;
      }
      var label = option.label || option.value || option;

      return _react2.default.createElement(
        'div',
        {
          key: value,
          className: optionClass,
          onMouseDown: this.setValue.bind(this, value, label),
          onClick: this.setValue.bind(this, value, label) },
        label
      );
    }
  }, {
    key: 'buildMenu',
    value: function buildMenu() {
      var _this2 = this;

      var _props = this.props,
          options = _props.options,
          baseClassName = _props.baseClassName;

      var ops = options.map(function (option) {
        if (option.type === 'group') {
          var groupTitle = _react2.default.createElement(
            'div',
            { className: baseClassName + '-title' },
            option.name
          );
          var _options = option.items.map(function (item) {
            return _this2.renderOption(item);
          });

          return _react2.default.createElement(
            'div',
            { className: baseClassName + '-group', key: option.name },
            groupTitle,
            _options
          );
        } else {
          return _this2.renderOption(option);
        }
      });

      return ops.length ? ops : _react2.default.createElement(
        'div',
        { className: baseClassName + '-noresults' },
        'No options found'
      );
    }
  }, {
    key: 'handleDocumentClick',
    value: function handleDocumentClick(event) {
      if (this.mounted) {
        if (!_reactDom2.default.findDOMNode(this).contains(event.target)) {
          if (this.state.isOpen) {
            this.setState({ isOpen: false });
          }
        }
      }
    }
  }, {
    key: 'render',
    value: function render() {
      var _classNames, _classNames2, _classNames3, _classNames4, _classNames5;

      var _props2 = this.props,
          baseClassName = _props2.baseClassName,
          controlClassName = _props2.controlClassName,
          placeholderClassName = _props2.placeholderClassName,
          menuClassName = _props2.menuClassName,
          arrowClassName = _props2.arrowClassName,
          className = _props2.className;


      var disabledClass = this.props.disabled ? 'Dropdown-disabled' : '';
      var placeHolderValue = typeof this.state.selected === 'string' ? this.state.selected : this.state.selected.label;

      var dropdownClass = (0, _classnames2.default)((_classNames = {}, _defineProperty(_classNames, baseClassName + '-root', true), _defineProperty(_classNames, className, !!className), _defineProperty(_classNames, 'is-open', this.state.isOpen), _classNames));
      var controlClass = (0, _classnames2.default)((_classNames2 = {}, _defineProperty(_classNames2, baseClassName + '-control', true), _defineProperty(_classNames2, controlClassName, !!controlClassName), _defineProperty(_classNames2, disabledClass, !!disabledClass), _classNames2));
      var placeholderClass = (0, _classnames2.default)((_classNames3 = {}, _defineProperty(_classNames3, baseClassName + '-placeholder', true), _defineProperty(_classNames3, placeholderClassName, !!placeholderClassName), _classNames3));
      var menuClass = (0, _classnames2.default)((_classNames4 = {}, _defineProperty(_classNames4, baseClassName + '-menu', true), _defineProperty(_classNames4, menuClassName, !!menuClassName), _classNames4));
      var arrowClass = (0, _classnames2.default)((_classNames5 = {}, _defineProperty(_classNames5, baseClassName + '-arrow', true), _defineProperty(_classNames5, arrowClassName, !!arrowClassName), _classNames5));

      var value = _react2.default.createElement(
        'div',
        { className: placeholderClass },
        placeHolderValue
      );
      var menu = this.state.isOpen ? _react2.default.createElement(
        'div',
        { className: menuClass },
        this.buildMenu()
      ) : null;

      return _react2.default.createElement(
        'div',
        { className: dropdownClass },
        _react2.default.createElement(
          'div',
          { className: controlClass, onMouseDown: this.handleMouseDown.bind(this), onTouchEnd: this.handleMouseDown.bind(this) },
          value,
          _react2.default.createElement('span', { className: arrowClass })
        ),
        menu
      );
    }
  }]);

  return Dropdown;
}(_react.Component);

Dropdown.defaultProps = { baseClassName: 'Dropdown' };
exports.default = Dropdown;


/***/ }),
/* 230 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.FinanceCityPopup = FinanceCityPopup;

var _immutable = __webpack_require__(17);

var _FinanceCityPopup = __webpack_require__(77);

var _popUpUtils = __webpack_require__(11);

var globalCity = (0, _popUpUtils.getGlobalCity)();
var globalCityName = globalCity && globalCity.name && globalCity.name.length > 0 ? globalCity.name : '';
var globalCityId = globalCity && globalCity.id > 0 ? globalCity.id : -1;
var initialState = (0, _immutable.fromJS)({
  isActive: false,
  Selection: {
    cityId: globalCityId,
    cityName: globalCityName,
    userChange: false
  },
  currentGlobalCityName: globalCityName,
  currentGlobalCityId: globalCityId,
  Popular: [],
  Other: [],
  IsFetching: false,
  RelatedModelId: -1,
  CityFetchError: false
});

function FinanceCityPopup() {
  var state = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : initialState;
  var action = arguments[1];

  try {
    if (state == undefined || state != undefined && state.size == 0) return initialState;
    var selectionObj = state.get('Selection').toJS();
    var currentCityId = selectionObj != null && selectionObj.cityId > 0 ? selectionObj.cityId : -1;
    switch (action.type) {
      case _FinanceCityPopup.financeCityPopup.FETCH_CITY_SUCCESS:
        return state.setIn(['Popular'], (0, _immutable.fromJS)(action.payload.cities.filter(function (item) {
          return item.popularityOrder < 7;
        }))).setIn(['Other'], (0, _immutable.fromJS)(action.payload.cities.filter(function (item) {
          return item.popularityOrder > 6;
        }))).setIn(['IsFetching'], false).setIn(['RelatedModelId'], action.payload.modelId);

      case _FinanceCityPopup.financeCityPopup.OPEN_CITY_POPUP:
        return state.setIn(['isActive'], true);

      case _FinanceCityPopup.financeCityPopup.CLOSE_CITY_POPUP:
        return state.setIn(['isActive'], false);

      case _FinanceCityPopup.financeCityPopup.CITY_NEXT:
        return state.setIn(['isActive'], false);

      case _FinanceCityPopup.financeCityPopup.SET_CITY:
        var actionPayload = action.payload;
        var cityId = actionPayload != null && actionPayload.cityId != null ? actionPayload.cityId : -1;
        var cityName = actionPayload != null && actionPayload.cityName != null ? actionPayload.cityName : "";
        var userChange = actionPayload != null && actionPayload.userChange != null ? actionPayload.userChange : false;
        var currentGlobalCityName = cityId > -1 ? cityName : state.get('currentGlobalCityName');
        var currentGlobalCityId = cityId > -1 ? cityId : state.get('currentGlobalCityId');
        if (cityId > -1) {
          (0, _popUpUtils.setGlobalCity)(cityId, cityName, currentCityId);
        }
        return state.setIn(['Selection'], (0, _immutable.fromJS)({
          cityId: cityId,
          cityName: cityName,
          userChange: userChange
        })).setIn(['currentGlobalCityId'], currentGlobalCityId).setIn(['currentGlobalCityName'], currentGlobalCityName);

      case _FinanceCityPopup.financeCityPopup.FETCH_CITY_FAILURE:
        return initialState.setIn(['IsFetching'], false).setIn(['RelatedModelId'], action.payload.modelId).setIn(['CityFetchError'], true);

      case _FinanceCityPopup.financeCityPopup.FETCH_CITY:
        return state.setIn(['IsFetching'], true);

      case _FinanceCityPopup.financeCityPopup.RESET_CITY_FAILURE:
        return state.setIn(['CityFetchError'], false);
      default:
        return state;
    }
  } catch (err) {
    console.log(err);
    return state;
  }
}

/***/ }),
/* 231 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

var _propTypes = __webpack_require__(1);

var _propTypes2 = _interopRequireDefault(_propTypes);

var _SimilarBikesEMI = __webpack_require__(78);

var _SpinnerRelative = __webpack_require__(8);

var _SpinnerRelative2 = _interopRequireDefault(_SpinnerRelative);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var propTypes = {
  // list type
  type: _propTypes2.default.string,
  // heading to show above carousel
  heading: _propTypes2.default.string,
  // carousel data
  data: _propTypes2.default.shape({
    data: _propTypes2.default.array,
    IsFetching: _propTypes2.default.bool
  }),
  // custom carousel card component
  carouselCard: _propTypes2.default.oneOfType([_propTypes2.default.func, _propTypes2.default.string])
};

var defaultProps = {
  type: '',
  heading: '',
  data: null,
  carouselCard: '' //TODO: Add default carousel card
};

var SwiperContainer = function (_React$Component) {
  _inherits(SwiperContainer, _React$Component);

  function SwiperContainer(props) {
    _classCallCheck(this, SwiperContainer);

    var _this = _possibleConstructorReturn(this, (SwiperContainer.__proto__ || Object.getPrototypeOf(SwiperContainer)).call(this, props));

    _this.handleCardClick = function (item, e) {
      if (item.modelId > 0) {
        _this.props.onCarouselCardClick(item, e);
      }
    };

    _this.getCarouselList = function () {
      var _this$props = _this.props,
          type = _this$props.type,
          data = _this$props.data,
          carouselCard = _this$props.carouselCard;


      var CarouselCard = carouselCard;

      var list = data.data.map(function (item, index) {
        return _react2.default.createElement(
          'li',
          { onClick: _this.handleCardClick.bind(_this, item), className: 'carousel__slide', key: index },
          _react2.default.createElement(CarouselCard, { item: item })
        );
      });

      return _react2.default.createElement(
        'ul',
        { className: "carousel__wrapper " + type },
        list
      );
    };

    return _this;
  }

  _createClass(SwiperContainer, [{
    key: 'render',
    value: function render() {
      if (this.props.data.IsFetching) {
        return _react2.default.createElement(
          'div',
          { className: 'carousel__container section-bottom-margin' },
          _react2.default.createElement(
            'div',
            { className: 'carousel__title' },
            _react2.default.createElement(
              'h2',
              { className: 'carousel-title__label' },
              this.props.heading
            )
          ),
          _react2.default.createElement(_SpinnerRelative2.default, null)
        );
      }
      return _react2.default.createElement(
        'div',
        { className: 'carousel__container section-bottom-margin' },
        _react2.default.createElement(
          'div',
          { className: 'carousel__title' },
          _react2.default.createElement(
            'h2',
            { className: 'carousel-title__label' },
            this.props.heading
          )
        ),
        this.getCarouselList()
      );
    }
  }]);

  return SwiperContainer;
}(_react2.default.Component);

SwiperContainer.propTypes = propTypes;
SwiperContainer.defaultProps = defaultProps;

exports.default = SwiperContainer;

/***/ }),
/* 232 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

var _reactLazyLoad = __webpack_require__(6);

var _reactLazyLoad2 = _interopRequireDefault(_reactLazyLoad);

var _reactRouterDom = __webpack_require__(2);

var _formatAmount = __webpack_require__(15);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var SwiperSimilarBikesEMI = function (_React$Component) {
  _inherits(SwiperSimilarBikesEMI, _React$Component);

  function SwiperSimilarBikesEMI(props) {
    _classCallCheck(this, SwiperSimilarBikesEMI);

    return _possibleConstructorReturn(this, (SwiperSimilarBikesEMI.__proto__ || Object.getPrototypeOf(SwiperSimilarBikesEMI)).call(this, props));
  }

  _createClass(SwiperSimilarBikesEMI, [{
    key: 'render',
    value: function render() {
      var item = this.props.item;


      return _react2.default.createElement(
        'div',
        { className: 'carousel-slide__card' },
        _react2.default.createElement(
          'div',
          { className: 'similar-emi-card__target' },
          _react2.default.createElement(
            'div',
            { className: 'similar-emi-card__image' },
            _react2.default.createElement(
              _reactLazyLoad2.default,
              null,
              _react2.default.createElement('img', { src: item.modelImage, alt: item.makeName + ' ' + item.modelName })
            )
          ),
          _react2.default.createElement(
            'div',
            { className: 'similar-emi-card__detail' },
            _react2.default.createElement(
              'h3',
              { className: 'carousel-card-detail__title' },
              item.makeName + ' ' + item.modelName
            ),
            _react2.default.createElement(
              'p',
              { className: 'carousel-card-detail__subtitle' },
              item.onRoadPriceLabel
            ),
            _react2.default.createElement(
              'p',
              { className: 'carousel-card-detail__amount' },
              '\u20B9 ',
              _react2.default.createElement(
                'span',
                { className: 'carousel-text--semibold' },
                (0, _formatAmount.formatToINR)(item.onRoadPriceAmount, false)
              )
            )
          )
        ),
        _react2.default.createElement(
          'div',
          { className: 'similar-emi-card__footer' },
          _react2.default.createElement(
            'p',
            { className: 'similar-emi-card-footer__title' },
            _react2.default.createElement(
              'span',
              { className: 'carousel-text--semibold' },
              item.emiLabel,
              ':\xA0'
            ),
            '\u20B9\xA0',
            _react2.default.createElement(
              'span',
              { className: 'carousel-text--bold' },
              (0, _formatAmount.formatToINR)(item.emiStart, false)
            ),
            '\xA0Onwards'
          )
        )
      );
    }
  }]);

  return SwiperSimilarBikesEMI;
}(_react2.default.Component);

exports.default = SwiperSimilarBikesEMI;

/***/ }),
/* 233 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var Documentation = function Documentation() {
  return _react2.default.createElement(
    "div",
    { className: "finance-documentation-container" },
    _react2.default.createElement(
      "div",
      { className: "finance-documentation__head" },
      _react2.default.createElement(
        "h2",
        { className: "finance-documentation__title" },
        "Documentation for Loans"
      )
    ),
    _react2.default.createElement(
      "ul",
      { className: "finance-documentation__list" },
      _react2.default.createElement(
        "li",
        { className: "documentation-list__item photo-identity" },
        _react2.default.createElement(
          "div",
          { className: "documentation-list-item__head documentation-item__photo-identity" },
          _react2.default.createElement("span", { className: "documentation-list-head__icon documentation-icon__photo" }),
          _react2.default.createElement(
            "span",
            { className: "documentation-list-head__title" },
            "Photo Identity"
          )
        ),
        _react2.default.createElement(
          "div",
          { className: "documentation-list-item__body" },
          _react2.default.createElement(
            "p",
            null,
            "Passport-size photographs, at least 2 to 6 are recommended to be kept handy for your loan application."
          )
        )
      ),
      _react2.default.createElement(
        "li",
        { className: "documentation-list__item identity-proof" },
        _react2.default.createElement(
          "div",
          { className: "documentation-list-item__head" },
          _react2.default.createElement("span", { className: "documentation-list-head__icon documentation-icon__identity" }),
          _react2.default.createElement(
            "span",
            { className: "documentation-list-head__title" },
            "Identity Proof"
          )
        ),
        _react2.default.createElement(
          "div",
          { className: "documentation-list-item__body" },
          _react2.default.createElement(
            "span",
            { className: "documentation-item__list-head" },
            "Any one of the following:"
          ),
          _react2.default.createElement(
            "ul",
            { className: "documentation-item__list" },
            _react2.default.createElement(
              "li",
              null,
              "Passport"
            ),
            _react2.default.createElement(
              "li",
              null,
              "Electoral Voter Identity card"
            ),
            _react2.default.createElement(
              "li",
              null,
              "Driving License"
            ),
            _react2.default.createElement(
              "li",
              null,
              "PAN Card"
            ),
            _react2.default.createElement(
              "li",
              null,
              "Aadhar Card"
            )
          )
        )
      ),
      _react2.default.createElement(
        "li",
        { className: "documentation-list__item address-proof" },
        _react2.default.createElement(
          "div",
          { className: "documentation-list-item__head identity-proof" },
          _react2.default.createElement("span", { className: "documentation-list-head__icon documentation-icon__address" }),
          _react2.default.createElement(
            "span",
            { className: "documentation-list-head__title" },
            "Address Proof"
          )
        ),
        _react2.default.createElement(
          "div",
          { className: "documentation-list-item__body" },
          _react2.default.createElement(
            "span",
            { className: "documentation-item__list-head" },
            "Permanent address proof: "
          ),
          _react2.default.createElement(
            "ul",
            { className: "documentation-item__list" },
            _react2.default.createElement(
              "li",
              null,
              "PAN card"
            ),
            _react2.default.createElement(
              "li",
              null,
              "Driving License "
            )
          ),
          _react2.default.createElement(
            "span",
            { className: "documentation-item__list-head list-head--padding-top" },
            "Temporary address proof: "
          ),
          _react2.default.createElement(
            "ul",
            { className: "documentation-item__list" },
            _react2.default.createElement(
              "li",
              null,
              "Bills against your name"
            )
          )
        )
      ),
      _react2.default.createElement(
        "li",
        { className: "documentation-list__item employement-proof" },
        _react2.default.createElement(
          "div",
          { className: "documentation-list-item__head identity-proof" },
          _react2.default.createElement("span", { className: "documentation-list-head__icon documentation-icon__employment" }),
          _react2.default.createElement(
            "span",
            { className: "documentation-list-head__title" },
            "Employment Proof"
          )
        ),
        _react2.default.createElement(
          "div",
          { className: "documentation-list-item__body" },
          _react2.default.createElement(
            "span",
            { className: "documentation-item__list-head" },
            "For salaried employees:"
          ),
          _react2.default.createElement(
            "ul",
            { className: "documentation-item__list" },
            _react2.default.createElement(
              "li",
              null,
              "Employment or offer letter"
            ),
            _react2.default.createElement(
              "li",
              null,
              "Bank statements"
            ),
            _react2.default.createElement(
              "li",
              null,
              "Salary slips"
            )
          ),
          _react2.default.createElement(
            "span",
            { className: "documentation-item__list-head list-head--padding-top" },
            "For self-employed individuals:"
          ),
          _react2.default.createElement(
            "ul",
            { className: "documentation-item__list" },
            _react2.default.createElement(
              "li",
              null,
              "Latest Form 16"
            ),
            _react2.default.createElement(
              "li",
              null,
              "Bank statements"
            ),
            _react2.default.createElement(
              "li",
              null,
              "Proof of their source of income"
            )
          )
        )
      )
    )
  );
};

module.exports = Documentation;

/***/ }),
/* 234 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

var _Accordion = __webpack_require__(79);

var _Accordion2 = _interopRequireDefault(_Accordion);

var _collapsible = __webpack_require__(235);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var FAQ = function FAQ() {
  var handleAccordionClick = function handleAccordionClick(event) {
    (0, _collapsible.focusCollapsible)(event);
  };

  var FAQList = [_react2.default.createElement(
    'div',
    { 'data-trigger': '1. What is a bike loan?', 'data-onOpen': handleAccordionClick },
    _react2.default.createElement(
      'div',
      { className: 'faq-list-item__content' },
      'A two-wheeler loan, more commonly known as bike loan, is like a secured personal loan which comes in very handy when you don\u2019t want to invest your savings in buying your dream two-wheeler. It eases your financial burden and also makes the entire bike purchase procedure all the more convenient, with simple loan terms, repayment options and ease of availability. These loans are mostly offered in collaboration with the dealerships which makes it possible for the buyer to make the purchase of two-wheeler and get the loan disbursed from the same place.'
    )
  ), _react2.default.createElement(
    'div',
    { 'data-trigger': '2. What are the things that I must know about a two-wheeler loan?', 'data-onOpen': handleAccordionClick },
    _react2.default.createElement(
      'div',
      { className: 'faq-list-item__content' },
      'Below are the various key features of a bike loan which you must know:',
      _react2.default.createElement('br', null),
      _react2.default.createElement(
        'ul',
        { className: 'faq-content__list' },
        _react2.default.createElement(
          'li',
          null,
          'Bike loans are provided by both banks and Non-Banking Financial Companies (NBFCs) in collaboration with the dealerships. The buyer can negotiate the loan terms with the dealer which makes the loan rates more competitive.'
        ),
        _react2.default.createElement(
          'li',
          null,
          'There is an eligibility criteria set for these loans, covering aspects such as age limit, profession, income levels, etc.'
        ),
        _react2.default.createElement(
          'li',
          null,
          'The loan repayment is done over a pre-defined tenure through Equated Monthly Instalments (EMIs). EMIs constitute a part of both the principal and the payable interest rate and are uniform throughout the duration of the repayment period.'
        ),
        _react2.default.createElement(
          'li',
          null,
          'You get a lot of flexibility when it comes to choosing the period of repayment for a two-wheeler loan, which ranges from 12 months to 48 months.'
        ),
        _react2.default.createElement(
          'li',
          null,
          'In most of the cases, no penalty charges are imposed in case the loan is paid before the loan term ends. In addition, this facility of prepayment is offered at zero cost.'
        ),
        _react2.default.createElement(
          'li',
          null,
          'The current two-wheeler loans not only cover the cost of the vehicle but also the costs of insurance, registration and even the accessories.'
        )
      )
    )
  ), _react2.default.createElement(
    'div',
    { 'data-trigger': '3. Why should I opt for a two-wheeler loan?', 'data-onOpen': handleAccordionClick },
    _react2.default.createElement(
      'div',
      { className: 'faq-list-item__content' },
      'With prices for bikes ranging from somewhere around Rs. 40,000 to as high as a few lakhs of rupees, a bike loan comes in very handy when you don\u2019t wish to invest a large part of your savings buying the bike. Moreover, in recent times, a two-wheeler loan has become very convenient to procure w.r.t terms and conditions, flexibility and affordability.'
    )
  ), _react2.default.createElement(
    'div',
    { 'data-trigger': '4. What are the eligibility requirements for a bike loan?', 'data-onOpen': handleAccordionClick },
    _react2.default.createElement(
      'div',
      { className: 'faq-list-item__content' },
      'Most banks and NBFCs follow below mentioned criteria set for procuring a two-wheeler loan:',
      _react2.default.createElement('br', null),
      _react2.default.createElement(
        'ul',
        { className: 'faq-content__list' },
        _react2.default.createElement(
          'li',
          null,
          'The minimum age for applying for a bike loan is in the range of 18-21 years.'
        ),
        _react2.default.createElement(
          'li',
          null,
          'A minimum income level is set for the loan applicants, whether they are salaried employees or self-employed.'
        ),
        _react2.default.createElement(
          'li',
          null,
          'The person should be a resident citizen of India in most of the cases.'
        ),
        _react2.default.createElement(
          'li',
          null,
          'Employment stability is required for a person to be eligible for a two-wheeler loan.'
        ),
        _react2.default.createElement(
          'li',
          null,
          'If the person has availed any kind of financing in the past, his/her credit score is required for assessing their capacity of repayment of the loan.'
        )
      )
    )
  ), _react2.default.createElement(
    'div',
    { 'data-trigger': '5. What is the minimum down payment expected for availing the loan?', 'data-onOpen': handleAccordionClick },
    _react2.default.createElement(
      'div',
      { className: 'faq-list-item__content' },
      'Banks and NBFCs provide up to 90% of the actual value of the bike and retain the remaining up to 10% amount as \u201Cmargin\u201D which is required to be paid by the loan applicant. This margin is also known as "down-payment" in common terms. The minimum down payment would vary depending upon your credit profile. With a lower credit score, you would be expected to pay a higher down payment.'
    )
  ), _react2.default.createElement(
    'div',
    { 'data-trigger': '6. What are the types of two-wheeler loans available to me?', 'data-onOpen': handleAccordionClick },
    _react2.default.createElement(
      'div',
      { className: 'faq-list-item__content' },
      'Based on the security of the loan, there are currently two types of two-wheeler loans available in the market:',
      _react2.default.createElement('br', null),
      _react2.default.createElement(
        'ul',
        { className: 'faq-content__list' },
        _react2.default.createElement(
          'li',
          null,
          _react2.default.createElement(
            'b',
            null,
            'Secured Two-wheeler loans -'
          ),
          ' These are the loans that are procured against some collateral or security which is generally an asset owned by the buyer and has equivalent value as that of the loan amount. These loans are easily available due to their security and their interest rates are also lower.'
        ),
        _react2.default.createElement(
          'li',
          null,
          _react2.default.createElement(
            'b',
            null,
            'Unsecured Two-wheeler loans -'
          ),
          ' These loans do not require any collateral and are ideal for buyers who do not have any asset to pledge against the loan. However, they have comparatively higher rates of interest.'
        )
      )
    )
  ), _react2.default.createElement(
    'div',
    { 'data-trigger': '7. How can I avail two-wheeler loan?', 'data-onOpen': handleAccordionClick },
    _react2.default.createElement(
      'div',
      { className: 'faq-list-item__content' },
      'Today, there are broadly two institutions from where you can avail a two-wheeler loan:',
      _react2.default.createElement('br', null),
      _react2.default.createElement(
        'ul',
        { className: 'faq-content__list' },
        _react2.default.createElement(
          'li',
          null,
          _react2.default.createElement(
            'b',
            null,
            'Banks -'
          ),
          ' From state-owned banks to private to commercial banks, all offer two-wheeler loan whether you have an account with them or not, provided you satisfy the requirements set out by them.'
        ),
        _react2.default.createElement(
          'li',
          null,
          _react2.default.createElement(
            'b',
            null,
            'Non-Banking Financial Companies (NBFCs) -'
          ),
          ' These financial institutions do not have a banking licence but offer banking services viz. loans and credit facilities. They also offer two-wheeler loans.'
        )
      ),
      'When it comes to the mode of application, you have two options:',
      _react2.default.createElement('br', null),
      _react2.default.createElement(
        'ul',
        { className: 'faq-content__list' },
        _react2.default.createElement(
          'li',
          null,
          _react2.default.createElement(
            'b',
            null,
            'Offline -'
          ),
          ' For applying loan offline, you need to go to the premises of the bank or the financial institution and complete all the formalities related to the loan. Some institutions even provide doorstep services wherein they would send across their representatives who would carry out the entire procedure at the comfort of your home.'
        ),
        _react2.default.createElement(
          'li',
          null,
          _react2.default.createElement(
            'b',
            null,
            'Online -'
          ),
          ' You can also apply for loan online where you get to compare between various loan options, their terms and estimated costs. Apart from being faster and simpler as compared to the offline route, the online mode has a dynamic tool known as EMI calculator which lets you vary your loan amount and other terms and accordingly calculate the EMI required to be paid.'
        )
      )
    )
  ), _react2.default.createElement(
    'div',
    { 'data-trigger': '8. What benefits do I get if I avail a two-wheeler loan?', 'data-onOpen': handleAccordionClick },
    _react2.default.createElement(
      'div',
      { className: 'faq-list-item__content' },
      'The various advantages of a two-wheeler loan are:',
      _react2.default.createElement('br', null),
      _react2.default.createElement(
        'ul',
        { className: 'faq-content__list' },
        _react2.default.createElement(
          'li',
          null,
          'The down payment required to be paid in this case is as low as 10% (up to 90% is financed through banks, depending on your credit profile). Also, you have to pay a relatively lower interest on a two-wheeler loan when compared with other means of finance available in the market, like personal loans. With more and more players coming into the market, the rates have become highly competitive.'
        ),
        _react2.default.createElement(
          'li',
          null,
          'The repayment of two-wheeler loans has never been more hassle-free than it is now. You can do the down-payment though multiple options such as cash, ECS or post-dated cheques.'
        ),
        _react2.default.createElement(
          'li',
          null,
          'Today, many loan providers have tied up with two-wheeler dealerships which make it very easy for you as a customer to get the loan sanctioned. Most of the times, the loan is disbursed instantly at the time of purchase.'
        ),
        _react2.default.createElement(
          'li',
          null,
          'The income criteria for a two-wheeler loan are kept very minimal which allows buyers from lower income categories to avail the loan.'
        ),
        _react2.default.createElement(
          'li',
          null,
          'In case the repayment of loan is done before the tenure ends, no prepayment charges are levied by the banks.'
        ),
        _react2.default.createElement(
          'li',
          null,
          'Many banks offer two-wheeler loans in combination with insurance packages due to which you do not need to buy an insurance cover separately for your two-wheeler. They also cover the costs of registration and even the accessories which makes them quite comprehensive.'
        )
      )
    )
  ), _react2.default.createElement(
    'div',
    { 'data-trigger': '9. What are the documents required in the entire process?', 'data-onOpen': handleAccordionClick },
    _react2.default.createElement(
      'div',
      { className: 'faq-list-item__content' },
      'You would be required to produce the following documents at the time of loan application:',
      _react2.default.createElement('br', null),
      _react2.default.createElement(
        'ul',
        { className: 'faq-content__list' },
        _react2.default.createElement(
          'li',
          null,
          _react2.default.createElement(
            'b',
            null,
            'ID Proof -'
          ),
          ' Driving Licence, Passport, Aadhar Card, PAN Card and Voter',
          "'",
          's ID Card are the options available in this category.'
        ),
        _react2.default.createElement(
          'li',
          null,
          _react2.default.createElement(
            'b',
            null,
            'Address Proof -'
          ),
          ' Two kinds of residential proofs have to be submitted, one is the proof of current address and the other is the proof of permanent address.'
        ),
        _react2.default.createElement(
          'li',
          null,
          _react2.default.createElement(
            'b',
            null,
            'Proof of employment -'
          ),
          ' Salary payslip, Offer Letter/Employment Letter, Form 16, Bank Account Statements etc. are some of the proofs required for salaried individuals. For self-employed individuals, proof of business identity, ITR filed by the business, Bank Account Statements of the business account etc. are some of the documents that would be required.'
        ),
        _react2.default.createElement(
          'li',
          null,
          _react2.default.createElement(
            'b',
            null,
            'Recent coloured passport-sized photographs'
          ),
          ' are also required.'
        )
      )
    )
  ), _react2.default.createElement(
    'div',
    { 'data-trigger': '10. What are the various components of the charges incorporated in a two-wheeler loan?', 'data-onOpen': handleAccordionClick },
    _react2.default.createElement(
      'div',
      { className: 'faq-list-item__content' },
      'The interest rate chargeable on the loan amount for two-wheeler loans ranges from 10.99% to 18.25%. Remaining associated charges and their structure is explained below:',
      _react2.default.createElement('br', null),
      _react2.default.createElement(
        'ul',
        { className: 'faq-content__list' },
        _react2.default.createElement(
          'li',
          null,
          _react2.default.createElement(
            'b',
            null,
            'Processing Charges -'
          ),
          ' A fixed percent of the loan amount is charged as processing fee at the time of loan application and this charge is levied irrespective of loan disbursement. Hence this amount would have to be paid by the loan applicant even when the loan does not get sanctioned.'
        ),
        _react2.default.createElement(
          'li',
          null,
          _react2.default.createElement(
            'b',
            null,
            'Foreclosure / Prepayment Charges -'
          ),
          ' This amount is charged when the loan is cleared ahead of the due date. Most of the current two-wheeler loans do not levy any foreclosure fees.'
        ),
        _react2.default.createElement(
          'li',
          null,
          _react2.default.createElement(
            'b',
            null,
            'Late Payment Charges -'
          ),
          ' This is a penalty imposed when instalment due date is missed and a late payment is done by the borrower.'
        ),
        _react2.default.createElement(
          'li',
          null,
          _react2.default.createElement(
            'b',
            null,
            'Other Charges -'
          ),
          ' These charges vary from lender to lender and include administrative charges, charges for issuing the credit report, charges for issuing a copy of the loan statement, etc.'
        )
      )
    )
  )];

  return _react2.default.createElement(
    'div',
    null,
    _react2.default.createElement(
      'div',
      { className: 'finance-faq__head' },
      _react2.default.createElement('div', { className: 'finance-faq__image' })
    ),
    _react2.default.createElement(
      'div',
      { className: 'finance-faq__body' },
      _react2.default.createElement(
        'h2',
        { className: 'finance-faq__heading' },
        'Frequently Asked Questions'
      ),
      _react2.default.createElement(
        'div',
        { className: 'finance-faq__list' },
        _react2.default.createElement(_Accordion2.default, {
          closeable: true,
          transitionCloseTime: 1,
          items: FAQList
        })
      )
    )
  );
};

exports.default = FAQ;

/***/ }),
/* 235 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var _closestPolyfill = __webpack_require__(38);

var _closestPolyfill2 = _interopRequireDefault(_closestPolyfill);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function focusCollapsible(event) {
  var eventRect = event.currentTarget.getBoundingClientRect();
  var windowScrollTop = window.pageYOffset || document.documentElement.scrollTop;
  var collapsibleTriggerHeight = event.currentTarget.closest('.collapsible').querySelector('.collapsible__trigger').offsetHeight;

  var floatingContentHeight = 0;
  var floatingContent = document.querySelector('.tabs--fixed .tabs__content');
  if (typeof floatingContent !== 'undefined') {
    floatingContentHeight = floatingContent.offsetHeight;
  }

  var positionY = windowScrollTop + eventRect.top - (collapsibleTriggerHeight + floatingContentHeight);

  window.scrollTo(0, positionY);
}

module.exports = {
  focusCollapsible: focusCollapsible
};

/***/ }),
/* 236 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


Object.defineProperty(exports, "__esModule", {
  value: true
});

var _react = __webpack_require__(0);

var _react2 = _interopRequireDefault(_react);

var _reactExpandCollapse = __webpack_require__(66);

var _reactExpandCollapse2 = _interopRequireDefault(_reactExpandCollapse);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var Disclaimer = function Disclaimer() {
  return _react2.default.createElement(
    'div',
    { className: 'disclaimer-content' },
    _react2.default.createElement(
      _reactExpandCollapse2.default,
      { previewHeight: '68px', expandText: 'Read more' },
      _react2.default.createElement(
        'b',
        null,
        'Disclaimer'
      ),
      ': The calculation performed by EMI Calculator is based on the information you entered and is for illustrative purposes only. This calculation reflects amounts in Indian Rupee rounded. The calculation performed by EMI Calculator is based on the information you entered and is for illustrative purposes only. This calculation reflects amounts in Indian Rupee rounded. The calculation performed by EMI Calculator is based on the information you entered and is for illustrative purposes only. This calculation reflects amounts in Indian Rupee rounded.'
    )
  );
};

exports.default = Disclaimer;

/***/ })
/******/ ]);