@model Bikewale.Models.PriceInCityPageVM
@{
    var gaBikeName = String.Format("{0}_{1}_{2}", Model.Make.MakeName, Model.BikeModel.ModelName, Model.CityEntity.CityName);
}

<ul class="overall-specs-tabs-wrapper">
    @if (Model.FirstVersion != null)
    {
        <li data-tabs="#orpContent" class="bw-price-in-city-ga">On-road price</li>
    }
    @if (Model.HasDealers || Model.HasServiceCenters)
    {
        <li data-tabs="#dealerContent" class="bw-price-in-city-ga">Dealers</li>
    }
    @if (Model.IsNew)
    {
        <li data-tabs="#emiContent" class="bw-price-in-city-ga">EMI calculator</li>
    }
    @if (Model.IsNew && (Model.BikeVersionPrices != null || Model.HasNearestPriceCities))
    {
        <li data-tabs="#versionContent" class="bw-price-in-city-ga">Price list</li>
    }
    @if (Model.HasAlternateBikes)
    {
        <li data-tabs="#similarContent" class="bw-price-in-city-ga">Compare @Model.BodyStyleText</li>
    }
</ul>