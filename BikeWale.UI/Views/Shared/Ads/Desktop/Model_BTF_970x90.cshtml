@{
    var AdId = Model.GetType().GetProperty("AdId").GetValue(Model, null);
}
<div id='div-gpt-ad-@string.Format("{0}-12", AdId)' class="margin-bottom15" >
    <script type='text/javascript'>
    googletag.cmd.push(function () { googletag.display('div-gpt-ad-@string.Format("{0}-12", AdId)'); });
    </script>
</div>
