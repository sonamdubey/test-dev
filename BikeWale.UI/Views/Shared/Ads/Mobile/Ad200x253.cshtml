@{
    var AdId = Model.GetType().GetProperty("AdId").GetValue(Model, null);
}
<div class="sponsored-card" id='div-gpt-ad-@string.Format("{0}-11", AdId)'>
    <script>
        googletag.cmd.push(function () { googletag.display('div-gpt-ad-@string.Format("{0}-11", AdId)'); });
    </script>
</div>
