@model Bikewale.Models.NewsDetailPageVM
@using Bikewale.Utility
@{
    Model.Page_ATF_CSS = "<link rel='stylesheet' type='text/css' href='/css/content/details.css' />";    
    Model.Page_JS_Path = "/src/jquery.floating-social-share.min.js";
    Model.AdTags.AdId = "1395995626568";
    Model.AdTags.AdPath = "/1017752/BikeWale_News_";
    Model.AdTags.Ad_300x250 = true;
    Model.AdTags.Ad_970x90 = true;
    Model.AdTags.Ad_970x90Bottom = true;
    Model.IsHeaderFix = true;
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<section class="container padding-top10">
    <div class="grid-12">
        <div class="breadcrumb margin-bottom15">
            <ul>
                <li itemscope="" itemtype="http://data-vocabulary.org/Breadcrumb">
                    <a href="/" itemprop="url">
                        <span itemprop="title">Home</span>
                    </a>
                </li>
                <li itemscope="" itemtype="http://data-vocabulary.org/Breadcrumb">
                    <span class="bwsprite fa-angle-right margin-right10"></span>
                    <a href="/news/" itemprop="url">
                        <span itemprop="title">Bike News</span>
                    </a>
                </li>
                @if (Model.ArticleDetails != null)
                {
                    <li><span class="bwsprite fa-angle-right margin-right10"></span>@Model.ArticleDetails.Title</li>
                }
            </ul>
            <div class="clear"></div>
        </div>
    </div>
    <div class="clear"></div>
</section>

<section>
    <div class="container margin-bottom20">
        <div class="grid-12">
            <div id="content" class="grid-8 alpha">
                <div class="bg-white">
                    @if (Model.ArticleDetails != null)
                    {
                        <div class="section-header">
                            <h1 class="margin-bottom5">@Model.ArticleDetails.Title</h1>
                            <div>
                                <span class="bwsprite calender-grey-sm-icon"></span>
                                <span class="article-stats-content margin-right20">@FormatDate.GetFormatDate(Convert.ToString(Model.ArticleDetails.DisplayDate), "MMMM dd, yyyy hh:mm tt")</span>
                                <span class="bwsprite author-grey-sm-icon"></span>
                                <span class="article-stats-content">@Model.ArticleDetails.AuthorName</span>
                            </div>
                        </div>
                        <div class="section-inner-padding">
                            <div id="post-@Model.ArticleDetails.BasicId">
                                <div class="article-content padding-top5">
                                    @if (!string.IsNullOrEmpty(Model.PageMetaTags.ShareImage))
                                    {
                                        <div style="text-align: center;">
                                            <img alt='@Model.ArticleDetails.Title' title='@Model.ArticleDetails.Title' src='@Model.PageMetaTags.ShareImage'>
                                        </div>
                                    }
                                    @Html.Raw(Model.ArticleDetails.Content)
                                    <div class="clear"></div>
                                </div>
                            </div>
                             @if (Model.BikeInfo != null)
                            {
                                @Html.Partial("~/Views/BikeModels/_MinBikeInfoCard.cshtml", Model.BikeInfo)
                            }
                            <div class="border-solid-top padding-top10">
                                @if (!string.IsNullOrEmpty(Model.ArticleDetails.PrevArticle.Title) && Model.PageMetaTags != null)
                                {
                                    <div class="grid-6 alpha border-solid-right">
                                        <a href="@Model.PageMetaTags.PreviousPageUrl" title="@Model.ArticleDetails.PrevArticle.Title" class="text-default next-prev-article-target">
                                            <span class="bwsprite prev-arrow"></span>
                                            <div class="next-prev-article-box inline-block padding-left5">
                                                <span class="font12 text-light">Previous</span><br />
                                                <span class="next-prev-article-title text-truncate">@Model.ArticleDetails.PrevArticle.Title</span>
                                            </div>
                                        </a>
                                    </div>
                                }
                                @if (!string.IsNullOrEmpty(Model.ArticleDetails.NextArticle.Title) && Model.PageMetaTags != null)
                                {
                                    <div class="grid-6 omega rightfloat">
                                        <a href="@Model.PageMetaTags.NextPageUrl" title="@Model.ArticleDetails.NextArticle.Title" class="text-default next-prev-article-target">
                                            <div class="next-prev-article-box inline-block padding-right5">
                                                <span class="font12 text-light">Next</span>
                                                <span class="next-prev-article-title text-truncate">@Model.ArticleDetails.NextArticle.Title</span>
                                            </div>
                                            <span class="bwsprite next-arrow"></span>
                                        </a>
                                    </div>
                                }
                                <div class="clear"></div>
                            </div>
                        </div>
                    }
                </div>
            </div>
            <div class="grid-4 omega">
                @if (Model.MostPopularBikes != null && Model.MostPopularBikes.Bikes != null && Model.MostPopularBikes.Bikes.Count() > 0)
                {
                    <div class="content-box-shadow padding-15-20-10 margin-bottom20">
                        <h2>@Model.MostPopularBikes.WidgetHeading</h2>
                        @Html.Partial("~/Views/BikeModels/_MostPopularBikesSideBar.cshtml", Model.MostPopularBikes)

                        <div class="view-all-btn-container padding-top10 padding-bottom10">
                            <a href="@Model.MostPopularBikes.WidgetHref" title="@Model.MostPopularBikes.WidgetLinkTitle" class="btn view-all-target-btn">View all bikes<span class="bwsprite teal-right"></span></a>
                        </div>
                    </div>
                }
                <div class="margin-bottom20">
                    @Html.Partial("~/Views/Shared/Ads/Desktop/Ad300x250.cshtml", Model.AdTags)
                </div>
                @if (Model.Model != null && Model.Model.ModelId > 0)
                {
                    if (Model.PopularBodyStyle != null && Model.PopularBodyStyle.PopularBikes != null && Model.PopularBodyStyle.PopularBikes.Count() > 0)
                    {
                        <div class="content-box-shadow padding-15-20-10 margin-bottom20">
                            <h2>@Model.PopularBodyStyle.WidgetHeading</h2>
                            @Html.Partial("~/Views/BestBikes/_PopularBodyStyle_Vertical.cshtml", Model.PopularBodyStyle)
                            <div class="view-all-btn-container padding-top10 padding-bottom10">
                                <a href="@Model.PopularBodyStyle.WidgetHref" title="@Model.PopularBodyStyle.WidgetLinkTitle" class="btn view-all-target-btn">View all bikes<span class="bwsprite teal-right"></span></a>
                            </div>
                        </div>
                    }
                }
                else
                {
                    if (Model.UpcomingBikes != null && Model.UpcomingBikes.UpcomingBikes != null && Model.UpcomingBikes.UpcomingBikes.Count() > 0)
                    {
                        <div class="content-box-shadow padding-15-20-10 margin-bottom20">
                            <h2>@Model.UpcomingBikes.WidgetHeading</h2>
                            @Html.Partial("~/Views/Upcoming/_UpcomingBikes_Vertical.cshtml", Model.UpcomingBikes)
                            <div class="view-all-btn-container padding-top10 padding-bottom10">
                                <a href="@Model.UpcomingBikes.WidgetHref" title="@Model.UpcomingBikes.WidgetLinkTitle" class="btn view-all-target-btn">View all bikes<span class="bwsprite teal-right"></span></a>
                            </div>
                        </div>
                    }
                }                
                </div>
                <div class="clear"></div>
            </div>
        <div class="clear"></div>
    </div>
</section>
<link rel='stylesheet' type='text/css' href='/css/jquery.floating-social-share.min.css' />
<script type="text/javascript">
    docReady(function () {
        $("body").floatingSocialShare();
    });
</script>