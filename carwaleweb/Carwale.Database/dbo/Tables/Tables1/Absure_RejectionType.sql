CREATE TABLE [dbo].[Absure_RejectionType] (
    [Id]   INT          IDENTITY (1, 1) NOT NULL,
    [Type] VARCHAR (50) NOT NULL,
    CONSTRAINT [PK_Absure_RejectionType] PRIMARY KEY CLUSTERED ([Id] ASC)
);

