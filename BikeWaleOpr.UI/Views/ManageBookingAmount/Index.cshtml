@model BikewaleOpr.Entity.ManageBookingAmountData
@{
    ViewBag.Title = "Manage Booking Amount";
    ViewBag.PageTitle = "Manage Booking Amount";
}
@if(Model != null)
{
    <div class="row">
        <div class="col m9">
            @if(Model.BookingAmountList != null && Model.BookingAmountList.Count() > 0)
            {
                <table id="bookingAmountList" class="bordered highlight z-depth-3 responsive-table">
                    <tr class="center-align">
                        <th>Id</th>
                        <th>Make</th>
                        <th>Model</th>
                        <th>Version</th>
                        <th>Updated By</th>
                        <th>Updated On</th>
                        <th>Amount</th>
                        <th>Active</th>
                        <th>Edit</th>
                        <th>Delete</th>
                    </tr>
                    @foreach (var obj in Model.BookingAmountList)
                    {
                        <tr data-id="@obj.BookingAmountBase.Id" data-makeid="@obj.BikeMake.MakeId" data-modelid="@obj.BikeModel.ModelId" 
                                data-versionid="@obj.BikeVersion.VersionId" data-amount="@obj.BookingAmountBase.Amount">
                            <td>@obj.BookingAmountBase.Id</td>
                            <td>@obj.BikeMake.MakeName</td>
                            <td>@obj.BikeModel.ModelName</td>
                            <td>@obj.BikeVersion.VersionName</td>
                            <td>@obj.LastUpdatedBy</td>
                            <td>@obj.UpdatedOn.ToString("dd/MM/yyyy")</td>
                            <td>@obj.BookingAmountBase.Amount</td>
                            <td><i class="material-icons @(obj.BookingAmountBase.IsActive?"icon-green":"icon-red")">@(obj.BookingAmountBase.IsActive ? "done" : "close")</i></td>
                            <td><a href="javascript:void(0)" rel="nofollow" data-bind="event: {click: function(d, e) { editBooking(e) }}"><i class="material-icons icon-blue">mode_edit</i></a></td>
                            <td><a href="javascript:void(0)" rel="nofollow" data-bind="event: {click: function(d, e) { deleteBooking(e) }}"><i class="material-icons icon-red">delete</i></a></td>
                        </tr>
                    }
                </table>
            }
        </div>
        @using (Html.BeginForm("Add", "ManageBookingAmount", FormMethod.Post))
        {
            <input id="dealerId" name="DealerId" type="number" data-bind="textInput: dealerId" hidden/>
            <input id="bookingId" name="BookingId" type="number" data-bind="textInput: bookingId" hidden />
            <div class="col m3">
                <ul class="collapsible" data-collapsible="accordian">
                    <li class="active">
                        <div class="collapsible-header active">
                            <strong>
                                Add New Booking Amount
                            </strong>
                        </div>
                        <div class="collapsible-body">
                            <div class="container">
                                <div class="row">
                                    <div class="input-field col m12">
                                        <select id = "selectMake" name="MakeId" data-bind="value: selectedMakeId, event: {change: getModels} ">
                                            <option value="" disabled selected>Select Make</option>
                                            @if (Model.BikeMakeList != null && Model.BikeMakeList.Count() > 0)
                                            {
                                                foreach(var state in Model.BikeMakeList)
                                                {
                                                    <option value="@state.MakeId">@state.MakeName</option>
                                                }
                                            }
                                        </select>
                                        <label for="MakeId">Make</label>
                                    </div>
                                </div>
                                <div class="row">
                                    <div class="input-field col m12">
                                        <select id="selectModel" class="invalid" name="ModelId" data-bind="value: selectedModelId, event: {change: getVersions}, options: models, optionsText: 'modelName', optionsValue: 'modelId', optionsCaption: 'Select Model'">
                                            <option value="" disabled selected>Select Model</option>
                                        </select>
                                        <label for="ModelId">Model</label>
                                    </div>
                                </div>
                                <div class="row">
                                    <div class="input-field col m12">
                                        <select id="selectVersion" name="VersionId" data-bind="options: versions, optionsText: 'versionName', optionsValue: 'versionId', value: selectedVersionId, optionsCaption: 'Select Version'">
                                            <option value="" disabled selected>Select Version</option>
                                        </select>
                                        <label for="VersionId">Version</label>
                                    </div>
                                </div>
                                <div class="row">
                                    <div class="input-field col m12">
                                        <input id="bookingAmount" name="BookingAmount" type="number" class="validate" data-bind="textInput: bookingAmount, css:bookingMsg()!=''?'invalid':'' "/>
                                        <label for="BookingAmount" data-error="" data-bind="attr:{'data-error': bookingMsg}, css:bookingMsg()!=''?'':'active'">Enter Booking Amount</label>
                                    </div>
                                </div>
                                <div class="row">
                                    <div class="input-field col m6">
                                        <button id="btnSave" class="btn waves-effect waves-light" type="submit" name="save" data-bind="event : { click : function() { return validateBookingSubmit();}}">
                                            Save
                                        </button>
                                    </div>
                                    <div class="input-field col m6">
                                        <button id="cancelEdit" class="btn waves-effect waves-light hide" name="cancel"  type="button" data-bind="event : { click : cancelEdit}">
                                            Cancel
                                        </button>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </li>
                </ul>
            </div>
        }
    </div>
    <script defer src="~/src/manageBookingAmount.js" type="text/javascript"></script>
}

