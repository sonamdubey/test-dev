@model Bikewaleopr.ManufacturerCampaign.Entities.ManufacturerCampaignPopup
@{
    ViewBag.Title = "Configure Campaign Popup";
    ViewBag.PageTitle = "Configure Campaign Popup for Dealer";
}

@Html.Partial("~/Views/ManufacturerCampaign/_NavigationBreadcrumbs.cshtml", 3)

@if (Model != null)
{
    <form action="/manufacturercampaign/save/popup/" method="post">
        <div class="row">
            <div class="input-field col s3 offset-s4">
                <input id="txtPopupHeading" type="text" class="validate" maxlength="100" name="PopupHeading" value="@Model.PopupHeading">
                <label for="txtPopupHeading">Popup Heading</label>
            </div>
        </div>

        <div class="row">
            <div class="input-field col s3 offset-s4">
                <input id="txtPopupDescription" type="text" class="validate" maxlength="100" name="PopupDescription" value="@Model.PopupDescription">
                <label for="txtPopupDescription">Popup Description</label>
            </div>
        </div>

        <div class="row">
            <div class="input-field col s3 offset-s4">
                <input id="txtpopupMessage" type="text" class="validate" maxlength="100" name="PopupSuccessMessage" value="@Model.PopupSuccessMessage">
                <label for="txtpopupMessage">Popup Success Message</label>
            </div>
        </div>

        <div class="row">
            <div class="col offset-s4">
                <input type="checkbox" class="filled-in" id="chkEmail" name="EmailRequired" @(Model.EmailRequired>0?"checked value=true":"") />
                <label for="chkEmail">Email Required</label>
            </div>
        </div>

        <div class="row">
            <div class="col s2 offset-s4">
                <input type="checkbox" class="filled-in" id="chkPincode" name="PinCodeRequired" @(Model.PinCodeRequired>0 ? "checked value=true":"") />
                <label for="chkPincode">Pincode Required</label>
            </div>
            <div class="col">
                <a href="">Map Manufacturer Pincodes</a>
            </div>
        </div>

        <div class="row">
            <div class="col s2 offset-s4">
                <input type="checkbox" class="filled-in" id="chkDealer" name="DealerRequired" @(Model.DealerRequired >0? "checked value=true":"") />
                <label for="chkDealer">Dealer Required</label>
            </div>
            <div class="col">
                <a href="">Map Manufacturer Dealers</a>
            </div>
        </div>

        <div class="row">
            <div class="col offset-s4">
                <button id="btnConfigurePopup" class="btn waves-effect waves-light" type="submit" name="action">
                    Configure popup
                </button>
            </div>
        </div>
    </form>
}
